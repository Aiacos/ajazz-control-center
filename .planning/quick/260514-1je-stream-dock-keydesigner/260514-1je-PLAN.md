---
quick_id: 260514-1je
slug: stream-dock-keydesigner
date: 2026-05-14
mode: standard-with-research
description: Stream Dock features research (all families) + KeyDesigner slice for AKP153
---

# Quick Task 260514-1je: Stream Dock features research + KeyDesigner slice

## Shape

Two-step quick task:

1. **Research** (background agent, run_in_background=true) → produces `260514-1je-FINDINGS.md` with:

   - Per-device feature matrix for the 5 AJAZZ Stream Dock families (AKP153/AKP153E/AKP03/AKP05/0x3004).
   - Canonical Stream Deck feature taxonomy (per-key, multi-action, folders, profiles, brightness, encoders, touch strip, RGB, firmware).
   - Gap analysis vs current codebase (file:line citations).
   - Recommended **minimum coherent slice** for KeyDesigner targeting AKP153 (15 keys 5×3).
   - Decisions the implementer must make before coding (icon source, multi-action shape, profile persistence shape, etc).

1. **Implementation** (this session, after FINDINGS review) → KeyDesigner.qml buildout per slice, ~200-400 LoC QML + minor C++ wiring.

## Scope

- **In:** KeyDesigner.qml for AKP153 family (15-key grid, no encoder). Per-key tile with: action assignment, label text, icon upload, hover/selection states. Bound to the existing device model + profile system (whatever shape that takes — to be confirmed from FINDINGS section 3).
- **Out (defer to follow-up quick tasks):** EncoderPanel.qml buildout (AKP03/AKP05), multi-action chains, folders/pages, profiles auto-switch, animation icons, sound, AKP05 touch strip UI, 0x3004 promotion. RGB picker integration if time permits.

## Risks identified up front

- The C++ profile model may not exist yet in the shape KeyDesigner needs. If FINDINGS surfaces "no profile persistence layer," we have to decide: stub it, or scope the slice to "visual-only" with no save.
- The icon upload flow may need a Qt file dialog + image format normalization (jpeg 85×85 for AKP153). Could add weight to the slice.
- ProfileEditor.qml currently has a 4-tab structure (Keys/RGB/Encoders/Mouse). The new KeyDesigner integrates as the "Keys" tab; tab routing already exists.

## Tasks

### Task 1 · Research agent (background)

Spawned as `general-purpose` subagent at session start. Awaiting FINDINGS.md.

### Task 2 · Codebase orientation (parallel-safe — avoid agent's files)

While the agent works:

- Read `profile_controller.{hpp,cpp}` (NOT in the agent's read list) to understand persistence.
- Read `src/app/CMakeLists.txt` to know where to wire new QML.
- Note: do NOT read KeyDesigner.qml / EncoderPanel.qml / akp153.cpp / register.cpp — agent is using them.

### Task 3 · Review FINDINGS + design (after agent return)

- Read FINDINGS sections 4 (slice recommendation) and 5 (decisions to make).
- Confirm the slice shape with user if there are material decisions surfaced.

### Task 4 · Implement KeyDesigner.qml

- Build out per FINDINGS section 4.
- Aim: ~200-400 LoC QML, minimal C++ touchpoints.
- Wire into ProfileEditor's Keys tab.

### Task 5 · Build, qmllint, manual smoke test

- `cmake --build build/linux-debug --target ajazz-control-center`
- Launch app, select AKP153 in sidebar, navigate to Keys tab, verify the KeyDesigner renders + interactions work.

### Task 6 · Commit (3 atomic) + push

- `feat(ui): Stream Dock KeyDesigner slice for AKP153` (code)
- `docs(quick-260514-1je): Stream Dock research findings` (FINDINGS.md)
- `docs(quick-260514-1je): PLAN + SUMMARY + STATE row` (planning docs)

## Verification

- FINDINGS.md exists at the documented path and has all 6 sections.
- KeyDesigner.qml is functionally non-stub (≥ 200 LoC, not just a placeholder Text element).
- Build clean, qmllint clean, app launches without QML errors.
- ProfileEditor's Keys tab renders the new component when an AKP153 device is selected.

## Routing

Out of v1.1 milestone scope. Tracked as quick task per FINDINGS. Follow-up implementation slices (EncoderPanel, multi-action, profiles, RGB integration, 0x3004 promotion) will be spawned as separate quick tasks based on FINDINGS section 4 prioritization.
