# Session handoff — 2026-05-18

Snapshot of what landed today and the queued follow-ups. Supersedes
[`HANDOFF-2026-05-17.md`](HANDOFF-2026-05-17.md) which referenced a
local pile of un-pushed commits — all of those have landed, plus a
full CI hardening cycle, plus a 5-agent codebase audit with corrections.

## TL;DR

- **CI is fully green on `main`** after a 13-commit hardening cycle
  (test ASCII discipline, Qt6 WebSockets in every workflow, plugin-
  server segfault fix, mdformat plugin version pin, bulk auto-fixer
  output).
- **5-agent comprehensive audit** ran across C++, QML/UI, tests,
  build/CI, docs/plans. Result: 0 critical C++ bugs, 3 critical
  cross-cutting issues, 6 high, 8 medium, 10 nits.
- **All 3 CRITICAL findings fixed** in this session: profile load
  runtime crash (stub reader implemented), schema doc realigned to
  writer reality, AK980 PRO 4-packet clock-sync end-to-end test.
- **6/6 HIGH and 4/8 MEDIUM findings fixed**; remaining MEDIUMs are
  testing scope (Linux/macOS hotplug, BatteryService poll) that need
  hardware mocks or design work.
- App still runs (PID 19852 from the prior session) — file lock on
  the .exe means a re-build is deferred until user closes the window.

## What landed in this session

### CI hardening (13 commits)

| Hash      | Theme                                                                |
| --------- | -------------------------------------------------------------------- |
| `4a88cc7` | Qt 6.8 `flipped` -> `mirrored` compat + gitleaks false-positive      |
| `3b1ebaa` | gitleaks TOML parser fix (no nested \[[rules]\])                     |
| `55679e2` | Em-dash sweep in TEST_CASE titles                                    |
| `2448a78` | Second ASCII sweep (≠ and § were also mangled)                       |
| `f99b1e7` | Pre-commit hook to reject non-ASCII in TEST_CASE titles              |
| `7961c14` | typos: Vai vocabulary + 2 vendor-RE docs excluded                    |
| `78c2761` | mdformat sweep on the 19 new vendor-RE docs                          |
| `2a56d6a` | Regen README + wiki AUTOGEN for AKP05 secondary screen               |
| `8d1eb68` | Qt6 WebSockets module in CI / lint / nightly / release               |
| `57ae986` | Pre-commit ASCII hook: shellcheck-clean + skip comments              |
| `f33fca6` | typos extend-words backup whitelist `fo`                             |
| `f4e28e6` | mdformat bulk auto-fix on 6 pre-existing drifted docs                |
| `c1873aa` | SdPluginServer `connectedPluginCount()` semantics fix                |
| `9ec89c2` | SdPluginServer drain DeferredDelete in stop()                        |
| `45c60cf` | SdPluginServer tests: leak QCoreApplication (static-dtor fix)        |
| `d59ad97` | Pin `mdformat-*` plugin versions in pre-commit                       |
| `54c29c9` | Bulk auto-fixer output (clang-format / cmake-format / shfmt / typos) |

### Comprehensive audit (5 agents, parallel)

C++ codebase audit produced 0 critical, 0 high-risk, 0 medium issues
(only 2 deprecation-warning nits — macOS `kIOMasterPortDefault`,
`QImage::mirrored` in Qt 6.13 future). COD-031 zero violations,
QML_SINGLETON 11/11 correct, Win32 env-block size captured pre-move.

QML/UI audit found 1 high-risk (LoadedPluginsPage hardcoded Tailwind
colors), 1 medium (PropertyInspector Loader no error handler), 2 nits
(acceptable hardcoded contrast colors).

Test-suite audit found 1 critical gap (AK980 PRO 4-packet clock-sync
not tested end-to-end), 1 medium (Linux/macOS hotplug + BatteryService
poll uncovered). Plus several clean wins (ASCII discipline ✓, ctest
syntax ✓, hotplug-smoke gate ✓).

Build/CI audit found 0 critical, 6 high (missing mypy/pytest in
lint.yml, lychee no retry, etc.), 6 medium, 7 nits.

Docs/plans audit found 1 critical (schema drift), 0 missing standard
files (LICENSE, CHANGELOG, SECURITY, etc. all real), 100% SPDX
headers, terminology drift "stream deck" vs "Stream Dock", stale
CLAUDE.md test count (178 -> 286), Phase 3 patches P3.6/P3.8/P3.15
declared but never landed.

### Audit corrections (10 commits)

| Hash                 | Theme                                                                                                             |
| -------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `4a47f1a`            | **CRITICAL**: implement profileFromJson (was a stub that crashed on load)                                         |
| `f388080`            | **CRITICAL**: align PROFILE_SCHEMA.md with writer (pages, settings-as-string, Encoder split, Action.kind/delayMs) |
| `8fd6dcc`            | **CRITICAL**: test_ak980_clock_sync_e2e.cpp end-to-end 4-packet test                                              |
| `ea1db85`            | HIGH: theme-aware semantic chip tokens (light-mode contrast fix) + derived `fgFaint`                              |
| `e2be300`            | HIGH: CLAUDE.md test count refresh 178 -> ~286 with live-count rule                                               |
| `733d60b`            | HIGH: README + generator "Stream Dock" disambiguation from Elgato "Stream Deck"                                   |
| `c7dcacf`            | HIGH: lychee.toml with retry / timeout / skip list                                                                |
| `5dad2b1`            | HIGH: CI lint adds mypy strict + pytest gate for Python code                                                      |
| `fd63b6d`            | MEDIUM: 3-in-1 (Loader error log, TSan/MSVC FATAL, coverage WERROR comment)                                       |
| `2026-05-18 HANDOFF` | this file (incrementally updated)                                                                                 |

### Audit-2 corrections (8 commits, 2026-05-18 PM)

A second 5-agent audit pass focused on code quality / OOP / extensibility /
cross-platform parity / RE retrospective / clock-sync re-check.
Headline: clock-sync is **byte-for-byte correct**, OOP architecture is
**production-ready** (0 critical, 0 high in C++), extensibility was
**7/10** with two hidden touch points + autostart/notifications Linux-
only. All HIGH-priority findings fixed in one session:

| Hash          | Theme                                                                                             |
| ------------- | ------------------------------------------------------------------------------------------------- |
| `03c84d9`     | fix(python): silence 8 mypy strict errors surfaced by the new CI gate                             |
| `5eef884`     | fix(app): generate device-maturity map from YAML (close hidden touch point #1)                    |
| `432815b`     | feat(time-sync): periodic 15-min auto-sync timer for DST + RTC drift                              |
| `0046765`     | feat(app): wire connected-codenames enumerator into TimeSyncService                               |
| `9550ec7`     | docs(guides): expand ADDING_A_DEVICE.md with full step-by-step + auto-wired list                  |
| `4917a10`     | feat(autostart): macOS LaunchAgent + Windows HKCU Run backends (closes cross-platform parity gap) |
| `158d5cd`     | feat(notifications): native macOS + Windows back-ends (was Linux only)                            |
| (this update) | docs(vendor): clarify `year - 2000` vs `year % 2000` clock byte 3 encoding                        |

### Remaining hidden touch point + open work

- **Plugin catalog opt-in** (`src/app/src/plugin_catalog_model.cpp:454-559`):
  still has hand-edited per-plugin device lists. Lift into devices.yaml +
  generate at build time, OR teach plugins to declare supported capabilities
  instead of specific codenames. Tracked in TODO.md.
- **Macro recorder**: stub on all 3 OSes (CGEventTap on macOS,
  SetWindowsHookExW on Windows, evdev on Linux). Untouched by this audit.
- **Foreground app detection**: not implemented anywhere; needed for the
  application-hints auto-activation feature.
- **Phase 3 patches P3.6 / P3.8 / P3.15 / P3.17 / P3.18 / P3.19 / P3.20**:
  still pending per phase3-patch-sequence.md.
- **Apple Silicon CI builder**: `macos-14` is Intel; arm64 only validated
  via release-time universal-DMG build.
- **Hardware witnesses** (carried): AK980 PRO setTime TFT visual confirm,
  BatteryIndicator update on wireless, AKP05 ULEND desync.

## Queued / open follow-ups

- **Phase 3 patches not yet landed** (visible drift from
  phase3-patch-sequence.md):
  - P3.6 CMD_FINISH 0xF0 emission on setTime (deferred)
  - P3.8 WinUSB framing comment in akp05_protocol.hpp
  - P3.15 9 host-side lighting effects (AKP05)
  - P3.17 plugin-store catalogue model
  - P3.18 bundle 12 default plugins
  - P3.19 AK980 macros record + assign
  - P3.20 IProfileCapable + ProfileManager + ForegroundAppMonitor
- **Test gaps** (audit findings, not yet implemented):
  - Linux udev hotplug backend smoke test
  - macOS IOKit hotplug backend smoke test
  - BatteryService 15s QTimer poll integration test
- **CI matrix gap**: no Apple Silicon (arm64) builder; macos-14 is Intel.
  Release workflow builds universal but CI only tests Intel slice.
- **Hardware witnesses still pending** (carried from prior HANDOFF):
  - AK980 PRO setTime visual confirmation on TFT
  - BatteryIndicator update on wireless reconnect
  - AKP05 ULEND firmware desync validation
- **App PID 19852** is still running for user testing; close it before
  the next local rebuild (.exe file lock).

## Conventions reaffirmed

- All audit-correction commits used Conventional Commits.
- ASCII-only TEST_CASE rule now enforced by `scripts/check-test-names-ascii.sh`
  pre-commit hook (catches the class of bug that caused 3 consecutive
  Windows CI failures earlier in the day).
- mdformat plugin deps version-pinned to defeat CI-cache drift.
- Schema doc is the source of truth for JSON wire keys (PROFILE_SCHEMA.md
  now correctly documents `device` vs `deviceCodename`, `settings`
  string-not-object, etc.).
