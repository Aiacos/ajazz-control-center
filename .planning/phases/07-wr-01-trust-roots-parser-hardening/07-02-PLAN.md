---
phase: 07-wr-01-trust-roots-parser-hardening
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/unit/test_load_trust_roots.cpp
  - tests/unit/CMakeLists.txt
  - tests/unit/fixtures/trust_roots/README.md
autonomous: true
requirements: [TRUST-03]
must_haves:
  truths:
    - tests/unit/test_load_trust_roots.cpp covers the TRUST-03 corpus shape from CONTEXT.md D-03 — positive cases (BOM, escape sequences, nested structures, mixed key order, boundary entry counts 1/2/100/1024, empty list) and negative cases (oversize, 1025 entries, embedded NUL, truncated JSON, malformed UTF-8) — every case from D-03 has at least one SECTION.
    - Negative cases mechanically prove the 1 MB byte cap and 1024-entry cap are enforced — oversize input returns empty list (not throw, not crash) and 1025-entry input returns empty list (matches the fail-closed contract from Plan 07-01).
    - The new test binary runs in well under 1 second on Linux (TRUST-03 sub-1s budget reserved for the fuzz corpus, but unit corpus stays fast — the 1024-entry boundary case is the slowest synthetic input and still completes <100ms on a stock dev machine).
    - All test inputs are constructed in std::filesystem::temp_directory_path() at test time (no checked-in binary fixtures, no risk of trust-root JSON files being mistaken for real config).
  artifacts:
    - path: tests/unit/test_load_trust_roots.cpp
      provides: TRUST-03 corpus — positive + negative + boundary cases for loadTrustRoots
      contains: TEST_CASE
    - path: tests/unit/CMakeLists.txt
      provides: test_load_trust_roots.cpp added to the ajazz_unit_tests source list
      contains: test_load_trust_roots
    - path: tests/unit/fixtures/trust_roots/README.md
      provides: Documents that trust-root test inputs are generated in-test (no checked-in binary fixtures); explains why
      contains: temp_directory_path
  key_links:
    - from: tests/unit/test_load_trust_roots.cpp
      to: ajazz::plugins::loadTrustRoots
      via: '#include "manifest_signer_common.hpp"'
      pattern: loadTrustRoots\(
    - from: Test SECTION on oversize
      to: kMaxTrustRootsBytes (1 MB cap from Plan 07-01)
      via: assert empty result on >1 MB synthetic input
      pattern: REQUIRE.*empty\(\)
---

<objective>
Land the TRUST-03 test corpus on top of the new nlohmann-based `loadTrustRoots` from Plan 07-01: a single Catch2 test file, `tests/unit/test_load_trust_roots.cpp`, with positive cases (BOM, escape sequences, nested structures, mixed key ordering, boundary entry counts) and negative cases (oversize, overcount, embedded NUL, truncated JSON, malformed UTF-8) per CONTEXT.md D-03.

Purpose: closes TRUST-03 verbatim (cover BOM, escape sequences, nested structures, embedded NUL bytes — fuzz corpus is Plan 07-03). Mechanically proves the 1 MB byte cap and 1024-entry cap from Plan 07-01 work in practice, not just by source inspection.

Output: One new test file with one new test binary entry in `tests/unit/CMakeLists.txt`. Additive commit on top of Plan 07-01 — does NOT modify the implementation, only exercises it (per CONTEXT.md `<specifics>` atomic-commit boundaries: corpus + fuzz harness ship separately from the parser swap).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-wr-01-trust-roots-parser-hardening/07-CONTEXT.md
@.planning/phases/07-wr-01-trust-roots-parser-hardening/07-01-PLAN.md
@.planning/research/PITFALLS.md
@tests/unit/test_manifest_signer.cpp
@tests/unit/CMakeLists.txt
@src/plugins/src/manifest_signer_common.hpp

<interfaces>
After Plan 07-01:

- `manifest_signer_common.hpp` exports `ajazz::plugins::TrustedPublisher` + `ajazz::plugins::loadTrustRoots(std::filesystem::path const&)`.
- The impl in `manifest_signer_common.cpp` enforces `kMaxTrustRootsBytes = 1024U * 1024U` (1 MB) and `kMaxTrustRootsEntries = 1024U`.
- All failure modes (oversize, overcount, malformed JSON, missing file, wrong shape) return an empty `std::vector<TrustedPublisher>` and log to `std::cerr`.

Existing test patterns (canonical references for include order, namespace, REQUIRE style):

- `tests/unit/test_manifest_signer.cpp` lines 266-336 — 4 existing loadTrustRoots cases (the model for "construct synthetic file in temp dir → call loadTrustRoots → assert").
- `tests/unit/CMakeLists.txt` — the source-list pattern (this plan adds `test_load_trust_roots.cpp` alongside `test_manifest_signer.cpp` in the existing `ajazz_unit_tests` target).

This plan deliberately puts the TRUST-03 corpus in a NEW file rather than extending `test_manifest_signer.cpp`. Rationale (per CONTEXT.md D-03 wording "or extend test_manifest_signer.cpp if the existing test file is still relevant"): the existing file is ~340 lines and mixes verifyManifest + loadTrustRoots cases. Splitting keeps each file focused, easier to navigate, and keeps the loadTrustRoots-specific compile dependencies (which will include nlohmann symbols indirectly via the common header) cleanly scoped.

CONTEXT.md D-03 corpus (full enumeration to mirror in SECTIONs):

Positive (must parse successfully, produce expected rows):

1. Valid trust-roots with 1 entry
1. Valid trust-roots with 2 entries
1. Valid trust-roots with 100 entries
1. Valid trust-roots with 1024 entries (boundary cap — must SUCCEED, not fail)
1. BOM-prefixed input (`\xEF\xBB\xBF{...}`)
1. Escape sequences in name field (`\"`, `\\`, `A`)
1. Nested structure variants — rows wrapped in `{"trust_roots": [...]}` should also work IF the impl supports it; per Plan 07-01 the impl currently accepts `{"publishers": [...]}` (the bundled shape) OR a top-level array. Adjust the SECTION to assert the actually-supported shapes; if `trust_roots` wrapper is NOT supported, document that as a deferred shape (CONTEXT.md `<deferred>` does not list it, so this is the planner-discretion call: assert only `{"publishers": [...]}` and top-level array, since those are what the impl supports).
1. Mixed key ordering (handled by Plan 07-01's D-02 rewrite — Plan 07-02 does NOT duplicate that case).
1. Empty trust list (`[]` or `{"publishers": []}`).

Negative (must fail closed, return empty list):

1. Oversize input (1.1 MB → fails 1 MB byte cap).
1. 1025-entry input → fails 1024 entry-count cap.
1. Embedded NUL byte mid-string → behaviour: nlohmann allows NUL bytes in JSON strings (they're valid UTF-8 codepoints) but the parsed `name` field then contains a real NUL — assert the impl either silently propagates the NUL (current behaviour — it's a string) OR document the assertion is "parses without throwing".
1. Truncated JSON (missing closing `]` or `}`) → parse error → empty list.
1. Pathological nesting → nlohmann's default depth limit handles this; do NOT add a 10000-deep input (memory expensive, not load-bearing). Instead assert via a moderate 100-deep nested object → still parses, but the impl rejects it because the top-level is not the expected `{publishers: [...]}` or top-level array shape.
1. Malformed UTF-8 in name field → nlohmann strict-by-default rejects → empty list.

Per CONTEXT.md D-03 "Empty trust list" + "Mixed key ordering" — case 8 is already covered by Plan 07-01 Task 2's renamed case ("arbitrary key order produces equivalent rows"), so Plan 07-02 does not duplicate it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/unit/test_load_trust_roots.cpp covering the TRUST-03 corpus + register in tests/unit/CMakeLists.txt</name>
  <files>tests/unit/test_load_trust_roots.cpp, tests/unit/CMakeLists.txt, tests/unit/fixtures/trust_roots/README.md</files>
  <read_first>
    - src/plugins/src/manifest_signer_common.hpp (Plan 07-01 output — public surface this test exercises)
    - src/plugins/src/manifest_signer_common.cpp (Plan 07-01 output — caps + failure-mode contract this test pins)
    - tests/unit/test_manifest_signer.cpp (existing Catch2 conventions: include order, fs::temp_directory_path() usage, std::ofstream binary mode, REQUIRE patterns, test tag style "[manifest-signer]")
    - tests/unit/CMakeLists.txt (the source-list shape — where to add the new test file alongside test_manifest_signer.cpp)
    - .planning/phases/07-wr-01-trust-roots-parser-hardening/07-CONTEXT.md (D-03 corpus enumeration; "fuzz corpus runs <1s on 100 KB inputs" note — fuzz corpus is Plan 07-03 not this plan)
    - .planning/REQUIREMENTS.md TRUST-03 verbatim (BOM, escape sequences, nested structures, embedded NUL bytes)
  </read_first>
  <action>
    Create tests/unit/test_load_trust_roots.cpp following the existing `test_manifest_signer.cpp` style (Catch2 v3, `<catch2/catch_test_macros.hpp>` include, `[manifest-signer]` test tag, fs:: namespace alias, temp-dir scratch files). Use ajazz::plugins::loadTrustRoots via `#include "../src/plugins/src/manifest_signer_common.hpp"` (mirroring how test_manifest_signer.cpp accesses `ajazz::plugins::loadTrustRoots` per its `using` line at 44).

Implement the following SECTIONs / TEST_CASEs (one TEST_CASE per logical group; use SECTION inside for multi-input cases). Each test constructs its scratch file in `fs::temp_directory_path() / "ajazz-test-trustroots-<unique>"` and removes it at end (mirroring test_manifest_signer.cpp:286-303 pattern).

```
TEST_CASE("loadTrustRoots: positive corpus — boundary entry counts", "[manifest-signer][trust-roots]") {
  SECTION("1 entry parses to 1 row") {
    // Build {"publishers":[{"key":"K1","name":"N1"}]}; assert size()==1, keyB64=="K1", name=="N1".
  }
  SECTION("2 entries parse to 2 rows in file order") {
    // Build with 2 entries; assert size()==2 and ordering matches file ordering.
  }
  SECTION("100 entries parse to 100 rows") {
    // Build via std::ostringstream loop; assert size()==100 and a spot-check on rows[0] and rows[99].
  }
  SECTION("1024 entries (boundary cap) parses to 1024 rows") {
    // Exactly at the cap — MUST succeed, not fail closed (Plan 07-01: cap rejects only >1024).
    // Assert size()==1024 and a spot-check on rows[1023].
  }
}

TEST_CASE("loadTrustRoots: positive corpus — text edge cases", "[manifest-signer][trust-roots]") {
  SECTION("BOM-prefixed input parses (nlohmann strips BOM)") {
    // File starts with \xEF\xBB\xBF then valid {"publishers":[{"key":"K","name":"N"}]}.
    // Assert size()==1 — confirms nlohmann handles BOM (per its parse() docs since v3.x).
    // If this fails, that's information: nlohmann v3.12.0 does NOT strip BOM by default,
    // and the impl needs an explicit pre-strip. Document outcome in summary.
  }
  SECTION("escape sequences in name field round-trip correctly") {
    // {"publishers":[{"key":"K","name":"He said \"hi\" \\ A"}]}
    // Assert size()==1 and name contains the literal characters: He said "hi" \ A
  }
  SECTION("empty publishers list returns empty vector") {
    // {"publishers":[]} — assert empty()
  }
  SECTION("top-level array shape is also accepted") {
    // [{"key":"K","name":"N"}] (the fallback shape from Plan 07-01)
    // Assert size()==1.
  }
}

TEST_CASE("loadTrustRoots: negative corpus — DoS caps fail closed", "[manifest-signer][trust-roots]") {
  SECTION("input >1 MB returns empty list (TRUST-02 byte cap)") {
    // Construct {"publishers":[{"key":"K","name":"<1.1 MB filler>"}]} via ostringstream
    // padding the name field with ~1.1 MB of 'A' characters; assert loadTrustRoots returns empty().
    // Cleanup the temp file (don't leave 1.1 MB in /tmp on test failure).
  }
  SECTION("input with 1025 entries returns empty list (TRUST-02 entry cap)") {
    // Build {"publishers":[ ... 1025 entries ... ]} (well under 1 MB total since each entry is small);
    // assert loadTrustRoots returns empty() (1024 succeeds per the positive case above; 1025 fails closed).
  }
}

TEST_CASE("loadTrustRoots: negative corpus — malformed input fails closed", "[manifest-signer][trust-roots]") {
  SECTION("truncated JSON (missing closing brace) returns empty list") {
    // {"publishers":[{"key":"K","name":"N"}    (no closing ] or })
    // nlohmann parse_error → caught → empty list.
  }
  SECTION("malformed UTF-8 in name field returns empty list") {
    // Inject raw bytes 0xC0 0xC0 (invalid UTF-8 start byte sequence) inside the name string.
    // nlohmann v3.12.0 strict mode rejects → caught → empty list.
  }
  SECTION("wrong top-level shape (object without publishers, not array) returns empty list") {
    // {"foo":"bar"} — neither {"publishers":[...]} nor top-level array → empty list.
  }
}

TEST_CASE("loadTrustRoots: NUL-byte handling — pinned behaviour", "[manifest-signer][trust-roots]") {
  // CONTEXT.md D-03 says: "Embedded NUL byte mid-string → fails (or sanitizes per nlohmann default; document which)."
  // This test PINS the actually-observed behaviour so it's documented in the test suite.
  // {"publishers":[{"key":"K","name":"A�B"}]} — name contains literal NUL.
  // nlohmann v3.12.0 accepts � in strings (it's a valid Unicode codepoint).
  // Assert: size()==1 AND name.size()==3 AND name[1]=='\0'.
  // The summary documents this as "NUL bytes in name fields are preserved as literal NULs in TrustedPublisher::name; downstream consumers must be NUL-tolerant".
}
```

Add `tests/unit/test_load_trust_roots.cpp` to the existing `add_executable(ajazz_unit_tests ...)` source list in `tests/unit/CMakeLists.txt` (find the line that lists `test_manifest_signer.cpp` and add `test_load_trust_roots.cpp` adjacent to it). The test binary already links `ajazz_plugins` (which exports `loadTrustRoots` through the new common header).

Create tests/unit/fixtures/trust_roots/README.md with one short paragraph: "Trust-root test inputs are constructed at test time in `std::filesystem::temp_directory_path()` and removed after each TEST_CASE. There are no checked-in binary fixtures here — this prevents accidental confusion with real `resources/trusted_publishers.json` and keeps tests reproducible across machines. The fuzz corpus seeds (Plan 07-03) live separately under `tests/fuzz/trust_roots_corpus/`."

Verify the build + run:

`cmake --build --preset linux-release --target ajazz_unit_tests` exits 0.
`ctest --preset linux-release -R trust-roots --output-on-failure` exits 0 — every SECTION passes.

Commit as a single additive commit on top of Plan 07-01:

```
test(plugins): add TRUST-03 corpus for loadTrustRoots (Plan 07-02)

Adds tests/unit/test_load_trust_roots.cpp covering the corpus shape from
CONTEXT.md D-03:
- Positive: BOM, escape sequences, boundary entry counts (1/2/100/1024),
  empty list, top-level array shape.
- Negative: >1 MB input fails closed (TRUST-02 byte cap), 1025 entries fails
  closed (TRUST-02 entry cap), truncated JSON, malformed UTF-8, wrong shape.
- NUL-byte handling pinned: � in name is preserved as literal NUL.

All tests construct scratch files in std::filesystem::temp_directory_path()
and remove them after each case — no checked-in binary fixtures.

Closes TRUST-03 (BOM, escape sequences, nested structures, embedded NUL
bytes covered). Fuzz corpus runs in Plan 07-03.

Refs: TRUST-03 (REQUIREMENTS.md), Pitfall 7 (DoS surface), CONTEXT.md D-03
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tee /tmp/build_unit.log | tail -20 ; grep -E "error:" /tmp/build_unit.log && exit 1 || true ; ctest --preset linux-release -R trust-roots --output-on-failure 2>&1 | tee /tmp/test_trust.log | tail -40 ; grep -E "FAILED|Errors" /tmp/test_trust.log && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/test_load_trust_roots.cpp` exits 0
    - `grep -c "TEST_CASE" tests/unit/test_load_trust_roots.cpp` returns at least 5
    - `grep -c "SECTION(" tests/unit/test_load_trust_roots.cpp` returns at least 12 (one per D-03 corpus item)
    - `grep -q "BOM" tests/unit/test_load_trust_roots.cpp` exits 0 (BOM case present)
    - `grep -q "1024" tests/unit/test_load_trust_roots.cpp` exits 0 (entry-count boundary case present)
    - `grep -q "1025" tests/unit/test_load_trust_roots.cpp` exits 0 (entry-count over-cap case present)
    - `grep -qE '\\\\u0000|NUL' tests/unit/test_load_trust_roots.cpp` exits 0 (NUL-byte case present)
    - `grep -q "test_load_trust_roots" tests/unit/CMakeLists.txt` exits 0 (registered in the unit test binary)
    - `test -f tests/unit/fixtures/trust_roots/README.md` exits 0
    - `ctest --preset linux-release -R trust-roots` exits 0 — every SECTION passes
    - The corpus run completes well under 5 seconds total (CTest output includes a per-test wall-clock; the longest case is the 1024-entry build which should be <500ms on stock hardware)
    - `git log --oneline -1` shows a single additive commit touching only the 3 files in `files_modified` — does NOT touch any of Plan 07-01's files (additive, not modifying)
  </acceptance_criteria>
  <done>
    TRUST-03 corpus is checked in, registered in CTest, and runs green on Linux. The 1 MB byte cap and 1024-entry cap from Plan 07-01 are mechanically proven by SECTION assertions (not just source inspection). The NUL-byte and BOM behaviours are pinned in tests so a future nlohmann upgrade that changes either silently breaks CI loudly. The fuzz harness (Plan 07-03) layers on top in a separate additive commit.
  </done>
</task>

</tasks>

<verification>
- `ctest --preset linux-release -R trust-roots` — every SECTION passes; total runtime <5 seconds (well under the TRUST-03 sub-1s-per-iteration budget which applies to the fuzz corpus, not the unit corpus).
- `git log --oneline -2 --stat HEAD` shows two commits: (HEAD) the test corpus addition (additive), and (HEAD~1) the Plan 07-01 atomic parser swap.
- The test binary ajazz_unit_tests transitively links nlohmann via ajazz_plugins, but no Test source file directly `#include`s `<nlohmann/json.hpp>` — confirming COD-031 isolation: nlohmann is only used by the impl, never by callers (test code is a caller).
</verification>

\<success_criteria>

- TRUST-03 satisfied verbatim: BOM, escape sequences, nested structures (top-level array fallback), and embedded NUL bytes are all covered by SECTIONs in tests/unit/test_load_trust_roots.cpp.
- The DoS caps from Plan 07-01 are exercised by tests, not just source-grepped — oversize and overcount inputs return empty list and the test enforces it.
- Test additions are purely additive — no changes to the impl from Plan 07-01, so TRUST-01..02..04 contracts from that commit remain mechanically pinned by their original verification path.
  \</success_criteria>

<output>
After completion, create `.planning/phases/07-wr-01-trust-roots-parser-hardening/07-02-SUMMARY.md` documenting: (a) commit hash + diffstat, (b) per-SECTION wall-clock from `ctest --output-on-failure -V`, (c) the actually-observed BOM behaviour (does nlohmann v3.12.0 strip BOM, or did the impl need a pre-strip — the test's pass/fail tells us), (d) the actually-observed NUL-byte behaviour (preserved? sanitized? rejected?), (e) any test that surfaced an unexpected impl behaviour worth reflecting back into Plan 07-01 (if so, recommend a follow-up fix commit). Cross-link Plan 07-03 (fuzz harness) as the next additive deliverable.
</output>
