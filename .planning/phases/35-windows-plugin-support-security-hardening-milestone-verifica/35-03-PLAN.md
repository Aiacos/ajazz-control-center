---
phase: 35-windows-plugin-support-security-hardening-milestone-verifica
plan: 03
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/app/src/loaded_plugins_model.hpp
  - src/app/src/loaded_plugins_model.cpp
  - src/app/qml/LoadedPluginsPage.qml
  - tests/unit/test_loaded_plugins_model.cpp
autonomous: false
requirements: [WINPLG-03, VERIF-01]
must_haves:
  truths:
    - LoadedPluginsModel exposes a platformStatus role derived from PluginInfo.winClass
    - LoadedPluginsPage renders a status chip reading 'Runs natively' / 'Requires Wine' / 'Unsupported on this OS'
    - Every new chip (status + unsigned-consent) AND the pre-existing trustChip carry an objectName + a readable label property
    - scripts/ajazz-debug qml.get returns each chip objectName + label headlessly
    - The consolidated live debug-channel phase gate (chip qml.get + consent restart round-trip + loopback confirm) passes
  artifacts:
    - path: src/app/src/loaded_plugins_model.hpp
      provides: PlatformStatusRole in the Roles enum
      contains: PlatformStatusRole
    - path: src/app/src/loaded_plugins_model.cpp
      provides: platformStatus role wiring + platformStatusOf() derive from winClass
      contains: platformStatus
    - path: src/app/qml/LoadedPluginsPage.qml
      provides: objectName-addressed status chip + unsigned-consent chip + objectName on trustChip
      contains: platformStatusChip
    - path: tests/unit/test_loaded_plugins_model.cpp
      provides: platformStatusOf derive cases (native/wine/unsupported)
      contains: platformStatus
  key_links:
    - from: src/app/qml/LoadedPluginsPage.qml
      to: LoadedPluginsModel platformStatus role
      via: delegate binding row.platformStatus
      pattern: platformStatus
    - from: src/app/src/loaded_plugins_model.cpp
      to: plugins::PluginInfo.winClass
      via: platformStatusOf(info) maps winClass + currentPlatform to a chip label
      pattern: winClass
---

<objective>
Render the WINPLG classification as a live-drivable status chip on
LoadedPluginsPage and close the VERIF-01 objectName gap. Add a platformStatus
model role derived from PluginInfo.winClass (stamped by Plan 01), a status chip,
an amber unsigned-consent chip, and objectName + readable-label properties on
every chip (the existing trustChip currently has NONE). Close the phase with the
consolidated live debug-channel gate.

Purpose: WINPLG-03 (chip-only this phase; Wine launch deferred) and VERIF-01.
This is the phase's UI deliverable and its mandatory live gate.

Output: a new model role, chips with objectNames, a derive unit test, and a
passed live debug-channel phase gate.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-RESEARCH.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-PATTERNS.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-VALIDATION.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-01-SUMMARY.md

<interfaces>
From Plan 01 (consumed here) — src/plugins/include/ajazz/plugins/i_plugin_host.hpp:
  struct PluginInfo { ...; int winClass{0}; };   // 0=NotWindowsOnly, 1=WsOnlyIpc, 2=VendorDll

LoadedPluginsModel role idiom — loaded_plugins_model.hpp:83-91 / .cpp:86-131:
enum Roles { IdRole=Qt::UserRole+1, NameRole, VersionRole, AuthorsRole, PermissionsRole,
SignedRole, PublisherRole, TrustLevelRole }; // add PlatformStatusRole here
case TrustLevelRole: return trustLevelOf(info); // .cpp:86-87
{ TrustLevelRole, "trustLevel" }, // .cpp:102 roleNames
static QString trustLevelOf(PluginInfo const&); // .hpp:152 / .cpp:131 — mirror with platformStatusOf

trustChip delegate to mirror — LoadedPluginsPage.qml:168-221 (Rectangle, visible on non-trusted, Theme.chipBg{Error,Warning},
Text + ToolTip + MouseArea). grep -c objectName LoadedPluginsPage.qml == 0 (the GAP).
objectName + readable-property analog — SettingsPage.qml:208-245 (waylandCapabilityWarningChip: objectName +
`property string warningText` so qml.get reads the label headlessly).
Harness gap (CLAUDE.md): qml.invoke fires onClicked but NOT Switch.toggled — use plain display chips, no Switch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: platformStatus model role derived from PluginInfo.winClass + derive unit test</name>
  <files>src/app/src/loaded_plugins_model.hpp, src/app/src/loaded_plugins_model.cpp, tests/unit/test_loaded_plugins_model.cpp</files>
  <read_first>
    - src/app/src/loaded_plugins_model.hpp (lines 83-91 Roles enum + line 152 trustLevelOf declaration)
    - src/app/src/loaded_plugins_model.cpp (lines 60-135 — data() role case :86, roleNames() :93-102, trustLevelOf derive :131)
    - src/plugins/include/ajazz/plugins/i_plugin_host.hpp (the winClass field added in Plan 01 with its int mapping)
    - tests/unit/test_loaded_plugins_model.cpp (existing trustLevelOf test style; add platformStatusOf cases the same way)
  </read_first>
  <behavior>
    - platformStatusOf: winClass=1 (WsOnlyIpc) -> "native" on any platform
    - platformStatusOf: winClass=2 (VendorDll), wine hard-false this phase -> "unsupported"
    - platformStatusOf: winClass=2 with the (future) wine branch -> "wine" (keep branch explicit; input is false this phase)
    - platformStatusOf: winClass=0 (NotWindowsOnly) -> "" (chip hidden)
  </behavior>
  <action>
    Add PlatformStatusRole to the Roles enum (loaded_plugins_model.hpp, after TrustLevelRole). Add a static [[nodiscard]] QString platformStatusOf(plugins::PluginInfo const& info) next to trustLevelOf (:152). In loaded_plugins_model.cpp: add case PlatformStatusRole: return platformStatusOf(info); in data() (mirror :86-87), add { PlatformStatusRole, "platformStatus" } in roleNames() (mirror :102), and implement platformStatusOf() mirroring trustLevelOf() (:131): switch on info.winClass (documented 0/1/2). Return "native" for WsOnlyIpc; for VendorDll return "wine" if a this-phase-false wineAvailable signal holds else "unsupported"; "" for NotWindowsOnly. Keep the wine-vs-unsupported branch explicit with a comment that wineAvailable is hard-false this phase (WINPLG-03 launch deferred) so a future session flips one input, not the derive shape.

```
Add platformStatusOf cases to tests/unit/test_loaded_plugins_model.cpp (ASCII names, existing tag) with PluginInfo fixtures winClass 0/1/2 asserting the derived string; confirm the role is in roleNames().
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release 2>&1 | tail -5 && ctest --preset linux-release -R "loaded-plugins|LoadedPlugins" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ctest --preset linux-release -R "loaded-plugins|LoadedPlugins" passes including the new platformStatusOf cases
    - grep -n "PlatformStatusRole\|platformStatus" src/app/src/loaded_plugins_model.hpp src/app/src/loaded_plugins_model.cpp shows the enum member, the data() case, the roleNames entry, and the derive
    - the derive maps winClass 1->"native", 2->"unsupported" (Linux, wine false), 0->"" — asserted by tests
    - full ctest --preset linux-release green
  </acceptance_criteria>
  <done>LoadedPluginsModel exposes platformStatus derived from PluginInfo.winClass; the derive is unit-locked for native/unsupported/empty.</done>
</task>

<task type="auto">
  <name>Task 2: status chip + unsigned-consent chip + objectNames on LoadedPluginsPage (VERIF-01)</name>
  <files>src/app/qml/LoadedPluginsPage.qml</files>
  <read_first>
    - src/app/qml/LoadedPluginsPage.qml (lines 160-228 — the trustChip delegate to mirror + the stale Phase-27 comment at :222-227)
    - src/app/qml/SettingsPage.qml (lines 208-245 — waylandCapabilityWarningChip: objectName + property string warningText + ToolTip idiom)
  </read_first>
  <action>
    In LoadedPluginsPage.qml, alongside trustChip (:168-221), add two sibling Rectangles mirroring the trustChip idiom and the SettingsPage amber-chip objectName/readable-property idiom:

```
(1) platformStatusChip — objectName "platformStatusChip"; visible: row.platformStatus !== ""; a readable property string statusLabel mapping row.platformStatus to the locked labels ("native"->"Runs natively", "wine"->"Requires Wine", "unsupported"->"Unsupported on this OS"); Theme.chip* coloring (warning for wine, error for unsupported, neutral/positive for native); Text bound to statusLabel + a ToolTip explaining the classification. Display only, no Switch.

(2) unsignedConsentChip — objectName "unsignedConsentChip"; visible: row.trustLevel === "unsigned"; a readable property string consentLabel: qsTr("Unsigned — requires consent"); amber Theme.chipBgWarning coloring. Display only.

(3) VERIF-01 fix: add objectName "trustChip" to the EXISTING trustChip and expose a readable property string trustLabel bound to its chip text, so qml.get reaches the existing chip too (it currently has none — grep -c objectName == 0).

Every chip MUST expose its label as a readable QML property (mirror warningText) so scripts/ajazz-debug qml.get returns the label headlessly. Keep all three as plain display chips.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release 2>&1 | tail -5 && ctest --preset linux-release -R qml --output-on-failure; test "$(grep -c objectName src/app/qml/LoadedPluginsPage.qml)" -ge 3 && echo OBJNAME_OK</automated>
  </verify>
  <acceptance_criteria>
    - cmake build green; ctest --preset linux-release -R qml green (offscreen QML smoke target)
    - grep -c objectName src/app/qml/LoadedPluginsPage.qml >= 3 (platformStatusChip + unsignedConsentChip + trustChip)
    - grep -n "platformStatusChip\|unsignedConsentChip\|statusLabel\|consentLabel\|trustLabel" src/app/qml/LoadedPluginsPage.qml shows all chips + readable label properties
    - no Switch/CheckBox added to the chips (harness gap — display-only)
  </acceptance_criteria>
  <done>Three objectName-addressed chips with readable labels render on LoadedPluginsPage; the WINPLG status chip is data-bound to platformStatus and the trustChip gap is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Consolidated live debug-channel phase gate (chip qml.get + consent restart + loopback)</name>
  <action>Run the consolidated VERIF-01 live debug-channel phase gate: drive the chips via scripts/ajazz-debug qml.get, run the PLGSEC-02 consent restart round-trip, and confirm PLGSEC-03 loopback — per the steps in how-to-verify below. This is a human-verify checkpoint; record every command + output (and screenshot path) in the SUMMARY, and log any un-runnable step honestly in 35-HUMAN-UAT.md (never fabricate).</action>
  <what-built>
    The WINPLG status chip + unsigned-consent chip on LoadedPluginsPage (data-bound to the platformStatus model role from Plan 01), objectNames on all three chips, and the verified PLGSEC invariants from Plan 02. This is the consolidated VERIF-01 live debug-channel phase gate for Phase 35.
  </what-built>
  <how-to-verify>
    Rebuild first (stale-binary trap): `cmake --build --preset linux-release`. Kill all running instances + rm the stale debug socket before launch (single-instance is keyed on app-ID), then launch headless: `AJAZZ_DEBUG_CONTROL=1 nohup build/linux-release/src/app/ajazz-control-center >/tmp/acc.log 2>&1 &`.

```
1. CHIP qml.get (WINPLG-03/VERIF-01): install a WS-only-IPC win-only `.sdPlugin` (os=[windows], CodePath .js) so it runs natively (Plan 01 WINPLG-02), and a vendor-DLL win-only `.sdPlugin` (CodePath .exe or an MZ-magic file). Then `scripts/ajazz-debug qml.get --params '{"name":"platformStatusChip","property":"statusLabel"}'` returns "Runs natively" for the WS-only plugin and "Unsupported on this OS" for the vendor-DLL plugin (Wine absent on this host). `qml.get` the `unsignedConsentChip` consentLabel and the `trustChip` trustLabel too. `screenshot` and READ it to confirm the chips render (not blank).

2. CONSENT restart round-trip (PLGSEC-02): install an unsigned `.sdPlugin` granting consent; `plugin.list` shows it loaded. Kill the app, relaunch, `plugin.list` again — the plugin is loaded WITHOUT a re-prompt (consent persisted in QSettings plugins/allowed/<uuid>, survived the launch-sweep).

3. LOOPBACK confirm (PLGSEC-03): `scripts/ajazz-debug state` (or `ping`) confirms the WS server is loopback-only.

Record the exact commands + outputs (and the screenshot path) in the SUMMARY. If any live step cannot be completed on this host (e.g. no real plugin available), record it HONESTLY as a deferred HUMAN-UAT item in 35-HUMAN-UAT.md and the milestone audit doc — NEVER fabricate a screenshot or a walk (Pitfall 5).
```

</how-to-verify>
  <resume-signal>Type "approved" with the recorded outputs, or describe the issues / which steps were deferred to HUMAN-UAT.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                        |
| ---------------------------- | ---------------------------------------------------------------------------------- |
| model role -> QML chip       | The classification verdict (untrusted-plugin-derived) is rendered as display text  |
| debug channel -> running app | qml.get/screenshot drive the live UI for verification (opt-in AJAZZ_DEBUG_CONTROL) |

## STRIDE Threat Register

| Threat ID        | Category        | Component                | Disposition | Mitigation Plan                                                                                                                                                                                                   |
| ---------------- | --------------- | ------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-03-01       | Spoofing        | platformStatusChip label | mitigate    | The chip is display-only; it surfaces the classification but grants no capability — a misclassified plugin still cannot run a vendor-DLL (supportsCurrentPlatform gates that in Plan 01; no in-process PE loader) |
| T-35-03-02       | EoP             | unsignedConsentChip      | accept      | The chip is informational; consent is granted through the existing PluginStore consent flow + QSettings (Plan 02), not through this chip (no interactive control here per the harness gap)                        |
| T-35-03-03       | Info disclosure | debug channel            | accept      | Debug channel is opt-in (AJAZZ_DEBUG_CONTROL=1) + loopback Unix socket; not shipped enabled                                                                                                                       |
| \</threat_model> |                 |                          |             |                                                                                                                                                                                                                   |

<verification>
- Full ctest --preset linux-release green + ctest -R qml green.
- grep -c objectName src/app/qml/LoadedPluginsPage.qml >= 3 (VERIF-01 gap closed).
- Live debug-channel gate: chip qml.get returns the correct labels for WS-only vs vendor-DLL plugins + screenshot read; consent restart round-trip loads without re-prompt; state/ping confirms loopback.
- Any un-runnable live step recorded honestly in 35-HUMAN-UAT.md (never fabricated).
</verification>

\<success_criteria>

- WINPLG-03 (partial, chip-only): the status chip shows "Runs natively"/"Requires Wine"/"Unsupported on this OS", objectName-addressed, derived from the Plan-01 classification; Wine launch remains deferred.
- VERIF-01: every new chip + the pre-existing trustChip are objectName-addressable with readable labels; the phase closes with a live debug-channel drive + screenshot (or an honest deferred-HUMAN-UAT record).
  \</success_criteria>

<output>
Create `.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-03-SUMMARY.md` when done. Record any deferred live steps in `35-HUMAN-UAT.md`.
</output>
