---
phase: 35-windows-plugin-support-security-hardening-milestone-verifica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tests/unit/test_plugin_verify_gate.cpp
  - docs/milestone-v2.0-modularity-audit.md
  - scripts/verif-milestone-gates.sh
autonomous: true
requirements: [PLGSEC-01, PLGSEC-02, PLGSEC-03, VERIF-02]
must_haves:
  truths:
    - A tampered plugin is refused even with userConfirmedUnsigned=true (CR-01), locked by a still-green Catch2 test
    - An unsigned plugin requires explicit consent; tampered != unsigned in the verdict enum
    - Per-plugin unsigned consent persists in QSettings plugins/allowed/<uuid> and survives the launch-sweep (existing test green)
    - The WS server binds QHostAddress::LocalHost; the CI grep gate for QHostAddress::Any is active and comment-aware
    - The VERIF-02 milestone grep gates all pass (mirajazz/nlohmann-include/akp05-symbol/QHostAddress::Any = 0) and are recorded in an audit doc
  artifacts:
    - path: docs/milestone-v2.0-modularity-audit.md
      provides: VERIF-02 grep-gate results + honest live-evidence vs HUMAN-UAT reconciliation
      contains: modularity audit
    - path: scripts/verif-milestone-gates.sh
      provides: scripted, comment-aware VERIF-02 grep gates runnable locally and in CI
      contains: QHostAddress::Any
    - path: .github/workflows/ci.yml
      provides: VERIF-02 milestone grep gates wired into CI (extends the Phase-30 loopback gate)
      contains: QHostAddress::Any
  key_links:
    - from: scripts/verif-milestone-gates.sh
      to: .github/workflows/ci.yml
      via: CI invokes the script (or the inline gates mirror it)
      pattern: verif-milestone-gates|QHostAddress::Any
    - from: tests/unit/test_plugin_verify_gate.cpp
      to: VerifyVerdict::Refused / Unsigned
      via: verdict-mapping assertion locking tampered != unsigned
      pattern: Refused|Unsigned
---

<objective>
Verify + test-lock + document the security and modularity invariants that are
ALREADY SHIPPED. This is a VERIFY plan, not an implement plan: PLGSEC-01/02/03 are
implemented, CR-fixed, and unit-tested; VERIF-02 grep gates already pass (comment-
only matches). The work is: confirm each invariant still holds, add the one
missing Unsigned-specific verdict-mapping case if absent, lock the VERIF-02 greps
as a scripted+CI gate, and author the honest milestone modularity audit doc.

Purpose: prevent regression/duplication of the existing security model (Pitfall 1:
do NOT redesign the verdict enum or consent store) and produce the VERIF-02
deliverable. Frame every PLGSEC task as confirm-the-file:line + assert-test-green.

Output: a milestone audit doc, a scripted comment-aware gate, CI wiring, and (if
needed) one added verdict-mapping test case.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-RESEARCH.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-PATTERNS.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-VALIDATION.md

<interfaces>
ALREADY-IMPLEMENTED (verify only, do NOT reimplement):
  VerifyVerdict { Trusted, SelfSigned, Unsigned, Refused }  — src/app/src/plugin_verify_gate.hpp:41-46  (Refused == tampered)
  verdictToTrustLevel(...) -> "trusted"/"self-signed"/"unsigned"/"tampered"  — plugin_verify_gate.hpp:89
  CR-01 tampered-refused-even-with-consent  — src/app/src/plugin_catalog_model.cpp:907-927
  FIX-CONSENT QSettings plugins/allowed/<uuid>=true  — plugin_catalog_model.cpp:1063-1076
  launch-sweep honors perPluginAllowed()  — plugin_catalog_model.cpp:173 (perPluginAllowed at :61-66)
  WS server loopback bind  — src/app/src/sd_plugin_server.cpp:93 listen(QHostAddress::LocalHost, port)
  catalog phone-home gate (opt-in onlineCatalogEnabled + disabled sentinel)  — plugin_catalog_model.cpp:521-535

EXISTING TESTS (must stay green):
test_plugin_install_from_file.cpp:581 "PluginInstallFromFile tampered plugin refused even with consent"
test_plugin_install_from_file.cpp:854 "PluginCatalog allowUnsignedPlugins=true tampered STILL refused (CR-01)"
test_plugin_install_from_file.cpp:942 "PluginCatalog per-plugin allow survives launch-sweep with global toggle OFF" (the PLGSEC-02 restart round-trip — fresh model = simulated restart)
test_plugin_verify_gate.cpp:265 tampered manifest -> Refused ; :305 unsigned -> Unsigned
test_sd_plugin_server.cpp:59-66, :331-340 bindAddress() == LocalHost, != Any/AnyIPv4/AnyIPv6 (PLGSEC-03 code assert ALREADY EXISTS)

EXISTING CI GATE — .github/workflows/ci.yml:67-83 (comment-aware QHostAddress::Any + SIGPIPE), hid_open gate :55-64.
VERIF-02 greps (current result = comment-only matches => PASS):
grep -rn mirajazz src/core/ src/app/src/plugin_manager.cpp src/app/src/plugin_device_bridge.cpp # expect 0
grep -rn '#include.*nlohmann' src/core/include/ # expect 0
grep -rn 'makeAkp05|makeAkp03|makeAkp153' src/ # expect 0
grep -rn 'QHostAddress::Any' src/ | grep -vP ':[0-9]+:\\s*(//|[\*])' # expect 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the PLGSEC invariants are green and lock the Unsigned/Tampered verdict split</name>
  <files>tests/unit/test_plugin_verify_gate.cpp (only if an unsigned->Unsigned case is absent)</files>
  <read_first>
    - src/app/src/plugin_verify_gate.hpp (lines 41-95 — VerifyVerdict enum + verdictToTrustLevel; confirm 4-way split unchanged)
    - src/app/src/plugin_catalog_model.cpp (lines 900-930 — CR-01 Refused branch; lines 1060-1080 — FIX-CONSENT; line 173 — launch-sweep perPluginAllowed read)
    - src/app/src/sd_plugin_server.cpp (lines 86-148 — LocalHost bind; the only QHostAddress::Any is the comment at :90)
    - tests/unit/test_plugin_verify_gate.cpp (lines 180-320 — verdict-mapping cases :265 tampered->Refused, :305 unsigned->Unsigned)
    - tests/unit/test_plugin_install_from_file.cpp (lines 575-600 CR-01 with-consent test; lines 942-1000 launch-sweep survival = PLGSEC-02 round-trip)
  </read_first>
  <action>
    This is a verification task — do NOT add or rename any VerifyVerdict member, do NOT add a new consent key, do NOT add a new bind site (Pitfall 1; the warning sign is any task that defines a new enum/key/bind).

```
PLGSEC-01: confirm plugin_verify_gate.hpp:41-46 still defines exactly { Trusted, SelfSigned, Unsigned, Refused } and that verdictToTrustLevel maps Refused->"tampered", Unsigned->"unsigned" (:89). Confirm the CR-01 Refused branch at plugin_catalog_model.cpp:907-927 still aborts install for Refused. Run the existing CR-01 tests (test_plugin_install_from_file.cpp:581 + :854) and the verdict-mapping tests and confirm green. THEN ensure an Unsigned-specific verdict-mapping assertion exists in test_plugin_verify_gate.cpp: grep the file for an unsigned->Unsigned + a tampered->Refused case; both should be at :265/:305. If the Unsigned case is missing or only implicit, ADD one ASCII-only TEST_CASE tagged [plugin-verify-gate] asserting an unsigned manifest maps to VerifyVerdict::Unsigned and verdictToTrustLevel == "unsigned" (mirror the existing :265 case style). If it already exists, add nothing.

PLGSEC-02: confirm the FIX-CONSENT write (plugin_catalog_model.cpp:1063-1076) and the launch-sweep perPluginAllowed read (:173) are intact, and that test_plugin_install_from_file.cpp:942 (per-plugin allow survives launch-sweep / simulated restart via a fresh model instance) is green. Per RESEARCH A3, this :942 test IS the persistence round-trip — do NOT add a duplicate unless grep shows :942 was removed/renamed.

PLGSEC-03: confirm sd_plugin_server.cpp:93 binds QHostAddress::LocalHost and that test_sd_plugin_server.cpp:59-66 + :331-340 assert LocalHost != Any. The code-level assert ALREADY EXISTS — do NOT add a new one; just confirm it runs green. Confirm the catalog phone-home gate (plugin_catalog_model.cpp:521-535) remains opt-in.
```

</action>
  <verify>
    <automated>ctest --preset linux-release -R "plugin-verify-gate|plugin-install|SdPluginServer" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ctest --preset linux-release -R plugin-verify-gate passes, including an unsigned->Unsigned AND a tampered->Refused verdict-mapping case
    - ctest --preset linux-release -R plugin-install passes, including the CR-01 tampered-with-consent test (test_plugin_install_from_file.cpp:581) and the launch-sweep survival test (:942)
    - ctest --preset linux-release -R SdPluginServer passes the LocalHost-not-Any assert
    - grep -n "enum class VerifyVerdict" src/app/src/plugin_verify_gate.hpp shows exactly { Trusted, SelfSigned, Unsigned, Refused } (unchanged — no rename/add)
    - git diff --stat shows NO change to plugin_catalog_model.cpp / plugin_verify_gate.hpp / sd_plugin_server.cpp (verify-only; the only allowed source edit is an added test case in test_plugin_verify_gate.cpp IF the unsigned case was absent)
  </acceptance_criteria>
  <done>All PLGSEC invariants confirmed green at their cited file:line; the verdict split is locked by both a tampered->Refused and an unsigned->Unsigned test; no security code was reimplemented.</done>
</task>

<task type="auto">
  <name>Task 2: Scripted comment-aware VERIF-02 grep gates + CI wiring + milestone modularity audit doc</name>
  <files>scripts/verif-milestone-gates.sh, .github/workflows/ci.yml, docs/milestone-v2.0-modularity-audit.md</files>
  <read_first>
    - .github/workflows/ci.yml (lines 48-83 — hid_open gate :55-64 + loopback/SIGPIPE gate :67-83; the comment-aware grep idiom grep -vP ':[0-9]+:\s*(//|[*])' to copy)
    - docs/plugin-event-parity.md (Phase-34 audit-doc style: table-driven, honest done/partial/deferred — mirror format)
    - .planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-HUMAN-UAT.md (a deferred-walk artifact — cite, do not fabricate)
    - .planning/phases/33-property-inspector-end-to-end/33-HUMAN-UAT.md
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-HUMAN-UAT.md
  </read_first>
  <action>
    Create scripts/verif-milestone-gates.sh (bash, set -euo pipefail, ASCII output) running the four VERIF-02 gates verbatim with the comment-aware filter from ci.yml: (1) grep -rn mirajazz src/core/ src/app/src/plugin_manager.cpp src/app/src/plugin_device_bridge.cpp expect 0; (2) grep -rn '#include.*nlohmann' src/core/include/ expect 0 (the meaningful gate is the include, not the word in boundary comments); (3) grep -rn 'makeAkp05\|makeAkp03\|makeAkp153' src/ expect 0 (prefer the function symbols over the akp05.cpp filename, which appears in comments); (4) grep -rn 'QHostAddress::Any' src/ | grep -vP ':[0-9]+:\s*(//|[*])' expect 0. Each gate prints PASS/FAIL with the count; the script exits non-zero on any FAIL. Also run an objectName-coverage scan over new v2.0 QML: enumerate the interactive controls added across Phases 30-35 QML and assert each has objectName (at minimum the Plan-03 chips; report the scan result rather than hard-failing on pre-v2.0 files).

```
Wire it into .github/workflows/ci.yml as a Linux-only step (mirror the Phase-30 loopback gate placement at :67-83) that invokes scripts/verif-milestone-gates.sh — OR add the four greps inline mirroring the existing comment-aware idiom if a script invocation does not fit the workflow shape. Do not remove or weaken the existing Phase-30 QHostAddress::Any/SIGPIPE/hid_open gates.

Author docs/milestone-v2.0-modularity-audit.md (mirror docs/plugin-event-parity.md table style). Enumerate: (a) the four VERIF-02 grep-gate results verbatim with their current counts and the note that any naive hit is comment-only (e.g. akp05 in stream_dock_control_service.cpp:193 + sidecar_stream_dock_device.hpp:16 comments; QHostAddress::Any in sd_plugin_server.cpp:90 comment); (b) the objectName-coverage scan result over new v2.0 QML (mark this section PROVISIONAL — Plan 02 runs in Wave 1 before Plan 03 adds the LoadedPluginsPage chips; the Plan-03 live gate refreshes/finalizes it after the chips land); (c) the HONEST per-phase live-evidence reconciliation — for each of Phases 30-35 state whether it has headless/automated live debug-channel + screenshot evidence vs a deferred HUMAN-UAT walk, citing the actual 32/33/34-HUMAN-UAT.md files. NEVER fabricate a screenshot or claim a walk that was deferred (Pitfall 5). Mark WINPLG-03 as partial (chip-only, Wine launch deferred) and PI-03 as partial (human-verify deferred).
```

</action>
  <verify>
    <automated>bash scripts/verif-milestone-gates.sh && test -f docs/milestone-v2.0-modularity-audit.md && grep -qi "modularity audit\|VERIF-02" docs/milestone-v2.0-modularity-audit.md && echo AUDIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/verif-milestone-gates.sh exits 0 and prints PASS for all four VERIF-02 gates (current tree)
    - the four gates use the comment-aware filter (a bare QHostAddress::Any comment does NOT trip the gate; an actual #include nlohmann WOULD)
    - .github/workflows/ci.yml invokes the gates (script or inline) on Linux without removing the Phase-30 gates — grep -n "QHostAddress::Any\|verif-milestone-gates" .github/workflows/ci.yml shows both the new and the pre-existing gate
    - docs/milestone-v2.0-modularity-audit.md exists with the four gate results, the objectName-coverage scan, and an honest per-phase live-evidence/HUMAN-UAT table citing 32/33/34-HUMAN-UAT.md
    - the audit doc explicitly marks WINPLG-03 and PI-03 as partial; no fabricated screenshot claim
  </acceptance_criteria>
  <done>VERIF-02 gates are scripted + comment-aware + CI-wired and pass on the current tree; the milestone modularity audit doc honestly enumerates gate results, objectName coverage, and live-evidence-vs-deferred reconciliation.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                      |
| --------------------------------------- | -------------------------------------------------------------------------------- |
| untrusted plugin install -> verify gate | Tampered/unsigned plugin archives cross into the verify gate (already mitigated) |
| plugin WS client -> SdPluginServer      | Local WebSocket clients connect to the plugin server (loopback-only)             |
| catalog -> network                      | The catalog could phone home (opt-in gated)                                      |

## STRIDE Threat Register

| Threat ID        | Category              | Component               | Disposition    | Mitigation Plan                                                                                                                                      |
| ---------------- | --------------------- | ----------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-02-01       | Tampering / EoP       | tampered signed plugin  | mitigate       | VerifyVerdict::Refused quarantines even with consent (plugin_catalog_model.cpp:907) — verified + test-locked (test_plugin_install_from_file.cpp:581) |
| T-35-02-02       | Spoofing / EoP        | unsigned sideload abuse | mitigate       | per-plugin consent in QSettings plugins/allowed/<uuid> + default-deny; tampered != unsigned in the verdict enum                                      |
| T-35-02-03       | Info disclosure / EoP | WS server exposure      | mitigate       | LocalHost bind (sd_plugin_server.cpp:93) + comment-aware CI grep gate (ci.yml:67) — both verified; code assert at test_sd_plugin_server.cpp:59       |
| T-35-02-04       | Info disclosure       | catalog phone-home      | accept (gated) | onlineCatalogEnabled opt-in + disabled sentinel (plugin_catalog_model.cpp:521); documented in the audit doc as gated, not URL-absent (RESEARCH A4)   |
| T-35-02-SC       | Tampering             | npm/pip/cargo installs  | accept         | No package installs this phase (RESEARCH: no external packages); the milestone gates assert no new wire/dep coupling                                 |
| \</threat_model> |                       |                         |                |                                                                                                                                                      |

<verification>
- ctest --preset linux-release -R "plugin-verify-gate|plugin-install|SdPluginServer" green (PLGSEC-01/02/03 invariants).
- bash scripts/verif-milestone-gates.sh exits 0 (VERIF-02 gates pass on the current tree).
- docs/milestone-v2.0-modularity-audit.md enumerates gate results + honest live-evidence reconciliation.
- No security source file (plugin_catalog_model.cpp / plugin_verify_gate.hpp / sd_plugin_server.cpp) was modified (verify-only).
</verification>

\<success_criteria>

- PLGSEC-01: tampered refused even with consent, unsigned consent-gated — confirmed green + verdict split test-locked.
- PLGSEC-02: per-plugin consent persists + survives the launch-sweep — existing :942 round-trip green.
- PLGSEC-03: WS server loopback-only, CI-gated, catalog opt-in — confirmed.
- VERIF-02: four milestone grep gates pass (comment-aware) and are scripted + CI-wired + documented in the audit doc.
  \</success_criteria>

<output>
Create `.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-02-SUMMARY.md` when done.
</output>
