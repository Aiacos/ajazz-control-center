---
phase: 35-windows-plugin-support-security-hardening-milestone-verifica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/src/plugin_manifest.hpp
  - src/app/src/plugin_manifest.cpp
  - src/app/src/plugin_manager.cpp
  - src/app/src/plugin_manager.hpp
  - src/plugins/include/ajazz/plugins/i_plugin_host.hpp
  - tests/unit/test_win_plugin_classification.cpp
  - tests/unit/CMakeLists.txt
  - .planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-ADR-windows-plugin-classification.md
autonomous: true
requirements: [WINPLG-01, WINPLG-02]
must_haves:
  truths:
    - A win-only .sdPlugin whose CodePath ends .js/.html (no PE binary in bundle) classifies as WsOnlyIpc
    - A win-only .sdPlugin whose CodePath ends .exe/.dll OR contains an MZ/PE-magic file classifies as VendorDll
    - A WS-only-IPC win-only manifest (os=[windows]) passes the platform gate and runs natively on Linux
    - PluginInfo carries the classification verdict so a downstream model renders a chip without re-scanning
    - The WINPLG-01 ADR documents the heuristic, the WS-only-runs-native decision, and the explicit Wine-launch deferral
  artifacts:
    - path: src/app/src/plugin_manifest.hpp
      provides: WinPluginClass enum + classifyWindowsPlugin() + supportsCurrentPlatform() declarations
      contains: classifyWindowsPlugin
    - path: src/app/src/plugin_manifest.cpp
      provides: classification heuristic (CodePath suffix + bounded PE-magic scan) + platform-support helper
      contains: classifyWindowsPlugin
    - path: tests/unit/test_win_plugin_classification.cpp
      provides: Catch2 classification + supportsCurrentPlatform cases
      contains: win-plugin-classification
    - path: .planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-ADR-windows-plugin-classification.md
      provides: WINPLG-01 feasibility-spike decision doc
      contains: WS-only-IPC
  key_links:
    - from: src/app/src/plugin_manager.cpp
      to: classifyWindowsPlugin / supportsCurrentPlatform
      via: scan-time call at the runnability gate (opt->sourceDir is the bundle dir)
      pattern: classifyWindowsPlugin|supportsCurrentPlatform
    - from: src/app/src/plugin_manager.cpp
      to: plugins::PluginInfo
      via: stamp the classification into the converted PluginInfo
      pattern: winClass
---

<objective>
Implement the WINPLG classification engine and native-run gate: a pure
classifyWindowsPlugin() helper (CodePath suffix + bounded PE-magic scan), a
supportsCurrentPlatform() helper that lets WS-only-IPC win-only plugins run
natively on Linux/macOS, the PluginInfo field carrying the verdict to the UI
tier, and the WINPLG-01 feasibility-spike ADR.

Purpose: WINPLG-01/02 are the only net-new feature in this phase. The status
chip in Plan 02 reads the verdict this plan stamps onto PluginInfo at scan time.
Classification MUST run at scan/install time and be cached — NOT lazily at launch
(locked CONTEXT decision).

Output: WinPluginClass enum, two free functions in plugin_manifest, a PluginInfo
classification field, scan-time wiring in PluginManager, a Catch2 suite, the ADR.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-RESEARCH.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-PATTERNS.md
@.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-VALIDATION.md

<interfaces>
Already-parsed manifest fields (DO NOT add a parser) — plugin_manifest.hpp:91-97:
  struct PluginManifest { std::vector<PluginOsRequirement> os; QString codePath; QString codePathWin; QString codePathMac; ... }
  PluginOsRequirement.platform is "windows" / "linux" / "mac".
  [[nodiscard]] bool manifestRunnableHere(PluginManifest const&, QString const& platform, QString const& appVer);  // :187
  [[nodiscard]] QString currentPlatformString();  // :197
Free-fn idiom to mirror — plugin_manifest.cpp:316-362 (never throws, [[nodiscard]], namespace ajazz::app).
The OS gate + LOCKED linux-accept block lives inside manifestRunnableHere at :326-350.

PluginInfo carrier — src/plugins/include/ajazz/plugins/i_plugin_host.hpp:46:
struct PluginInfo { std::string id; std::string name; std::string version; std::string authors;
std::vector<std::string> actionIds; std::vector<std::string> permissions; bool signed\_{false}; ... };
NOTE: no bundle-path field — classification MUST be stamped at scan time, it cannot be re-derived later.

Scan-time gate (opt->sourceDir is the bundle dir) — plugin_manager.cpp:278-289:
if (!manifestRunnableHere(\*opt, currentPlatformString(), QString::fromLatin1(kEmulatedSdVersion))) { continue; }
opt->sourceDir = m_pluginsDir + '/' + entry; // bundle dir available here
runnable.push_back(std::move(\*opt));
PluginManager::plugins() (plugin_manager.hpp:147-150) converts m_live manifest entries -> PluginInfo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WinPluginClass enum + classifyWindowsPlugin() + supportsCurrentPlatform() with Catch2 suite</name>
  <files>src/app/src/plugin_manifest.hpp, src/app/src/plugin_manifest.cpp, tests/unit/test_win_plugin_classification.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - src/app/src/plugin_manifest.hpp (lines 60-200 — PluginManifest struct + manifestRunnableHere/currentPlatformString declarations to mirror)
    - src/app/src/plugin_manifest.cpp (lines 316-372 — manifestRunnableHere free-fn idiom + OS gate + LOCKED linux-accept block; the exact analog)
    - tests/unit/test_plugin_manifest.cpp (TEST_CASE naming + manifest-fixture style, tag [plugin-manifest])
    - tests/unit/test_plugin_verify_gate.cpp (verdict-mapping test style)
    - tests/unit/CMakeLists.txt (how test_plugin_manifest.cpp is registered — add the new TU the same way)
  </read_first>
  <behavior>
    - classifyWindowsPlugin: no os entry, or os without "windows" -> NotWindowsOnly
    - classifyWindowsPlugin: os=[windows], CodePath ends .js/.html/.cjs, no PE-magic in bundle -> WsOnlyIpc
    - classifyWindowsPlugin: os=[windows], CodePath ends .exe/.dll -> VendorDll
    - classifyWindowsPlugin: os=[windows], CodePath ends .js BUT a bundle file begins with MZ -> VendorDll (corroborator overrides mislabeled manifest)
    - classifyWindowsPlugin: empty/non-existent bundleDir handled without crash (suffix alone classifies)
    - supportsCurrentPlatform: WsOnlyIpc -> true on every platform
    - supportsCurrentPlatform: VendorDll -> true only when platform=="windows" (wineDetected DEFERRED=false this phase -> false on linux/mac)
    - supportsCurrentPlatform: NotWindowsOnly -> false (caller still uses manifestRunnableHere)
  </behavior>
  <action>
    Add to plugin_manifest.hpp (mirror manifestRunnableHere declaration idiom at :187, namespace ajazz::app): enum class WinPluginClass { WsOnlyIpc, VendorDll, NotWindowsOnly }, and two [[nodiscard]] declarations classifyWindowsPlugin(PluginManifest const& m, QString const& bundleDir) and supportsCurrentPlatform(PluginManifest const& m, QString const& platform, WinPluginClass cls).

```
Implement in plugin_manifest.cpp next to manifestRunnableHere (never throw; pure). classifyWindowsPlugin: (1) os empty or no "windows" entry -> NotWindowsOnly. (2) Primary signal from the effective code path (codePathWin if non-empty else codePath): .exe/.dll suffix (case-insensitive) -> VendorDll. (3) Corroborator: QDirIterator over bundleDir files, read only the first 2 bytes of each code file; any file beginning with MZ (0x4D 0x5A) -> VendorDll. Bound it: 2-byte read cap per file + a file-count cap (~4096) to avoid a DoS — mirror the bounded-read discipline in plugin_catalog_model.cpp installFromFile (~:843). (4) Else -> WsOnlyIpc. NO full PE parse, NO execution — magic bytes only.

Implement supportsCurrentPlatform: WsOnlyIpc -> true regardless of platform; VendorDll -> platform == "windows" (Wine detection DEFERRED — leave an inline comment marking the WINPLG-03 deferral point, treat wineDetected as false); NotWindowsOnly -> false.

Create tests/unit/test_win_plugin_classification.cpp, ASCII-only TEST_CASE names tagged [win-plugin-classification], covering every behavior case. Build PluginManifest fixtures + a QTemporaryDir bundle with a 2-byte MZ file (QFile raw write) for the PE-magic case. Register the TU in tests/unit/CMakeLists.txt exactly as test_plugin_manifest.cpp.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release 2>&1 | tail -5 && ctest --preset linux-release -R "win-plugin-classification|plugin-manifest" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ctest --preset linux-release -R win-plugin-classification passes all new cases
    - ctest --preset linux-release -R plugin-manifest still green (no manifestRunnableHere regression)
    - grep -n "classifyWindowsPlugin\|supportsCurrentPlatform\|WinPluginClass" src/app/src/plugin_manifest.hpp shows the enum + both declarations
    - grep -rc "include.*nlohmann" src/core/include/ returns 0 (COD-031 unchanged)
  </acceptance_criteria>
  <done>WinPluginClass + classifyWindowsPlugin + supportsCurrentPlatform implemented, registered, locked by a green [win-plugin-classification] suite; plugin-manifest unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire classification at scan time onto PluginInfo + native-run gate; commit the WINPLG-01 ADR</name>
  <files>src/app/src/plugin_manager.cpp, src/app/src/plugin_manager.hpp, src/plugins/include/ajazz/plugins/i_plugin_host.hpp, .planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-ADR-windows-plugin-classification.md</files>
  <read_first>
    - src/app/src/plugin_manager.cpp (lines 250-300 — the manifestRunnableHere gate at :278 + opt->sourceDir at :287; native-run override + classify call go here)
    - src/app/src/plugin_manager.hpp (lines 145-155 — plugins() converts m_live entries to PluginInfo; where to stamp winClass)
    - src/plugins/include/ajazz/plugins/i_plugin_host.hpp (lines 40-70 — PluginInfo struct; keep Qt-free + nlohmann-free)
    - .planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md (ADR structure: # ADR / Status / Context / Decision / Consequences)
  </read_first>
  <action>
    Extend plugins::PluginInfo (i_plugin_host.hpp) with int winClass{0}; documented as a WinPluginClass cache (0=NotWindowsOnly, 1=WsOnlyIpc, 2=VendorDll). Keep the int<->enum mapping explicit in the doc comment so Plan 02's model derive mirrors it. Use a plain int to avoid a cross-tier enum dependency (no Qt/nlohmann in this header).

```
In plugin_manager.cpp at the runnability gate (:278): keep manifestRunnableHere(...) as the base gate, then add the WINPLG-02 native-run override — compute the bundle dir once (same m_pluginsDir + '/' + entry as :287), call classifyWindowsPlugin(*opt, bundleDir), and accept when manifestRunnableHere(...) || supportsCurrentPlatform(*opt, currentPlatformString(), cls). Do NOT weaken the strict reject for VendorDll. Carry the computed class with the runnable entry (stash in m_live or a uuid->class map) so plugins() can stamp it without re-scanning.

In PluginManager::plugins(): set info.winClass to the stashed class (mapped to the int) so the verdict reaches LoadedPluginsModel without a re-scan — resolves the PATTERNS residual risk (PluginInfo has no bundle path for a live PE scan). Default 0 when no class stashed.

Author .planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-ADR-windows-plugin-classification.md mirroring the 30-ADR structure. Document: (1) the heuristic (CodePath suffix primary + bounded MZ/PE-magic corroborator), (2) WS-only-IPC runs natively via supportsCurrentPlatform (no Wine), (3) the EXPLICIT WINPLG-03 Wine-launch DEFERRAL (no-hardware rationale + never-bundle-Wine anti-decision), (4) consequence: VendorDll surfaces as a chip only this phase.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release 2>&1 | tail -5 && ctest --preset linux-release -R "plugin-manifest|win-plugin-classification|plugin-manager" --output-on-failure && grep -q "WS-only-IPC" .planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-ADR-windows-plugin-classification.md && echo ADR_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "winClass" src/plugins/include/ajazz/plugins/i_plugin_host.hpp shows the field with the documented int mapping
    - grep -n "classifyWindowsPlugin\|supportsCurrentPlatform" src/app/src/plugin_manager.cpp shows the scan-time call + native-run override at the gate
    - grep -n "winClass" src/app/src/plugin_manager.cpp shows plugins() stamping the verdict
    - grep -c "nlohmann\|#include <Q" src/plugins/include/ajazz/plugins/i_plugin_host.hpp returns 0 (plugins-tier header stays Qt/nlohmann-free)
    - the ADR exists, contains "WS-only-IPC" and an explicit deferred Wine-launch section
    - full ctest --preset linux-release green
  </acceptance_criteria>
  <done>PluginInfo carries winClass; the scan-time gate classifies + lets WS-only-IPC win plugins run natively (WINPLG-02); plugins() stamps the verdict; the WINPLG-01 ADR is committed. Full suite green.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                      |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------ |
| untrusted .sdPlugin bundle on disk -> classifier | Plugin bundle contents (manifest + code files) are untrusted input read by classifyWindowsPlugin |
| classifier -> spawn-eligibility gate             | The classification verdict influences whether a plugin is accepted to run natively               |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                                                          |
| ---------------- | ---------------------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-35-01-01       | Tampering              | classifyWindowsPlugin bundle scan              | mitigate    | Read only the first 2 magic bytes per file; never parse a full PE; never execute any bundle file — magic bytes only (Pattern 3 bounded-read discipline)                                                                  |
| T-35-01-02       | DoS                    | classifyWindowsPlugin bundle scan              | mitigate    | Bounded per-file read (2 bytes) + file-count cap (~4096); bundle is already extracted + zip-slip-guarded upstream, no archive recursion                                                                                  |
| T-35-01-03       | Elevation of Privilege | WINPLG-02 native-run override                  | mitigate    | supportsCurrentPlatform accepts ONLY WsOnlyIpc cross-platform; VendorDll stays strict-rejected off Windows (no in-process PE loader — permanently rejected); Wine launch DEFERRED so zero Wine attack surface this phase |
| T-35-01-04       | Tampering              | mislabeled manifest (declares .js, ships .dll) | mitigate    | PE-magic corroborator reclassifies to VendorDll regardless of the declared CodePath suffix                                                                                                                               |
| \</threat_model> |                        |                                                |             |                                                                                                                                                                                                                          |

<verification>
- Full ctest --preset linux-release green (~785+ cases) after Task 2.
- [win-plugin-classification] covers WsOnlyIpc / VendorDll / NotWindowsOnly + PE-magic-overrides-suffix + supportsCurrentPlatform cross-platform behavior.
- COD-031 preserved: grep -rc "include.*nlohmann" src/core/include/ = 0; plugins-tier header stays Qt-free.
- The WINPLG-01 ADR committed under the phase dir.
</verification>

\<success_criteria>

- WINPLG-01: classifyWindowsPlugin distinguishes WS-only-IPC vs vendor-DLL via CodePath suffix + PE-magic; the ADR documents heuristic + deferral.
- WINPLG-02: a WS-only-IPC win-only manifest (os=[windows]) passes the gate and runs natively on Linux; VendorDll stays rejected.
- PluginInfo.winClass carries the verdict to the UI tier (consumed by Plan 02) with no re-scan.
  \</success_criteria>

<output>
Create `.planning/phases/35-windows-plugin-support-security-hardening-milestone-verifica/35-01-SUMMARY.md` when done.
</output>
