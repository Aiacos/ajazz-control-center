---
phase: 11-ajazz-2-4g-8k-mouse-probe-and-confirm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/protocols/mouse/aj_series.md
  - src/devices/mouse/src/aj_series_protocol.hpp
  - src/devices/mouse/src/aj_series.cpp
  - tests/unit/test_aj_series_wire_format.cpp
autonomous: true
requirements:
  - MOUSE-01
deferred_followups:
  - 'Build precondition: C++ configure currently fails (Qt6 CorePrivate / qzipreader_p.h missing -> needs qt6-qtbase-private-devel). Resolve in the environment (not the project tree, per CLAUDE.md) before any cmake/ctest step here.'
  - 'CAPTURE-PENDING (P0 AJ-series reconciliation): the V1.0 (OemDrv, 17-byte) vs Max (HIDUsb, 20-byte) envelope choice for 3151:5007 resolves only when the Phase 9.x AJ-series capture + diff doc lands (CAPTURE-05/06). This plan documents BOTH candidate envelopes and installs the switch-point; it does NOT pick one. makeAjSeries factory split (Pattern B / makeAjazz24g8k) is gated on that capture and is therefore SPECULATIVE until then.'
  - Real-hardware round-trip witness needs a physical 3151:5007 (out of agent scope per CLAUDE.md). Promotion past `partial` is gated on it.

must_haves:
  truths:
    - docs/protocols/mouse/aj_series.md carries an explicit 3151:5007 AJ199 V1.0-vs-Max reconciliation section that names both candidate envelopes (OemDrv 17-byte, HIDUsb 20-byte) and states the choice is UNRESOLVED pending the Phase 9.x capture.
    - The wire-format reconciliation status is encoded in code as a single named CAPTURE-PENDING switch-point that downstream encoder work (DPI/poll/LOD/RGB) routes through, so no setter speculatively bakes an envelope.
    - The doc records that the documented opcode map (0x21/0x22/0x23/0x30) and the currently-shipped backend opcode map (0x04/0x54/0x07/0x53) DIVERGE, and that the capture decides which is correct for 3151:5007.
  artifacts:
    - path: docs/protocols/mouse/aj_series.md
      provides: 3151:5007 V1.0-vs-Max reconciliation section + opcode-divergence note + capture-pending status
      contains: CAPTURE-PENDING
    - path: src/devices/mouse/src/aj_series_protocol.hpp
      provides: Named capture-pending envelope switch-point enum/constant the encoders consult
      contains: CAPTURE-PENDING
  key_links:
    - from: src/devices/mouse/src/aj_series.cpp
      to: src/devices/mouse/src/aj_series_protocol.hpp
      via: the capture-pending envelope switch-point constant, consulted before any 3151:5007 byte-level encode
      pattern: CAPTURE.PENDING|EnvelopeVariant|kAjEnvelope
---

<objective>
Establish the MOUSE-01 wire-format reconciliation footing for `ajazz_24g_8k` (`3151:5007`):
extend `docs/protocols/mouse/aj_series.md` with the AJ199 V1.0-vs-Max envelope reconciliation
for this PID, and install a single named `// CAPTURE-PENDING` switch-point in
`aj_series_protocol.hpp` that every downstream byte-level encoder (DPI/poll/LOD/RGB plans)
routes through — so NO setter speculatively bakes an envelope.

Purpose: This is the load-bearing honesty footing for the whole phase. The current
`aj_series.cpp` / `aj_series_protocol.hpp` ship a 65-byte ReportId-0x05 envelope with an AJ159
APEX vendor opcode map (`0x04` poll / `0x54` DPI table / `0x07` LED / `0x53` omnibus) that
matches NEITHER the documented `aj_series.md` envelope (opcodes `0x21`/`0x22`/`0x23`/`0x30`)
NOR a confirmed `3151:5007` capture. CONTEXT.md names this the P0 "AJ-series wire-format
reconciliation" divergence: V1.0 (OemDrv, 17-byte) vs Max (HIDUsb, 20-byte). This plan does
NOT resolve it — it documents both candidates and installs the switch-point so the resolution
is a one-place edit when the Phase 9.x capture lands.

Output: an extended protocol doc + a single capture-pending switch-point constant downstream
plans consult, with a wire-format regression test pinning that the switch-point exists and
defaults to "unresolved" (does not silently commit to an envelope).

CONDITIONAL-EXECUTION GATES (read before executing — this is a plan-only artifact):

1. **Build precondition.** The C++ tree currently fails to configure: Qt6 `CorePrivate` /
   `qzipreader_p.h` is missing and needs `qt6-qtbase-private-devel`. Do NOT attempt any
   `cmake`/`ctest` step until that package is installed in the environment (an environment fix,
   NOT a project-tree mutation, per CLAUDE.md hard rules).
1. **CAPTURE-PENDING (P0 reconciliation).** Per CONTEXT.md the V1.0-vs-Max envelope choice for
   `3151:5007` resolves only when the Phase 9.x AJ-series capture + diff doc (CAPTURE-05/06)
   lands. This plan documents BOTH candidate envelopes and installs the switch-point; it MUST
   NOT pick one. The `makeAjSeries` -> `makeAjazz24g8k` Pattern B factory split is gated on that
   capture and is SPECULATIVE until then — do NOT split the factory in this plan; record the
   split CONDITION in the doc instead.
   </objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ajazz-2-4g-8k-mouse-probe-and-confirm/11-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/protocols/mouse/aj_series.md
@CLAUDE.md

<interfaces>
<!-- The reconciliation footing the downstream plans (11-02 DPI, 11-03 poll+RGB) consult.
     Extracted from the codebase so the executor needs no exploration. -->

From src/devices/mouse/src/aj_series_protocol.hpp (CURRENT shipped envelope — the divergent one):

- inline constexpr std::size_t kReportSize = 65; // 1 ReportID + 64 vendor bytes
- inline constexpr std::uint8_t kReportId = 0x05;
- enum class FeaCmd : std::uint8_t {
  SetReport = 0x04, // poll rate (current backend) — DIVERGES from documented 0x22
  SetLedParam = 0x07, // LED (current backend) — DIVERGES from documented 0x30
  MouseSetOption0 = 0x53, // LOD/omnibus(current backend) — DIVERGES from documented 0x23
  MouseSetOption1 = 0x54, // DPI table (current backend) — DIVERGES from documented 0x21
  SetTftLcdData = 0x25, // TFT chunked RGB565 (clock face)
  ... };
- void stampBit7Checksum(std::array\<std::uint8_t, kReportSize>& pkt) noexcept;

From docs/protocols/mouse/aj_series.md (DOCUMENTED envelope — 64-byte, the OTHER candidate):

- byte 0 report id 0x05, byte 1 command id, byte 2 sub-command, byte 3 payload length,
  byte 63 checksum = sum(bytes 1..62) mod 256.
- Command ids: 0x21 DPI, 0x22 Poll rate, 0x23 Lift-off, 0x30 RGB, 0x50 Commit.

These two opcode maps are mutually contradictory. The capture decides which is real for 3151:5007.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend aj_series.md with the 3151:5007 V1.0-vs-Max reconciliation section</name>
  <files>docs/protocols/mouse/aj_series.md</files>
  <action>
Per MOUSE-01, add a new top-level section to `docs/protocols/mouse/aj_series.md` titled
"3151:5007 (ajazz_24g_8k) wire-format reconciliation — CAPTURE-PENDING". The section MUST:

1. State the open question plainly: whether `3151:5007` uses the AJ199 V1.0 envelope (OemDrv,
   17-byte payload window) or the AJ199 Max envelope (HIDUsb, 20-byte payload window), and that
   this is the P0 reconciliation item carried from CONTEXT.md.
1. Tabulate BOTH candidate envelopes side-by-side: report id, payload-length-byte position,
   payload window size (17 vs 20), checksum rule. Do NOT assert which one is correct.
1. Record the THIRD divergence explicitly: the current shipped backend
   (`aj_series_protocol.hpp` FeaCmd) uses opcodes `0x04` (poll) / `0x54` (DPI table) / `0x07`
   (LED) / `0x53` (omnibus LOD), whereas this doc's documented opcode table uses `0x21` (DPI) /
   `0x22` (poll) / `0x23` (LOD) / `0x30` (RGB). Note that the capture decides which opcode map is
   real for `3151:5007` and that the documented 0x21/0x22/0x23/0x30 map is what Phase 11 plans
   against pending confirmation.
1. Record the factory-split CONDITION (not the action): "IF the capture confirms the Max
   (HIDUsb-20B) envelope materially diverges from the shipped backend, `makeAjSeries` splits per
   Pattern B into a dedicated `makeAjazz24g8k` factory entry; IF V1.0 holds, the existing factory
   shape is preserved." Mark the whole section status `CAPTURE-PENDING` and cross-reference
   Phase 9.x CAPTURE-05/06 as the resolving artifact.

Keep the existing doc content intact; this is an additive section. No code change in this task.
</action>
<verify>
<automated>grep -q "CAPTURE-PENDING" docs/protocols/mouse/aj_series.md && grep -qi "17-byte" docs/protocols/mouse/aj_series.md && grep -qi "20-byte" docs/protocols/mouse/aj_series.md && grep -q "makeAjazz24g8k" docs/protocols/mouse/aj_series.md</automated>
</verify>
<done>aj_series.md has an additive CAPTURE-PENDING 3151:5007 section naming both candidate envelopes (17B/20B), the opcode-map divergence, and the conditional factory split. No envelope is asserted as correct.</done>
</task>

<task type="auto">
  <name>Task 2: Install the named CAPTURE-PENDING envelope switch-point + pin it with a test</name>
  <files>
    src/devices/mouse/src/aj_series_protocol.hpp,
    src/devices/mouse/src/aj_series.cpp,
    tests/unit/test_aj_series_wire_format.cpp
  </files>
  <action>
Per MOUSE-01 (the in-code half — install the switch-point downstream plans route through; do NOT
pick an envelope):

1. In `aj_series_protocol.hpp`, add an `enum class AjEnvelopeVariant : std::uint8_t { Unresolved, V1_0_OemDrv17, Max_HidUsb20 };` and an `inline constexpr AjEnvelopeVariant kAj24g8kEnvelope = AjEnvelopeVariant::Unresolved;` constant. Immediately above it place a multi-line comment block
   beginning `// CAPTURE-PENDING (MOUSE-01 / P0 AJ-series reconciliation):` that explains: this
   constant is the single switch-point for the 3151:5007 byte-level envelope; downstream encoders
   (DPI/poll/LOD/RGB) MUST consult it and MUST NOT hardcode an envelope; it stays `Unresolved`
   until the Phase 9.x capture (CAPTURE-05/06) confirms V1.0 vs Max; flipping this one constant +
   the diff doc is the entire resolution. Do NOT change any existing FeaCmd value or builder body
   in this task — the existing AJ159 APEX builders are untouched (they belong to other SKUs).
1. In `aj_series.cpp`, add a single file-scope reference comment near the `AjSeriesMouse` class
   doc noting that 3151:5007 setter encoders are gated on `aj_series::kAj24g8kEnvelope` and that
   while it is `Unresolved` the documented (0x21/0x22/0x23/0x30) opcode path is planned-against but
   not committed. No behavioral change in this task — this is the honesty anchor the DPI/poll/RGB
   plans build on. (Use `qsizetype` for any QString/container index if introduced; no `nlohmann`.)
1. In `test_aj_series_wire_format.cpp`, add a TEST_CASE with an ASCII-only name and tag
   `[mouse][aj_series][wire][reconciliation]` that REQUIREs `kAj24g8kEnvelope == AjEnvelopeVariant::Unresolved`. This pins the honesty contract: a future change that
   speculatively commits to V1.0 or Max WITHOUT a capture fails the test loudly. Add a comment in
   the test body: when the capture lands, this assertion flips to the confirmed variant in the
   same commit as the encoder migration — never before.
   </action>
   <verify>
   <automated>grep -q "CAPTURE-PENDING" src/devices/mouse/src/aj_series_protocol.hpp && grep -q "AjEnvelopeVariant::Unresolved" src/devices/mouse/src/aj_series_protocol.hpp && grep -q "kAj24g8kEnvelope" src/devices/mouse/src/aj_series.cpp && grep -q "AjEnvelopeVariant::Unresolved" tests/unit/test_aj_series_wire_format.cpp</automated>
   </verify>
   <done>A named `kAj24g8kEnvelope` switch-point defaults to `Unresolved`; aj_series.cpp references it as the encoder gate; a wire-format test pins the Unresolved default so no speculative envelope commit can land silently. (ctest gated on the Qt6 CorePrivate build fix — see objective gate 1.)</done>
   </task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                    |
| ---------------------------------- | ------------------------------------------------------------------------------ |
| host application -> mouse firmware | configuration opcodes cross the USB HID write boundary to 3151:5007 firmware   |
| documented-vs-shipped opcode map   | two contradictory wire-format models coexist until the capture reconciles them |

## STRIDE Threat Register

| Threat ID        | Category  | Component                                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                     |
| ---------------- | --------- | ------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-11-01          | Spoofing  | Synthesising a fake wire envelope (V1.0 vs Max) before the capture confirms it | accept      | Pitfall 19 honesty contract: the `kAj24g8kEnvelope` switch-point defaults to `Unresolved`; a test pins it; the doc names both candidates without asserting one. Resolution is a single edit when the capture lands. |
| T-11-02          | Tampering | A future setter hardcoding an opcode/envelope, bypassing the switch-point      | mitigate    | Downstream plans (11-02/11-03) route every 3151:5007 encode through `kAj24g8kEnvelope`; the Unresolved test asserts no silent commitment.                                                                           |
| T-11-SC          | Tampering | npm/pip/cargo installs                                                         | mitigate    | No new package-manager installs in this plan (doc + header constant + test only). No `[ASSUMED]`/`[SUS]` packages introduced.                                                                                       |
| \</threat_model> |           |                                                                                |             |                                                                                                                                                                                                                     |

<verification>
1. `grep -q "CAPTURE-PENDING" docs/protocols/mouse/aj_series.md` matches; both 17-byte and 20-byte candidates documented.
2. `grep -q "AjEnvelopeVariant::Unresolved" src/devices/mouse/src/aj_series_protocol.hpp` matches; the switch-point exists and defaults Unresolved.
3. `aj_series.cpp` references `kAj24g8kEnvelope` as the encoder gate.
4. `test_aj_series_wire_format.cpp` has the reconciliation TEST_CASE pinning the Unresolved default.
5. (Gated on build fix) `ctest --preset linux-release -R "reconciliation"` passes once `qt6-qtbase-private-devel` is installed.
6. COD-031 grep gate: `grep -rn nlohmann src/core/include/` returns 0.
</verification>

\<success_criteria>

- MOUSE-01: `aj_series.md` extended with the 3151:5007 V1.0-vs-Max reconciliation + opcode-divergence note; the in-code `kAj24g8kEnvelope` switch-point installed, defaulting to `Unresolved`; factory split recorded as a CONDITION, NOT performed.
- No envelope is speculatively committed (Pitfall 19 honesty contract held; pinned by test).
- One or more atomic Conventional Commits (doc + switch-point may be one `docs+feat` pair or split per atomicity).
  \</success_criteria>

<output>
After completion, create `.planning/phases/11-ajazz-2-4g-8k-mouse-probe-and-confirm/11-01-SUMMARY.md`
documenting: the reconciliation doc section added, the `kAj24g8kEnvelope` switch-point + its
`Unresolved` default, the test that pins it, and an explicit note that the V1.0-vs-Max envelope
choice + the `makeAjazz24g8k` factory split remain CAPTURE-PENDING on Phase 9.x CAPTURE-05/06 —
including whether the build fix (`qt6-qtbase-private-devel`) was available at execution time or
the C++ verification steps were deferred.
</output>
