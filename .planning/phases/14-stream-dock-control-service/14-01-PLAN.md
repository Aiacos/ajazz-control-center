---
phase: 14-stream-dock-control-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/devices/streamdeck/src/register.cpp
  - tests/unit/test_register_akp05e_clock.cpp
  - tests/unit/CMakeLists.txt
  - docs/_data/devices.yaml
autonomous: true
requirements: [DEVICES-11]
must_haves:
  truths:
    - The akp05e device descriptor advertises hasClock == false.
    - The ak980pro descriptor is unchanged (still hasClock == true).
    - A unit test asserts the akp05e descriptor's hasClock value so the honesty fix cannot silently regress.
  artifacts:
    - path: src/devices/streamdeck/src/register.cpp
      provides: akp05e descriptor row with hasClock=false (line ~305)
      contains: .codename = "akp05e"
    - path: tests/unit/test_register_akp05e_clock.cpp
      provides: Descriptor honesty assertion for DEVICES-11
      min_lines: 20
  key_links:
    - from: tests/unit/test_register_akp05e_clock.cpp
      to: ajazz::streamdeck::registerAll / DeviceRegistry::enumerate
      via: enumerate() then find codename==akp05e and assert !hasClock
      pattern: hasClock
---

<objective>
Make the `akp05e` (`0x0300:0x3004`) device descriptor honest: the Stream Dock
family has no firmware RTC (ARCH-05), so `hasClock` must be `false`. This closes
Phase-10 UAT #6 (a phantom "Sync time" affordance on a clockless device) and is
the single-line runtime source-of-truth fix for DEVICES-11.

Purpose: The runtime Sync-button visibility reads the device descriptor, not the
doc-only `devices.yaml`. Flipping the descriptor is what actually removes the
clock affordance. A regression test pins the value so a future descriptor edit
cannot silently re-enable it.

Output:

- `register.cpp` akp05e row `hasClock = false`.
- New `tests/unit/test_register_akp05e_clock.cpp` asserting it (and that
  `ak980pro` is untouched).
- Optional doc-honesty: prune the doc-only `clock` capability from the akp05e
  row in `docs/_data/devices.yaml` so the doc surface matches runtime.
  </objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-stream-dock-control-service/14-CONTEXT.md
@.planning/phases/14-stream-dock-control-service/14-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Extracted from src/devices/streamdeck/src/register.cpp this session — use directly. -->

The akp05e descriptor row (register.cpp ~294-307), the DEVICES-11 target is the
`.hasClock` line at ~305:

```
reg.registerDevice(
    core::DeviceDescriptor{
        .vendorId = akp05::VendorId, // 0x0300
        .productId = Akp05ePid,      // 0x3004
        ...
        .codename = "akp05e",
        ...
        .hasClock = true, // A-03 / D-03: shares Akp05Device backend ...  <-- flip to false
    },
    &makeAkp05);
```

DeviceDescriptor is in src/core/include/ajazz/core/device_registry.hpp (codename
is std::string, hasClock is bool). DeviceRegistry::enumerate() returns
std::vector<DeviceDescriptor>. ajazz::streamdeck::registerAll(DeviceRegistry&)
populates the Stream Deck rows. The ak980pro descriptor lives in the mouse/AK
register path (NOT this file row) and must remain hasClock=true (ARCH-05.1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip akp05e hasClock to false (DEVICES-11)</name>
  <files>src/devices/streamdeck/src/register.cpp, docs/_data/devices.yaml</files>
  <read_first>
    - src/devices/streamdeck/src/register.cpp (the full akp05/mirabox_n4/akp05e block ~256-308)
    - .planning/phases/14-stream-dock-control-service/14-RESEARCH.md (Open Question 1: row scope; Pitfall 6: wrong row)
    - CLAUDE.md (RE source-of-truth; atomic Conventional Commits)
  </read_first>
  <action>
    In register.cpp, change ONLY the `akp05e` descriptor row (`.codename = "akp05e"`,
    `.productId = Akp05ePid` // 0x3004, line ~305): set `.hasClock = false` and update
    the trailing comment to cite DEVICES-11 / ARCH-05 (no Stream Dock firmware RTC).
    Do NOT touch the `akp05` (provisional, ~262) or `mirabox_n4` (~279) rows — DEVICES-11
    names akp05e only; the wider Stream Dock family flip is deferred to a later honesty
    sweep (Open Question 1; surface it as a one-line follow-up note in the SUMMARY, do
    not act on it here). Do NOT touch any `ak980pro` row (ARCH-05.1 keeps it true; Pitfall 6).
    Then, in docs/_data/devices.yaml, locate the `akp05e` entry's `capabilities` array
    (~line 273) and remove the `clock` entry so the doc surface matches runtime — this is
    doc-only (drives README/wiki AUTOGEN) and non-destructive. Keep this in the SAME atomic
    commit as the register.cpp flip (one honesty fix). If the yaml `clock` entry is already
    absent, note that in the SUMMARY and leave the yaml unchanged.
  </action>
  <verify>
    <automated>grep -n 'codename = "akp05e"' src/devices/streamdeck/src/register.cpp && grep -A12 'codename = "akp05e"' src/devices/streamdeck/src/register.cpp | grep -v '^#' | grep -c 'hasClock = false'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A12 'codename = "akp05e"' src/devices/streamdeck/src/register.cpp` shows `.hasClock = false`.
    - `grep -A12 'codename = "mirabox_n4"' src/devices/streamdeck/src/register.cpp` still shows `.hasClock = true` (unchanged).
    - `grep -A12 'codename = "akp05"' ...` (the provisional row) still shows `.hasClock = true` (unchanged).
    - No `ak980pro` row is modified anywhere in the diff (`git diff --stat` lists only register.cpp + devices.yaml).
    - In docs/_data/devices.yaml the akp05e `capabilities` array no longer lists `clock` (or the SUMMARY records it was already absent).
  </acceptance_criteria>
  <done>akp05e descriptor advertises hasClock=false at runtime; mirabox_n4/akp05/ak980pro untouched; devices.yaml doc surface matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin DEVICES-11 with a descriptor honesty test</name>
  <files>tests/unit/test_register_akp05e_clock.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - tests/unit/CMakeLists.txt (lines ~1-90 for the streamdeck device test targets; ~145-200 for the source-link pattern)
    - tests/unit/test_akp05_protocol.cpp (Catch2 + streamdeck registerAll usage pattern; ASCII-only TEST_CASE titles)
    - src/core/include/ajazz/core/device_registry.hpp (DeviceRegistry::enumerate / registerDevice / DeviceDescriptor fields)
    - src/devices/streamdeck/src/register.cpp (the registerAll entry point name + signature)
  </read_first>
  <behavior>
    - Test: after `ajazz::streamdeck::registerAll(registry)`, enumerate() contains a descriptor with codename "akp05e" whose hasClock == false.
    - Test: the same enumeration's "mirabox_n4" descriptor still has hasClock == true (guards against an over-broad flip).
  </behavior>
  <action>
    Create tests/unit/test_register_akp05e_clock.cpp: include Catch2 + the streamdeck
    register header (match the include path used by test_akp05_protocol.cpp), construct a
    `core::DeviceRegistry`, call `ajazz::streamdeck::registerAll(registry)`, call
    `registry.enumerate()`, find the entry with `codename == "akp05e"` and `REQUIRE` it
    exists, then `CHECK(!entry.hasClock)`. Add a second `CHECK` that the `mirabox_n4`
    entry's `hasClock` is still true. Use ASCII-only TEST_CASE/SECTION titles and a
    `[stream-dock-control]` (and `[register]`) tag (Pitfall 5; Win32 codepage). Register
    the new test in tests/unit/CMakeLists.txt following the existing add_executable/source
    list pattern — link the streamdeck device library/source the way test_akp05_protocol.cpp
    does (do NOT pull the full app target). Confirm the exact `registerAll` symbol/namespace
    against register.cpp before writing the include.
  </action>
  <verify>
    <automated>cmake --preset linux-release >/dev/null 2>&1; cmake --build --preset linux-release >/dev/null 2>&1 && ctest --preset linux-release -R akp05e_clock --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R akp05e_clock` runs at least one test case and reports 100% passed.
    - The test fails (RED) if `.hasClock` is reverted to true in register.cpp — confirm by a local mental check; do not commit a reverted register.cpp.
    - TEST_CASE / SECTION titles are ASCII-only (no em-dash, no arrow).
    - `ctest --preset linux-release` full suite stays green (no collateral breakage).
  </acceptance_criteria>
  <done>A green, regex-discoverable unit test asserts akp05e hasClock==false and mirabox_n4 hasClock==true.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                             |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| static descriptor table -> UI affordance | An incorrect capability flag exposes a non-functional control (the Sync button). No untrusted input crosses here — the descriptor is compile-time data. |

## STRIDE Threat Register

| Threat ID | Category    | Component                   | Disposition | Mitigation Plan                                                                                                       |
| --------- | ----------- | --------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-14a-01  | Tampering   | register.cpp descriptor row | mitigate    | Regression test pins akp05e hasClock==false so an edit cannot silently re-enable a phantom clock affordance (Task 2). |
| T-14a-02  | Repudiation | wrong-row edit              | accept      | Diff is one file + doc; CHECK on mirabox_n4 hasClock==true detects an over-broad flip. Low value, code-only change.   |

No network, auth, or untrusted-input surface in this plan (V5 input validation N/A — compile-time data).
\</threat_model>

<verification>
- `ctest --preset linux-release -R akp05e_clock` green.
- `git diff --stat` lists only `register.cpp`, `devices.yaml`, the new test, and CMakeLists.txt.
- `ak980pro` appears nowhere in the diff.
- Full `ctest --preset linux-release` suite green.
</verification>

\<success_criteria>

- akp05e descriptor `hasClock == false` at runtime (DEVICES-11).
- mirabox_n4 / akp05 / ak980pro rows unchanged.
- New honesty test green and regex-discoverable.
- devices.yaml akp05e capability list no longer lists `clock` (or SUMMARY notes it was absent).
  \</success_criteria>

<output>
Create `.planning/phases/14-stream-dock-control-service/14-01-SUMMARY.md` when done.
Record: the exact register.cpp line changed, whether devices.yaml needed editing, and
a one-line follow-up note on Open Question 1 (whether the akp05/mirabox_n4/other Stream
Dock rows should also flip in a later honesty sweep).
</output>
