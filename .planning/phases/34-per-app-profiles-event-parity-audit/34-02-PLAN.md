---
phase: 34-per-app-profiles-event-parity-audit
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - docs/plugin-event-parity.md
  - tests/unit/willappear_payload_test.cpp
  - tests/unit/controller_token_test.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-04]
must_haves:
  truths:
    - docs/plugin-event-parity.md lists every OpenDeck events/outbound + inbound + Elgato SDK event marked supported/partial/missing/hardware-gated with a test reference per supported event
    - A Catch2 test asserts the willAppear envelope+payload carries action/context/device/event and payload{row,column,controller,state,isInMultiAction} with a normalized controller token
    - An audit confirms no code path emits the literal Knob on the wire; every emission uses Encoder; the Knob->Encoder mapping is recorded in the parity doc
  artifacts:
    - path: docs/plugin-event-parity.md
      provides: EVENT-01 coverage deliverable
      contains: willAppear
    - path: tests/unit/willappear_payload_test.cpp
      provides: EVENT-02 payload/envelope completeness assertion
      contains: willAppear
    - path: tests/unit/controller_token_test.cpp
      provides: EVENT-04 Knob->Encoder normalization audit test
      contains: Encoder
  key_links:
    - from: tests/unit/willappear_payload_test.cpp
      to: plugin_device_bridge instancePayload/eventEnvelope
      via: envelope+payload shape assertions
      pattern: isInMultiAction
    - from: docs/plugin-event-parity.md
      to: SdPluginServer / PluginDeviceBridge events
      via: per-event supported/test-reference table
      pattern: supported|partial|missing|hardware-gated
---

<objective>
Deliver the EVENT audit track that is independent of the watcher: the `docs/plugin-event-parity.md` coverage table (EVENT-01), the `willAppear` payload-completeness Catch2 test (EVENT-02), and the `Knob`<->`Encoder` controller-token normalization audit + test (EVENT-04).

Purpose: lock the existing event surfaces against the OpenDeck/Elgato contract with a verification deliverable and tests, so plugin handlers never silently mismatch. This is mostly audit + assertion of code that already exists.
Output: one committed doc + two Catch2 test files registered in ctest.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-VALIDATION.md

<interfaces>
<!-- The EXACT existing shapes EVENT-02 asserts. Read the bridge before writing the test. -->
From src/app/src/plugin_device_bridge.cpp:311 (instancePayload) + :374 (eventEnvelope) + :1213 (emit):
  - instancePayload(ctx) -> { settings, coordinates:{row,column}, controller, state, isInMultiAction }
  - eventEnvelope(event, ctx, payload) -> { event, action(=ctx.actionUUID), context(=deriveContextId), device, payload }
  - emit: m_server->sendEvent(owner, eventEnvelope("willAppear", ctx, instancePayload(ctx)))
From src/app/src/plugin_manifest.cpp:144:
  - token == "Knob" || token == "Encoder" -> encoder bit (the ONLY sanctioned Knob acceptance, at the manifest boundary)
From src/app/src/plugin_device_bridge.cpp (Encoder emission sites):
  - byCoord lookups + registration + switch arms all use QStringLiteral("Encoder") on the wire.
From docs/protocols/streamdeck/akp_plugin_sdk.md:263-286:
  - event catalogue (willAppear/dialRotate/touchTap/systemDidWakeUp/applicationDidLaunch payloads; Knob vs Encoder vocabulary).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/plugin-event-parity.md coverage table (EVENT-01)</name>
  <files>docs/plugin-event-parity.md</files>
  <read_first>
    - docs/protocols/streamdeck/akp_plugin_sdk.md (the in-repo event catalogue, lines ~263-286)
    - src/app/src/sd_plugin_server.cpp (kRoutedActions inbound list ~line 412-424; sendEvent ~534)
    - src/app/src/plugin_device_bridge.cpp (the willAppear/willDisappear/dialRotate/dialDown/dialUp/touch emission sites)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md (EVENT-01 row + Sources)
  </read_first>
  <action>
    Create docs/plugin-event-parity.md as a coverage table cross-referencing our SdPluginServer/PluginDeviceBridge surfaces against the OpenDeck events/inbound + events/outbound sets and the Elgato Stream Deck SDK event list. For every event give: name, direction (inbound/outbound), status (one of supported / partial / missing / hardware-gated), the implementing file:symbol where supported, and a test reference (the ctest -R filter or test name) for each supported event. Record the Knob->Encoder mapping note (manifest accepts Knob; the wire uses Encoder) cross-linked to EVENT-04. Mark real encoder/touch input wire decode as hardware-gated (demo unit delivers zero input per STATE.md). Mark applicationDidLaunch/Terminate, systemDidWakeUp, switchToProfile per their Phase 34 status and reference the EVENT-03/APROF-04 test names landing in Plans 02/04. Keep it ASCII-only. State at the top that this doc is a living verification deliverable kept current.
  </action>
  <verify>
    <automated>test -f docs/plugin-event-parity.md && grep -qi "willAppear" docs/plugin-event-parity.md && grep -Eqi "supported|partial|missing|hardware-gated" docs/plugin-event-parity.md && grep -qi "Knob" docs/plugin-event-parity.md && echo DOC-OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/plugin-event-parity.md exists and contains a per-event table with status values from {supported, partial, missing, hardware-gated}.
    - Every event in OpenDeck events/outbound appears (willAppear, willDisappear, keyDown/Up, dialRotate, dialDown/Up, touchTap, titleParametersDidChange, didReceiveSettings, etc.).
    - Each supported event has a test reference (ctest filter or test name).
    - The Knob->Encoder mapping is recorded and cross-linked to EVENT-04.
    - File is ASCII-only.
  </acceptance_criteria>
  <done>The coverage doc enumerates every OpenDeck/Elgato event with status + test reference and records the Knob->Encoder mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: willAppear payload/envelope completeness test (EVENT-02)</name>
  <files>tests/unit/willappear_payload_test.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - src/app/src/plugin_device_bridge.cpp (instancePayload :311, eventEnvelope :374, willAppear emit :1213)
    - tests/unit/test_profile_*.cpp (existing bridge/profile test fixture idiom)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-VALIDATION.md (willappear_payload row)
  </read_first>
  <behavior>
    - Building a willAppear envelope via eventEnvelope("willAppear", ctx, instancePayload(ctx)) yields a top-level object with "action", "context", "device", "event"=="willAppear".
    - The nested "payload" object carries "coordinates" with "row" and "column", "controller", "state", and "isInMultiAction".
    - The "controller" value is a normalized token ("Keypad" or "Encoder"); it is never the literal "Knob".
    - TAG: willappear_payload (ASCII-only test name).
  </behavior>
  <action>
    Add tests/unit/willappear_payload_test.cpp asserting the EVENT-02 contract against the real bridge builders (instancePayload/eventEnvelope). If those builders are file-local/static, reach them through the smallest existing bridge test seam (mirror how test_profile_*.cpp constructs the bridge fixture) rather than copying the shape — the test must assert the ACTUAL emitted envelope, not a hand-rolled duplicate. Assert presence of every required key and that controller is normalized (not "Knob"). Register the TU in tests/unit/CMakeLists.txt. ASCII-only names/tags.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/tmp/b3402.log 2>&1 && ctest --preset linux-release -R willappear_payload 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R willappear_payload` passes.
    - The test asserts top-level action/context/device/event and payload{coordinates.row, coordinates.column, controller, state, isInMultiAction}.
    - The test asserts controller is normalized and never "Knob".
    - The TU is registered in tests/unit/CMakeLists.txt.
  </acceptance_criteria>
  <done>willappear_payload passes green, asserting the real bridge envelope+payload completeness with a normalized controller token.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Knob->Encoder controller-token normalization audit + test (EVENT-04)</name>
  <files>tests/unit/controller_token_test.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - src/app/src/plugin_manifest.cpp (line ~144: token == "Knob" || token == "Encoder")
    - src/app/src/plugin_device_bridge.cpp (Encoder byCoord + registration + switch-arm emission sites)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md (EVENT-04 controller-token audit subsection)
  </read_first>
  <behavior>
    - The manifest boundary accepts both "Knob" and "Encoder" and maps both to the encoder controller bit.
    - Every wire-emission path normalizes encoder controllers to the literal "Encoder"; no path emits "Knob" on the wire.
    - TAG: controller_token (ASCII-only).
  </behavior>
  <action>
    Add tests/unit/controller_token_test.cpp asserting: (1) the manifest normalizer maps "Knob" and "Encoder" to the same encoder controller (call plugin_manifest's controller-parse function), and (2) a representative encoder emission (dialRotate or encoder willAppear) carries controller == "Encoder", never "Knob". Complement with a grep-gate in the verify step asserting no wire-emission site under src/app/src/*.cpp emits the literal "Knob" (only the manifest acceptance line may). Register the TU in tests/unit/CMakeLists.txt. ASCII-only names/tags.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/tmp/b3403.log 2>&1 && ctest --preset linux-release -R controller_token 2>&1 | tail -5; grep -rn '"Knob"' src/app/src/*.cpp | grep -v plugin_manifest.cpp || echo "no Knob outside manifest OK"</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R controller_token` passes.
    - The test asserts Knob and Encoder map to the same encoder controller at the manifest boundary.
    - The test asserts a representative encoder emission carries controller=="Encoder".
    - `grep -rn '"Knob"' src/app/src/*.cpp` matches only plugin_manifest.cpp.
    - The TU is registered in tests/unit/CMakeLists.txt.
  </acceptance_criteria>
  <done>controller_token passes green; the only "Knob" literal in src/app/src is the manifest acceptance; the audit is recorded in the parity doc (Task 1).</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                         |
| ------------------------------------- | ----------------------------------------------------------------------------------- |
| plugin handler \<- willAppear payload | Plugins consume the envelope; a missing/mismatched field is a silent contract break |
| manifest token -> runtime controller  | Manifest "Knob" vocabulary vs runtime "Encoder" wire token                          |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                   |
| ---------------- | ---------------------- | ------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------- |
| T-34-02-01       | Tampering              | willAppear payload shape drift                    | mitigate    | EVENT-02 Catch2 test asserts the full envelope+payload against the real bridge builders so drift breaks the build |
| T-34-02-02       | Repudiation            | controller token mismatch dropping encoder events | mitigate    | EVENT-04 test + grep-gate guarantee no path emits "Knob" on the wire; mapping recorded in the parity doc          |
| T-34-02-03       | Information Disclosure | event-parity doc lists internal surfaces          | accept      | Doc references public event names + file:symbol only; no secrets; standard verification deliverable               |
| \</threat_model> |                        |                                                   |             |                                                                                                                   |

<verification>
- `ctest --preset linux-release -R 'willappear_payload|controller_token'` green.
- `grep -rn '"Knob"' src/app/src/*.cpp` matches only plugin_manifest.cpp.
- docs/plugin-event-parity.md exists with status + test references for every OpenDeck/Elgato event.
</verification>

\<success_criteria>

- EVENT-01 coverage doc committed, complete, ASCII-only, with per-event status + test refs.
- EVENT-02 willAppear payload completeness test green against the real bridge.
- EVENT-04 Knob->Encoder normalization test green + grep-gate clean.
  \</success_criteria>

<output>
Create `.planning/phases/34-per-app-profiles-event-parity-audit/34-02-SUMMARY.md` when done.
</output>
