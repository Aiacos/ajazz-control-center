---
phase: 34-per-app-profiles-event-parity-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/wayland/wlr-foreign-toplevel-management-unstable-v1.xml
  - src/core/include/ajazz/core/active_window_watcher.hpp
  - src/core/src/active_window_watcher_stub.cpp
  - src/core/CMakeLists.txt
  - src/app/CMakeLists.txt
  - src/app/src/debug_control_facade.cpp
  - tests/unit/active_window_watcher_test.cpp
  - tests/unit/app_profile_switch_test.cpp
  - tests/unit/app_lifecycle_events_test.cpp
  - .github/workflows/ci.yml
  - packaging/flatpak/io.github.Aiacos.AjazzControlCenter.yml
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [APROF-01]
must_haves:
  truths:
    - The project builds with Qt6::WaylandClient + libX11 linked and the vendored wlr XML code-generated
    - The watcher interface exists in ajazz_core and is Qt-free + nlohmann-free (COD-031 holds)
    - A StubActiveWindowWatcher records injected foreground changes and is the default factory result in tests
    - A window.setForeground debug RPC injects a synthetic foreground change through the watcher seam
    - RED test files for active_window / app_profile_switch / app_lifecycle exist and are registered in ctest
  artifacts:
    - path: src/app/wayland/wlr-foreign-toplevel-management-unstable-v1.xml
      provides: Vendored canonical wlr protocol definition for qtwaylandscanner
      contains: zwlr_foreign_toplevel_manager_v1
    - path: src/core/include/ajazz/core/active_window_watcher.hpp
      provides: IActiveWindowWatcher interface + ActiveWindowInfo + makeDefaultActiveWindowWatcher factory
      contains: class IActiveWindowWatcher
    - path: src/core/src/active_window_watcher_stub.cpp
      provides: StubActiveWindowWatcher recording stub + default factory definition
      contains: StubActiveWindowWatcher
    - path: tests/unit/active_window_watcher_test.cpp
      provides: RED factory/stub/debounce test scaffold
      contains: active_window
  key_links:
    - from: src/core/CMakeLists.txt
      to: active_window_watcher_stub.cpp
      via: unconditional source-list entry mirroring input_synthesizer_stub.cpp
      pattern: active_window_watcher_stub
    - from: src/app/CMakeLists.txt
      to: wlr-foreign-toplevel-management-unstable-v1.xml
      via: qt_generate_wayland_protocol_client_sources
      pattern: qt_generate_wayland_protocol_client_sources
    - from: src/app/src/debug_control_facade.cpp
      to: watcher stub injectForeground seam
      via: window.setForeground registerMethod
      pattern: window.setForeground
---

<objective>
Lay the Phase 34 foundation: the platform-split `IActiveWindowWatcher` interface (Qt-free, COD-031-safe) + the recording stub + the build-environment plumbing (vendored wlr XML, CMake codegen + link, CI/Flatpak deps), the `window.setForeground` debug RPC seam, and the RED Catch2 scaffolds the APROF backends and switch logic will turn green.

Purpose: every downstream APROF plan (watcher backends, auto-switch, UI) depends on the interface contract, the build env, and the test seams existing first. This is the interface-first / Wave-0 plan — it defines contracts and fails-by-default tests, no live backend behavior yet.
Output: a compiling tree with the watcher interface + stub + factory, the Wayland/X11 build wiring, the debug RPC, and three registered RED test files.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-VALIDATION.md

<interfaces>
<!-- CANONICAL analog the watcher MUST mirror. Read these first; do not re-derive. -->
From src/core/include/ajazz/core/input_synthesizer.hpp:
  - Value struct pattern (KeyChord), bounded interface (IInputSynthesizer pure virtuals),
    free factory `std::unique_ptr<IInputSynthesizer> makeDefaultInputSynthesizer();`
  - Header is deliberately Qt-free + nlohmann-free (COD-031 boundary stated verbatim in the header).
From src/core/src/input_synthesizer_stub.cpp:
  - StubInputSynthesizer records each call into `std::vector<std::string> log_`; exposes `log()`/`clearLog()`.
  - Factory defined HERE; forward-declares `makePlatformInputSynthesizer()` behind the feature gate;
    `makeDefault*` returns the platform impl if gated, else the stub.
From src/core/CMakeLists.txt:28-31:
  - input_synthesizer_{stub,linux,win,mac}.cpp listed UNCONDITIONALLY (each TU self-empty behind #if).
From src/app/src/debug_control_facade.cpp:303:
  - `server.registerMethod("input.key", [&app](QJsonObject const& params, QString& err){ ... })`
    reaches `app.pluginDebug()`; returns a QJsonObject echo. window.setForeground mirrors this shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor wlr XML + Qt-free watcher interface + recording stub + factory</name>
  <files>src/app/wayland/wlr-foreign-toplevel-management-unstable-v1.xml, src/core/include/ajazz/core/active_window_watcher.hpp, src/core/src/active_window_watcher_stub.cpp</files>
  <read_first>
    - src/core/include/ajazz/core/input_synthesizer.hpp (interface + factory shape, COD-031 note)
    - src/core/src/input_synthesizer_stub.cpp (recording stub + factory-here + platform forward-decl)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md (sections for active_window_watcher.hpp and active_window_watcher_stub.cpp)
  </read_first>
  <action>
    Vendor the canonical `wlr-foreign-toplevel-management-unstable-v1.xml` from swaywm/wlr-protocols into src/app/wayland/ with a leading XML comment pinning the source repo + revision/date (per RESEARCH Package Legitimacy Audit). Define `struct ActiveWindowInfo { std::string appId; std::string title; }` and `class IActiveWindowWatcher` in active_window_watcher.hpp with pure virtuals: `void start(std::function<void(ActiveWindowInfo)> onChange)`, `void stop()`, and `[[nodiscard]] bool capabilityAvailable() const` (maps to QWaylandClientExtension::isActive() in the Wayland backend; drives the APROF-03 chip). Declare the free factory `[[nodiscard]] std::unique_ptr<IActiveWindowWatcher> makeDefaultActiveWindowWatcher();`. The header MUST be Qt-free and nlohmann-free — include only <functional>, <memory>, <string> — and carry the verbatim COD-031 boundary comment as input_synthesizer.hpp does. Implement `StubActiveWindowWatcher` in active_window_watcher_stub.cpp: stores the onChange callback from start(), records each change into `std::vector<std::string> log_`, exposes `log()`/`clearLog()`, a settable `capabilityAvailable()` (constructor or setter, default true) for the degradation test, and a test seam `void injectForeground(ActiveWindowInfo)` that fires the stored callback and appends to log_. Define `makeDefaultActiveWindowWatcher()` HERE, mirroring the stub-factory idiom: forward-declare `makePlatformActiveWindowWatcher()` behind the platform/feature gate, return it when available, else the stub. Do NOT add nlohmann or Qt to any core header.
  </action>
  <verify>
    <automated>grep -rn nlohmann src/core/include/ active_window_watcher.hpp 2>/dev/null | grep -v '^#' | grep -c nlohmann | grep -qx 0 && echo COD-031-OK; grep -c "QObject\|QTimer\|#include <Q" src/core/include/ajazz/core/active_window_watcher.hpp | grep -qx 0 && echo Qt-free-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn nlohmann src/core/include/` returns 0 (filtered of comment lines) — COD-031 preserved.
    - active_window_watcher.hpp contains no `#include <Q...>`, no `QObject`, no `QTimer`.
    - active_window_watcher.hpp declares `class IActiveWindowWatcher`, `struct ActiveWindowInfo`, and `makeDefaultActiveWindowWatcher`.
    - active_window_watcher_stub.cpp defines `StubActiveWindowWatcher` with `injectForeground`, `log()`, `clearLog()`, settable `capabilityAvailable()`, and defines `makeDefaultActiveWindowWatcher()`.
    - The vendored XML contains `zwlr_foreign_toplevel_manager_v1` and a source-pin comment.
  </acceptance_criteria>
  <done>The interface header is Qt-free/nlohmann-free, the stub records + injects, the factory is defined in the stub TU, and the wlr XML is vendored with a source pin.</done>
</task>

<task type="auto">
  <name>Task 2: CMake codegen + link wiring + CI/Flatpak build-env deps</name>
  <files>src/core/CMakeLists.txt, src/app/CMakeLists.txt, .github/workflows/ci.yml, packaging/flatpak/io.github.Aiacos.AjazzControlCenter.yml</files>
  <read_first>
    - src/core/CMakeLists.txt (lines around 24-31 + 53-63: unconditional per-OS source list + per-platform link branch)
    - src/app/CMakeLists.txt (target definition + existing find_package(Qt6 ...) + link_libraries)
    - .github/workflows/ci.yml (lines around 89 and 352: the apt-get install blocks)
    - packaging/flatpak/io.github.Aiacos.AjazzControlCenter.yml (modules/build-options/dependencies)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md (Environment Availability + CI/Flatpak note)
  </read_first>
  <action>
    In src/core/CMakeLists.txt add `src/active_window_watcher_stub.cpp` to the ajazz_core source list UNCONDITIONALLY (mirror input_synthesizer_stub.cpp at line 28). In src/app/CMakeLists.txt: add `find_package(Qt6 REQUIRED COMPONENTS WaylandClient)`; call `qt_generate_wayland_protocol_client_sources(<app-target> PRIVATE_CODE FILES ${CMAKE_CURRENT_SOURCE_DIR}/wayland/wlr-foreign-toplevel-management-unstable-v1.xml)`; add the four per-OS watcher TUs to the app target source list UNCONDITIONALLY (active_window_watcher_wayland.cpp, _x11.cpp, _win.cpp, _mac.mm — created by Plan 03; reference them now so the build list is stable, but guard with a comment that the files land in Plan 03 — if they do not yet exist, create empty self-#if-guarded placeholder TUs so the build stays green); link `Qt6::WaylandClient` on the app target; in the existing per-platform link branch add libX11 (find_package or pkg-config for x11) on Linux, `-framework AppKit` on macOS, `user32` on Windows. In .github/workflows/ci.yml add `qt6-wayland`/`qtwayland`-equivalent, `libwayland-dev`, `wayland-protocols`, and `libx11-dev` to the ubuntu apt-get install block (the build deps block). In the Flatpak manifest add the qtwayland module / wayland + X11 dev to the build environment so the manifest builds with the Wayland backend (RESEARCH explicitly flags the manifest currently does not build with these).
  </action>
  <verify>
    <automated>cmake --preset linux-release -DCMAKE_BUILD_TYPE=Release >/tmp/cfg34.log 2>&1 && grep -q "active_window_watcher_stub" src/core/CMakeLists.txt && grep -q "qt_generate_wayland_protocol_client_sources" src/app/CMakeLists.txt && echo WIRED || (tail -30 /tmp/cfg34.log; false)</automated>
  </verify>
  <acceptance_criteria>
    - `cmake --preset linux-release` configures without error with Qt6::WaylandClient found.
    - src/core/CMakeLists.txt lists active_window_watcher_stub.cpp unconditionally.
    - src/app/CMakeLists.txt calls qt_generate_wayland_protocol_client_sources over the vendored XML and links Qt6::WaylandClient + libX11.
    - ci.yml ubuntu apt-get block installs libwayland-dev, wayland-protocols, libx11-dev, and the Qt wayland module.
    - The Flatpak manifest adds the qtwayland module / wayland + X11 build deps.
  </acceptance_criteria>
  <done>cmake configures clean with Wayland codegen wired; CI and Flatpak provide the qtwayland/wayland/X11 build deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: window.setForeground debug RPC + RED Catch2 scaffolds (active_window / app_profile_switch / app_lifecycle)</name>
  <files>src/app/src/debug_control_facade.cpp, tests/unit/active_window_watcher_test.cpp, tests/unit/app_profile_switch_test.cpp, tests/unit/app_lifecycle_events_test.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - src/app/src/debug_control_facade.cpp (lines around 303-346: input.key/input.encoder RPC shape + the app accessor seam)
    - tests/unit/test_input_synth.cpp (Catch2 factory + recording-stub + injection idiom)
    - tests/unit/CMakeLists.txt (how test TUs are registered/discovered)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
  </read_first>
  <behavior>
    - active_window_watcher_test.cpp: makeDefaultActiveWindowWatcher returns a usable watcher; start() registers a callback; injectForeground fires it once per inject; rapid injects within the debounce window coalesce to a single emit (TAG: active_window_debounce); capabilityAvailable() reflects the settable stub state.
    - app_profile_switch_test.cpp: a foreground appId matching a Profile::applicationHints entry selects that profile; no match falls back to the default profile (TAG: app_profile_switch). RED until Plan 04 wires the matcher.
    - app_lifecycle_events_test.cpp: applicationDidLaunch/applicationDidTerminate are delivered only to subscribed/registered plugins (TAG: app_lifecycle_events). RED until Plan 04.
  </behavior>
  <action>
    Add a `window.setForeground` RPC in debug_control_facade.cpp mirroring input.key (line 303): read `params.value("appId").toString()` (and optional "title"), reach the watcher stub through an app accessor seam (`app.activeWindowWatcher()` — add the accessor the same way input.key reaches `app.pluginDebug()`; if the live watcher is not the stub in the running app, route through the same injectForeground seam exposed for tests), call `injectForeground({appId, title})`, return `{{"appId", appId}}` on success or set `err` if unavailable. Create the three Catch2 test files asserting the behaviors above; mark currently-unimplementable assertions with explicit RED markers (Catch2 `[!shouldfail]` tag OR a `FAIL("Wave 0 RED: implemented in Plan 0X")` so the suite compiles + runs and is unmistakably pending). Register the three TUs in tests/unit/CMakeLists.txt following the existing discovery pattern. Use ASCII-only Catch2 test names and tags (CLAUDE.md: ctest filter mangles non-ASCII). Ensure the active_window_watcher_test cases are genuinely runnable against the stub (NOT RED) — only the switch/lifecycle cases are RED pending Plan 04.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/tmp/b34.log 2>&1 && ctest --preset linux-release -R 'active_window' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R active_window` runs (stub factory + injectForeground + debounce cases PASS green).
    - `ctest --preset linux-release -R 'app_profile_switch|app_lifecycle_events'` runs and is visibly RED/pending (not silently absent).
    - `grep -n "window.setForeground" src/app/src/debug_control_facade.cpp` returns a registerMethod hit.
    - All three test TUs are registered in tests/unit/CMakeLists.txt; test names are ASCII-only.
  </acceptance_criteria>
  <done>window.setForeground injects through the watcher seam; active_window cases are green against the stub; app_profile_switch + app_lifecycle scaffolds are registered and explicitly RED for Plan 04.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                       |
| ------------------------------------ | ------------------------------------------------------------------------------------------------- |
| OS focus state -> watcher            | Untrusted OS-reported app-id/title strings enter the app                                          |
| debug socket -> window.setForeground | Local debug RPC injects a synthetic foreground (dev-only channel, opt-in via AJAZZ_DEBUG_CONTROL) |
| vendored XML -> qtwaylandscanner     | Build-time codegen input                                                                          |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                                   | Disposition | Mitigation Plan                                                                                                                                            |
| ---------------- | ---------------------- | ----------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-01          | Tampering              | vendored wlr XML                                            | mitigate    | Copy canonical swaywm/wlr-protocols XML only; pin source+revision in a leading comment; no executable dependency added (RESEARCH Package Legitimacy Audit) |
| T-34-02          | Information Disclosure | ActiveWindowInfo strings                                    | mitigate    | Interface deals in app-id strings only; no JSON/Qt in the core header (COD-031); fan-out scoping is enforced in Plan 04, not here                          |
| T-34-03          | Tampering              | untrusted app-id token                                      | mitigate    | appId is a plain std::string match token; never eval/exec/shell-out (hard rule); no path uses it as a command                                              |
| T-34-SC          | Tampering              | system build deps (apt/flatpak: qtwayland, wayland, libX11) | accept      | Distro/Qt-SDK system libraries, not registry packages (RESEARCH: C++/CMake, no npm/pip/cargo audit applicable)                                             |
| \</threat_model> |                        |                                                             |             |                                                                                                                                                            |

<verification>
- `cmake --preset linux-release` configures clean with Qt6::WaylandClient.
- `grep -rn nlohmann src/core/include/` returns 0 (COD-031).
- `ctest --preset linux-release -R active_window` green; `-R 'app_profile_switch|app_lifecycle_events'` registered + RED.
- `grep -n "window.setForeground" src/app/src/debug_control_facade.cpp` returns a hit.
</verification>

\<success_criteria>

- IActiveWindowWatcher interface + StubActiveWindowWatcher + factory exist, Qt-free + nlohmann-free.
- Build env (CMake codegen/link + CI + Flatpak) provides qtwayland/wayland/X11.
- window.setForeground RPC injects synthetic foreground changes.
- Three RED/partial test scaffolds registered in ctest per VALIDATION.md Wave 0.
  \</success_criteria>

<output>
Create `.planning/phases/34-per-app-profiles-event-parity-audit/34-01-SUMMARY.md` when done.
</output>
