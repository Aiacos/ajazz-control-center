---
phase: 34-per-app-profiles-event-parity-audit
plan: 03
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/app/src/active_window_watcher_wayland.cpp
  - src/app/src/active_window_watcher_x11.cpp
  - src/app/src/active_window_watcher_win.cpp
  - src/app/src/active_window_watcher_mac.mm
  - src/core/src/active_window_watcher_stub.cpp
  - tests/unit/active_window_watcher_test.cpp
autonomous: false
requirements: [APROF-01]
must_haves:
  truths:
    - On Linux the factory selects the Wayland backend under a wayland session and the X11 backend under an xcb session at runtime, not compile-time
    - The Wayland backend binds zwlr_foreign_toplevel_manager_v1 and emits the app_id of the activated toplevel; isActive()==false drives capabilityAvailable()==false
    - The X11 backend reads _NET_ACTIVE_WINDOW + _NET_WM_PID/WM_CLASS and emits the foreground app id
    - 'The Win/macOS backends compile behind their platform #if and are unit-test/compile-guarded only'
    - Foreground changes are debounced ~150-250ms so rapid focus thrash coalesces to one emit
  artifacts:
    - path: src/app/src/active_window_watcher_wayland.cpp
      provides: wlr-foreign-toplevel Wayland backend via QWaylandClientExtensionTemplate
      contains: zwlr_foreign_toplevel_manager_v1
    - path: src/app/src/active_window_watcher_x11.cpp
      provides: X11/EWMH backend
      contains: _NET_ACTIVE_WINDOW
    - path: src/app/src/active_window_watcher_win.cpp
      provides: Win32 GetForegroundWindow backend (compile-guarded)
      contains: GetForegroundWindow
    - path: src/app/src/active_window_watcher_mac.mm
      provides: macOS NSWorkspace backend (compile-guarded, Obj-C++)
      contains: NSWorkspace
  key_links:
    - from: makeDefaultActiveWindowWatcher / makePlatformActiveWindowWatcher
      to: wayland vs x11 backend
      via: QGuiApplication::platformName() / XDG_SESSION_TYPE runtime selection
      pattern: platformName|XDG_SESSION_TYPE
    - from: WlrToplevelManager
      to: capabilityAvailable()
      via: QWaylandClientExtension::isActive() / activeChanged
      pattern: isActive
---

<objective>
Implement the four `IActiveWindowWatcher` platform backends behind the Plan-01 interface: Wayland (wlr-foreign-toplevel via `QWaylandClientExtensionTemplate`), X11/EWMH, Win32 (compile-guarded), macOS (compile-guarded `.mm`), plus the runtime backend-selection factory (`makePlatformActiveWindowWatcher`) and QTimer debounce. Linux Wayland + X11 are live-verified on niri here; Win/macOS are compile-guarded + unit-tested.

Purpose: this is the genuinely new code of Phase 34 — the foreground-detection engine that feeds the auto-switch (Plan 04). It turns the active_window debounce/factory tests green and exercises the wlr binding on the live niri compositor.
Output: four backend TUs + the platform factory + debounce, with a live Wayland-on-niri verification checkpoint.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-VALIDATION.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-01-SUMMARY.md

<interfaces>
<!-- The interface + stub contract from Plan 01 the backends implement. -->
From src/core/include/ajazz/core/active_window_watcher.hpp (Plan 01):
  - struct ActiveWindowInfo { std::string appId; std::string title; };
  - class IActiveWindowWatcher { start(std::function<void(ActiveWindowInfo)>), stop(), bool capabilityAvailable() const };
  - std::unique_ptr<IActiveWindowWatcher> makeDefaultActiveWindowWatcher();
From src/core/src/input_synthesizer_linux.cpp:26 (gated-TU shell to mirror):
  - whole TU is empty unless `#if defined(__linux__) [&& feature]`; still compiles on every platform.
RESEARCH Pattern 2 (Wayland binding shape):
  - class WlrToplevelManager : QWaylandClientExtensionTemplate<WlrToplevelManager>, QtWayland::zwlr_foreign_toplevel_manager_v1
  - per-handle override _state(array) -> detect ZWLR_..._STATE_ACTIVATED; _app_id/_title -> identity; _done()/_closed().
  - connect(activeChanged) -> if(!isActive()) capabilityAvailable=false.
RESEARCH Pitfall 5/6: construct on GUI thread after QGuiApplication; pick wayland-vs-x11 at RUNTIME.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wayland (wlr) + X11 Linux backends + runtime factory + debounce</name>
  <files>src/app/src/active_window_watcher_wayland.cpp, src/app/src/active_window_watcher_x11.cpp, src/core/src/active_window_watcher_stub.cpp</files>
  <read_first>
    - src/core/include/ajazz/core/active_window_watcher.hpp (the interface from Plan 01)
    - src/core/src/active_window_watcher_stub.cpp (the factory + makePlatform forward-decl from Plan 01)
    - src/core/src/input_synthesizer_linux.cpp (gated-TU shell idiom)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md (Wayland backend + gated-TU sections; Pitfall 5/6)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md (Pattern 2; Code Examples; Pitfall 1 ext-vs-wlr; Pitfall 4 no-activated-handle)
  </read_first>
  <action>
    Implement active_window_watcher_wayland.cpp guarded `#if defined(__linux__)`: a `WlrToplevelManager : QWaylandClientExtensionTemplate<WlrToplevelManager>, QtWayland::zwlr_foreign_toplevel_manager_v1` constructed with version 3; override the manager's toplevel handler to wrap each handle; per handle override `_app_id`/`_title` for identity and `_state(array)` to detect `ZWLR_FOREIGN_TOPLEVEL_HANDLE_V1_STATE_ACTIVATED`; `_done()` commits, `_closed()` disposes. The focused app = the handle whose latest state contains activated. Connect `QWaylandClientExtension::activeChanged`; when `isActive()==false`, the watcher reports `capabilityAvailable()==false` (Pitfall 1: use wlr ONLY, not ext-). On a state update with no activated handle, do not emit a spurious empty change (Pitfall 4). Implement active_window_watcher_x11.cpp guarded `#if defined(__linux__)`: read `_NET_ACTIVE_WINDOW`, then `_NET_WM_PID`/`WM_CLASS` via Xlib to resolve the app id; poll on the X event for property changes (no subprocess — RESEARCH forbids xdotool/swaymsg). Wrap BOTH Linux watcher impls with a QTimer single-shot trailing-edge debounce (~150-250ms, mirror HotplugDebouncer 300ms idiom) that coalesces rapid changes before firing onChange. In active_window_watcher_stub.cpp define `makePlatformActiveWindowWatcher()` (behind the platform gate) to choose the Wayland vs X11 backend at RUNTIME using `QGuiApplication::platformName()` ("wayland" vs "xcb") / `XDG_SESSION_TYPE` (Pitfall 6) — NOT compile-time. Construct backends lazily so the factory can be invoked from Application init after QGuiApplication (Pitfall 5). Keep all Qt/QTimer/QWaylandClientExtension machinery in these app-tier TUs; the core header stays Qt-free.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release >/tmp/b3403a.log 2>&1 && grep -q "zwlr_foreign_toplevel_manager_v1" src/app/src/active_window_watcher_wayland.cpp && grep -q "_NET_ACTIVE_WINDOW" src/app/src/active_window_watcher_x11.cpp && grep -Eq "platformName|XDG_SESSION_TYPE" src/core/src/active_window_watcher_stub.cpp && echo BACKENDS-WIRED || (tail -30 /tmp/b3403a.log; false)</automated>
  </verify>
  <acceptance_criteria>
    - The full app builds with both Linux backends + the runtime factory.
    - active_window_watcher_wayland.cpp uses zwlr_foreign_toplevel_manager_v1 and gates capabilityAvailable on isActive().
    - active_window_watcher_x11.cpp reads _NET_ACTIVE_WINDOW + _NET_WM_PID/WM_CLASS, no subprocess.
    - makePlatformActiveWindowWatcher selects wayland vs x11 at runtime via platformName()/XDG_SESSION_TYPE.
    - Debounce coalesces rapid changes (QTimer trailing-edge).
  </acceptance_criteria>
  <done>Both Linux backends build, bind/read foreground state, debounce, and the factory chooses at runtime; core header remains Qt-free.</done>
</task>

<task type="auto">
  <name>Task 2: Win32 + macOS compile-guarded backends + active_window tests green</name>
  <files>src/app/src/active_window_watcher_win.cpp, src/app/src/active_window_watcher_mac.mm, tests/unit/active_window_watcher_test.cpp</files>
  <read_first>
    - src/core/src/input_synthesizer_win.cpp + input_synthesizer_mac.cpp (gated-TU + .mm idiom)
    - tests/unit/active_window_watcher_test.cpp (the RED scaffold from Plan 01)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md (Standard Stack Win/macOS rows; A3)
  </read_first>
  <action>
    Implement active_window_watcher_win.cpp guarded `#if defined(Q_OS_WIN)`: `GetForegroundWindow` + `GetWindowThreadProcessId` + `QueryFullProcessImageNameW`, normalize the image base name (firefox.exe -> firefox), debounce via the shared QTimer helper. Implement active_window_watcher_mac.mm guarded `#if defined(Q_OS_MACOS)`: `NSWorkspace.frontmostApplication` + observe `NSWorkspaceDidActivateApplicationNotification`, emit the bundle id / localized name. Both TUs are self-empty otherwise so they compile on Linux. These are NOT live-testable here (no Windows/macOS device) — they are compile-guarded only. Finalize tests/unit/active_window_watcher_test.cpp so the factory/stub/debounce cases are fully green against the stub + the runtime-selection logic (assert the factory returns a non-null watcher; assert capabilityAvailable() reflects the settable stub; assert the debounce coalesces N rapid injectForeground calls within the window to one onChange — TAG active_window_debounce). Define ONE app-identity normalization contract (case-insensitive app_id/WM_CLASS/image-base/bundle-id) and document it in a comment + the parity doc cross-ref (Pitfall 2). ASCII-only test names.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/tmp/b3403b.log 2>&1 && ctest --preset linux-release -R active_window 2>&1 | tail -6 && grep -q "GetForegroundWindow" src/app/src/active_window_watcher_win.cpp && grep -q "NSWorkspace" src/app/src/active_window_watcher_mac.mm && echo ALL-BACKENDS</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R active_window` is fully green (factory, stub, debounce, debounce-coalesce).
    - active_window_watcher_win.cpp contains GetForegroundWindow behind Q_OS_WIN and is self-empty on Linux.
    - active_window_watcher_mac.mm contains NSWorkspace behind Q_OS_MACOS and is self-empty on Linux.
    - The app-identity normalization contract is documented (comment + parity doc reference).
  </acceptance_criteria>
  <done>All four backends compile on Linux; the active_window suite is green; Win/macOS are compile-guarded with the documented identity contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Wayland (wlr-foreign-toplevel) backend running live on the niri compositor: focus changes between two real applications drive ActiveWindowInfo.appId emissions through the debounce.</what-built>
  <how-to-verify>
    1. Build: `cmake --build --preset linux-release`.
    2. Launch headless-drivable: `AJAZZ_DEBUG_CONTROL=1 nohup build/linux-release/src/app/ajazz-control-center >/tmp/acc34.log 2>&1 &` (kill any prior instance + stale socket first per CLAUDE.md headless recipe).
    3. Focus application A (e.g. a terminal), then focus application B (e.g. a browser) on the niri desktop.
    4. Confirm `log.tail` (via `scripts/ajazz-debug log.tail`) shows the watcher emitting the app_id change for B (and that the wlr global bound: capabilityAvailable()==true / isActive()==true).
    5. Confirm rapid alt-tab thrash produces a single coalesced emit (debounce), not one per intermediate focus.
  </how-to-verify>
  <resume-signal>Type "approved" once the live niri Wayland focus-change emissions + debounce are confirmed in log.tail, or describe what you saw.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                 |
| ---------------------------------------- | ----------------------------------------------------------- |
| Wayland compositor -> WlrToplevelManager | Compositor-reported app_id/title/state events enter the app |
| X11 server -> Xlib property reads        | Untrusted window manager property values                    |
| OS focus API -> debounce                 | Per-platform foreground identity strings                    |

## STRIDE Threat Register

| Threat ID        | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                       |
| ---------------- | ---------------------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-03-01       | Denial of Service      | rapid focus thrash                | mitigate    | QTimer trailing-edge debounce (~150-250ms) coalesces emits before any downstream switch                                               |
| T-34-03-02       | Spoofing/degraded      | Wayland global absent or spoofed  | mitigate    | isActive() gate -> capabilityAvailable()==false; fail safe (warning chip in Plan 05); never fail open; no desktop-name blocklist      |
| T-34-03-03       | Tampering              | untrusted app_id/WM_CLASS strings | mitigate    | Treated strictly as match tokens (consumed in Plan 04); never eval/exec/shell-out; no subprocess fallback (xdotool/swaymsg forbidden) |
| T-34-03-04       | Information Disclosure | foreground app id read            | accept      | Reading focus state is the feature's purpose; disclosure to plugins is gated in Plan 04 (registered-only)                             |
| \</threat_model> |                        |                                   |             |                                                                                                                                       |

<verification>
- `cmake --build --preset linux-release` builds all four backends on Linux.
- `ctest --preset linux-release -R active_window` green.
- Live niri checkpoint: focus-change app_id emissions + debounce confirmed via log.tail.
</verification>

\<success_criteria>

- APROF-01: interface + 4 platform backends + runtime factory + debounce.
- Wayland-on-niri live-verified; Win/macOS compile-guarded + unit-tested.
- capabilityAvailable() mapped to isActive() for the Plan 05 warning chip.
  \</success_criteria>

<output>
Create `.planning/phases/34-per-app-profiles-event-parity-audit/34-03-SUMMARY.md` when done.
</output>
