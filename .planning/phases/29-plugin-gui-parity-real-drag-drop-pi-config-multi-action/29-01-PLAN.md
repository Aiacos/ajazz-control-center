---
phase: 29-plugin-gui-parity-real-drag-drop-pi-config-multi-action
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/src/debug_control_qml.cpp
  - src/app/qml/DragRelay.qml
  - src/app/qml/Main.qml
  - src/app/qml/ActionLibraryPane.qml
autonomous: false
requirements: [PLUGIN-21]
must_haves:
  truths:
    - A physical-mouse drag of a plugin tile onto a key binds + renders on the device (no commitBinding shortcut)
    - The debug channel can synthesize a real pointer drag from one objectName to another
    - The DragRelay/Drag.Internal Wayland dead-drag root cause is identified with file:line and fixed
  artifacts:
    - path: src/app/src/debug_control_qml.cpp
      provides: qml.drag pointer-drag driver RPC (synthesizes press/move/release QMouseEvents in scene coords)
      contains: qml.drag
    - path: src/app/qml/DragRelay.qml
      provides: fixed gesture relay (or unchanged if root cause is elsewhere; file:line documented)
  key_links:
    - from: src/app/src/debug_control_qml.cpp
      to: QQuickWindow event delivery
      via: QMouseEvent press/move/release posted to the window
      pattern: QMouseEvent|sendEvent|mousePress
    - from: ActionLibraryPane.qml tile DragHandler
      to: KeyCell DropArea onDropped
      via: DragRelay ghost Drag.Internal + dropRequested
      pattern: DragRelay\.(begin|finish)
---

<objective>
Fix the user-blocking defect: a physical-mouse drag of a plugin action tile onto a KeyCell (and EncoderDial / TouchStripLane) does nothing on Wayland/niri. Root-cause the `DragRelay` singleton + `Drag.Internal` cursor-follow ghost gesture path with file:line, fix it, and prove the fix by driving a REAL pointer sequence (press tile -> move over key -> release) through the running GUI.

Purpose: Phase 28 marked drag-to-bind "complete" by verifying the `commitKeyBinding` DATA path via the debug channel — it never exercised a real Wayland pointer drag, which is exactly what is broken. The commit path is CONFIRMED correct (CONTEXT.md) and MUST NOT be rewritten. This plan fixes only the GESTURE layer and adds the debug-channel pointer-drag driver that makes the real gesture autonomously regression-testable.

Output: A `qml.drag` (or `input.pointer` press/move/release) RPC on the debug channel that synthesizes real pointer events against window/scene coordinates, plus the gesture-path fix in DragRelay.qml / Main.qml / ActionLibraryPane.qml.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase — executor should use these directly. -->

debug_control_qml.cpp already exposes (registerQmlControlMethods(DebugControlServer&, QQmlApplicationEngine&)):

- findByName(engine, name) -> QObject\* (visual-tree aware: unions childItems(), reaches ListView/Repeater delegates)
- firstWindow(engine) -> QQuickWindow\*
- qml.tree / qml.get / qml.set / qml.invoke / qml.click / screenshot are all registered here.
  A new pointer-drag method registers in the SAME function via server.registerMethod(...).

DragRelay.qml (singleton, pragma Singleton) API:

- begin(key, payloadStr, iconUrl, iconName, label, sx, sy) // sx,sy = scene coords
- moveTo(sx, sy)
- finish() // emits dropRequested() then clear()
- clear()
- properties: active(bool), mimeKey, payload, hotspot(point), ghostIconUrl/Name/Label
- signal dropRequested()

ActionLibraryPane.qml tile drag source (lines 199-237):

- DragHandler { id: tileDragHandler; target: null; dragThreshold: 8 } onActiveChanged -> DragRelay.begin/finish
- Binding feeds DragRelay.hotspot = tileDragHandler.centroid.scenePosition while active.
- tile objectName = "libraryTile\_" + index

Main.qml dragGhost (lines 346-427): Item objectName "dragGhost", z 100000, Drag.dragType Drag.Internal,
Drag.active: DragRelay.active, Drag.mimeData branches on DragRelay.mimeKey,
Connections onDropRequested -> dragGhost.Drag.drop().

KeyCell.qml DropArea (lines 187-279): keys ["application/x-ajazz-action","application/x-ajazz-binding"];
onEntered affordance-gates (mask & 1); onDropped -> root.cellActionDropped(index, payload) (UNCHANGED — works for internal drag).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a debug-channel real pointer-drag driver (qml.drag)</name>
  <files>src/app/src/debug_control_qml.cpp</files>
  <read_first>
    - src/app/src/debug_control_qml.cpp (whole file — registration pattern, findByName, firstWindow)
    - src/app/src/debug_control_facade.cpp (input.key/encoder pattern for RPC shape, lines 298-346)
    - src/app/qml/Main.qml:346-427 (dragGhost — what the synthesized drag must drive)
  </read_first>
  <action>
    In debug_control_qml.cpp, inside registerQmlControlMethods, register a new RPC "qml.drag" with params {from: objectName, to: objectName, steps?: int=8}. Resolve both objects via findByName; compute each item's scene-center via QQuickItem::mapToScene(boundingRect().center()) then map to window coords via QQuickWindow::contentItem mapping. Synthesize a REAL pointer sequence on the QQuickWindow returned by firstWindow(): a left-button press QMouseEvent at the source center, several move events interpolated from source to target center (steps), and a release at the target center. Deliver via QCoreApplication::sendEvent / QWindow event posting against the QQuickWindow (mouse events with Qt::LeftButton, QEventPoint/QPointF in window-local coordinates). Return {from, to, delivered: true}. Also register a lower-level "input.pointer" {action: press|move|release, x, y} that posts a single QMouseEvent at window coords so a manual press->move->release can be scripted. Both methods must error with a clear message when an object or the window is not found. Add objectName-free C++ only — no QML changes here. Per CLAUDE.md the new driver IS the debug RPC.
    Do NOT use qml.invoke/qml.click — those emit signals, not pointer events, and per CLAUDE.md do not reproduce gesture side effects (the Phase-28 trap).
  </action>
  <verify>
    <automated>cmake --build build/linux-release --target ajazz-control-center ajazz_unit_tests -j$(nproc) 2>&1 | tail -5; grep -c '"qml.drag"\|"input.pointer"' src/app/src/debug_control_qml.cpp</automated>
  </verify>
  <acceptance_criteria>
    - debug_control_qml.cpp registers both "qml.drag" and "input.pointer"; `grep -c` returns >= 2.
    - Build of ajazz-control-center + ajazz_unit_tests succeeds (0 errors).
    - The methods deliver real QMouseEvents to the QQuickWindow (grep `QMouseEvent` present in the new handlers), not signal invocations.
  </acceptance_criteria>
  <done>qml.drag {from,to} and input.pointer {action,x,y} are registered debug RPCs that synthesize real pointer events against the live QQuickWindow; the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Root-cause + fix the Wayland dead-drag gesture path</name>
  <files>src/app/qml/DragRelay.qml, src/app/qml/Main.qml, src/app/qml/ActionLibraryPane.qml</files>
  <read_first>
    - src/app/qml/DragRelay.qml (whole file)
    - src/app/qml/Main.qml:346-427 (dragGhost)
    - src/app/qml/ActionLibraryPane.qml:199-245 (tile DragHandler + hotspot Binding)
    - src/app/qml/components/KeyCell.qml:186-279 (DropArea — must remain unchanged)
    - 29-CONTEXT.md canonical_refs (drag-drop section)
  </read_first>
  <action>
    Drive a real drag via the Task-1 qml.drag from objectName "libraryTile_0" to a target KeyCell objectName (discover the key objectName via qml.tree on the device editor; KeyCell sets an objectName). Observe whether onDropped fires (read the bound binding via profile.list/ProfileController or qml.get on the cell). When the drop does NOT fire, root-cause the break among the documented candidates (CONTEXT.md:23): (a) DragRelay never activated because the tile's DragHandler centroid never crosses dragThreshold under synthesized/real pointer events; (b) the ghost's `hotspot` Binding does not update so the ghost is not over the DropArea at drop() time; (c) `dragGhost.Drag.drop()` fires but the DropArea's `keys`/`hasFormat` mismatch the ghost's `Drag.keys`/`Drag.mimeData`; (d) Drag.Internal hit-test uses scene geometry but the ghost width/height is 1px so it collides at a single point that misses the DropArea center. Document the confirmed cause WITH file:line in the SUMMARY. Apply the minimal fix in DragRelay.qml / Main.qml / ActionLibraryPane.qml ONLY — e.g. ensure the ghost is positioned at the cursor scene point at drop() time, give the ghost a hit area matching the hotspot, or correct the moveTo/hotspot feed. Do NOT touch any DropArea, KeyCell.cellActionDropped, DeviceView.onKeyActionDropped, or ProfileController — the commit path is correct (CONTEXT.md:22).
  </action>
  <verify>
    <automated>cmake --build build/linux-release --target ajazz-control-center ajazz_unit_tests -j$(nproc) 2>&1 | tail -5 && ctest --preset linux-release -E qml 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The confirmed root cause is documented with file:line in the plan SUMMARY (not a guess — observed via the driven drag).
    - The fix touches ONLY DragRelay.qml / Main.qml / ActionLibraryPane.qml (verify with `git diff --name-only`); no DropArea/KeyCell/DeviceView/ProfileController edits.
    - ctest `-E qml` passes at >= baseline (well above 739), 0 failed.
  </acceptance_criteria>
  <done>The dead-drag root cause is identified with file:line and fixed in the gesture layer only; tests green; commit path untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify real-mouse drag binds + renders</name>
  <action>Human-in-the-loop verification gate. Drive the live debug-channel checks in <how-to-verify> against the running GUI, then pause for operator sign-off.</action>
  <what-built>A debug-channel pointer-drag driver (qml.drag / input.pointer) and a fix to the DragRelay/Drag.Internal Wayland gesture path so a physical-mouse drag of a plugin tile onto a key binds + renders.</what-built>
  <how-to-verify>
    1. Build: `cmake --build build/linux-release --target ajazz-control-center -j$(nproc)`.
    2. Launch GUI: `setsid env AJAZZ_DEBUG_CONTROL=1 build/linux-release/src/app/ajazz-control-center` (AKP05E connected).
    3. AUTONOMOUS regression: `scripts/ajazz-debug qml.drag --params '{"from":"libraryTile_0","to":"<keycell-objectName>"}'`; then `scripts/ajazz-debug profile.list` (or qml.get on the cell) and confirm the key now holds the dragged plugin action; `scripts/ajazz-debug screenshot --params '{"path":"/tmp/drag.png"}'` and read it — the key shows the plugin icon.
    4. REAL MOUSE (the crux, Phase-28 trap): with a physical mouse, drag a plugin tile from the action library onto an empty key. Confirm the binding persists AND the device renders the plugin. Repeat onto an encoder dial and a touch-strip zone.
  </how-to-verify>
  <resume-signal>Type "approved" once a REAL physical-mouse drag binds + renders, or describe what still fails to drop.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                          |
| ------------------------ | ---------------------------------------------------------------------------------------------------- |
| debug channel -> live UI | AJAZZ_DEBUG_CONTROL is opt-in, LocalSocket only; synthesized pointer events drive the running window |

## STRIDE Threat Register

| Threat ID        | Category  | Component                | Disposition | Mitigation Plan                                                                                               |
| ---------------- | --------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-29-01          | Elevation | qml.drag synthetic input | accept      | Channel is opt-in (env var), LocalSocket-bound, dev-only; no new attack surface vs existing input.key/qml.set |
| T-29-02          | Tampering | gesture-layer QML edit   | mitigate    | Edits confined to DragRelay/Main/ActionLibraryPane; commit path + DropArea untouched (git diff gate)          |
| \</threat_model> |           |                          |             |                                                                                                               |

<verification>
- `cmake --build build/linux-release --target ajazz-control-center ajazz_unit_tests -j$(nproc)` succeeds.
- `ctest --preset linux-release -E qml` >= baseline, 0 failed.
- A driven `qml.drag` AND a real physical-mouse drag both bind + render a plugin tile on a key.
</verification>

\<success_criteria>
A physical-mouse drag of a plugin tile onto a key/dial/touch-zone binds and renders, proven via a driven pointer sequence (not commitBinding); the root cause is documented with file:line; the debug channel exposes a reusable pointer-drag driver; COD-031 clean; no protocol/wire changes.
\</success_criteria>

<output>
Create `.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-01-SUMMARY.md` when done. MUST record the confirmed dead-drag root cause with file:line.
</output>
