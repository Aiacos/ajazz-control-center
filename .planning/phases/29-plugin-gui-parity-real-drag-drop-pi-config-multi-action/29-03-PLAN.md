---
phase: 29-plugin-gui-parity-real-drag-drop-pi-config-multi-action
plan: 03
type: execute
wave: 3
depends_on: [29-01, 29-02]
files_modified:
  - src/app/src/profile_controller.hpp
  - src/app/src/profile_controller.cpp
  - src/app/qml/DeviceView.qml
  - src/app/qml/components/KeyBindingList.qml
autonomous: false
requirements: [PLUGIN-23]
must_haves:
  truths:
    - A key can hold more than one action (OpenDeck multi-action)
    - Actions in a key's binding list can be drag-reordered
    - A binding can be removed by drag-to-trash, and the multi-action list persists across restart
  artifacts:
    - path: src/app/src/profile_controller.cpp
      provides: append/reorder/remove-at-index for a key's onPress action vector
      contains: onPress
    - path: src/app/qml/components/KeyBindingList.qml
      provides: per-key binding-list editor with drag-reorder + drag-to-trash
      min_lines: 30
  key_links:
    - from: src/app/qml/components/KeyBindingList.qml
      to: ProfileController append/reorder/remove RPCs
      via: Q_INVOKABLE calls on edit
      pattern: ProfileController\.(appendKeyAction|reorderKeyAction|removeKeyActionAt)
    - from: src/app/src/profile_controller.cpp
      to: core::Profile Binding.onPress vector
      via: push_back / erase / rotate on onPress
      pattern: onPress\.(push_back|erase|insert)
---

<objective>
Extend the editor to OpenDeck per-button parity: a key can hold MULTIPLE actions (`core::Profile` Binding.onPress is already a `std::vector<Action>`), the action list can be drag-reordered, and an action can be removed by drag-to-trash. Today QML + ProfileController commit a SINGLE action (`binding.onPress = {act}` overwrites at profile_controller.cpp:409). Persist the multi-action list across restart.

Purpose: PLUGIN-23 — OpenDeck multi-action editing parity at the QML + ProfileController + core::Profile layer. The model layer is already multi-capable; this plan adds the controller verbs and the QML binding-list editor.

Output: ProfileController append/reorder/remove-at-index verbs for a key's onPress vector; a per-key KeyBindingList editor with drag-reorder + drag-to-trash; persistence across restart; affordance gating preserved.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase — use directly. -->

SERIALIZATION: This plan (29-03) and 29-02 BOTH modify src/app/qml/DeviceView.qml. They MUST
NOT run in parallel. 29-03 is wave 3 (depends_on [29-01, 29-02]); 29-02 (wave 2) lands its
DeviceView edit (the Inspector deviceCodename/keyIndex bindings, ~DeviceView.qml:431-450) first.
29-03 then adds the KeyBindingList surface to DeviceView. Re-read DeviceView.qml fresh before
editing — 29-02's bindings will already be present.

core::Profile (src/core/include/ajazz/core/profile.hpp):
struct Action { ActionKind kind; std::string id; ... }; // kind 0 = Plugin
struct Binding { std::vector<Action> onPress; onRelease; onLongPress; ... }; // ALREADY multi
Keypad bindings live in Profile.keys (map idx -> Binding).

ProfileController (src/app/src/profile_controller.{hpp,cpp}):
Q_INVOKABLE void commitKeyBinding(int keyIndex, icon, label, actionKind, params, actionId)
-> profile_controller.cpp:409 does `binding.onPress = {std::move(act)};` ← single-overwrite to EXTEND.
Q_INVOKABLE QVariantList activeKeyBindings() const -> cpp:295; today exposes only onPress.front()
as {index, iconSource, label, actionKind, actionId}. ← EXTEND to expose the full onPress list.
Existing swap verbs to mirror the style: swapEncoderBindings/swapTouchZoneBindings (cpp), saveActiveProfile/loadActiveProfile.

DeviceView.qml owns `codename` (:54) + `selectedKeyIndex` (:56); :316-330 onKeyActionDropped:
updates the live `bindings` model + calls commitKeyBinding; the Phase-26 trash DropArea
(trashBtn/trashDropArea, DeviceView.qml ~353-383) already exists (D-09) for drag-to-remove.
The Inspector is instantiated at DeviceView.qml:431-450 — do NOT add the KeyBindingList INSIDE
the Inspector; surface it in DeviceView (see Task 2).

KeyCell.qml onDropped (lines 247-278) is the drop entry; cellActionDropped/cellSwapRequested are its signals.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileController multi-action verbs over the onPress vector</name>
  <files>src/app/src/profile_controller.hpp, src/app/src/profile_controller.cpp</files>
  <read_first>
    - src/app/src/profile_controller.hpp (commitKeyBinding, activeKeyBindings, swap* signatures)
    - src/app/src/profile_controller.cpp:295-460 (activeKeyBindings + commitKeyBinding bodies)
    - src/core/include/ajazz/core/profile.hpp:67-116 (Action + Binding)
    - tests/unit (find the existing profile_controller test file to extend)
  </read_first>
  <behavior>
    - appendKeyAction(keyIndex, actionKind, params, actionId): pushes a new Action onto Binding.onPress WITHOUT clearing existing entries (a key with one action then gains a second).
    - reorderKeyAction(keyIndex, fromPos, toPos): moves the action at fromPos to toPos within onPress; out-of-range is a no-op (no crash).
    - removeKeyActionAt(keyIndex, pos): erases onPress[pos]; pos out of range is a no-op; removing the last action leaves an empty onPress (key reads as empty/cleared).
    - activeKeyBindings() exposes the FULL onPress list per key (a nested QVariantList of {actionKind, actionId, label, iconSource}), preserving the existing front()-derived top-level fields for back-compat.
    - Round-trip: append two actions, saveActiveProfile, loadActiveProfile -> both actions survive in order.
  </behavior>
  <action>
    Add three Q_INVOKABLE verbs to ProfileController (hpp + cpp): appendKeyAction, reorderKeyAction, removeKeyActionAt, operating on Profile.keys[keyIndex].onPress (push_back / std::rotate or erase+insert / erase). Mirror the keyIndex range validation already in commitKeyBinding (cpp:382) and the actionKind range guard (cpp:390). Keep commitKeyBinding's single-overwrite behavior as the "set primary / replace-all" entry (drag-drop onto an empty key still uses it), but make appendKeyAction the additive path. Extend activeKeyBindings() to include the full per-key action list (nested list) alongside the existing front-derived fields. Persist via the existing save/load path (onPress is already serialized — confirm the profile JSON writer/reader round-trips a multi-element onPress; if it truncates to one, fix the serializer in the app-layer profile IO, NOT in core wire code). COD-031: no nlohmann in core. Write the failing tests FIRST (RED) per the behavior block, then implement (GREEN).
  </action>
  <verify>
    <automated>cmake --build build/linux-release --target ajazz_unit_tests -j$(nproc) 2>&1 | tail -5 && ctest --preset linux-release -R 'profile|Profile' -E qml 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - profile_controller.hpp declares appendKeyAction / reorderKeyAction / removeKeyActionAt as Q_INVOKABLE.
    - New unit tests cover append-keeps-existing, reorder, remove-at-index, and save/load round-trip of a 2-action onPress; all pass.
    - `ctest -R 'profile|Profile' -E qml` 0 failed; `grep -rn nlohmann src/core/include/` returns 0.
  </acceptance_criteria>
  <done>ProfileController exposes multi-action append/reorder/remove over onPress, persists a multi-element list across save/load, with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Per-key binding-list editor (KeyBindingList.qml) with drag-reorder + drag-to-trash</name>
  <files>src/app/qml/components/KeyBindingList.qml, src/app/qml/DeviceView.qml</files>
  <read_first>
    - src/app/qml/DeviceView.qml (selectedKeyIndex, bindings model, onKeyActionDropped, trashBtn/trashDropArea ~353-383, and 29-02's Inspector deviceCodename/keyIndex bindings ~431-450 which land first)
    - src/app/qml/components/KeyCell.qml:186-279 (DropArea + affordance gate to mirror)
    - src/app/qml/DragRelay.qml (the relay the reorder/trash drags must route through — Wayland)
  </read_first>
  <action>
    Create src/app/qml/components/KeyBindingList.qml: a vertical list of the selected key's onPress actions (sourced from the extended activeKeyBindings() full list). Surface it for the selected key in DeviceView.qml ONLY — instantiate the KeyBindingList directly in DeviceView (near the device editor / Inspector area). Do NOT host it inside Inspector.qml: Inspector.qml is owned by 29-02 and editing it here is a merge trap. Each row shows the action icon/label and is a drag SOURCE routed through DragRelay (mime "application/x-ajazz-binding" with {controller:"Keypad", position, actionPos}) so drag-reorder works on Wayland (same relay pattern as KeyCell/EncoderDial — native Drag.Automatic is dead on niri). Dropping a row onto another row calls ProfileController.reorderKeyAction(keyIndex, fromPos, toPos); dropping a row onto the existing trash DropArea calls ProfileController.removeKeyActionAt(keyIndex, pos). A drop of a NEW library action onto the list calls ProfileController.appendKeyAction(...) (additive, keeping affordance gating mask & 1). Every row, the list, and any new control set objectName (e.g. "keyBindingRow_<pos>", "keyBindingList") per CLAUDE.md so it is debug-addressable. After every edit refresh the live `bindings` model so the device re-renders. Do NOT introduce native drag.
  </action>
  <verify>
    <automated>cmake --build build/linux-release --target ajazz-control-center -j$(nproc) 2>&1 | tail -5 && ctest --preset linux-release -E qml 2>&1 | tail -6 && grep -c 'objectName' src/app/qml/components/KeyBindingList.qml</automated>
  </verify>
  <acceptance_criteria>
    - KeyBindingList.qml exists, lists the selected key's onPress actions, and every row/list control sets objectName (`grep -c objectName` >= 2).
    - KeyBindingList is instantiated in DeviceView.qml (NOT inside Inspector.qml — `git diff --name-only` must NOT show Inspector.qml in this plan's diff).
    - Reorder rows -> ProfileController.reorderKeyAction; trash-drop -> removeKeyActionAt; new-action drop -> appendKeyAction (grep shows all three calls).
    - App builds; ctest `-E qml` >= baseline, 0 failed.
  </acceptance_criteria>
  <done>A per-key multi-action editor (hosted in DeviceView, not Inspector) renders the onPress list with DragRelay-routed reorder + drag-to-trash + additive append; debug-addressable; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify multi-action + reorder + trash + restart-persistence</name>
  <action>Human-in-the-loop verification gate. Drive the live debug-channel checks in <how-to-verify> against the running GUI, then pause for operator sign-off.</action>
  <what-built>ProfileController multi-action verbs + a per-key KeyBindingList editor: a key holds multiple actions, drag-reorder within the list, drag-to-trash to remove, persisted across restart.</what-built>
  <how-to-verify>
    1. Build + launch: `setsid env AJAZZ_DEBUG_CONTROL=1 build/linux-release/src/app/ajazz-control-center` (AKP05E + a plugin).
    2. Drag a plugin action onto an empty key; drag a SECOND action onto the same key — the KeyBindingList shows two rows.
    3. Drag-reorder the two rows (real mouse or `qml.drag` keyBindingRow_1 -> keyBindingRow_0); confirm order swaps and persists in the model.
    4. Drag one row to the trash zone; confirm it is removed and the device re-renders.
    5. Restart the app; confirm the key still holds its remaining multi-action list in order (persistence).
  </how-to-verify>
  <resume-signal>Type "approved" once multi-action + reorder + trash-remove + restart-persistence all work, or describe the failure.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                    | Description                                          |
| --------------------------- | ---------------------------------------------------- |
| QML editor -> core::Profile | user edits mutate the persisted profile action chain |

## STRIDE Threat Register

| Threat ID        | Category          | Component                          | Disposition | Mitigation Plan                                                                                 |
| ---------------- | ----------------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-29-05          | Tampering         | reorder/remove index args from QML | mitigate    | Range-validate keyIndex + pos; out-of-range is a no-op (mirrors commitKeyBinding cpp:382 guard) |
| T-29-06          | Denial of service | unbounded onPress growth           | accept      | OpenDeck-parity multi-action; user-driven append only; no programmatic loop                     |
| \</threat_model> |                   |                                    |             |                                                                                                 |

<verification>
- ajazz-control-center + ajazz_unit_tests build; `ctest --preset linux-release -E qml` >= baseline, 0 failed.
- `grep -rn nlohmann src/core/include/` returns 0 (COD-031).
- Live: 2+ actions per key, drag-reorder, drag-to-trash, survive restart.
</verification>

\<success_criteria>
A key holds multiple actions with drag-reorder and drag-to-trash matching OpenDeck per-button editing; persists across restart; affordance gating preserved; COD-031 clean; no protocol/wire changes.
\</success_criteria>

<output>
Create `.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-03-SUMMARY.md` when done.
</output>
