---
phase: 04-hot-plug-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/src/device_model.cpp
  - src/app/src/device_model.hpp
  - src/app/qml/components/DeviceList.qml
autonomous: false
requirements: [HOTPLUG-02, HOTPLUG-03, HOTPLUG-04]
must_haves:
  truths:
    - Sidebar rows are sorted lexicographically by (deviceClass, codename) at all times — stable across arrival, departure, re-arrival; never reordered by recency (D-04 + HOTPLUG-04).
    - A user's QML selection (the highlighted sidebar row) survives a disconnect/reconnect cycle for the same codename — selection focus is retained because the row index doesn't move (HOTPLUG-03).
    - On hot-plug arrival or departure, only the affected row's ConnectedRole flips — no beginResetModel/endResetModel, no full QML list rebuild; the row stays in place with an offline badge while disconnected (HOTPLUG-02 + D-03).
    - 8 different rebadge VID/PIDs that share `codename=akp03` collapse to ONE sidebar row whose ConnectedRole=true iff ANY rebadge is plugged in (D-04 rebadge clarification).
  artifacts:
    - path: src/app/src/device_model.cpp
      provides: refresh() emits per-row dataChanged({ConnectedRole}); rows sorted lex by (family, codename); diff-driven not reset-driven
      contains: dataChanged
    - path: src/app/qml/components/DeviceList.qml
      provides: QML offline badge surfaces on rows where connected=false (consistent with existing badge styling)
      contains: offline
  key_links:
    - from: DeviceModel::refresh()
      to: dataChanged signal
      via: per-row emit dataChanged(index, index, {ConnectedRole})
      pattern: emit dataChanged|dataChanged\(
    - from: QML delegate
      to: ConnectedRole binding
      via: model.connected property exposed via roleNames()
      pattern: 'model.connected|connected:'
---

<objective>
Switch `DeviceModel::refresh()` from `beginResetModel`/`endResetModel` to a diff-driven fine-grained `dataChanged(idx, idx, {ConnectedRole})` propagation per D-03. Sort rows lexicographically by `(family, codename)` at registration / refresh time per D-04 + HOTPLUG-04. Surface an offline badge on disconnected rows in the QML DeviceList delegate per HOTPLUG-02. Selection retention (HOTPLUG-03) is automatic — fine-grained updates don't move row indices, so QML's `currentIndex` stays bound to the same row.

Purpose: closes Pitfall 15 (DeviceModel refresh thrash) properly without the QML-side codename-restore hack the v1.0 code briefly considered. Combined with Plan 03's debouncer, the user-visible UX flow is: cable yank → 300ms quiet → one dataChanged for the affected row → offline badge appears, selection preserved, scroll position preserved.

Output: ~30 LoC delta in `device_model.cpp` for the diff logic; small sort key addition; QML delegate gains the offline badge state (or extends an existing badge component if Phase 8's MaturityRole already lays the groundwork — for v1.1 we add the connected-state badge fresh).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/phases/04-hot-plug-hardening/04-CONTEXT.md
@.planning/research/PITFALLS.md
@src/app/src/device_model.cpp
@src/app/src/device_model.hpp
@src/app/qml/components/DeviceList.qml

<interfaces>
From src/app/src/device_model.hpp:
- Roles enum at line 60-73: ModelRole, CodenameRole, FamilyRole, VidRole, PidRole, ConnectedRole, KeyCountRole, GridColumnsRole, EncoderCountRole, DpiStageCountRole, HasRgbRole, HasTouchStripRole.
- Private members: `std::vector<core::DeviceDescriptor> m_rows; std::set<std::pair<uint16_t,uint16_t>> m_connected;`.

From src/app/src/device_model.cpp:

- refresh() at line 105-110: currently does beginResetModel; rebuild m_rows from registry.enumerate(); refreshLiveEnumeration() (which updates m_connected); endResetModel.
- data() ConnectedRole at line 64-70: returns `m_connected.find(make_pair(d.vendorId, d.productId)) != m_connected.end()`.
- m_rows is built from `m_registry.enumerate()` which currently returns descriptors in registration insertion order (registerDevice push_back).

From src/app/qml/components/DeviceList.qml (current — check actual delegate structure):

- Repeater + ColumnLayout pattern (per recent commit 42febbd — "switch to Repeater+ColumnLayout for connected-only filter").
- Recent commit d377d80 added "show only currently-connected devices in the sidebar" — Phase 4 reverses this: rows STAY in place when disconnected (offline badge), per HOTPLUG-02. The filter must be removed; the delegate's visual treatment must distinguish online vs offline.

The D-04 rebadge insight: rows are keyed by `codename`, not by `(vid, pid)`. Multiple registered backends with the same `codename` (8 AKP03 rebadges) MUST collapse into one logical sidebar row in `m_rows`. The collapse happens at `refresh()` time when consuming `registry.enumerate()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff-driven refresh() + lex sort + codename collapse</name>
  <files>src/app/src/device_model.cpp, src/app/src/device_model.hpp</files>
  <read_first>
    - src/app/src/device_model.hpp (full file — Roles enum, members, signature of refresh())
    - src/app/src/device_model.cpp (full file — refresh() at line 105-110; refreshLiveEnumeration() at line 112-129; data() ConnectedRole branch at line 64-70)
    - .planning/phases/04-hot-plug-hardening/04-CONTEXT.md (D-03 fine-grained dataChanged; D-04 rebadge clarification: row identity = codename; HOTPLUG-04 lex sort by (deviceClass, codename))
    - .planning/research/PITFALLS.md Pitfall 15 (the refresh-thrash this fix closes)
    - src/core/include/ajazz/core/device.hpp (DeviceDescriptor shape — `family`, `codename` fields)
  </read_first>
  <action>
    Edit src/app/src/device_model.cpp `refresh()` body (line 105-110):
    - Remove `beginResetModel()` and `endResetModel()`.
    - Snapshot the current state before refreshing: `auto const old_connected = m_connected;` (the per-(vid,pid) set we computed last time).
    - Rebuild m_rows from `m_registry.enumerate()`, then COLLAPSE by codename: build a `std::vector<DeviceDescriptor> collapsed;` walking registry output once and skipping duplicates whose `codename` already appears in `collapsed`. For collapse-conflicts (e.g. AKP03 rebadges sharing codename="akp03"), retain the first descriptor encountered — its `model`/`vendorId`/`productId` fields are representative; ConnectedRole is OR'd across all rebadge keys (next bullet).
    - SORT `collapsed` lexicographically by `(family, codename)`: `std::sort(collapsed.begin(), collapsed.end(), [](auto const& a, auto const& b){ if (a.family != b.family) return static_cast<int>(a.family) < static_cast<int>(b.family); return a.codename < b.codename; });`
    - For the ConnectedRole semantics under D-04: when a row's codename has N registered (vid, pid) pairs in the registry, the row is connected iff ANY of them is in `m_connected`. Build an internal `std::map<std::string codename, std::set<pair<uint16_t,uint16_t>>> m_codename_keys` member at refresh time so ConnectedRole's data() lookup can OR across rebadge VID/PIDs. (Add the member to device_model.hpp.)
    - If `collapsed.size() != m_rows.size()` (e.g. a brand-new backend was registered between calls — unlikely during a Phase 4 run since registration is bootstrap-only, but defensively handled): fall back to a `beginResetModel/endResetModel` pair — this is the "registered backend list grew" path. Log INFO "rows.size changed — performing reset" so it's visible if it ever happens.
    - Common path (sizes equal, codenames stable): assign `m_rows = std::move(collapsed);`, call `refreshLiveEnumeration()` to update `m_connected`, then for each row whose connected-state-now != connected-state-then, emit `dataChanged(createIndex(row, 0), createIndex(row, 0), {ConnectedRole});` Use the new `m_codename_keys` to compute connected-now vs connected-then per row.

```
Edit src/app/src/device_model.cpp `data()` ConnectedRole branch (currently line 64-70): change from `m_connected.find(make_pair(d.vendorId, d.productId)) != m_connected.end()` to a codename-OR lookup using `m_codename_keys[d.codename]` — return true iff any (vid, pid) in that set intersects `m_connected`.

Update doc comment on `refresh()` to describe the diff-driven contract per D-03: "Diffs old vs new connected-state per codename row, emits per-row dataChanged({ConnectedRole}) for changed rows. Selection and scroll position survive automatically — no row index moves. Falls back to beginResetModel/endResetModel only if the row count changes (new backend registered)."
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release && ctest --preset linux-release -R "(device_model|device_registry)" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "beginResetModel" src/app/src/device_model.cpp` returns 1 hit at most (the fallback path), zero hits in the common refresh path.
    - `grep -q "dataChanged" src/app/src/device_model.cpp` exits 0 (fine-grained emit present).
    - `grep -q "m_codename_keys" src/app/src/device_model.cpp src/app/src/device_model.hpp` exits 0 (codename collapse member added).
    - `grep -E "std::sort.*(family|codename)" src/app/src/device_model.cpp` exits 0 — lex sort by (family, codename) is in source.
    - `cmake --build --preset linux-release` exits 0.
    - All existing tests pass — `ctest --preset linux-release` zero failures.
  </acceptance_criteria>
  <done>
    DeviceModel::refresh() is diff-driven. Rows are lex-sorted by (family, codename). Multiple rebadge VID/PIDs collapse onto one codename row whose ConnectedRole ORs across rebadge keys. Selection and scroll position will survive disconnect/reconnect because row indices don't move.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: QML offline badge + verify selection/scroll retention by hand</name>
  <files>src/app/qml/components/DeviceList.qml</files>
  <action>
    Edit src/app/qml/components/DeviceList.qml:
    - Remove the "show only currently-connected devices" filter introduced in commit d377d80 (the per-delegate `visible: connected` binding or the DelegateModel filter, depending on which form survived). Rows must always be present in the QML list — visibility doesn't depend on connected.
    - Within each row delegate, render a small "Offline" badge (text + subdued color) when `model.connected === false`. Reuse the existing typography + spacing token vocabulary from the v1.0 component library (commit 685a4a2) — likely a `Tag` or `Pill` component, or fall back to a `Label` with the existing offline-state color from the palette. If no analogous badge component exists, add a minimal inline rendering using a Rectangle + Text with the project's subtle/disabled palette token.
    - Do NOT add any animation, sound, or modal — per D-01 the only visible state change is the badge.
    - Do NOT add per-row "last seen N minutes ago" tooltip (deferred per CONTEXT Deferred Ideas).
    Then perform the manual verification described in `<how-to-verify>` and confirm before resume.
  </action>
  <what-built>
    Reversal of the connected-only filter (per d377d80) plus an "Offline" badge rendered when `model.connected === false`. Existing v1.0 typography/spacing tokens reused; no new toast, sound, or modal — D-01 silent-badge policy applies.
  </what-built>
  <how-to-verify>
    1. Build and launch the app: `cmake --build --preset linux-release && ./build/linux-release/src/app/ajazz-control-center` — at least one device should be connected.
    2. Confirm the sidebar shows ALL registered codenames (not just connected ones) — disconnected rows are visible with an "Offline" badge.
    3. Click to select a row corresponding to a CONNECTED device — note its position (e.g. "AKP153 is row 3, highlighted").
    4. Yank the USB cable for that device. Wait 1 second (covering the 300ms debounce).
    5. Confirm:
       (a) The row stayed at the SAME position (row 3, no jump).
       (b) The selection highlight is STILL on that row.
       (c) The Offline badge appeared on that row.
       (d) NO toast appeared (D-01 silent-badge policy).
       (e) Scroll position in the sidebar list did not change.
    6. Reconnect the same device. Wait 1 second.
    7. Confirm: Offline badge disappears, row stays at position 3, selection still on that row, no toast.
    8. For lex-sort check: pick two devices whose codenames are adjacent alphabetically (e.g. akp03 vs akp05). Confirm akp03 appears above akp05 in the sidebar regardless of which was last connected.
  </how-to-verify>
  <verify>Manual — covered by `<how-to-verify>` steps 1-8 above. No automated command for QML visual verification.</verify>
  <done>All 5 sub-checks (badge appears, row stays in place, selection retained, no toast, scroll preserved) pass after a real disconnect/reconnect cycle. Lex sort verified visually for at least one adjacent codename pair.</done>
  <resume-signal>Type "approved" if all 5 sub-checks (badge, row-stable, selection-stable, no toast, scroll-stable) pass, OR describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- A unit test in Plan 05 will assert that after a synthetic Removed event for a known (vid, pid), `DeviceModel` emits exactly one dataChanged for {ConnectedRole} on the affected row, and the row count is unchanged.
- Manual checkpoint in Task 2 verifies the user-visible end state.
- Lex sort is observable in the Sidebar visual order.
</verification>

\<success_criteria>

- HOTPLUG-02 satisfied: disconnected row stays in place with an offline badge.
- HOTPLUG-03 satisfied: selection survives disconnect/reconnect.
- HOTPLUG-04 satisfied: rows lex-sorted by (deviceClass, codename); never reordered.
- D-03 + D-04 implemented: no beginResetModel in the common path; codename is row identity.
- ROADMAP SC2 + SC3 satisfied.
  \</success_criteria>

<output>
After completion, create `.planning/phases/04-hot-plug-hardening/04-04-SUMMARY.md` documenting: how the codename-collapse handled the 8 AKP03 rebadges (one row, OR'd ConnectedRole — observed during manual test), whether the QML offline badge reused an existing component or required a new one, and the QML delegate's visual reduction (font weight, opacity reduction, or color shift used to indicate offline).
</output>
