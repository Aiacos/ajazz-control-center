---
phase: 33-property-inspector-end-to-end
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified: []
autonomous: false
requirements: [PI-03]

must_haves:
  truths:
    - A real .sdPlugin's PI JS calling $SD.setSettings({...}) causes didReceiveSettings to reach the plugin process
    - The setting written by the real PI survives a full app restart (persisted to disk per-context)
  artifacts: []
  key_links:
    - from: real PI JS $SD.setSettings()
      to: plugin process didReceiveSettings + on-disk <AppData>/plugins/<uuid>/settings/<ctx>.json
      via: PIBridge -> plugin_settings_store (atomic QSaveFile) -> PluginDeviceBridge::onPropertyInspectorSettings
      pattern: didReceiveSettings
---

<objective>
The DEFERRED human-verify checkpoint (ROADMAP criterion 5 / PI-03 human half). This is the ONE
criterion that cannot be driven headlessly — it requires a human to interact with a real plugin's PI
HTML so the PI's own JS calls `$SD.setSettings({...})`. Everything else (criteria 1-4) is already
built + headlessly verified by plans 01 and 02.

Per CONTEXT: build + headless-verify everything first, THEN pause and hand the user a concrete
walk-through using a REAL `.sdPlugin` with a settings PI (System Monitor / a weather plugin / a vendor
plugin) — NOT a synthetic test PI.

Purpose: proves the real-JS settings round-trip + restart persistence end-to-end.
Output: human-confirmed PI-03 round-trip + restart survival; phase ready for verify.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-property-inspector-end-to-end/33-RESEARCH.md
@.planning/phases/33-property-inspector-end-to-end/33-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: HUMAN-VERIFY — real PI JS $SD.setSettings() round-trip + restart (criterion 5)</name>
  <action>
    Claude does the prep autonomously (build, launch the WebEngine binary with the debug channel,
    install + bind a real .sdPlugin with a settings PI), then PAUSES and hands the user the
    walk-through below. This is the one criterion that cannot be driven headlessly — the human must
    interact with the real PI HTML so its JS calls $SD.setSettings(). Coordinate timing with the user
    first (STATE.md flags this as a user-agreed pause point). Do NOT mark DONE until the user reports
    the result. See <what-built>/<how-to-verify> for the concrete steps.
  </action>
  <what-built>
    The full PI stack: PI-01 render (GREEN), PI-02 objectName affordances (plan 02), PI-03 settings
    persistence (GREEN) + relay, and PI-04 lifecycle events (plan 01). Criteria 1-4 are already
    confirmed headlessly. This checkpoint is the real-JS half that only a human can drive.
  </what-built>
  <how-to-verify>
    Coordinate timing with the user first (STATE.md flags this as a user-agreed pause point).

```
Prep (Claude does this part):
1. Build + launch the WebEngine binary with the debug channel:
   `cmake --build build/linux-release --target ajazz-control-center`, launch with
   `AJAZZ_DEBUG_CONTROL=1` (kill any stale instance + rm the socket first).
2. Ensure a REAL .sdPlugin with a settings PI is installed and bound to a key (System Monitor
   recipe in memory project_plugin_install_demo_working, or a vendor/weather plugin with a
   configurable PI). Confirm which plugin is used (research Open Question 3).

Human steps (the user drives the real PI):
3. Select the bound action key so the PI panel opens (or click openPiButton); confirm the
   plugin's real PI HTML renders.
4. In the PI, change a setting so the PI's JS calls `$SD.setSettings({...})` (e.g. edit a field /
   toggle an option the plugin's PI exposes).
5. Confirm `didReceiveSettings` reaches the plugin (watch `scripts/ajazz-debug plugin.protocolLog`
   for the OUT didReceiveSettings, and/or observe the plugin reflect the change).
6. Fully quit and relaunch the app. Reopen the same action's PI. Confirm the setting the human
   wrote in step 4 is still present (read back from the PI and/or
   `<AppData>/plugins/<uuid>/settings/<ctx>.json`).

Pass = the real PI JS setSettings reached the plugin AND survived the restart.
```

</how-to-verify>
  <resume-signal>Type "approved" if the real PI setting reached the plugin and survived restart, or describe what failed (no render / no didReceiveSettings / setting lost on restart).</resume-signal>
</task>

</tasks>

<verification>
- Human confirms: real PI JS `$SD.setSettings()` -> didReceiveSettings reaches the plugin.
- Human confirms: the setting survives a full app restart (read back from the PI / on-disk store).
</verification>

\<success_criteria>

- ROADMAP criterion 5 satisfied: real-JS settings round-trip + restart survival, human-verified.
- Phase 33 ready for `/gsd:verify-work` (criteria 1-4 headless + criterion 5 human-verified).
  \</success_criteria>

<output>
Create `.planning/phases/33-property-inspector-end-to-end/33-03-SUMMARY.md` when done.
Record which real .sdPlugin was used + the human's approval. If criterion 5 fails, capture the exact
failure mode (render / relay / persistence) for a gap-closure plan.
</output>
