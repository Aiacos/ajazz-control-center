---
phase: 25-hardware-verification-real-plugin
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - .planning/phases/25-hardware-verification-real-plugin/25-UAT.md
  - docs/protocols/streamdeck/akp05.md
  - docs/protocols/streamdeck/akp05_vendor.md
autonomous: false
requirements: [VERIFY-05, VERIFY-06]
must_haves:
  truths:
    - A human operator has walked 25-UAT.md against the physically connected AKP05E and recorded pass/fail for every VERIFY-05 criterion
    - A real third-party .sdPlugin registered over the loopback WebSocket, painted a key via setImage, and received keyDown/dialRotate from a physical press/turn with observable effect (VERIFY-06)
    - Each provisional §5 wire item (DRA rect header, encoder-overlay framing, touch zone+swipe map) is reconciled; where the hardware contradicts a provisional value the RE doc (and code if needed) is corrected — hardware wins
    - hasClock=false confirmed on the device (no Sync button on the AKP05E row)
  artifacts:
    - path: .planning/phases/25-hardware-verification-real-plugin/25-UAT.md
      provides: Completed runbook with operator pass/fail results and reconciliation findings
      contains: 'result:'
    - path: docs/protocols/streamdeck/akp05.md
      provides: Reconciled §5 touch/encoder framing (updated only where hardware contradicted)
      contains: akp05
  key_links:
    - from: operator hardware observation
      to: docs/protocols/streamdeck/akp05.md and akp05_vendor.md
      via: 'confirm-or-correct: on mismatch the RE doc is updated (hardware wins)'
      pattern: DRA|touch|swipe|encoder
---

<objective>
The human operator walks the `25-UAT.md` runbook against the physically connected AKP05E, confirms
VERIFY-05 and VERIFY-06, and reconciles every provisional §5 RE value — updating the RE doc (and code
where needed) wherever the hardware contradicts a provisional value (hardware wins). This is the
phase witness; it cannot be auto-executed.

Purpose: Close the v1.3 milestone honestly. The whole slice is verified live, the provisional RE is
promoted to confirmed (or corrected), and a real third-party plugin proves the Elgato-compatible SDK
end-to-end.
Output: A completed `25-UAT.md` (pass/fail recorded) + any RE-doc/code corrections triggered by
hardware mismatches.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/phases/25-hardware-verification-real-plugin/25-CONTEXT.md
@.planning/phases/25-hardware-verification-real-plugin/25-UAT.md

\<read_first>

- `.planning/phases/25-hardware-verification-real-plugin/25-UAT.md` — the runbook authored in Plan 01;
  this plan executes it.
- `docs/protocols/streamdeck/akp05.md` (§5 touch-strip table: 4 zones, tap zone index 0..3, swipe
  gesture codes; the encoder-overlay framing) — the doc to update on mismatch.
- `docs/protocols/streamdeck/akp05_vendor.md` §4 + the `DRA` row (BE16 rect fields, location byte,
  32-byte header) — the doc to update on a DRA-layout mismatch.
- `CLAUDE.md` — RE is the source of truth BUT the hardware wins on provisional §5 values; the Linux
  device-access recovery (replug / setfacl on `/dev/hidraw*`; systemd ≥258); D-02 honesty contract.
  \</read_first>
  </context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator walks VERIFY-05 + provisional-§5 reconciliation on the AKP05E</name>
  <what-built>
    The full v1.3 device slice (Phases 14-23, suite green per Plan 01) plus the `25-UAT.md` runbook.
    Nothing further to automate here — the app is built and the runbook is authored; this step is the
    human witness against the physical AKP05E (`0300:3004`, fw `V3.AKP05E.01.007`).
  </what-built>
  <action>No further automation: the app is built (Plan 01 suite green) and the runbook authored. This step is the human witness — walk the 25-UAT.md VERIFY-05 tests and provisional-§5 rows against the physical AKP05E and record results. See how-to-verify for the exact steps.</action>
  <how-to-verify>
    Pre-flight: confirm `ctest --preset linux-release` is green; confirm `/dev/hidraw*` is readable —
    if root-only after a USB re-enumeration, physically replug or run
    `setfacl -m u:$(id -u):rw /dev/hidraw*` (systemd ≥258 uaccess regression, per CLAUDE.md). Launch
    the app, select the AKP05E in the sidebar.

```
Walk every VERIFY-05 test in `25-UAT.md` and record `result:` for each: (1) assigned image appears
on an LCD key; (2) a key press fires its bound action; (3) each encoder fires on rotate CW; (4)
each encoder fires on rotate CCW; (5) each encoder fires on press; (6) a touch-zone tap fires the
under-encoder action (zone 0..3); (7) swipe left/right changes pages; (8) the brightness slider
visibly changes brightness; (9) "clear all" blanks the panel; (10) the AKP05E row shows NO
Sync/time-sync control (hasClock=false).

Then reconcile each provisional-§5 row by observing the actual device behaviour/wire bytes: the
`DRA` rect-header layout, the encoder-overlay framing (in-code per-encoder ENC LCD vs touch-strip
zone — which one the hardware actually renders), and the touch zone + swipe coordinate map. For
each, record CONFIRMED or the corrected value in `25-UAT.md`.
```

</how-to-verify>
  <acceptance_criteria>
    - Every VERIFY-05 numbered test in `25-UAT.md` has a recorded `result:` (pass or fail with note).
    - Each of the three provisional-§5 rows is recorded as CONFIRMED or with the corrected value.
    - The hasClock=false row is confirmed (no Sync button observed).
    - Operator-witnessed: this step is not auto-executable.
  </acceptance_criteria>
  <resume-signal>Type "VERIFY-05 done" with pass/fail per criterion and the three provisional-§5 findings (confirmed or corrected), or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs a real third-party .sdPlugin (VERIFY-06)</name>
  <what-built>
    The Elgato-compatible SDK (Phases 17-22): loopback WebSocket server, manifest parser, spawn host,
    signature gate, setImage/event routing. This step exercises it with a real third-party plugin.
  </what-built>
  <action>No further automation: the SDK is built. Install and run a trusted third-party .sdPlugin and record the VERIFY-06 round-trip in 25-UAT.md. See how-to-verify for the exact steps.</action>
  <how-to-verify>
    Install a simple, trusted third-party Elgato/Mirabox `.sdPlugin` (an HTML or system-Node ≥20
    plugin). Confirm it passes the Phase-22 signature gate, then: it registers over the loopback
    (LocalHost-only) WebSocket; it paints a key via `setImage` (the image appears on the physical key);
    a physical key press delivers `keyDown` to the plugin and an encoder turn delivers `dialRotate`,
    each with the plugin's observable effect. Record pass/fail in the `25-UAT.md` VERIFY-06 section.
  </how-to-verify>
  <acceptance_criteria>
    - The sample `.sdPlugin` registered over the loopback WS, painted a key via setImage, and received
      keyDown + dialRotate from a physical press/turn with observable effect.
    - The VERIFY-06 section of `25-UAT.md` records the result.
    - Operator-witnessed: this step is not auto-executable.
  </acceptance_criteria>
  <resume-signal>Type "VERIFY-06 done" with the round-trip result (register / setImage / keyDown / dialRotate), or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply RE-doc/code reconciliation for any hardware mismatch (hardware wins)</name>
  <files>docs/protocols/streamdeck/akp05.md, docs/protocols/streamdeck/akp05_vendor.md, .planning/phases/25-hardware-verification-real-plugin/25-UAT.md</files>
  <read_first>
    - `25-UAT.md` reconciliation findings recorded in Tasks 1-2.
    - `docs/protocols/streamdeck/akp05.md` §5 + `akp05_vendor.md` §4 / `DRA` row — the sections to edit.
    - `CLAUDE.md` — hardware wins on provisional values; before touching any opcode/wire layout,
      re-read the relevant RE doc; update the doc when the RE and hardware disagree.
  </read_first>
  <action>
    For each provisional-§5 item the operator recorded as CORRECTED (not CONFIRMED) in `25-UAT.md`,
    update the relevant RE doc to the hardware-observed value — the DRA rect-header layout in
    `akp05_vendor.md` §4, the encoder-overlay framing (drop whichever of the in-code per-encoder ENC
    LCD model vs touch-strip-zone model the hardware does NOT use) in `akp05.md` §5, and the touch
    zone + swipe coordinate map in `akp05.md` §5. Remove or downgrade the "provisional"/"unconfirmed"
    flags on confirmed rows; replace provisional values with the confirmed/corrected ones. Where a
    correction implies a small code change (e.g. the encoder-overlay framing that lost), make the
    minimal correction in the affected `src/devices/streamdeck/**` code to match the hardware — this is
    a provisional-value reconciliation, not a new feature; do NOT add new capabilities. If every item
    was CONFIRMED with no mismatch, this task is limited to removing the "provisional" flags and
    recording the confirmation. Mark `25-UAT.md` Summary as complete.
  </action>
  <verify>
    <automated>grep -rniE 'provisional|unconfirmed' docs/protocols/streamdeck/akp05.md docs/protocols/streamdeck/akp05_vendor.md | grep -iE 'DRA|touch|swipe|encoder|zone' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Every provisional-§5 item recorded as CORRECTED is reflected in the RE doc (hardware value), and
      the corresponding "provisional"/"unconfirmed" flag is removed or downgraded.
    - Any code correction implied by a losing encoder-overlay framing is minimal and matches the
      hardware (no new capability added).
    - If a code file changed, `ctest --preset linux-release` is still green.
    - `25-UAT.md` Summary records the milestone-close verdict.
  </acceptance_criteria>
  <done>The provisional §5 RE is reconciled (confirmed or corrected to the hardware), the RE docs updated where the hardware contradicted them, and the 25-UAT.md Summary is complete.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                           |
| -------------------------------------------- | --------------------------------------------------------------------- |
| third-party `.sdPlugin` → loopback WebSocket | VERIFY-06 executes third-party plugin code over the LocalHost-only WS |
| operator host → `/dev/hidraw*`               | Operator-local device-access ACL (uaccess / setfacl)                  |

## STRIDE Threat Register

| Threat ID        | Category  | Component                             | Disposition | Mitigation Plan                                                                                                                                                                        |
| ---------------- | --------- | ------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-25-04          | Tampering | real `.sdPlugin` run during VERIFY-06 | accept      | The Phase-22 signature gate (verified pre-run) + the Phase-18 spawn host already mitigate; the UAT uses a trusted sample plugin. No new surface; the runbook exercises existing gates. |
| T-25-05          | Spoofing  | plugin registers over loopback WS     | mitigate    | The server is LocalHost-only (PLUGIN-01 invariant) + passHello salt/challenge (PLUGIN-05); the operator confirms the loopback-only bind as a runbook precondition.                     |
| T-25-06          | Elevation | operator setfacl on hidraw nodes      | accept      | Operator-local action scoped to `/dev/hidraw*` (CLAUDE.md); no system-level mutation by tooling.                                                                                       |
| T-25-SC          | Tampering | npm/pip/cargo installs                | accept      | No package installs; the sample plugin is run by the existing spawn host, not installed by this plan.                                                                                  |
| \</threat_model> |           |                                       |             |                                                                                                                                                                                        |

<verification>
- `25-UAT.md` has a recorded `result:` for every VERIFY-05 criterion and the VERIFY-06 round-trip, and
  a CONFIRMED/CORRECTED value for each provisional-§5 row.
- RE docs updated wherever the operator recorded a CORRECTED value; "provisional" flags removed on
  confirmed rows.
- If any code changed, `ctest --preset linux-release` is green.
</verification>

\<success_criteria>

- VERIFY-05 confirmed live on the AKP05E (image, key press, 4 encoders CW/CCW/press, touch tap-zone,
  swipe-page, brightness, clear-all, hasClock=false).
- VERIFY-06 confirmed: a real `.sdPlugin` registered, painted a key via setImage, and received
  keyDown/dialRotate from a physical press/turn with observable effect.
- Every provisional §5 wire item reconciled (hardware wins); RE docs corrected where contradicted; the
  family is promoted honestly and the milestone closes.
  \</success_criteria>

<output>
Create `.planning/phases/25-hardware-verification-real-plugin/25-02-SUMMARY.md` when done.
</output>
