---
phase: 20-property-inspector-settings
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - src/app/src/property_inspector_controller.cpp
  - src/app/qml/Inspector.qml
  - tests/unit/test_pi_bridge.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [PLUGIN-09]
user_setup: []
must_haves:
  truths:
    - Selecting an action that carries a propertyInspectorPath loads its HTML PI and the host emits didReceiveSettings carrying that action context's persisted settings
    - Selecting a built-in/native action (no propertyInspectorPath) closes the HTML inspector so the native renderer takes over
    - loadInspector inserts the cefQuery shim into the per-plugin profile once (mirroring the 18-03 mechanism), so window.cefQuery exists before the PI page script runs
  artifacts:
    - path: src/app/src/property_inspector_controller.cpp
      provides: cefQuery shim insertion in loadInspector + a getSettings emit after load
      contains: makeCefQueryShim
    - path: src/app/qml/Inspector.qml
      provides: action-selection -> loadInspector / closeInspector trigger reading binding.propertyInspectorPath
      contains: loadInspector
  key_links:
    - from: Inspector.qml action selection
      to: PropertyInspectorController.loadInspector
      via: binding.propertyInspectorPath != '' branch
      pattern: loadInspector
    - from: loadInspector
      to: PIBridge::getSettings -> didReceiveSettings
      via: emit on load so the PI receives its persisted settings
      pattern: getSettings
    - from: loadInspector
      to: makeCefQueryShim()
      via: profile->scripts()->insert on a fresh profile
      pattern: scripts
---

<objective>
Close the third PLUGIN-09 gap: the registerPropertyInspector LOAD handshake is not driven from the
UI today (loadInspector has zero QML callers; Inspector.qml never touches the controller). Wire
action-selection -> loadInspector -> didReceiveSettings, insert the Phase-20 cefQuery shim into the
per-plugin profile (mirroring the 18-03 injection mechanism on the SAME profile), and add the MINIMAL
action-model field the editor needs to carry a PI path. The relay (sendToPlugin/sendToPropertyInspector)
and passHello are gated on Phases 17/18 (NOT executed) — see the STOP gate; the live plugin-process
round-trip witness is Phase 25.

Purpose: PLUGIN-09 requires that selecting an action with a PropertyInspectorPath loads its HTML PI
and the host replies with the action's current settings (didReceiveSettings). This plan delivers that
trigger + the shim insertion seam, hardware-free.
Output: loadInspector inserts makeCefQueryShim() once per fresh profile and emits the action's
settings on load; Inspector.qml triggers load/close on selection; a controller-level test proves the
load -> didReceiveSettings path.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-property-inspector-settings/20-CONTEXT.md
@.planning/phases/20-property-inspector-settings/20-RESEARCH.md
@.planning/phases/20-property-inspector-settings/20-VALIDATION.md
@CLAUDE.md

\<stop_gate>
This plan references seams owned by Phases 17 and 18, which are PLANNED but NOT EXECUTED (no SUMMARY
files: `.planning/phases/17-*/17-*-SUMMARY.md` and `.planning/phases/18-*/18-*-SUMMARY.md` are absent).
Before implementing the RELAY wiring in Task 3, STOP and confirm:

- If `.planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md` (sendEvent host->plugin writer)
  does NOT exist, the SdPluginServer has NO sendEvent / passHello surface to connect to. In that case
  Task 3 MUST scope to the bridge-side seam only (verify the existing PIBridge::sendToPropertyInspector
  signal + sendToPlugin slot are present and documented as the relay endpoints) and DEFER the live
  bridge\<->server connection + passHello reply to the phase that lands sendEvent (17/19/25).
- Tasks 1-2 (the LOAD handshake + shim insertion + UI trigger) are INDEPENDENT of 17/18 and proceed
  regardless — they touch only PropertyInspectorController, Inspector.qml, and the PI test.

Do not invent a sendEvent / passHello API on SdPluginServer in this plan; that is Phase 17's deliverable.
\</stop_gate>

<interfaces>
From src/app/src/property_inspector_controller.hpp (the singleton + load API):
  Q_INVOKABLE void loadInspector(QString const& pluginUuid, QString const& htmlAbsPath,
                                 QString const& actionUuid, QString const& contextUuid);
  Q_INVOKABLE void closeInspector();
  Q_PROPERTY(bool webEngineAvailable READ ... CONSTANT)
  Q_PROPERTY(bool hasHtmlInspector READ ... NOTIFY hasHtmlInspectorChanged)
  // QML_NAMED_ELEMENT(PropertyInspectorController) + QML_SINGLETON (registerInstance/create).

From property_inspector_controller.cpp loadInspector (the seam to extend, lines 159-241):

- creates/reuses webEngine\_->profilesByPluginUuid[pluginUuid] (freshProfile flag at line 179-188)
- installs PIUrlRequestInterceptor on the profile (lines 196-203)
- builds a fresh QQmlWebChannel + PIBridge registered as "$SD" (lines 209-223)
- emits activeInspectorChanged; sets hasHtmlInspector\_ = true (lines 219-228)
  // GAP: never inserts a QWebEngineScript; never emits the action's settings on load.

From src/app/src/pi_cef_shim.hpp (Plan 20-02 deliverable):
QWebEngineScript makeCefQueryShim(); // DocumentCreation / MainWorld / runsOnSubFrames (WebEngine-gated)

From src/app/src/pi_bridge.hpp:
Q_INVOKABLE void getSettings(); // emits didReceiveSettings(json) read from disk (20-01-proven)
signals: void sendToPropertyInspector(QString json); // relay endpoint, plugin->PI direction
Q_INVOKABLE void sendToPlugin(QString const& json); // relay endpoint, PI->plugin direction (M5 stub today)

From src/app/qml/Inspector.qml (the editor pane, today bound to a `binding` JS object):
property var binding: null; // keys: actionKind, actionParams, label, iconSource (NO PI fields yet)
signal bindingFieldChanged(string field, var value);
// GAP: Inspector.qml does not reference PropertyInspectorController or loadInspector at all.

From src/app/qml/PropertyInspector.qml:
// Loader switches to PIWebView when PropertyInspectorController.hasHtmlInspector && webEngineAvailable;
// else NativePropertyInspector. The controller decides; QML follows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loadInspector inserts the cefQuery shim once + emits the action's settings on load</name>
  <files>src/app/src/property_inspector_controller.cpp, tests/unit/test_pi_bridge.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - src/app/src/property_inspector_controller.cpp lines 159-241 (loadInspector; the freshProfile guard + interceptor wiring + channel/bridge build).
    - src/app/src/pi_cef_shim.hpp (Plan 20-02: makeCefQueryShim signature + the WebEngine gate).
    - src/app/src/pi_bridge.hpp getSettings + didReceiveSettings (the settings-on-load mechanism, proven by 20-01).
    - 20-RESEARCH.md "Pattern 1" insertion-site note (profile->scripts()->insert(makeCefQueryShim()) guarded on freshProfile, mirroring the interceptor wiring at :198) + Pitfall 1 (inject at DocumentCreation, not after load).
    - tests/unit/CMakeLists.txt lines 193-219 (the SOURCES block; property_inspector_controller.cpp is NOT currently linked into the test binary — adding it requires the WebEngine gate, see action).
  </read_first>
  <behavior>
    - On a FRESH per-plugin profile, loadInspector inserts makeCefQueryShim() into profile->scripts() exactly once (guarded on the existing freshProfile flag, like the interceptor at line 198), so window.cefQuery exists before the PI page script runs. Re-loading another action for the same plugin does NOT insert a duplicate.
    - After the channel + bridge are built and activeInspectorChanged is emitted, loadInspector triggers the bridge's getSettings() so the freshly-loaded PI receives its persisted per-context settings via didReceiveSettings (the §7 handshake's "+ current settings" half).
    - When AJAZZ_HAVE_WEBENGINE is absent, the behavior is unchanged (the existing stub path: hasHtmlInspector stays false).
  </behavior>
  <action>
    In property_inspector_controller.cpp loadInspector, inside the AJAZZ_HAVE_WEBENGINE block: after the
    profile is resolved and the freshProfile flag is known (line ~188), insert the cefQuery shim into the
    profile's script collection when freshProfile is true — profile->scripts()->insert(makeCefQueryShim())
    — mirroring the interceptor's freshProfile guard so the script is added once per profile, not per load.
    Include pi_cef_shim.hpp in the AJAZZ_HAVE_WEBENGINE include group. After the activeInspectorChanged emit
    (line ~223), call the bridge's getSettings() (the bridge built at line 216) so the PI receives its
    persisted settings on load. If the 18-03 Mirabox shim has ALSO landed by execution time (check for
    src/app/src/plugin_mirabox_shim.hpp), insert makeMiraboxShim() on the same freshProfile guard too;
    otherwise insert only the cefQuery shim (PLUGIN-11 stays Phase 18's deliverable — do not re-own it).
    CMake (tests): to make loadInspector unit-testable, link property_inspector_controller.cpp +
    pi_cef_shim.cpp + pi_url_request_interceptor.cpp into ajazz_unit_tests ONLY behind a WebEngine-gated
    block (mirror src/app/CMakeLists.txt:400-410) — i.e. find_package(Qt6 QUIET COMPONENTS WebEngineQuick
    WebChannelQuick) in tests/unit/CMakeLists.txt; if found, add those TUs + the AJAZZ_HAVE_WEBENGINE define
    + the WebEngine link to the test target and compile the controller-load TEST_CASE; if NOT found, skip
    linking the controller and gate the load TEST_CASE out (the 20-01/20-02 pure tests still cover
    persistence + shim string + dispatcher + sdpi helper without WebEngine). Add a [pi-bridge][inspector]
    case (WebEngine-gated) constructing PropertyInspectorController, registering it via registerInstance,
    writing settings for a context through a PIBridge first (reuse the 20-01 hermetic-AppData idiom), then
    calling loadInspector(plugin, htmlAbsPath, action, context) and asserting hasHtmlInspector() becomes
    true. The didReceiveSettings emit-on-load is observed at the controller's bridge; since the bridge is
    private, assert the load path indirectly (hasHtmlInspector flips true; activeUrl is the file URL) and
    leave the full DOM round-trip to Phase 25 (manual). ASCII titles.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -5; ctest --preset linux-release -R "inspector|pi-bridge" --output-on-failure; grep -c 'makeCefQueryShim' src/app/src/property_inspector_controller.cpp</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'makeCefQueryShim' src/app/src/property_inspector_controller.cpp >= 1, inside the AJAZZ_HAVE_WEBENGINE block, guarded on freshProfile (inserted once per profile).
    - loadInspector calls the bridge getSettings() after activeInspectorChanged so the PI receives its persisted settings on load.
    - The WebEngine-gated [inspector] case (where WebEngine is present) asserts loadInspector flips hasHtmlInspector() to true and sets activeUrl to the file URL; the non-WebEngine build still passes pi-bridge tests.
    - The app target builds clean; no QCefView introduced (grep -rn QCefView src/app/src/ stays 0).
  </acceptance_criteria>
  <done>loadInspector injects the cefQuery shim once per profile (DocumentCreation, mirroring 18-03) and emits the action's persisted settings on load; the load path is asserted where WebEngine is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inspector.qml triggers loadInspector / closeInspector on action selection</name>
  <files>src/app/qml/Inspector.qml</files>
  <read_first>
    - src/app/qml/Inspector.qml (full) — the `binding` var (actionKind/actionParams/label/iconSource) + bindingFieldChanged signal; it does NOT reference PropertyInspectorController today.
    - src/app/qml/PropertyInspector.qml (the Loader switches on PropertyInspectorController.hasHtmlInspector + webEngineAvailable).
    - src/app/src/property_inspector_controller.hpp (the QML singleton surface: webEngineAvailable, loadInspector, closeInspector).
    - 20-RESEARCH.md "Pattern 3: Action-selection -> loadInspector trigger" + A3 (the action model must carry the PI path + plugin/context uuids; add the MINIMAL fields).
    - CLAUDE.md QML gotchas (QML_SINGLETON; do not assign to read-only props).
  </read_first>
  <behavior>
    - When the selected binding carries a non-empty propertyInspectorPath AND PropertyInspectorController.webEngineAvailable is true, Inspector reacts to selection by calling PropertyInspectorController.loadInspector(binding.pluginUuid, binding.propertyInspectorPath, binding.actionUuid, binding.contextUuid).
    - When the selection has no propertyInspectorPath (built-in/native action) or WebEngine is unavailable, Inspector calls PropertyInspectorController.closeInspector() so PropertyInspector.qml's Loader falls back to NativePropertyInspector.
    - Selecting nothing (binding == null / hasSelection false) closes the inspector.
  </behavior>
  <action>
    Add the minimal PI fields to the binding contract read by Inspector.qml — read binding.propertyInspectorPath,
    binding.pluginUuid, binding.actionUuid, binding.contextUuid (all optional; absent => empty string).
    These are the MINIMAL action-model additions A3 calls for; the editor/KeyDesigner supplies them when a
    plugin action is selected (the model-side population is the editor's responsibility — Inspector.qml only
    consumes them, defaulting to "" when absent so existing non-plugin bindings keep working). Import
    AjazzControlCenter (already imported) so the PropertyInspectorController singleton is reachable. Add a
    function maybeLoadInspector() that implements the behavior branch above, and call it from an
    onBindingChanged handler (or a Connections/onSelectionLabelChanged that fires when the selected element
    changes). Use the singleton by type name (PropertyInspectorController.loadInspector / .closeInspector /
    .webEngineAvailable) — do NOT instantiate it. Guard every binding.<field> read with the existing
    `root.binding && ...` null-guard idiom already used throughout the file. Do NOT change the existing
    bindingFieldChanged form-field wiring. Keep it qmllint-clean (pragma ComponentBehavior already implied by
    the module; match the file's existing style).
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -8; grep -c 'loadInspector\|closeInspector' src/app/qml/Inspector.qml</automated>
  </verify>
  <acceptance_criteria>
    - grep -c on Inspector.qml shows both loadInspector and closeInspector referenced (>= 2 total).
    - The QML compiles into the app target (qmllint/qmlcachegen pass during the build) with no missing-property or duplicated-name warnings introduced.
    - The load branch reads binding.propertyInspectorPath + pluginUuid + actionUuid + contextUuid, each null-guarded; the no-PI / no-WebEngine branch calls closeInspector.
    - No read-only-property assignment to the controller; the singleton is used by type name, not instantiated.
  </acceptance_criteria>
  <done>Selecting a plugin action with a PI path drives loadInspector; selecting a native/built-in action (or no selection) drives closeInspector so the native renderer takes over.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the relay endpoints exist; defer the live bridge<->server connection per the STOP gate</name>
  <files>tests/unit/test_pi_bridge.cpp</files>
  <read_first>
    - The <stop_gate> above — check for 17-02-SUMMARY.md (sendEvent) before wiring anything server-side.
    - src/app/src/pi_bridge.hpp lines 95-122 (sendToPlugin slot + sendToPropertyInspector signal — the relay endpoints) + pi_bridge.cpp lines 332-339 (sendToPlugin is an M5 logging stub today).
    - src/app/src/sd_plugin_server.hpp lines 103-126 (signals: pluginRegistered/actionReceived/unhandledEventReceived — NO sendEvent/passHello today).
    - 20-RESEARCH.md Pitfall 5 + A4 (scope the relay WIRING here only if the server seam exists; the live plugin-process round-trip is Phase 25).
  </read_first>
  <action>
    Per the STOP gate: if `.planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md` is ABSENT (the
    expected state today), do NOT add any SdPluginServer sendEvent/passHello API and do NOT wire a live
    bridge<->server connection — that is Phase 17/19's deliverable. Instead, document the relay endpoints with
    an assertion-level test: add a [pi-bridge][relay] case to test_pi_bridge.cpp that constructs
    PIBridge(nullptr,"com.example.foo","act1","ctx1"), connects a lambda to the sendToPropertyInspector signal,
    manually emits it (or calls a path that emits it) and asserts the lambda fires — proving the PI-bound relay
    endpoint exists and is connectable; and asserts that calling sendToPlugin(json) does not crash and is a
    no-op stub today (the live route lands with sendEvent). Add a code comment in the test citing A4 + Pitfall 5
    that the live plugin-process round-trip is gated to the phase that lands SdPluginServer::sendEvent (17/19)
    and verified on hardware in Phase 25. If 17-02-SUMMARY.md IS present at execution time, additionally
    connect PIBridge::sendToPlugin -> SdPluginServer::sendEvent at the Application wiring layer and add a
    loopback test that a sendToPlugin call reaches the server's sendEvent for the bound uuid (only then). Qt
    JSON only. ASCII titles.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -3; ctest --preset linux-release -R "relay" --output-on-failure; ls .planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md 2>/dev/null || echo "17-02 NOT executed (relay deferred per STOP gate)"</automated>
  </verify>
  <acceptance_criteria>
    - The [relay] case proves sendToPropertyInspector is a connectable signal and sendToPlugin is a safe no-op stub today.
    - If 17-02-SUMMARY.md is absent, NO sendEvent/passHello API is added to SdPluginServer and no live connection is wired (deferred per A4); a comment records the deferral.
    - If 17-02-SUMMARY.md is present, the bridge->server connection is wired and a loopback test asserts sendToPlugin reaches sendEvent for the bound uuid.
    - ASCII titles; Qt JSON only.
  </acceptance_criteria>
  <done>The relay endpoints are confirmed present and test-pinned; the live bridge<->server route is wired only if Phase 17's sendEvent has landed, else deferred to Phase 17/19/25 per the STOP gate.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                           |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------- |
| PI page load -> per-plugin profile | The injected shim + interceptor scope the untrusted PI page; loadInspector picks the html path        |
| PI JS -> $SD relay                 | sendToPlugin / sendToPropertyInspector cross between the untrusted PI and the (future) plugin process |

## STRIDE Threat Register

| Threat ID        | Category                    | Component                                                       | Disposition                    | Mitigation Plan                                                                                                                                                                                                                 |
| ---------------- | --------------------------- | --------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-20-NAV         | Tampering / Info Disclosure | PI WebView navigation to an arbitrary remote origin             | mitigate (assert pre-existing) | The per-profile PIUrlRequestInterceptor + isLoadUrlAllowed gate every sub-resource (file:// scoped to piDir, https:// to the 2-host allowlist, http:// blocked); loadInspector keeps installing it per profile — unchanged here |
| T-20-SHIM-ONCE   | Tampering                   | duplicate shim insertion / wrong injection timing               | mitigate                       | Shim inserted once per fresh profile (freshProfile guard) at DocumentCreation (18-03 Pitfall 1/4)                                                                                                                               |
| T-20-RELAY-SCOPE | EoP                         | premature relay wiring exposing an unauthenticated plugin route | accept/defer                   | The live bridge\<->server connection is deferred until Phase 17 lands sendEvent + passHello auth (STOP gate); no relay API is invented here                                                                                     |
| \</threat_model> |                             |                                                                 |                                |                                                                                                                                                                                                                                 |

<verification>
- ctest --preset linux-release -R "inspector|pi-bridge|relay" green.
- grep -c 'makeCefQueryShim' src/app/src/property_inspector_controller.cpp >= 1.
- grep -c 'loadInspector' src/app/qml/Inspector.qml >= 1 and 'closeInspector' >= 1.
- grep -rn QCefView src/app/src/ returns 0.
- Full suite: ctest --preset linux-release green before /gsd:verify-work.
</verification>

\<success_criteria>
The registerPropertyInspector LOAD handshake is driven from the editor: selecting a plugin action
with a PI path loads the HTML PI, injects the cefQuery shim at DocumentCreation, and emits the action's
persisted settings via didReceiveSettings; native actions fall back to the schema renderer. The relay
endpoints are test-pinned; the live route is deferred to Phase 17/19/25 per the STOP gate.
\</success_criteria>

<output>
Create `.planning/phases/20-property-inspector-settings/20-03-SUMMARY.md` when done.
</output>
