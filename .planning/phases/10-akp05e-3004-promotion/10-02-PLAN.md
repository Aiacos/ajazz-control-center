---
phase: 10-akp05e-3004-promotion
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/devices/streamdeck/src/akp03.cpp
  - src/app/src/encoder_coalescer.hpp
  - src/app/src/encoder_coalescer.cpp
  - src/app/CMakeLists.txt
  - tests/unit/test_encoder_coalescer.cpp
  - tests/unit/CMakeLists.txt
  - docs/_data/devices.yaml
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - DEVICES-05
deferred_followups:
  - 'ARCH-05 DEFAULT VERDICT (capture-pending): the no-RTC verdict for akp05e is finalized by the Phase 9.x all-4-devices time-set capture (Pitfall 19 three-witness rule). DEVICES-05 clock removal is correct under the default verdict; commit body MUST cite the conditional status.'
  - 'Build precondition: Qt6 CorePrivate / qzipreader_p.h missing -> qt6-qtbase-private-devel. Resolve before any compile/ctest step.'
  - 'Encoder-to-QML wiring: the coalescer is a unit-tested primitive; live DeviceEvent fan-out to a QML-facing encoder controller is a follow-up if that path does not yet exist at execution time.'

must_haves:
  truths:
    - Spinning an AKP03 encoder delivers DeviceEvent::Kind::EncoderTurned for each detent and a proper DeviceEvent::Kind::EncoderReleased on knob release — the value=0 half-step workaround at akp03.cpp:289-293 is removed.
    - EncoderTurned delta is post-quadrature-decode (+1 CW / -1 CCW per detent), NOT raw quadrature pairs (Pitfall 23); no code path emits EncoderTurned with value==0 as a stand-in for a release.
    - 'A fast encoder spin is coalesced at the app/QML observer layer by a 16ms trailing-edge QTimer: many detents within one 16ms window emit ONE accumulated-delta signal (single signal per repaint frame).'
    - docs/_data/devices.yaml akp05e row no longer lists clock in capabilities; a notes line cites ARCH-05; maturity is functional.
  artifacts:
    - path: src/devices/streamdeck/src/akp03.cpp
      provides: Encoder press/release/turn dispatch without the value=0 workaround
      contains: EncoderReleased
    - path: src/app/src/encoder_coalescer.hpp
      provides: 16ms trailing-edge per-encoder delta-accumulating coalescer (mirrors HotplugDebouncer)
      contains: kCoalesceMs
    - path: src/app/src/encoder_coalescer.cpp
      provides: QTimer-backed accumulate-and-emit implementation
      contains: QTimer
    - path: docs/_data/devices.yaml
      provides: 'akp05e row: clock removed, ARCH-05 notes, maturity functional'
      contains: functional
    - path: tests/unit/test_encoder_coalescer.cpp
      provides: 'Tests: N detents in one window -> 1 signal with summed delta; per-encoder isolation; signed accumulation'
      contains: encoderCoalesced
  key_links:
    - from: src/app/src/encoder_coalescer.cpp
      to: src/app/src/hotplug_debouncer.hpp
      via: mirrors the per-key trailing-edge QTimer coalescer pattern
      pattern: QTimer
    - from: docs/_data/devices.yaml
      to: ARCH-05
      via: notes line citing the no-RTC default verdict
      pattern: ARCH-05
---

<objective>
NOTE 2026-05-20: the `0300:3004` device is firmware-confirmed to be an AKP05E
"Stream Dock Plus" (10 LCD keys / 4 endless encoders / LCD touch strip /
protocol_version 3), codename `akp05e`, routed via `makeAkp05` — NOT a 6-key
AKP03. The mechanics below reference the shared `akp03.cpp` Stream Dock backend
(the file name is historical). See STATE.md.

Make AKP05E encoder input correct and honest, and demote the false clock capability:
deliver a proper `EncoderReleased(int encoderIndex)` event in the backend (INPUT-01,
removing the `value=0` half-step workaround), coalesce fast encoder spins at the app/QML
observer layer with a 16ms trailing-edge `QTimer` (INPUT-02, mirroring the existing
`HotplugDebouncer`), and remove `clock` from the `akp05e` `devices.yaml` row with
a maturity bump to `functional` (DEVICES-05).

Purpose: The `core::DeviceEvent::Kind::EncoderReleased` enumerator already exists, and the AKP03
parser already classifies encoder press/release in `akp03_protocol.hpp`; this plan finishes the
wiring and removes the legacy `value=0` workaround, then closes the signal-storm (Pitfall 23) at
the UI boundary. DEVICES-05 lands the v1.1 D-02 honesty contract: no AJAZZ Stream Dock exposes a
host-settable RTC, so the row stops lying.

Output: corrected encoder dispatch, a reusable encoder coalescer, a catalogue row that tells the
truth, and the `scaffolded -> functional` promotion.

CONDITIONAL-EXECUTION GATES (plan-only artifact):

1. Build precondition. Qt6 `CorePrivate` / `qzipreader_p.h` missing -> needs
   `qt6-qtbase-private-devel`. No `cmake`/`ctest` step until installed (environment fix; no
   project-tree mutation per CLAUDE.md).
1. ARCH-05 DEFAULT VERDICT (capture-pending). Per
   `.planning/phases/09-research-captures-hygiene/ARCH-05.md`, the no-RTC verdict for
   `akp05e` is DEFAULT, finalized by the Phase 9.x all-4-devices time-set capture
   (Pitfall 19 three-witness rule — structurally unsatisfiable for clock on this family). The
   DEVICES-05 clock removal is correct under the default verdict; the commit body MUST cite the
   conditional status. ARCH-05.1 already flipped the verdict for `ak980pro` ONLY — that does NOT
   affect the Stream Dock family; AKP03 stays no-RTC.
1. Wave 2 ordering. This plan edits `akp03.cpp`, which plan 10-01 also edits. It MUST run after
   10-01 lands (depends_on: 10-01) — same-file ownership forces sequential waves.
   </objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-akp05e-3004-promotion/10-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-research-captures-hygiene/ARCH-05.md
@docs/protocols/streamdeck/akp03.md
@src/app/src/hotplug_debouncer.hpp
@CLAUDE.md

<interfaces>
<!-- The core event enum ALREADY has EncoderReleased; the AKP03 parser ALREADY classifies
     encoder press/release. The backend poll() already maps them. The "value=0 workaround" is
     historical phrasing — verify current state and remove any residual half-step hack. -->

From src/core/include/ajazz/core/device.hpp (DeviceEvent — already complete):

```
struct DeviceEvent {
    enum class Kind : uint8 { KeyPressed, KeyReleased, EncoderTurned, EncoderPressed,
                              EncoderReleased, TouchStrip, Connected, Disconnected };
    Kind kind; uint16 index; int32 value;   // value = encoder delta / press state
};
```

From src/devices/streamdeck/src/akp03_protocol.hpp (parser already emits these InputEvent kinds):

```
struct InputEvent {
    enum class Kind : uint8 { KeyPressed, KeyReleased, SideButton, EncoderTurned,
                              EncoderPressed, EncoderReleased, Nop };
    Kind kind; uint8 index; int8 delta;   // delta +1 CW / -1 CCW
};
```

From src/devices/streamdeck/src/akp03.cpp poll() (current EncoderReleased branch already exists at
~line 388; the "value=0 workaround" is the half-step phrasing in ROADMAP — verify no residual hack
remains and the release edge is genuine, not synthesised from a value==0 turn):

```
case akp03::InputEvent::Kind::EncoderReleased:
    devEv.kind = DeviceEvent::Kind::EncoderReleased;
    devEv.index = ev->index;
    devEv.value = 0;
    break;
```

From src/app/src/hotplug_debouncer.hpp (the coalescer pattern to MIRROR — per-key QTimer,
trailing-edge, marshals onto owning GUI thread, kDebounceMs=300; encoder coalescer uses 16ms and
ACCUMULATES delta instead of replacing):

```
class HotplugDebouncer : public QObject {
    Q_OBJECT
    void observe(core::HotplugEvent const& ev);   // any thread; marshals to GUI thread
    static constexpr int kDebounceMs = 300;
signals:
    void coalesced(ajazz::core::HotplugEvent ev);
private:
    QHash<HotplugKey, QTimer*> m_timers;
    QHash<HotplugKey, core::HotplugEvent> m_pending;
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the encoder value=0 half-step workaround; deliver genuine EncoderReleased</name>
  <files>
    src/devices/streamdeck/src/akp03.cpp
  </files>
  <behavior>
    - A rotation report (0x90/0x91, 0x50/0x51, 0x60/0x61) dispatches DeviceEvent::Kind::EncoderTurned
      with index 0..2 and value = +1 (CW) / -1 (CCW) — post-quadrature single detent (Pitfall 23).
    - An encoder-press report (0x33/0x35/0x34 with byte10 != 0) dispatches EncoderPressed.
    - An encoder-release report (same code, byte10 == 0) dispatches a genuine EncoderReleased — NOT
      a synthesised EncoderPressed-with-value-0 nor an EncoderTurned-with-value-0.
    - No code path emits EncoderTurned with value==0 as a stand-in for a release.
  </behavior>
  <action>
Per INPUT-01 (replace the value=0 half-step workaround at akp03.cpp:289-293):

1. Read the current `poll()` switch and the `parseInputReport` press/release branch. The
   `EncoderReleased` InputEvent kind and the `DeviceEvent::Kind::EncoderReleased` dispatch already
   exist. The "value=0 workaround" is the residual phrasing where a release was historically
   modelled as a zero-delta turn. CONFIRM the dispatch maps
   `InputEvent::Kind::EncoderReleased -> DeviceEvent::Kind::EncoderReleased` directly and that
   `EncoderTurned` is NEVER emitted with `value == 0`. If any residual half-step hack remains
   (a value==0 turn synthesised on release, or a comment referencing the workaround), remove it so
   releases flow only through the `EncoderReleased` branch.
1. Update the doc-comment on `poll()` / the encoder branch to reference INPUT-01 + Pitfall 23 and
   state the delta is post-quadrature single-detent (+1/-1). Fix the stale akp03.cpp file header
   that still says "one rotary encoder" — the AKP05E has 4 endless encoders.
1. Add encoder-dispatch assertions to `tests/unit/test_akp03_protocol.cpp` (owned by plan 10-01;
   this is an EDIT after 10-01 lands — wave-2 sequencing makes that safe). Enqueue a synthetic
   release report (encoder-press code with byte10 == 0) via MockTransport and assert the emitted
   DeviceEvent is `EncoderReleased`; enqueue a CW rotation report and assert `EncoderTurned`
   value == +1. ASCII-only test names; no nlohmann; use the documented action-code table only.
   </action>
   <verify>
   <automated>grep -q "EncoderReleased" src/devices/streamdeck/src/akp03.cpp && ! grep -nE 'EncoderTurned[^;]*value\[[:space:]\]*=\[[:space:]\]\*0' src/devices/streamdeck/src/akp03.cpp && grep -qiE "Pitfall 23|INPUT-01" src/devices/streamdeck/src/akp03.cpp</automated>
   </verify>
   <done>
   The backend dispatches a genuine EncoderReleased on knob release; no EncoderTurned-value-0
   half-step remains; the file header reflects 4 encoders; INPUT-01/Pitfall 23 cited in-code.
   (ctest gated on the build fix.)
   </done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2: Add the 16ms trailing-edge encoder coalescer (app/QML observer layer)</name>
  <files>
    src/app/src/encoder_coalescer.hpp,
    src/app/src/encoder_coalescer.cpp,
    src/app/CMakeLists.txt,
    tests/unit/test_encoder_coalescer.cpp,
    tests/unit/CMakeLists.txt
  </files>
  <behavior>
    - observe(encoderIndex, delta) accumulates delta into a per-encoder pending total and
      starts/restarts that encoder's 16ms trailing-edge QTimer.
    - When the timer fires, emit encoderCoalesced(encoderIndex, accumulatedDelta) ONCE with the
      summed delta, then reset that encoder's pending total to 0.
    - Many observe() calls for the same encoder within one 16ms window -> exactly ONE emission with
      the summed delta (10x +1 within 16ms -> one signal, delta == 10).
    - Per-encoder isolation: distinct encoder indices never restart each other's timers (4 encoders
      spun simultaneously -> 4 emissions, not 1, not 16).
    - Rotation delta only; press/release are discrete events and are NOT delta-coalesced.
  </behavior>
  <action>
Per INPUT-02 (coalesce rotation events at the QML observer layer, 16ms QTimer, single signal per
repaint frame, total delta accumulated and emitted once — Pitfall 23 signal-storm):

1. Create `src/app/src/encoder_coalescer.hpp` + `.cpp` in the `ajazz::app` namespace, mirroring
   `HotplugDebouncer` (Q_OBJECT, parented to a GUI-thread QObject, per-key QTimer hash, marshals
   onto the owning thread via Qt::QueuedConnection before touching QTimers; delete copy/move).
   Differences:
   - Key is the encoder index (`int`), not a (vid,pid,serial) tuple.
   - `static constexpr int kCoalesceMs = 16;` (one repaint frame at ~60fps).
   - `void observe(int encoderIndex, int delta);` ACCUMULATES delta into `m_pending[index]` and
     restarts that index's timer (trailing-edge).
   - On fire: `emit encoderCoalesced(encoderIndex, m_pending[index]); m_pending[index] = 0;`
   - signal: `void encoderCoalesced(int encoderIndex, int delta);`
     Document the threading contract and cite INPUT-02 + Pitfall 23 in the file header.
1. Register `src/encoder_coalescer.cpp` in `src/app/CMakeLists.txt` adjacent to
   `src/hotplug_debouncer.cpp` (AUTOMOC is already on for the app target — it is a Q_OBJECT).
1. Create `tests/unit/test_encoder_coalescer.cpp` mirroring the debounce SECTION in
   `test_hotplug_harness.cpp`: `QSignalSpy` + the `qt_app_fixture.hpp` event-loop pump. Cases
   (ASCII tags `[encoder][coalesce]`):
   - "10 detents within one window emit one summed-delta signal" (spy.count()==1, arg==10).
   - "per-encoder isolation - 3 indices emit 3 signals not 1 or 9".
   - "delta accumulates with sign" (+1,+1,-1 -> +1).
     Register `test_encoder_coalescer.cpp` in `tests/unit/CMakeLists.txt`; the `src/app/src` include
     dir is already on the unit-test target.
1. Do NOT wire the coalescer into `application.cpp` here unless the encoder-event-to-QML routing
   already exists — there is currently NO DeviceEvent->QML encoder path in src/app/src. The
   coalescer is the reusable primitive; the actual wiring into a QML-facing controller is a
   follow-up when the encoder UI consumes live events. If a DeviceEvent fan-out to QML lands during
   execution, connect EncoderTurned -> coalescer.observe and coalescer.encoderCoalesced -> the QML
   signal; otherwise leave the primitive unit-tested and note the wiring gap in the SUMMARY.
   </action>
   <verify>
   <automated>test -f src/app/src/encoder_coalescer.hpp && test -f src/app/src/encoder_coalescer.cpp && grep -q "kCoalesceMs = 16" src/app/src/encoder_coalescer.hpp && grep -q "encoder_coalescer.cpp" src/app/CMakeLists.txt && grep -q "test_encoder_coalescer.cpp" tests/unit/CMakeLists.txt && grep -q "encoderCoalesced" tests/unit/test_encoder_coalescer.cpp</automated>
   </verify>
   <done>
   A 16ms trailing-edge per-encoder delta-accumulating coalescer exists, is built into the app target
   and the unit-test target, and has tests proving single-signal-per-window + per-encoder isolation +
   signed accumulation. (ctest gated on the build fix.)
   </done>
   </task>

<task type="auto">
  <name>Task 3: Remove clock from akp05e devices.yaml row; promote to functional</name>
  <files>
    docs/_data/devices.yaml
  </files>
  <action>
Per DEVICES-05 + ARCH-05 default verdict:

1. In `docs/_data/devices.yaml`, edit the `akp05e` row (currently line ~265):
   - `capabilities: [display, encoder, clock]` -> `capabilities: [display, encoder]` (remove
     `clock` only; preserve `display` + `encoder`).
   - `maturity: scaffolded` -> `maturity: functional`.
   - Extend the `notes:` line to cite ARCH-05 verbatim per the requirement: "no RTC opcode in
     mirajazz/opendeck-akp03/ajazz-sdk; LCD clock widgets are host-rendered images via the display
     capability, not firmware time. Ratified at ARCH-05 default verdict 2026-05-15." Keep the
     existing HOTSPOTEKUSB capture-provenance note (append, do not delete).
1. This is a doc-data edit (drives README/wiki AUTOGEN + the maturity tooltip per the
   devices.yaml-vs-runtime convention — the runtime sidebar is hardcoded in register.cpp and is
   NOT changed here; DEVICES-05 is a catalogue-honesty edit only). Do NOT touch register.cpp.
1. The commit-message body MUST cite the ARCH-05 DEFAULT VERDICT (capture-pending) status: the
   clock demotion is correct under the default verdict and the Phase 9.x all-4-devices time-set
   capture is the finalization gate (Pitfall 19 three-witness rule, structurally unsatisfiable for
   clock on the Stream Dock family). Note explicitly that ARCH-05.1's RTC flip applies to
   `ak980pro` ONLY and does not affect this row.

mdformat/typos may reflow YAML/markdown on commit; re-stage and re-commit if so (CLAUDE.md). Do
NOT use --no-verify.
</action>
<verify>
<automated>awk '/codename: akp05e/{f=1} f&&/^ - codename:/&&!/3004/{exit} f' docs/\_data/devices.yaml > /tmp/akp03_row.txt; grep -q "maturity: functional" /tmp/akp03_row.txt && ! grep -q "clock" /tmp/akp03_row.txt && grep -q "ARCH-05" /tmp/akp03_row.txt</automated>
</verify>
<done>
The akp05e row lists capabilities [display, encoder] (no clock), maturity functional,
and a notes line citing ARCH-05. Atomic Conventional Commit lands the catalogue honesty edit with
the ARCH-05 default-verdict conditional in the body.
</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                           |
| -------------------------------------- | --------------------------------------------------------------------- |
| device firmware -> host event pipeline | Untrusted HID input reports (action codes) parsed into DeviceEvents   |
| device-event I/O thread -> GUI thread  | Encoder events marshalled across threads into the coalescer's QTimers |

## STRIDE Threat Register

| Threat ID        | Category          | Component                                                                       | Disposition | Mitigation Plan                                                                                                           |
| ---------------- | ----------------- | ------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-10-04          | Denial of Service | Signal-storm: fast encoder spin floods QML with repaint events (Pitfall 23)     | mitigate    | 16ms trailing-edge coalescer accumulates delta and emits one signal per repaint frame (Task 2).                           |
| T-10-05          | Tampering         | Malformed/unknown action code in an input report                                | mitigate    | parseInputReport returns nullopt for ACK/NOP/short/unknown frames (existing); only documented codes dispatch.             |
| T-10-06          | Spoofing          | Re-advertising a clock capability the firmware does not have (lying success UX) | mitigate    | DEVICES-05 removes clock; setTime stays NotImplemented; per-row exclamation glyph honesty contract (v1.1 D-02) preserved. |
| T-10-07          | Tampering         | QTimer started off the owning thread (Qt assert / UB)                           | mitigate    | Coalescer marshals onto its GUI-thread owner via Qt::QueuedConnection before touching timers (mirrors HotplugDebouncer).  |
| T-10-SC          | Tampering         | npm/pip/cargo installs                                                          | mitigate    | No new package-manager installs in this plan. No `[ASSUMED]`/`[SUS]` packages introduced.                                 |
| \</threat_model> |                   |                                                                                 |             |                                                                                                                           |

<verification>
1. `grep -q "EncoderReleased" akp03.cpp` and no `EncoderTurned ... value = 0` half-step remains.
2. `src/app/src/encoder_coalescer.{hpp,cpp}` exist with `kCoalesceMs = 16`; registered in app + unit CMake.
3. `test_encoder_coalescer.cpp` proves single-signal-per-window, per-encoder isolation, signed accumulation.
4. `devices.yaml` akp05e row: capabilities `[display, encoder]`, maturity `functional`, notes cite ARCH-05.
5. (Gated on build fix) `ctest --preset linux-release -R "akp03|encoder"` passes once `qt6-qtbase-private-devel` is installed.
6. COD-031 grep gate: `grep -rn nlohmann src/core/include/` returns 0.
</verification>

\<success_criteria>

- INPUT-01: genuine EncoderReleased event; value=0 half-step workaround removed; delta post-quadrature (+1/-1).
- INPUT-02: 16ms trailing-edge encoder coalescer emits one accumulated-delta signal per repaint frame; unit-tested.
- DEVICES-05: devices.yaml row removes clock, cites ARCH-05, promotes scaffolded -> functional.
- ARCH-05 DEFAULT-VERDICT conditional surfaced in the commit-message body.
- Atomic Conventional Commits (backend / coalescer / catalogue are independently revertable changes).
  \</success_criteria>

<output>
After completion, create `.planning/phases/10-akp05e-3004-promotion/10-02-SUMMARY.md`
documenting: the encoder-dispatch correction (what residual workaround was found and removed, if
any), the coalescer design + test matrix, whether the coalescer was wired into a live QML path or
left as an unit-tested primitive (and why), the devices.yaml row change, an explicit note that the
ARCH-05 no-RTC verdict remains CONDITIONAL on the Phase 9.x capture, and whether the build fix
(`qt6-qtbase-private-devel`) was available at execution time or the C++ verification was deferred.
</output>
