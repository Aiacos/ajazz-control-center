---
phase: 05-time-sync-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/include/ajazz/core/capabilities.hpp
  - src/core/include/ajazz/core/device.hpp
autonomous: true
requirements: [TIMESYNC-01]
must_haves:
  truths:
    - Capability::Clock = 1u << 15 lands without renumbering any existing bit (Pitfall 13 — append-only enum discipline).
    - TimeSyncResult tri-state (Ok / NotImplemented / IoError) is defined; backends will return NotImplemented in Plans 02/03.
    - IClockCapable abstract mix-in with [[nodiscard]] virtual TimeSyncResult setTime(std::chrono::system_clock::time_point) compiles cleanly across every TU that already #includes capabilities.hpp.
    - DeviceDescriptor.hasClock{false} mirrors the existing hasRgb / hasTouchStrip flag pattern — cheap UI gating, no runtime cast required for visibility.
    - A static_assert pins Capability::Clock to (1u << 15) so any future renumbering breaks the build (Pitfall 13 lock).
  artifacts:
    - path: src/core/include/ajazz/core/capabilities.hpp
      provides: Capability::Clock = 1u << 15; TimeSyncResult enum; IClockCapable interface; static_assert lock
      contains: Clock = 1u << 15
    - path: src/core/include/ajazz/core/device.hpp
      provides: bool hasClock{false} member in DeviceDescriptor
      contains: hasClock{false}
  key_links:
    - from: DeviceDescriptor.hasClock
      to: Capability::Clock advertisement
      via: backend constructors will set the descriptor field AND inherit IClockCapable + or-in the bit (Plans 02 + 03)
      pattern: hasClock\s*=\s*true
---

<objective>
Land the four foundation atoms — `Capability::Clock` bit, `TimeSyncResult` tri-state enum, `IClockCapable` mix-in interface, and `DeviceDescriptor.hasClock` flag — as a single atomic header-only commit. Closes Pitfall 13 with a `static_assert` that pins the bit value so future renumbering is a compile error rather than a silent ABI break.

This is the load-bearing prerequisite for every other plan in Phase 5 (Plans 02..08). All four atoms are scoped to `src/core/include/ajazz/core/` headers — no `.cpp` files change in this plan.

This plan is a thin pointer-plan: it adopts **Task 1 of the upstream plan at `docs/superpowers/plans/2026-05-13-time-sync.md` (lines 51-184)** verbatim, with one amendment captured below.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-time-sync-scaffolding/05-CONTEXT.md
@.planning/research/PITFALLS.md
@docs/superpowers/plans/2026-05-13-time-sync.md
@docs/superpowers/specs/2026-05-13-time-sync-design.md
@src/core/include/ajazz/core/capabilities.hpp
@src/core/include/ajazz/core/device.hpp

\<upstream_pointer>
**Source plan:** `docs/superpowers/plans/2026-05-13-time-sync.md` — Task 1 (lines 51-184).

**Steps adopted verbatim:**

- Step 1.1 (lines 59-130) — Add `Capability::Clock = 1u << 15`, `TimeSyncResult` enum, `IClockCapable` interface, `#include <chrono>`.
- Step 1.2 (lines 131-141) — Add `bool hasClock{false}` to `DeviceDescriptor` after `hasTouchStrip`.
- Step 1.3 (lines 142-151) — Build to verify headers compile.
- Step 1.4 (lines 152-161) — Run existing test suite to verify no regression.
- Step 1.5 (lines 162-184) — Commit message: `feat(core): add IClockCapable capability + descriptor flag (scaffolded)`.

**Amendment to Task 1 (this plan only):**

- **A-01 — Pitfall 13 lock.** Per CONTEXT.md "Claude's Discretion" + ROADMAP SC1, append immediately after the `enum class Capability` closing brace:
  `static_assert(static_cast<unsigned>(Capability::Clock) == (1u << 15), "Capability::Clock must remain 1u << 15 — Pitfall 13: never renumber capability bits; only append.");`
  This is **NOT** in the upstream plan (the upstream relies on convention alone). The static_assert converts the invariant into a build-break so future contributors cannot silently shift the bit.

**Pre-execution verification (one-shot grep before editing):**

- `grep -nE "1u << (15|16)" src/core/include/ajazz/core/capabilities.hpp` must return zero hits (bit 15 free, bit 16 free for the next capability). If a hit appears, STOP — escalate to the user; the design doc's bit choice has been pre-emptied by an out-of-band change.

\</upstream_pointer>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply upstream Task 1 (Steps 1.1–1.4) + amendment A-01</name>
  <files>src/core/include/ajazz/core/capabilities.hpp, src/core/include/ajazz/core/device.hpp</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 51-184 (Task 1 verbatim — Steps 1.1, 1.2, 1.3, 1.4)
    - docs/superpowers/specs/2026-05-13-time-sync-design.md (5-layer design; bit-numbering rationale)
    - src/core/include/ajazz/core/capabilities.hpp (current `enum class Capability` ends at `PerAppProfiles = 1u << 14` on line 47; `IFirmwareCapable` is the last interface before namespace close)
    - src/core/include/ajazz/core/device.hpp (current `DeviceDescriptor` has `hasRgb`, `hasTouchStrip` — `hasClock` goes immediately after)
    - .planning/research/PITFALLS.md Pitfall 13 (append-only enum + static_assert lock + load-time setting validation)
    - .planning/phases/05-time-sync-scaffolding/05-CONTEXT.md decisions D-01..D-03 + Claude's Discretion section
  </read_first>
  <action>
    Apply upstream Task 1 verbatim. The upstream plan's Step 1.1 specifies the exact source insertions for `Capability::Clock = 1u << 15`, the `TimeSyncResult` tri-state enum (`Ok = 0`, `NotImplemented = 1`, `IoError = 2`), the `IClockCapable` interface (single pure-virtual `[[nodiscard]] TimeSyncResult setTime(std::chrono::system_clock::time_point)`), and the `#include <chrono>` placement. Step 1.2 specifies the `bool hasClock{false}` placement in `DeviceDescriptor` immediately after `hasTouchStrip{false}`.

```
Amendment A-01: After applying Step 1.1, append a static_assert immediately after the closing brace of `enum class Capability` (and before the `inline constexpr` operator overloads if any exist, otherwise just after the enum):
`static_assert(static_cast<unsigned>(Capability::Clock) == (1u << 15), "Capability::Clock must remain 1u << 15 — Pitfall 13: never renumber capability bits; only append.");`
This is the only addition beyond the upstream Task 1 text.

Execute Steps 1.3 (build) and 1.4 (existing test suite) verbatim per upstream. Step 1.5 (commit) is executed at the end of this plan as the atomic Plan 05-01 commit — message format per upstream: `feat(core): add IClockCapable capability + descriptor flag (scaffolded)`. Add a trailing line `Refs: TIMESYNC-01, Pitfall 13, Phase 5 D-01.` to the commit body.

Do NOT touch any .cpp file in this plan. The four atoms are header-only by design — every backend stub lands in Plan 05-02 (Stream Dock) and Plan 05-03 (AKB980 PRO). Header-only scope guarantees this plan's atomic commit cannot break the link step (headers recompile downstream TUs but produce no new symbols).
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_core 2>&1 | tee /tmp/build_05_01.log | tail -30 ; grep -E "error:|warning:.*excess elements" /tmp/build_05_01.log && exit 1 || true ; ctest --preset linux-release 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Clock = 1u << 15' src/core/include/ajazz/core/capabilities.hpp` exits 0
    - `grep -q 'enum class TimeSyncResult' src/core/include/ajazz/core/capabilities.hpp` exits 0
    - `grep -q 'NotImplemented = 1' src/core/include/ajazz/core/capabilities.hpp` exits 0
    - `grep -q 'class IClockCapable' src/core/include/ajazz/core/capabilities.hpp` exits 0
    - `grep -q 'virtual TimeSyncResult.*setTime' src/core/include/ajazz/core/capabilities.hpp` exits 0
    - `grep -q '#include <chrono>' src/core/include/ajazz/core/capabilities.hpp` exits 0
    - `grep -q 'static_assert(static_cast<unsigned>(Capability::Clock) == (1u << 15)' src/core/include/ajazz/core/capabilities.hpp` exits 0 (A-01 — this plan's amendment beyond upstream Task 1)
    - `grep -q 'bool hasClock{false}' src/core/include/ajazz/core/device.hpp` exits 0
    - `cmake --build --preset linux-release --target ajazz_core` exits 0 — every TU including capabilities.hpp / device.hpp recompiles without errors.
    - `ctest --preset linux-release` — no regression; existing tests pass.
    - `git show --stat HEAD` lists exactly the two header files (no .cpp changes in this commit).
  </acceptance_criteria>
  <done>
    The four foundation atoms (capability bit, result enum, mix-in interface, descriptor flag) plus the Pitfall-13 static_assert are landed as a single atomic commit. ajazz_core compiles. Plans 02..08 can now proceed.
  </done>
</task>

</tasks>

<verification>
- `git show --stat HEAD` shows exactly 2 files: capabilities.hpp and device.hpp. No .cpp churn.
- A deliberate corruption test (mental): replacing `1u << 15` with `1u << 14` in the static_assert subject would fail compilation — the lock is observable.
- TimeSyncResult is used by zero call sites yet — that lands in Plan 05-02 (Stream Dock stubs) and Plan 05-03 (AKB980 stub) which both #include this header.
- IClockCapable has zero implementations yet — same reason. The interface compiles in isolation because it's a pure abstract; nothing references its vtable until Plans 02/03 derive from it.
</verification>

\<success_criteria>

- ROADMAP SC1 prerequisite: `Capability::Clock` bit and `DeviceDescriptor.hasClock` flag exist (visibility plumbing lands in Plan 05-05 / 05-06).
- ROADMAP SC2 prerequisite: `IClockCapable` and `TimeSyncResult::NotImplemented` exist for the backend stubs in Plans 02/03 to consume.
- Pitfall 13 closed at the bit-numbering surface: the static_assert pins `1u << 15` and the load-time setting validation closes the runtime surface in Plan 05-04.
- Single atomic commit per ARCH-03 spirit (this plan is a clean foundation: every downstream plan can rebase on top of it).

\</success_criteria>

<output>
After completion, create `.planning/phases/05-time-sync-scaffolding/05-01-SUMMARY.md` noting: (1) which bit was assigned (should be `1u << 15` per design — escalate if pre-emptied), (2) which existing files now recompile because they include capabilities.hpp / device.hpp (informational — informs Plan 02 / 03 read_first lists), (3) whether the static_assert added any new TUs to the recompile graph (it shouldn't — it's `constexpr`-evaluated at every translation unit's include site but emits no runtime code).
</output>
