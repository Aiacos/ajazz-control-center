---
phase: 05-time-sync-scaffolding
plan: 05
type: execute
wave: 3
depends_on: [05-01]
files_modified:
  - src/app/src/device_model.hpp
  - src/app/src/device_model.cpp
autonomous: true
requirements: [TIMESYNC-01]
must_haves:
  truths:
    - DeviceModel exposes HasClockRole as a Qt::UserRole-derived enum value, mirroring HasRgbRole / HasTouchStripRole precedent.
    - roleNames() returns a QByteArray "deviceHasClock" mapping for HasClockRole — QML can bind via deviceHasClock role on the model.
    - data(index, HasClockRole) returns the bool descriptor.hasClock for the row.
    - No backend churn — DeviceModel is purely a view-model role addition; existing roles/data paths unchanged.
  artifacts:
    - path: src/app/src/device_model.hpp
      provides: HasClockRole enum value (next available Qt::UserRole + N), mirroring HasRgbRole pattern
      contains: HasClockRole
    - path: src/app/src/device_model.cpp
      provides: roleNames() entry "deviceHasClock"; data() branch returning descriptor.hasClock as QVariant<bool>
      contains: deviceHasClock
  key_links:
    - from: DeviceModel::data(index, HasClockRole)
      to: DeviceDescriptor.hasClock
      via: row → descriptor lookup → boolean role return
      pattern: descriptor\.hasClock
    - from: DeviceModel::roleNames
      to: '"deviceHasClock"'
      via: QHash<int, QByteArray> entry for HasClockRole
      pattern: deviceHasClock
---

<objective>
Add `HasClockRole` to `DeviceModel` so QML can read each device row's `descriptor.hasClock` flag and conditionally show the "Sync time" button + glyph (Plan 05-06). This is a strict mirror of the existing `HasRgbRole` / `HasTouchStripRole` pattern — pure view-model role addition with zero behavior change to existing roles.

This plan is a thin pointer-plan: it adopts **Task 5 of the upstream plan at `docs/superpowers/plans/2026-05-13-time-sync.md` (lines 979-1053)** verbatim — no amendments required.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-time-sync-scaffolding/05-CONTEXT.md
@docs/superpowers/plans/2026-05-13-time-sync.md
@src/app/src/device_model.hpp
@src/app/src/device_model.cpp
@src/core/include/ajazz/core/device.hpp

\<upstream_pointer>
**Source plan:** `docs/superpowers/plans/2026-05-13-time-sync.md` — Task 5 (lines 979-1053).

**Steps adopted verbatim:**

- Step 5.1 (lines 987-997) — Add `HasClockRole` to the Roles enum in device_model.hpp, immediately after `HasTouchStripRole` (next sequential Qt::UserRole + N).
- Step 5.2 (lines 998-1016) — Add the `data()` switch branch returning `QVariant::fromValue(descriptor.hasClock)` for `HasClockRole`, and the `roleNames()` entry `{HasClockRole, "deviceHasClock"}`.
- Step 5.3 (lines 1017-1026) — Build to verify.
- Step 5.4 (lines 1027-1036) — Run all tests.
- Step 5.5 (lines 1037-1053) — Commit message: `feat(app): expose hasClock to QML via DeviceModel role`.

**No amendments — the upstream covers this exactly as Phase 5 requires.**

\</upstream_pointer>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply upstream Task 5 (Steps 5.1–5.4) — DeviceModel HasClockRole</name>
  <files>src/app/src/device_model.hpp, src/app/src/device_model.cpp</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 979-1053 (Task 5 verbatim)
    - src/app/src/device_model.hpp (current Roles enum — find HasRgbRole, HasTouchStripRole as exemplars; HasClockRole goes immediately after HasTouchStripRole, with the next sequential Qt::UserRole + N value)
    - src/app/src/device_model.cpp (current roleNames() return + data() switch — both need a parallel new entry)
    - src/core/include/ajazz/core/device.hpp (DeviceDescriptor.hasClock landed in Plan 05-01)
  </read_first>
  <action>
    Apply upstream Step 5.1 — in src/app/src/device_model.hpp, locate the Roles enum and add `HasClockRole` immediately after `HasTouchStripRole`. The enum value is the next sequential `Qt::UserRole + N` (read the existing values to determine N — do NOT skip or reorder existing values; append-only per the same Pitfall 13 spirit applied to QAbstractItemModel role enums).

```
Apply upstream Step 5.2 — in src/app/src/device_model.cpp:
1. In data(QModelIndex const& index, int role) const, add a case branch for HasClockRole that returns `QVariant::fromValue(descriptor.hasClock)` (or equivalent — match existing HasRgbRole branch's QVariant construction style).
2. In roleNames(), add `{HasClockRole, "deviceHasClock"}` to the returned QHash. This is the role name QML uses to bind (`model.deviceHasClock` in DeviceRow.qml — landed in Plan 05-06).

Then run upstream Steps 5.3 (build) and 5.4 (existing test suite) verbatim. Step 5.5 (commit) is the atomic commit for this plan: `feat(app): expose hasClock to QML via DeviceModel role` with body line `Refs: TIMESYNC-01.`
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_app 2>&1 | tee /tmp/build_05_05.log | tail -20 ; grep -E "error:" /tmp/build_05_05.log && exit 1 || true ; ctest --preset linux-release 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'HasClockRole' src/app/src/device_model.hpp` exits 0
    - `grep -q 'HasClockRole' src/app/src/device_model.cpp` exits 0 (both data() and roleNames())
    - `grep -q '"deviceHasClock"' src/app/src/device_model.cpp` exits 0
    - `grep -q 'descriptor\.hasClock' src/app/src/device_model.cpp` exits 0
    - HasClockRole's enum value is sequential — `grep -B1 'HasClockRole' src/app/src/device_model.hpp` shows it immediately following HasTouchStripRole (no value skipped or out of order).
    - `cmake --build --preset linux-release --target ajazz_app` exits 0.
    - `ctest --preset linux-release` exits 0 — no regression.
  </acceptance_criteria>
  <done>
    DeviceModel exposes deviceHasClock role to QML. Plan 05-06 will bind it for the Sync button visibility.
  </done>
</task>

</tasks>

<verification>
- `git show --stat HEAD` lists exactly 2 files: device_model.hpp, device_model.cpp.
- The role enum addition is append-only (HasClockRole appears AFTER HasTouchStripRole in source order, not interleaved) — preserves binary compat with any code holding role-int values across model updates.
- No new signal shape, no behavior change to existing roles — the DeviceModel test suite (if any) should pass unchanged.
</verification>

\<success_criteria>

- ROADMAP SC1 prerequisite: QML now has the `deviceHasClock` role available for cheap UI gating without runtime cast — Plan 05-06 binds the Sync button visibility to it.
- TIMESYNC-01 progressed: capability advertisement → descriptor flag → model role → UI binding — the visibility chain is complete after Plan 05-06.

\</success_criteria>

<output>
After completion, create `.planning/phases/05-time-sync-scaffolding/05-05-SUMMARY.md` noting: (1) the Qt::UserRole + N value assigned to HasClockRole, (2) confirmation the addition is append-only (no existing role re-numbered), (3) any pre-existing DeviceModel test that exercises roleNames() (informational — Plan 05-06 may want to extend it).
</output>
