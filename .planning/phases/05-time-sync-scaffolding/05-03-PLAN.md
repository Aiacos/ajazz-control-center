---
phase: 05-time-sync-scaffolding
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/devices/keyboard/src/proprietary_keyboard.cpp
  - src/devices/keyboard/src/register.cpp
autonomous: true
requirements: [TIMESYNC-02]
must_haves:
  truths:
    - ProprietaryKeyboard inherits IClockCapable and advertises Capability::Clock; its setTime() returns TimeSyncResult::NotImplemented with a std::once_flag-gated WARN (Pitfall 14).
    - The AKB980 PRO descriptor (vid 0x0c45, pid 0x8009) has .hasClock = true in register.cpp.
    - VIA-protocol keyboards (ViaKeyboard) do NOT inherit IClockCapable and their descriptors do NOT have .hasClock — D-03 explicitly excludes QMK-style keyboards because they expose no vendor clock surface.
    - The std::once_flag for ProprietaryKeyboard is distinct from the four Stream Dock flags (Pitfall 14: per-backend, never shared).
  artifacts:
    - path: src/devices/keyboard/src/proprietary_keyboard.cpp
      provides: 'ProprietaryKeyboard : public IClockCapable; setTime() WARN-once + NotImplemented; capabilities() includes Capability::Clock'
      contains: setTime
    - path: src/devices/keyboard/src/register.cpp
      provides: AKB980 PRO descriptor has .hasClock = true; VIA keyboard descriptors do NOT
      contains: hasClock = true
  key_links:
    - from: ProprietaryKeyboard::setTime
      to: static std::once_flag in proprietary_keyboard.cpp file scope (codename "akb980")
      via: std::call_once
      pattern: std::call_once
---

<objective>
Apply the same `IClockCapable` stub pattern from Plan 05-02 to the AKB980 PRO keyboard backend (`ProprietaryKeyboard`) and set `.hasClock = true` on the AKB980 PRO descriptor in `src/devices/keyboard/src/register.cpp`. Per D-03, VIA-protocol keyboards (which inherit `ViaKeyboard`, not `ProprietaryKeyboard`) are explicitly excluded — they're QMK-style with no vendor-specific clock surface.

This plan is a thin pointer-plan: it adopts **Task 3 of the upstream plan at `docs/superpowers/plans/2026-05-13-time-sync.md` (lines 366-451)** verbatim — no amendments required. The upstream plan already correctly scopes this to ProprietaryKeyboard + the AKB980 PRO descriptor row.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-time-sync-scaffolding/05-CONTEXT.md
@.planning/research/PITFALLS.md
@docs/superpowers/plans/2026-05-13-time-sync.md
@src/devices/keyboard/src/proprietary_keyboard.cpp
@src/devices/keyboard/src/register.cpp
@src/devices/keyboard/src/via_keyboard.cpp

\<upstream_pointer>
**Source plan:** `docs/superpowers/plans/2026-05-13-time-sync.md` — Task 3 (lines 366-451).

**Steps adopted verbatim:**

- Step 3.1 (lines 374-404) — Add `IClockCapable` to `ProprietaryKeyboard`'s inheritance list, or-in `Capability::Clock` in `capabilities()`, declare file-static `std::once_flag s_warned_akb980`, implement `setTime()` returning `TimeSyncResult::NotImplemented` after WARN-once. Codename: `"akb980"`.
- Step 3.2 (lines 405-408) — Set `.hasClock = true` on the AKB980 PRO descriptor (vid `0x0c45`, pid `0x8009`) in `src/devices/keyboard/src/register.cpp`.
- Step 3.3 (lines 409-418) — Build to verify.
- Step 3.4 (lines 419-428) — Run existing test suite.
- Step 3.5 (lines 429-451) — Commit message: `feat(keyboard): scaffold setTime() for AKB980 PRO`.

**No amendments to Task 3 — the upstream covers this exactly as Phase 5 requires.**

**Exclusion check (D-03):**

- ViaKeyboard (`src/devices/keyboard/src/via_keyboard.cpp`) does **NOT** inherit `IClockCapable`. Its descriptors in `register.cpp` do **NOT** get `.hasClock = true`. This is intentional per D-03 — VIA keyboards are QMK-style; the protocol has no vendor clock surface and inventing one would be a lie.

\</upstream_pointer>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply upstream Task 3 (Steps 3.1–3.4) — ProprietaryKeyboard setTime stub + descriptor flag</name>
  <files>src/devices/keyboard/src/proprietary_keyboard.cpp, src/devices/keyboard/src/register.cpp</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 366-451 (Task 3 verbatim — Steps 3.1, 3.2, 3.3, 3.4)
    - src/devices/keyboard/src/proprietary_keyboard.cpp (class declaration + capabilities() body — current inheritance and capability bits; mirror of the Stream Dock stub pattern)
    - src/devices/keyboard/src/register.cpp (AKB980 PRO descriptor row at vid 0x0c45 pid 0x8009 — find by VID/PID, set .hasClock = true)
    - src/devices/keyboard/src/via_keyboard.cpp (sanity check — confirm Plan 05-03 does NOT touch this file; ViaKeyboard remains clock-less per D-03)
    - src/core/include/ajazz/core/capabilities.hpp (IClockCapable interface — landed in Plan 05-01)
    - .planning/research/PITFALLS.md Pitfall 14 (log-once-per-process)
    - .planning/phases/05-time-sync-scaffolding/05-CONTEXT.md D-03 (exclusion rationale for VIA keyboards)
  </read_first>
  <action>
    Apply upstream Step 3.1 verbatim to ProprietaryKeyboard in proprietary_keyboard.cpp:

```
1. Add `IClockCapable` to the inheritance list. The class currently inherits from `IDevice` (and possibly other capability mix-ins like IFirmwareCapable) — append `, public IClockCapable`.
2. Or-in `Capability::Clock` in the `capabilities()` return expression.
3. In the anonymous namespace at top of proprietary_keyboard.cpp, declare `std::once_flag s_warned_akb980;` (distinct from the four Stream Dock flags — this is a different translation unit anyway, but the naming convention makes the per-backend intent explicit).
4. Implement `setTime(std::chrono::system_clock::time_point tp) override`:
   - body wraps `std::call_once(s_warned_akb980, [&]{ AJAZZ_LOG_WARN("keyboard.akb980", "setTime() not yet implemented for akb980"); });`
   - mark `tp` as `[[maybe_unused]]` (or `(void)tp;`)
   - return `TimeSyncResult::NotImplemented`.

Then apply Step 3.2: in src/devices/keyboard/src/register.cpp, locate the AKB980 PRO descriptor (the row with vid 0x0c45 and pid 0x8009 — there should be exactly one) and add `.hasClock = true,` after the existing `.hasTouchStrip = ...` line (mirroring the field order in device.hpp).

CRITICAL exclusion: do NOT add .hasClock = true to any ViaKeyboard descriptor row. Per D-03, VIA-protocol keyboards are QMK-style and have no vendor clock surface.

After all edits, run Steps 3.3 (build) and 3.4 (existing test suite) verbatim. Step 3.5 (commit) is executed at the end of this plan as the atomic Plan 05-03 commit — message per upstream: `feat(keyboard): scaffold setTime() for AKB980 PRO`, body line `Refs: TIMESYNC-02, D-03, Pitfall 14.`
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_keyboard 2>&1 | tee /tmp/build_05_03.log | tail -20 ; grep -E "error:" /tmp/build_05_03.log && exit 1 || true ; ctest --preset linux-release 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'public IClockCapable' src/devices/keyboard/src/proprietary_keyboard.cpp` exits 0
    - `grep -q 's_warned_akb980' src/devices/keyboard/src/proprietary_keyboard.cpp` exits 0 (distinct named flag)
    - `grep -q 'Capability::Clock' src/devices/keyboard/src/proprietary_keyboard.cpp` exits 0
    - `grep -q 'TimeSyncResult::NotImplemented' src/devices/keyboard/src/proprietary_keyboard.cpp` exits 0
    - `grep -q 'keyboard.akb980.*not yet implemented' src/devices/keyboard/src/proprietary_keyboard.cpp` exits 0
    - `grep -q 'hasClock = true' src/devices/keyboard/src/register.cpp` exits 0 (the AKB980 PRO row)
    - `grep -c 'hasClock = true' src/devices/keyboard/src/register.cpp` reports exactly 1 (ONLY the AKB980 PRO row — D-03 exclusion: VIA keyboards stay clock-less)
    - `grep -q 'IClockCapable' src/devices/keyboard/src/via_keyboard.cpp` returns no match (D-03 negative-space check)
    - `cmake --build --preset linux-release` exits 0 (full project links).
    - `ctest --preset linux-release` — no regression in existing tests.
  </acceptance_criteria>
  <done>
    ProprietaryKeyboard advertises Capability::Clock + returns NotImplemented with WARN-once. AKB980 PRO descriptor has hasClock = true. VIA keyboards remain explicitly clock-less per D-03. Single atomic commit lands.
  </done>
</task>

</tasks>

<verification>
- `git show --stat HEAD` lists exactly 2 files: proprietary_keyboard.cpp and register.cpp (keyboard). No header churn, no ViaKeyboard churn.
- D-03 negative-space verification: `grep -E "IClockCapable|Capability::Clock|hasClock" src/devices/keyboard/src/via_keyboard.cpp` returns zero hits — ViaKeyboard is untouched.
- `grep -c "hasClock = true" src/devices/keyboard/src/register.cpp` reports 1 (only AKB980 PRO).
- Cumulative across Plans 05-02 + 05-03: there are now 5 distinct `std::once_flag` instances (one per backend: akp153, akp03, akp05, akp815, akb980) and 18 descriptors with `.hasClock = true` (17 streamdeck rows + AKB980 PRO).
</verification>

\<success_criteria>

- ROADMAP SC2: AKB980 PRO keyboard backend joins the four Stream Dock backends in returning NotImplemented with WARN-once. Maps to TIMESYNC-02.
- D-03 closed at the keyboard layer: ProprietaryKeyboard advertises Capability::Clock; ViaKeyboard does not.
- Pitfall 14 closed across all 5 functional backends: one log per backend per process lifetime.

\</success_criteria>

<output>
After completion, create `.planning/phases/05-time-sync-scaffolding/05-03-SUMMARY.md` noting: (1) confirmation that ViaKeyboard is untouched (D-03 negative-space check passed), (2) total backend stubs across Plans 05-02 + 05-03 — should be 5 distinct backends, (3) total .hasClock = true rows across all register.cpp files — should be 18 (17 streamdeck + 1 keyboard).
</output>
