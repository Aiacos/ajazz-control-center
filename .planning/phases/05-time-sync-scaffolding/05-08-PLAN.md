---
phase: 05-time-sync-scaffolding
plan: 08
type: execute
wave: 6
depends_on: [05-02, 05-03, 05-04, 05-07]
files_modified:
  - docs/_data/devices.yaml
  - docs/protocols/streamdeck/akp153.md
  - docs/protocols/streamdeck/akp03.md
  - docs/protocols/streamdeck/akp05.md
  - docs/protocols/streamdeck/akp815.md
  - docs/protocols/keyboard/proprietary.md
  - CHANGELOG.md
  - tests/integration/test_time_sync_e2e.cpp
  - tests/integration/CMakeLists.txt
autonomous: true
requirements: [TIMESYNC-02, TIMESYNC-03, TIMESYNC-04, TIMESYNC-05, TIMESYNC-06]
must_haves:
  truths:
    - 'devices.yaml has clock added to capabilities: list on every entry that gets hasClock=true (mirrors register.cpp truth — divergence is a docs bug).'
    - Each per-protocol doc (akp153.md, akp03.md, akp05.md, akp815.md, proprietary.md keyboard) has an appended "## Time sync" section noting "TBD wire format pending vendor-app capture or firmware reverse-engineering" and a one-paragraph link back to docs/superpowers/specs/2026-05-13-time-sync-design.md.
    - 'CHANGELOG.md [Unreleased] Added section gains: "Time Sync infrastructure (capability + UI + service); device-side wire format is still TBD per family."'
    - tests/integration/test_time_sync_e2e.cpp uses Phase 4 ARCH-02 HotplugMonitor::injectEvent + MockHidEnumerator to drive a synthetic deviceArrived event end-to-end — no real hardware required.
    - 'Integration test asserts: (1) NotImplemented surfaces correctly, (2) the 300ms QTimer debounce is observable (synthetic clock or QSignalSpy::wait), (3) auto-sync NotImplemented does NOT emit a toast signal (D-02 enforcement).'
    - 'Plan 05-08 commit closes Phase 5: amendment-bearing 8-task adoption complete; runtime + tests + docs all green.'
  artifacts:
    - path: docs/_data/devices.yaml
      provides: 'clock capability added to capabilities: list on every hasClock=true entry'
      contains: '- clock'
    - path: docs/protocols/streamdeck/akp815.md
      provides: '"## Time sync" section with TBD wire format note (D-01 amendment 1 — AKP815 doc parity)'
      contains: '## Time sync'
    - path: tests/integration/test_time_sync_e2e.cpp
      provides: Catch2 integration test driving synthetic hotplug → 300ms debounce → setTime stub → assertion
      contains: TEST_CASE
    - path: CHANGELOG.md
      provides: '[Unreleased] Added entry for Time Sync infrastructure'
      contains: Time Sync
  key_links:
    - from: tests/integration/test_time_sync_e2e.cpp
      to: HotplugMonitor::injectEvent (ARCH-02)
      via: Phase 4 mock seam
      pattern: injectEvent
    - from: tests/integration/test_time_sync_e2e.cpp
      to: TimeSyncService::onDeviceArrived
      via: deviceArrived signal driven by injected synthetic event
      pattern: onDeviceArrived
    - from: devices.yaml entries
      to: register.cpp .hasClock = true rows
      via: human-maintained mirror — divergence is a docs bug
      pattern: '- clock'
---

<objective>
Close Phase 5 with the documentation deliverables (devices.yaml, per-protocol "## Time sync" sections, CHANGELOG entry) and the integration test that exercises the entire auto-sync loop end-to-end without real hardware (using Phase 4's `HotplugMonitor::injectEvent` ARCH-02 shim + a `MockHidEnumerator` to drive synthetic `deviceArrived` events).

This plan is a thin pointer-plan: it adopts **Task 8 of the upstream plan at `docs/superpowers/plans/2026-05-13-time-sync.md` (lines 1440-1726)** — with one amendment for D-01 amendment 1 (AKP815 docs parity per A-07) and one observability amendment for D-02 (auto-sync toast-suppression assertion in the integration test per A-08).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-time-sync-scaffolding/05-CONTEXT.md
@.planning/phases/04-hot-plug-hardening/04-CONTEXT.md
@.planning/phases/03-architectural-decisions/ARCH-02-mock-seam.md
@docs/superpowers/plans/2026-05-13-time-sync.md
@docs/superpowers/specs/2026-05-13-time-sync-design.md
@docs/_data/devices.yaml
@docs/protocols/streamdeck/akp153.md
@docs/protocols/streamdeck/akp03.md
@docs/protocols/streamdeck/akp05.md
@docs/protocols/streamdeck/akp815.md
@docs/protocols/keyboard/proprietary.md
@CHANGELOG.md
@tests/integration/CMakeLists.txt
@src/app/src/time_sync_service.hpp
@src/devices/streamdeck/src/register.cpp

\<upstream_pointer>
**Source plan:** `docs/superpowers/plans/2026-05-13-time-sync.md` — Task 8 (lines 1440-1726).

**Steps adopted verbatim:**

- Step 8.1 (lines 1460-1508) — Add `- clock` to the `capabilities:` list of every devices.yaml entry that gets `hasClock = true` in the corresponding register.cpp row. The 1:1 mirror with register.cpp matters — divergence between docs/\_data/devices.yaml and register.cpp is a documentation drift bug.
- Step 8.2 (lines 1509-1547) — Append a "## Time sync" section to each per-device protocol doc: akp153.md, akp03.md, akp05.md, proprietary.md (keyboard). The section text is a one-paragraph note: "Status: not yet implemented. The device firmware does not currently expose a host-settable RTC over HID. The application's `TimeSyncService::setSystemTimeOn` calls `IClockCapable::setTime()`, which returns `TimeSyncResult::NotImplemented` and emits a `WARN` log once per process lifetime. The day a wire format is captured (vendor-app reverse-engineering or firmware static analysis), only this backend's `setTime()` body changes — the entire UI/service/capability surface stays. See `docs/superpowers/specs/2026-05-13-time-sync-design.md` for the design contract."
- Step 8.3 (CHANGELOG — implicit in upstream Step 8.1's commit body OR a separate step; treat as part of this plan) — Under `[Unreleased]` → `Added`: "Time Sync infrastructure (capability + UI + service); device-side wire format is still TBD per family. Backends return `NotImplemented` honestly — no false success toasts. See `docs/superpowers/specs/2026-05-13-time-sync-design.md` for the design + `docs/superpowers/plans/2026-05-13-time-sync.md` for the implementation plan."
- Step 8.4 (lines 1548-1620) — Write `tests/integration/test_time_sync_e2e.cpp` — Catch2 integration test using `HotplugMonitor::injectEvent` (ARCH-02 from Phase 4) + `MockHidEnumerator` to drive synthetic `deviceArrived` events end-to-end. Assert that with autoSync=true, a synthetic Stream Dock arrival triggers the 300ms debounce + `setTime()` call + `NotImplemented` result + the appropriate signal/log; with autoSync=false, no setTime is called.
- Step 8.5 (lines 1621-1624) — Wire test into tests/integration/CMakeLists.txt.
- Step 8.6 (lines 1625-1634) — Build to verify.
- Step 8.7 (lines 1635-1644) — Run all tests including the new integration test.
- Step 8.8 (lines 1645-1726) — Commit message: `docs+test(time-sync): protocol docs, devices.yaml, CHANGELOG, integration test (Phase 5 close)`.

**Amendments to Task 8 (this plan only):**

- **A-07 (D-01 amendment 1 docs parity) — AKP815 protocol doc.** Append the "## Time sync" section to `docs/protocols/streamdeck/akp815.md` as well. The upstream plan (written before AKP815 backend existed) only listed 4 protocol docs; AKP815 needs the same treatment for symmetric documentation coverage.
- **A-08 (D-02 enforcement) — Toast-suppression assertion in integration test.** The upstream test asserts the auto-sync path fires. This plan additionally asserts that the auto-sync path does NOT emit a toast-bearing signal — only the glyph-bearing signal (`autoSyncResult` or whichever Plan 05-04 chose). Use a QSignalSpy on the toast-bearing `syncFailed` signal to assert it stays at 0 emissions during the auto-sync exercise (D-02 enforcement at the integration-test boundary).

\</upstream_pointer>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply upstream Steps 8.1–8.3 — devices.yaml + per-protocol docs + CHANGELOG (with A-07 AKP815 doc)</name>
  <files>docs/_data/devices.yaml, docs/protocols/streamdeck/akp153.md, docs/protocols/streamdeck/akp03.md, docs/protocols/streamdeck/akp05.md, docs/protocols/streamdeck/akp815.md, docs/protocols/keyboard/proprietary.md, CHANGELOG.md</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 1460-1547 (Steps 8.1, 8.2)
    - docs/_data/devices.yaml (current entries — find the capabilities: list per entry)
    - src/devices/streamdeck/src/register.cpp (the 17 .hasClock = true rows landed in Plan 05-02 — devices.yaml must mirror)
    - src/devices/keyboard/src/register.cpp (the AKB980 PRO .hasClock = true row landed in Plan 05-03)
    - docs/protocols/streamdeck/akp153.md (existing structure — "## Time sync" section gets appended at the end)
    - docs/protocols/streamdeck/akp815.md (A-07 — same treatment as the other three streamdeck protocol docs)
    - CHANGELOG.md (current [Unreleased] section structure)
    - .planning/phases/05-time-sync-scaffolding/05-CONTEXT.md (CHANGELOG line wording)
  </read_first>
  <action>
    Apply upstream Step 8.1 — in docs/_data/devices.yaml, for every entry whose register.cpp counterpart has `.hasClock = true`, add `- clock` to the `capabilities:` list. Cross-reference against register.cpp's 18 rows (17 streamdeck + 1 keyboard) — devices.yaml may be aggregated by codename rather than VID/PID, so the count may be lower than 18 (e.g., AKP03 + 7 rebadges → one yaml entry per rebadge OR one entry covering them). Mirror whatever shape devices.yaml uses.

```
Apply upstream Step 8.2 — append the "## Time sync" section verbatim (per upstream wording) to:
- docs/protocols/streamdeck/akp153.md
- docs/protocols/streamdeck/akp03.md
- docs/protocols/streamdeck/akp05.md
- docs/protocols/streamdeck/akp815.md  ← A-07 (NOT in upstream — AKP815 doc parity per D-01 amendment 1)
- docs/protocols/keyboard/proprietary.md

Section text (per upstream Step 8.2):
> ## Time sync
>
> Status: not yet implemented. The device firmware does not currently expose a host-settable RTC over HID. The application's `TimeSyncService::setSystemTimeOn` calls `IClockCapable::setTime()`, which returns `TimeSyncResult::NotImplemented` and emits a `WARN` log once per process lifetime via `std::call_once`. The day a wire format is captured (vendor-app reverse-engineering or firmware static analysis), only this backend's `setTime()` body changes — the UI / service / capability advertisement / per-row glyph all stay. See `docs/superpowers/specs/2026-05-13-time-sync-design.md` for the design contract and `docs/superpowers/plans/2026-05-13-time-sync.md` for the implementation plan.

Apply upstream Step 8.3 (CHANGELOG entry) — under `[Unreleased]` → `Added`:
> - Time Sync infrastructure (capability + UI + service); device-side wire format is still TBD per family. Backends honestly return `NotImplemented` — no false success toasts. (TIMESYNC-01..06; D-01..D-03; Pitfalls 2/4/13/14.) See `docs/superpowers/specs/2026-05-13-time-sync-design.md` and `docs/superpowers/plans/2026-05-13-time-sync.md`.
```

</action>
  <verify>
    <automated>grep -c "^- clock$" docs/_data/devices.yaml ; for f in docs/protocols/streamdeck/akp153.md docs/protocols/streamdeck/akp03.md docs/protocols/streamdeck/akp05.md docs/protocols/streamdeck/akp815.md docs/protocols/keyboard/proprietary.md ; do grep -q "## Time sync" "$f" && echo "OK: $f" || echo "FAIL: $f" ; done ; grep -q "Time Sync infrastructure" CHANGELOG.md && echo "CHANGELOG OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- clock" docs/_data/devices.yaml` (or equivalent for the YAML format used) reports a count consistent with the register.cpp truth (count is shape-dependent — verify the math in SUMMARY.md).
    - `grep -q "## Time sync" docs/protocols/streamdeck/akp153.md` exits 0
    - `grep -q "## Time sync" docs/protocols/streamdeck/akp03.md` exits 0
    - `grep -q "## Time sync" docs/protocols/streamdeck/akp05.md` exits 0
    - `grep -q "## Time sync" docs/protocols/streamdeck/akp815.md` exits 0 (A-07 — D-01 amendment 1 docs parity)
    - `grep -q "## Time sync" docs/protocols/keyboard/proprietary.md` exits 0
    - `grep -q "Time Sync infrastructure" CHANGELOG.md` exits 0
    - The CHANGELOG entry references TIMESYNC-01..06 + D-01..D-03 + at least one Pitfall.
  </acceptance_criteria>
  <done>
    Documentation deliverables landed. devices.yaml mirrors register.cpp's hasClock truth. All five per-protocol docs (4 streamdeck + 1 keyboard, A-07 included) have the "## Time sync" section. CHANGELOG entry is in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply upstream Steps 8.4, 8.5 — Integration test using HotplugMonitor::injectEvent + A-08 toast-suppression assertion</name>
  <files>tests/integration/test_time_sync_e2e.cpp, tests/integration/CMakeLists.txt</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 1548-1624 (Steps 8.4, 8.5)
    - .planning/phases/03-architectural-decisions/ARCH-02-mock-seam.md (HotplugMonitor::injectEvent contract — synthetic event injection seam)
    - .planning/phases/04-hot-plug-hardening/04-CONTEXT.md (Phase 4 confirmation that ARCH-02 + MockHidEnumerator are in place)
    - tests/integration/CMakeLists.txt (existing integration test wiring pattern)
    - src/app/src/time_sync_service.hpp (signal shapes — A-08 needs the exact signal name to QSignalSpy)
    - .planning/phases/05-time-sync-scaffolding/05-CONTEXT.md decision D-02 (toast suppression on auto-sync)
  </read_first>
  <action>
    Apply upstream Step 8.4 — write tests/integration/test_time_sync_e2e.cpp. Skeleton:

```
TEST_CASE: "Auto-sync fires after 300ms debounce on synthetic deviceArrived; result is NotImplemented; no toast signal" — using:
  - A real DeviceRegistry registered with the actual factories (so the synthetic deviceArrived produces a real backend that returns NotImplemented honestly).
  - A MockHidEnumerator so the registry's enumerate() responds to the synthetic event.
  - HotplugMonitor::injectEvent(arrived, fakeDeviceId) — the ARCH-02 shim.
  - TimeSyncService construction wired to the registry; autoSync=true.
  - QSignalSpy on TimeSyncService::autoSyncResult (or whichever auto-sync signal Plan 05-04 chose) — asserts ≥1 emission within ~500ms.
  - QSignalSpy on TimeSyncService::syncFailed (the toast-bearing signal) — A-08: asserts EXACTLY 0 emissions during the auto-sync exercise (D-02 enforcement).

TEST_CASE: "Manual sync emits both syncFailed (toast) and the glyph signal on NotImplemented" — direct call to TimeSyncService::setSystemTimeOn(codename); asserts syncFailed fires once with reason="not_implemented" AND the glyph signal fires once.

TEST_CASE: "autoSync=false suppresses the auto-sync path entirely" — same harness as test 1 but TimeSyncService.autoSync = false; asserts no signal fires from a synthetic deviceArrived.

If the test harness can't drive a real backend through MockHidEnumerator (because the synthetic device's VID/PID doesn't match a registered factory), use a registered factory whose backend is one of the 5 stubs from Plans 05-02/03 (e.g., the AKP153 vid/pid pair) — the stub returns NotImplemented uniformly, which is exactly what the test wants to observe.

Apply Step 8.5 — wire test_time_sync_e2e.cpp into tests/integration/CMakeLists.txt mirroring the existing integration-test entries.

Then run Steps 8.6 (build), 8.7 (full ctest including the new integration test), and 8.8 (commit) — commit message per upstream: `docs+test(time-sync): protocol docs, devices.yaml, CHANGELOG, integration test (Phase 5 close)` with body lines:
  Refs: TIMESYNC-02, TIMESYNC-03, TIMESYNC-04, TIMESYNC-05, TIMESYNC-06.
  Closes Phase 5: 8-task adoption + 3 amendments (D-01..D-03) + ARCH-02 reuse.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target test_time_sync_e2e 2>&1 | tail -20 ; ctest --preset linux-release -R test_time_sync_e2e --output-on-failure 2>&1 | tail -30 ; ctest --preset linux-release 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^TEST_CASE' tests/integration/test_time_sync_e2e.cpp` reports ≥ 3
    - `grep -q 'injectEvent' tests/integration/test_time_sync_e2e.cpp` exits 0 (ARCH-02 reuse)
    - `grep -q 'MockHidEnumerator' tests/integration/test_time_sync_e2e.cpp` exits 0
    - `grep -q 'QSignalSpy' tests/integration/test_time_sync_e2e.cpp` exits 0 (signal assertions)
    - `grep -q 'autoSync' tests/integration/test_time_sync_e2e.cpp` exits 0 (toggle exercised)
    - A-08 enforcement: the test asserts `spy_syncFailed.count() == 0` (or equivalent) during the auto-sync test path.
    - `grep -q 'test_time_sync_e2e' tests/integration/CMakeLists.txt` exits 0
    - `ctest --preset linux-release -R test_time_sync_e2e` exits 0 — all integration cases pass.
    - `ctest --preset linux-release` exits 0 — full suite green (unit + integration).
  </acceptance_criteria>
  <done>
    Integration test exercises the auto-sync hotplug → debounce → setTime stub → NotImplemented loop end-to-end without real hardware. D-02 toast-suppression contract is asserted (A-08). Plan 05-08 commit closes Phase 5.
  </done>
</task>

</tasks>

<verification>
- `git show --stat HEAD` lists 9 files: devices.yaml, 5 protocol docs (including akp815.md per A-07), CHANGELOG.md, test_time_sync_e2e.cpp, tests/integration CMakeLists.txt.
- The "## Time sync" section count across docs/protocols/{streamdeck,keyboard}/ is 5 (one per backend) — A-07 raises this to 5; without A-07 it would be 4.
- The integration test's QSignalSpy on `syncFailed` reports 0 emissions during the auto-sync exercise — D-02 enforced at the test boundary.
- Phase 5 cumulative: 8 plan files + 8 atomic commits. Upstream 8-task plan adopted as the spine; 3 amendments (D-01 amendment 1 → A-02 + A-07 + a register.cpp row count change; D-01 amendment 2 → A-03; D-01 amendment 3 → A-04 + A-06) all observable in the commit history.
</verification>

\<success_criteria>

- ROADMAP SC2 fully closed (TIMESYNC-02): all 5 backends advertise + return NotImplemented; integration test verifies the loop.
- ROADMAP SC4 closed (TIMESYNC-04): NotImplemented surfaces as glyph (auto) or toast+glyph (manual); test asserts the split.
- ROADMAP SC6 closed (TIMESYNC-06): the 8-task upstream plan was adopted as the Phase 5 spine; 8 plan files exist; 8 atomic commits land.
- D-01..D-03 amendments closed observable in code + tests + docs.
- ARCH-02 reuse: integration test consumes the HotplugMonitor::injectEvent shim, validating the Phase 3 architectural decision retroactively.

\</success_criteria>

<output>
After completion, create `.planning/phases/05-time-sync-scaffolding/05-08-SUMMARY.md` noting: (1) the actual `- clock` count in devices.yaml + the count rationale (e.g., "10 entries because rebadges share a yaml row"), (2) test count + pass/fail/skip per case, (3) any test that was DEFERRED because the harness couldn't drive a synthetic device (e.g., MockHidEnumerator gap), (4) Phase 5 close-out checklist: all 8 plans committed, all 6 ROADMAP success criteria observable as truths in the codebase.
</output>
