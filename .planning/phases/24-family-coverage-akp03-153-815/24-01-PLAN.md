---
phase: 24-family-coverage-akp03-153-815
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/devices/streamdeck/include/ajazz/streamdeck/streamdeck.hpp
  - src/devices/streamdeck/src/akp03.cpp
  - src/devices/streamdeck/src/akp153.cpp
  - src/devices/streamdeck/src/akp815.cpp
autonomous: true
requirements: [DEVICES-10]
must_haves:
  truths:
    - Execution STOPS before any code change if 14-02-SUMMARY.md or 15-01-SUMMARY.md is absent (the control-service + input seams this phase generalises do not exist yet).
    - makeAkp03WithTransport / makeAkp153WithTransport / makeAkp815WithTransport DI overloads exist and inject an ITransport the same way makeAkp05WithTransport does, so MockTransport byte tests can drive each family backend without hardware.
    - The new overloads add NO wire-format change - they reuse the existing per-family construction path (same descriptor, same opcodes, same image transforms), only the transport is injected.
  artifacts:
    - path: src/devices/streamdeck/include/ajazz/streamdeck/streamdeck.hpp
      provides: public declarations for makeAkp03WithTransport / makeAkp153WithTransport / makeAkp815WithTransport (parallels makeAkp05WithTransport)
      contains: makeAkp03WithTransport
    - path: src/devices/streamdeck/src/akp03.cpp
      provides: makeAkp03WithTransport definition routing the COD-026 DI ctor across the TU boundary
      contains: makeAkp03WithTransport
    - path: src/devices/streamdeck/src/akp153.cpp
      provides: makeAkp153WithTransport definition
      contains: makeAkp153WithTransport
    - path: src/devices/streamdeck/src/akp815.cpp
      provides: makeAkp815WithTransport definition
      contains: makeAkp815WithTransport
  key_links:
    - from: src/devices/streamdeck/src/akp03.cpp
      to: tests (MockTransport)
      via: makeAkp03WithTransport exposes the anonymous-namespace DI ctor across the TU boundary (CAPTURE-04 pattern, mirrors makeAkp05WithTransport at akp05.cpp:928)
      pattern: makeAkp03WithTransport
---

<objective>
Gate the phase on its prerequisites, then add the missing per-family test-injection
seam so AKP03 / AKP153 / AKP815 can be driven by `MockTransport` byte tests exactly
the way the AKP05E template (`makeAkp05WithTransport`, akp05.cpp:928) already is.

Purpose: Phase 24 generalises the AKP05E vertical slice (Phases 14-19) across the rest
of the Stream Dock family. That generalisation is verified hardware-free by per-family
`MockTransport` byte tests - but only `makeAkp05WithTransport` exists today; AKP03/153/815
expose just the production `makeAkp03`/`makeAkp153`/`makeAkp815` factories with no
transport-injection overload. This plan is the load-bearing test enabler (Wave 0 for
24-02's family test) and the dependency gate: it STOPS if the Phase-14 control service
and Phase-15 input service this phase reuses have not actually been built yet.

Output: three new `make*WithTransport` DI overloads (header + per-family .cpp definition),
no wire-format change, plus a hard STOP-if-SUMMARY-absent prerequisite gate.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-family-coverage-akp03-153-815/24-CONTEXT.md
@.planning/phases/24-family-coverage-akp03-153-815/24-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted in-tree this session. Reuse directly; do NOT alter the wire layer. -->

The TEMPLATE to copy (src/devices/streamdeck/src/akp05.cpp:920-935, declared in
src/devices/streamdeck/include/ajazz/streamdeck/streamdeck.hpp:87-94):
makeAkp05WithTransport(core::DeviceDescriptor const& d, core::DeviceId id, core::TransportPtr transport) -> core::DevicePtr
It exposes the anonymous-namespace COD-026 DI constructor across the TU boundary so a
test can inject a MockTransport. This is the CAPTURE-04 pattern (also makeAjSeriesWithTransport).

The PRODUCTION factories that already exist (each in its own .cpp, each building its own
descriptor/backend with the per-family opcodes + image transforms):
src/devices/streamdeck/src/akp03.cpp:576 makeAkp03(DeviceDescriptor const&, DeviceId)
src/devices/streamdeck/src/akp153.cpp:463 makeAkp153(DeviceDescriptor const&, DeviceId)
src/devices/streamdeck/src/akp815.cpp:306 makeAkp815(DeviceDescriptor const&, DeviceId)
Each constructs its concrete backend via a private/anonymous-namespace ctor that already
takes (or can take) a TransportPtr - read each file's existing ctor to confirm the exact
DI ctor signature before adding the overload (mirror how akp05.cpp's ctor is reached by
makeAkp05WithTransport).

core::TransportPtr / core::DevicePtr / core::DeviceDescriptor / core::DeviceId live in
src/core/include/ajazz/core/device.hpp. tests/unit/fixtures/mock_transport.hpp implements
MockTransport satisfying ITransport (open / read / write / writes() / enqueueRead /
enqueueReadFeature).

Per-family facts (confirm in the protocol headers before writing 24-02 tests):
AKP03 (akp03_protocol.hpp): DisplayKeyCount=6, SideButtonCount=3, KeyCount=9, EncoderCount=3,
60x60, image cmd "B..."(CmdImage), init "DIS", PacketSize=512.
AKP153 (akp153_protocol.hpp): KeyCount=15, 85x85, CmdBat "B...", PacketSize=512; transform Rot90+mirror.
AKP815 (akp815_protocol.hpp): KeyCount=15, KeyRows=5, KeyCols=3, 100x100, PacketSize=512; transform Rot180.
Each backend OWNS its ImageTransform internally (mirrors akp05.cpp akp05KeyTransform()):
do NOT move transform selection out of the backend; the control service stays geometry-generic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STOP-if-SUMMARY-absent prerequisite gate (Phases 14 + 15)</name>
  <files>(read-only gate; no file modified)</files>
  <read_first>
    - .planning/phases/24-family-coverage-akp03-153-815/24-CONTEXT.md (LOCKED decision 1: depends_on [14,15,16,19]; the AKP05E slice is the template and is PLANNED but may not be executed)
    - .planning/STATE.md (Current Position: confirm whether Phases 14/15 are executed or only planned)
  </read_first>
  <action>
    Before touching any code, verify the prerequisites that this phase generalises actually
    exist. Run, in the repo root, ls on
    .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md and on
    .planning/phases/15-stream-dock-input-routing/15-01-SUMMARY.md. Both SUMMARY files MUST
    exist. They record the executed shape of the capability-generic StreamDockControlService
    (the assignKeyImage/repaintFromProfile/setActiveDevice seam, the chosen keyIndex base
    mapping) and StreamDockInputService (the ProfileAccessor + injected ActionEngine seam,
    the coalescer, the synthetic-encoder-release hook) that Plan 24-02 reuses to drive the
    family. If EITHER summary is absent, STOP immediately and report to the operator: "Phase 24
    depends on the executed Phase-14 control service and Phase-15 input service (per 24-CONTEXT
    decision 1); 14-02-SUMMARY.md and/or 15-01-SUMMARY.md are absent - execute Phases 14 and 15
    first, then resume Phase 24." Do NOT proceed to Task 2. If both exist, read the two SUMMARY
    files now and record (for Task 2 / Plan 24-02) the actual public surface of each service and
    the keyIndex base mapping they chose.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md && test -f .planning/phases/15-stream-dock-input-routing/15-01-SUMMARY.md && echo PREREQS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints `PREREQS_PRESENT` (both SUMMARY files exist); otherwise execution STOPPED here with the operator message above and no code was changed.
    - The actual control-service and input-service public surfaces (method names, the ProfileAccessor/engine seam, the keyIndex base) are extracted from the two SUMMARY files for use by Plan 24-02.
  </acceptance_criteria>
  <done>Both Phase-14 and Phase-15 SUMMARY files confirmed present and read; OR execution halted with a clear "build 14/15 first" message and zero code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add makeAkp03/153/815 WithTransport DI overloads (test-injection seam)</name>
  <files>src/devices/streamdeck/include/ajazz/streamdeck/streamdeck.hpp, src/devices/streamdeck/src/akp03.cpp, src/devices/streamdeck/src/akp153.cpp, src/devices/streamdeck/src/akp815.cpp</files>
  <read_first>
    - src/devices/streamdeck/include/ajazz/streamdeck/streamdeck.hpp (lines 53-114: makeAkp153/makeAkp03/makeAkp05/makeAkp815 + the makeAkp05WithTransport declaration to mirror)
    - src/devices/streamdeck/src/akp05.cpp (lines 914-935: makeAkp05 vs makeAkp05WithTransport - the exact DI-ctor-across-TU pattern to copy)
    - src/devices/streamdeck/src/akp03.cpp (the makeAkp03 factory at :576 AND the backend ctor it calls - find the ctor that accepts a TransportPtr; READ ONLY the wire/opcode code, do not change it)
    - src/devices/streamdeck/src/akp153.cpp (makeAkp153 at :463 + its backend ctor)
    - src/devices/streamdeck/src/akp815.cpp (makeAkp815 at :306 + its backend ctor; note AKP815 currently reuses AKP153 construction per 24-CONTEXT deferred note)
    - src/devices/streamdeck/src/akp03_protocol.hpp, akp153_protocol.hpp, akp815_protocol.hpp (READ ONLY - confirm no wire change is needed)
    - tests/unit/fixtures/mock_transport.hpp (the ITransport the overload must accept)
  </read_first>
  <action>
    Add three transport-injection factory overloads that exactly parallel makeAkp05WithTransport
    (akp05.cpp:928) - the ONLY new code is the DI seam; reuse each family's existing
    construction/wire/transform path verbatim. In streamdeck.hpp, declare makeAkp03WithTransport,
    makeAkp153WithTransport, makeAkp815WithTransport (each taking DeviceDescriptor const&,
    DeviceId, TransportPtr and returning core::DevicePtr), placed next to each family's existing
    make* factory and mirroring the makeAkp05WithTransport doxygen + signature. In akp03.cpp,
    define makeAkp03WithTransport by constructing the SAME concrete backend makeAkp03 builds, but
    passing the injected transport into the backend's DI ctor instead of letting it create a
    HidTransport - exactly as makeAkp05WithTransport does for Akp05Device. Read the existing
    makeAkp03 body first and reuse its descriptor/backend wiring; only the transport source
    differs. Do the same in akp153.cpp (makeAkp153WithTransport) and akp815.cpp
    (makeAkp815WithTransport). If a family's production factory does not yet route a TransportPtr
    into its ctor, add the minimal DI ctor overload on that backend (parallel to Akp05Device's
    COD-026 anonymous-namespace DI ctor) WITHOUT touching any opcode, packet layout, image
    transform, init sequence, or report framing - the wire layer is RE source of truth (CLAUDE.md).
    Do NOT change any *_protocol.hpp. Do NOT change PacketSize, any Cmd* opcode, any ImageTransform,
    or any init/version/clear builder. Do NOT add nlohmann/json anywhere (COD-031). The overloads
    are construction-only. Keep cross-platform clean: no -Wunused, ASCII-only, and export each new
    symbol the same way makeAkp05WithTransport is (same header section, same visibility).
  </action>
  <verify>
    <automated>cmake --preset linux-release >/dev/null 2>&1; cmake --build --preset linux-release >/dev/null 2>&1 && grep -q 'makeAkp03WithTransport' src/devices/streamdeck/src/akp03.cpp && grep -q 'makeAkp153WithTransport' src/devices/streamdeck/src/akp153.cpp && grep -q 'makeAkp815WithTransport' src/devices/streamdeck/src/akp815.cpp && echo OVERLOADS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - The app + device libraries build clean under `cmake --build --preset linux-release` (cross-platform-strict; no -Wunused / no new warning-as-error).
    - `grep makeAkp03WithTransport src/devices/streamdeck/src/akp03.cpp`, `makeAkp153WithTransport` in akp153.cpp, and `makeAkp815WithTransport` in akp815.cpp each return a definition; all three are declared in streamdeck.hpp.
    - Each overload accepts a `core::TransportPtr` and routes it into the same backend `makeAkp0*` constructs (DI ctor reached across the TU boundary, mirroring makeAkp05WithTransport).
    - `git diff --stat` shows NO changes to akp03_protocol.hpp / akp153_protocol.hpp / akp815_protocol.hpp / akp_common_protocol.hpp (wire layer untouched, CLAUDE.md RE-source-of-truth).
    - No PacketSize / Cmd* / ImageTransform / init / version / clear builder changed (construction-only diff).
    - `grep -c nlohmann src/core/include` unchanged (COD-031 boundary intact).
    - Full `ctest --preset linux-release` suite still green (no regression; existing per-family byte tests still pass).
  </acceptance_criteria>
  <done>AKP03/AKP153/AKP815 each expose a makeAkp0*WithTransport DI overload mirroring makeAkp05WithTransport; the wire layer and COD-031 boundary are untouched; the suite is green. The family MockTransport test (24-02) can now drive each backend without hardware.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                                                                                                                                                                              |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| test transport -> per-family backend ctor (DI seam) | The new overload injects an arbitrary ITransport into the backend. In production only HidTransport is used; the DI overload is a test-only construction path (parallels makeAkp05WithTransport) and adds no new runtime attack surface.                                  |
| reused per-family wire layer (paint/decode bounds)  | Reuses the already-tested AKP05E-class paint/decode bounds across the family; same wire layer, same image_pipeline. keyIndex bounds differ per family: AKP03 1..9, AKP153/AKP815 1..15 - enforced by each backend's existing keyIndexInRange, not bypassed by this plan. |

## STRIDE Threat Register

| Threat ID | Category  | Component                                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                            |
| --------- | --------- | ------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-24-01   | Tampering | new make\*WithTransport DI overload (construction-only) | mitigate    | Overload is construction-only and reuses the existing backend ctor + wire path; no opcode/packet/transform/init change. Verified by `git diff --stat` excluding all \*\_protocol.hpp and by the no-builder-changed acceptance criterion. [VERIFIED akp05.cpp:928 template] |
| T-24-02   | Tampering | reused per-family wire layer (RE source of truth)       | mitigate    | No wire edit (24-CONTEXT decision 2). per-family keyIndex bounds (AKP03 1..9, AKP153/815 1..15) remain enforced by each backend's keyIndexInRange; this plan does not touch them.                                                                                          |
| T-24-SC   | Tampering | npm/pip/cargo installs                                  | accept      | No package installs - pure in-tree C++/Qt. Package Legitimacy Gate N/A (generalisation phase; no new dependency).                                                                                                                                                          |

V5 (input validation): no new wire builder, no new parser; per-family index/brightness/payload guards in the existing backends are reused unchanged. The DI overload only swaps the transport.
\</threat_model>

<verification>
- Task 1: `test -f` gate for both 14-02-SUMMARY.md and 15-01-SUMMARY.md prints PREREQS_PRESENT (else execution halted, zero code changes).
- Task 2: build green; `make*WithTransport` defined in each family .cpp and declared in streamdeck.hpp.
- `git diff --stat` excludes every *_protocol.hpp (wire layer untouched).
- `grep -c nlohmann src/core/include` unchanged (COD-031).
- Full `ctest --preset linux-release` suite green (no regression).
</verification>

\<success_criteria>

- DEVICES-10 (enabler): each Stream Dock family backend (AKP03/153/815) has a MockTransport DI factory mirroring makeAkp05WithTransport, so the capability-generic family test in 24-02 can drive it hardware-free.
- The phase prerequisites (executed Phase-14 control service + Phase-15 input service) are confirmed present, or the phase halted with a clear "build 14/15 first" message.
- No wire-format change anywhere; COD-031 boundary intact; cross-platform-strict build clean.
  \</success_criteria>

<output>
Create `.planning/phases/24-family-coverage-akp03-153-815/24-01-SUMMARY.md` when done.
Record: confirmation that 14-02/15-01 SUMMARY files were present and the actual control/input
service public surfaces + keyIndex base mapping extracted from them (for 24-02); the exact
DI ctor signature each family backend exposes; whether any backend needed a new DI ctor
overload added (and that it was construction-only); and any per-family construction quirk
(e.g. AKP815 reusing AKP153 construction) relevant to the 24-02 family test.
</output>
