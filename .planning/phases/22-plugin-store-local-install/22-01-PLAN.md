---
phase: 22-plugin-store-local-install
plan: 01
type: execute
wave: 1
depends_on: [18]
files_modified:
  - src/app/CMakeLists.txt
  - src/app/src/plugin_verify_gate.hpp
  - src/app/src/plugin_verify_gate.cpp
  - src/app/src/plugin_catalog_model.hpp
  - src/app/src/plugin_catalog_model.cpp
  - tests/unit/test_plugin_verify_gate.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [PLUGIN-14]
user_setup: []
must_haves:
  truths:
    - The Phase-18 SUMMARY (18-04) is confirmed present before any code consuming the PluginManager on-disk layout is written; if absent, execution STOPS and reports Phase 18 must execute first
    - A reusable verify gate runs verifyManifest against a staging dir's manifest.json and returns a three-way verdict (trusted / self-signed / refused) using the same trustLevelOf vocabulary
    - The verify gate is fail-closed when the Ed25519 signer is unavailable (AJAZZ_BUILD_PYTHON_HOST=OFF or python/script missing) - it returns refused, never a silent pass
    - manifest_signer (ajazz::plugins) link and the AJAZZ_PLUGIN_VERIFIER_SCRIPT / AJAZZ_PLUGIN_TRUST_ROOTS compile defs are available to the app whenever the verify gate is built (un-gated from AJAZZ_BUILD_PYTHON_HOST, or fail-closed wired)
    - The existing network install() and the launch-time extractStandalonePluginArchives sweep route through the same verify gate - a tampered package is never promoted/marked-installed by any path
  artifacts:
    - path: src/app/src/plugin_verify_gate.hpp
      provides: makeSignerConfig() + verifyStagedPlugin(stagingManifestPath) -> VerifyVerdict {trusted/self-signed/refused}; fail-closed when signer unavailable
      contains: verifyStagedPlugin
    - path: src/app/src/plugin_verify_gate.cpp
      provides: config construction from AJAZZ_PLUGIN_* defs (no path re-derivation) + verifyManifest call + verdict collapse + fail-closed branch
      contains: verifyStagedPlugin
    - path: tests/unit/test_plugin_verify_gate.cpp
      provides: signed->trusted/self-signed, tampered->refused, unsigned->refused, signer-unavailable->refused cases using the manifest_signer keygen/sign/tamper recipe
      contains: PluginVerifyGate
  key_links:
    - from: verifyStagedPlugin
      to: ajazz::plugins::verifyManifest
      via: ManifestSignerConfig built from AJAZZ_PLUGIN_VERIFIER_SCRIPT + AJAZZ_PLUGIN_TRUST_ROOTS defs
      pattern: verifyManifest
    - from: PluginCatalogModel::install (network) + extractStandalonePluginArchives sweep call site
      to: verifyStagedPlugin
      via: verify staged extraction before flipping installed=true / before sweeping into place
      pattern: verifyStagedPlugin
    - from: src/app/CMakeLists.txt
      to: ajazz::plugins + AJAZZ_PLUGIN_* defs
      via: link + defs available wherever the verify gate compiles (un-gated or fail-closed)
      pattern: AJAZZ_PLUGIN_VERIFIER_SCRIPT
---

<objective>
Land the trust-boundary foundation for Phase 22: a single reusable verify gate
(`verifyStagedPlugin`) wired to the existing Ed25519 `verifyManifest`, made available to the app
regardless of `AJAZZ_BUILD_PYTHON_HOST`, and applied to the EXISTING unverified install paths (the
network `install()` and the launch-time `extractStandalonePluginArchives` sweep) so no path can
promote an unverified/tampered plugin. The new local-file entry point (plan 02) reuses this gate.

This plan also performs the **STOP-if-SUMMARY-absent gate** for Phase 18 (mirroring 19-01): the
executor MUST confirm `18-04-SUMMARY.md` exists and read the finalized `installedPlugins/` on-disk
layout before writing anything that depends on it; if absent, STOP and report Phase 18 must execute
first. PLUGIN-14.

Purpose: PLUGIN-14 trust gate — close the vendor's no-signature gap by making verify the choke
point ALL install paths pass through, fail-closed when the verifier is unavailable.
Output: `plugin_verify_gate.{hpp,cpp}` + its unit suite; the network/sweep paths gated; the signer
link + verifier defs available to the app unconditionally (or fail-closed if the build omits them).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-plugin-store-local-install/22-CONTEXT.md
@.planning/phases/22-plugin-store-local-install/22-RESEARCH.md
@.planning/phases/22-plugin-store-local-install/22-VALIDATION.md
@CLAUDE.md

\<read_first>

- **STOP GATE (REQUIRED — read first; if absent, STOP and report Phase 18 must execute before Phase 22):**
  - `.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-SUMMARY.md` — the FINAL on-disk layout `PluginManager` discovers (the `installedPlugins/` vs single-`plugins/` resolution, RESEARCH A2) and the manifest.json relative path the discovery scans. The verify gate must point `verifyManifest` at the SAME `manifest.json` path the manager will load. As of planning (2026-05-23) this SUMMARY does NOT exist on disk.
  - Fallback if absent: do NOT assume the layout. STOP, report `18-04-SUMMARY.md` missing, recommend executing Phase 18 first.
- `src/plugins/include/ajazz/plugins/manifest_signer.hpp` lines 57-99 — `verifyManifest(manifestPath, ManifestSignerConfig) -> ManifestVerifyResult{valid, publisherKeyB64, publisherName}`; the fail-closed contract (valid=false on any error); the three-way semantics (valid=false → unsigned/tampered; valid=true+empty publisher → self-signed; valid=true+publisher → trusted).
- `src/app/src/loaded_plugins_model.hpp` lines ~45-92,152 — `trustLevelOf` collapse to `"trusted"`/`"self-signed"`/`"unsigned"`; reuse the SAME enum strings for the gate verdict so UI stays consistent.
- `src/app/src/plugin_catalog_model.cpp` lines 415-427 (`validateDownloadedArchive`), 463-603 (the network `install()` finished handler that today extracts then sets `installed=true` at :586-594 with NO verify), 99-158 (constructor `reload()` + the launch `extractStandalonePluginArchives(userPluginsDir())` sweep at ~:110), 395-411 (`userPluginsDir()`).
- `src/app/src/sdplugin_extractor.hpp` — `extractSdPluginArchive(archivePath, destDir, targetSubdir)` (zip-slip-guarded, atomic tmp+rename); `extractStandalonePluginArchives(pluginsDir)` (launch sweep — verify must be inserted here).
- `src/app/CMakeLists.txt` lines 437-451 — `if(AJAZZ_BUILD_PYTHON_HOST)`: `target_link_libraries(... ajazz::plugins)` + `AJAZZ_PLUGIN_VERIFIER_SCRIPT` / `AJAZZ_PLUGIN_TRUST_ROOTS` compile defs. Decide: un-gate these (recommended — the signer subprocess-calls python at runtime and does not need the python *host*) OR keep gated and make the gate compile-time fail-closed when the defs are absent.
- `CMakeLists.txt` lines 29-34 — `AJAZZ_BUILD_PYTHON_HOST` option (default ON; some CI matrices pass `-DAJAZZ_BUILD_PYTHON_HOST=OFF`).
- `tests/unit/test_manifest_signer.cpp` lines ~194-260 — the keygen / sign / tamper / unsigned recipe (shell out to `scripts/sign-plugin-manifest.py keygen|sign`; flip a byte after signing for tampered; omit the `Ajazz.Signing` block for unsigned) — reuse verbatim to build gate fixtures.
- `tests/unit/test_sdplugin_extractor.cpp` lines ~42-57 — `QZipWriter` `.sdPlugin` fixture recipe (`addFile("com.example.sdPlugin/manifest.json", bytes)`, `close()` before reading on Win32).
- `.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-PLAN.md` — the STOP-condition wording precedent + `extractStandalonePluginArchives` reference in 18's manager.
- `resources/trusted_publishers.json` — placeholder key (`REPLACE_WITH_..._PLACEHOLDER==`): every validly-signed plugin currently resolves self-signed, never trusted (Pitfall 4). The gate verdict must allow self-signed (per plan 02 policy) and must not promise a trusted badge that cannot appear.
- `docs/protocols/streamdeck/akp_plugin_sdk.md` §6 — the plugin store / no-signature gap this gate closes.
- CLAUDE.md — COD-031 (`manifest_signer` stays in `ajazz_plugins`; app links it PRIVATE; no `nlohmann` leak into `ajazz_core`); never skip pre-commit; ASCII test names; ctest filter `-R`.
  \</read_first>

<interfaces>
<!-- Reused contracts the executor must NOT reimplement. -->

From src/plugins/include/ajazz/plugins/manifest_signer.hpp:
struct ManifestSignerConfig { std::string pythonExecutable{"python3"}; std::filesystem::path verifierScript; std::filesystem::path trustedPublishersFile; };
struct ManifestVerifyResult { bool valid{false}; std::string publisherKeyB64; std::string publisherName; };
ManifestVerifyResult verifyManifest(std::filesystem::path const& manifestPath, ManifestSignerConfig const& config);

From src/app/src/sdplugin_extractor.hpp:
bool extractSdPluginArchive(QString const& archivePath, QString const& destDir, QString const& targetSubdir);
void extractStandalonePluginArchives(QString const& pluginsDir);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STOP-gate Phase 18 + un-gate the signer link/defs (or fail-closed) + the verify-gate helper</name>
  <files>src/app/CMakeLists.txt, src/app/src/plugin_verify_gate.hpp, src/app/src/plugin_verify_gate.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
  - `18-04-SUMMARY.md` (STOP gate — confirm present, read the `installedPlugins/`/manifest.json layout).
  - manifest_signer.hpp lines 57-99 (verifyManifest + fail-closed + three-way semantics).
  - src/app/CMakeLists.txt lines 437-451 (the AJAZZ_BUILD_PYTHON_HOST guard around the link + defs).
  - loaded_plugins_model.hpp ~152 (trustLevelOf vocabulary to reuse).
  </read_first>
  <action>
  FIRST verify the STOP GATE: confirm `.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-SUMMARY.md`
  exists. If absent, STOP and report "Phase 22 blocked: dependency SUMMARY 18-04 absent — execute
  Phase 18 first" without writing code. Record in this plan's SUMMARY the FINAL on-disk layout (the
  `installedPlugins/` resolution + the manifest.json relative path) so plan 02's promote target and
  this gate's `verifyManifest` path point at the same place Phase-18 discovery scans.

CMake (the COD-031 build-flag fix, RESEARCH Pitfall 1): make `ajazz::plugins` and the
`AJAZZ_PLUGIN_VERIFIER_SCRIPT` / `AJAZZ_PLUGIN_TRUST_ROOTS` compile defs available to the app
whenever the verify gate compiles. PREFERRED: lift the `target_link_libraries(... ajazz::plugins)`
and those two `target_compile_definitions` OUT of the `if(AJAZZ_BUILD_PYTHON_HOST)` block (keep
`AJAZZ_PYTHON_HOST` + the host-script/PYTHONPATH defs inside the guard — only the verifier link +
the two verifier/trust-roots defs move out). The signer subprocess-calls python at runtime; it does
NOT need the python host built. If the executor instead keeps the guard, it MUST define a sentinel
(e.g. compile-time `#if !defined(AJAZZ_PLUGIN_VERIFIER_SCRIPT)`) so the gate fails closed. Either
way the contract is: the verify gate is always present and never silently bypassed (per D-05
fail-closed). Land this on all three platforms (the link + defs are platform-agnostic; no per-OS
branch needed).

Create `src/app/src/plugin_verify_gate.hpp` in `namespace ajazz::app`:

- `enum class VerifyVerdict { Trusted, SelfSigned, Refused };`
- `struct VerifyOutcome { VerifyVerdict verdict; QString publisherName; QString reason; };`
- `[[nodiscard]] ajazz::plugins::ManifestSignerConfig makeSignerConfig();` — constructs the config
  from the `AJAZZ_PLUGIN_VERIFIER_SCRIPT` + `AJAZZ_PLUGIN_TRUST_ROOTS` compile defs (do NOT
  re-derive paths — RESEARCH anti-pattern). When those defs are absent (the fail-closed build),
  leave the paths empty so `verifyManifest` fails closed.
- `[[nodiscard]] VerifyOutcome verifyStagedPlugin(QString const& stagedManifestJsonPath);`
- a free `QString verdictToTrustLevel(VerifyVerdict)` returning the SAME strings as
  `LoadedPluginsModel::trustLevelOf` (`"trusted"`/`"self-signed"`/`"unsigned"`) so the UI is
  consistent (Refused maps to `"unsigned"`).

Create `plugin_verify_gate.cpp`: implement `verifyStagedPlugin` to (1) if no verifier script def
is compiled in, return `{Refused, {}, "signature verification unavailable in this build"}`
(fail-closed — never trust); (2) else call `verifyManifest(stagedManifestJsonPath, makeSignerConfig())`;
(3) collapse: `valid==false` → `Refused`; `valid==true && publisherName empty` → `SelfSigned`;
`valid==true && publisherName set` → `Trusted`. COD-031: include `<ajazz/plugins/manifest_signer.hpp>`
(PRIVATE-linked) + Qt headers only; never `nlohmann`.

Add `src/plugin_verify_gate.{cpp,hpp}` to the app library `target_sources` block (NOT inside the
WebSockets gate — this is needed unconditionally). Register the test file in
`tests/unit/CMakeLists.txt` linking `plugin_verify_gate.cpp` + `ajazz::plugins`.
</action>
<verify>
<automated>test -f .planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-SUMMARY.md && cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -5; grep -c verifyStagedPlugin src/app/src/plugin_verify_gate.hpp; grep -v '^#' src/app/CMakeLists.txt | grep -c 'AJAZZ_PLUGIN_VERIFIER_SCRIPT'; grep -rc nlohmann src/app/src/plugin_verify_gate.cpp src/app/src/plugin_verify_gate.hpp</automated>
</verify>
\<acceptance_criteria>

- `18-04-SUMMARY.md` exists (STOP gate passes); the on-disk `installedPlugins/`/manifest.json layout is recorded in this plan's SUMMARY.
- The app links `ajazz::plugins` and sees `AJAZZ_PLUGIN_VERIFIER_SCRIPT`/`AJAZZ_PLUGIN_TRUST_ROOTS` independent of `AJAZZ_BUILD_PYTHON_HOST` (or the gate is compile-time fail-closed when they are absent).
- `verifyStagedPlugin` returns `Refused` when the verifier script def is absent (fail-closed), never a pass.
- `grep -c verifyStagedPlugin ...` returns >=1; `grep -rc nlohmann plugin_verify_gate.*` == 0 (COD-031).
- The app target compiles with the new files in the unconditional source block.
  \</acceptance_criteria>
  <done>STOP gate passed and layout recorded; the signer link + verifier defs are available to the app unconditionally (or fail-closed); `verifyStagedPlugin` compiles with the three-way verdict + fail-closed branch.</done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Verify-gate unit suite + gate the network install() and the launch sweep</name>
  <files>src/app/src/plugin_catalog_model.hpp, src/app/src/plugin_catalog_model.cpp, tests/unit/test_plugin_verify_gate.cpp, tests/unit/CMakeLists.txt</files>
  <behavior>
    - verifyStagedPlugin on a signed (self-signed key, placeholder trust roots) staged manifest -> SelfSigned (valid=true, publisher empty per Pitfall 4).
    - verifyStagedPlugin on a tampered manifest (byte flipped after signing) -> Refused; never Trusted/SelfSigned.
    - verifyStagedPlugin on an unsigned manifest (no Ajazz.Signing block) -> Refused (fail-closed).
    - verifyStagedPlugin with an empty/nonexistent verifier-script config -> Refused (signer unavailable, fail-closed) - no crash.
    - verdictToTrustLevel(Refused) == "unsigned"; (SelfSigned) == "self-signed"; (Trusted) == "trusted".
    - The network install() finished handler does NOT set installed=true when the staged extraction's verdict is Refused (tampered/unsigned package never marked installed).
    - The launch extractStandalonePluginArchives sweep does not leave a Refused package in a discoverable extracted state (quarantined/removed, not promoted).
  </behavior>
  <read_first>
  - manifest_signer.hpp + test_manifest_signer.cpp ~194-260 (keygen/sign/tamper/unsigned recipe).
  - test_sdplugin_extractor.cpp ~42-57 (QZipWriter .sdPlugin fixture builder).
  - plugin_catalog_model.cpp lines 463-603 (network install() — verify must run on the extracted staging dir BEFORE installed=true at :586-594) and ~99-158/:110 (the launch sweep call site).
  - sdplugin_extractor.hpp (extractSdPluginArchive target subdir; extractStandalonePluginArchives sweep).
  </read_first>
  <action>
  Create `tests/unit/test_plugin_verify_gate.cpp` with Catch2 cases covering every `<behavior>`
  bullet. Build fixtures with the reused recipes: keygen + sign via `scripts/sign-plugin-manifest.py`
  (shell out exactly as test_manifest_signer does); build the `.sdPlugin` staging tree with
  `QZipWriter` then `extractSdPluginArchive` into a temp dir; point `verifyStagedPlugin` at the
  extracted `manifest.json`. Tampered = flip a byte in the signed manifest after signing. Unsigned =
  a manifest with no `Ajazz.Signing` block. Signer-unavailable = pass a `makeSignerConfig`-like config
  with an empty `verifierScript` (use a small test seam: have `verifyStagedPlugin` accept an optional
  config override param defaulting to `makeSignerConfig()`, so the test can inject an empty config).
  ASCII-only TEST_CASE names + tags (`[plugin-verify-gate]`, `signed -> self-signed`,
  `tampered -> refused`); reuse the test harness conventions.

Gate the EXISTING paths (RESEARCH Pitfall 3 — close the two unverified back doors):

- In `plugin_catalog_model.cpp` network `install()` finished handler: after the successful
  `extractSdPluginArchive`, call `verifyStagedPlugin` on the extracted `manifest.json`. If the
  verdict is `Refused`, do NOT set `installed=true`; remove/quarantine the extracted dir, emit
  `installFinished(uuid, false, tr("signature verification failed"))`, and return. Only `Trusted`
  or `SelfSigned` proceed to flip `installed=true` (the SelfSigned confirm UX is plan 02's concern;
  here the network path treats SelfSigned as allowed-but-logged, matching today's behaviour minus
  the tampered/unsigned hole). Reuse `verdictToTrustLevel` for the log line.
- In the launch `extractStandalonePluginArchives` sweep path: ensure a swept package whose verdict
  is `Refused` is not left in a discoverable extracted state. If the extractor's API does not expose
  a per-package hook, add a thin verify pass in the catalog-model call site (after the sweep, scan
  the swept dirs and quarantine any whose `verifyStagedPlugin` is `Refused`); document the chosen
  seam in the SUMMARY. Do NOT modify the zip-slip guard or extractor internals.

Keep the change minimal and atomic: one commit for the gate helper + suite, one for wiring the two
existing paths. COD-031: no `nlohmann` in the new code.
</action>
<verify>
<automated>ctest --preset linux-release -R "PluginVerifyGate|plugin-verify-gate" --output-on-failure 2>&1 | tail -25</automated>
</verify>
\<acceptance_criteria>

- Signed (self-signed) staged manifest -> SelfSigned; tampered -> Refused; unsigned -> Refused; signer-unavailable -> Refused (no crash) — all green.
- `verdictToTrustLevel` returns the trustLevelOf vocabulary (`trusted`/`self-signed`/`unsigned`).
- The network `install()` path does not mark a Refused package installed (asserted by a test or a code-path review documented in the SUMMARY); the launch sweep does not leave a Refused package discoverable.
- All TEST_CASE names/tags ASCII; `ctest --preset linux-release -R PluginVerifyGate` green.
- `grep -rc nlohmann` on the new code == 0.
  \</acceptance_criteria>
  <done>The verify gate is proven green (signed/tampered/unsigned/unavailable), and the two pre-existing unverified paths (network install + launch sweep) now route through it so a tampered package is never promoted by any path.</done>
  </task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                         |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| downloaded/local `.sdPlugin` -> install path | Untrusted archive bytes + untrusted manifest cross here; verify gate is the trust choke point before promotion.     |
| build config -> verify availability          | `AJAZZ_BUILD_PYTHON_HOST=OFF` could remove the verifier link; the gate must fail closed rather than silently trust. |

## STRIDE Threat Register

| Threat ID        | Category            | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                                                                      |
| ---------------- | ------------------- | ------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-22-tamper      | Tampering           | `verifyStagedPlugin` over a tampered manifest                                   | mitigate    | Ed25519 `verifyManifest` over canonical bytes incl. embedded key → valid=false → `Refused`; tampered never promoted. Asserted by the tampered->refused test.                                         |
| T-22-bypass      | Spoofing/Elevation  | `AJAZZ_BUILD_PYTHON_HOST=OFF` silent-bypass                                     | mitigate    | Signer link + verifier defs un-gated (or compile-time fail-closed); `verifyStagedPlugin` returns `Refused` when the verifier script def is absent. Asserted by the signer-unavailable->refused test. |
| T-22-backdoor    | Tampering/Elevation | unverified network `install()` + launch `extractStandalonePluginArchives` sweep | mitigate    | Both pre-existing paths routed through `verifyStagedPlugin`; a `Refused` package is never marked installed nor left discoverable (Pitfall 3 closed).                                                 |
| T-22-path        | Elevation           | verifier interpreter PATH-hijack                                                | accept      | Already mitigated upstream: `manifest_signer` resolves python against vetted dirs, not `$PATH` (CWE-426); this plan reuses it unchanged.                                                             |
| T-22-SC          | Tampering           | npm/pip/cargo installs                                                          | accept      | No package installs in Phase 22 (pure in-tree C++/Qt6 + existing Python verifier); Package Legitimacy Gate N/A per 22-RESEARCH.                                                                      |
| \</threat_model> |                     |                                                                                 |             |                                                                                                                                                                                                      |

<verification>
- `ctest --preset linux-release -R PluginVerifyGate` green (signed/tampered/unsigned/unavailable).
- `cmake --build --preset linux-release --target ajazz-control-center` compiles with the signer link available regardless of `AJAZZ_BUILD_PYTHON_HOST`.
- `grep -v '^#' src/app/CMakeLists.txt | grep -c AJAZZ_PLUGIN_VERIFIER_SCRIPT` >= 1 outside the python-host guard (or the documented fail-closed sentinel is present).
- `grep -rc nlohmann src/app/src/plugin_verify_gate.*` == 0 (COD-031).
- STOP gate: `18-04-SUMMARY.md` exists (else execution halted and reported).
</verification>

\<success_criteria>

- The STOP gate passed and recorded the Phase-18 on-disk layout for plan 02's promote target.
- A single fail-closed verify gate exists and is reused; the verifier link is available to the app independent of the python-host build flag.
- The two pre-existing unverified install paths (network + launch sweep) now pass through the gate; tampered/unsigned packages are refused by every path.
  \</success_criteria>

<output>
Create `.planning/phases/22-plugin-store-local-install/22-01-SUMMARY.md` when done — record the
Phase-18 `installedPlugins/`/manifest.json layout, the CMake un-gate (or fail-closed sentinel)
choice, the `verifyStagedPlugin` signature, and the seam used to gate the launch sweep.
</output>
