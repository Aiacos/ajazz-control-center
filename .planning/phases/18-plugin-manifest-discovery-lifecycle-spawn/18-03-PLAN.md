---
phase: 18-plugin-manifest-discovery-lifecycle-spawn
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - src/app/src/plugin_mirabox_shim.hpp
  - src/app/src/plugin_mirabox_shim.cpp
  - src/app/CMakeLists.txt
  - tests/unit/test_mirabox_compat.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [PLUGIN-11]
must_haves:
  truths:
    - A constexpr/static JS source string defines window.connectMiraBoxSDSocket as a forwarder to window.connectElgatoStreamDeckSocket with the same arguments
    - makeMiraboxShim() returns a QWebEngineScript injected at DocumentCreation in MainWorld so the alias exists BEFORE the plugin's own script runs
    - The alias forwards all arguments unchanged (a pure forwarding wrapper, same signature)
  artifacts:
    - path: src/app/src/plugin_mirabox_shim.hpp
      provides: kMiraboxShimSource + makeMiraboxShim declarations
      contains: connectMiraBoxSDSocket
    - path: src/app/src/plugin_mirabox_shim.cpp
      provides: QWebEngineScript builder (DocumentCreation/MainWorld) under AJAZZ_HAVE_WEBENGINE
      contains: DocumentCreation
    - path: tests/unit/test_mirabox_compat.cpp
      provides: CompatTest::miraboxSocket_aliasedToElgato (source + injection-point assertions)
      contains: miraboxSocket_aliasedToElgato
  key_links:
    - from: makeMiraboxShim
      to: QWebEngineScript
      via: setInjectionPoint(DocumentCreation) + setWorldId(MainWorld)
      pattern: DocumentCreation
    - from: tests/unit/test_mirabox_compat.cpp
      to: kMiraboxShimSource
      via: assert the JS source aliases connectMiraBoxSDSocket -> connectElgatoStreamDeckSocket
      pattern: connectElgatoStreamDeckSocket
---

<objective>
Build the Mirabox/Elgato compat shim (PLUGIN-11): a `QWebEngineScript` that aliases
`connectMiraBoxSDSocket(...)` to `connectElgatoStreamDeckSocket(...)` so AJAZZ-only HTML packages
load unmodified. The shim MUST inject at `DocumentCreation` in `MainWorld` — a `runJavaScript`
after page load races the plugin's own script (Pitfall 4 / Alternatives table). The shim attaches
to the per-plugin `QQuickWebEngineProfile` that the Wave-4 PluginManager hosts HTML plugins on.

Purpose: PLUGIN-11. Isolated as a small, unit-testable JS-source + builder so the alias string is
asserted without a full DOM round-trip (the DOM-level test is WebEngine-gated and optional).
Output: `plugin_mirabox_shim.{hpp,cpp}` + `CompatTest::miraboxSocket_aliasedToElgato`.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-CONTEXT.md
@.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-RESEARCH.md
@.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-VALIDATION.md
@CLAUDE.md

\<read_first>

- docs/protocols/streamdeck/akp_plugin_sdk.md §9 (the Compatibility-shim row + target name CompatTest::miraboxSocket_aliasedToElgato) + §3 step 3/5 (the HTML plugin calls connectElgatoStreamDeckSocket(...) — the function the alias forwards to).
- 18-RESEARCH.md "Pattern 2: Mirabox alias via document-creation UserScript" (the QWebEngineScript code example: setInjectionPoint DocumentCreation, setWorldId MainWorld, setRunsOnSubFrames true, the forwarding source), Pitfall 4 (alias injected too late), Open Question 3 (where the shim lives — LOCKED to a sibling plugin_mirabox_shim.hpp helper), and Assumption A4 (pure forwarding wrapper, same signature).
- src/app/src/property_inspector_controller.hpp (the AJAZZ_HAVE_WEBENGINE gating pattern + the per-plugin QQuickWebEngineProfile\* activeProfile() the shim attaches to; the PIMPL WebEngineImpl note at :176).
- src/app/src/pi_url_policy.hpp (the existing WebEngine-gated test compilation discipline; how pi_url_policy.cpp is built into the unit target without dragging full WebEngine in — tests/unit/CMakeLists.txt:195-197).
- src/app/CMakeLists.txt:41-66 + the WebEngine gating around property_inspector_controller (~66, 298).
  \</read_first>

<interfaces>
<!-- Contracts THIS plan creates (consumed by 18-04 PluginManager HTML hosting). -->
namespace ajazz::app:
  // Pure JS source — always available, even without WebEngine, so it is unit-testable as a string.
  extern char const* const kMiraboxShimSource;   // or a constexpr std::string_view
  #if defined(AJAZZ_HAVE_WEBENGINE)
  QWebEngineScript makeMiraboxShim();             // DocumentCreation / MainWorld / runsOnSubFrames
  #endif

The forwarding JS (same signature, all args passed through):
window.connectMiraBoxSDSocket = function(){
return window.connectElgatoStreamDeckSocket.apply(window, arguments);
};

The PluginManager (18-04) inserts makeMiraboxShim() into the per-plugin profile via
profile->scripts()->insert(...) BEFORE loading index.html. CLAUDE.md: QWebEngineView +
Qt6::WebChannelQuick — never QCefView.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kMiraboxShimSource + makeMiraboxShim QWebEngineScript builder</name>
  <files>src/app/src/plugin_mirabox_shim.hpp, src/app/src/plugin_mirabox_shim.cpp, src/app/CMakeLists.txt</files>
  <read_first>
    - docs/protocols/streamdeck/akp_plugin_sdk.md §9 (shim row) + §3 (connectElgatoStreamDeckSocket)
    - 18-RESEARCH.md Pattern 2 code example + Pitfall 4 + Open Question 3 + A4
    - src/app/src/property_inspector_controller.hpp (AJAZZ_HAVE_WEBENGINE gating + activeProfile)
  </read_first>
  <behavior>
    - kMiraboxShimSource is a constant JS string that defines window.connectMiraBoxSDSocket as a function forwarding all arguments to window.connectElgatoStreamDeckSocket via .apply(window, arguments) — a pure forwarding wrapper, no semantic change (A4).
    - makeMiraboxShim() (only when AJAZZ_HAVE_WEBENGINE) returns a QWebEngineScript whose name is set, injectionPoint == QWebEngineScript::DocumentCreation, worldId == QWebEngineScript::MainWorld, runsOnSubFrames == true, and sourceCode == kMiraboxShimSource.
  </behavior>
  <action>
    Create plugin_mirabox_shim.hpp (namespace ajazz::app). Always declare the JS source as a
    grep-able constant (`inline constexpr std::string_view kMiraboxShimSource` or
    `char const* const`) carrying the forwarding wrapper from <interfaces> verbatim — this is
    available WITHOUT WebEngine so it is unit-testable as a pure string. Guard the QWebEngineScript
    builder behind `#if defined(AJAZZ_HAVE_WEBENGINE)` (mirror property_inspector_controller.hpp's
    gating + the QtWebEngineQuick include discipline): declare `QWebEngineScript makeMiraboxShim();`.
    Implement plugin_mirabox_shim.cpp: makeMiraboxShim builds the QWebEngineScript per <behavior>,
    naming it "ajazz-mirabox-shim", setInjectionPoint(QWebEngineScript::DocumentCreation),
    setWorldId(QWebEngineScript::MainWorld), setRunsOnSubFrames(true),
    setSourceCode(QString::fromUtf8(kMiraboxShimSource)). Cite akp_plugin_sdk.md §9 + Pitfall 4 in a
    comment explaining why DocumentCreation (not runJavaScript-after-load) is the only correct point.
    No nlohmann (COD-031). No fenced code in source beyond the implementation. Register
    src/plugin_mirabox_shim.cpp + .hpp in src/app/CMakeLists.txt next to property_inspector_controller
    inside the WebEngine-gated block (so the QWebEngineScript include only compiles when WebEngine is
    present); the kMiraboxShimSource string itself must remain reachable in non-WebEngine builds.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -5; grep -c 'DocumentCreation' src/app/src/plugin_mirabox_shim.cpp</automated>
  </verify>
  <acceptance_criteria>
    - plugin_mirabox_shim.{hpp,cpp} compile into the app target; kMiraboxShimSource is reachable in non-WebEngine builds.
    - makeMiraboxShim sets DocumentCreation + MainWorld + runsOnSubFrames and uses kMiraboxShimSource (`grep -c DocumentCreation` >= 1).
    - The JS source aliases connectMiraBoxSDSocket -> connectElgatoStreamDeckSocket via .apply(window, arguments).
  </acceptance_criteria>
  <done>The shim source + DocumentCreation/MainWorld QWebEngineScript builder compile; the alias forwards all arguments to connectElgatoStreamDeckSocket.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompatTest::miraboxSocket_aliasedToElgato + unit wiring</name>
  <files>tests/unit/test_mirabox_compat.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - docs/protocols/streamdeck/akp_plugin_sdk.md §9 (target name)
    - 18-RESEARCH.md Phase Requirements -> Test Map (PLUGIN-11 row: pure string test + optional WebEngine-gated DOM test)
    - tests/unit/CMakeLists.txt:195-197 (how pi_url_policy.cpp is built into the unit target — the WebEngine-gated test discipline)
  </read_first>
  <behavior>
    - miraboxSocket_aliasedToElgato (always-on, pure): kMiraboxShimSource contains both "connectMiraBoxSDSocket" and "connectElgatoStreamDeckSocket" and forwards via .apply(window, arguments) — assert the source string defines the alias as a forwarder.
    - injection point (WebEngine-gated, only when AJAZZ_HAVE_WEBENGINE): makeMiraboxShim().injectionPoint() == QWebEngineScript::DocumentCreation and worldId() == QWebEngineScript::MainWorld and sourceCode() == the shim source.
  </behavior>
  <action>
    Author test_mirabox_compat.cpp with Catch2. An ALWAYS-compiled case
    "CompatTest miraboxSocket_aliasedToElgato" (tag "[mirabox-compat]") asserts the pure string:
    QString src = QString::fromUtf8(kMiraboxShimSource); REQUIRE(src.contains("connectMiraBoxSDSocket"));
    REQUIRE(src.contains("connectElgatoStreamDeckSocket")); REQUIRE(src.contains("apply(window, arguments)"))
    (proving the forwarder shape). A SECOND case
    "CompatTest mirabox shim injects at document creation" guarded by `#if defined(AJAZZ_HAVE_WEBENGINE)`
    builds makeMiraboxShim() and asserts injectionPoint()==QWebEngineScript::DocumentCreation,
    worldId()==QWebEngineScript::MainWorld, sourceCode()==QString::fromUtf8(kMiraboxShimSource). The
    full DOM round-trip (a fixture index.html that calls connectMiraBoxSDSocket and reaches the
    bridge) is OUT OF SCOPE here — it depends on a live profile + the manager's hosting path (18-04)
    and the live witness is Phase 25; note this in a comment. ASCII-only TEST_CASE titles. Wire
    test_mirabox_compat.cpp + ${CMAKE_SOURCE_DIR}/src/app/src/plugin_mirabox_shim.cpp into
    tests/unit/CMakeLists.txt; gate the WebEngine link/define exactly as the existing
    AJAZZ_HAVE_WEBENGINE blocks do (the pure string case compiles unconditionally; the second case
    needs Qt6::WebEngineCore for QWebEngineScript). If AJAZZ_HAVE_WEBENGINE is unavailable in this
    build, the string case alone still proves the alias. Ensure catch_discover_tests picks it up.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R "mirabox-compat" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R "mirabox-compat"` runs and the pure-string CompatTest case passes unconditionally.
    - The string case asserts the forwarder aliases connectMiraBoxSDSocket -> connectElgatoStreamDeckSocket via .apply(window, arguments).
    - When AJAZZ_HAVE_WEBENGINE is set, the injection-point case asserts DocumentCreation + MainWorld + matching sourceCode.
  </acceptance_criteria>
  <done>CompatTest::miraboxSocket_aliasedToElgato is green (pure string always; injection-point assertion when WebEngine is built).</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                     |
| ------------------------------------------ | --------------------------------------------------------------- |
| host UserScript -> plugin page (MainWorld) | The shim runs in the same JS world as the untrusted plugin page |

## STRIDE Threat Register

| Threat ID        | Category                      | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                              |
| ---------------- | ----------------------------- | ------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-18-SHIM-RACE   | Tampering / DoS               | alias injected after the plugin script runs -> ReferenceError | mitigate    | Inject at QWebEngineScript::DocumentCreation (not runJavaScript-after-load) so connectMiraBoxSDSocket exists before the plugin's own script executes (Pitfall 4)             |
| T-18-SHIM-WORLD  | Tampering                     | shim leaking into an isolated content world                   | accept      | MainWorld is required because the plugin's own connect call lives in MainWorld; the shim is a pure forwarder with no privileged capability of its own (A4)                   |
| T-18-HTML-NAV    | Information Disclosure / SSRF | the HTML plugin loading hostile remote resources              | mitigate    | Sub-resource navigation is constrained by the existing pi_url_policy allow/deny (reused by the 18-04 hosting path); this plan adds only the alias, no new navigation surface |
| T-18-SC          | Tampering                     | npm/pip/cargo installs                                        | n/a         | Zero external packages — Qt6 in-tree only                                                                                                                                    |
| \</threat_model> |                               |                                                               |             |                                                                                                                                                                              |

<verification>
- `ctest --preset linux-release -R "mirabox-compat"` — CompatTest cases green.
- `grep -c 'DocumentCreation' src/app/src/plugin_mirabox_shim.cpp` >= 1 (correct injection point).
- `grep -rn 'nlohmann' src/app/src/plugin_mirabox_shim.cpp` == 0 (COD-031); `grep -rn 'QCefView' src/app/src/plugin_mirabox_shim.*` == 0 (CLAUDE.md).
- Full suite green: `ctest --preset linux-release`.
</verification>

\<success_criteria>

- connectMiraBoxSDSocket is aliased to connectElgatoStreamDeckSocket (PLUGIN-11); existing AJAZZ packages load unmodified.
- The alias is injected at DocumentCreation so it exists before the plugin script runs; QWebEngineScript (never QCefView).
  \</success_criteria>

<output>
Create `.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-03-SUMMARY.md` when done.
</output>
</content>
