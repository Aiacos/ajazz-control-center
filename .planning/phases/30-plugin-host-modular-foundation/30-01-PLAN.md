---
phase: 30-plugin-host-modular-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_plugin_host2.cpp
  - tests/unit/CMakeLists.txt
  - .github/workflows/ci.yml
  - .planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md
autonomous: true
requirements: [HOST-01, HOST-02, HOST-03]

must_haves:
  truths:
    - A failing Catch2 test asserts disconnect-before-register causes zero crash and connectedPluginCount() == 0 (RED until Plan 02 lands)
    - A failing Catch2 test asserts a pre-registration process exit does NOT mark the plugin disabled (RED until Plan 02 lands)
    - CI fails the build if QHostAddress::Any appears in non-comment source or SIGPIPE SIG_IGN is missing from main.cpp
    - An ADR records the UNIFY decision for IPluginHost2 with rationale and the SKU-boundary rule
  artifacts:
    - path: tests/unit/test_plugin_host2.cpp
      provides: disconnect-before-register + pre-registration-exit Catch2 cases (RED scaffold)
      min_lines: 60
      contains: disconnect-before-register
    - path: .github/workflows/ci.yml
      provides: QHostAddress::Any + SIGPIPE grep gate (Linux step)
      contains: QHostAddress::Any
    - path: .planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md
      provides: IPluginHost2 UNIFY decision record
      min_lines: 40
      contains: UNIFY
  key_links:
    - from: tests/unit/CMakeLists.txt
      to: tests/unit/test_plugin_host2.cpp
      via: target_sources under AJAZZ_HAVE_WEBSOCKETS guard
      pattern: test_plugin_host2.cpp
---

<objective>
Lay the Wave-0 scaffolding for the plugin-host refactor: a RED Catch2 test file that pins the
HOST-02 pre-registration-safety contract, a permanent CI grep gate for the HOST-03 loopback-only
and SIGPIPE security invariants, and the IPluginHost2 UNIFY ADR that downstream phases will not
re-derive.

Purpose: tests-first (Nyquist) — the disconnect-before-register and pre-registration-exit
behaviours must have failing assertions before the implementation lands in Plan 02, so the GREEN
transition is the proof of correctness. The CI gate and ADR are pure additions.

Output: tests/unit/test_plugin_host2.cpp (RED), CI step in .github/workflows/ci.yml,
30-ADR-plugin-host-unification.md.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plugin-host-modular-foundation/30-CONTEXT.md
@.planning/phases/30-plugin-host-modular-foundation/30-RESEARCH.md
@.planning/phases/30-plugin-host-modular-foundation/30-VALIDATION.md

<interfaces>
From src/app/src/sd_plugin_server.hpp (SdPluginServer surface the test drives):
  int  connectedPluginCount() const noexcept;   // socket!=nullptr && uuid non-empty (and non-sentinel after Plan 02)
  signals: void pluginRegistered(QString const& pluginUuid);
  signals: void pluginDisconnected(QString const& pluginUuid);
  // Server listens loopback; test connects a real QWebSocket to ws://127.0.0.1:<port>.
  // test_sd_plugin_server.cpp is the canonical server + client-socket pattern.

From src/app/src/plugin_manager.hpp (PluginManager surface for the pre-reg-exit case):
explicit PluginManager(QString const& pluginsDir, SdPluginServer\* server, NodeProbe fakeProbe);
void onProcessFailed(QString const& uuid); // must NOT credit a crash pre-registration
bool isDisabled(QString const& uuid) const;
signals: void pluginDisabled(QString const& uuid, QString const& reason);
// m_live is keyed by .sdPlugin directory name. uuid absent from m_live == pre-registration exit.
// test_plugin_lifecycle.cpp shows the FakeProbe + clock-injection construction.

From .github/workflows/ci.yml (existing grep-gate to COPY — lines ~52-62):

- name: Enforce hid_open invariant (Pitfall 11)
  if: runner.os == 'Linux'
  run: |
  if grep -rn 'hid_open(' src/ | grep -v hid_transport.cpp | grep -v 'hid_open_path('; then
  echo "::error::..."; exit 1; fi
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: RED Catch2 scaffold for pre-registration safety</name>
  <files>tests/unit/test_plugin_host2.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - tests/unit/test_sd_plugin_server.cpp (server + loopback QWebSocket client; QSignalSpy on pluginRegistered/pluginDisconnected)
    - tests/unit/test_plugin_lifecycle.cpp (PluginManager(scratch.path(), server, fakeProbe), clock injection, onProcessFailed driving)
    - tests/unit/CMakeLists.txt lines 593-611 (AJAZZ_HAVE_WEBSOCKETS block that compiles plugin_manager.cpp for the test target)
    - src/app/src/sd_plugin_server.hpp (connectedPluginCount, pluginDisconnected/pluginRegistered signals)
    - src/app/src/plugin_manager.hpp (onProcessFailed, isDisabled, pluginDisabled signal)
  </read_first>
  <action>
    Create tests/unit/test_plugin_host2.cpp with ASCII-only TEST_CASE names. Two failing cases pin the Plan-02 contract:
    (1) TEST_CASE "disconnect-before-register leaves zero connected and does not crash": spin up an SdPluginServer
    on loopback (mirror test_sd_plugin_server.cpp), open a real QWebSocket client, wait for the connection, then
    close it WITHOUT sending registerPlugin; pump the event loop; assert server.connectedPluginCount() == 0 and
    that no pluginDisconnected fired for a real (non-sentinel) UUID (QSignalSpy count == 0). Add an assertion that
    pluginDisconnected was NOT emitted with a "__pending__"-prefixed UUID — the GREEN target of Plan 02; comment
    "// GREEN after 30-02".
    (2) TEST_CASE "pre-registration process exit is not counted as a crash": construct PluginManager with a
    FakeProbe + injected clock (test_plugin_lifecycle.cpp pattern); call onProcessFailed("uuid-never-registered")
    three times inside the 30s window for a uuid NOT in m_live; assert manager.isDisabled("uuid-never-registered")
    is FALSE and the pluginDisabled QSignalSpy count is 0. MUST be RED today (current onProcessFailed credits the
    crash unconditionally); comment "// RED until 30-02 wires the m_live.find guard".
    Register the file in tests/unit/CMakeLists.txt INSIDE the existing AJAZZ_HAVE_WEBSOCKETS block (~line 597 that
    already compiles plugin_manager.cpp) via target_sources(ajazz_unit_tests PRIVATE test_plugin_host2.cpp). Do NOT
    add another copy of plugin_manager.cpp/sd_plugin_server to the link line. Implement no sentinel logic in
    production code here — RED scaffold only.
  </action>
  <verify>
    <automated>cmake --preset linux-release >/dev/null 2>&1 && cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_plugin_host2.cpp compiles and links into ajazz_unit_tests.
    - `ctest --preset linux-release -R "pre.registration.exit"` discovers the case and it reports FAIL (RED).
    - `grep -c "TEST_CASE" tests/unit/test_plugin_host2.cpp` returns >= 2.
    - `grep -P '[^\x00-\x7F]' tests/unit/test_plugin_host2.cpp` returns nothing (ASCII-only case names).
  </acceptance_criteria>
  <done>Test compiles, registers in CMake, pre-registration-exit case is RED pending Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: CI grep gate for QHostAddress::Any + SIGPIPE invariants</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines ~48-63 (the "Enforce hid_open invariant" Linux-only step — copy its shape)
    - src/app/src/sd_plugin_server.cpp (confirm QHostAddress::LocalHost bind; QHostAddress::Any only in comments)
    - src/app/src/main.cpp (confirm ::signal(SIGPIPE, SIG_IGN) near line 55)
  </read_first>
  <action>
    Add a new Linux-only CI step "Enforce loopback-only + SIGPIPE invariants (Phase 30)" immediately after the
    hid_open invariant step, BEFORE the Qt install/build steps (fail-fast). It must:
    (a) fail if grep -rn 'QHostAddress::Any' src/ returns any hit that is NOT a comment line — filter out lines
    whose first non-whitespace content is // or * (the two existing comment hits in sd_plugin_server.cpp/.hpp must
    NOT trip it); emit "::error::QHostAddress::Any found in non-comment source — loopback-only violated." and exit 1.
    (b) fail if grep -n 'SIGPIPE' src/app/src/main.cpp does not contain a SIG_IGN match; emit
    "::error::SIGPIPE SIG_IGN handler missing from main.cpp." and exit 1.
    Gate with if: runner.os == 'Linux'. Do NOT add any SKU-string grep gate (akp05e|akp03|akp153) — that is
    CODE-REVIEW-ONLY per the locked decision; adding it is a deferred-idea violation.
  </action>
  <verify>
    <automated>bash -c 'h=$(grep -rn "QHostAddress::Any" src/ | grep -vP ":[0-9]+: *(//|[*])"); [ -n "$h" ] && echo NONCOMMENT_HIT || echo CLEAN' && grep -n SIGPIPE src/app/src/main.cpp | grep -q SIG_IGN && echo SIGPIPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The new step exists in ci.yml with `if: runner.os == 'Linux'` and a name containing "loopback-only + SIGPIPE".
    - Local QHostAddress::Any non-comment grep prints CLEAN (existing hits are comments).
    - Local SIGPIPE SIG_IGN found in main.cpp (SIGPIPE_OK printed).
    - `grep -n 'akp05e' .github/workflows/ci.yml` returns nothing (no SKU gate added).
  </acceptance_criteria>
  <done>CI step added (Linux-only, fail-fast); local invariants CLEAN + SIGPIPE_OK; no SKU gate.</done>
</task>

<task type="auto">
  <name>Task 3: IPluginHost2 unification ADR</name>
  <files>.planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md</files>
  <read_first>
    - .planning/phases/30-plugin-host-modular-foundation/30-CONTEXT.md (locked UNIFY override + SKU code-review-only)
    - .planning/phases/30-plugin-host-modular-foundation/30-RESEARCH.md "Pattern 4: IPluginHost2 ADR Structure" + "State of the Art"
    - src/plugins/include/ajazz/plugins/i_plugin_host.hpp (existing Python-only IPluginHost — name-confusion pitfall 6)
    - CLAUDE.md (COD-031 boundary)
  </read_first>
  <action>
    Write 30-ADR-plugin-host-unification.md as a standard ADR with these sections in order:
    (1) Status: Accepted (2026-06-06). (2) Context: two separate host implementations coexist — PluginManager
    (.sdPlugin Node/HTML/native over WebSocket) and OutOfProcessPluginHost (Python over line-JSON stdin/stdout);
    they share the name IPluginHost but PluginManager does NOT implement it. (3) Decision: UNIFY — introduce
    IPluginHost2 (app-layer, src/app/src/, Qt types) as the single spawn/lifecycle/IPC contract; PluginManager is
    refactored onto it; the Python path is wrapped. State explicitly this OVERRIDES the prior v2.0 research verdict
    "keep separate". (4) Rationale: one lifecycle, one crash-window, one shutdown path, simplifies Phase 31+ work;
    cite the user override. (5) Consequences: regression risk on each of the four runtimes — mitigation is a
    per-runtime no-regression check, not just the disconnect test. (6) SKU boundary rule: PluginManager dispatch
    carries zero SKU strings (akp05e|akp03|akp153); device I/O reaches hardware ONLY via PluginDeviceBridge →
    StreamDockControlService → mirajazz sidecar; enforcement is CODE-REVIEW-ONLY (no CI grep gate) per the locked
    decision; note the separate QHostAddress::Any + SIGPIPE CI gate DOES apply. (7) Rejected alternative: "keep
    separate" (the research verdict) — rejected because it leaves two lifecycle systems to maintain. (8) COD-031
    note: IPluginHost2 lives in src/app/src/ and may use QJson; it must NOT pull nlohmann::json into ajazz_core or
    any installed header.
    Do NOT edit CLAUDE.md here (PR-gated project memory, out of scope); the ADR is the authoritative record.
  </action>
  <verify>
    <automated>f=.planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md; test -f "$f" && grep -q UNIFY "$f" && grep -qi code-review "$f" && echo ADR_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 8 sections (Status, Context, Decision, Rationale, Consequences, SKU boundary, Rejected alternative, COD-031).
    - Contains "UNIFY" documented as an override of the keep-separate verdict.
    - States SKU enforcement is CODE-REVIEW-ONLY and the QHostAddress::Any + SIGPIPE CI gate still applies.
    - File is >= 40 lines.
  </acceptance_criteria>
  <done>ADR records the UNIFY decision, regression-budget consequence, SKU code-review-only boundary, and COD-031 note.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                           |
| ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| plugin process -> SdPluginServer (loopback WS) | Untrusted plugin input crosses here; loopback-only bind is the access-control control |
| host process \<- OS signals (SIGPIPE)          | A broken plugin IPC pipe must not kill the host                                       |

## STRIDE Threat Register

| Threat ID        | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                     |
| ---------------- | ---------------------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-30-bind        | Information Disclosure | SdPluginServer listen address      | mitigate    | CI grep gate fails build if QHostAddress::Any appears in non-comment source (Task 2); bind stays QHostAddress::LocalHost (existing) |
| T-30-sigpipe     | Denial of Service      | host process on broken plugin pipe | mitigate    | CI grep gate asserts SIGPIPE SIG_IGN present in main.cpp (Task 2); handler is existing                                              |
| T-30-pre-reg     | Spoofing / DoS         | pre-registration crash accounting  | mitigate    | RED tests authored here (Task 1) pin the contract; sentinel-UUID guard lands in Plan 02                                             |
| T-30-SC          | Tampering              | npm/pip/cargo installs             | accept      | No package installs this phase (internal refactor + tests + CI + docs); RESEARCH Package Legitimacy Audit = not applicable          |
| \</threat_model> |                        |                                    |             |                                                                                                                                     |

<verification>
- cmake --build --preset linux-release --target ajazz_unit_tests succeeds with the new test linked.
- ctest --preset linux-release -R "pre.registration.exit" is RED (expected pre-Plan-02).
- Local invariants: QHostAddress::Any non-comment grep CLEAN; SIGPIPE SIG_IGN present.
- ADR file exists with the UNIFY decision and code-review-only SKU note.
</verification>

\<success_criteria>

- RED Catch2 scaffold compiles and pre-registration-exit fails (proves the guard is absent).
- CI gate added for QHostAddress::Any + SIGPIPE, Linux-only, fail-fast, no SKU gate.
- ADR committed with all required sections and the UNIFY override.
  \</success_criteria>

<output>
Create .planning/phases/30-plugin-host-modular-foundation/30-01-SUMMARY.md when done.
</output>
