---
phase: 30-plugin-host-modular-foundation
plan: 02
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - src/app/src/sd_plugin_server.cpp
  - src/app/src/sd_plugin_server.hpp
  - src/app/src/plugin_manager.cpp
autonomous: true
requirements: [HOST-02]

must_haves:
  truths:
    - A WebSocket that connects then disconnects before registerPlugin causes zero crash and connectedPluginCount() stays 0
    - A pre-registration process exit does NOT increment the crash counter and never disables the plugin
    - registerPlugin rekeys the sentinel slot to the real UUID, guarded against the socket-dying-during-rekey race
    - The existing 3-in-30s disable+notify, restart, and exitApp behaviours remain green
  artifacts:
    - path: src/app/src/sd_plugin_server.cpp
      provides: sentinel-UUID insertion at connect, rekey at registerPlugin, sentinel-aware disconnect
      contains: __pending__
    - path: src/app/src/plugin_manager.cpp
      provides: pre-registration-exit guard in onProcessFailed (m_live.find authority)
      contains: m_live.find
  key_links:
    - from: src/app/src/sd_plugin_server.cpp onNewConnection
      to: registerPlugin rekey
      via: find-by-socket-pointer + null-check before write
      pattern: c.socket == client
    - from: src/app/src/plugin_manager.cpp onProcessFailed
      to: m_live
      via: skip crash credit when uuid absent from m_live
      pattern: m_live.find
---

<objective>
Implement HOST-02 pre-registration crash isolation: insert every freshly connected WebSocket into
SdPluginServer::m_connections under a synthetic sentinel UUID, rekey to the real UUID on
registerPlugin (race-guarded), keep sentinel connections out of pluginDisconnected and the crash
window, and guard PluginManager::onProcessFailed so a process that exits before registering does not
consume a crash credit. Turn the Plan-01 RED tests GREEN.

Purpose: a plugin that crashes mid-handshake (before its contexts register) must never crash the app,
never leave a dangling pointer, and never count toward the 3-in-30s disable window. This is the
SINGLE MAP + SENTINEL UUID locked decision.

Output: modified sd_plugin_server.{cpp,hpp} + plugin_manager.cpp; Plan-01 tests GREEN; existing suite stays green.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plugin-host-modular-foundation/30-CONTEXT.md
@.planning/phases/30-plugin-host-modular-foundation/30-RESEARCH.md
@.planning/phases/30-plugin-host-modular-foundation/30-VALIDATION.md
@tests/unit/test_plugin_host2.cpp

<interfaces>
From src/app/src/sd_plugin_server.cpp (the slots to modify):
  void SdPluginServer::onNewConnection();        // ~line 157: pushes {QString{}, client, "", 0, false} today
  void SdPluginServer::onClientDisconnected();   // ~line 178: erases by socket ptr, emits pluginDisconnected if uuid non-empty
  // registerPlugin/registerPropertyInspector branch ~line 223: finds slot by socket ptr, CR-03 impersonation
  //   guard, WR-02 re-registration cleanup, then it->uuid = uuid and emits pluginRegistered.
  int  SdPluginServer::connectedPluginCount();   // ~line 150: counts socket!=nullptr && uuid non-empty
  QString SdPluginServer::uuidForClient(QWebSocket*) const; // ~line 462: returns it->uuid by socket ptr
  // PluginConnection: { QString uuid; QWebSocket* socket{nullptr}; QString salt; int authAttempts{0}; bool authenticated{false}; }

From src/app/src/plugin_manager.cpp (the crash-window authority):
void PluginManager::onProcessFailed(QString const& uuid); // ~line 623: recordCrash + shouldDisable, defers handleProcessFailure
void PluginManager::handleProcessFailure(QString const& uuid);// ~line 646: m_live.find guard already present for teardown
// m_live is std::map\<QString, LivePlugin> keyed by .sdPlugin directory name; uuid absent == pre-registration exit.
// CR-02 (lines ~419-436): FailedToStart routed only through finished, not errorOccurred — must stay intact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sentinel-UUID insertion, race-guarded rekey, sentinel-aware disconnect</name>
  <files>src/app/src/sd_plugin_server.cpp, src/app/src/sd_plugin_server.hpp</files>
  <behavior>
    - onNewConnection inserts a unique sentinel UUID ("__pending__" + QUuid::createUuid().toString(WithoutBraces)) instead of an empty UUID.
    - connectedPluginCount() excludes sentinel-prefixed UUIDs (still counts only fully-registered plugins).
    - registerPlugin finds the slot by socket pointer, null-checks it->socket before writing, then overwrites the sentinel with the real UUID.
    - A disconnect while the UUID is still a sentinel emits NO pluginDisconnected and logs a pre-registration disconnect.
    - The CR-03 impersonation guard and WR-02 re-registration cleanup still fire only for real (non-sentinel) prior UUIDs.
  </behavior>
  <read_first>
    - src/app/src/sd_plugin_server.cpp lines 150-200 (connectedPluginCount, onNewConnection, onClientDisconnected)
    - src/app/src/sd_plugin_server.cpp lines 223-312 (registerPlugin rekey region, CR-03, WR-02)
    - src/app/src/sd_plugin_server.cpp lines 462-490 (uuidForClient, socketForUuid — never cache the socket pointer)
    - src/app/src/sd_plugin_server.hpp lines 222-230 (PluginConnection struct + m_connections)
    - 30-RESEARCH.md "Pattern 1: Sentinel-UUID Pre-Registration Tracking" (the exact sketch)
    - tests/unit/test_plugin_host2.cpp (the GREEN target assertions)
  </read_first>
  <action>
    In onNewConnection, replace the empty-UUID push with a sentinel: sentinelUuid =
    QStringLiteral("__pending__") + QUuid::createUuid().toString(QUuid::WithoutBraces); push
    {sentinelUuid, client, QString{}, 0, false}. Add a private helper isSentinelUuid(QString const&)
    returning uuid.startsWith(QStringLiteral("__pending__")).
    In connectedPluginCount(), change the predicate to socket != nullptr and uuid non-empty and NOT
    isSentinelUuid(uuid).
    In the registerPlugin/registerPropertyInspector branch: the slot is already located by socket pointer
    (c.socket == client). Before overwriting it->uuid, add the race guard: if it->socket == nullptr, return
    (slot mid-teardown). The WR-02 "old uuid replaced" pluginDisconnected emit must be guarded with
    NOT isSentinelUuid(it->uuid) so the sentinel-to-real rekey does NOT emit a spurious pluginDisconnected;
    the CR-03 impersonation check is unaffected (it matches by real uuid equality). Then set it->uuid = uuid
    (rekey) and proceed unchanged.
    In onClientDisconnected: capture uuid via uuidForClient as today; after erasing the slot, emit
    pluginDisconnected ONLY when uuid non-empty and NOT isSentinelUuid(uuid). For sentinel disconnects log
    "pre-registration client disconnected (sentinel)" and emit nothing.
    Do NOT cache the QWebSocket* across the connect-to-registerPlugin gap (Pitfall 4). Keep re-resolving by
    socket pointer. Use QJson/QString only, no nlohmann (COD-031).
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/dev/null 2>&1 && ctest --preset linux-release -R "disconnect.before.register" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "__pending__" src/app/src/sd_plugin_server.cpp` returns >= 2 (insertion + sentinel guard).
    - `ctest --preset linux-release -R "disconnect.before.register"` passes (GREEN); connectedPluginCount stays 0 and no real-UUID pluginDisconnected fires.
    - `ctest --preset linux-release -R SdPluginServer` stays fully green (no regression in CR-03/WR-02 cases).
    - No QWebSocket pointer cached across messages: `grep -n "QWebSocket\* *cached\|m_cachedSocket" src/app/src/sd_plugin_server.cpp` returns nothing.
  </acceptance_criteria>
  <done>Sentinel inserted on connect, rekeyed race-guarded on register, sentinel disconnects silent; disconnect-before-register test GREEN; SdPluginServer suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-registration-exit guard in PluginManager crash window</name>
  <files>src/app/src/plugin_manager.cpp</files>
  <behavior>
    - onProcessFailed for a uuid absent from m_live (process exited before registerPlugin) does NOT call recordCrash and does NOT disable.
    - onProcessFailed for a live (registered) uuid behaves exactly as before: recordCrash + shouldDisable + deferred teardown.
    - The CR-02 double-fire guard (FailedToStart routed only via finished) is preserved.
    - 3-in-30s disable+notify, restart, exitApp shutdown remain green.
  </behavior>
  <read_first>
    - src/app/src/plugin_manager.cpp lines 623-665 (onProcessFailed + handleProcessFailure, the deferred-teardown pattern + existing m_live.find guard)
    - src/app/src/plugin_manager.cpp lines 419-436 (CR-02 FailedToStart routing — do not touch the connection wiring)
    - src/app/src/plugin_manager.hpp lines 361-377 (LivePlugin + m_live keyed by .sdPlugin dir name)
    - tests/unit/test_plugin_lifecycle.cpp (the 3-in-30s, exitApp, FailedToStart cases that must stay green)
    - tests/unit/test_plugin_host2.cpp (the pre-registration-exit RED case to turn GREEN)
  </read_first>
  <action>
    At the very top of onProcessFailed(uuid), add the pre-registration guard: if m_live.find(uuid) ==
    m_live.end(), this is a process that exited before its socket registered (or was already torn down).
    Log "pre-registration exit for {uuid} — not counted toward crash window" and return BEFORE
    m_crashTracker.recordCrash — so no crash credit, no shouldDisable, no respawn. Only when the uuid IS in
    m_live do the existing recordCrash + shouldDisable + deferred handleProcessFailure path run unchanged.
    Do NOT alter the QProcess errorOccurred/finished connections (CR-02 stays intact). Do NOT touch
    handleProcessFailure (its own m_live.find guard already short-circuits an already-erased entry).
    Keep using m_clock() for timing; no nlohmann (COD-031).
    Note: because m_live is the authority, a registered plugin that genuinely crashes still counts — the
    guard only skips the case where the process died before registration, exactly matching the WS-side
    sentinel.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/dev/null 2>&1 && ctest --preset linux-release -R "pre.registration.exit|crash 3 in 30s|shutdown sends exitApp" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "m_live.find" src/app/src/plugin_manager.cpp` shows a guard inside onProcessFailed (in addition to the existing handleProcessFailure one).
    - `ctest --preset linux-release -R "pre.registration.exit"` passes (GREEN): isDisabled false, pluginDisabled count 0.
    - `ctest --preset linux-release -R "crash 3 in 30s"` passes (registered-plugin crash window unchanged).
    - `ctest --preset linux-release -R "shutdown sends exitApp"` passes.
    - `ctest --preset linux-release -R PluginManagerTest` and `-R PluginConcurrency` stay green (CR-02 / WR-02 intact).
  </acceptance_criteria>
  <done>Pre-registration exits skip the crash window; registered-plugin lifecycle (3-in-30s, restart, exitApp) unchanged and green.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                   |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| plugin process -> SdPluginServer (loopback WS)     | Untrusted plugin connects, may die before registering; sentinel tracks it without exposing it |
| QProcess exit signal -> PluginManager crash window | A pre-registration exit must not be treated as a runtime crash                                |

## STRIDE Threat Register

| Threat ID          | Category          | Component                          | Disposition | Mitigation Plan                                                                                                                            |
| ------------------ | ----------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-30-pre-reg       | Denial of Service | crash-window accounting            | mitigate    | onProcessFailed skips recordCrash when uuid absent from m_live (Task 2); sentinel keeps pre-reg sockets out of pluginDisconnected (Task 1) |
| T-30-rekey-race    | Tampering / UAF   | registerPlugin rekey vs disconnect | mitigate    | find slot by socket pointer + null-check it->socket before write; onClientDisconnected erases by socket pointer (Task 1)                   |
| T-30-impersonation | Spoofing          | registerPlugin UUID collision      | mitigate    | existing CR-03 impersonation guard preserved; sentinel rekey does not weaken it (Task 1)                                                   |
| T-30-SC            | Tampering         | npm/pip/cargo installs             | accept      | No package installs this phase (internal refactor); RESEARCH Package Legitimacy Audit = not applicable                                     |
| \</threat_model>   |                   |                                    |             |                                                                                                                                            |

<verification>
- ctest --preset linux-release -R "disconnect.before.register|pre.registration.exit" both GREEN.
- ctest --preset linux-release -R "crash 3 in 30s|shutdown sends exitApp" GREEN (lifecycle preserved).
- ctest --preset linux-release -R "SdPluginServer|PluginManagerTest|PluginConcurrency" GREEN (no regression).
- Full suite: ctest --preset linux-release stays green vs the 694-test baseline.
- Live (debug channel, before declaring done): launch AJAZZ_DEBUG_CONTROL=1, connect a socket that dies before registerPlugin, run scripts/ajazz-debug plugin.list, confirm connectedCount: 0 and the app is still responsive.
</verification>

\<success_criteria>

- Disconnect-before-register and pre-registration-exit tests are GREEN.
- 3-in-30s disable+notify, restart, exitApp behaviours unchanged and green.
- Rekey is race-guarded; no dangling pointer; SdPluginServer suite green.
- scripts/ajazz-debug plugin.list returns connectedCount 0 (not a crash) in the live disconnect scenario.
  \</success_criteria>

<output>
Create .planning/phases/30-plugin-host-modular-foundation/30-02-SUMMARY.md when done.
</output>
