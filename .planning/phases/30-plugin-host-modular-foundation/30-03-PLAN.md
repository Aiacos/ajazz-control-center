---
phase: 30-plugin-host-modular-foundation
plan: 03
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - src/app/src/i_plugin_host2.hpp
  - src/app/src/plugin_manager.hpp
  - src/app/src/plugin_manager.cpp
  - src/app/src/application.hpp
  - src/app/src/application.cpp
  - tests/unit/test_plugin_host2.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [HOST-01, HOST-03]

must_haves:
  truths:
    - A single IPluginHost2 contract unifies the Node, HTML, native, and Python runtimes behind one spawn/lifecycle/IPC surface
    - PluginManager dispatch routes all four runtimes with zero SKU-specific branches
    - Each of the four runtimes has an explicit no-regression check (not just the disconnect test)
    - Device I/O from plugins reaches hardware only via PluginDeviceBridge -> StreamDockControlService -> sidecar
  artifacts:
    - path: src/app/src/i_plugin_host2.hpp
      provides: the unified spawn/lifecycle/IPC contract (Qt types, app-layer)
      min_lines: 30
      contains: class IPluginHost2
    - path: src/app/src/plugin_manager.hpp
      provides: PluginManager declared as IPluginHost2 implementer
      contains: IPluginHost2
  key_links:
    - from: src/app/src/application.cpp
      to: IPluginHost2
      via: Application owns/exposes the unified host surface
      pattern: IPluginHost2
    - from: src/app/src/plugin_manager.cpp
      to: PluginDeviceBridge
      via: device I/O only through the bridge, zero SKU strings in dispatch
      pattern: PluginDeviceBridge
---

<objective>
Introduce IPluginHost2 — the single app-layer spawn/lifecycle/IPC contract — and refactor
PluginManager (Node/HTML/native over WebSocket) plus OutOfProcessPluginHost (Python) onto it so all
four runtimes present one unified surface to Application. Add the four explicit per-runtime
no-regression checks the locked decision requires, and verify (code-review audit) that the plugin
dispatch layer has zero SKU strings and reaches devices only through PluginDeviceBridge.

Purpose: HOST-01 (FULL UNIFICATION NOW, per the locked override of the keep-separate research verdict)
and HOST-03 (SKU/wire decoupling, code-review-only). The ADR from Plan 01 documents the decision; this
plan implements it.

Output: new i_plugin_host2.hpp; PluginManager implements IPluginHost2; Application wires the unified
host; four per-runtime regression tests; SKU audit clean.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plugin-host-modular-foundation/30-CONTEXT.md
@.planning/phases/30-plugin-host-modular-foundation/30-RESEARCH.md
@.planning/phases/30-plugin-host-modular-foundation/30-VALIDATION.md
@.planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md

<interfaces>
From src/app/src/plugin_manager.hpp (surface to subsume under IPluginHost2):
  std::vector<PluginManifest> discover();
  void spawn(PluginManifest const& manifest);     // .js -> QProcess(node), .html -> QWebEnginePage, else -> QProcess(native)
  void rediscover();
  void onProcessFailed(QString const& uuid);
  void shutdown();                                 // sendEvent(uuid,"exitApp") then terminate then kill
  void setPluginEnabled(QString const& pluginId, bool enabled);
  bool isDisabled(QString const& uuid) const;
  signals: void pluginDisabled(QString const& uuid, QString const& reason);

From src/plugins/include/ajazz/plugins/i_plugin_host.hpp (the EXISTING Python-only interface — do NOT confuse):
virtual void addSearchPath(std::filesystem::path const&) = 0;
virtual std::size_t loadAll() = 0;
virtual std::vector<PluginInfo> plugins() = 0;
virtual bool dispatch(std::string_view pluginId, std::string_view actionId, std::string_view settingsJson) = 0;
// Implemented ONLY by OutOfProcessPluginHost. PluginManager does NOT implement this.

From src/app/src/application.hpp (the two host members today):
std::unique_ptr<SdPluginServer> m_pluginServer;
std::unique_ptr<PluginManager> m_pluginManager; // .sdPlugin
std::unique_ptr<plugins::IPluginHost> m_pluginHost; // Python OOP
SdPluginServer\* pluginServer() const noexcept;
PluginManager\* pluginManager() const noexcept;
void initPluginHost();

From src/app/src/plugin_device_bridge.hpp (HOST-03 boundary owner — device I/O ONLY here):
// PluginDeviceBridge -> StreamDockControlService -> mirajazz sidecar. plugin_device_bridge.cpp lines
// 1242/1251 hold "akp05e" fallback strings — CORRECT in the bridge tier; do NOT move them into plugin_manager.cpp.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IPluginHost2 contract and make PluginManager implement it</name>
  <files>src/app/src/i_plugin_host2.hpp, src/app/src/plugin_manager.hpp, src/app/src/plugin_manager.cpp</files>
  <read_first>
    - src/app/src/plugin_manager.hpp (full public surface: discover, spawn, rediscover, shutdown, onProcessFailed, setPluginEnabled, isDisabled, pluginDisabled)
    - src/plugins/include/ajazz/plugins/i_plugin_host.hpp (existing Python interface — IPluginHost2 must NOT be this; name-confusion Pitfall 6)
    - src/plugins/include/ajazz/plugins/out_of_process_plugin_host.hpp (the Python host whose surface IPluginHost2 must accommodate)
    - 30-RESEARCH.md "Pattern 2: IPluginHost2 Unified Contract" + Open Questions 1-2 (Qt-types recommendation, discover+spawn vs loadAll)
    - 30-ADR-plugin-host-unification.md (the locked UNIFY decision + COD-031 note)
  </read_first>
  <action>
    Create src/app/src/i_plugin_host2.hpp declaring an abstract class IPluginHost2 (app-layer, Qt types only —
    QString/QJsonObject; NO nlohmann, COD-031). Minimal surface (names are Claude's discretion): virtual dtor;
    spawn(PluginManifest const&); shutdown(); connectedPluginCount() const noexcept; plugins() returning the
    merged live inventory; dispatch(QString pluginUuid, QString actionId, QJsonObject payload); pluginServer()
    const noexcept returning SdPluginServer*. Add a doc comment stating this SUPERSEDES the per-runtime split and
    is NOT the Python-only plugins::IPluginHost.
    Make PluginManager implement IPluginHost2: add IPluginHost2 as a base class in plugin_manager.hpp (alongside
    QObject) and provide overrides by delegating to existing methods (spawn/shutdown exist; connectedPluginCount()
    forwards to the SdPluginServer; dispatch() routes via the existing WS action path; pluginServer() returns the
    injected server). The Python path stays owned by OutOfProcessPluginHost and is folded into plugins() at the
    Application layer (Task 2) — do NOT make OutOfProcessPluginHost inherit IPluginHost2 (it lives in src/plugins/
    and cannot take Qt types; COD-031 layer boundary).
    Keep the four runtime branches inside spawn() (.js/.html/native) and the Python dispatch wrapper, but ensure
    NONE of them branch on a device SKU string. Do not regress the CR-02/WR-02/sentinel paths from Plan 02.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -8 ; grep -c "class IPluginHost2" src/app/src/i_plugin_host2.hpp</automated>
  </verify>
  <acceptance_criteria>
    - src/app/src/i_plugin_host2.hpp exists and declares `class IPluginHost2` with a virtual destructor and >= 4 pure-virtual methods.
    - plugin_manager.hpp shows PluginManager deriving from IPluginHost2: `grep -n "IPluginHost2" src/app/src/plugin_manager.hpp` returns a base-class hit.
    - The app target builds clean (no link error from the new base class).
    - `grep -rn "nlohmann" src/app/src/i_plugin_host2.hpp` returns nothing (COD-031); `grep -rn "nlohmann" src/core/include/` still returns 0.
    - `grep -rn "akp05e\|akp03\|akp153" src/app/src/plugin_manager.cpp` returns 0 (HOST-03 dispatch clean).
  </acceptance_criteria>
  <done>IPluginHost2 contract exists; PluginManager implements it; app builds; dispatch layer SKU-free; COD-031 intact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Application onto the unified host + fold Python into plugins()</name>
  <files>src/app/src/application.hpp, src/app/src/application.cpp</files>
  <read_first>
    - src/app/src/application.hpp lines 140-170, 270-330 (m_pluginServer, m_pluginManager, m_pluginHost members; pluginServer()/pluginManager() accessors; initPluginHost())
    - src/app/src/application.cpp (initPluginHost() body — how OutOfProcessPluginHost is constructed, search paths, loadAll)
    - src/app/src/plugin_manager.cpp shutdown() (the exitApp-then-terminate path that must still run for all live plugins)
    - 30-RESEARCH.md "Open Questions" (plugins() merge of .sdPlugin + Python inventory; Qt-types at the Python wrapper boundary)
  </read_first>
  <action>
    Expose the unified surface from Application: add an accessor pluginHost2() returning IPluginHost2* backed by
    m_pluginManager (which now implements it). Keep m_pluginManager and m_pluginHost (Python) as the concrete
    owners — full unification is at the CONTRACT level (one IPluginHost2 surface), not a forced merge of the two
    concrete objects. In plugins(), merge the .sdPlugin live inventory with the Python host's plugins() so callers
    (LoadedPluginsModel) see one list; convert the Python PluginInfo (STL types) to Qt-typed entries at this
    boundary (the wrapper does the std::string -> QString conversion, per the research recommendation). Route
    dispatch() so a Python pluginUuid goes to m_pluginHost->dispatch() (converting QJsonObject payload ->
    settingsJson std::string at the boundary) and a .sdPlugin uuid goes through the WS action path. Preserve
    shutdown ordering: exitApp to all live .sdPlugin plugins, then the Python host teardown.
    Do NOT introduce any device-SKU coupling here; plugin device I/O continues to flow through PluginDeviceBridge only.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -6 ; ctest --preset linux-release -R "OutOfProcess" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "IPluginHost2" src/app/src/application.hpp` shows the pluginHost2() accessor or member.
    - The app target builds clean and links.
    - `ctest --preset linux-release -R OutOfProcess` stays green (Python dispatch through the wrapped path unchanged).
    - Conversion is at the app/wrapper boundary; `grep -rn "nlohmann" src/core/include/` still returns 0 (COD-031).
  </acceptance_criteria>
  <done>Application exposes one IPluginHost2 surface; plugins() merges .sdPlugin + Python; dispatch + shutdown ordering preserved; Python tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Four-runtime no-regression checks + SKU audit</name>
  <files>tests/unit/test_plugin_host2.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - tests/unit/test_plugin_lifecycle.cpp (existing Node/HTML/native spawn-dispatch cases + FakeProbe to extend)
    - tests/unit/test_out_of_process_plugin_host.cpp (Python dispatch case pattern)
    - tests/unit/test_plugin_host2.cpp (the Plan-01/02 file to extend; reuse its fixtures)
    - src/app/src/plugin_manager.cpp spawn() (.js/.html/native dispatch — confirm no SKU branch)
    - 30-VALIDATION.md Per-Task Verification Map (the four HOST-01 runtime rows + their ctest commands)
  </read_first>
  <action>
    Add four explicit per-runtime no-regression TEST_CASEs to test_plugin_host2.cpp (ASCII-only names), each
    asserting the runtime still spawns/dispatches through the IPluginHost2 surface without a SKU branch:
    (1) "Node runtime spawns and registers via unified host" — FakeProbe + a .js manifest; assert spawn routes to
    the node QProcess path (reuse lastNodeArgvForTesting) and connectedPluginCount reflects registration.
    (2) "HTML runtime loads via QWebEnginePage via unified host" — guard with AJAZZ_HAVE_WEBENGINE; assert the HTML
    branch is taken and the WR-02 no-respawn guard (process==nullptr) still holds.
    (3) "Native runtime spawns via unified host" — a non-.js/.html CodePath manifest; assert the native QProcess branch is taken.
    (4) "Python runtime dispatches via OOP host through unified surface" — drive dispatch() for a Python uuid and
    assert it reaches the OutOfProcessPluginHost path (use the existing OOP fixture / a fake host).
    Where a case is better expressed against an existing suite, instead ADD assertions to test_plugin_lifecycle.cpp /
    test_out_of_process_plugin_host.cpp and reference them from the VALIDATION map — but each of the four behaviours
    MUST have a named automated assertion. Then perform the HOST-03 code-review audit inline: run the SKU grep and
    record the result (0 hits) in the SUMMARY (no CI gate — code-review-only per the locked decision). Register any
    new source in tests/unit/CMakeLists.txt under the AJAZZ_HAVE_WEBSOCKETS block.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/dev/null 2>&1 ; ctest --preset linux-release -R "PluginManagerTest|OutOfProcess|unified host" 2>&1 | tail -10 ; echo "SKU hits:" ; grep -rn "akp05e\|akp03\|akp153" src/app/src/plugin_manager.cpp | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - test_plugin_host2.cpp (or the referenced suites) contains a named automated assertion for each of the four runtimes (Node, HTML, native, Python).
    - `ctest --preset linux-release -R "PluginManagerTest|OutOfProcess"` is fully green (no runtime regressed).
    - `grep -rn "akp05e\|akp03\|akp153" src/app/src/plugin_manager.cpp | wc -l` returns 0 (HOST-03 audit clean).
    - Full suite `ctest --preset linux-release` is green vs the 694-test baseline.
  </acceptance_criteria>
  <done>Four per-runtime no-regression checks pass; SKU audit returns 0 in plugin_manager.cpp; full suite green.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                                 | Description                                                                                           |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| plugin runtime (Node/HTML/native/Python) -> IPluginHost2 | One contract mediates all four untrusted runtimes; spawn/dispatch must not branch on device internals |
| plugin -> PluginDeviceBridge -> sidecar                  | The only path plugin device I/O may take; SKU/wire internals stay below the bridge                    |

## STRIDE Threat Register

| Threat ID        | Category                          | Component                           | Disposition            | Mitigation Plan                                                                                                           |
| ---------------- | --------------------------------- | ----------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-30-sku         | Information Disclosure / coupling | PluginManager dispatch              | mitigate (code-review) | SKU grep audit returns 0 in plugin_manager.cpp (Task 3); ADR documents the boundary; per locked decision NO CI gate added |
| T-30-cod031      | Tampering                         | layer boundary (nlohmann into core) | mitigate               | IPluginHost2 uses Qt types only; grep asserts 0 nlohmann in src/core/include/ and in i_plugin_host2.hpp (Tasks 1-2)       |
| T-30-regress     | Denial of Service                 | four-runtime spawn/dispatch         | mitigate               | explicit per-runtime no-regression tests (Task 3); CR-02/WR-02/sentinel paths preserved                                   |
| T-30-SC          | Tampering                         | npm/pip/cargo installs              | accept                 | No package installs this phase (internal refactor); RESEARCH Package Legitimacy Audit = not applicable                    |
| \</threat_model> |                                   |                                     |                        |                                                                                                                           |

<verification>
- App target builds clean with IPluginHost2 as PluginManager's base and Application exposing pluginHost2().
- ctest --preset linux-release -R "PluginManagerTest|OutOfProcess" green (all four runtimes no-regression).
- SKU audit: grep -rn "akp05e|akp03|akp153" src/app/src/plugin_manager.cpp returns 0.
- COD-031: grep -rn "nlohmann" src/core/include/ returns 0; i_plugin_host2.hpp has no nlohmann.
- Full suite ctest --preset linux-release green vs the 694-test baseline.
- Live (debug channel, before declaring done): scripts/ajazz-debug plugin.list returns the merged plugin inventory without a crash.
</verification>

\<success_criteria>

- IPluginHost2 is the single contract; PluginManager implements it; Application exposes one host surface.
- All four runtimes (Node, HTML, native, Python) pass an explicit no-regression check.
- plugin_manager.cpp dispatch is SKU-free (audit 0); device I/O only via PluginDeviceBridge.
- COD-031 intact; full suite green.
  \</success_criteria>

<output>
Create .planning/phases/30-plugin-host-modular-foundation/30-03-SUMMARY.md when done.
</output>
