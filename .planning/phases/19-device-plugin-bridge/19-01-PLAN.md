---
phase: 19-device-plugin-bridge
plan: 01
type: execute
wave: 1
depends_on: [14, 15, 17, 18]
files_modified:
  - src/app/src/plugin_device_bridge.hpp
  - src/app/src/plugin_device_bridge.cpp
  - src/app/CMakeLists.txt
  - tests/unit/test_plugin_device_bridge.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [PLUGIN-10]
must_haves:
  truths:
    - The four dependency SUMMARY files (14-02, 15-01, 17-02, 18-04) are confirmed present before any seam-consuming code is written; if any is absent execution STOPS
    - A 1-based device keyIndex round-trips to a 0-based Elgato {row,column} coordinate and back through named helpers sourced from displayInfo grid dims (key 1 <-> {0,0}, key 10 <-> {1,4} on a 2x5 grid)
    - A well-formed data:image/...;base64 URI decodes to a non-null QImage; a malformed/truncated/empty data-URI yields a decode-failure result (no crash, no exception)
    - A dotted action UUID resolves to its owning registered plugin UUID by longest-prefix match against the registered-uuid set; an action with no registered owner resolves to empty
    - The ContextRegistry maps an opaque context string to {deviceId, pageId, row, column, controller, actionUUID, pluginUuid} and supports both inbound lookup (by context) and outbound lookup (by controller+row+column)
  artifacts:
    - path: src/app/src/plugin_device_bridge.hpp
      provides: PluginDeviceBridge QObject declaration + ActionContext struct + ContextRegistry + free helper declarations (decodeDataUriImage, coordsForKeyIndex, keyIndexForCoords, ownerForActionUuid)
      contains: class PluginDeviceBridge
    - path: src/app/src/plugin_device_bridge.cpp
      provides: pure-helper definitions (decode, coordinate conversion, prefix resolution, registry methods)
      contains: decodeDataUriImage
    - path: tests/unit/test_plugin_device_bridge.cpp
      provides: unit cases for coordinate round-trip, data-URI decode incl malformed, UUID-prefix resolution, registry lookups
      contains: PluginDeviceBridge
  key_links:
    - from: keyIndexForCoords / coordsForKeyIndex
      to: core::DisplayInfo keyRows/keyCols
      via: grid math sourced from displayInfo, not literals
      pattern: keyCols
    - from: decodeDataUriImage
      to: QByteArray::fromBase64 + QImage::loadFromData
      via: strip prefix at first comma, validated base64 decode, loadFromData bool check
      pattern: loadFromData
    - from: ownerForActionUuid
      to: registered plugin uuid set
      via: longest-prefix match over registered uuids
      pattern: ownerForActionUuid
---

<objective>
Land the genuinely-new, seam-free core of the plugin<->device bridge: the `PluginDeviceBridge`
QObject shell, the `ActionContext` struct + `ContextRegistry`, and the four pure helper functions
that the inbound (19-02) and outbound (19-03) wiring will compose — the `data:`-URI -> `QImage`
decoder (with crash-free failure result), the 1-based-keyIndex <-> 0-based-{row,column} coordinate
converters, and the action-UUID -> owning-plugin-UUID longest-prefix resolver. All of this is unit
testable WITHOUT the Phase 14/15/17 seams, so it ships first and de-risks the wiring plans.

This plan also performs the **STOP-if-SUMMARY-absent gate** for the dependency phases (mirroring
18-04): the executor MUST confirm the finalized seam signatures from the dependency SUMMARY files
exist before writing anything that consumes them in 19-02/19-03. PLUGIN-10.

Purpose: PLUGIN-10 — isolate and prove the three new data structures/algorithms (registry,
decode, coordinate math) the bridge needs, before any QObject::connect wiring.
Output: `plugin_device_bridge.{hpp,cpp}` (helpers + shell) + a `PluginDeviceBridge` unit suite,
registered in CMake source + AUTOMOC + the test CMakeLists under the WebSockets gate.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-device-plugin-bridge/19-CONTEXT.md
@.planning/phases/19-device-plugin-bridge/19-RESEARCH.md
@.planning/phases/19-device-plugin-bridge/19-VALIDATION.md
@CLAUDE.md

\<read_first>

- **STOP GATE (REQUIRED — read these first; if ANY is absent, STOP and report the dependency phase must be executed before Phase 19):**
  - `.planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md` — the FINAL `StreamDockControlService::assignKeyImage` arg shape (QImage vs RGBA span; 14-02-PLAN leaves this to executor discretion) + the profile key-index base the repaint path settled on.
  - `.planning/phases/15-stream-dock-input-routing/15-01-SUMMARY.md` (+ `15-02-SUMMARY.md` if present) — the FINAL `DeviceEvent` tap seam (a subscribable signal vs the `plugin` ActionExecutor stub) and the synthetic-encoder-release observability hook.
  - `.planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md` — the FINAL `SdPluginServer::sendEvent` signature.
  - `.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-SUMMARY.md` — confirms Phase 18 shipped (no direct seam consumed here, but ROADMAP lists 18 as a hard dep; record presence).
  - Fallback if absent: do NOT reimplement any seam. STOP, report which SUMMARY is missing, and recommend executing that phase first. As of planning (2026-05-23) NONE exist on disk.
- `.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-PLAN.md` lines 74-78 — the STOP-condition wording precedent to mirror.
- `src/core/include/ajazz/core/capabilities.hpp` lines 103-149 — `DisplayInfo{widthPx,heightPx,keyRows,keyCols}` (the grid dims source) + `IDisplayCapable::displayInfo()`/`setKeyImage` (note the doxygen "zero-based" is OUTDATED; the shipping backend is 1-based — see CLAUDE.md / Pitfall 2).
- `src/core/include/ajazz/core/profile.hpp` lines 42-67 (`ActionKind::Plugin`, `Action.id` dotted UUID), 100-161 (`Binding.onPress/onRelease`, `EncoderBinding.onCw/onCcw/onPress`, `ProfilePage.id`/`keys`, `Profile.keys`/`encoders`/`deviceCodename`/`pages`).
- `src/devices/streamdeck/src/akp05_protocol.hpp` lines 60-97 — KeyCount=10, KeyRows=2, KeyCols=5, EncoderCount=4 (the grid the round-trip test asserts against; source dims from displayInfo at runtime, not these literals — these are the test's expected values).
- `tests/unit/test_sd_plugin_server.cpp` lines 25-55, 92-100 — the `ensureQCoreApp()` leaked-singleton + `pump()`/`waitForSpy()` harness pattern (Pitfall 5: never allocate a fresh QCoreApplication per TEST_CASE) — this plan's unit cases need QImage only, but reuse the harness for the QObject-construction case.
- `tests/unit/CMakeLists.txt` — the existing test-registration block + the `AJAZZ_HAVE_WEBSOCKETS` gate + include-dir / source-link convention (mirror how `test_sd_plugin_server` links `sd_plugin_server.cpp`).
- `src/app/CMakeLists.txt` lines 38-89 — the library source block + the WebSockets-gated `target_sources(... sd_plugin_server.cpp)` at line 89; the AUTOMOC header listing starting ~line 272 (mirror `branding_service.{cpp,hpp}` placement so the new QObject's moc runs).
- `docs/protocols/streamdeck/akp_plugin_sdk.md` §4.4 (the `coordinates {row,column}` + `controller` "Encoder"/"Keypad" wire keys — schema is source of truth) + §5 (data-URI prefix shape + the "placeholder on decode failure, no failure event" contract).
  \</read_first>

<interfaces>
<!-- Consumed contracts the executor must CONFIRM from the dependency SUMMARY files (signatures below are the PLAN-level contract; the SUMMARY is authoritative if it diverges). Do NOT re-implement. -->

From src/core/include/ajazz/core/capabilities.hpp:
struct DisplayInfo { std::uint16_t widthPx; std::uint16_t heightPx; std::uint8_t keyRows; std::uint8_t keyCols; };

From src/core/include/ajazz/core/profile.hpp:
enum class ActionKind : std::uint8_t { Plugin = 0, /\* ... */ };
struct Action { ActionKind kind; std::string id; /* ... */ }; // id dotted "<plugin>.<action>" when kind==Plugin
struct Binding { std::vector<Action> onPress; std::vector<Action> onRelease; };
struct EncoderBinding { std::vector<Action> onCw, onCcw, onPress; };
struct ProfilePage { std::string id; std::unordered_map\<std::uint16_t, Binding> keys; /* ... */ };
struct Profile { std::string id; std::string deviceCodename;
std::unordered_map\<std::uint16_t, Binding> keys;
std::unordered_map\<std::uint16_t, EncoderBinding> encoders; /* ... \*/ };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STOP-gate the dependency SUMMARY files + scaffold PluginDeviceBridge shell with ContextRegistry</name>
  <files>src/app/src/plugin_device_bridge.hpp, src/app/src/plugin_device_bridge.cpp, src/app/CMakeLists.txt</files>
  <read_first>
  - The four dependency SUMMARY files listed in the STOP GATE above (confirm each exists; abort otherwise).
  - src/app/CMakeLists.txt lines 38-89 + ~272 (source block, WebSockets gate at :89, AUTOMOC header listing).
  - src/core/include/ajazz/core/profile.hpp (ActionContext fields draw from Profile/ProfilePage/Action).
  </read_first>
  <action>
  FIRST verify the STOP GATE: confirm `14-02-SUMMARY.md`, `15-01-SUMMARY.md`, `17-02-SUMMARY.md`,
  `18-04-SUMMARY.md` all exist. If any is missing, STOP and report "Phase 19 blocked: dependency
  SUMMARY <name> absent — execute that phase first" without writing any seam-consuming code. Record
  in the SUMMARY the FINAL signatures read from each (the `assignKeyImage` arg shape, the DeviceEvent
  tap seam, the `sendEvent` signature) — 19-02/19-03 depend on these being the actual, not assumed,
  contracts.

Create `plugin_device_bridge.hpp`: declare `class PluginDeviceBridge : public QObject` with
`Q_OBJECT`. Declare an `ActionContext` POD struct with fields `QString deviceId; QString pageId;   int row{0}; int column{0}; QString controller; QString actionUUID; QString pluginUuid;` (row/column
are the 0-based Elgato coordinate per spec §4.4; controller is "Keypad" or "Encoder"). Declare a
`ContextRegistry` (a nested type or sibling class the bridge owns) holding `QHash<QString,   ActionContext> m_byContext` (inbound resolve by opaque context string) and `QHash<QString, QString>   m_byCoord` (outbound: key = controller + "#" + row + "#" + column -> context string). Give it
methods: `QString registerContext(ActionContext const&)` (mints/derives the context id — use the
encoded-tuple scheme `deviceId#pageId#controller#row#column` so re-activating a page yields a stable
id plugins may cache; document this choice in the SUMMARY), `std::optional<ActionContext>   byContext(QString const&) const`, `std::optional<ActionContext> byCoord(QString const& controller,   int row, int column) const`, `void retire(QString const& context)`, `void retireDevice(QString   const& deviceId)`, `void retirePage(QString const& deviceId, QString const& pageId)`, and `void   clear()`. Constructor signature is a stub for now (19-02 will inject the server/control/input
service pointers) — declare it taking a `QObject* parent = nullptr` plus forward-declared
service-pointer params commented as "wired in 19-02"; keep the body empty in this plan.

COD-031: app-layer QJson/QImage only — `#include <QImage>`, `<QJsonObject>`, `<QHash>`, `<QString>`,
`<optional>`; never `nlohmann`.

Add `src/plugin_device_bridge.cpp` to `src/app/CMakeLists.txt` inside the same
`AJAZZ_HAVE_WEBSOCKETS`-gated `target_sources(...)` block as `sd_plugin_server.cpp` (~line 89), and
add `src/plugin_device_bridge.hpp` + `src/plugin_device_bridge.cpp` to the AUTOMOC header/source
listing (~line 272, mirror `branding_service`) so the QObject's moc runs. Implement the
ContextRegistry method bodies in the .cpp (pure map operations, no seams).
</action>
<verify>
<automated>test -f .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md && test -f .planning/phases/15-stream-dock-input-routing/15-01-SUMMARY.md && test -f .planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md && test -f .planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-04-SUMMARY.md && cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -5; grep -c 'class PluginDeviceBridge' src/app/src/plugin_device_bridge.hpp</automated>
</verify>
\<acceptance_criteria>

- All four dependency SUMMARY files exist (the STOP gate passes); the SUMMARY records each FINAL seam signature read.
- `plugin_device_bridge.hpp` declares `class PluginDeviceBridge : public QObject` with `Q_OBJECT`, the `ActionContext` struct (7 fields), and the `ContextRegistry` with `registerContext`/`byContext`/`byCoord`/`retire`/`retireDevice`/`retirePage`/`clear`.
- The app target compiles with the new files in the source + AUTOMOC blocks; `grep -c 'class PluginDeviceBridge' ...` returns 1.
- No `nlohmann` include anywhere in the new files (`grep -c nlohmann src/app/src/plugin_device_bridge.*` == 0).
  \</acceptance_criteria>
  <done>The STOP gate passed (all four SUMMARY files present, signatures recorded), the bridge shell + ContextRegistry compile into the app target with moc wired, encoded-tuple context scheme documented.</done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Pure helpers — data-URI decode, coordinate conversion, UUID-prefix resolution — with unit tests</name>
  <files>src/app/src/plugin_device_bridge.hpp, src/app/src/plugin_device_bridge.cpp, tests/unit/test_plugin_device_bridge.cpp, tests/unit/CMakeLists.txt</files>
  <behavior>
  - coordsForKeyIndex(keyIndex=1, keyCols=5) -> {row:0, column:0}; keyIndex=5 -> {0,4}; keyIndex=6 -> {1,0}; keyIndex=10 -> {1,4}.
  - keyIndexForCoords(row=0,col=0,keyCols=5) -> 1; (1,4) -> 10; round-trip coordsForKeyIndex(keyIndexForCoords(r,c)) == {r,c} for all grid cells.
  - decodeDataUriImage("data:image/png;base64,<valid-png>") -> {ok:true, non-null QImage}; a 1x1 PNG decodes to a 1x1 image.
  - decodeDataUriImage("data:image/png;base64,!!!notbase64!!!") -> {ok:false} (no crash); decodeDataUriImage("") -> {ok:false}; decodeDataUriImage("data:image/png;base64,") (empty body) -> {ok:false}; a truncated/garbage PNG body that base64-decodes but is not a valid image -> {ok:false}.
  - decodeDataUriImage tolerates a raw (no "data:" prefix) base64 body: if no comma is present, treat the whole string as the base64 body.
  - ownerForActionUuid("com.x.plugin.action1", {"com.x.plugin","com.y.other"}) -> "com.x.plugin"; longest-prefix wins when two registered uuids both prefix-match; an action with no registered owner -> empty QString.
  </behavior>
  <read_first>
  - src/core/include/ajazz/core/capabilities.hpp (DisplayInfo keyRows/keyCols — the converters take keyCols as a param so they are pure/testable).
  - tests/unit/test_sd_plugin_server.cpp lines 25-55 (ensureQCoreApp/pump harness — Pitfall 5).
  - tests/unit/CMakeLists.txt (the AJAZZ_HAVE_WEBSOCKETS gate + how test_sd_plugin_server links sd_plugin_server.cpp; mirror for plugin_device_bridge.cpp).
  - docs/protocols/streamdeck/akp_plugin_sdk.md §5 (data-URI prefix; placeholder-on-failure contract — the decoder returns a failure result, the CALLER paints the placeholder in 19-02).
  </read_first>
  <action>
  Declare in `plugin_device_bridge.hpp` (free functions in the bridge's namespace, so they are
  unit-testable without constructing the QObject):
  - `struct GridCoord { int row; int column; };`
  - `GridCoord coordsForKeyIndex(std::uint8_t oneBasedKeyIndex, std::uint8_t keyCols)` -> `{row =
    (k-1)/keyCols, column = (k-1)%keyCols}`. Source keyCols from `displayInfo().keyCols` at the call
    site (19-03) — here it is a param so the helper is pure.
  - `std::uint8_t keyIndexForCoords(int row, int column, std::uint8_t keyCols)` -> `row*keyCols +
    column + 1` (1-based device index).
  - `struct DecodedImage { bool ok{false}; QImage image; };`
  - `DecodedImage decodeDataUriImage(QString const& dataUri)`: find the first ','; the base64 body is
    everything after it (or the whole string if no comma — tolerate a raw body). `QByteArray::fromBase64`
    the body (use the plain overload; an empty/garbage decode yields empty bytes). Call
    `QImage::loadFromData(raw)`; if it returns false OR the image is null, return `{ok:false, {}}`.
    On success return `{ok:true, img}`. NEVER throw; NEVER return a null QImage with ok:true. (Don't
    hand-roll base64 or image decode — Qt's validated decoders only; §5 + Pitfall 6.)
  - `QString ownerForActionUuid(QString const& dottedActionUuid, QSet<QString> const&
    registeredPluginUuids)`: return the longest registered uuid that is a dotted-prefix of the action
    uuid (prefix boundary on '.' so "com.x.plug" does not match "com.x.plugin.a"); empty QString if
    none (Pitfall 4 — longest-prefix, not last-segment trim).

Define all four in the .cpp. Create `tests/unit/test_plugin_device_bridge.cpp` with Catch2
TEST_CASEs covering every bullet in <behavior>. Build the valid 1x1 PNG fixture in-test via
`QImage(1,1,QImage::Format_ARGB32)` -> `QBuffer`/`QImageWriter` to a `QByteArray`, then
`.toBase64()` with the `"data:image/png;base64,"` prefix prepended (this is test-fixture
construction, NOT a second device encode path — it never touches image_pipeline or the device).
Reuse `ensureQCoreApp()` (Pitfall 5). ASCII-only TEST_CASE names and tags (no em-dash/arrow —
use "-" and "->"; CLAUDE.md Win32 ctest codepage rule). Register the test in
`tests/unit/CMakeLists.txt` linking `plugin_device_bridge.cpp` (+ sd_plugin_server.cpp if the link
graph needs it) inside the `AJAZZ_HAVE_WEBSOCKETS` gate, mirroring `test_sd_plugin_server`.
</action>
<verify>
<automated>ctest --preset linux-release -R PluginDeviceBridge --output-on-failure 2>&1 | tail -20</automated>
</verify>
\<acceptance_criteria>

- The coordinate round-trip cases pass for key 1\<->{0,0}, key 5\<->{0,4}, key 6\<->{1,0}, key 10\<->{1,4}, and the full-grid round-trip property.
- A valid base64 PNG data-URI decodes to a non-null QImage (ok:true); malformed base64, empty string, empty body, and a non-image body each return ok:false with no crash and no exception.
- A data-URI with no "data:" prefix (raw base64 body, no comma) decodes correctly.
- `ownerForActionUuid` returns the longest registered-uuid prefix; returns empty for an unowned action; does not split on the last dotted segment.
- All TEST_CASE names/tags are ASCII; `ctest --preset linux-release -R PluginDeviceBridge` is green.
  \</acceptance_criteria>
  <done>The three new algorithms (coordinate conversion, data-URI decode with crash-free failure, longest-prefix owner resolution) are defined as pure helpers and proven green under ctest, with ASCII names and no second encode path.</done>
  </task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                                 |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| plugin WS client -> bridge | Untrusted plugin-supplied `context` string + `data:` URI payload cross here (consumed in 19-02; the decode helper landed here is the validation primitive). |
| device backend -> bridge   | 1-based keyIndex / encoder index cross here (consumed in 19-03; the coordinate converters landed here are the translation primitive).                       |

## STRIDE Threat Register

| Threat ID        | Category                         | Component                                                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                     |
| ---------------- | -------------------------------- | ---------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-19-img         | Tampering/DoS                    | `decodeDataUriImage` (malformed/oversized data-URI)                                            | mitigate    | Qt-validated `QByteArray::fromBase64` + `QImage::loadFromData` bool check; return `{ok:false}` on any failure (no crash, no exception). The CALLER (19-02) paints a placeholder; no failure event to the plugin (§5). Tested with malformed/empty/non-image bodies. |
| T-19-coord       | Tampering                        | `coordsForKeyIndex`/`keyIndexForCoords` (off-by-one between 1-based device and 0-based Elgato) | mitigate    | Single named converter pair sourced from `displayInfo` grid dims; round-trip unit-tested (Pitfall 2). Prevents painting/reporting the neighbor key.                                                                                                                 |
| T-19-owner       | Elevation/Information Disclosure | `ownerForActionUuid` (action attributed to wrong plugin)                                       | mitigate    | Longest-prefix match against the registered-uuid set (Pitfall 4); unowned action -> empty (no-op upstream). Foundation for the cross-plugin access control enforced in 19-02/19-03.                                                                                 |
| T-19-SC          | Tampering                        | npm/pip/cargo installs                                                                         | accept      | No package installs in Phase 19 (pure in-tree C++/Qt6); Package Legitimacy Gate N/A per 19-RESEARCH.                                                                                                                                                                |
| \</threat_model> |                                  |                                                                                                |             |                                                                                                                                                                                                                                                                     |

<verification>
- `ctest --preset linux-release -R PluginDeviceBridge` green (helper unit cases).
- `cmake --build --preset linux-release --target ajazz-control-center` compiles the new files.
- `grep -c nlohmann src/app/src/plugin_device_bridge.*` == 0 (COD-031).
- All TEST_CASE names ASCII (no em-dash/arrow).
- STOP gate: the four dependency SUMMARY files exist (else execution halted and reported).
</verification>

\<success_criteria>

- The STOP gate passed and recorded the final seam signatures for 19-02/19-03 to consume.
- The bridge shell + ContextRegistry + the three pure helpers (decode, coordinate conversion, owner resolution) compile and are unit-green, fully independent of the not-yet-wired seams.
- keyIndex 1-based \<-> coordinates 0-based conversion is explicit, named, displayInfo-sourced, and round-trip tested.
  \</success_criteria>

<output>
Create `.planning/phases/19-device-plugin-bridge/19-01-SUMMARY.md` when done — record the FINAL
`assignKeyImage` arg shape, the DeviceEvent tap seam, and the `sendEvent` signature read from the
dependency SUMMARY files, plus the chosen context-id scheme.
</output>
