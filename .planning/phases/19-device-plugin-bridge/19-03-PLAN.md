---
phase: 19-device-plugin-bridge
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - src/app/src/plugin_device_bridge.hpp
  - src/app/src/plugin_device_bridge.cpp
  - src/app/src/application.cpp
  - tests/unit/test_plugin_device_bridge.cpp
autonomous: true
requirements: [PLUGIN-10]
must_haves:
  truths:
    - A fed KeyPressed/KeyReleased DeviceEvent for a 1-based key delivers keyDown/keyUp to the owning plugin via sendEvent with 0-based coordinates {row,column} computed by coordsForKeyIndex
    - A fed EncoderTurned DeviceEvent delivers dialRotate with signed ticks + controller "Encoder"; EncoderPressed/EncoderReleased deliver dialDown/dialUp; a TouchStrip tap delivers touchTap
    - An input event at a coordinate with no bound plugin context is dropped (no sendEvent, no cross-plugin leakage)
    - When the active profile page becomes visible (on plugin registration / device connect), each bound ActionKind::Plugin action mints a context and willAppear is sent to its owning plugin carrying that context + coordinates; willDisappear/deviceDidDisconnect retire contexts on page-change/removal
    - deviceDidConnect/deviceDidDisconnect are sent to registered plugins on device arrival/removal, tapping the existing hot-plug wire (no second HotplugMonitor)
  artifacts:
    - path: src/app/src/plugin_device_bridge.cpp
      provides: onDeviceEvent DeviceEvent->§4.4 envelope mapping, populateContextsForActivePage (willAppear), deviceDidConnect/Disconnect lifecycle, owner resolution via ownerForActionUuid
      contains: onDeviceEvent
    - path: src/app/src/application.cpp
      provides: connect the Phase-15 DeviceEvent tap + the hot-plug + the profileChanged wire to the bridge
      contains: onDeviceEvent
    - path: tests/unit/test_plugin_device_bridge.cpp
      provides: loopback-client receives keyDown(coords)/dialRotate(ticks)/willAppear(context); unbound-coordinate-drop; cross-plugin-no-leak cases
      contains: keyDown
  key_links:
    - from: StreamDockInputService DeviceEvent tap
      to: PluginDeviceBridge::onDeviceEvent
      via: QObject::connect to the Phase-15 DeviceEvent signal (or the plugin ActionExecutor seam per 15-01-SUMMARY)
      pattern: onDeviceEvent
    - from: PluginDeviceBridge::onDeviceEvent
      to: SdPluginServer::sendEvent
      via: coordsForKeyIndex -> byCoord lookup -> sendEvent(ownerUuid, eventName, §4.4 payload)
      pattern: sendEvent
    - from: PluginDeviceBridge::populateContextsForActivePage
      to: SdPluginServer::sendEvent willAppear
      via: enumerate active page bindings, registerContext per ActionKind::Plugin, sendEvent willAppear
      pattern: willAppear
---

<objective>
Wire the outbound half of the bridge: tap the Phase-15 `DeviceEvent` stream and map each event to its
§4.4 envelope — `KeyPressed`/`KeyReleased` -> `keyDown`/`keyUp` with 0-based `coordinates`,
`EncoderTurned` -> `dialRotate` with signed `ticks` + `controller:"Encoder"`, `EncoderPressed`/
`EncoderReleased` -> `dialDown`/`dialUp` (+ legacy `keyDownCord`/`keyUpCord`), `TouchStrip` tap ->
`touchTap` — and deliver it via `SdPluginServer::sendEvent` ONLY to the plugin that owns the bound
action at that coordinate (via the context registry). Add the lifecycle: on plugin registration /
device connect, populate contexts for the active page's bound plugin actions and emit per-action
`willAppear`; emit `deviceDidConnect`/`deviceDidDisconnect` on hot-plug; retire contexts +
`willDisappear` on page-change/disconnect. Verify with a loopback `QWebSocket` client + a canned
`DeviceEvent` feed. PLUGIN-10.

Purpose: PLUGIN-10 — the outbound (device -> plugin) round-trip + the lifecycle, completing the
bidirectional bridge and closing the convergence point.
Output: `onDeviceEvent` mapping + lifecycle methods + the Application taps + the loopback-client
receives-event e2e tests.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-device-plugin-bridge/19-CONTEXT.md
@.planning/phases/19-device-plugin-bridge/19-RESEARCH.md
@.planning/phases/19-device-plugin-bridge/19-VALIDATION.md
@CLAUDE.md

\<read_first>

- `.planning/phases/19-device-plugin-bridge/19-01-SUMMARY.md` + `19-02-SUMMARY.md` (the helper signatures, the context-id scheme, the bridge ctor + the actionReceived wiring already in place).
- `.planning/phases/15-stream-dock-input-routing/15-01-SUMMARY.md` (REQUIRED — the FINAL DeviceEvent tap seam: is there a subscribable `DeviceEvent` Qt signal on `StreamDockInputService`, or only the `plugin` ActionExecutor stub? + the synthetic-encoder-release hook + the touch-zone derivation; if absent, STOP). Also `15-01-PLAN.md` lines 89-131 (DeviceEvent::Kind index/value semantics: KeyPressed index=1-based; EncoderTurned index=0..3 value=±delta; EncoderPressed value=1 / EncoderReleased value=0; TouchStrip value=(gesture\<<16)|X; TouchZoneCount=4, TouchStripRangeX=640) + lines 53-65 (the `plugin` ActionExecutor is the explicit Phase-19 seam). Pitfall 3: prefer a raw DeviceEvent tap (full coordinates/ticks) + registry lookup over the executor (which lost coordinates); if 15-01 exposes ONLY the executor, plan a minimal additive signal on the input service rather than re-deriving coordinates.
- `.planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md` (REQUIRED — the FINAL `bool sendEvent(QString targetUuid, QString eventName, QJsonObject payload = {})` signature + that it re-resolves the live slot each call and returns false safely on a missing socket — never cache a QWebSocket\*).
- `src/core/include/ajazz/core/device.hpp` lines 105-130 (`DeviceEvent{Kind kind; uint16 index; int32 value;}`; Connected/Disconnected kinds for hot-plug).
- `src/core/include/ajazz/core/profile.hpp` lines 100-161 (`Binding.onPress/onRelease`, `EncoderBinding.onCw/onCcw/onPress`, `ProfilePage`, `Profile.keys`/`encoders`/`pages`; `Action.kind==ActionKind::Plugin`, `Action.id` dotted UUID -> owner via 19-01 `ownerForActionUuid`).
- `src/app/src/application.cpp` (the hot-plug wire `Application` already re-resolves on for Phase 14 — tap the SAME signal for deviceDidConnect/Disconnect; the `profileChanged` connect — tap it for page activation; do NOT add a second HotplugMonitor).
- `docs/protocols/streamdeck/akp_plugin_sdk.md` §4.4 (the exact payload keys — schema is source of truth: `keyDown`/`keyUp` `{settings, coordinates:{row,column}, state, isInMultiAction}`; `dialRotate`/`dialDown`/`dialUp` `{ticks, pressed, controller, deviceCoordinates:{point,width,height}, settings}`; `keyDownCord`/`keyUpCord` AJAZZ-legacy alias; `touchTap` `{x, y, hold, settings}`; `willAppear`/`willDisappear` `{settings, coordinates, state, isInMultiAction}`; `deviceDidConnect`/`deviceDidDisconnect` `{deviceInfo:{name,type,size}}`).
- 19-RESEARCH.md Pattern 4 (the DeviceEvent->event table) + Pitfall 4 (longest-prefix owner) + the Code Examples block (onDeviceEvent skeleton).
  \</read_first>

<interfaces>
<!-- Consumed contracts — CONFIRM from 15-01-SUMMARY + 17-02-SUMMARY. -->
From src/app/src/sd_plugin_server.hpp (Phase 17-02):
  bool sendEvent(QString const& targetUuid, QString const& eventName, QJsonObject const& payload = {});
  void pluginRegistered(QString const& pluginUuid);   // populate contexts + willAppear on this
  void pluginDisconnected(QString const& pluginUuid); // retire on this

From src/app/src/stream_dock_input_service.hpp (Phase 15 — CONFIRM the tap seam in 15-01-SUMMARY):
// EITHER a subscribable signal: void deviceEvent(core::DeviceEvent const&);
// OR the ActionExecutors.plugin stub (loses coordinates) — prefer the signal; add one if absent.

From src/core/include/ajazz/core/device.hpp:
struct DeviceEvent { enum class Kind { KeyPressed, KeyReleased, EncoderTurned, EncoderPressed,
EncoderReleased, TouchStrip, Connected, Disconnected }; Kind kind;
std::uint16_t index; std::int32_t value; };

From src/app/src/plugin_device_bridge.hpp (19-01/19-02):
GridCoord coordsForKeyIndex(std::uint8_t k, std::uint8_t keyCols);
QString ownerForActionUuid(QString const& dotted, QSet<QString> const& registered);
// ContextRegistry::byCoord / registerContext / retire\*
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: onDeviceEvent DeviceEvent->§4.4 envelope mapping + sendEvent to the owning plugin</name>
  <files>src/app/src/plugin_device_bridge.hpp, src/app/src/plugin_device_bridge.cpp</files>
  <behavior>
  - KeyPressed index=k -> sendEvent(owner, "keyDown", {coordinates:{row,column} via coordsForKeyIndex(k,keyCols), isInMultiAction:false}); KeyReleased -> "keyUp" same payload.
  - EncoderTurned index=e value=+3 -> sendEvent(owner, "dialRotate", {ticks:3, pressed:false, controller:"Encoder"}); value=-2 -> ticks:-2 (signed preserved).
  - EncoderPressed index=e -> sendEvent(owner, "dialDown", {controller:"Encoder", ...}) AND the legacy alias keyDownCord; EncoderReleased -> "dialUp" + keyUpCord.
  - TouchStrip tap (gesture 0) -> sendEvent(owner, "touchTap", {x, y:0, hold:false}); swipe gestures (1/2) are NOT plugin events (page-nav intent owned by Phase 16) -> dropped here.
  - An event at a coordinate with no registry context (byCoord miss) sends nothing (no leakage, no crash).
  - The owner uuid is resolved from the context's pluginUuid (the registry already binds context->pluginUuid); sendEvent's bool return is ignored safely (a closed socket is not a crash).
  </behavior>
  <read_first>
  - 15-01-SUMMARY.md (DeviceEvent index/value semantics + the tap seam) + 17-02-SUMMARY.md (sendEvent signature).
  - device.hpp lines 105-130 (DeviceEvent kinds).
  - docs/protocols/streamdeck/akp_plugin_sdk.md §4.4 (exact payload keys — do NOT rename to C++ field names; schema wins).
  - 19-RESEARCH.md Pattern 4 table + Code Examples onDeviceEvent.
  </read_first>
  <action>
  Add `void onDeviceEvent(QString const& deviceId, core::DeviceEvent const& ev)` to the bridge.
  Switch on `ev.kind`:
  - `KeyPressed`/`KeyReleased`: `auto gc = coordsForKeyIndex(static_cast<std::uint8_t>(ev.index),
    keyCols)`; `auto ctx = m_registry.byCoord("Keypad", gc.row, gc.column)`; if no ctx, return. Build
    `QJsonObject payload{{"coordinates", QJsonObject{{"row", gc.row}, {"column", gc.column}}},
    {"isInMultiAction", false}}` and (optionally `state`/`settings` empty for now); call
    `m_server->sendEvent(ctx->pluginUuid, ev.kind==KeyPressed ? "keyDown" : "keyUp", payload)`.
  - `EncoderTurned`: encoder index is 0-based; look up the encoder context (controller "Encoder",
    row 0, column = ev.index — document the encoder-as-coordinate convention; confirm against the
    registry scheme from 19-01). Build `{"ticks", ev.value}` (SIGNED — preserve sign), `{"pressed",
    false}`, `{"controller", "Encoder"}`; sendEvent "dialRotate".
  - `EncoderPressed`: sendEvent "dialDown" with `{controller:"Encoder", ...}` AND sendEvent the legacy
    "keyDownCord" alias (§4.4 AJAZZ-legacy). `EncoderReleased`: "dialUp" + "keyUpCord". (The synthetic
    release comes from the Phase-15 hook per 15-01-SUMMARY — the bridge just maps the EncoderReleased
    event it receives.)
  - `TouchStrip`: unpack `gesture = ev.value >> 16`, `x = ev.value & 0xFFFF`. Only gesture 0 (tap) ->
    sendEvent "touchTap" `{x, "y":0, "hold":false}` to the touch/zone-owning context (use the same
    zone derivation Phase 15 uses to find the owning encoder/zone context). Swipe gestures (1/2) are
    page-nav intents (Phase 16) -> drop (no plugin event).
  - `Connected`/`Disconnected`: handled by the lifecycle in Task 2 — return here.
  Source `keyCols` from the active device `displayInfo().keyCols`. For every branch, a registry miss
  is a silent drop (no sendEvent — prevents cross-plugin leakage, T-19-leak). Ignore sendEvent's bool
  return (a missing/closed socket returns false, not a crash — 17-02). Never cache a QWebSocket*
  (address by uuid only). COD-031: QJsonObject only.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -5; grep -c 'onDeviceEvent\|dialRotate\|keyDownCord' src/app/src/plugin_device_bridge.cpp</automated>
  </verify>
  <acceptance_criteria>
  - KeyPressed/KeyReleased map to keyDown/keyUp with 0-based coordinates from `coordsForKeyIndex` and call sendEvent with the context's owner uuid.
  - EncoderTurned maps to dialRotate preserving the signed ticks + controller "Encoder"; EncoderPressed/EncoderReleased map to dialDown/dialUp + the legacy keyDownCord/keyUpCord alias.
  - TouchStrip gesture 0 maps to touchTap; swipe gestures are dropped (not sent as plugin events).
  - A coordinate with no registry context produces no sendEvent call (no leakage), no crash.
  - The bridge never caches a QWebSocket* (`grep -c QWebSocket src/app/src/plugin_device_bridge.cpp` == 0); no `nlohmann`; app target compiles.
  </acceptance_criteria>
  <done>Device input maps to the correct §4.4 envelope (keyDown/keyUp/dialRotate/dialDown/dialUp/touchTap) and reaches only the owning plugin via sendEvent; unbound coordinates are silently dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Lifecycle (willAppear/deviceDidConnect/retire) + Application taps; loopback receives-event e2e tests</name>
  <files>src/app/src/plugin_device_bridge.hpp, src/app/src/plugin_device_bridge.cpp, src/app/src/application.cpp, tests/unit/test_plugin_device_bridge.cpp</files>
  <read_first>
  - 19-02-SUMMARY.md (the bridge ctor + the actionReceived wiring already in Application; mirror the connect style for the new taps).
  - src/app/src/application.cpp (the existing hot-plug signal `Application` already consumes for Phase-14 re-resolve + the `profileChanged` connect — tap the SAME wires; do NOT add a second HotplugMonitor).
  - 15-01-SUMMARY.md (the DeviceEvent tap seam to connect onDeviceEvent to) + 17-02-SUMMARY.md (sendEvent).
  - profile.hpp lines 100-161 (enumerate the active page's bound ActionKind::Plugin actions for willAppear).
  - docs/protocols/streamdeck/akp_plugin_sdk.md §4.4 (willAppear/deviceDidConnect payload shapes).
  - tests/unit/test_sd_plugin_server.cpp (loopback client + ensureQCoreApp/pump/waitForSpy; the client subscribes to its own incoming messages to assert received events).
  </read_first>
  <action>
  Add to the bridge:
  - `void populateContextsForActivePage(QString const& deviceId)`: enumerate the active profile's
    active/root page (per CONTEXT discretion A6: Phase 16 page authority is NOT a hard dep — scope to
    the root page if the multi-page model is unavailable; note the simplification in SUMMARY). For
    each key `Binding` and `EncoderBinding`, for each `Action` with `kind==ActionKind::Plugin`, resolve
    the owner via `ownerForActionUuid(action.id, m_registeredPlugins)`; if owned by a registered
    plugin, `registerContext(...)` (controller "Keypad" for keys with coordsForKeyIndex; "Encoder"
    for encoders) and `sendEvent(owner, "willAppear", {coordinates, isInMultiAction:false})`. This is
    the per-visible-action willAppear (spec-faithful, A6).
  - `void onPluginRegistered(QString const& uuid)` / `onPluginDisconnected(QString const& uuid)`:
    maintain `QSet<QString> m_registeredPlugins`; on register, populate contexts + willAppear for that
    plugin's actions on the active page; on disconnect, retire its contexts.
  - `void onDeviceConnected(QString const& deviceId)` / `onDeviceDisconnected(QString const&
    deviceId)`: on connect, `populateContextsForActivePage(deviceId)` and `sendEvent` "deviceDidConnect"
    `{deviceInfo:{name,type,size}}` to each registered plugin; on disconnect, `sendEvent`
    "deviceDidDisconnect" + `m_registry.retireDevice(deviceId)`.
  - `void onActivePageChanged(...)`: retire the old page's contexts (`retirePage`) + emit willDisappear
    for them, then `populateContextsForActivePage` (willAppear) for the new page.

In `application.cpp` (inside the WebSockets gate, after the bridge is constructed in 19-02): connect
the Phase-15 input service's DeviceEvent tap to `PluginDeviceBridge::onDeviceEvent` (use the
subscribable signal per 15-01-SUMMARY; if only the executor stub exists, the input service needs a
minimal additive `deviceEvent` signal — land it in the input service header and emit it from the
existing onEvent path, the smallest change); connect `pluginRegistered`/`pluginDisconnected` to the
lifecycle slots; tap the existing hot-plug signal to `onDeviceConnected`/`onDeviceDisconnected`; tap
the `profileChanged`/page-activation wire to `onActivePageChanged`.

Extend `tests/unit/test_plugin_device_bridge.cpp` (loopback client + the wired bridge + control
service + a canned DeviceEvent feed):

- **keyDown delivered with coordinates (PLUGIN-10):** register a context for key 3 ({0,2}, owner
  "com.test.plug"); the loopback client (registered as "com.test.plug") listens for incoming
  messages; feed a `DeviceEvent{KeyPressed, index=3}` into `onDeviceEvent`; pump; assert the client
  received `{event:"keyDown", payload:{coordinates:{row:0,column:2}, ...}}`.
- **dialRotate signed ticks:** register an encoder context (owner "com.test.plug"); feed
  `DeviceEvent{EncoderTurned, index=0, value=-2}`; pump; assert the client received
  `{event:"dialRotate", payload:{ticks:-2, controller:"Encoder", pressed:false}}`.
- **willAppear on activation:** with a profile binding a plugin action to key 3 owned by
  "com.test.plug", trigger page activation (call `onActivePageChanged`/`onPluginRegistered`); pump;
  assert the client received `{event:"willAppear", payload:{coordinates:{row:0,column:2}, ...}}`
  carrying the minted context (the `context` envelope field present).
- **unbound-coordinate drop / no cross-plugin leak:** feed a KeyPressed for a key with no registered
  context; pump; assert the client received NO event. Register key 4 owned by "com.other"; feed
  KeyPressed index=4 while only "com.test.plug" is connected; assert "com.test.plug" received nothing
  (no leakage).
  Reuse `ensureQCoreApp()`; ASCII-only TEST_CASE names. (Test link block already covers the sources
  from 19-02; add the input-service source if a new signal was added there.)
  </action>
  <verify>
  <automated>ctest --preset linux-release -R PluginDeviceBridge --output-on-failure 2>&1 | tail -30; grep -c 'willAppear|deviceDidConnect' src/app/src/plugin_device_bridge.cpp</automated>
  </verify>
  \<acceptance_criteria>
- A fed KeyPressed for key 3 delivers `keyDown` with `coordinates:{row:0,column:2}` to the owning loopback client; EncoderTurned value=-2 delivers `dialRotate` with `ticks:-2`.
- On page activation / plugin registration, `willAppear` is sent per bound plugin action carrying the minted context + coordinates.
- `deviceDidConnect`/`deviceDidDisconnect` are sent to registered plugins on hot-plug tapping the existing wire (no second HotplugMonitor in the bridge — `grep -c HotplugMonitor src/app/src/plugin_device_bridge.cpp` == 0).
- An unbound coordinate sends nothing; an event for a context owned by another plugin does not reach a different plugin (no leakage).
- `ctest --preset linux-release -R PluginDeviceBridge` green; full suite green at wave merge; ASCII names.
  \</acceptance_criteria>
  <done>Device input reaches the bound plugin (keyDown/dialRotate with correct envelopes), willAppear/deviceDidConnect lifecycle fires per-visible-action and on hot-plug, contexts retire on page-change/disconnect, and the loopback-client e2e test proves the full device->plugin round-trip — closing PLUGIN-10.</done>
  </task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                           |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| device backend -> input service -> bridge               | 1-based key / 0-based encoder index + touch value cross here via the DeviceEvent tap. |
| bridge -> SdPluginServer::sendEvent -> plugin WS client | §4.4 event envelopes leave the host to a specific plugin uuid.                        |

## STRIDE Threat Register

| Threat ID        | Category                         | Component                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                    |
| ---------------- | -------------------------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-19-leak        | Information Disclosure/Elevation | `onDeviceEvent` routing                 | mitigate    | Resolve the coordinate to a single context via `byCoord`; sendEvent ONLY to `ctx.pluginUuid`. An unbound coordinate or a coordinate owned by another plugin -> no sendEvent (no cross-plugin event delivery, ASVS V4). Tested by the unbound-drop + no-leak cases. |
| T-19-owner       | Spoofing                         | willAppear/lifecycle owner resolution   | mitigate    | Owner resolved via 19-01 `ownerForActionUuid` longest-prefix against the REGISTERED-plugin set; events only to currently-registered plugins (Pitfall 4).                                                                                                           |
| T-19-sock        | Tampering/DoS                    | `sendEvent` to a closed/rejected socket | mitigate    | `sendEvent` re-resolves the live slot each call and returns false safely (17-02); the bridge addresses by uuid, never caches a QWebSocket\*.                                                                                                                       |
| T-19-stale       | Tampering                        | events after page-change/device removal | mitigate    | Contexts retired on `willDisappear`/`deviceDidDisconnect`/`pluginDisconnected`; a post-retire event finds no context -> dropped.                                                                                                                                   |
| T-19-SC          | Tampering                        | npm/pip/cargo installs                  | accept      | No package installs (pure in-tree C++/Qt6); Package Legitimacy Gate N/A per 19-RESEARCH.                                                                                                                                                                           |
| \</threat_model> |                                  |                                         |             |                                                                                                                                                                                                                                                                    |

<verification>
- `ctest --preset linux-release -R PluginDeviceBridge` green (outbound + lifecycle e2e cases).
- `cmake --build --preset linux-release` compiles the bridge + Application taps.
- `grep -c QWebSocket src/app/src/plugin_device_bridge.cpp` == 0 (never cache a socket).
- `grep -c HotplugMonitor src/app/src/plugin_device_bridge.cpp` == 0 (tap the existing hot-plug wire).
- `grep -c nlohmann src/app/src/plugin_device_bridge.*` == 0 (COD-031).
- ASCII TEST_CASE names; full suite green at wave merge → ready for `/gsd:verify-work`.
</verification>

\<success_criteria>

- PLUGIN-10 outbound: device input (keyDown/keyUp, dialRotate/dialDown/dialUp, touchTap) reaches the registered plugin with the correct §4.4 envelope + 0-based coordinates + signed ticks.
- willAppear/deviceDidConnect lifecycle fires per-visible-action and on hot-plug; willDisappear/deviceDidDisconnect + context retirement on page-change/removal.
- Inbound events reach only the owning plugin (no cross-plugin leakage); unbound coordinates are dropped.
- The full bidirectional bridge round-trip (setImage in 19-02 + input out here) is proven hardware-free + real-plugin-free, closing the convergence point.
  \</success_criteria>

<output>
Create `.planning/phases/19-device-plugin-bridge/19-03-SUMMARY.md` when done — record the DeviceEvent
tap seam used (existing signal vs an added input-service signal), the encoder-as-coordinate
convention, and the willAppear scope (root page vs full multi-page) per the A6 simplification.
</output>
