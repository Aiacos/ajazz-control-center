---
phase: 12-ak980-pro-promotion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_data/devices.yaml
  - docs/protocols/keyboard/ak980pro.md
  - src/app/qml/SettingsRow.qml
  - src/app/src/settings_service.hpp
  - src/app/src/settings_service.cpp
autonomous: false
requirements:
  - DEVICES-06
deferred_followups:
  - 'Clock for ak980pro is RESOLVED (2026-05-20): KEEP per ARCH-05.1 (FINAL, opcode 0x28 RTC, setTime shipped, hasClock=true). ARCH-05.1 is the ratifying flip ARCH-05 anticipated; ARCH-05 stands for the Stream Dock family only. The remaining Phase 9.x gate is the physical round-trip witness that promotes the clock partial->functional, NOT a demotion decision.'
  - 'Build precondition (for the QML host-save-vs-device-flash UX task): Qt6 CorePrivate / qzipreader_p.h missing -> needs qt6-qtbase-private-devel. The devices.yaml + docs tasks have NO build dependency and can land independently.'

must_haves:
  truths:
    - ? The devices.yaml ak980pro row KEEPS clock per ARCH-05.1 (FINAL, 2026-05-17 — real 0x28 firmware RTC, setTime shipped); notes cite ARCH-05.1. (Conflict with ARCH-05 RESOLVED 2026-05-20
      : ARCH-05.1 is the ratifying flip ARCH-05's own escape clause anticipated; ARCH-05 stands for the Stream Dock family only.)
    - The ak980pro maturity is partial (already true) with RGB + sleep-timer in feature_summary.works and macros / layers / per-key RGB / battery honestly listed under feature_summary.pending (Pitfall 29) for v1.3+.
    - docs/protocols/keyboard/ak980pro.md exists (or is extended) documenting the cmd 0x13 RGB + cmd 0x17 sleep-timer + cmd 0x28 RTC wire formats, citing ARCH-05.1 (clock) and the capture-pending status for RGB/sleep byte detail.
    - The UI separates "Save profile" (instant, host-disk) from "Push to device" (deliberate, NVM-flash, <=1/min) so NVM wear cannot be triggered on every edit (Pitfall 25 / REQUIREMENTS Out-of-Scope "Auto-flashing ... on every UI change").
  artifacts:
    - path: docs/_data/devices.yaml
      provides: ak980pro row with clock kept per ARCH-05.1 + works/pending honesty + partial maturity
      contains: ak980pro
    - path: docs/protocols/keyboard/ak980pro.md
      provides: cmd 0x13 RGB + cmd 0x17 sleep-timer wire-format doc citing ARCH-05 default verdict
      contains: '0x17'
    - path: src/app/qml/SettingsRow.qml
      provides: distinct Save-profile (host-disk) vs Push-to-device (NVM-flash, throttled) controls
      contains: Push
  key_links:
    - from: src/app/qml/SettingsRow.qml
      to: src/app/src/settings_service.cpp
      via: Push-to-device control invokes a deliberate, <=1/min-throttled flash; Save-profile writes host disk only
      pattern: pushToDevice|flush|commit
---

<objective>
Close DEVICES-06 honesty for `ak980pro` and add the host-save-vs-device-flash UX separation. Two
deliverables: (1) an honest `devices.yaml` row keeping `clock` per ARCH-05.1 (resolved) + a
protocol doc; (2) a UI that separates instant host-disk "Save profile" from
deliberate, throttled "Push to device" NVM-flash (Pitfall 25).

Purpose: RESOLVED 2026-05-20 — `ak980pro` KEEPS `clock` per ARCH-05.1 (FINAL, 2026-05-17), which
located a real firmware RTC (opcode 0x28), shipped a working `setTime`, set `hasClock=true` in
`register.cpp`, and lists `clock` in `devices.yaml`. ARCH-05.1 is the ratifying follow-up ADR that
ARCH-05's own escape clause anticipated, so there is no live conflict: ARCH-05 stands for the Stream
Dock family only. DEVICES-06 work here is honesty bookkeeping (works/pending split), the protocol
doc, and the save-vs-flash UX — NOT a clock change.

Output: an approved `devices.yaml` ak980pro row, a `docs/protocols/keyboard/ak980pro.md` protocol
doc, and a Save-profile / Push-to-device UX split.

CONDITIONAL-EXECUTION GATES (read before executing — this is a plan-only artifact):

1. **Clock disposition RESOLVED (2026-05-20): KEEP per ARCH-05.1.** Do NOT edit the `clock`
   capability or `hasClock`/`setTime` — the as-built code is correct. No blocking checkpoint remains.
1. **Clock citation = ARCH-05.1 (FINAL).** Cite ARCH-05.1 in the devices.yaml `notes:` + the protocol
   doc + the commit body. The Phase 9.x physical witness (round-trip + negative on a physical
   0c45:8009) is now only the gate that promotes the clock `partial` -> `functional`, not a
   keep/demote decision. (RGB/sleep byte-level detail stays capture-pending separately.)
1. **Build precondition** applies only to the QML task (qt6-qtbase-private-devel). The devices.yaml +
   protocol-doc tasks are doc-only and have NO build dependency — they can land first.
1. **Wave 1, parallel-safe.** This plan touches docs + devices.yaml + settings UI; it does NOT touch
   `proprietary_keyboard.cpp` / `proprietary_protocol.hpp` / `capabilities.hpp` (the backend files
   owned by 12-01/02/03), so it runs in parallel with 12-01. SettingsRow.qml + settings_service.\* are
   also edited by 12-03 Task 2 — to avoid a same-wave file conflict, the Save-vs-Push UX (Task 3 here)
   is sequenced into a LATER wave than 12-03 if executed concurrently; if 12-03 has not started,
   coordinate ownership (this plan's SettingsRow edits are the Save/Push buttons; 12-03's are the
   sleep-timer picker — distinct regions, but flag the shared file to the orchestrator).
   </objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ak980-pro-promotion/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-research-captures-hygiene/ARCH-05.md
@docs/_data/devices.yaml
@CLAUDE.md

<interfaces>
<!-- The ak980pro devices.yaml row ALREADY EXISTS at maturity: partial with capabilities including
     clock + a notes: block citing ARCH-05.1 (the live setTime). This plan reconciles it with
     ARCH-05 per the Task 1 decision. -->

Current devices.yaml ak980pro row (abridged — note clock is PRESENT and notes cite ARCH-05.1 setTime):

```yaml
- codename: ak980pro
  family: keyboard
  vid: "0x0c45"
  pid: "0x8009"
  maturity: partial
  capabilities: [rgb, macros, layers, clock, battery]
  notes: "... ARCH-05.1 (2026-05-17) located the firmware RTC ... setTime() implemented end-to-end;
          promotion partial -> functional gates on Phase 9.x physical round-trip witness ..."
  feature_summary:
    works: [...]
    partial: [...]
    pending: [..., "20-mode RGB enum expansion", "Sleep-timer wire format (cmd 0x17 ...)"]
```

ARCH-05's original "Binding to downstream plans" said Phase 12 removes clock for ak980pro — but
ARCH-05.1 (FINAL, 2026-05-17) FLIPPED that for this device (real 0x28 firmware RTC, source-corroborated

- vendor-binary disassembly). RESOLVED 2026-05-20: ak980pro KEEPS clock per ARCH-05.1; the as-built row
> (NOTE 2026-05-20: this device — USB 0x3004 — was later firmware-confirmed to be an AKP05E, codename akp05e; see STATE.md.)

  is correct. ARCH-05 stands for the Stream Dock family (akp03_variant_3004, akp05) only.

From src/app/src/settings_service.{hpp,cpp} — the existing settings push path (ISettingsCapable
bridge). The Save-vs-Push split adds a deliberate flash invokable with a \<=1/min throttle.
</interfaces>
</context>

<tasks>

<!-- RESOLVED 2026-05-20 (consistency pass) — was a blocking checkpoint:decision.
     Clock disposition for ak980pro = KEEP, per ARCH-05.1 (FINAL, 2026-05-17).
     ARCH-05.1 is the ratifying follow-up ADR that ARCH-05's own escape clause
     anticipated ("a follow-up ADR ratifies the flip, never an in-place edit"),
     so the as-built code (hasClock=true, setTime via the 4-packet 0x28 envelope)
     is correct and must NOT be regressed. ARCH-05 stands for the Stream Dock
     family (akp03_variant_3004, akp05) only. ROADMAP/REQUIREMENTS/CONTEXT
     DEVICES-06 wording was corrected to match in the same pass. Task 2 below is
     therefore deterministic: keep clock + cite ARCH-05.1; no register.cpp change. -->

<task type="auto">
  <name>Task 1 (resolved decision record): ak980pro keeps clock per ARCH-05.1</name>
  <files></files>
  <action>
No action — the clock disposition is decided (KEEP per ARCH-05.1). This task
exists only to preserve the decision provenance for the SUMMARY. Proceed to
Task 2, which writes the honest row with clock retained and an ARCH-05.1
citation. Do NOT touch register.cpp / proprietary_keyboard.cpp clock code —
it is correct as shipped.
  </action>
  <verify><automated>grep -q "ARCH-05.1" .planning/phases/12-ak980-pro-promotion/12-04-PLAN.md</automated></verify>
  <done>Decision recorded: keep clock, cite ARCH-05.1.</done>
</task>

<task type="auto">
  <name>Task 2: Honest ak980pro devices.yaml row + ak980pro.md protocol doc (per the Task 1 decision)</name>
  <files>
    docs/_data/devices.yaml,
    docs/protocols/keyboard/ak980pro.md
  </files>
  <action>
Per DEVICES-06 + the Task 1 decision (doc-only; no build dependency):

1. Update the `ak980pro` `devices.yaml` row:
   - Maturity stays `partial` (already true; RGB + sleep-timer functional this phase).
   - `capabilities:` KEEPS `clock` (resolved per ARCH-05.1 — real 0x28 RTC, setTime shipped);
     cite ARCH-05.1 as the ratifying flip. Do NOT remove clock; do NOT touch register.cpp.
   - `feature_summary.works:` adds "20-mode firmware RGB picker with brightness/speed/direction
     (cmd 0x13, KEYBOARD-01/02)" and "dedicated cmd 0x17 sleep-timer with discrete picker
     (KEYBOARD-03)" and "wireless RGB rate-limit (\<=10 writes/sec, KEYBOARD-04)".
   - `feature_summary.pending:` honestly lists macros / layers / per-key RGB / battery for v1.3+
     (Pitfall 29) — per DEVICES-06 these stay pending, NOT advertised as functional.
   - `notes:` cites ARCH-05.1 (clock, FINAL) + the capture-pending status for the RGB/sleep
     byte-level detail. Update the existing stale pending lines ("20-mode RGB enum expansion",
     "Sleep-timer wire format ... capture per-byte layout") since those are now DONE — move to works.
1. Create/extend `docs/protocols/keyboard/ak980pro.md` documenting the wire formats this phase wired:
   cmd 0x13 RGB 5-packet envelope (mode/brightness/speed/direction byte layout), cmd 0x17 sleep-timer
   (byte1/byte2/byte9 + captured enum), and the wireless rate-limit rationale. Cite ARCH-05.1 (clock)
   - the TaxMachine corpus provenance, and mark `// CAPTURE-PENDING` where byte-level detail
     is unconfirmed against a first-party 0c45:8009 capture. Cross-reference ak980pro_vendor.md.

If the devices.yaml has an AUTOGEN README/wiki step (per the project's devices.yaml-is-doc-only
convention), do NOT hand-edit generated outputs — only the source row. No `nlohmann` (docs only).
Atomic commit: `docs(devices): honest ak980pro capabilities + protocol doc (DEVICES-06)`.
</action>
<verify>
<automated>grep -q "KEYBOARD-0" docs/\_data/devices.yaml && test -f docs/protocols/keyboard/ak980pro.md && grep -q "0x17" docs/protocols/keyboard/ak980pro.md && grep -qi "ARCH-05" docs/protocols/keyboard/ak980pro.md && grep -qi "pending" docs/\_data/devices.yaml</automated>
</verify>
<done>
The ak980pro row reflects the approved clock disposition + RGB/sleep-timer/rate-limit in works +
macros/layers/per-key/battery in pending (Pitfall 29) at partial maturity; ak980pro.md documents the
0x13 + 0x17 wire formats citing ARCH-05 default verdict.
</done>
</task>

<task type="auto">
  <name>Task 3: Host-save-vs-device-flash UX separation (Pitfall 25)</name>
  <files>
    src/app/src/settings_service.hpp,
    src/app/src/settings_service.cpp,
    src/app/qml/SettingsRow.qml
  </files>
  <action>
Per KEYBOARD-04 (UX half) + REQUIREMENTS Out-of-Scope "Auto-flashing ... on every UI change"
(Pitfall 25):

1. In `settings_service.{hpp,cpp}`, add two distinct operations: `saveProfile(codename)` — writes the
   current config to host disk (QSettings / profile JSON) INSTANTLY, no device I/O; and
   `pushToDevice(codename)` — performs the deliberate NVM flash (the existing ISettingsCapable /
   sleep-timer / lighting commits), throttled to at most one flash per 60 seconds per device (track a
   last-flash steady_clock timestamp; reject/no-op + WARN if called again within 60s). Expose both as
   Q_INVOKABLE. Add doc-comments citing Pitfall 25 (NVM wear bricks the device in ~3-8h of editing if
   every edit flashes).
1. In `SettingsRow.qml`, render TWO distinct controls: a "Save profile" button (instant, host-disk;
   always enabled) and a "Push to device" button (deliberate flash; visibly disabled / shows a
   cooldown affordance for 60s after a push). Wire them to the two service methods. Make the
   distinction unmistakable in the UI copy so a user understands editing does NOT flash NVM until they
   explicitly push. Follow CLAUDE.md QML gotchas.

This is service + QML glue. The 60s throttle is unit-testable in settings_service if a clock seam is
added (optional; mirror 12-01's injectable-clock pattern if you want a deterministic test). Atomic
commit: `feat(app): separate host-save from deliberate device-flash (Pitfall 25)`.
</action>
<verify>
<automated>grep -qE "saveProfile|pushToDevice" src/app/src/settings_service.hpp && grep -qE "pushToDevice" src/app/src/settings_service.cpp && grep -qiE "push.\*device|Push to device" src/app/qml/SettingsRow.qml && grep -qiE "save.\*profile|Save profile" src/app/qml/SettingsRow.qml</automated>
</verify>
<done>
"Save profile" (instant host-disk) and "Push to device" (deliberate NVM flash, \<=1/min) are distinct,
clearly-labeled controls; editing never auto-flashes NVM (Pitfall 25 closed in the UX).
(QML smoke gated on the build fix.)
</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                    |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| devices.yaml advertised capability -> user trust | A row that advertises a capability the device cannot honestly perform is a lie (Pitfall 19/29) |
| UI edit -> device NVM                            | Every flash wears the NVM; only deliberate, throttled flushes should reach it (Pitfall 25)     |

## STRIDE Threat Register

| Threat ID        | Category          | Component                                                                                              | Disposition | Mitigation Plan                                                                                                                                               |
| ---------------- | ----------------- | ------------------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-12-10          | Repudiation       | Catalogue could misrepresent the clock verdict if docs lag the ARCH-05.1 flip                          | mitigate    | RESOLVED 2026-05-20: ak980pro keeps clock per ARCH-05.1; devices.yaml + protocol doc cite ARCH-05.1; ROADMAP/REQUIREMENTS/CONTEXT corrected in the same pass. |
| T-12-11          | Tampering         | Advertising macros/layers/per-key RGB/battery as functional when only RGB+sleep are wired (Pitfall 29) | mitigate    | feature_summary.pending honestly lists the unbuilt capabilities; maturity stays partial, not functional (Task 2).                                             |
| T-12-12          | Denial of Service | Auto-flashing NVM on every UI edit bricks the device in ~3-8h (Pitfall 25)                             | mitigate    | Save-profile (host-disk, instant) separated from Push-to-device (NVM, \<=1/min throttle) (Task 3).                                                            |
| T-12-SC          | Tampering         | npm/pip/cargo installs                                                                                 | mitigate    | No package-manager installs (docs + QML + service glue only). No `[ASSUMED]`/`[SUS]` packages.                                                                |
| \</threat_model> |                   |                                                                                                        |             |                                                                                                                                                               |

<verification>
1. devices.yaml keeps `clock` + cites ARCH-05.1 (resolved decision); no register.cpp change.
2. devices.yaml ak980pro: partial maturity; RGB/sleep/rate-limit in works; macros/layers/per-key/battery in pending (Pitfall 29).
3. `docs/protocols/keyboard/ak980pro.md` documents cmd 0x13 + cmd 0x17 with `// CAPTURE-PENDING` markers + ARCH-05 citation.
4. SettingsRow.qml has distinct Save-profile (host-disk) + Push-to-device (<=1/min flash) controls wired to settings_service.
5. COD-031 grep gate: `grep -rn nlohmann src/core/include/` returns 0 (no core public-header change here anyway).
</verification>

\<success_criteria>

- DEVICES-06: ak980pro devices.yaml is honest (clock kept per ARCH-05.1; partial maturity; works/pending split per Pitfall 29); protocol doc added.
- The ARCH-05-vs-ARCH-05.1 clock question resolved 2026-05-20 (consistency pass): KEEP per ARCH-05.1 (the ratifying flip ARCH-05 anticipated); downstream ROADMAP/REQUIREMENTS/CONTEXT corrected to match.
- Host-save-vs-device-flash UX separation closes Pitfall 25 (no NVM auto-flash on edit).
- ARCH-05 DEFAULT VERDICT status cited throughout; atomic Conventional Commits.
  \</success_criteria>

<output>
After completion, create `.planning/phases/12-ak980-pro-promotion/12-04-SUMMARY.md` documenting:
the clock disposition (KEEP per ARCH-05.1 — resolved 2026-05-20, not an executor decision), the
honest devices.yaml row + protocol doc, the save-vs-flash UX, and a note that the ARCH-05 vs
ARCH-05.1 question was an amendment-chain (not a live conflict) — plus whether the build fix
(`qt6-qtbase-private-devel`) was available for the QML task at execution time.
</output>
