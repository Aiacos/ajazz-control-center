---
phase: 28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials
plan: 05
type: execute
wave: 5
depends_on: [28-04]
files_modified:
  - .planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-LIVE-VERIFICATION.md
autonomous: true
requirements: [PLUGIN-18, PLUGIN-19, PLUGIN-20]
must_haves:
  truths:
    - plugin.installedActions count equals the manifest visible-action count for System Monitor and Weather
    - Binding a plugin action to a dial then firing synthetic input.encoder makes the plugin receive dialRotate with controller=Encoder
    - Firing synthetic input.encoderPress makes the plugin receive dialDown
  artifacts:
    - path: .planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-LIVE-VERIFICATION.md
      provides: Captured live debug-channel transcript proving the round-trip + the installedActions diagnostic
      contains: dialRotate
  key_links:
    - from: scripts/ajazz-debug input.encoder
      to: plugin dialRotate event
      via: ContextRegistry lookup after drop
      pattern: dialRotate
---

<objective>
Prove the whole phase LIVE through the debug channel — the real definition-of-done per CLAUDE.md ("ctest green is necessary but NOT sufficient"). Confirm (a) PLUGIN-18: `plugin.installedActions` count matches each manifest's visible-action count for System Monitor + Weather, and (b) PLUGIN-19/20: a plugin action bound to a dial receives `dialRotate`/`dialDown` with `controller:"Encoder"` when a synthetic `input.encoder` fires.

Purpose: Unit tests passed in Plans 01-04 but integration/wiring bugs are invisible to ctest (the Phase-27 "Allow" button passed 713 tests and a code review yet was a live no-op). This plan drives the running app and reads the actual behavior.
Output: A committed `28-LIVE-VERIFICATION.md` transcript capturing the diagnostic counts and the dial round-trip evidence.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-RESEARCH.md
@.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-04-SUMMARY.md

<interfaces>
<!-- Debug RPCs (all present after Plan 02 / commit 825eccd): -->
<!-- plugin.list, plugin.installedActions (NEW Plan 02), plugin.installFromFile, plugin.rediscover -->
<!-- input.encoder {index,delta}, input.encoderPress {index}, input.key, input.touch -->
<!-- state, log.tail, screenshot, qml.tree/get/set/invoke/click -->
<!-- System Monitor installed at ~/.local/share/Aiacos/AJAZZ Control Center/plugins/178366994579015.sdPlugin (13 actions) -->
<!-- Weather corpus: ~/MEGAsync/ajazz-reverse-engineering/raw-workdir/sd-app/defaultData/defaultPlugins/com.mirabox.streamdock.weather.sdPlugin (1 action) -->
<!-- Harness gap (CLAUDE.md): qml.invoke toggle/click does NOT fire onToggled; to set a binding without a real UI drag, drive ProfileController.commitEncoderBinding via qml.invoke OR the backing Profile + relaunch -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PLUGIN-18 live diagnostic — installedActions count vs manifest visible count for System Monitor + Weather</name>
  <files>.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-LIVE-VERIFICATION.md</files>
  <read_first>
    - 28-RESEARCH.md §Repro Plugins + §Diagnostic Design for PLUGIN-18 + the live diagnostic sequence
    - CLAUDE.md "Debug-channel verification — MANDATORY" (build -> launch AJAZZ_DEBUG_CONTROL=1 -> drive -> screenshot -> read)
    - CLAUDE.md launch hygiene (plain nohup, kill by exact PID, never pkill -f)
  </read_first>
  <action>
    Build the app (build/linux-release). Launch with AJAZZ_DEBUG_CONTROL=1 via nohup, capture the exact PID. Install Weather from the corpus. IMPORTANT prerequisite: `plugin.installFromFile` opens its path with QFile::ReadOnly and expects a ZIP archive, NOT a directory — the corpus `com.mirabox.streamdock.weather.sdPlugin` is a directory. So FIRST stage it one of two ways: (a) zip it — `cd <corpus parent> && zip -r /tmp/weather.sdPlugin.zip com.mirabox.streamdock.weather.sdPlugin` — then `scripts/ajazz-debug plugin.installFromFile` with path=/tmp/weather.sdPlugin.zip (the installer strips .zip and ensures the .sdPlugin suffix per commit 50055a3); OR (b) copy the directory straight into the plugins dir (the dir `plugin.list`/discover scans) and skip installFromFile. EITHER way, then call `scripts/ajazz-debug plugin.rediscover` (spawns the newly-installed plugin with no app restart). Call scripts/ajazz-debug plugin.installedActions and capture the JSON. Independently count each manifest's visible actions: System Monitor Actions[] minus any VisibleInActionsList:false (expected 13 visible), Weather (expected 1 visible). Assert installedActions count for these two plugins equals their visible-action count, and that the diagnostics block reports hiddenByVisibility / skippedUuidName / skippedParseFailure. If the emoji vendor plugin (2 actions, 1 hidden) is installed, confirm only its 1 visible action surfaces; if not installed, note that. Take a screenshot of the action library and READ it to confirm the visible rows. Kill the app by exact PID. Record every command + output verbatim in 28-LIVE-VERIFICATION.md under a "PLUGIN-18 diagnostic" section, including the manifest-vs-installedActions comparison and a PASS/FAIL verdict.
  </action>
  <verify>
    <automated>F=.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-LIVE-VERIFICATION.md; test -f "$F" && grep -q 'PLUGIN-18' "$F" && grep -qiE 'installedActions|visible' "$F" && echo P18_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - 28-LIVE-VERIFICATION.md contains the verbatim plugin.installedActions JSON output.
    - It states System Monitor installedActions count == 13 (visible) and Weather == 1, with an explicit PASS/FAIL.
    - The diagnostics block (hiddenByVisibility / skippedUuidName / skippedParseFailure) is captured.
    - A screenshot path is referenced and its readout (which rows are visible) is described.
    - App was launched and killed by exact PID, not pkill -f.
  </acceptance_criteria>
  <done>Live installedActions count matches manifest visible count for both repro plugins; diagnostic counts captured; verdict recorded.</done>
</task>

<task type="auto">
  <name>Task 2: PLUGIN-19/20 live round-trip — bind plugin action to a dial, fire synthetic encoder, assert plugin receives dialRotate/dialDown</name>
  <files>.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-LIVE-VERIFICATION.md</files>
  <read_first>
    - 28-RESEARCH.md §Debug-channel live verification sequence (Steps 1-7) + §ActionContext Registration Gap
    - memory project_akp05_autonomous_input_test (synthetic input.encoder/encoderPress route through the full pipeline; no input hardware needed)
    - CLAUDE.md harness gap (qml.invoke toggle/click does not fire onToggled — to set a binding drive a Q_INVOKABLE such as commitEncoderBinding or the backing store)
  </read_first>
  <action>
    With the app running under AJAZZ_DEBUG_CONTROL=1 and System Monitor connected (confirm via scripts/ajazz-debug plugin.list), bind a System Monitor action to encoder 0. Because the harness cannot reproduce a real mouse drag, drive the binding via scripts/ajazz-debug qml.invoke on ProfileController.commitEncoderBinding with index 0, empty icon, label "CPU", the plugin actionKind, empty settings, and the actual System Monitor dial-capable action UUID confirmed from plugin.installedActions (e.g. com.hotspot.stream.cpu). This emits profileChanged and, via Plan 04, registers the ActionContext. Confirm persistence via scripts/ajazz-debug state (encoder[0].onPress id == the UUID). Then fire scripts/ajazz-debug input.encoder with index 0 delta 1, and scripts/ajazz-debug input.encoderPress with index 0. Capture scripts/ajazz-debug log.tail showing the plugin received a dialRotate event with payload ticks 1 and controller "Encoder", and a dialDown event. If the plugin does not log receipt, use the plugin server outbound-event log to confirm the event was dispatched to the plugin socket. Take a screenshot. Record the full command+output transcript and a PASS/FAIL verdict in 28-LIVE-VERIFICATION.md under "PLUGIN-19/20 dial round-trip". If the round-trip FAILS, capture the exact failure point (binding not persisted / context not registered / event not dispatched) as the gap-closure signal for a follow-up.
  </action>
  <verify>
    <automated>F=.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-LIVE-VERIFICATION.md; grep -q 'dialRotate' "$F" && grep -qiE 'controller.*Encoder' "$F" && grep -qiE 'PASS|FAIL' "$F" && echo P19_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - 28-LIVE-VERIFICATION.md captures the verbatim state output showing encoder[0] onPress id == the bound plugin UUID (persistence proven live).
    - It captures log/event evidence of dialRotate with controller "Encoder" and of dialDown after the synthetic input.
    - An explicit PASS/FAIL verdict is recorded; on FAIL the exact broken stage is named.
    - A screenshot path is referenced.
  </acceptance_criteria>
  <done>Live: a plugin bound to a dial receives dialRotate/dialDown with controller=Encoder on synthetic input; binding persistence confirmed via state; verdict recorded.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                           |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------- |
| debug channel -> running app        | All RPCs gated behind AJAZZ_DEBUG_CONTROL=1 on the local-only debug socket                            |
| corpus .sdPlugin -> installFromFile | Weather is installed from a local corpus dir through the existing trust/zip-slip-guarded install path |

## STRIDE Threat Register

| Threat ID        | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| ---------------- | ---------------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28-12          | Tampering              | Weather install from corpus             | mitigate    | Install goes through the existing Phase-27 verifyStagedPlugin path (tampered always refused; zip-slip guarded). Weather corpus dir is unsigned-but-trusted local content; install with the standard consent path, not a bypass. |
| T-28-13          | Information Disclosure | log.tail / state transcripts in the doc | accept      | Transcript captures local plugin event payloads only (no secrets); committed to the planning dir alongside the other phase summary artifacts.                                                                                   |
| T-28-SC          | Tampering              | npm/pip/cargo installs                  | mitigate    | No package installs (debug-channel drive of an already-built app). slopcheck N/A.                                                                                                                                               |
| \</threat_model> |                        |                                         |             |                                                                                                                                                                                                                                 |

<verification>
- 28-LIVE-VERIFICATION.md exists, committed, with both PLUGIN-18 and PLUGIN-19/20 sections and explicit verdicts.
- Full suite confirmation: `ctest --preset linux-release -E qml` green before declaring the phase done (run as the phase gate).
- `grep -rn nlohmann src/core/include/` returns 0 (COD-031).
</verification>

\<success_criteria>

- PLUGIN-18 proven live: installedActions count == manifest visible count for System Monitor + Weather, diagnostic counts captured.
- PLUGIN-19/20 proven live: a dial-bound plugin action receives dialRotate/dialDown with controller=Encoder on synthetic input; binding persists.
- A FAIL at any stage is recorded with the exact broken stage (gap-closure input), not silently passed.
  \</success_criteria>

<output>
Create `.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-05-SUMMARY.md` when done
</output>
