---
phase: 32-binding-layer-fix-multi-toggle-action-device-editor
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_plugin_device_bridge.cpp
autonomous: false
requirements: [BIND-03, BIND-06]
must_haves:
  truths:
    - The willAppear envelope carries the top-level action field for key, encoder, and touch contexts
    - A stored-owner action (manifest map, not dotted prefix) resolves to its owner plugin
    - Key, encoder-dial, and touch-zone events each resolve a pre-registered context with no SKU branch
    - A drag-to-bind commit fires willAppear at the plugin process within the same interaction (no reconnect)
  artifacts:
    - path: tests/unit/test_plugin_device_bridge.cpp
      provides: 'regression tests: envelope.action present; stored-owner resolves; per-controller-type routing'
      contains: action
  key_links:
    - from: tests/unit/test_plugin_device_bridge.cpp
      to: eventEnvelope
      via: asserts the willAppear/keyDown envelope includes the action field
      pattern: '"action"'
    - from: tests/unit/test_plugin_device_bridge.cpp
      to: resolveOwner / onDeviceEvent
      via: parametrized key/encoder/touch context resolution
      pattern: Encoder|Keypad
---

<objective>
Lock BIND-03 and BIND-06 with regression tests + live debug-channel verification. Both are
ALREADY IMPLEMENTED (RESEARCH Findings 1 + 4): the willAppear envelope carries `action`
(plugin_device_bridge.cpp:337, commit ddabc16); owner-match uses the stored-owner map via
`resolveOwner` (:851-863, commit 519ecd0); `onDeviceEvent` dispatch switches on control TYPE
with zero SKU branching. This plan is VERIFY + REGRESSION-TEST, NOT a rewrite. Do NOT touch
eventEnvelope, resolveOwner, or onDeviceEvent.

Purpose: prove the binding layer fires plugins in-interaction and is device-generic, and pin
that behavior so a future ad-hoc commit on this heavily-churned branch cannot silently regress it.
Output: extended Catch2 bridge tests + a recorded live debug-channel verification.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-RESEARCH.md

# Read-only anchors (already-shipped code — DO NOT MODIFY, only assert against):

# eventEnvelope — src/app/src/plugin_device_bridge.cpp:333-342 ("action":ctx.actionUUID at :337)

# resolveOwner — src/app/src/plugin_device_bridge.cpp:851-863 (stored-owner map first)

# onDeviceEvent (generic) — src/app/src/plugin_device_bridge.cpp:865-1004 (switch on ev.kind; byCoord; no SKU)

# willAppear emission sites— key :1081, encoder :1124, touch :1173

</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression tests — envelope.action present + stored-owner resolves + per-controller-type routing</name>
  <read_first>
    tests/unit/test_plugin_device_bridge.cpp (full — existing fixture + helpers to reuse);
    src/app/src/plugin_device_bridge.cpp:310-342 (instancePayload + eventEnvelope), :851-863 (resolveOwner),
    :865-1004 (onDeviceEvent switch), :1073-1135 (context registration coords incl. touch-zone under
    controller="Encoder",row=0,column=zoneIndex — a TYPE convention, not SKU code).
  </read_first>
  <action>
    Extend the [plugin_device_bridge] suite with regression cases that DO NOT modify production code:
    (1) assert a willAppear (or keyDown) envelope built for a registered context includes a non-empty
    top-level "action" field equal to the context's actionUUID (guards Hypothesis-A regression);
    (2) assert that an action whose owner is recorded ONLY via the stored-owner resolver (NOT a dotted
    prefix of the plugin UUID) resolves to the owner plugin through resolveOwner (guards Hypothesis-B
    regression — inject a fake m_actionOwnerResolver via the existing fixture seam);
    (3) a parametrized-by-controller-type case (Keypad key, Encoder dial, Encoder/row0 touch-zone) that
    pre-registers a context for each and asserts the corresponding synthetic DeviceEvent (KeyPressed,
    EncoderTurned, TouchUp) resolves THAT context and emits an outbound event to the right plugin uuid,
    with no SKU/codename string anywhere in the path. Reuse the existing fixture's context-registration
    and event-injection helpers; do not introduce a new harness. ASCII-only TEST_CASE names.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R plugin_device --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R plugin_device --output-on-failure` exits 0 with the new cases counted.
    - `grep -c '"action"' tests/unit/test_plugin_device_bridge.cpp` > 0 (envelope.action assertion present).
    - The new parametrized case names contain `Keypad`, `Encoder`, and a touch-zone assertion (grep confirms all three controller types are exercised).
    - `git diff --stat src/app/src/plugin_device_bridge.cpp` shows NO changes (verify-only; production code untouched).
  </acceptance_criteria>
  <done>Bridge suite green with new regression cases asserting envelope.action, stored-owner resolution, and per-controller-type routing; no production code changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Regression tests pinning the already-shipped BIND-03 (in-interaction willAppear) and BIND-06
    (device-generic key/encoder/touch routing) behavior. This checkpoint performs the MANDATORY live
    debug-channel verification (ctest is necessary but NOT sufficient — CLAUDE.md).
  </what-built>
  <how-to-verify>
    1. Build: `ctest --preset linux-release` (or just build the app target).
    2. Kill any running instance + stale socket, then launch headless:
       `AJAZZ_DEBUG_CONTROL=1 AJAZZ_ALLOW_UNTRUSTED_PLUGINS=1 nohup build/linux-release/src/app/ajazz-control-center &`
    3. Install + bind a REAL plugin action (System Monitor — NOT a built-in; built-ins have no plugin
       process so willAppear is correctly skipped, RESEARCH Finding 1). Use the documented install+bind
       recipe (auto-memory project_plugin_install_demo_working).
    4. Drag the action onto a key via `scripts/ajazz-debug qml.drag` (or commit the binding), then:
       `scripts/ajazz-debug plugin.protocolLog` — confirm a `willAppear` line arrives within the SAME
       interaction, WITHOUT any disconnect/reconnect in the log.
    5. Route all three controller types through the full pipeline:
       `scripts/ajazz-debug input.key {index,pressed}`, `input.encoder`, `input.touch` — confirm
       `plugin.protocolLog` shows keyDown / dialRotate / touchTap reaching the registered context each
       time, with no reconnect.
    Note (RESEARCH Finding 1 caveat): `populateContextsForActivePage` hardcodes 5 key columns — use an
    AKP05E-class (5-col) device for verification; flag non-5-col SKUs as a known limitation, out of scope.
  </how-to-verify>
  <resume-signal>Type "approved" once protocolLog shows in-interaction willAppear + all three controller types routing without reconnect, or describe the failure.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                | Description                                               |
| --------------------------------------- | --------------------------------------------------------- |
| device event -> plugin context dispatch | synthetic/real input resolves a registered plugin context |
| built-in action -> plugin-owned context | a built-in must not drive a context it does not own       |

## STRIDE Threat Register

| Threat ID        | Category  | Component                  | Disposition | Mitigation Plan                                                                                                               |
| ---------------- | --------- | -------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-32-03          | Tampering | cross-plugin context drive | accept      | existing ctx.pluginUuid ownership guard (plugin_device_bridge.cpp:573-575, T-19-xplugin) unchanged; this plan only adds tests |
| T-32-SC          | Tampering | npm/pip/cargo installs     | mitigate    | none — zero new dependencies this plan                                                                                        |
| \</threat_model> |           |                            |             |                                                                                                                               |

<verification>
- `ctest --preset linux-release -R plugin_device` green.
- Live: `plugin.protocolLog` shows in-interaction willAppear (no reconnect) for a real plugin action.
- Live: `input.key`/`input.encoder`/`input.touch` each route to their registered context.
</verification>

\<success_criteria>

- Envelope carries `action`; stored-owner resolves; key/encoder/touch route generically — all pinned by tests.
- Live debug-channel verification confirms in-interaction willAppear and three-controller routing with no reconnect.
  \</success_criteria>

<output>
Create `.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-04-SUMMARY.md` when done.
</output>
