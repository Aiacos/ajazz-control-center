---
phase: 31-actioninstance-core-model-profile-schema-v2
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - tests/unit/test_action_instance.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [BIND-02]
user_setup: []

must_haves:
  truths:
    - ctest --preset linux-release -R action_instance runs the new tests and exits 0
    - Round-trip is verified for 0-state, 1-state, 3-state, and 2-children ActionInstance variants
    - 'A legacy singular state: inside an instance folds to a states[] array of one and re-serializes as states[]'
    - A binding with no instance key parses to instance == std::nullopt on both Binding and EncoderBinding
    - An out-of-range currentState clamps to 0 on read (lossless load)
    - Existing profile-serialization tests stay green (no v1 regression)
  artifacts:
    - path: tests/unit/test_action_instance.cpp
      provides: '[action_instance]-tagged Catch2 round-trip + migration + nullopt + clamp tests'
      contains: TEST_CASE
    - path: tests/unit/CMakeLists.txt
      provides: test_action_instance.cpp registered in the ajazz_unit_tests source list
      contains: test_action_instance.cpp
  key_links:
    - from: tests/unit/test_action_instance.cpp
      to: profileToJson / profileFromJson
      via: round-trip assertions through the public profile API
      pattern: profileFromJson\(profileToJson
    - from: tests/unit/CMakeLists.txt
      to: ajazz_unit_tests target
      via: appended source-list entry
      pattern: test_action_instance.cpp
---

<objective>
Add the BIND-02 verification: a new [action_instance]-tagged Catch2 test file covering the
ActionInstance round-trip variants (0/1/3-state + 2-children), the lazy v1->v2 singular-state fold,
the no-instance -> std::nullopt contract on both Binding and EncoderBinding, and the currentState
out-of-range clamp; registered as a runnable `ctest --preset linux-release -R action_instance` target.

Purpose: Prove the model serializer from 31-01 round-trips losslessly and migrates legacy profiles
without error (success criteria 2, 3, 4) before Phase 32 builds dispatch on top of it.

Output: tests/unit/test_action_instance.cpp (new), tests/unit/CMakeLists.txt (one appended source line).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-actioninstance-core-model-profile-schema-v2/31-RESEARCH.md
@.planning/phases/31-actioninstance-core-model-profile-schema-v2/31-VALIDATION.md

<interfaces>
<!-- The model + serializer this plan tests, delivered by 31-01. -->
From src/core/include/ajazz/core/action_instance.hpp (31-01):
```cpp
struct ActionState { KeyState visual; };
struct ActionInstance {
    std::string id;
    std::vector<ActionState> states;
    std::uint32_t currentState{0};
    std::string settings;            // escaped JSON string
    std::vector<ActionInstance> children;
};
```
From src/core/include/ajazz/core/profile.hpp:
```cpp
struct Binding        { ...; KeyState state; std::optional<ActionInstance> instance; };
struct EncoderBinding { ...; KeyState state; std::optional<ActionInstance> instance; };
std::string profileToJson(Profile const&);
Profile     profileFromJson(std::string_view);
```
Catch2 round-trip idiom (mirror tests/unit/test_profile_serialization.cpp): build a Profile, run
`profileFromJson(profileToJson(p))`, REQUIRE on the restored fields. KeyState's text is
`std::optional<std::string>` — assert via `.value()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the [action_instance] round-trip + migration + nullopt + clamp tests</name>
  <files>tests/unit/test_action_instance.cpp</files>
  <read_first>
    - tests/unit/test_profile_serialization.cpp (round-trip idiom ~line 43-132; the v1 touchZones migration test ~line 202-222 — the exact template for the singular-state migration test; ordering-regression idiom)
    - src/core/include/ajazz/core/action_instance.hpp (the struct shapes — delivered by 31-01)
    - src/core/include/ajazz/core/profile.hpp (Binding/EncoderBinding instance field, profileToJson/profileFromJson decls)
    - .planning/phases/31-actioninstance-core-model-profile-schema-v2/31-RESEARCH.md (the "Required round-trip variants" list + test skeleton, lines ~328-368)
  </read_first>
  <behavior>
    - 0-state round-trip: ActionInstance with empty states on a key Binding -> restored
      instance.has_value(), states empty, currentState == 0; serialized JSON contains `"states":[]`.
    - 1-state round-trip: one ActionState with a set visual.text -> restored states.size()==1, text matches.
    - 3-state round-trip: three states, currentState==2, settings==R"({"x":1})" ->
      restored states.size()==3, currentState==2, settings round-trips byte-equal, states[1].visual.text matches.
    - 2-children round-trip: instance.children has 2 child ActionInstances (each with its own state)
      -> restored children.size()==2 and each child round-trips (recursion verified).
    - v1->v2 migration: parse a LITERAL legacy JSON profile string whose binding instance carries a
      singular `"state":{...}` (NOT `"states"`) -> restored instance.states.size()==1; then re-serialize
      the restored profile and assert the output contains `"states":[` (writer emits array form).
      Mirror test_profile_serialization.cpp's touchZones v1-migration test structure.
    - no instance -> nullopt: parse a binding JSON with no "instance" key -> restored
      keys.at(k).instance == std::nullopt; and an encoder with no instance -> encoders.at(e).instance == std::nullopt.
    - currentState clamp: parse an instance literal with `"currentState":99` and 2 states ->
      restored currentState == 0 (lossless defensive read).
  </behavior>
  <action>
    Create tests/unit/test_action_instance.cpp beginning with the SPDX header line, then
    `#include "ajazz/core/action_instance.hpp"`, `#include "ajazz/core/profile.hpp"`, and
    `#include <catch2/catch_test_macros.hpp>`. Write one TEST_CASE per behavior above (or grouped
    SECTIONs), each tagged `[action_instance]` (plus a secondary tag like `[roundtrip]` /
    `[migration]` as helpful). Use the `profileFromJson(profileToJson(p))` round-trip idiom for the
    constructed-Profile cases and literal JSON string inputs for the migration / nullopt / clamp
    cases (so the legacy singular `"state"` and the out-of-range index are exercised directly on the
    reader). Place the instance on a key Binding via `p.keys[0]` and assert through
    `restored.keys.at(0).instance`; cover the EncoderBinding nullopt case via `p.encoders[0]`.

```
ASCII-ONLY test titles — use `-` and `->` only, never em-dash/arrow (Win32 CMD codepage
mangles them and Catch2's filter stops matching — CLAUDE.md cross-platform rule). Keep all
REQUIRE messages ASCII.

Do NOT add a new add_executable or target_link_libraries here — registration is Task 2
(the file joins the monolithic ajazz_unit_tests target).
```

</action>
  <verify>
    <automated>test -f tests/unit/test_action_instance.cpp && grep -c '\[action_instance\]' tests/unit/test_action_instance.cpp && grep -c 'profileFromJson(profileToJson' tests/unit/test_action_instance.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/test_action_instance.cpp` succeeds.
    - `grep -c '\[action_instance\]' tests/unit/test_action_instance.cpp` >= 4 (multiple tagged cases).
    - File exercises all seven behaviors: grep for the literal substrings `"states":[]` (0-state),
      `currentState` (3-state + clamp), `children` (2-children), singular legacy `"state":` literal
      (migration), `std::nullopt` or `== std::nullopt` (nullopt case), `currentState":99` or `99` (clamp).
    - Test titles are ASCII-only: `grep -nP '[^\x00-\x7F]' tests/unit/test_action_instance.cpp` returns no lines.
    - No new `add_executable` / `target_link_libraries` introduced in this .cpp (it is a test source, not a build script).
  </acceptance_criteria>
  <done>test_action_instance.cpp exists with [action_instance]-tagged TEST_CASEs covering 0/1/3-state, 2-children, v1->v2 singular-state fold, no-instance->nullopt (Binding+EncoderBinding), and currentState clamp; all titles ASCII-only.</done>
</task>

<task type="auto">
  <name>Task 2: Register the test source and run the suite green</name>
  <files>tests/unit/CMakeLists.txt</files>
  <read_first>
    - tests/unit/CMakeLists.txt lines 170-190 (the ajazz_unit_tests add_executable source list near test_profile_serialization.cpp / test_profile_multiaction.cpp at ~line 184; ajazz::core + Catch2 already linked ~line 421; catch_discover_tests ~line 679)
  </read_first>
  <action>
    Append `test_action_instance.cpp` to the ajazz_unit_tests source list in tests/unit/CMakeLists.txt,
    placed next to the sibling profile tests (after the `test_profile_multiaction.cpp` entry, ~line 184).
    Precede it with a short comment: `# Phase 31 (BIND-01/BIND-02): ActionInstance / ActionState core
    model + Profile schema v2 round-trip + v1->v2 migration tests (pure-core, no Qt/event loop).
    Runnable via ctest --preset linux-release -R action_instance.` No new target, no new
    target_link_libraries / target_sources block — ajazz::core (carrying profile.cpp +
    action_instance.hpp) and Catch2::Catch2WithMain are already linked; catch_discover_tests
    auto-registers each TEST_CASE by name so `-R action_instance` matches.

```
Then build and run: reconfigure if needed, build ajazz_unit_tests, and run the quick filter.
If any test fails, fix the TEST against the 31-01 serializer behavior (do NOT change the
serializer here — if a genuine serializer defect is found, record it in the SUMMARY as a finding
for 31-01 follow-up; the expected outcome is green). Pre-commit may reformat CMakeLists.txt
(cmake-format) — re-add and re-commit if so; NEVER use --no-verify.
```

</action>
  <verify>
    <automated>grep -c 'test_action_instance.cpp' tests/unit/CMakeLists.txt && ctest --preset linux-release -R action_instance --output-on-failure; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test_action_instance.cpp' tests/unit/CMakeLists.txt` == 1 (single source-list entry, no duplicate target).
    - `ctest --preset linux-release -R action_instance` exits 0 and matches >= 1 test (not "No tests were found").
    - Per-wave regression guard: `ctest --preset linux-release -R "profile|action_instance"` exits 0 (existing profile-serialization tests still green — no v1 regression, BIND-02 backward-compat).
    - `grep -rn '#include.*nlohmann' src/core/include/` still returns 0 lines (boundary held across the whole phase; gate on includes, not on the legitimate `nlohmann::json` COD-031 boundary comments in builtin_action_registry.hpp / input_synthesizer.hpp).
  </acceptance_criteria>
  <done>test_action_instance.cpp registered in ajazz_unit_tests; `ctest --preset linux-release -R action_instance` exits 0; `-R "profile|action_instance"` stays green (no v1 regression); `#include.*nlohmann` grep 0 in src/core/include/.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                                            | Description                                                                                                                                                                               |
| ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| literal legacy/hostile profile JSON -> profileFromJson() (in tests) | The migration/clamp tests feed crafted legacy and out-of-range JSON across the reader boundary to assert defensive behavior. No new production trust boundary is introduced by this plan. |

## STRIDE Threat Register

| Threat ID        | Category  | Component                         | Disposition | Mitigation Plan                                                                                                                                                          |
| ---------------- | --------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-31-04          | Tampering | currentState out-of-range on read | mitigate    | The clamp test (Task 1) is the regression guard proving `currentState:99` clamps to 0 — locks in the 31-01 defensive read so a future change cannot silently regress it. |
| T-31-05          | Tampering | legacy singular state mis-parse   | mitigate    | The migration test asserts the fold to states[] of one and re-emission as states[], guarding the BIND-02 lossless-load contract.                                         |
| T-31-SC          | Tampering | npm/pip/cargo installs            | n/a         | No package-manager installs (Catch2 already vendored/linked). No legitimacy gate required.                                                                               |
| \</threat_model> |           |                                   |             |                                                                                                                                                                          |

<verification>
- `ctest --preset linux-release -R action_instance` exits 0 (success criterion 2).
- The migration test proves singular `state:` -> `states[]` of one (success criterion 3).
- The nullopt tests prove no-instance -> std::nullopt on Binding + EncoderBinding (success criterion 4).
- `ctest --preset linux-release -R "profile|action_instance"` stays green (no v1 regression).
- Phase gate (run before /gsd:verify-work): full `ctest --preset linux-release` green.
</verification>

\<success_criteria>

- test_action_instance.cpp covers 0/1/3-state + 2-children round-trip, v1->v2 fold, nullopt (Binding+EncoderBinding), currentState clamp; ASCII-only titles.
- Registered in ajazz_unit_tests; `-R action_instance` exits 0.
- No v1 profile-serialization regression; `#include.*nlohmann` grep in src/core/include/ returns 0.
  \</success_criteria>

<output>
Create `.planning/phases/31-actioninstance-core-model-profile-schema-v2/31-02-SUMMARY.md` when done.
</output>
