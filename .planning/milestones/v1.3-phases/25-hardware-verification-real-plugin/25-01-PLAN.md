---
phase: 25-hardware-verification-real-plugin
plan: 01
type: execute
wave: 1
depends_on: [14-01, 15-01, 16-01, 17-01, 18-01, 19-01, 20-01, 21-01, 22-01, 23-01]
files_modified:
  - .planning/phases/25-hardware-verification-real-plugin/25-UAT.md
  - src/devices/streamdeck/src/register.cpp
autonomous: true
requirements: [VERIFY-05, VERIFY-06]
must_haves:
  truths:
    - The full Phase 14-24 ctest suite is green (the slice exists and is buildable)
    - 25-UAT.md exists with a walkable per-criterion VERIFY-05 checklist (image-on-key, key press, 4 encoders CW/CCW/press, touch tap-zone, swipe-page, brightness slider, clear-all)
    - 25-UAT.md has confirm-or-correct rows for every provisional §5 item (DRA rect header, ENC-LCD-vs-touch-strip-zone framing, touch zone+swipe map) and a hasClock=false row
    - 25-UAT.md has a VERIFY-06 real-.sdPlugin round-trip section (register over loopback WS, setImage paints a key, physical press/turn delivers keyDown/dialRotate with observable effect)
    - register.cpp advertises hasClock=false for the akp05e descriptor row (no Sync button)
  artifacts:
    - path: .planning/phases/25-hardware-verification-real-plugin/25-UAT.md
      provides: Operator runbook for VERIFY-05 + VERIFY-06 + provisional-§5 reconciliation
      contains: VERIFY-05
    - path: src/devices/streamdeck/src/register.cpp
      provides: akp05e descriptor with hasClock=false
      contains: akp05e
  key_links:
    - from: 25-UAT.md
      to: docs/protocols/streamdeck/akp05.md
      via: provisional-§5 confirm-or-correct rows reference the RE doc sections to update on mismatch
      pattern: akp05
---

<objective>
Author the `25-UAT.md` operator runbook (the load-bearing deliverable of this phase) and land the
one deterministic code correction that does not need hardware: `hasClock=false` for the `akp05e`
descriptor row. Establish the automated precondition for the operator walk — the full Phase 14-24
ctest suite must be green before the hardware UAT begins.

Purpose: Phase 25 is the HARDWARE-GATED milestone close-out. The runbook is what the human operator
executes against the physically connected AKP05E (VERIFY-05, VERIFY-06). This plan produces that
runbook autonomously and fixes the hasClock honesty regression so the AKP05E row never advertises a
Sync button it cannot back.
Output: `25-UAT.md` runbook + a `register.cpp` correction; suite-green confirmed.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-hardware-verification-real-plugin/25-CONTEXT.md
@.planning/phases/25-hardware-verification-real-plugin/25-VALIDATION.md

\<read_first>

- `.planning/phases/10-akp05e-3004-promotion/10-UAT.md` — the UAT runbook style to mirror (per-test
  expected/result blocks; ASCII names; per-criterion pass/fail). Reuse this structure.
- `docs/protocols/streamdeck/akp05.md` — §5 / touch-strip table (4 touch zones, tap zone index 0..3,
  swipe-left/right gesture codes, the encoder-overlay framing) — the provisional items.
- `docs/protocols/streamdeck/akp05_vendor.md` §4 + the `DRA` row (BE16 rect.width/height/x/y, 32-byte
  header packet, location byte) — the DRA rect-header layout to confirm-or-correct.
- `src/devices/streamdeck/src/register.cpp` — the `akp05e` (`codename = "akp05e"`) descriptor row,
  currently inheriting the family `hasClock=true` default (A-03/D-03 comment); must be `false`.
- `.planning/phases/09-research-captures-hygiene/ARCH-05.md` — the ADR that mandates hasClock=false
  for the Stream Dock family (no firmware RTC).
- `CLAUDE.md` — Linux device-access section (uaccess / setfacl / systemd ≥258 replug); RE is the
  source of truth BUT the hardware wins on provisional §5 values (update the RE doc + code on
  mismatch); D-02 honesty contract; ASCII-only test names.
  \</read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Correct akp05e hasClock to false in register.cpp</name>
  <files>src/devices/streamdeck/src/register.cpp</files>
  <read_first>
    - `src/devices/streamdeck/src/register.cpp` — locate the descriptor row where `codename = "akp05e"`.
      The row currently inherits the family `hasClock = true` (A-03/D-03 comment block at lines ~117-181).
    - `.planning/phases/09-research-captures-hygiene/ARCH-05.md` — the ADR mandating hasClock=false on
      the Stream Dock family (no firmware RTC; setTime stays NotImplemented).
  </read_first>
  <action>
    Set `hasClock = false` on the `akp05e` descriptor row (DEVICES-11) per ARCH-05 and CONTEXT.md
    honesty decision. The Stream Dock family has no firmware RTC, so the AKP05E row must NOT advertise
    `Capability::Clock` — this is what removes the Sync button from the AKP05E sidebar row and closes
    Phase-10 UAT #6. Update the inline A-03/D-03 comment on that specific row to record the
    hasClock=false correction and cite ARCH-05 (do not edit other family rows; this correction is
    scoped to akp05e per the locked Phase-25 decision). If a test fixture or descriptor-table test
    asserts `hasClock` for akp05e, update its expectation to false. Do NOT add a new RTC/setTime code
    path — this is a capability-flag correction, not a feature.
  </action>
  <verify>
    <automated>grep -n 'codename = "akp05e"' -A 12 src/devices/streamdeck/src/register.cpp | grep -v '^#' | grep -c 'hasClock = false'</automated>
  </verify>
  <acceptance_criteria>
    - The `akp05e` descriptor row sets `hasClock = false`.
    - No other descriptor row's `hasClock` value changed.
    - The build compiles and `ctest --preset linux-release` is green (no descriptor-table test asserts
      hasClock=true for akp05e).
  </acceptance_criteria>
  <done>akp05e advertises hasClock=false; suite green; the Sync button no longer appears on the AKP05E row.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 25-UAT.md operator runbook</name>
  <files>.planning/phases/25-hardware-verification-real-plugin/25-UAT.md</files>
  <read_first>
    - `.planning/phases/10-akp05e-3004-promotion/10-UAT.md` — mirror this structure: a `## Current Test`
      pointer, then numbered `### N. <name>` blocks each with `expected:` and `result: [pending]`, and a
      `## Summary` at the end. ASCII-only names.
    - `docs/protocols/streamdeck/akp05.md` (touch-strip zones, tap zone index 0..3, swipe gesture codes,
      encoder-overlay framing) and `akp05_vendor.md` §4 / `DRA` row (rect header BE16 fields) — to write
      the confirm-or-correct rows precisely.
    - `CLAUDE.md` Linux device-access section — to write the pre-flight ACL step (replug or
      `setfacl -m u:$(id -u):rw /dev/hidraw*` if root-only after re-enumeration; systemd ≥258).
  </read_first>
  <action>
    Create `25-UAT.md` as the operator runbook the human walks against the connected AKP05E
    (`0300:3004`, fw `V3.AKP05E.01.007`). Use the 10-UAT.md format (numbered tests, `expected:` +
    `result: [pending]`, ASCII names, `## Summary`). Include a `## Pre-flight` section: confirm the
    full Phase 14-24 `ctest --preset linux-release` suite is green (the slice exists), then confirm
    `/dev/hidraw*` is operator-readable — if root-only after a USB re-enumeration, physically replug
    or run `setfacl -m u:$(id -u):rw /dev/hidraw*` per CLAUDE.md (systemd ≥258 uaccess regression). Do
    NOT instruct any system-level mutation beyond the operator's own setfacl on the hidraw nodes.

```
VERIFY-05 criteria (one numbered test each): (1) an assigned image appears on an LCD key; (2) a key
press fires its bound action; (3) each of the 4 encoders fires on rotate CW; (4) each encoder fires
on rotate CCW; (5) each encoder fires on press (host synthesises release per INPUT-04); (6) a
touch-zone tap fires the action bound to the encoder under that zone (zone index 0..3); (7) a swipe
left/right changes pages (prev/next); (8) the brightness slider visibly changes panel brightness
(LIG); (9) "clear all" blanks the panel (CLE); (10) hasClock=false — the AKP05E sidebar row shows
NO Sync/time-sync control.

Provisional-§5 confirm-or-correct rows (each names the RE doc section to update on mismatch, since
the hardware wins): (a) the `DRA` rect-header layout (location byte, BE16 width/height/x/y, 32-byte
header) per akp05_vendor.md — confirm the on-wire bytes match or record the corrected layout to land
in akp05_vendor.md; (b) the encoder-overlay framing — Phase 23 shipped both the in-code per-encoder
`ENC` LCD model AND akp05.md's touch-strip-zone model; this row records which one the hardware
actually renders and which doc/code to drop the other from; (c) the touch zone + swipe coordinate
map (tap zone index 0..3, swipe-left/right gesture codes) per akp05.md §5 — confirm or record the
corrected map.

VERIFY-06 section: a real third-party Elgato/Mirabox `.sdPlugin` (a simple HTML or system-Node ≥20
plugin — operator picks one) registers over the loopback WebSocket, paints a key via `setImage`,
and a physical key press / encoder turn delivers `keyDown` / `dialRotate` to the plugin with its
observable effect. Name the loopback (LocalHost-only) invariant and the signature-gate
precondition (the plugin must pass the Phase-22 signature gate; the sample is a trusted plugin).

Each provisional-§5 and each VERIFY criterion is a numbered test with `result: [pending]`. Mark the
whole runbook as operator-witnessed (it is walked in Plan 02). Add a frontmatter block matching
10-UAT.md (`status: testing`, `phase: 25-hardware-verification-real-plugin`).
```

</action>
  <verify>
    <automated>test -f .planning/phases/25-hardware-verification-real-plugin/25-UAT.md && grep -c 'result: \[pending\]' .planning/phases/25-hardware-verification-real-plugin/25-UAT.md | awk '$1>=13{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `25-UAT.md` exists and follows the 10-UAT.md structure (numbered tests, expected/result, ASCII
      names, Summary).
    - All 10 VERIFY-05 criteria are present and walkable (image-on-key, key press, encoder CW, encoder
      CCW, encoder press, touch tap-zone, swipe-page, brightness, clear-all, hasClock=false row).
    - All three provisional-§5 confirm-or-correct rows are present and each names the RE doc/code to
      update on mismatch (hardware wins).
    - The VERIFY-06 real-`.sdPlugin` round-trip section is present (register → setImage → physical
      keyDown/dialRotate → observable effect) and names the loopback + signature-gate preconditions.
    - A Pre-flight section requires the Phase 14-24 suite green and documents the setfacl/replug ACL
      recovery.
  </acceptance_criteria>
  <done>25-UAT.md is a complete, walkable operator runbook covering VERIFY-05, VERIFY-06, and the three provisional-§5 reconciliation rows; ready for Plan 02's operator walk.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                  |
| --------------------------------------- | -------------------------------------------------------------------------------------------- |
| operator host → `/dev/hidraw*`          | The operator's local ACL (uaccess / setfacl) governs device access; out of the app's control |
| third-party plugin → loopback WebSocket | VERIFY-06 runs third-party plugin code; crosses into the app over the LocalHost-only WS      |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                       |
| ---------------- | ---------------------- | ------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-25-01          | Tampering              | third-party `.sdPlugin` executes during VERIFY-06 | accept      | The Phase-22 signature gate + Phase-18 spawn sandbox already mitigate; the UAT uses a trusted sample plugin, not an arbitrary one. No new attack surface — the runbook only exercises existing gates. |
| T-25-02          | Elevation              | operator runs `setfacl` on hidraw nodes           | accept      | The setfacl is the operator's own local device-access action (CLAUDE.md), scoped to `/dev/hidraw*`; the project tooling performs no system-level mutation.                                            |
| T-25-03          | Information disclosure | runbook authoring                                 | accept      | This plan writes a Markdown runbook + a one-flag code correction; no new code path, no new network/IO surface.                                                                                        |
| T-25-SC          | Tampering              | npm/pip/cargo installs                            | accept      | No package installs in this plan; the VERIFY-06 sample plugin is run by the existing spawn host, not installed by this plan.                                                                          |
| \</threat_model> |                        |                                                   |             |                                                                                                                                                                                                       |

<verification>
- `ctest --preset linux-release` green after the register.cpp correction (the slice still builds; no
  descriptor-table test regresses).
- `25-UAT.md` exists, follows the 10-UAT.md structure, and is walkable end-to-end (Plan 02 consumes it).
- `grep` confirms `hasClock = false` on the akp05e row and no other row changed.
</verification>

\<success_criteria>

- akp05e advertises hasClock=false (DEVICES-11 / ARCH-05); suite green.
- `25-UAT.md` covers all 10 VERIFY-05 criteria, the three provisional-§5 confirm-or-correct rows, and
  the VERIFY-06 real-`.sdPlugin` round-trip, in the 10-UAT.md style with ASCII names.
- The runbook documents the Pre-flight (suite-green + ACL recovery) so the operator can begin Plan 02.
  \</success_criteria>

<output>
Create `.planning/phases/25-hardware-verification-real-plugin/25-01-SUMMARY.md` when done.
</output>
