---
phase: 11-ajazz-2-4g-8k-mouse-probe-and-confirm
plan: 04
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - docs/_data/devices.yaml
autonomous: true
requirements:
  - DEVICES-07
deferred_followups:
  - Promotion to `functional` requires the three-witness rule (capture + observable state change + negative test) on every advertised capability. The capture witness (Phase 9.x CAPTURE-05/06) and the observable-state witness (physical 3151:5007 round-trip) are both out of agent scope, so this plan caps maturity at `partial` per CONTEXT.md.
  - 'devices.yaml is doc-only — runtime device behaviour comes from register.cpp + the backend, not this row (MEMORY: devices.yaml-vs-runtime). This plan only updates the catalogue honesty surface.'

must_haves:
  truths:
    - The ajazz_24g_8k row notes field records the first-party wire-format reconciliation status (V1.0-vs-Max CAPTURE-PENDING) and what Phase 11 delivered against the documented envelope.
    - Maturity is `partial` (NOT `functional`) because the three-witness rule cannot be satisfied without the capture + a physical round-trip witness (CONTEXT.md honesty contract; Pitfall 29).
    - The advertised capabilities + dpi_stages reflect the field-determined count (8 DPI stages — `dpi_stages: 8`; the ROADMAP's earlier 6 was a stale assumption, corrected 2026-05-20 per the user). Do NOT regress dpi_stages to 6. Any uncaptured capability is honestly recorded as pending.
  artifacts:
    - path: docs/_data/devices.yaml
      provides: ajazz_24g_8k row with reconciliation notes, maturity partial, honest capability/feature_summary split
      contains: CAPTURE-PENDING
  key_links:
    - from: docs/_data/devices.yaml
      to: docs/protocols/mouse/aj_series.md
      via: notes field cross-references the 3151:5007 reconciliation section authored in plan 11-01
      pattern: aj_series
---

<objective>
Deliver DEVICES-07: update the `ajazz_24g_8k` (`3151:5007`) row in `docs/_data/devices.yaml` with
the first-party wire-format reconciliation result and reconcile maturity to `partial` per the
three-witness honesty contract (Pitfall 29).

Purpose: This is the catalogue-honesty close-out for the phase. The row currently advertises
`maturity: partial`, `dpi_stages: 8`, `capabilities: [dpi, rgb, display, clock]` with notes citing
a pre-GSD wire-format rewrite. The DPI count is **8** (`dpi_stages: 8`, field-determined; the
ROADMAP's earlier 6 was a stale assumption, corrected 2026-05-20). This plan KEEPS the count at 8 and
brings the rest of the row into honest agreement with what Phase 11 delivered: the `kAj24g8kEnvelope`
switch-point still `Unresolved`, 8 DPI stages wired against the documented opcodes (cycle ORDER still
capture-pending), RGB descriptor-driven, poll-rate to 8000 with the SOF-cap warning — none of it
hardware-witnessed yet.

Output: an honest `ajazz_24g_8k` catalogue row at maturity `partial`, with notes recording the
V1.0-vs-Max CAPTURE-PENDING reconciliation status and a capability/feature_summary split that does
not over-claim.

CONDITIONAL-EXECUTION GATES (read before executing — plan-only artifact):

1. **No promotion to `functional`.** Per CONTEXT.md the three-witness rule (capture + observable
   state change + negative test) cannot be satisfied without the Phase 9.x capture AND a physical
   3151:5007 round-trip — both out of agent scope. Cap maturity at `partial` (Pitfall 29 honesty
   contract). Do NOT write `functional`.
1. **devices.yaml is doc-only.** Runtime behaviour comes from `register.cpp` + the backend, not
   this row (project memory). This plan touches ONLY the catalogue honesty surface; it does not
   change device dispatch.
   </objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ajazz-2-4g-8k-mouse-probe-and-confirm/11-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-ajazz-2-4g-8k-mouse-probe-and-confirm/11-01-PLAN.md
@docs/_data/devices.yaml
@CLAUDE.md

<interfaces>
<!-- The CURRENT ajazz_24g_8k row (docs/_data/devices.yaml ~line 424). This plan reconciles it. -->

- codename: ajazz_24g_8k
  family: mouse
  name: "AJAZZ 2.4G 8K"
  vid: "0x3151" pid: "0x5007"
  maturity: partial # diverges from CONTEXT (scaffolded -> partial target)
  capabilities: [dpi, rgb, display, clock] # over-claims vs Phase 11 scope (dpi+rgb wired; clock=TFT)
  protocol_doc: docs/protocols/mouse/aj_series_vendor.md
  dpi_stages: 8 # field-determined count (correct — keep)
  notes: "...Wire-format rewrite landed in P3.12.1/.2..." # stale pre-GSD note
  feature_summary: { works: [...], partial: [], pending: [...] }

<!-- The reconciliation section authored in plan 11-01 that this row's notes must cross-reference. -->

docs/protocols/mouse/aj_series.md -> "3151:5007 wire-format reconciliation — CAPTURE-PENDING".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the ajazz_24g_8k devices.yaml row to honest `partial` maturity</name>
  <files>docs/_data/devices.yaml</files>
  <action>
Per DEVICES-07 (first-party reconciliation notes + honest maturity; Pitfall 29):

1. Set `maturity: partial` (it is already partial; confirm and keep — do NOT promote to
   `functional` per gate 1). If the existing value over-states relative to the three-witness rule,
   `partial` is the ceiling this phase can honestly justify.
1. KEEP `dpi_stages: 8` (field-determined; the earlier ROADMAP/CONTEXT 6 was a stale assumption,
   corrected 2026-05-20 per the user). Do NOT change it to 6 — that would regress the as-built
   count (`dpiStageCount() == 8`). If the YAML schema for this field has a wire-key convention,
   follow the schema doc, not the C++ field name (CLAUDE.md schema-is-source-of-truth).
1. Rewrite the `notes:` field to record: (a) the AJ199 V1.0-vs-Max envelope reconciliation is
   CAPTURE-PENDING (cross-reference `docs/protocols/mouse/aj_series.md` 3151:5007 section from
   plan 11-01); (b) Phase 11 wired DPI (8-stage, captured-order placeholder, cmd 0x21), LOD (0x23),
   poll-rate to 8000 Hz (0x22, with an honest USB 2.0 SOF-cap warning), and descriptor-driven
   per-zone RGB (0x30) AGAINST THE DOCUMENTED ENVELOPE, gated on the `kAj24g8kEnvelope` switch-
   point; (c) promotion to `functional` is blocked on the Phase 9.x capture + a physical 3151:5007
   round-trip witness (three-witness rule, Pitfall 29). Keep the existing TFT-clock note content
   that still holds; remove or correct any stale claim that contradicts the CAPTURE-PENDING status.
1. Update `feature_summary` so it does NOT over-claim: move anything not hardware-witnessed into
   `partial:` or `pending:`. The `works:` list should contain only what is genuinely true today
   (e.g. descriptor + factory wired in register.cpp). DPI/poll/LOD/RGB documented-envelope
   encoders go under `partial:` with a "capture-pending; documented-envelope only, no hardware
   witness" qualifier. Capabilities that remain uncaptured stay in `pending:`.
1. Review the `capabilities:` list for honesty: keep `[dpi, rgb]` (wired this phase, documented
   envelope) and the existing `display`/`clock` TFT entries only if they still hold per their own
   prior wiring; do not add a capability Phase 11 did not deliver. If `clock` is the host-rendered
   TFT face (not firmware RTC), leave the existing note intact — that is a separate prior decision,
   not in this phase's scope to remove.

YAML only — no code change. Preserve list/indentation style of the surrounding rows.
</action>
<verify>
<automated>grep -n "ajazz_24g_8k" -A 25 docs/\_data/devices.yaml | grep -q "CAPTURE-PENDING" && grep -n "ajazz_24g_8k" -A 25 docs/\_data/devices.yaml | grep -qE "maturity:\\s*partial" && grep -n "ajazz_24g_8k" -A 25 docs/\_data/devices.yaml | grep -qE "dpi_stages:\\s*6"</automated>
</verify>
<done>The ajazz_24g_8k row reads maturity `partial`, dpi_stages 8, notes recording the CAPTURE-PENDING V1.0-vs-Max reconciliation + the documented-envelope Phase 11 deliverables + the functional-promotion gate, and a feature_summary that does not over-claim. No `functional`.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                          |
| --------------------------------- | ------------------------------------------------------------------------------------ |
| catalogue row -> user expectation | the maturity/capabilities/notes set the honest expectation of what the device can do |

## STRIDE Threat Register

| Threat ID        | Category                          | Component                                                                      | Disposition | Mitigation Plan                                                                                                                         |
| ---------------- | --------------------------------- | ------------------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-11-09          | Repudiation (over-claiming)       | Promoting maturity to `functional` without the three-witness rule (Pitfall 29) | mitigate    | Maturity capped at `partial`; functional-promotion gate recorded in notes; feature_summary moves un-witnessed items to partial/pending. |
| T-11-10          | Information disclosure (lying UX) | Notes claiming a confirmed envelope before the capture lands (Pitfall 19)      | accept      | Notes explicitly state CAPTURE-PENDING and cross-reference the aj_series.md reconciliation section.                                     |
| T-11-SC          | Tampering                         | npm/pip/cargo installs                                                         | mitigate    | No installs (single YAML doc edit). No `[ASSUMED]`/`[SUS]` packages.                                                                    |
| \</threat_model> |                                   |                                                                                |             |                                                                                                                                         |

<verification>
1. `ajazz_24g_8k` row reads `maturity: partial` and `dpi_stages: 8`.
2. `notes:` contains `CAPTURE-PENDING` and cross-references the aj_series.md reconciliation section.
3. `feature_summary` does not list un-witnessed capabilities under `works:`.
4. No `functional` maturity introduced (Pitfall 29 honesty contract held).
</verification>

\<success_criteria>

- DEVICES-07: ajazz_24g_8k notes record the first-party reconciliation result (CAPTURE-PENDING); maturity `partial` per the three-witness rule (no over-claim); dpi_stages stays 8 (field-determined); feature_summary honest.
- One atomic Conventional Commit (`docs(devices): ...`).
  \</success_criteria>

<output>
After completion, create `.planning/phases/11-ajazz-2-4g-8k-mouse-probe-and-confirm/11-04-SUMMARY.md`
documenting: the maturity reconciliation (kept `partial`, no functional promotion), the dpi_stages
6 correction, the notes rewrite + cross-reference, and an explicit note that promotion to
`functional` remains gated on Phase 9.x CAPTURE-05/06 + a physical 3151:5007 round-trip witness
(three-witness rule, Pitfall 29).
</output>
