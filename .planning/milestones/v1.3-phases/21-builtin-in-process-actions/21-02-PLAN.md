---
phase: 21-builtin-in-process-actions
plan: 02
type: execute
wave: 1
depends_on: [15, 16, 19]
files_modified:
  - src/app/src/obs_client.hpp
  - src/app/src/obs_client.cpp
  - src/app/CMakeLists.txt
  - tests/unit/test_obs_client.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [PLUGIN-12]
must_haves:
  truths:
    - computeObsAuth(password, salt, challenge) returns exactly Base64(SHA256(Base64(SHA256(password+salt))+challenge)) computed via QCryptographicHash::Sha256, matching a known fixed (password,salt,challenge)->auth vector.
    - Against a mock OBS QWebSocketServer that sends Hello(op:0) WITH d.authentication{challenge,salt}, the client replies Identify(op:1) carrying the computed authentication and rpcVersion 1, and the server reaches Identified(op:2).
    - When the mock server's Hello carries d.authentication but NO OBS password is configured, the client REFUSES to send Identify and surfaces an error (auth default-on; never connect to an auth-demanding OBS without a password).
    - A SetCurrentProgramScene request is emitted as op:6 with requestType "SetCurrentProgramScene", a requestId, and requestData{sceneName}; the mock server receives the well-formed Request after a successful Identify.
    - The OBS built-in compiles out cleanly when AJAZZ_HAVE_WEBSOCKETS is undefined (mirrors SdPluginServer); core/COD-031 untouched (all JSON lives in the app tier).
  artifacts:
    - path: src/app/src/obs_client.hpp
      provides: 'ObsClient QObject: connectToObs(host,port,password), setScene/setPreviewScene/toggleRecord/toggleStream, computeObsAuth() static, connected/authFailed/error signals - gated AJAZZ_HAVE_WEBSOCKETS'
      contains: class ObsClient
    - path: src/app/src/obs_client.cpp
      provides: QWebSocket Hello/Identify v5 auth handshake (auth default-on), op:6 request emit for the four-request subset, QJsonDocument message framing
      contains: computeObsAuth
    - path: tests/unit/test_obs_client.cpp
      provides: loopback mock OBS QWebSocketServer; known-vector auth assertion; Identify-on-auth + refuse-when-no-password; well-formed SetCurrentProgramScene request
      min_lines: 120
  key_links:
    - from: src/app/src/obs_client.cpp
      to: QCryptographicHash::Sha256
      via: computeObsAuth nested-SHA256-of-base64 + challenge per obs-websocket v5 protocol.md
      pattern: QCryptographicHash
    - from: src/app/src/obs_client.cpp
      to: mock OBS QWebSocketServer (test)
      via: QWebSocket connect -> recv Hello(0) -> send Identify(1) -> recv Identified(2) -> send Request(6)
      pattern: Identify
    - from: tests/unit/test_obs_client.cpp
      to: src/app/src/obs_client.cpp
      via: loopback QWebSocketServer mock (test_sd_plugin_server.cpp pattern) asserts auth bytes + refuse-unauthenticated
      pattern: QWebSocketServer
---

<objective>
Build the app-tier obs-websocket v5 client (`obs_client.{hpp,cpp}`) behind the existing
`AJAZZ_HAVE_WEBSOCKETS` build gate: a `QWebSocket` that connects to OBS, completes the
Hello/Identify SHA256 challenge/response handshake with auth **default-on** (refuses to connect
to an auth-demanding OBS when no password is configured - the LOCKED anti-feature mitigation),
and emits the first-cut request subset (SetCurrentProgramScene / SetCurrentPreviewScene /
ToggleRecord / ToggleStream) as op:6 messages. Proven by a loopback mock OBS `QWebSocketServer`
that asserts the exact auth string against a known challenge/salt vector AND that the client
refuses Identify when a password is required but unset.

This plan is an independent file (`obs_client.*`) with NO overlap with 21-01 (synthesizer) or
21-03 (registry), so it runs in Wave 1 parallel to 21-01. It consumes no 15/16/19 seam (21-03
wires the `obsstudio` built-in to this client); the STOP gate for 15/16/19 is performed by 21-01.

Purpose: PLUGIN-12 `obsstudio` - the obs-websocket v5 client + the auth-default-on mitigation,
hand-rolled over QWebSocket + QJsonDocument + QCryptographicHash (the v5 subset is ~6 messages;
no mature slop-free C++ client exists - hand-rolling is correct here, not a "don't hand-roll").
Output: `obs_client.{hpp,cpp}` (gated) + the mock-OBS auth test, registered in CMake under the
WebSockets gate.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-builtin-in-process-actions/21-CONTEXT.md
@.planning/phases/21-builtin-in-process-actions/21-RESEARCH.md
@.planning/phases/21-builtin-in-process-actions/21-VALIDATION.md
@CLAUDE.md

\<read_first>

- `tests/unit/test_sd_plugin_server.cpp` lines 25-131 - the `ensureQCoreApp()` leaked-singleton + `pump()`/`waitForSpy()` harness AND the loopback `QWebSocketServer` (bound to 127.0.0.1) + `QWebSocket` client + `QSignalSpy` pattern to clone for the mock OBS server (Pitfall 5: never a fresh QCoreApplication per TEST_CASE).
- `src/app/src/sd_plugin_server.hpp` + `sd_plugin_server.cpp` - the `QWebSocket`/`QWebSocketServer` usage + the `AJAZZ_HAVE_WEBSOCKETS` gating shape the OBS client mirrors (signals, the LocalHost bind precedent, the QJsonDocument framing).
- `src/app/src/single_instance_guard.cpp` - the existing `QCryptographicHash::Sha256` usage precedent (Qt ships it; do NOT bundle a crypto lib - Don't Hand-Roll table).
- `src/app/CMakeLists.txt` lines 88-92 - the `if(AJAZZ_HAVE_WEBSOCKETS) target_sources(... sd_plugin_server.cpp) target_link_libraries(... Qt6::WebSockets) endif()` block to extend with `obs_client.cpp`.
- `tests/unit/CMakeLists.txt` lines 335-341 - the `AJAZZ_HAVE_WEBSOCKETS`-gated test link block (test_sd_plugin_server links sd_plugin_server.cpp); mirror for test_obs_client.cpp.
- `docs/protocols/streamdeck/akp_plugin_sdk.md` §1 - confirm the `com.hotspot.streamdock.obsstudio` UUID is the built-in this client backs (the registry wiring is 21-03).
- 21-RESEARCH.md Pattern 3 (the auth algorithm + Hello/Identify/Request op shapes + the four-request subset + the auth-default-on gate) and Pitfall 5 (auth-bypass / inverted refuse-when-unauthenticated logic - assert BOTH directions).
- CLAUDE.md: ASCII-only test names; MSVC `_s` variants if any CRT is touched (unlikely here); `Qt6::WebChannelQuick` rules are PI-only (not relevant to the OBS client).

<interfaces>
<!-- obs-websocket v5 [CITED: github.com/obsproject/obs-websocket protocol.md]. Schema wire keys are source of truth. -->
// Auth (when Hello carries d.authentication):
//   auth = Base64( SHA256( Base64( SHA256( password + salt ) ) + challenge ) )   via QCryptographicHash::Sha256
// Hello   (op 0): server -> client; d.rpcVersion; d.authentication{challenge,salt} present ONLY when auth enabled.
// Identify(op 1): client -> server; d.rpcVersion=1; d.authentication=<computed> (omit if server omits authentication); d.eventSubscriptions=0.
// Identified(op 2): server -> client on success.
// Request (op 6): client -> server; d.requestType; d.requestId; d.requestData{...}.
// First-cut requestTypes: SetCurrentProgramScene{sceneName}, SetCurrentPreviewScene{sceneName}, ToggleRecord{}, ToggleStream{}.
// Default port 4455, rpcVersion 1 [ASSUMED port; user-configurable].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ObsClient v5 handshake + computeObsAuth + the four-request subset (auth default-on)</name>
  <files>src/app/src/obs_client.hpp, src/app/src/obs_client.cpp, src/app/CMakeLists.txt</files>
  <read_first>
    - src/app/src/sd_plugin_server.{hpp,cpp} (QWebSocket usage + AJAZZ_HAVE_WEBSOCKETS gating + QJsonDocument framing).
    - src/app/src/single_instance_guard.cpp (QCryptographicHash::Sha256 precedent).
    - src/app/CMakeLists.txt lines 88-92 (the WebSockets-gated target_sources/link block).
    - 21-RESEARCH.md Pattern 3 (auth algorithm + op shapes + the four-request subset) and Pitfall 5 (assert auth BOTH directions).
    - docs/protocols/streamdeck/akp_plugin_sdk.md §1 (obsstudio built-in).
  </read_first>
  <behavior>
    - computeObsAuth(EXAMPLE_PASSWORD, EXAMPLE_SALT, EXAMPLE_CHALLENGE) - where the three inputs are placeholder test tokens, NOT real credentials - returns the documented Base64(SHA256(Base64(SHA256(pw+salt))+challenge)) string for that exact input vector (assert against a precomputed expected value - compute the expected with the same QCryptographicHash steps in the test, or pin a known protocol.md example vector).
    - computeObsAuth is deterministic and pure (same input -> same output; no socket).
    - With auth enabled and a password set: a connectToObs(...) call that receives Hello{op:0,d.authentication{challenge,salt}} produces an Identify{op:1,d.rpcVersion:1,d.authentication:<computed>} message (assert the emitted Identify JSON shape).
    - With auth enabled and NO password set: connectToObs(...) does NOT emit Identify and emits an authFailed/error signal (refuse-unauthenticated).
    - With a server that omits d.authentication (auth disabled): the client emits Identify WITHOUT d.authentication (still rpcVersion 1) - the client never fabricates auth.
    - setScene("Scene 2") after Identified emits op:6 requestType "SetCurrentProgramScene" requestData{sceneName:"Scene 2"} with a non-empty requestId.
  </behavior>
  <action>
    Create `src/app/src/obs_client.hpp` declaring `class ObsClient : public QObject` with `Q_OBJECT`,
    in namespace `ajazz::app`, the WHOLE class gated by `#if defined(AJAZZ_HAVE_WEBSOCKETS)` (mirror
    sd_plugin_server.hpp so the app builds when the module is absent). Public surface:
      - `static QString computeObsAuth(QString const&amp; password, QString const&amp; salt, QString const&amp; challenge);`
        - the pure auth function: `Base64(SHA256(Base64(SHA256(password+salt))+challenge))` via
        `QCryptographicHash::Sha256` (do NOT bundle crypto - Qt ships it).
      - `void connectToObs(QString const&amp; host, quint16 port, QString const&amp; password);` (default port
        4455 documented; the password is the auth-default-on credential).
      - `void setScene(QString const&amp; sceneName);` / `void setPreviewScene(QString const&amp; sceneName);`
        / `void toggleRecord();` / `void toggleStream();` - the four-request subset, each building an op:6
        message via a private `sendRequest(QString requestType, QJsonObject requestData)` that mints a
        requestId (QUuid::createUuid().toString()).
      - signals: `void connected();` (Identified received), `void authFailed(QString reason);`,
        `void errorOccurred(QString reason);`, optionally `void requestSucceeded(QString requestId);`.
      - private: `QWebSocket m_socket;` (or a unique_ptr), `QString m_password;`, a state member, and the
        `onTextMessage(QString)` slot that parses the op (QJsonDocument), branches on Hello(0)/Identified(2)/
        RequestResponse(7), and drives the handshake.

```
Implement `obs_client.cpp` (also fully gated by `#if defined(AJAZZ_HAVE_WEBSOCKETS)`): wire
`QWebSocket::textMessageReceived` to `onTextMessage`. On Hello(op:0): read `d.authentication`
(a QJsonObject - present ONLY when auth is enabled). If present: REQUIRE m_password to be non-empty;
if empty, emit `authFailed("OBS requires authentication but no password is configured")` and close -
do NOT send Identify (auth default-on, LOCKED anti-feature; Pitfall 5). If present + password set:
compute the auth via computeObsAuth(m_password, salt, challenge) and send Identify(op:1) with
`d.rpcVersion=1`, `d.authentication=<auth>`, `d.eventSubscriptions=0`. If `d.authentication` is ABSENT
(auth disabled server-side): send Identify(op:1) with rpcVersion 1 and NO authentication field (never
fabricate auth; never silently proceed past a missing-auth surprise - log it). On Identified(op:2):
emit connected(). `sendRequest` builds `{op:6,d:{requestType,requestId,requestData}}` via QJsonObject
-> QJsonDocument(Compact) -> m_socket.sendTextMessage. All JSON is app-tier QJson (COD-031: never
nlohmann; the OBS client is app-layer).

Wire CMake: extend the existing `if(AJAZZ_HAVE_WEBSOCKETS)` block in `src/app/CMakeLists.txt`
(lines 88-92) with `src/obs_client.cpp` in `target_sources`. Add `src/obs_client.hpp` +
`src/obs_client.cpp` to the AUTOMOC header/source listing (mirror sd_plugin_server placement) so the
QObject's moc runs. Do NOT add the OBS client to the QML module sources (it is wired by the registry
service in 21-03, not QML-exposed here).
```

</action>
  <verify>
    <automated>cmake --preset linux-release >/dev/null 2>&1; cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -5; grep -c 'computeObsAuth' src/app/src/obs_client.cpp; grep -c nlohmann src/app/src/obs_client.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `obs_client.hpp` declares `class ObsClient : public QObject` (Q_OBJECT) with `computeObsAuth` (static), `connectToObs`, the four request methods, and the connected/authFailed/errorOccurred signals - the whole class gated by `#if defined(AJAZZ_HAVE_WEBSOCKETS)`.
    - `computeObsAuth` computes `Base64(SHA256(Base64(SHA256(password+salt))+challenge))` via QCryptographicHash::Sha256 (no bundled crypto).
    - On Hello with d.authentication and an EMPTY password, the client emits authFailed and does NOT send Identify (refuse-unauthenticated; auth default-on).
    - On Hello with d.authentication and a SET password, the client sends Identify(op:1) with rpcVersion 1 + the computed authentication.
    - `sendRequest` emits op:6 with requestType/requestId/requestData (the four subset methods route through it).
    - The app target compiles; `grep -c nlohmann src/app/src/obs_client.cpp` == 0 (COD-031); `grep -c 'computeObsAuth' ...` >= 1.
  </acceptance_criteria>
  <done>ObsClient implements the obs-websocket v5 Hello/Identify SHA256 challenge/response with auth default-on (refuses Identify when a password is required but unset), the four-request op:6 subset, and the pure computeObsAuth - all gated under AJAZZ_HAVE_WEBSOCKETS, app-tier QJson, COD-031 clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mock OBS QWebSocketServer test - auth vector + Identify-on-auth + refuse-when-no-password + well-formed request</name>
  <files>tests/unit/test_obs_client.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - src/app/src/obs_client.hpp (the API from Task 1 + the signal names to QSignalSpy).
    - tests/unit/test_sd_plugin_server.cpp lines 25-131 (ensureQCoreApp/pump/waitForSpy + the loopback QWebSocketServer-on-127.0.0.1 + client + QSignalSpy pattern to clone for the mock OBS server).
    - tests/unit/CMakeLists.txt lines 335-341 (the AJAZZ_HAVE_WEBSOCKETS-gated test link block - mirror to link obs_client.cpp).
    - 21-RESEARCH.md Pitfall 5 (assert BOTH auth directions: correct auth bytes AND refuse-when-no-password).
  </read_first>
  <behavior>
    - Known-vector auth: computeObsAuth(pw, salt, challenge) equals the value produced by re-running the documented SHA256/base64 steps in-test on the same inputs (and matches a pinned protocol.md example if available) - asserts the algorithm, not just self-consistency.
    - Identify-on-auth: a mock server bound to 127.0.0.1 sends Hello{op:0,d:{rpcVersion:1,authentication:{challenge,salt}}}; the client (password set) replies Identify{op:1} whose d.authentication == computeObsAuth(pw, salt, challenge) and d.rpcVersion == 1; the server validates and sends Identified{op:2}; the client emits connected() (QSignalSpy fires).
    - Refuse-when-no-password: the same mock server demands auth; the client connected with an EMPTY password emits authFailed (QSignalSpy) and never sends Identify (the mock server records zero Identify messages).
    - Auth-disabled server: a mock server whose Hello omits d.authentication; the client (any password) sends Identify WITHOUT d.authentication and reaches connected().
    - Well-formed request: after Identified, setScene("Scene 2") makes the mock server receive an op:6 message with requestType "SetCurrentProgramScene", a non-empty requestId, and requestData.sceneName == "Scene 2".
  </behavior>
  <action>
    Create `tests/unit/test_obs_client.cpp`, gated under `#if defined(AJAZZ_HAVE_WEBSOCKETS)`. Build a
    mock OBS server by cloning the loopback `QWebSocketServer` pattern from test_sd_plugin_server.cpp:
    bind a `QWebSocketServer` to `QHostAddress::LocalHost` on port 0 (ephemeral), accept the client
    connection, and on connect send a Hello frame. Parametrize the Hello content per case (with/without
    d.authentication). On receiving the client's Identify, validate `d.authentication` against the
    expected computeObsAuth for the auth case (the server knows the salt/challenge it sent), then reply
    Identified(op:2). For the request case, record the next op:6 message the server receives and assert
    its requestType/requestId/requestData.
    Reuse `ensureQCoreApp()` + `pump()`/`waitForSpy()` (Pitfall 5 - never a fresh QCoreApplication).
    Use `QSignalSpy` on ObsClient::connected / ObsClient::authFailed. Cover every &lt;behavior&gt; bullet:
    the known-vector auth assertion (compute expected in-test via the same QCryptographicHash steps),
    Identify-on-auth -> Identified -> connected, refuse-when-no-password (authFailed + zero Identify
    received by the server), auth-disabled-server (Identify with no auth field), and the well-formed
    SetCurrentProgramScene request. ASCII-only TEST_CASE/SECTION titles (no em-dash/arrow - use "-"/"->";
    CLAUDE.md Win32 ctest codepage rule); tag `[obs-client]` so `ctest --preset linux-release -R ObsClient`
    selects it (matches VALIDATION.md). Register the test in tests/unit/CMakeLists.txt INSIDE the existing
    `AJAZZ_HAVE_WEBSOCKETS` gate, linking `obs_client.cpp` (mirror the test_sd_plugin_server link block;
    add Qt6::WebSockets if the test target does not already inherit it).
  </action>
  <verify>
    <automated>cmake --preset linux-release >/dev/null 2>&1; cmake --build --preset linux-release >/dev/null 2>&1 && ctest --preset linux-release -R ObsClient --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R ObsClient` runs and reports 100% passed.
    - The known-vector test asserts computeObsAuth equals the documented SHA256/base64 result for a fixed (password,salt,challenge) - the algorithm is proven, not merely self-consistent.
    - Identify-on-auth: the mock server sees an Identify whose authentication == computeObsAuth(pw,salt,challenge) and rpcVersion == 1; the client emits connected() after Identified.
    - Refuse-when-no-password: the client emits authFailed and the mock server records ZERO Identify messages (Pitfall 5 - auth default-on proven in the refuse direction).
    - Auth-disabled server: the client sends Identify with NO authentication field and reaches connected().
    - Well-formed request: after Identified, setScene("Scene 2") makes the server receive op:6 SetCurrentProgramScene with requestData.sceneName=="Scene 2" and a non-empty requestId.
    - ASCII-only TEST_CASE titles; the test is gated under AJAZZ_HAVE_WEBSOCKETS; full `ctest --preset linux-release` suite green.
  </acceptance_criteria>
  <done>A loopback mock OBS QWebSocketServer proves the v5 auth handshake in BOTH directions (correct auth bytes vs a known vector when a password is set; refusal with zero Identify when a password is required but unset) plus the auth-disabled path and a well-formed op:6 request - the hardware-free gating proof for the obsstudio built-in.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                                                     |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| ObsClient -> OBS WebSocket server                   | The client authenticates to a remote OBS instance over a plaintext WebSocket (no TLS by OBS design); the credential is the user's OBS password. |
| user-configured OBS password -> QSettings (at rest) | The OBS password, if persisted by 21-03, is stored via QSettings - plaintext at rest unless an OS keychain is used.                             |

## STRIDE Threat Register

| Threat ID      | Category               | Component                                                            | Disposition         | Mitigation Plan                                                                                                                                                                                                                                                                                                                                  |
| -------------- | ---------------------- | -------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-21-obsauth   | Spoofing / Tampering   | auto-connect to an unauthenticated OBS (credential/control exposure) | mitigate            | Auth default-on (LOCKED anti-feature): when Hello carries d.authentication a configured password is MANDATORY - the client refuses Identify and emits authFailed when none is set. Asserted in BOTH directions (correct auth bytes vs a known vector; refusal with zero Identify). The client never fabricates auth and never silently proceeds. |
| T-21-obscrypto | Cryptography           | computeObsAuth correctness                                           | mitigate            | The SHA256 challenge/response is computed via `QCryptographicHash::Sha256` (never hand-rolled crypto - Qt ships it); the exact byte output is pinned against a known (password,salt,challenge)->auth vector.                                                                                                                                     |
| T-21-obsjson   | Input Validation / DoS | malformed/oversized OBS server frames                                | mitigate            | All inbound frames parsed defensively via QJsonDocument (`isObject`/`toObject`/`toString`/`toInt(default)`); unknown ops are ignored; no exceptions on missing/typed-wrong keys (ASVS V5). The mock-server tests exercise the happy + refuse paths.                                                                                              |
| T-21-obspw     | Information Disclosure | OBS password at rest in QSettings (plaintext)                        | accept (documented) | Persisting the OBS password (21-03) uses QSettings - plaintext at rest. Documented as a known limitation (honesty rule - do NOT claim encryption that is not there); an OS keychain is a later-phase consideration. No claim of encryption is made in the UI/docs.                                                                               |
| T-21-SC        | Tampering              | npm/pip/cargo installs                                               | accept              | No package installs - pure in-tree C++/Qt6 (QWebSocket + QCryptographicHash). Package Legitimacy Gate N/A (21-RESEARCH.md audit: not applicable; no [SLOP]/[SUS] exposure).                                                                                                                                                                      |

V2 (authentication): obs-websocket v5 SHA256 challenge/response, auth default-on, refuse-unauthenticated. V6 (cryptography): QCryptographicHash::Sha256, no hand-rolled crypto.
\</threat_model>

<verification>
- `ctest --preset linux-release -R ObsClient` green (known-vector auth + Identify-on-auth + refuse-when-no-password + auth-disabled + well-formed request).
- `grep -c nlohmann src/app/src/obs_client.*` == 0 (COD-031 - OBS JSON is app-tier QJson).
- The OBS client + test are fully gated under AJAZZ_HAVE_WEBSOCKETS (the app + suite build when the module is absent).
- `git diff --stat` shows NO changes to src/core/** (the OBS client is app-only) and no edits to akp05.cpp / akp05_protocol.hpp.
- ASCII-only TEST_CASE names; full `ctest --preset linux-release` suite green.
</verification>

\<success_criteria>

- The obs-websocket v5 client connects with auth default-on and refuses an auth-demanding OBS when no password is configured (anti-feature mitigation proven in both directions).
- computeObsAuth is a pure, byte-correct SHA256 challenge/response asserted against a known vector.
- The four-request subset emits well-formed op:6 messages after Identify.
- The client is gated under AJAZZ_HAVE_WEBSOCKETS and is COD-031-clean (app-tier QJson); 21-03 wires the obsstudio built-in to it.
  \</success_criteria>

<output>
Create `.planning/phases/21-builtin-in-process-actions/21-02-SUMMARY.md` when done.
Record: the ObsClient API shape (connectToObs/setScene/.../signals) for 21-03 to wire, the auth-vector
used in the test, the default port choice, the QSettings password-at-rest limitation (T-21-obspw,
honesty note), the auth-disabled-server handling, and any follow-up for 21-03 (the obsstudio handler
binding host/port/password from settings + the action subset mapping) or Phase 25 (live OBS witness).
</output>
