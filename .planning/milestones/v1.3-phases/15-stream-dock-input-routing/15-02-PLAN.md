---
phase: 15-stream-dock-input-routing
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/app/src/application.hpp
  - src/app/src/application.cpp
autonomous: true
requirements: [INPUT-03, INPUT-04, INPUT-05]
must_haves:
  truths:
    - Application owns one QtExecutor-backed core::ActionEngine and the StreamDockInputService (the first ActionEngine instantiation in the app).
    - On a Stream Deck hot-plug arrival, the input service drives the SAME held handle the Phase-14 control service owns (no second open()).
    - The ActionEngine is constructed with real keyPress(stub)/runCommand(QProcess)/openUrl(QDesktopServices)/plugin(stub) executors and the QtExecutor so Sleep defers.
    - The active profile accessor is wired from ProfileController::activeProfile() so dispatch resolves bindings against the loaded profile.
    - The swipe pageNavRequested signal is connected to a sink (logged intent in Phase 15; Phase 16 wires the page model).
  artifacts:
    - path: src/app/src/application.hpp
      provides: m_qtExecutor, m_actionEngine, m_streamDockInput members in -Wreorder-clean init order
      contains: m_streamDockInput
    - path: src/app/src/application.cpp
      provides: ActionEngine+QtExecutor construction, executor lambdas, shared-handle wiring on arrival, profile accessor, pageNavRequested connect
      contains: StreamDockInputService
  key_links:
    - from: src/app/src/application.cpp
      to: StreamDockControlService held handle
      via: on Stream Deck arrival, pass the control service's held shared_ptr<IDevice> to StreamDockInputService::setActiveDevice (share, never re-open)
      pattern: setActiveDevice
    - from: src/app/src/application.cpp
      to: core::ActionEngine
      via: construct with ActionExecutors{keyPress stub, runCommand QProcess, openUrl QDesktopServices, plugin stub} + m_qtExecutor; profile accessor from m_profileController
      pattern: ActionEngine
    - from: src/app/src/application.cpp
      to: StreamDockInputService::pageNavRequested
      via: QObject::connect to a logged sink (Phase 16 page-model consumer)
      pattern: pageNavRequested
---

<objective>
Wire the Plan 15-01 `StreamDockInputService` into `Application`: construct the app's
single `QtExecutor` and the first `core::ActionEngine` (with the real executor lambdas —
keyPress stub, runCommand via QProcess, openUrl via QDesktopServices, plugin stub), inject
them and the `ProfileController::activeProfile()` accessor into the input service, share the
Phase-14 `StreamDockControlService`'s held `shared_ptr<IDevice>` on Stream Deck arrival
(no second `open()` — ARCH-03), and connect the `pageNavRequested` swipe intent to a logged
sink (Phase 16 owns the page model).

Purpose: 15-01 built and unit-tested the service in isolation with injected seams; this plan
makes it live in the running app so a physical press/turn/tap actually fires bound actions —
closing INPUT-03/04/05 at the app integration level. The ActionEngine + QtExecutor ownership
here is the load-bearing reuse surface Phase 19 extends (it adds the plugin bridge as a second
event consumer and replaces the plugin stub).

Output: edits to application.hpp/.cpp only (member declarations -Wreorder-clean, ctor init
list, executor lambdas, arrival/handle-sharing wiring, signal connects). No new files.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-stream-dock-input-routing/15-CONTEXT.md
@.planning/phases/15-stream-dock-input-routing/15-RESEARCH.md
@.planning/phases/15-stream-dock-input-routing/15-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From 15-01 (this phase) and the Phase-14 control service. Confirm exact signatures
     in the as-built 15-01 SUMMARY + headers before wiring — 15-01 chose the seams. -->

StreamDockInputService (src/app/src/stream_dock_input_service.hpp — built in 15-01):

- ctor takes the injection seams chosen in 15-01 (a ProfileAccessor std::function\<core::Profile const&()>
  and a core::ActionEngine\* or owned engine; read the 15-01 SUMMARY for the exact shape).
- void setActiveDevice(std::shared_ptr<core::IDevice> device); // HOLDS the handle, starts the poll pump
- Q_SIGNAL void pageNavRequested(int direction); // swipe intent (-1 prev / +1 next)

StreamDockControlService (src/app/src/stream_dock_control_service.hpp — Phase 14):

- holds the active Stream Deck's std::shared_ptr<core::IDevice> for the session.
- exposes a way to obtain that held handle (an accessor/getter, OR the same DeviceLookup the
  Application lambda uses — the flyweight ARCH-03 guarantees one backend per VID/PID, so a
  second lookup returns the SAME shared_ptr the control service holds). Read the 14-02 SUMMARY
  for whether a getter was added; if not, resolve via the shared DeviceLookup (NOT a fresh open()).

ProfileController (src/app/src/profile_controller.hpp — Phase 14 Task 1 added the getter):

- \[[nodiscard]\] ajazz::core::Profile const& activeProfile() const noexcept;
- Q_SIGNAL void profileChanged();

core::ActionEngine (action_engine.hpp):
explicit ActionEngine(ActionExecutors executors = {}, std::shared_ptr<Executor> executor = nullptr);
void setProfile(Profile); // call on profileChanged if the engine's folder-nav needs the profile

app::QtExecutor (qt_executor.hpp): QObject + core::Executor; must outlive the ActionEngine; own in Application.

Application ctor init-list order (application.cpp:84-200): services constructed in member-declaration
order with the SAME DeviceLookup lambda (codename -> enumerate -> DeviceRegistry::open). The Phase-14
StreamDockControlService is constructed between m_firmwareUpdate and m_hotplug. The hotplug/debouncer
connect block is near line 207 (m_debouncer->coalesced -> m_deviceModel->refresh).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construct the app ActionEngine + QtExecutor + executor lambdas; wire StreamDockInputService into Application</name>
  <files>src/app/src/application.hpp, src/app/src/application.cpp</files>
  <read_first>
    - .planning/phases/15-stream-dock-input-routing/15-01-SUMMARY.md (the as-built injection seams + signal names — authoritative over the interfaces block)
    - .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md (whether the control service exposes a held-handle getter; the active-device-selection simplification; the profile-accessor seam it chose)
    - src/app/src/stream_dock_input_service.hpp (the ctor signature + setActiveDevice + pageNavRequested as built in 15-01)
    - src/app/src/stream_dock_control_service.hpp (held-handle accessor; setActiveDevice on arrival)
    - src/app/src/profile_controller.hpp (activeProfile() getter + profileChanged signal)
    - src/app/src/qt_executor.hpp (QtExecutor ctor + lifetime note)
    - src/core/include/ajazz/core/action_engine.hpp (ActionEngine ctor + ActionExecutors fields + setProfile)
    - src/app/src/application.hpp (full member declaration block — note the -Wreorder ordering and where StreamDockControlService is declared)
    - src/app/src/application.cpp (lines 84-211: the 5x DeviceLookup lambda precedent, ctor init-list order, the hotplug/debouncer connect block; find where StreamDockControlService::setActiveDevice is invoked on arrival from Phase 14)
    - src/app/src/time_sync_service.cpp (onDeviceArrivedDebounced: re-resolve on arrival pattern — Pattern 2 / Pitfall 2)
  </read_first>
  <action>
    In application.hpp add three members in -Wreorder-clean order (declare in the SAME relative
    position as their init-list construction; GCC -Wreorder is -Werror): a
    `std::unique_ptr<QtExecutor> m_qtExecutor;`, a `std::unique_ptr<core::ActionEngine> m_actionEngine;`,
    and a `std::unique_ptr<StreamDockInputService> m_streamDockInput;`. Place them AFTER the Phase-14
    `m_streamDockControl` and BEFORE `m_hotplug` so the input service can share the control service's
    handle and the engine/executor outlive it. Add the `#include "stream_dock_input_service.hpp"` (and
    qt_executor.hpp / ajazz/core/action_engine.hpp as needed).

```
In application.cpp construct, in init-list order:
  1. m_qtExecutor = std::make_unique<QtExecutor>(this);  // owned by Application; outlives the engine (qt_executor.hpp lifetime note).
  2. m_actionEngine: build `core::ActionExecutors` with the four lambdas (COD-031: all JSON parsing
     is app-layer; the engine forwards settingsJson verbatim):
       - keyPress  = STUB: log via AJAZZ_LOG_INFO("input", "keyPress action ignored (OS key injection arrives Phase 21): {}", settingsJson);
                     (do NOT implement uinput/SendInput/CGEvent here — Open Question 4 / Assumption A5 resolved to stub; T-15-03)
       - runCommand = parse settingsJson into program + args and call QProcess::startDetached(program, QStringList args)
                     — NEVER system() or a shell string (T-15-02 / V5 command-injection). If settingsJson shape is
                     not yet defined (Phase 20 PI owns it), parse minimally/defensively and log on malformed input.
       - openUrl   = QDesktopServices::openUrl(QUrl::fromUserInput(QString::fromUtf8(url)));
       - plugin    = STUB: log via AJAZZ_LOG_INFO("input", "plugin action {} ignored (plugin host arrives Phase 19)", id); (T-15-03 Phase-19 seam)
     Construct `m_actionEngine = std::make_unique<core::ActionEngine>(std::move(executors), m_qtExecutor /* shared_ptr<Executor> */);`
     (QtExecutor derives core::Executor; pass it as the shared_ptr so Sleep defers — Pitfall 4 / A2).
  3. m_streamDockInput: construct with the injection seams from 15-01 — the ProfileAccessor
     (`[this]() -> core::Profile const& { return m_profileController->activeProfile(); }`) and the
     m_actionEngine.get() (or whatever the 15-01 ctor expects). `this` as QObject parent.

In the ctor body (near the existing hotplug/debouncer connect block ~line 207):
  - Wire the active Stream Deck handle on arrival: model it on the Phase-14 path that calls
    m_streamDockControl->setActiveDevice(codename) on a Stream Deck arrival. When a Stream Deck
    becomes active, obtain the SAME held shared_ptr<IDevice> the control service holds (via the
    control service's getter if 14-02 added one, OR the shared DeviceLookup — the ARCH-03 flyweight
    returns the same backend) and call m_streamDockInput->setActiveDevice(handle). Do NOT call a fresh
    DeviceRegistry::open for input and do NOT add a second HotplugMonitor — reuse the existing
    debouncer/hotplug path. If multiple Stream Decks are connected, drive the same one the control
    service holds (the Phase-14 "first connected Stream Deck" simplification — note any divergence).
    When the active Stream Deck departs, call m_streamDockInput->setActiveDevice(nullptr) to stop the pump.
  - If the ActionEngine's folder navigation needs the loaded profile, connect
    ProfileController::profileChanged -> m_actionEngine->setProfile(m_profileController->activeProfile())
    (a copy is acceptable for the engine's own nav state; the dispatch path reads bindings via the
    ProfileAccessor, so this is only for OpenFolder/BackToParent context — note the decision).
  - Connect m_streamDockInput->pageNavRequested to a logged sink for Phase 15
    (`QObject::connect(m_streamDockInput.get(), &StreamDockInputService::pageNavRequested, this,
    [](int dir){ AJAZZ_LOG_INFO("input", "page nav intent {} (page model arrives Phase 16)", dir); });`).
    Phase 16 replaces this sink with the real page-nav consumer.

Keep the member declaration order and init-list order identical (GCC -Wreorder is -Werror under
the project's strictness). Do NOT change any existing service's construction. Do NOT touch
akp05.cpp / akp05_protocol.hpp.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release >/dev/null 2>&1 && grep -n 'StreamDockInputService\|m_actionEngine\|m_qtExecutor' src/app/src/application.cpp && grep -n 'pageNavRequested' src/app/src/application.cpp && ctest --preset linux-release -R StreamDockInput --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - The app target compiles clean under `cmake --build --preset linux-release` (no -Wreorder warning-as-error on the three new members).
    - `grep -n 'm_qtExecutor' src/app/src/application.cpp` shows a QtExecutor owned by Application.
    - `grep -n 'core::ActionEngine\|m_actionEngine' src/app/src/application.cpp` shows ONE ActionEngine constructed with the QtExecutor (the first ActionEngine instantiation in the app) and the four ActionExecutors lambdas.
    - The runCommand lambda calls `QProcess::startDetached` with an argv list (NOT system() / not a shell string) — `grep -n 'startDetached' src/app/src/application.cpp` present; `grep -n 'system(' src/app/src/application.cpp` shows no shell-string command launch added.
    - `grep -n 'StreamDockInputService\|m_streamDockInput' src/app/src/application.cpp` shows the input service constructed with the ProfileAccessor + ActionEngine and given QObject parent.
    - setActiveDevice is invoked on Stream Deck arrival sharing the control service's held handle (no fresh open() for input; no second HotplugMonitor) — confirmed by reading the wired arrival path.
    - A connect from StreamDockInputService::pageNavRequested to a sink exists in application.cpp.
    - `git diff --stat` shows NO changes to src/devices/streamdeck/src/akp05.cpp or akp05_protocol.hpp.
    - `grep -c nlohmann src/core/include` unchanged (COD-031 intact).
    - Full `ctest --preset linux-release` suite green (incl. `-R StreamDockInput`).
  </acceptance_criteria>
  <done>Application owns the QtExecutor + the first ActionEngine (with keyPress-stub/runCommand-QProcess/openUrl-QDesktopServices/plugin-stub executors) and the StreamDockInputService; on a Stream Deck arrival the input service drives the Phase-14 held handle (shared, never re-opened) and starts the poll pump; the swipe page-nav intent is logged (Phase 16 sink); member/init order -Wreorder-clean; suite green; wire decode + COD-031 untouched.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                                                                     |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| profile-authored action chain -> OS side effect    | Application's runCommand/openUrl executors execute profile-defined payloads (same trust model as a keyboard macro; profile is local/user-authored in Phase 15). |
| Phase-14 held handle shared into the input service | The control service owns the session handle; the input service must share it (ARCH-03 single handle), never open a second.                                      |

## STRIDE Threat Register

| Threat ID | Category            | Component                                       | Disposition       | Mitigation Plan                                                                                                                                                                                                                                          |
| --------- | ------------------- | ----------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-15-02   | Elevation/Tampering | runCommand executor lambda -> command injection | mitigate          | `QProcess::startDetached(program, args)` with an explicit argv list — never `system()`/shell string; minimal/defensive settingsJson parse with a log on malformed input. Profile is user-authored in Phase 15; untrusted-source handling is Phase 19/22. |
| T-15-03   | Elevation           | keyPress / plugin executors                     | accept (Phase 15) | keyPress STUBBED (log only — OS key injection deferred to Phase 21, opt-in posture per PLUGIN-12); plugin STUBBED (log only — Phase-19 seam). Neither produces a side effect now.                                                                        |
| T-15-04   | DoS                 | Sleep chain blocking GUI/poll thread            | mitigate          | The single Application-owned ActionEngine is constructed WITH the QtExecutor so Sleep/delayMs defer via QTimer::singleShot (audit A2). [VERIFIED qt_executor.hpp]                                                                                        |
| T-15-05   | Tampering/DoS       | sharing a stale/raw handle on arrival/departure | mitigate          | Share the control service's `shared_ptr<IDevice>` (flyweight ARCH-03); call setActiveDevice(nullptr) on departure to stop the pump; never cache a raw IDevice\*. [VERIFIED device.hpp:145]                                                               |
| T-15-SC   | Tampering           | npm/pip/cargo installs                          | accept            | No package installs — pure in-tree C++/Qt wiring. Package Legitimacy Gate N/A.                                                                                                                                                                           |

V5 (input validation): the executors receive profile-authored settingsJson; runCommand parses to an explicit argv list (no shell). No new wire path; the device input is validated upstream by parseInputReport (Plan 15-01 threat register).
\</threat_model>

<verification>
- `ctest --preset linux-release -R StreamDockInput` green after wiring; full `ctest --preset linux-release` suite green.
- App compiles -Wreorder-clean; one ActionEngine + one QtExecutor owned by Application (first ActionEngine instantiation — `grep core::ActionEngine src/app/` now non-zero).
- `grep startDetached src/app/src/application.cpp` present and `grep 'system(' src/app/src/application.cpp` shows no shell-string launch (T-15-02).
- `git diff --stat` excludes akp05.cpp / akp05_protocol.hpp; `grep -c nlohmann src/core/include` unchanged (COD-031).
- The input service shares the Phase-14 held handle on arrival (no second open(), no second HotplugMonitor).
</verification>

\<success_criteria>

- The app instantiates one QtExecutor-backed ActionEngine (first in the app) with real executor lambdas and the StreamDockInputService.
- On a Stream Deck arrival the input service drives the Phase-14 held handle and pumps poll(); on departure the pump stops.
- INPUT-03/04/05 are live in the running app (key/encoder/touch fire bound chains; swipe emits a logged page-nav intent).
- The ActionEngine/QtExecutor ownership is the reuse surface Phase 19 extends; wire decode + COD-031 boundary untouched.
  \</success_criteria>

<output>
Create `.planning/phases/15-stream-dock-input-routing/15-02-SUMMARY.md` when done.
Record: how the input service obtained the Phase-14 held handle (control-service getter vs shared
DeviceLookup), the arrival/departure wiring used, whether ActionEngine::setProfile was wired on
profileChanged (and why), the runCommand settingsJson parse shape chosen (defensive minimal vs full —
Phase 20 owns the PI schema), the page-nav sink (logged) and the Phase-16 hand-off, and any follow-up
for Phase 19 (plugin executor + second event consumer) or Phase 25 (live hardware witness).
</output>
