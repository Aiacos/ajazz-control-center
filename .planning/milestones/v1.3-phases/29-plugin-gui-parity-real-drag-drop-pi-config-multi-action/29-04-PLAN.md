---
phase: 29-plugin-gui-parity-real-drag-drop-pi-config-multi-action
plan: 04
type: execute
wave: 4
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - .planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-LIVE-VERIFICATION.md
autonomous: false
requirements: [PLUGIN-21, PLUGIN-22, PLUGIN-23]
must_haves:
  truths:
    - All three deliverables verified LIVE on the running GUI against the AKP05E via the debug channel
    - PLUGIN-21 is proven by a REAL pointer drag, not a commitBinding call
    - ctest -E qml >= baseline with 0 failed across the full phase
  artifacts:
    - path: .planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-LIVE-VERIFICATION.md
      provides: recorded live-verification walkthrough with screenshot + protocolLog evidence
      min_lines: 40
  key_links:
    - from: 29-LIVE-VERIFICATION.md
      to: qml.drag / plugin.protocolLog / screenshot evidence
      via: debug-channel observations recorded per deliverable
      pattern: qml.drag|protocolLog|screenshot
---

<objective>
End-to-end LIVE verification of all three Phase 29 deliverables on the running GUI against the physical AKP05E, driven through the AJAZZ_DEBUG_CONTROL channel. This is the convergence gate: it catches the integration/wiring bugs unit tests + code review miss (the recurring CLAUDE.md lesson — Phase 27/28 each had a 700+-green-tests bug caught only live).

Purpose: PLUGIN-21 + PLUGIN-22 + PLUGIN-23 success criteria each demand a LIVE proof. PLUGIN-21 specifically MUST be proven by a driven REAL pointer drag (press tile -> move over key -> release), NOT a ProfileController.commitKeyBinding call (the exact Phase-28 trap). Record the walkthrough with screenshot + on-disk + protocolLog evidence.

Output: 29-LIVE-VERIFICATION.md with pass/fail per deliverable + the evidence (driven pointer drag result, PI round-trip on-disk + didReceiveSettings, multi-action reorder/trash/restart).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-CONTEXT.md
@.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-01-PLAN.md
@.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-02-PLAN.md
@.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-03-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driven end-to-end walkthrough + evidence capture</name>
  <files>.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-LIVE-VERIFICATION.md</files>
  <read_first>
    - 29-01-SUMMARY.md, 29-02-SUMMARY.md, 29-03-SUMMARY.md (what landed + the documented root cause + the worked context-id example)
    - 29-CONTEXT.md (the three deliverables + the Phase-28 trap)
    - CLAUDE.md "Debug-channel verification — MANDATORY" + "Headless live-drive recipe"
  </read_first>
  <action>
    Build (`cmake --build build/linux-release --target ajazz-control-center ajazz_unit_tests -j$(nproc)`) and launch `setsid env AJAZZ_DEBUG_CONTROL=1 build/linux-release/src/app/ajazz-control-center` (AKP05E + System Monitor / Weather plugin installed; kill ALL prior instances + rm the stale socket first per the headless recipe). Drive and capture evidence for each deliverable:
    (PLUGIN-21) `scripts/ajazz-debug qml.drag --params '{"from":"libraryTile_0","to":"<keycell-objectName>"}'` -> confirm via profile.list/qml.get the key bound the plugin action AND `screenshot` shows the plugin icon on the rendered key. Repeat for an encoder dial + a touch-strip zone. Explicitly note this is a DRIVEN POINTER drag, not a commitBinding call.
    (PLUGIN-22) Select the bound key, set a value in the PI (driven), then read the on-disk record at `plugins/<uuid>/settings/<codename>#root#Keypad#<row>#<col>.json` AND `plugin.protocolLog` for the plugin's didReceiveSettings — record that all three agree.
    (PLUGIN-23) Append a second action, `qml.drag` reorder two rows, `qml.drag` a row to the trash, restart the app, confirm the surviving multi-action list persists in order.
    Run `ctest --preset linux-release -E qml` and record the count + 0-failed. Write all findings (pass/fail, the exact commands, screenshot paths, on-disk JSON snippets, protocolLog lines) into 29-LIVE-VERIFICATION.md. If any deliverable FAILS, record it honestly with the observed symptom and file:line hypothesis (per CLAUDE.md honesty contract) — do NOT mark a broken gesture "done".
  </action>
  <verify>
    <automated>test -f .planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-LIVE-VERIFICATION.md && grep -c 'qml.drag\|protocolLog\|screenshot\|PASS\|FAIL' .planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-LIVE-VERIFICATION.md && ctest --preset linux-release -E qml 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - 29-LIVE-VERIFICATION.md exists and records a PASS/FAIL per deliverable with evidence (qml.drag result, screenshot path, on-disk JSON, protocolLog line).
    - PLUGIN-21 evidence is a DRIVEN POINTER drag (qml.drag / input.pointer), explicitly NOT a commitBinding call.
    - ctest `-E qml` count + 0-failed recorded; >= baseline (well above 739).
  </acceptance_criteria>
  <done>All three deliverables driven live with recorded evidence; PLUGIN-21 proven by a real pointer drag; ctest green; 29-LIVE-VERIFICATION.md written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm live-verification evidence for all three deliverables</name>
  <action>Human-in-the-loop verification gate. Drive the live debug-channel checks in <how-to-verify> against the running GUI, then pause for operator sign-off.</action>
  <what-built>A recorded live-verification walkthrough proving all three Phase 29 deliverables on the running GUI against the AKP05E.</what-built>
  <how-to-verify>
    1. Read 29-LIVE-VERIFICATION.md.
    2. Confirm PLUGIN-21 was proven by a DRIVEN POINTER drag (and ideally a real physical-mouse drag), not a commitBinding shortcut.
    3. Confirm PLUGIN-22 shows PI value == on-disk record == plugin didReceiveSettings.
    4. Confirm PLUGIN-23 shows 2+ actions, reorder, trash-remove, and restart-persistence.
    5. Confirm ctest `-E qml` >= baseline, 0 failed, COD-031 clean (`grep -rn nlohmann src/core/include/` == 0), no protocol/wire/hidraw changes.
  </how-to-verify>
  <resume-signal>Type "approved" if all three deliverables verify live with honest evidence, or list the gaps to route into gap-closure.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                        |
| ---------------------------------- | ------------------------------------------------------------------ |
| debug channel -> live app + device | verification drives the real UI and renders on the physical AKP05E |

## STRIDE Threat Register

| Threat ID        | Category    | Component                         | Disposition | Mitigation Plan                                                                                               |
| ---------------- | ----------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-29-07          | Repudiation | "done" claimed without live proof | mitigate    | This plan IS the live-proof gate; PLUGIN-21 requires a driven pointer drag, not commitBinding (Phase-28 trap) |
| \</threat_model> |             |                                   |             |                                                                                                               |

<verification>
- 29-LIVE-VERIFICATION.md records PASS/FAIL + evidence per deliverable.
- `ctest --preset linux-release -E qml` >= baseline, 0 failed; `grep -rn nlohmann src/core/include/` == 0.
- No opcode/BAT/LIG/hidraw/protocol changes anywhere in the phase diff.
</verification>

\<success_criteria>
All three Phase 29 deliverables verified live on the AKP05E via the debug channel with recorded evidence; PLUGIN-21 proven by a real driven pointer drag; tests green; COD-031 clean; no wire changes.
\</success_criteria>

<output>
Create `.planning/phases/29-plugin-gui-parity-real-drag-drop-pi-config-multi-action/29-04-SUMMARY.md` when done.
</output>
