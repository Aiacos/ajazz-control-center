---
phase: 23-auxiliary-display-surfaces
plan: 02
type: execute
wave: 2
depends_on: [14, 16, 23-01]
files_modified:
  - src/app/src/stream_dock_control_service.hpp
  - src/app/src/stream_dock_control_service.cpp
  - src/app/src/application.cpp
  - tests/unit/test_stream_dock_control_service.cpp
autonomous: true
requirements: [DISPLAY-10]
must_haves:
  truths:
    - repaintEncodersFromProfile iterates the active Profile's encoders map and paints each bound encoder overlay from its EncoderBinding::state KeyState (imagePath -> loaded QImage; else background -> solid fill).
    - Each bound encoder repaint defaults to the vendor-preferred DRA touch-strip-zone path (assignTouchStripZone, x=encoderIndex*200); the ENC path (assignEncoderImage) remains reachable as the documented fallback and is NOT deleted.
    - The existing profileChanged repaint trigger also repaints encoder overlays (encoder overlays come back on profile load like keys do).
    - The encoder repaint reuses the SAME profile-accessor seam and the SAME held handle + coalesced drain as the Phase-14 key repaint — no parallel profile accessor, no parallel drain.
  artifacts:
    - path: src/app/src/stream_dock_control_service.cpp
      provides: repaintEncodersFromProfile iterating Profile::encoders -> renderKeyState -> assignTouchStripZone (DRA default) with ENC kept reachable
      contains: repaintEncodersFromProfile
    - path: src/app/src/application.cpp
      provides: profileChanged wired to also repaint encoder overlays (no second monitor / accessor)
      contains: repaintEncodersFromProfile
    - path: tests/unit/test_stream_dock_control_service.cpp
      provides: a test proving >=1 bound encoder produces a DRA zone burst on repaint from a fake profile
      contains: repaintEncodersFromProfile
  key_links:
    - from: src/app/src/stream_dock_control_service.cpp
      to: core::Profile::encoders
      via: iterate encoders map, EncoderBinding::state KeyState -> renderKeyState -> assignTouchStripZone
      pattern: repaintEncodersFromProfile
    - from: src/app/src/application.cpp
      to: StreamDockControlService::repaintEncodersFromProfile
      via: the existing ProfileController::profileChanged connection (reuse, do not add a second)
      pattern: repaintEncodersFromProfile
---

<objective>
Repaint per-encoder overlays from the active profile: extend `StreamDockControlService`
with `repaintEncodersFromProfile`, which iterates `Profile::encoders` (each
`EncoderBinding::state` is a `KeyState` carrying imagePath/background) and paints each
bound encoder's overlay through the Plan-01 `assignTouchStripZone` (the vendor-preferred
DRA zone path, x=encoderIndex*200), keeping `assignEncoderImage` (ENC) reachable as the
documented fallback. Wire it into Application's existing `profileChanged` repaint so
encoder overlays come back on profile load exactly like keys do.

Purpose: completes DISPLAY-10's profile-repaint half. Phase 14 repaints keys on
profileChanged; this adds the parallel encoder repaint reusing the SAME profile-accessor
seam + held handle + coalesced drain (RESEARCH A5 — reuse verbatim, do not invent a
parallel one). The whole-strip MAI image is an editor/explicit action, not a per-control
profile binding (RESEARCH A4), so only encoder overlays are profile-driven here. The
DRA-default-with-ENC-fallback choice ships BOTH paths and flags the divergence PROVISIONAL
for the Phase-25 hardware reconciliation (LOCKED: hardware wins).

Output: `repaintEncodersFromProfile` on the service, its Application wiring, and a
MockTransport test proving a bound encoder yields a DRA zone burst on repaint.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-auxiliary-display-surfaces/23-CONTEXT.md
@.planning/phases/23-auxiliary-display-surfaces/23-RESEARCH.md
@.planning/phases/23-auxiliary-display-surfaces/23-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Plan-01 added assignMainImage / assignEncoderImage / assignTouchStripZone to the service.
     This plan adds the profile-driven encoder repaint that calls them. -->

core::Profile (src/core/include/ajazz/core/profile.hpp):
std::string deviceCodename;
std::unordered_map\<std::uint16_t, Binding> keys; // key index -> Binding (Phase 14 repaints these)
std::unordered_map\<std::uint16_t, EncoderBinding> encoders; // encoder index -> EncoderBinding
// EncoderBinding::state is a KeyState: std::optional<std::string> imagePath; std::optional<Rgb> background;
// Encoders are 0-based (0..3). Confirm the profile encoder-index base by reading profile usage before
// the loop; pass the index THROUGH to the backend (which range-checks). Document the mapping in SUMMARY.

Plan-01 service surface (reuse — do NOT re-route through the wire layer directly):
void assignTouchStripZone(std::uint8_t zone /*0..3*/, QImage const& img); // DRA, x=zone\*200 (default)
void assignEncoderImage(std::uint8_t encoderIndex /*0-based*/, QImage const& img); // ENC (kept fallback)

Phase-14 reuse seams (read 14-02-SUMMARY for the canonical names):

- The profile-accessor seam (lambda vs ProfileController pointer) that repaintFromProfile uses —
  repaintEncodersFromProfile MUST use the SAME accessor, not a new one.
- renderKeyState (or the equivalent Phase-14 helper that turns a KeyState into a QImage:
  imagePath -> loaded QImage via Qt safe decoders; else background -> solid-fill QImage). REUSE it;
  do NOT duplicate the load/fill logic.
- The single-shot QTimer coalesced drain that assignKeyImage/assignTouchStripZone already arm.
- The ProfileController::profileChanged connection in application.cpp that already drives
  repaintFromProfile — reuse it to ALSO drive repaintEncodersFromProfile (no second monitor).

ENCODER-OVERLAY -> ZONE mapping (PROVISIONAL — akp05_vendor.md §5; confirm Phase 25):
encoder i (0..3) -> assignTouchStripZone(i, img) -> DRA location=i, x=i\*200, y=0, w=200, h=100.
Default to DRA (vendor §10 P0 partial-update path); keep assignEncoderImage(i, img) (ENC) as the
documented fallback in a comment. Do NOT delete ENC (LOCKED: hardware decides in Phase 25).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repaintEncodersFromProfile (DISPLAY-10 repaint) + MockTransport test</name>
  <files>src/app/src/stream_dock_control_service.hpp, src/app/src/stream_dock_control_service.cpp, tests/unit/test_stream_dock_control_service.cpp</files>
  <read_first>
    - src/app/src/stream_dock_control_service.cpp (repaintFromProfile: the key repaint loop, the profile-accessor seam, the renderKeyState helper, the coalesced drain — repaintEncodersFromProfile mirrors this for encoders)
    - .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md (the canonical profile-accessor seam + renderKeyState helper name + keyIndex mapping to reuse)
    - .planning/phases/23-auxiliary-display-surfaces/23-01-SUMMARY.md (the Plan-01 assignTouchStripZone/assignEncoderImage signatures + the pending-write/drain representation chosen for aux surfaces)
    - src/core/include/ajazz/core/profile.hpp (Profile::encoders map, EncoderBinding::state KeyState imagePath/background)
    - src/devices/streamdeck/src/akp05.cpp:715 setTouchStripImage (DRA, refuses location>=4), :635 setEncoderImage (ENC, kept fallback)
    - docs/protocols/streamdeck/akp05.md (§Layout "overlays are touch-strip zones") + akp05_vendor.md §5 (DRA rect PROVISIONAL framing)
  </read_first>
  <behavior>
    - Test (DISPLAY-10 repaint): with a fake active profile whose encoders map binds >=1 encoder with an imagePath (or background), firing repaintEncodersFromProfile() enqueues an image for that encoder and the drain emits a DRA zone burst — bytes[5..7]==0x44,0x52,0x41 (D,R,A), location byte[12]==encoderIndex, BE16 x at bytes[17..18]==encoderIndex*200, trailing ULEND.
    - Test (multiple encoders): with >=2 bound encoders, each bound encoder produces its own DRA burst with the correct per-encoder x (x=i*200).
    - Test (unbound encoders skipped): an encoder with neither imagePath nor background produces no write.
    - Test (ENC fallback reachable / static): grep proves assignEncoderImage (ENC) remains called/reachable from the service as the documented fallback (not deleted).
  </behavior>
  <action>
    Add `void repaintEncodersFromProfile()` to StreamDockControlService, mirroring the Phase-14
    repaintFromProfile key loop for encoders. It MUST:
    - Obtain the active Profile via the SAME profile-accessor seam repaintFromProfile uses (the
      lambda/controller-pointer recorded in 14-02-SUMMARY) — do NOT add a new accessor.
    - Iterate `Profile::encoders` (each entry is encoderIndex -> EncoderBinding; EncoderBinding::state
      is a KeyState). For each bound entry (imagePath OR background present), render the overlay QImage
      using the SAME renderKeyState helper the key repaint uses (imagePath -> loaded QImage via Qt safe
      decoders; else background -> solid fill) — do NOT duplicate the load/fill logic.
    - Default each encoder overlay to the vendor-preferred DRA zone path by calling
      `assignTouchStripZone(static_cast<std::uint8_t>(encoderIndex), img)` (Plan-01) — this routes
      through the held handle + coalesced drain with x=encoderIndex*200. Pass the encoder index THROUGH
      (the backend range-checks `< TouchZoneCount`); confirm the profile encoder-index base first and
      document the mapping in the SUMMARY (Pitfall 3).
    - Keep `assignEncoderImage` (ENC) reachable as the documented fallback: leave a clearly-commented
      call path (e.g. a commented sibling line or a guarded alternate) so the ENC path is NOT deleted.
      Add/keep the PROVISIONAL §5 comment stating Phase 25 (VERIFY-05) reconciles ENC-vs-zone on hardware
      and updates the RE doc + code (LOCKED: hardware wins; do NOT assert which path is correct).
    Skip unbound encoders (neither imagePath nor background) — no write.

```
Then EXTEND tests/unit/test_stream_dock_control_service.cpp with the behaviors above: build a
MockTransport-backed Akp05Device, inject it via the fake DeviceLookup, drive a fake active profile
(via the test seam the Phase-14 repaint test already uses to set Profile::encoders), call
repaintEncodersFromProfile(), pump the drain, and assert the DRA zone bytes (D,R,A at 5..7, location
at 12, BE16 x at 17..18 == i*200) + trailing ULEND per bound encoder, plus that an unbound encoder
produces no write. Use tests::qtGuiApp(); ASCII-only TEST_CASE/SECTION titles; tag `[stream-dock-control]`.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release >/dev/null 2>&1 && ctest --preset linux-release -R "stream-dock-control|StreamDockControlService" --output-on-failure && grep -n 'repaintEncodersFromProfile\|assignEncoderImage' src/app/src/stream_dock_control_service.cpp | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R "stream-dock-control|StreamDockControlService"` reports 100% passed.
    - A bound encoder repaint emits a DRA burst: bytes[5..7]==0x44,0x52,0x41, location byte[12]==encoderIndex, bytes[17..18]==encoderIndex*200 (BE16), + trailing ULEND (DISPLAY-10 repaint).
    - >=2 bound encoders each emit a DRA burst with the correct per-encoder x.
    - An unbound encoder (no imagePath/background) produces zero writes.
    - `grep -n repaintEncodersFromProfile src/app/src/stream_dock_control_service.cpp` shows the method iterating Profile::encoders via the SAME profile accessor as repaintFromProfile (no new accessor).
    - `grep -n assignEncoderImage src/app/src/stream_dock_control_service.cpp` shows the ENC fallback reachable + a PROVISIONAL comment routing reconciliation to Phase 25 (ENC NOT deleted).
    - `git diff --stat` excludes akp05.cpp / akp05_protocol.hpp; ASCII titles; full suite green.
  </acceptance_criteria>
  <done>repaintEncodersFromProfile paints each bound encoder overlay from Profile::encoders through the DRA zone path (x=encoderIndex*200) on the held handle + coalesced drain, with ENC kept as the documented PROVISIONAL fallback; MockTransport test proves the per-encoder DRA bytes and is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire encoder repaint into Application's profileChanged path</name>
  <files>src/app/src/application.cpp</files>
  <read_first>
    - src/app/src/application.cpp (the existing ProfileController::profileChanged -> StreamDockControlService::repaintFromProfile connection block, added in Phase 14; the m_streamDockControl member + setActiveDevice-on-arrival path)
    - src/app/src/stream_dock_control_service.hpp (repaintEncodersFromProfile from Task 1 + the Phase-14 repaintFromProfile signature)
    - .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md (where/how the profileChanged connection + active-device selection were wired — reuse, do not add a second monitor or accessor)
  </read_first>
  <action>
    In application.cpp, ensure a loaded profile also repaints encoder overlays. Reuse the EXISTING
    ProfileController::profileChanged connection that already drives repaintFromProfile (Phase 14):
    either connect profileChanged additionally to
    `StreamDockControlService::repaintEncodersFromProfile`, OR (preferred if Phase 14 used a single
    repaint slot) have the existing repaint slot also invoke repaintEncodersFromProfile so keys and
    encoder overlays repaint together. Do NOT add a second HotplugMonitor, a second profile accessor,
    or a second QTimer — reuse the Phase-14 plumbing verbatim (RESEARCH A5). Keep the connect block
    near the existing profileChanged wiring; keep member/init order -Wreorder-clean (no new member is
    needed — m_streamDockControl already exists from Phase 14). If Phase 14 chose a different active-
    device selection simplification, honor it unchanged (encoder repaint targets the same active
    Stream Deck).
  </action>
  <verify>
    <automated>cmake --build --preset linux-release >/dev/null 2>&1 && grep -n 'repaintEncodersFromProfile' src/app/src/application.cpp && ctest --preset linux-release --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - The app target compiles clean under `cmake --build --preset linux-release` (no -Wreorder error).
    - `grep -n 'repaintEncodersFromProfile' src/app/src/application.cpp` shows it driven from the EXISTING profileChanged path (key repaint and encoder repaint fire together on profile load).
    - No second HotplugMonitor, profile accessor, or QTimer was added (reuses Phase-14 plumbing).
    - Full `ctest --preset linux-release` suite green.
  </acceptance_criteria>
  <done>A loaded profile repaints both keys and encoder overlays via the existing Phase-14 profileChanged plumbing; no parallel monitor/accessor/timer added; suite green.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                                                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| profile imagePath -> QImage load        | Profile-supplied encoder-overlay image paths are decoded and pushed; in Phase 23 the profile is user-authored/local. Use Qt's safe image decoders (the same as the key repaint); deep untrusted-path validation is Phase 16/19. |
| app service -> USB device (held handle) | The encoder repaint pushes through the held shared_ptr<IDevice> spanning event-loop turns; a device yank mid-repaint must degrade safely (backend false-return no-op).                                                          |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                 | Disposition       | Mitigation Plan                                                                                                                                                                                                          |
| --------- | --------- | --------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-23b-01  | DoS       | profile with many bound encoders repaints in a burst      | mitigate          | Each encoder repaint routes through the Phase-14 single-shot QTimer coalesced drain (last-write-wins per surface) reused via assignTouchStripZone; backend caps payload + emits ULEND per burst (DOCK-02). No new drain. |
| T-23b-02  | Tampering | untrusted profile imagePath -> encoder-overlay image load | accept (Phase 23) | Reuse the SAME renderKeyState/Qt safe-decoder path as the Phase-14 key repaint (T-14b-03); no new raw arbitrary-path read. Deep validation is Phase 16/19. [ASSUMED — confirm Phase-16 posture]                          |
| T-23b-03  | Tampering | profile encoder index out of range pokes wrong zone       | mitigate          | Pass the profile encoder index THROUGH assignTouchStripZone -> backend range-check (location < TouchZoneCount, akp05.cpp:726); the service does not pre-compute an unchecked location.                                   |
| T-23b-04  | DoS       | device yank mid encoder-repaint burst                     | mitigate          | setTouchStripImage / sendImage catch the transport throw -> false return; held-handle re-resolve on hot-plug, not on a stale ptr (Pitfall 2). [VERIFIED akp05.cpp:887]                                                   |
| T-23b-SC  | Tampering | npm/pip/cargo installs                                    | accept            | No package installs — pure in-tree C++/Qt; Package Legitimacy Gate N/A.                                                                                                                                                  |

V5 (input validation): encoder/zone index range-checked by the backend (< 4); the repaint passes through Plan-01's guarded assignTouchStripZone, never bypassing it.
\</threat_model>

<verification>
- `ctest --preset linux-release -R "stream-dock-control|StreamDockControlService"` green (DISPLAY-10 encoder repaint -> DRA zone bursts).
- `grep -n repaintEncodersFromProfile src/app/src/application.cpp` shows it driven from the existing profileChanged path.
- `grep -n assignEncoderImage src/app/src/stream_dock_control_service.cpp` shows the ENC fallback reachable + PROVISIONAL comment (ENC NOT deleted — LOCKED).
- `git diff --stat` excludes akp05.cpp / akp05_protocol.hpp; `grep -c nlohmann src/core/include` unchanged (COD-031).
- App compiles -Wreorder-clean; full `ctest --preset linux-release` suite green.
</verification>

\<success_criteria>

- DISPLAY-10 (repaint): repaintEncodersFromProfile paints each bound encoder overlay from Profile::encoders through the DRA zone path on profile load.
- DISPLAY-10 (provisional): the DRA-default-with-ENC-fallback choice ships both paths, deletes neither, and flags ENC-vs-zone PROVISIONAL for Phase-25 reconciliation.
- Encoder repaint reuses the Phase-14 profile accessor + held handle + coalesced drain + profileChanged connection (no parallel plumbing).
- The wire layer and COD-031 boundary are untouched; MockTransport test is the hardware-free gating proof; LIVE confirmation is Phase 25.
  \</success_criteria>

<output>
Create `.planning/phases/23-auxiliary-display-surfaces/23-02-SUMMARY.md` when done.
Record: the profile encoder-index base mapping confirmed, how repaintEncodersFromProfile was wired
into the existing profileChanged path (additional connect vs single combined slot), the ENC-fallback
representation kept (commented sibling vs guarded alternate), and any follow-up for Phase 16 (editor
per-surface assignment UX) or Phase 25 (live witness: which path the firmware honors, geometry confirm).
</output>
