---
phase: 23-auxiliary-display-surfaces
plan: 01
type: execute
wave: 1
depends_on: [14, 16]
files_modified:
  - src/app/src/stream_dock_control_service.hpp
  - src/app/src/stream_dock_control_service.cpp
  - tests/unit/test_stream_dock_control_service.cpp
autonomous: false
requirements: [DISPLAY-10]
must_haves:
  truths:
    - Phase 14 has executed — src/app/src/stream_dock_control_service.{hpp,cpp} exist and the Phase-14 StreamDockControlService MockTransport test is green BEFORE any aux-surface wiring runs.
    - assignMainImage(QImage) routes through the held shared_ptr<IDevice> via dynamic_cast<IDisplayCapable*> and emits a MAI header (bytes 5..7 == M,A,I) with no index byte, terminated by ULEND.
    - assignEncoderImage(0-based index, QImage) routes through dynamic_cast<IEncoderCapable*> and emits an ENC header (bytes 5..7 == E,N,C) with the 0-based encoder index at byte 12, terminated by ULEND.
    - assignTouchStripZone(zone, QImage) routes through dynamic_cast<ITouchStripDisplayCapable*> and emits a DRA rect header (bytes 5..7 == D,R,A) with x == zone*200, terminated by ULEND.
    - Both the ENC per-encoder path and the DRA touch-strip-zone path are wired and reachable; NEITHER is deleted; the ENC-vs-zone divergence is commented PROVISIONAL (§5) for Phase-25 reconciliation.
  artifacts:
    - path: src/app/src/stream_dock_control_service.hpp
      provides: 'public aux-surface reuse surface: assignMainImage, assignEncoderImage, assignTouchStripZone'
      contains: assignTouchStripZone
    - path: src/app/src/stream_dock_control_service.cpp
      provides: dynamic_cast routing to IDisplayCapable/IEncoderCapable/ITouchStripDisplayCapable through the held handle, reusing the coalesced QTimer drain
      contains: assignTouchStripZone
    - path: tests/unit/test_stream_dock_control_service.cpp
      provides: service-level MockTransport wire assertions for ENC index byte, MAI whole-strip, DRA rect x=zone*200
      contains: assignTouchStripZone
  key_links:
    - from: src/app/src/stream_dock_control_service.cpp
      to: core::IEncoderCapable
      via: dynamic_cast on held shared_ptr.get(), null-check within 3 lines, setEncoderImage(0-based)
      pattern: dynamic_cast<core::IEncoderCapable
    - from: src/app/src/stream_dock_control_service.cpp
      to: core::ITouchStripDisplayCapable
      via: dynamic_cast on held shared_ptr.get(), null-check within 3 lines, setTouchStripImage(location=zone, x=zone*200)
      pattern: dynamic_cast<core::ITouchStripDisplayCapable
    - from: src/app/src/stream_dock_control_service.cpp
      to: core::IDisplayCapable
      via: dynamic_cast on held shared_ptr.get(), null-check within 3 lines, setMainImage
      pattern: dynamic_cast<core::IDisplayCapable
---

<objective>
Extend the Phase-14 `StreamDockControlService` with the three auxiliary-surface
assign methods — `assignMainImage` (MAI, 800x100), `assignEncoderImage` (ENC,
100x100, 0-based index), and `assignTouchStripZone` (DRA rect, x=zone*200) — and
prove each emits the correct wire bytes with a service-level MockTransport test.
This is the app-layer wiring of the already-byte-tested AKP05E aux-surface backend
(DISPLAY-10): today `grep setMainImage|setEncoderImage|setTouchStripImage src/app/`
returns 0 hits, exactly the Phase-10 gap pattern that Phase 14 closed for keys.

Purpose: closes DISPLAY-10's wiring half. Every byte that hits the wire is already
implemented + tested in `akp05.cpp` (do NOT touch it); the only new code is
app-service glue routing through the held `shared_ptr<IDevice>` via `dynamic_cast`,
reusing the Phase-14 coalesced QTimer drain. Ships BOTH the ENC per-encoder path and
the DRA touch-strip-zone path, deletes NEITHER, and flags the ENC-vs-zone divergence
PROVISIONAL — the live reconciliation is Phase 25 (VERIFY-05), not here.

Output: the three new public methods on `StreamDockControlService`, the
PROVISIONAL §5 framing comment, and extended `test_stream_dock_control_service.cpp`
wire assertions that are the hardware-free gating proof.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-auxiliary-display-surfaces/23-CONTEXT.md
@.planning/phases/23-auxiliary-display-surfaces/23-RESEARCH.md
@.planning/phases/23-auxiliary-display-surfaces/23-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted in-tree this session. Use these directly; do NOT alter the wire layer (akp05.cpp / akp05_protocol.hpp are LOCKED). -->

INDEX-BASE CONVENTIONS (Pitfall 3 — mixing these silently no-ops via backend range-check):

- keys: 1-based (1..10), backend rejects 0. [out of scope here — Phase 14 owns keys]
- encoders: 0-based (0..EncoderCount-1, EncoderCount=4); setEncoderImage refuses >=4.
- touch-strip location: 0-based zone-id, must be < TouchZoneCount (4); setTouchStripImage refuses >=4 and refuses 0x12 (reserved M_V).

core::IDisplayCapable (capabilities.hpp:124, :182):
void setMainImage(std::span\<std::uint8_t const> rgba, std::uint16_t width, std::uint16_t height);
// backend resizes to 800x100 native + JPEG (akp05MainTransform), emits MAI header + chunks + ULEND.

core::IEncoderCapable (capabilities.hpp:740, :760):
EncoderInfo encoderInfo() const noexcept; // .count == EncoderCount (4)
void setEncoderImage(std::uint8_t index /*0-based*/, std::span\<std::uint8_t const> rgba,
std::uint16_t width, std::uint16_t height);
// backend resizes to 100x100 (akp05EncoderTransform), range-checks index < 4, emits ENC header
// with the 0-based index at byte 12 + chunks + ULEND.

core::ITouchStripDisplayCapable (capabilities.hpp:1531, :1566):
TouchStripInfo touchStripInfo() const noexcept; // .widthPx 800, .heightPx 480, .zoneCount 4
bool setTouchStripImage(std::span\<std::uint8_t const> rgba,
std::uint16_t srcWidth, std::uint16_t srcHeight,
std::uint8_t location, std::uint16_t x, std::uint16_t y,
std::uint16_t rectWidth, std::uint16_t rectHeight);
// emits DRA rect header (BE32 size 8..11, location byte 12, BE16 width 13..14 / height 15..16 /
// x 17..18 / y 19..20) + chunks + ULEND. Returns false on out-of-range location / empty input /
// device-yank mid-burst (does NOT throw). PROVISIONAL geometry (akp05_vendor.md §5).

Protocol opcode markers for MockTransport assertions (akp05_protocol.hpp):

- CmdMainImage "MAI" = 0x4d 0x41 0x49 at write offset bytes 5..7 (no index byte).
- CmdEncImage "ENC" = 0x45 0x4e 0x43 at bytes 5..7; 0-based encoder index at byte 12.
- CmdSecondaryScreen "DRA" = 0x44 0x52 0x41 at bytes 5..7; location byte 12; BE16 x at bytes 17..18.
- ULEND = "U L E N D" = 0x55 0x4c 0x45 0x4e 0x44 at bytes 5..9 of the trailing write of every burst.

ENCODER-ZONE -> DRA RECT GEOMETRY (host-side, PROVISIONAL — akp05_vendor.md §5; confirm Phase 25):
zone i (0..3): location = i, x = i\*200, y = 0, rectWidth = 200, rectHeight = 100.
The width/height/y are a Ghidra-derived hypothesis; the test pins what OUR CODE EMITS (a contract),
NOT what the device wants (unknown). Comment provisional; route reconciliation to Phase 25.

Phase-14 service shape (read 14-02-SUMMARY for the canonical seam — do NOT invent a parallel one):
StreamDockControlService is a QObject with a held `std::shared_ptr<core::IDevice> m_activeDevice`,
a single-shot drain `QTimer`, a pending-write map, and a DeviceLookup. The aux assigns reuse this
SAME held handle + the SAME coalesced drain — they are siblings of assignKeyImage, not a new path.

Test factory (akp05.cpp:928) + header-only mock (tests/unit/fixtures/mock_transport.hpp):
core::DevicePtr makeAkp05WithTransport(DeviceDescriptor, DeviceId, TransportPtr);
MockTransport: open(); enqueueReadFeature(bytes); writes() -> recorded packets.
Use tests::qtGuiApp() (the encode path drives QImage). ASCII-only TEST_CASE/SECTION titles.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: STOP gate — confirm Phase 14 StreamDockControlService exists and is green</name>
  <files>(read-only gate — no files modified)</files>
  <what-built>
    Nothing yet. This is the dependency STOP gate (RESEARCH.md Pitfall 1 / STOP Gate table):
    Phase 23 extends the Phase-14 `StreamDockControlService`, which was planned-not-executed at
    Phase-23 planning time (no 14-02-SUMMARY.md; src/app/src/stream_dock_control_service.{hpp,cpp}
    did not exist). If Phase 14 (and ideally 16) have not executed, there is nothing to extend and
    Phase 23 MUST NOT start.
  </what-built>
  <read_first>
    - .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md (the canonical service seam:
      the profile-accessor seam chosen, the keyIndex base mapping, the default-brightness constant,
      whether the service was QML-exposed — Phase 23 MUST reuse the SAME held handle + coalesced
      drain, not invent a parallel one)
    - src/app/src/stream_dock_control_service.hpp (the existing public surface to extend)
  </read_first>
  <action>
    This is a verification-only gate; it modifies no files. Run the four checks under
    how-to-verify and confirm Phase 14 has executed before any aux-surface code is written.
    Read 14-02-SUMMARY.md and record the held-handle + coalesced-drain + profile-accessor seam
    that Task 2 (and Plan 02) must reuse verbatim. If any check fails, report the gate RED and halt.
  </action>
  <how-to-verify>
    Run these checks; ALL must pass before proceeding to Task 2:
    1. `ls src/app/src/stream_dock_control_service.hpp src/app/src/stream_dock_control_service.cpp`
       — both files exist.
    2. `ls .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md` — exists (Phase 14
       executed). If absent, STOP: report "Phase 14 not executed — Phase 23 has no service to extend"
       and do not proceed.
    3. `ctest --preset linux-release -R StreamDockControlService --output-on-failure` — reports
       100% passed (the Phase-14 gating proof is green).
    4. Read 14-02-SUMMARY.md and record the held-handle + coalesced-drain seam Phase 23 will reuse.
    If any check fails, the gate is RED — surface the failure and stop; do not write aux-surface code.
  </how-to-verify>
  <acceptance_criteria>
    - Both `stream_dock_control_service.{hpp,cpp}` exist.
    - `14-02-SUMMARY.md` exists OR the gate is reported RED and the phase halts.
    - `ctest --preset linux-release -R StreamDockControlService` is green.
    - The Phase-14 held-handle + drain seam is recorded for reuse in Task 2.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all four checks pass, or report which check failed.</resume-signal>
  <done>Phase 14 confirmed executed (service files present, 14-02-SUMMARY.md present, Phase-14 test green) and its reuse seam recorded; OR the gate is RED and the phase halts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add aux-surface assign methods + service-level MockTransport wire tests (DISPLAY-10)</name>
  <files>src/app/src/stream_dock_control_service.hpp, src/app/src/stream_dock_control_service.cpp, tests/unit/test_stream_dock_control_service.cpp</files>
  <read_first>
    - src/app/src/stream_dock_control_service.hpp (the public surface + held shared_ptr + drain QTimer to extend)
    - src/app/src/stream_dock_control_service.cpp (assignKeyImage's dynamic_cast + null-check + coalesced-drain pattern — the aux assigns are siblings of this)
    - .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md (the canonical held-handle / drain / profile-accessor seam to reuse verbatim)
    - src/devices/streamdeck/src/akp05.cpp:604 setMainImage, :635 setEncoderImage (0-based, refuses >=4), :715 setTouchStripImage (refuses location>=4 and 0x12), :670 setSecondaryScreenImage, :856 sendImage emits ULEND
    - src/core/include/ajazz/core/capabilities.hpp (IDisplayCapable :182, IEncoderCapable :760, ITouchStripDisplayCapable :1566 signatures — see <interfaces>)
    - docs/protocols/streamdeck/akp05.md (§Layout: "no separate encoder LCD; overlays are touch-strip zones") + docs/protocols/streamdeck/akp05_vendor.md §2/§5 (ENC/MAI/DRA opcode table; DRA 32-byte rect header — PROVISIONAL; the ENC-vs-zone divergence framing)
    - tests/unit/test_akp05_touch_strip.cpp (the DRA byte-assertion pattern: header[5..7]==D,R,A; BE16 x at 17..18; trailing ULEND at bytes 5..9) + tests/unit/fixtures/mock_transport.hpp (writes())
  </read_first>
  <behavior>
    - Test (DISPLAY-10 MAI): assignMainImage(img) then draining the queue produces a write whose bytes[5..7]==0x4d,0x41,0x49 (M,A,I) with NO encoder/index byte semantics, followed by >=1 chunk and a trailing ULEND (bytes[5..9]==0x55,0x4c,0x45,0x4e,0x44).
    - Test (DISPLAY-10 ENC index byte): assignEncoderImage(2, img) then drain produces a write whose bytes[5..7]==0x45,0x4e,0x43 (E,N,C) and byte[12]==0x02 (0-based encoder index), followed by chunk(s) + ULEND.
    - Test (DISPLAY-10 DRA zone x=zone*200): assignTouchStripZone(1, img) then drain produces a write whose bytes[5..7]==0x44,0x52,0x41 (D,R,A), location byte[12]==0x01, and BE16 x at bytes[17..18]==0x00,0xC8 (200 = zone 1 * 200), followed by chunk(s) + ULEND.
    - Test (range refusal): assignEncoderImage(4, img) and assignTouchStripZone(4, img) each pass through the backend range-check and produce ZERO wire writes (backend refuses index/location >= 4) — the service must NOT pre-compute an unchecked index that bypasses the guard.
    - Test (held handle / Pitfall 2): an aux assign after a prior assign does NOT re-open the transport (no duplicate open recorded) — reuses the Phase-14 held shared_ptr.
    - Test (PROVISIONAL no-deletion / static): grep proves both setEncoderImage and setTouchStripImage are reachable from the service .cpp (ENC path NOT deleted).
  </behavior>
  <action>
    Add three public methods to StreamDockControlService, each a sibling of the existing
    assignKeyImage that REUSES the SAME held m_activeDevice shared_ptr and the SAME coalesced
    single-shot QTimer drain (do NOT open a new handle, do NOT add a second drain, do NOT
    JPEG-encode in the app — ARCH-04 single encode path; pass RGBA8 + dims only):

```
- `void assignMainImage(QImage const& img)`: convert img to RGBA8, route via
  `dynamic_cast<core::IDisplayCapable*>(m_activeDevice.get())` with a null-check within 3
  lines (Pitfall 1), and call `setMainImage(rgba, width, height)`. The backend resizes to
  800x100 and emits MAI + chunks + ULEND. The whole-strip image is an editor/explicit action,
  not a per-control profile binding (RESEARCH A4) — no profile field is read here.

- `void assignEncoderImage(std::uint8_t encoderIndex /*0-based*/, QImage const& img)`: convert
  to RGBA8, route via `dynamic_cast<core::IEncoderCapable*>(m_activeDevice.get())` null-checked
  within 3 lines, and call `setEncoderImage(encoderIndex, rgba, w, h)`. Pass the 0-based index
  THROUGH unchanged — the backend range-checks `< EncoderCount` (do NOT pre-validate or remap;
  let the backend guard refuse out-of-range, Pitfall 3). The ENC path is RE-as-coded and is
  KEPT REACHABLE — do NOT delete it (LOCKED: hardware wins in Phase 25).

- `void assignTouchStripZone(std::uint8_t zone /*0..3*/, QImage const& img)`: convert to RGBA8,
  route via `dynamic_cast<core::ITouchStripDisplayCapable*>(m_activeDevice.get())` null-checked
  within 3 lines, and call `setTouchStripImage(rgba, srcW, srcH, /*location=*/zone,
  /*x=*/zone*200, /*y=*/0, /*rectWidth=*/200, /*rectHeight=*/100)`. The backend range-checks
  location `< TouchZoneCount` and refuses 0x12 — pass `zone` through; do NOT bypass the guard.

Coalescing: prefer routing the three aux assigns through the SAME pending-write/drain machinery
used by assignKeyImage (mirror its key for the aux surfaces, e.g. distinct queue entries for
main / encoder-i / zone-i, last-write-wins) so rapid aux bursts are coalesced (DOCK-02). If the
Phase-14 drain map is keyed only by keyIndex, extend the pending representation to carry a small
surface-tag discriminant rather than adding a parallel timer; document the chosen representation
in the SUMMARY. Whatever the seam, the aux assigns MUST drain through the existing single-shot
QTimer, not a new one.

PROVISIONAL §5 framing (the load-bearing honesty deliverable): add a block comment above the
encoder/zone routing stating that akp05.md reports "no separate encoder LCD; per-encoder
overlays render as the 4 touch-strip zones (DRA)" while the in-code model has an ENC per-encoder
LCD; that BOTH paths are wired and NEITHER is deleted; that the encoder-zone -> rect geometry
(x=zone*200, w=200, h=100, y=0) is a Ghidra-derived hypothesis (akp05_vendor.md §5); and that
Phase 25 (VERIFY-05) is the live witness that reconciles which path the firmware honors and
updates the RE doc + code. Do NOT assert the geometry as confirmed.

Then EXTEND tests/unit/test_stream_dock_control_service.cpp (do NOT create a new test file —
keep the suite registration as-is) with the behaviors above: build a MockTransport, enqueue a
VER feature response, makeAkp05WithTransport, inject via the fake DeviceLookup the same way the
Phase-14 tests do, call each aux assign, pump the QTimer drain via the same event-loop spin the
existing tests use, and assert the exact wire bytes via MockTransport.writes() (offsets per the
<interfaces> block). Add a static/grep-style assertion or an explicit reachability test proving
both setEncoderImage and setTouchStripImage are called from the service (ENC not deleted). Use
tests::qtGuiApp(); ASCII-only TEST_CASE/SECTION titles; tag `[stream-dock-control]`.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release >/dev/null 2>&1 && ctest --preset linux-release -R "stream-dock-control|StreamDockControlService" --output-on-failure && grep -n 'setEncoderImage\|setTouchStripImage' src/app/src/stream_dock_control_service.cpp | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R "stream-dock-control|StreamDockControlService"` reports 100% passed.
    - The MAI assertion: a write with bytes[5..7]==0x4d,0x41,0x49 followed by a trailing ULEND, no index-byte semantics (DISPLAY-10 MAI).
    - The ENC assertion: a write with bytes[5..7]==0x45,0x4e,0x43 and byte[12]==0x02 for assignEncoderImage(2) + trailing ULEND (DISPLAY-10 ENC index byte).
    - The DRA assertion: a write with bytes[5..7]==0x44,0x52,0x41, byte[12]==0x01, and bytes[17..18]==0x00,0xC8 for assignTouchStripZone(1) + trailing ULEND (DISPLAY-10 DRA x=zone*200).
    - assignEncoderImage(4) and assignTouchStripZone(4) each produce ZERO writes (backend range-check honored, not bypassed).
    - The held-handle assertion: no duplicate transport open across two aux assigns (Pitfall 2).
    - `grep -n 'dynamic_cast<core::IEncoderCapable' src/app/src/stream_dock_control_service.cpp` and the IDisplayCapable / ITouchStripDisplayCapable casts are each followed within 3 lines by a nullptr check (Pitfall 1).
    - `grep -n 'setEncoderImage' src/app/src/stream_dock_control_service.cpp` returns a call site (ENC path NOT deleted — LOCKED) AND a comment containing "PROVISIONAL" frames the ENC-vs-zone divergence for Phase 25.
    - `git diff --stat` does NOT include src/devices/streamdeck/src/akp05.cpp or akp05_protocol.hpp (wire layer untouched; LOCKED).
    - TEST_CASE/SECTION titles ASCII-only; full `ctest --preset linux-release` suite green.
  </acceptance_criteria>
  <done>StreamDockControlService exposes assignMainImage/assignEncoderImage/assignTouchStripZone routing through the held handle + coalesced drain to the already-byte-tested MAI/ENC/DRA backend; both ENC and DRA paths reachable, neither deleted, ENC-vs-zone flagged PROVISIONAL for Phase 25; MockTransport wire test green and is the hardware-free gating proof for DISPLAY-10.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                                                    |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| profile/editor image -> QImage load     | An assigned aux-surface image is decoded and pushed; in Phase 23 the source is user-authored/local. Use Qt's safe image decoders; deep untrusted-path handling is Phase 16/19. |
| app service -> USB device (held handle) | The held shared_ptr<IDevice> spans event-loop turns; a device yank mid-aux-burst must degrade safely (backend zombie-contract no-op / false return).                           |
| encoder/zone index -> backend surface   | An out-of-range encoder/zone index could poke the wrong surface if the guard is bypassed.                                                                                      |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                        | Disposition       | Mitigation Plan                                                                                                                                                                                                                                                                                                   |
| --------- | --------- | ---------------------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-23-01   | DoS       | oversized/malformed aux image desyncs firmware                   | mitigate          | Backend caps JPEG payload at 65535 (SEC-008/CWE-190 in sendImage) + emits ULEND per burst; service coalesces aux assigns through the Phase-14 single-shot QTimer drain (DOCK-02). Reuse the bounded encode (akp05.cpp:861), do not add an app-side encode.                                                        |
| T-23-02   | Tampering | out-of-range encoder/zone index pokes wrong surface              | mitigate          | Pass the 0-based encoder index / 0-based zone-id THROUGH the backend range-check (setEncoderImage refuses >=4 at akp05.cpp:640; setTouchStripImage refuses >=4 and 0x12 at :726,:678); the service must not pre-compute an unchecked index that bypasses the guard. Test asserts zero writes for index/zone == 4. |
| T-23-03   | DoS       | device yank mid aux-burst                                        | mitigate          | setTouchStripImage / sendImage catch the transport throw -> return false (never escapes the override); re-resolve on hot-plug arrival, not on a stale ptr (Pitfall 2). [VERIFIED akp05.cpp:887 + test_akp05_touch_strip.cpp:325]                                                                                  |
| T-23-04   | DoS       | dynamic_cast nullptr deref when active device is not aux-capable | mitigate          | Null-check within 3 lines of every dynamic_cast (IDisplayCapable / IEncoderCapable / ITouchStripDisplayCapable); no-op cleanly for non-aux devices (Pitfall 1).                                                                                                                                                   |
| T-23-05   | Tampering | untrusted image source -> image decode for aux surface           | accept (Phase 23) | Load via Qt's safe image decoders only; reuse the bounded decode carried from Phase 14 T-14b-03 / the image_pipeline. Deep path validation is Phase 16/19. [ASSUMED — confirm with Phase-16 posture]                                                                                                              |
| T-23-SC   | Tampering | npm/pip/cargo installs                                           | accept            | No package installs in this plan — pure in-tree C++/Qt; Package Legitimacy Gate N/A (RESEARCH.md audit: not applicable).                                                                                                                                                                                          |

V5 (input validation): encoder index range-checked < 4, touch location < zoneCount(4) + 0x12 refused, JPEG payload capped 65535, all by the backend — the service passes THROUGH these guards and never bypasses them.
\</threat_model>

<verification>
- `ctest --preset linux-release -R "stream-dock-control|StreamDockControlService"` green (DISPLAY-10 MAI/ENC/DRA wire assertions + range refusal + held handle).
- `grep -rn "setMainImage\|setEncoderImage\|setTouchStripImage" src/app/` now returns the service call sites (was 0 hits — DISPLAY-10 wiring gap closed).
- `grep -n setEncoderImage src/app/src/stream_dock_control_service.cpp` shows the ENC path reachable + a PROVISIONAL comment (ENC NOT deleted — LOCKED).
- `git diff --stat` excludes akp05.cpp / akp05_protocol.hpp (wire layer untouched).
- `grep -c nlohmann src/core/include` unchanged (COD-031 boundary intact; service is app-layer).
- Full `ctest --preset linux-release` suite green.
</verification>

\<success_criteria>

- DISPLAY-10 (MAI): assignMainImage emits a MAI whole-strip header + ULEND through the held handle.
- DISPLAY-10 (ENC): assignEncoderImage emits an ENC header with the 0-based index at byte 12 + ULEND.
- DISPLAY-10 (DRA): assignTouchStripZone emits a DRA rect header with x=zone\*200 + ULEND.
- DISPLAY-10 (provisional): ENC and DRA both wired, neither deleted, ENC-vs-zone flagged PROVISIONAL for Phase 25.
- The wire layer (akp05.cpp / akp05_protocol.hpp) and COD-031 boundary are untouched; MockTransport test is the hardware-free gating proof; LIVE confirmation is Phase 25.
  \</success_criteria>

<output>
Create `.planning/phases/23-auxiliary-display-surfaces/23-01-SUMMARY.md` when done.
Record: the pending-write/drain representation chosen for aux surfaces (surface-tag discriminant
vs separate map), the exact ENC-vs-zone PROVISIONAL framing committed, the encoder-zone -> rect
geometry shipped (x=zone*200, w=200, h=100, y=0) flagged for Phase-25 reconciliation, and any
follow-up for Phase 16 (editor assignment UX) or Phase 25 (live witness, geometry confirm).
</output>
