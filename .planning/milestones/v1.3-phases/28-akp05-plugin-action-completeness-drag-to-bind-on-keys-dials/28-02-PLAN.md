---
phase: 28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/app/src/plugin_catalog_model.cpp
  - src/app/src/plugin_catalog_model.hpp
  - src/app/qml/ActionLibraryPane.qml
  - src/app/src/debug_control_facade.cpp
  - tests/unit/test_catalog_offline.cpp
autonomous: true
requirements: [PLUGIN-18, PLUGIN-20]
must_haves:
  truths:
    - installedActions() hides VisibleInActionsList=false actions and exposes them as a separate hidden count, not as errors
    - installedActions() surfaces a countable diagnostic distinguishing hidden vs malformed-skipped actions
    - The action library and its drag MIME payload carry controllers + affordanceMask for every action
    - scripts/ajazz-debug plugin.installedActions returns the action count + full action list
  artifacts:
    - path: src/app/src/plugin_catalog_model.cpp
      provides: VisibleInActionsList filter, hidden/error diagnostic counters, extended QVariantMap
      contains: visibleInActionsList
    - path: src/app/qml/ActionLibraryPane.qml
      provides: controllers + affordanceMask in model rows and MIME payload
      contains: affordanceMask
    - path: src/app/src/debug_control_facade.cpp
      provides: plugin.installedActions debug RPC
      contains: plugin.installedActions
  key_links:
    - from: src/app/qml/ActionLibraryPane.qml
      to: installedActions() QVariantMap
      via: actionModel.append controllers/affordanceMask
      pattern: affordanceMask
    - from: src/app/src/debug_control_facade.cpp
      to: PluginCatalogModel::installedActions
      via: plugin.installedActions RPC
      pattern: installedActions
---

<objective>
Make installed plugins surface exactly their visible actions, expose a countable skip/hide diagnostic, ship the `plugin.installedActions` debug RPC the live PLUGIN-18 check needs, and push `controllers`/`affordanceMask` through the model row + drag MIME payload so the next wave's drop targets can gate.

Purpose: Today hidden vendor actions (emoji_send, touchbar internals — 11 in the corpus) leak into the library, malformed actions vanish silently, and the drag payload carries no affordance info. PLUGIN-18 (completeness + diagnostic) + PLUGIN-20 (affordance into the payload) land here.
Output: Filtered + diagnosed `installedActions()`, extended MIME payload, new debug RPC, catalog tests.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-RESEARCH.md
@.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-PATTERNS.md
@.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-01-SUMMARY.md

<interfaces>
<!-- From 28-01 (this plan depends on it): affordanceMask(QStringList) + new PluginAction fields are now available -->
[[nodiscard]] int affordanceMask(QStringList const& controllers) noexcept;  // plugin_manifest.hpp
// PluginAction now has: visibleInActionsList, disableAutomaticStates, defaultSettings, encoderBlock

<!-- installedActions() current body — plugin_catalog_model.cpp:348-432 -->

<!-- skip gates: empty UUID/Name continue at :382; whole-plugin parse-skip at :375 -->

<!-- QVariantMap build at :416-427 (controllers already inserted at :426) -->

<!-- ActionLibraryPane _rebuild() plugin-row append at :72-80; builtin at :54-58; hint at :66-70 -->

<!-- Drag.mimeData at ActionLibraryPane.qml:192-201; LibraryTile required props at :163-174 -->

<!-- debug RPC analog: plugin.list at debug_control_facade.cpp:446-455; plugin.installFromFile at :479-494 -->

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VisibleInActionsList filter + hidden/error diagnostic counters + extended QVariantMap, with catalog tests</name>
  <files>src/app/src/plugin_catalog_model.cpp, src/app/src/plugin_catalog_model.hpp, tests/unit/test_catalog_offline.cpp</files>
  <read_first>
    - src/app/src/plugin_catalog_model.cpp:348-432 (installedActions() full body)
    - src/app/src/plugin_catalog_model.hpp (class decl — where a diagnostic signal/accessor goes)
    - tests/unit/test_catalog_offline.cpp:225-310 (fixture-plant + installedActions() assertion pattern)
    - 28-PATTERNS.md §3 (filter placement + QVariantMap extension)
    - 28-RESEARCH.md §Diagnostic Design + §Common Pitfalls 7 (hidden vs error counters)
  </read_first>
  <behavior>
    - A 3-action manifest with one "VisibleInActionsList": false -> installedActions() returns 2 rows; the hidden UUID is absent from the output.
    - An action with empty UUID or Name -> excluded AND counted as an error-skip, distinct from the hidden count.
    - Each output row gains affordanceMask, visibleInActionsList, stateCount, disableAutomaticStates, defaultSettings, encoderLayout.
    - A diagnostic surface (signal skippedActionsChanged(int skippedCount,int totalScanned) OR an accessor returning hiddenCount/errorSkipCount/installedCount) reports the counts after a scan; hidden actions are NOT counted as errors.
  </behavior>
  <action>
    In installedActions(), before the existing `if (action.uuid.isEmpty() || action.name.isEmpty())` check at :382, add `if (!action.visibleInActionsList) { ++hiddenCount; continue; }`. Change the existing empty-UUID/Name continue to `++errorSkipCount;` before `continue;`. Keep the whole-plugin parse-skip at :375 but increment a plugin-granularity `++parseFailureCount;`. Declare the three counters as locals at the top of the method.
    Extend the QVariantMap build at :416-427 with `m.insert(QStringLiteral("affordanceMask"), affordanceMask(action.controllers));`, `m.insert(QStringLiteral("visibleInActionsList"), action.visibleInActionsList);`, `m.insert(QStringLiteral("stateCount"), static_cast<int>(action.states.size()));`, `m.insert(QStringLiteral("disableAutomaticStates"), action.disableAutomaticStates);`, `m.insert(QStringLiteral("defaultSettings"), QString::fromStdString(action.defaultSettings));`, `m.insert(QStringLiteral("encoderLayout"), action.encoderBlock.layout);`.
    Add the diagnostic surface to plugin_catalog_model.hpp: a Q_SIGNAL `void skippedActionsChanged(int errorSkipCount, int hiddenCount, int parseFailureCount, int totalScanned);` emitted at the end of installedActions(), AND a `[[nodiscard]] Q_INVOKABLE QVariantMap lastScanDiagnostics() const;` accessor returning {installedCount, skippedUuidName, hiddenByVisibility, skippedParseFailure} so both QML and the debug RPC can read it without re-scanning. Store the last counts in members.
    Add the new Catch2 TEST_CASEs (ASCII names, 28-PATTERNS.md §8): "CatalogOffline VisibleInActionsList false filters action" (size==2), "CatalogOffline diagnostic hidden count vs error count" (assert lastScanDiagnostics hiddenByVisibility==1 and skippedUuidName==1 on a mixed fixture), "CatalogOffline controllers in output map", "CatalogOffline affordanceMask in output map".
  </action>
  <verify>
    <automated>ctest --preset linux-release -R "catalog" -E qml --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/app/src/plugin_catalog_model.cpp | grep -c 'visibleInActionsList'` >= 1 (filter + insert).
    - `grep -q 'affordanceMask(action.controllers)' src/app/src/plugin_catalog_model.cpp`.
    - `grep -q 'skippedActionsChanged' src/app/src/plugin_catalog_model.hpp` and `grep -q 'lastScanDiagnostics' src/app/src/plugin_catalog_model.hpp`.
    - New catalog TEST_CASE asserting `actions.size() == 2` for a 3-action manifest with 1 hidden passes.
    - Diagnostic test asserts hiddenByVisibility and skippedUuidName are reported as DISTINCT counts (Pitfall 7).
    - `ctest ... -R catalog` green, 0 failed.
  </acceptance_criteria>
  <done>Hidden actions filtered + counted separately from errors; output map carries affordanceMask + new fields; diagnostic accessor present; catalog tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Push controllers + affordanceMask through ActionLibraryPane model + MIME payload; add plugin.installedActions debug RPC</name>
  <files>src/app/qml/ActionLibraryPane.qml, src/app/src/debug_control_facade.cpp</files>
  <read_first>
    - src/app/qml/ActionLibraryPane.qml:50-81 (_rebuild) + :160-201 (LibraryTile + Drag.mimeData)
    - src/app/src/debug_control_facade.cpp:446-455 (plugin.list) + :479-494 (plugin.installFromFile)
    - 28-PATTERNS.md §5 (model+MIME extension) + §7 (debug RPC shape)
    - CLAUDE.md "Debug-channel verification — MANDATORY" + objectName rule
  </read_first>
  <behavior>
    - Every plugin row appended in _rebuild() carries controllers (from a.controllers) and affordanceMask (from a.affordanceMask); builtins carry controllers:[] + affordanceMask:1; hints carry affordanceMask:0.
    - The drag MIME "application/x-ajazz-action" payload includes "affordanceMask": tile.affordanceMask.
    - scripts/ajazz-debug plugin.installedActions returns {count:N, actions:[...]} plus the diagnostic counts.
  </behavior>
  <action>
    In ActionLibraryPane.qml _rebuild() plugin-row append (:72-80), add `controllers: a.controllers || []` and `affordanceMask: a.affordanceMask || 0`, mirroring the existing `|| []` fallback. Add `controllers: []`/`affordanceMask: 1` to the builtin append (:54-58) and `controllers: []`/`affordanceMask: 0` to the hint append (:66-70). Add the matching ListElement role fields. Add `required property int affordanceMask` to LibraryTile's required-property block (:163-174) following the existing `required property int kind` style. In Drag.mimeData (:192-201) add `affordanceMask: tile.affordanceMask` to the stringified JSON. Do NOT change any objectName already present; if LibraryTile lacks an objectName, add `objectName: "libraryTile_" + index` per the CLAUDE.md debug-addressability rule.
    In debug_control_facade.cpp, immediately after the plugin.list registration (:455), inside `#ifdef AJAZZ_HAVE_WEBSOCKETS`, register "plugin.installedActions": guard `app.pluginCatalog() == nullptr` -> err "plugin catalog unavailable"; call `cat->installedActions()`, convert each QVariant map to QJsonObject via `QJsonObject::fromVariantMap(v.toMap())` into a QJsonArray, and return `{{"count", size}, {"actions", arr}, {"diagnostics", QJsonObject::fromVariantMap(cat->lastScanDiagnostics())}}`. Follow the plugin.list lambda shape exactly.
  </action>
  <verify>
    <automated>cmake --build build/linux-release --target ajazz-control-center 2>&1 | tail -5; grep -q 'plugin.installedActions' src/app/src/debug_control_facade.cpp && grep -q 'affordanceMask: tile.affordanceMask' src/app/qml/ActionLibraryPane.qml && echo WIRED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'affordanceMask: a.affordanceMask' src/app/qml/ActionLibraryPane.qml` (model append).
    - `grep -q 'affordanceMask: tile.affordanceMask' src/app/qml/ActionLibraryPane.qml` (MIME payload).
    - `grep -q 'required property int affordanceMask' src/app/qml/ActionLibraryPane.qml`.
    - `grep -q '"plugin.installedActions"' src/app/src/debug_control_facade.cpp`.
    - App target builds clean.
    - LibraryTile has an objectName (debug-addressable).
  </acceptance_criteria>
  <done>Library model + MIME payload carry affordanceMask; plugin.installedActions RPC registered; app builds.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                     |
| ----------------------------------- | ----------------------------------------------------------------------------------------------- |
| .sdPlugin manifest -> catalog model | Untrusted manifest fields surface into the library model + MIME payload                         |
| debug RPC -> running app            | plugin.installedActions only active under AJAZZ_DEBUG_CONTROL=1 (opt-in, loopback debug socket) |

## STRIDE Threat Register

| Threat ID        | Category                                   | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                            |
| ---------------- | ------------------------------------------ | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-28-04          | Information Disclosure                     | plugin.installedActions RPC                   | accept      | RPC exposes only manifest-declared action metadata already readable from disk; gated behind AJAZZ_DEBUG_CONTROL=1 on the local-only debug socket. No secrets in the payload.                                                               |
| T-28-05          | Elevation of Privilege (visibility bypass) | VisibleInActionsList filter                   | mitigate    | Filter at :381 BEFORE the UUID/Name check guarantees hidden actions never enter the output list and thus are never routable (V4 access control — 28-RESEARCH Security Domain). A hidden action cannot be dragged because it never appears. |
| T-28-06          | Spoofing/Tampering                         | hostile affordanceMask value injected via row | mitigate    | affordanceMask is computed server-side by the C++ helper from controllers, NOT read from an untrusted external value; QML reads it from the trusted model.                                                                                 |
| T-28-SC          | Tampering                                  | npm/pip/cargo installs                        | mitigate    | No package installs (in-tree C++/QML only). slopcheck N/A.                                                                                                                                                                                 |
| \</threat_model> |                                            |                                               |             |                                                                                                                                                                                                                                            |

<verification>
- `ctest --preset linux-release -R catalog -E qml` green.
- App target builds; `grep -rn nlohmann src/core/include/` returns 0 (COD-031 — catalog model is app-layer, QJson only).
- `plugin.installedActions` RPC present and reachable (live-confirmed in Plan 05).
</verification>

\<success_criteria>

- installedActions() returns ONLY VisibleInActionsList!=false actions; hidden and malformed counts are separately reported.
- Every action row + drag payload carries affordanceMask + controllers.
- plugin.installedActions debug RPC returns count + actions + diagnostics.
  \</success_criteria>

<output>
Create `.planning/phases/28-akp05-plugin-action-completeness-drag-to-bind-on-keys-dials/28-02-SUMMARY.md` when done
</output>
