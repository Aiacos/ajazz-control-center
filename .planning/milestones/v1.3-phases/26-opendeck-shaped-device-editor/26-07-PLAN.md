---
phase: 26-opendeck-shaped-device-editor
plan: 07
type: execute
wave: 5
depends_on: [26-04, 26-06]
files_modified:
  - .planning/phases/25-hardware-verification-real-plugin/25-UAT.md
autonomous: false
requirements:
  - REQ-26-E
must_haves:
  truths:
    - On the live AKP05E demo unit (0x0300:0x3004, fw V3.AKP05E.01.007), the operator re-walks 25-UAT.md Tests 1 + 6 and records PASS results, citing the Phase 26 completion commit SHA in the session note.
    - 25-UAT.md Summary `passed:` count is at least 5 (the existing 3 PASS rows + the new Tests 1 + 6 PASS rows).
    - If hardware UAT reveals a regression, this plan opens a follow-up issue — it does NOT silently mark FAIL items as PASS.
  artifacts:
    - path: .planning/phases/25-hardware-verification-real-plugin/25-UAT.md
      provides: Updated UAT runbook with Test 1 + Test 6 result fields flipped from FAIL/NO_AFFORDANCE to PASS (or an honest BLOCKED-or-still-failing note when hardware contradicts)
      contains: 'result: PASS'
  key_links:
    - from: .planning/phases/25-hardware-verification-real-plugin/25-UAT.md Summary table
      to: Phase 26 completion commit SHA
      via: session note referencing the commit
      pattern: Phase 26
---

<objective>
Operator-gated final wave: the user re-walks Phase 25 25-UAT.md Tests 1
(image-upload to AKP05E LCD key) and 6 (touch-strip zone drop target)
on the live AKP05E demo unit (`0x0300:0x3004`, fw `V3.AKP05E.01.007`)
after all Phase 26 plans 26-01..26-06 have landed. The expected outcome:
both Tests convert from FAIL/NO_AFFORDANCE to PASS, the 25-UAT.md
Summary `passed:` count rises to ≥ 5, and the session note references
the Phase 26 completion commit SHA.

Purpose: REQ-26-E is the acceptance gate for Phase 26 as a whole. The
SPEC pins the criterion at `grep "result: PASS" .planning/phases/25-hardware-verification-real-plugin/25-UAT.md | wc -l`
≥ 5 (the existing 3 PASS rows from Phase 25 partial — brightness, clear,
hasClock-honesty — plus the two new PASS rows for Test 1 and Test 6).

This plan is HARDWARE-GATED and OPERATOR-DRIVEN:

- `autonomous: false` — Claude does not silently mark tests PASS. The
  operator runs the runbook on the physical device and records the
  result.
- The plan contains ONE `checkpoint:human-verify` task that frames the
  steps + expected outcomes + the resume signal.
- If the operator observes a regression (e.g. AKP05E selection still
  does not paint the key after binding edit), the plan does NOT mark
  PASS. The operator returns a description and the planner/orchestrator
  decides whether the regression routes to a hot-fix plan or a
  follow-up phase.

Why this is a separate plan: Phase 26 cannot mark itself complete from
autonomous code alone — REQ-26-E requires a physical-device
verification that has no automated path. Plans 26-01..26-06 ship the
code; this plan ships the operator action + the artifact update.

Bundled into this plan are the documentation updates that follow REQ-26-E
PASS:

- 25-UAT.md Test 1 `result:` field flipped from FAIL to PASS.
- 25-UAT.md Test 6 `result:` field flipped from NO_AFFORDANCE to PASS.
- 25-UAT.md Summary `passed:` updated to at least 5.
- A session note referencing the Phase 26 completion commit SHA (the
  most recent commit on `feat/streamdock` at the time of operator
  walkthrough).
  </objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/phases/26-opendeck-shaped-device-editor/26-SPEC.md
@.planning/phases/26-opendeck-shaped-device-editor/26-CONTEXT.md
@.planning/phases/26-opendeck-shaped-device-editor/26-RESEARCH.md
@.planning/phases/26-opendeck-shaped-device-editor/26-PATTERNS.md
@.planning/phases/25-hardware-verification-real-plugin/25-UAT.md
@.planning/phases/25-hardware-verification-real-plugin/25-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The existing 25-UAT.md schema. -->

25-UAT.md is a runbook with per-test sections containing:

- A test number + title
- A `result:` field (PASS / FAIL / NO_AFFORDANCE / BLOCKED / NOT_WALKED)
- A session note field
  A Summary table near the top with a `passed:` count.

Test 1 (current state, from 25-02-SUMMARY.md): FAIL — image-upload Inspector preview works (L1+L2 fixed in commit `24651a3`) but device key stays blank (L3 setActiveDevice gap → routed to Phase 26).
Test 6 (current state): NO_AFFORDANCE — no touch-strip drop target in KeyDesigner.qml.

Tests still BLOCKED after Phase 26 (per CLAUDE.md AKP05E glossary §7.1 — demo unit input-streaming gap):

- Tests 2, 3, 4, 5, 15, 16 (encoder rotate/press, touch-zone tap, swipe, key press)

Tests still NOT_WALKED:

- Tests 10-12 (encoder rotation-derived behaviours; gated on Tests 2-5)
- Tests 13-14 (real .sdPlugin round-trip; gated on Phase 17-22 + a real plugin sample)
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator re-walks 25-UAT.md Tests 1 + 6 on live AKP05E demo unit</name>
  <files>.planning/phases/25-hardware-verification-real-plugin/25-UAT.md</files>
  <action>
Hand the Phase 26 completion state to the operator for live-hardware verification on the
AKP05E demo unit. The execution environment MUST NOT mark any test PASS in 25-UAT.md
autonomously. After both Tests 1 and 6 PASS, the operator edits 25-UAT.md, updates the
Summary `passed:` count, and lands an atomic `docs(25): mark UAT Test 1 + Test 6 PASS
after Phase 26 landed (REQ-26-E)` commit. If either test FAILs, the operator returns a
failure description via the resume signal and Phase 26 stays PARTIAL until a hot-fix plan
lands. The runbook + acceptance steps are in the `<what-built>` / `<how-to-verify>`
blocks below. The execution agent is NOT allowed to build, launch the app, or drive
hardware — that is exclusively the operator's action.
  </action>
  <what-built>
    Phase 26 plans 26-01..26-06 have landed:
    - Plan 26-01: `Main.qml:onDeviceSelected` calls `StreamDockControlService.setActiveDevice(codename)` — closes GAP-25A.
    - Plan 26-02: `DeviceDescriptor` carries `keyRows` + `touchZoneCount` + main-screen pixel dims; AKP05E descriptor populated; regression test passes.
    - Plan 26-03: `Profile::touchZones` map + schema v2 with auto-migrate; `ProfileController::commitTouchZoneBinding` Q_INVOKABLE exists.
    - Plan 26-04: `DeviceView.qml` replaces `KeyDesigner.qml`; three-stacked-rows render (5×2 keys + 4 encoder dials + 4 touch zones for AKP05E); drag-drop wired via `ActionLibraryPane` tiles; trash zone in top-right; `KeyDesigner.qml` deleted atomically.
    - Plan 26-05: Offscreen QML tests cover geometry rendering for 3 SKU classes + drag-drop to each commit method.
    - Plan 26-06: AKP05E layout JSON + photo + attribution README; AKP05/N4 + AKP153 family + AKP03 family + Mirabox N3 variants layout JSONs all ship.

```
Suite status before this walkthrough: `ctest --preset linux-release -E qml` ≥ 647 passed, 0 failed.
```

</what-built>
  <how-to-verify>
    Prerequisites:
    1. The AKP05E (`0x0300:0x3004`, fw `V3.AKP05E.01.007`) is physically connected and `uaccess` is honored. Verify with `ls -la /dev/hidraw*` — at least one node should be owned by the user's group with rw permissions. If root-only after a re-enumeration storm, physically replug or run `sudo setfacl -m u:$(id -u):rw /dev/hidraw*` (per CLAUDE.md systemd ≥258 note).
    2. The build is current on `feat/streamdock`: `git rev-parse HEAD` matches the latest Phase 26 commit. Record this SHA — it goes into the session note.
    3. Launch the app: `./build/linux-release/src/app/ajazz-control-center`.

```
Test 1 — image upload to LCD key:
1. In the sidebar, click the AKP05E row. Observe journalctl with `journalctl --user -f -n 0` in another terminal; within 1s the line `[akp05] device opened: AJAZZ AKP05E (Stream Dock Plus)` MUST appear (this confirms Plan 26-01's setActiveDevice wire is firing).
2. The DeviceView shows the AKP05E photo background with 10 LCD-key cells (2 rows × 5) on top, 4 round encoder dials in a row below, and 4 touch-strip-zone cells along the bottom (this confirms Plan 26-04 + 26-06 are live).
3. Click key 1 (top-left LCD cell). The Inspector pane on the right shows the empty binding state.
4. In the Inspector, use the FileDialog icon picker to select a small JPEG/PNG (e.g. an 85×85 test image; one is typically present under `resources/dev/` if available, or pick anything from `~/Pictures`).
5. WAIT 2-3 seconds. The physical LCD key 1 on the AKP05E MUST display the selected image (the BAT → chunks → ULEND wire writes from Phase 14, now reachable because setActiveDevice was called).
6. If the image renders on the device: Test 1 = PASS. If still blank: Test 1 = FAIL (do NOT mark PASS — return with the failure description).

Test 6 — touch-strip zone drop target:
1. Sidebar still on AKP05E. DeviceView already rendered from Test 1.
2. In the left ActionLibraryPane, locate the "Open URL" tile (or any tile).
3. Click and HOLD on the tile, drag it onto touch-strip zone 0 (bottom-left zone of the strip). The drop-target SHOULD scale up to 1.05× during drag-over (D-10 animation).
4. Release. The touch-strip zone 0 should now show a "link" Material icon overlay (the picked action's iconName). The Inspector should reflect the new binding when the zone is selected.
5. ALSO: drag from the same tile onto an encoder dial (e.g. dial 0). The encoder dial should similarly accept the drop and bind the action.
6. If touch-strip zones AND encoder dials accept drops with the expected visual feedback: Test 6 = PASS. If no drop-target affordance OR if the drop is silently ignored: Test 6 = FAIL.

Negative-test for cross-controller drag (D-09 strictness):
1. Drag from an occupied KeyCell onto an encoder dial. The drop SHOULD be REJECTED (no scale animation; the drop is silently ignored). The KeyCell remains bound; the encoder dial remains as it was.
2. If cross-controller drops are rejected as designed: this confirms the D-09 grammar is enforced. (This is not a numbered UAT row; it's a sanity check.)

NOTE: Tests 2-5 (encoder rotate/press, touch tap, swipe) and Tests 15-16 (key press) REMAIN BLOCKED per CLAUDE.md AKP05E glossary §7.1 — the demo unit's input-streaming gap. Phase 26 cannot change documented hardware behaviour. Do NOT attempt to walk them; the existing BLOCKED rows in 25-UAT.md stay BLOCKED.

After both Tests 1 and 6 record PASS:
1. Edit `.planning/phases/25-hardware-verification-real-plugin/25-UAT.md`:
   - Change Test 1's `result:` field from `FAIL` to `PASS`. Append a session note like "PASS 2026-MM-DD (Phase 26 commit <SHA>): image renders on AKP05E LCD key 1; setActiveDevice wire confirmed via journalctl [akp05] device opened line."
   - Change Test 6's `result:` field from `NO_AFFORDANCE` to `PASS`. Append a session note: "PASS 2026-MM-DD (Phase 26 commit <SHA>): touch-strip zone 0 accepted drag-drop of Open URL tile; zone displays link icon; Inspector reflects binding."
   - Update the Summary table `passed:` count to at least 5 (was 3; add 2).
2. Save + commit as `docs(25): mark UAT Test 1 + Test 6 PASS after Phase 26 landed (REQ-26-E)`. Atomic conventional commit.

If EITHER test fails:
- Do NOT edit 25-UAT.md to mark PASS. The honesty contract from v1.1
  D-02 forbids lying success.
- In the resume signal, describe the failure: which step, what was
  expected, what was observed, any journalctl snippet, any QML
  qWarning lines.
- The planner/orchestrator then decides whether to land a hot-fix
  plan (e.g. fix a `loadLayout()` JSON parse bug, a `DropArea`
  MIME-key mismatch) or to mark Phase 26 PARTIAL and route the
  remaining gap to a follow-up.
```

</how-to-verify>
  <verify>
    <human-check>Both Test 1 (image upload to AKP05E LCD key 1) and Test 6 (drag-drop onto touch-strip zone 0) result in PASS during the operator session. 25-UAT.md is edited and the `docs(25):` commit is landed on `feat/streamdock`.</human-check>
    <automated>grep -c "result: PASS" .planning/phases/25-hardware-verification-real-plugin/25-UAT.md | awk '{ if ($1 &lt; 5) exit 1 }'</automated>
  </verify>
  <done>The operator session is complete; both Tests 1 and 6 record PASS in 25-UAT.md; the Summary `passed:` count is ≥ 5; a `docs(25):` commit citing the Phase 26 completion SHA is on `feat/streamdock`. If a test failed, this task is NOT done — the orchestrator routes the gap to a hot-fix plan instead.</done>
  <resume-signal>Type `approved` if both Test 1 and Test 6 PASS and 25-UAT.md was updated + committed. Type `failure: <description>` if either test failed; include which step + observed output + relevant log snippets.</resume-signal>
</task>

</tasks>

<verification>
- After operator approval AND 25-UAT.md commit lands:
  - `grep "result: PASS" .planning/phases/25-hardware-verification-real-plugin/25-UAT.md | wc -l` returns ≥ 5.
  - `grep "result: FAIL" .planning/phases/25-hardware-verification-real-plugin/25-UAT.md` does NOT match Test 1 (it may match other unrelated tests if any).
  - `grep "result: NO_AFFORDANCE" .planning/phases/25-hardware-verification-real-plugin/25-UAT.md` does NOT match Test 6.
  - The 25-UAT.md Summary `passed:` line shows at least 5.
  - The git log on `feat/streamdock` shows a `docs(25):` commit for the UAT update referencing Phase 26.
</verification>

\<success_criteria>

- REQ-26-E is met: Tests 1 + 6 PASS on the live AKP05E demo unit AND 25-UAT.md is updated to reflect the new state AND `passed:` count ≥ 5.
- The honesty contract is preserved: if any test failed, the operator returned a failure description and Phase 26 stays PARTIAL until a hot-fix plan lands.
- The completion commit SHA is cited in the session notes for traceability.
  \</success_criteria>

\<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                             |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Operator → 25-UAT.md edit | The operator is trusted to record honest results. The "do NOT mark PASS on failure" rule is documented and enforced via the resume-signal failure path. |
| AKP05E hardware → host    | Existing trust posture from Phase 14 + Phase 25; no new boundaries.                                                                                     |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                    |
| ---------------- | ---------------------- | ------------------------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-26-23          | Repudiation            | Test marked PASS without actual hardware walk                                                    | mitigate    | The session note MUST reference the Phase 26 commit SHA (the operator copies it from `git rev-parse HEAD`); the journalctl `[akp05] device opened` line is the corroborating evidence pinned in the runbook.                                                                       |
| T-26-24          | Tampering              | The demo unit's input gap re-surfaces as a Test 1/6 failure even though Phase 26 code is correct | accept      | CLAUDE.md AKP05E glossary §7.1 explicitly documents the input-streaming gap. Test 1 (image-upload) and Test 6 (drop-target affordance) do not depend on input streaming — both are exercising OUTPUT paths. If they fail, the cause is a Phase 26 code bug, not the demo-unit gap. |
| T-26-25          | Information Disclosure | journalctl logs contain device serial / firmware                                                 | accept      | Already shipped by Phase 14; codename + firmware version are non-PII.                                                                                                                                                                                                              |
| \</threat_model> |                        |                                                                                                  |             |                                                                                                                                                                                                                                                                                    |

<output>
Create `.planning/phases/26-opendeck-shaped-device-editor/26-07-SUMMARY.md` when done. The summary should include:
- The operator session date/time.
- The Phase 26 completion commit SHA cited in the 25-UAT.md updates.
- Test 1 + Test 6 PASS evidence (the journalctl + visual checks).
- The 25-UAT.md `passed:` count before vs after.
- Any residual BLOCKED / NOT_WALKED rows still present and why (demo-unit input gap, plugin Phase 17-22, etc.).
</output>
