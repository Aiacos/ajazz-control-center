---
phase: 26-opendeck-shaped-device-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/qml/Main.qml
autonomous: true
requirements:
  - REQ-26-A
must_haves:
  truths:
    - Selecting an LCD-key device in the sidebar produces a real device-open (per D-01..D-13 wiring; closes GAP-25A).
    - The next binding edit after selection produces wire writes (BAT header + chunks + ULEND) on hidraw — `m_activeDevice` is non-null.
  artifacts:
    - path: src/app/qml/Main.qml
      provides: onDeviceSelected handler that invokes StreamDockControlService.setActiveDevice(codename) before editor.codename assignment
      contains: StreamDockControlService.setActiveDevice(codename)
  key_links:
    - from: src/app/qml/Main.qml onDeviceSelected
      to: StreamDockControlService::setActiveDevice
      via: QML singleton call
      pattern: StreamDockControlService\.setActiveDevice\(codename\)
---

<objective>
Close GAP-25A by wiring `StreamDockControlService.setActiveDevice(codename)` into
`Main.qml:onDeviceSelected` BEFORE the existing `editor.codename` /
`editor.capabilities` assignments. This is the smallest-blast-radius change in
Phase 26: a single-line addition in one QML handler. It implements REQ-26-A
(per D-01..D-13 source set; CONTEXT.md `<decisions> → Claude's Discretion`
Wave 1) and unblocks Phase 25 25-UAT.md Test 1 retest on the live AKP05E
even before the rest of Phase 26 lands.

Purpose: `StreamDockControlService::setActiveDevice` is declared in C++ at
`src/app/src/stream_dock_control_service.{hpp:153,cpp:116}` but never called
from any QML file. As a result `m_activeDevice` stays null and
`repaintPage()` early-returns at `if (!m_activeDevice) return;`. The
QML→C++→device image-upload path is broken end-to-end even after the L1+L2
URL handling fixes landed in commit `24651a3` (Phase 25).

Output: One atomic conventional commit adds one line to `Main.qml`. No new
files, no tests modified (REQ-26-E live walk in Plan 26-07 is the
verification anchor).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-opendeck-shaped-device-editor/26-CONTEXT.md
@.planning/phases/26-opendeck-shaped-device-editor/26-SPEC.md
@.planning/phases/26-opendeck-shaped-device-editor/26-PATTERNS.md
@CLAUDE.md
@src/app/qml/Main.qml
@src/app/src/stream_dock_control_service.hpp
@src/app/src/stream_dock_control_service.cpp

<interfaces>
<!-- The C++ singleton signature the new QML line invokes. -->
<!-- Already registered as a QML singleton; accessible by name from QML without an import. -->

From src/app/src/stream_dock_control_service.hpp (line 153):

- Q_INVOKABLE void setActiveDevice(QString codename);
  Behavior: opens the matching device via DeviceRegistry, sends LIG ON, caches
  the firmware version via CRT VER, sets m_activeDevice to the opened
  shared_ptr. Idempotent on same-codename re-invocation. Pass an empty
  QString to close the active device (sets m_activeDevice = nullptr).

Current QML handler (src/app/qml/Main.qml lines 127-130):

- onDeviceSelected: codename => {
- ```
  editor.codename = codename;
  ```
- ```
  editor.capabilities = DeviceModel.capabilitiesFor(codename);
  ```
- }

Target QML handler (one-line addition BEFORE the assignments):

- onDeviceSelected: codename => {
- ```
  StreamDockControlService.setActiveDevice(codename); // closes GAP-25A
  ```
- ```
  editor.codename = codename;
  ```
- ```
  editor.capabilities = DeviceModel.capabilitiesFor(codename);
  ```
- }
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Wire setActiveDevice on sidebar selection (REQ-26-A)</name>
  <files>src/app/qml/Main.qml</files>
  <read_first>
    - src/app/qml/Main.qml (lines 120-145) — current handler state; verify `DeviceList { id: sidebar; onDeviceSelected: codename => { ... } }` still has the two-line body as documented in CONTEXT.md `<canonical_refs>`.
    - src/app/src/stream_dock_control_service.hpp (lines 145-160) — verify `setActiveDevice(QString)` is `Q_INVOKABLE` and registered. Confirm the singleton name `StreamDockControlService` is used by other QML files (grep `StreamDockControlService\\.` in src/app/qml).
    - .planning/phases/26-opendeck-shaped-device-editor/26-CONTEXT.md `<canonical_refs>` section — confirms the call site and the exact signature.
    - .planning/phases/26-opendeck-shaped-device-editor/26-SPEC.md REQ-26-A — acceptance criteria.
    - CLAUDE.md "Methodology" section — DO NOT re-investigate the AKP05E demo unit input gap or the URL handling fix; both are already pinned.
  </read_first>
  <action>
    Add exactly ONE new line at the top of the `onDeviceSelected` arrow-function body in
    `src/app/qml/Main.qml`, BEFORE the existing `editor.codename = codename;`
    assignment (current line 128). The added line is
    `StreamDockControlService.setActiveDevice(codename);` followed by an inline
    comment referencing GAP-25A and REQ-26-A. Do NOT change the existing
    assignments. Do NOT add an import — `StreamDockControlService` is a
    pre-registered QML singleton (verified by grep of other QML callers).
    Per D-01..D-13 Claude's Discretion (CONTEXT.md): this is the Wave 1 atomic
    one-liner. No other files in this plan.
  </action>
  <acceptance_criteria>
    - `grep -n "StreamDockControlService.setActiveDevice" src/app/qml/Main.qml` returns exactly one line; the line number is between 127 and 132 (inside the `onDeviceSelected` handler block).
    - `grep -nc "StreamDockControlService.setActiveDevice(codename)" src/app/qml/Main.qml | grep -v '^#'` returns `1`.
    - `cmake --build --preset linux-release --target ajazz-control-center` exits 0.
    - `ctest --preset linux-release -E qml --output-on-failure` exits 0 with ≥ 645 passed, 0 failed (baseline preserved).
    - No new QML qWarning lines on app launch (manual: launch app, observe stderr for `QML`-tagged warnings; none new compared to baseline).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "StreamDockControlService.setActiveDevice" src/app/qml/Main.qml | grep -v '^#' &amp;&amp; cmake --build --preset linux-release --target ajazz-control-center &amp;&amp; ctest --preset linux-release -E qml --output-on-failure</automated>
  </verify>
  <done>The handler at `Main.qml:127-130` now invokes `setActiveDevice` first, then assigns `editor.codename` and `editor.capabilities`. App builds and runs. Live operator verification of journalctl `[akp05] device opened` on AKP05E selection is deferred to Plan 26-07 (REQ-26-E re-walk).</done>
</task>

</tasks>

<verification>
- `grep -n "StreamDockControlService.setActiveDevice" src/app/qml/Main.qml` finds the new line in the `onDeviceSelected` handler.
- `cmake --build --preset linux-release` exits 0.
- `ctest --preset linux-release -E qml` ≥ 645 passed, 0 failed.
- Atomic conventional commit lands as `feat(app): wire setActiveDevice on sidebar selection (REQ-26-A, closes GAP-25A)`.
</verification>

\<success_criteria>

- REQ-26-A acceptance from 26-SPEC.md is met: after sidebar selection, the next binding edit drives wire writes on hidraw. (Live verification is bundled into Plan 26-07's REQ-26-E re-walk.)
- No regressions in the existing 645-test suite.
- The one-line change lands as ONE atomic commit on `feat/streamdock`.
  \</success_criteria>

\<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                         |
| ----------------- | ------------------------------------------------------------------------------------------------------------------- |
| QML→C++ singleton | Trusted internal call; codename is an opaque identifier from a project-owned `DeviceModel`. Not user-supplied text. |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                      |
| ---------------- | ---------------------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-26-01          | Tampering              | `codename` string passed to setActiveDevice | accept      | Codename originates from `DeviceModel` (project-owned enumeration), not from any external boundary. No injection surface.                                                                                            |
| T-26-02          | Denial of Service      | Rapid sidebar selection storm               | accept      | `setActiveDevice` is documented idempotent on same-codename; on different-codename it closes-then-opens. Phase 14 already enforces single held-open handle (ARCH-03 weak_ptr flyweight). No new throttling required. |
| T-26-03          | Information Disclosure | journalctl logs `[akp05] device opened`     | accept      | Logged at INFO; codename is non-PII. Already shipped by Phase 14.                                                                                                                                                    |
| \</threat_model> |                        |                                             |             |                                                                                                                                                                                                                      |

<output>
Create `.planning/phases/26-opendeck-shaped-device-editor/26-01-SUMMARY.md` when done.
</output>
