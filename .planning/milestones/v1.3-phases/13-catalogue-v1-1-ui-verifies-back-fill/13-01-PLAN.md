---
phase: 13-catalogue-v1-1-ui-verifies-back-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_data/devices.yaml
  - docs/protocols/keyboard/microdia_dongle.md
  - README.md
  - docs/wiki/Supported-Devices.md
autonomous: true
requirements:
  - DEVICES-08
  - DEVICES-09
deferred_followups:
  - ARCH-06 is DEFAULT VERDICT (PENDING CAPTURE CONFIRMATION). The separate-dongle assumption encoded in this catalogue row + stub doc is gated on the Phase 9.x 2-minute physical unplug test (unplug ak980pro 2.4G receiver; confirm 0c45:7016 does NOT disappear simultaneously). The row notes + stub doc MUST cite this conditional. If the unplug test contradicts, ARCH-06 flips to FIRING and a Phase 12.5 composite-HID dedup lands BEFORE Phase 12 — this row would then be re-folded into the ak980pro row per ARCH-06 §CONDITIONAL re-sequencing.
  - 'PRE-EXISTING ROW CONFLICT (surfaced at planning, 2026-05-20): docs/_data/devices.yaml ALREADY contains a 0c45:7016 row (codename ak980pro_dongle_24g, added 2026-05-15) whose notes claim the OPPOSITE topology to ARCH-06 — "the same physical keyboard enumerates twice" + "Full-Speed 12Mbps vs the AK980 PRO High-Speed 480Mbps". This is internally inconsistent (it cites ARCH-06 NOT firing yet describes a same-physical-device composite). Task 1 reconciles this row to the ARCH-06 default verdict rather than adding a duplicate. See planning risk note in the planner report.'

must_haves:
  truths:
    - A single 0c45:7016 catalogue row exists at probed tier with capabilities [] and an ASCII codename, carrying topology evidence consistent with ARCH-06 (separate dongle, different USB bus branch) and citing the DEFAULT VERDICT conditional.
    - docs/protocols/keyboard/microdia_dongle.md exists and documents the HID descriptor placeholder, the four ARCH-06 topology facts, the identification methodology, and a pointer to ARCH-06.
    - No second/duplicate 0c45:7016 row remains; no composite-HID dedup logic is referenced anywhere (ARCH-06 NOT firing is encoded by the row's standalone existence).
    - README.md + docs/wiki/Supported-Devices.md AUTOGEN blocks regenerate cleanly from the edited devices.yaml with the dongle row reflected.
  artifacts:
    - path: docs/_data/devices.yaml
      provides: probed-tier 0c45:7016 dongle row reconciled to ARCH-06 default verdict
      contains: '7016'
    - path: docs/protocols/keyboard/microdia_dongle.md
      provides: NEW stub — HID descriptor + topology + identification methodology + ARCH-06 pointer
      contains: ARCH-06
  key_links:
    - from: docs/_data/devices.yaml
      to: docs/protocols/keyboard/microdia_dongle.md
      via: protocol_doc field on the 0c45:7016 row points at the new stub
      pattern: microdia_dongle\.md
    - from: docs/protocols/keyboard/microdia_dongle.md
      to: .planning/phases/09-research-captures-hygiene/ARCH-06.md
      via: explicit ADR cross-reference
      pattern: ARCH-06
---

<objective>
Close DEVICES-08 + DEVICES-09: enter `0c45:7016` in the device catalogue at `probed`
tier with the ARCH-06 topology evidence, and create the NEW
`docs/protocols/keyboard/microdia_dongle.md` stub documenting the dongle's HID
descriptor + USB topology + identification methodology so a future SKU enumerating
the same way is recognised without re-doing the topology forensics.

Purpose: ARCH-06's negative ratification (composite-HID dedup NOT firing) is encoded
*in the catalogue itself* — by carrying `0c45:7016` as a standalone `probed`-tier
device with no dedup logic anywhere, the topology-driven decision is made
load-bearing in the data, not just in an ADR. The stub doc preserves the
identification forensics for the next maintainer.

Output: a reconciled `devices.yaml` dongle row + a new `microdia_dongle.md` stub +
the regenerated README / wiki AUTOGEN blocks.

⚠️ CONDITIONAL-EXECUTION GATES (read before executing — this is a plan-only artifact):

1. **ARCH-06 is DEFAULT VERDICT (PENDING CAPTURE CONFIRMATION).** Per
   `.planning/phases/09-research-captures-hygiene/ARCH-06.md`, the separate-dongle
   verdict is the *expected* outcome but is gated on the Phase 9.x 2-minute physical
   unplug test. The catalogue row `notes:` and the stub doc MUST cite this conditional
   status verbatim (do NOT promote ARCH-06 to "Locked" in this plan — that promotion
   lands in the Phase 9.x follow-up commit). This is the honesty contract (D-05).

1. **PRE-EXISTING ROW CONFLICT (surfaced 2026-05-20 at planning time).**
   `docs/_data/devices.yaml` ALREADY contains a `0c45:7016` row
   (`codename: ak980pro_dongle_24g`, added 2026-05-15). Its `notes:` are internally
   inconsistent: they cite "ARCH-06 default verdict ... composite-HID dedup NOT firing"
   yet ALSO assert "the same physical keyboard enumerates twice" and
   "Full-Speed 12Mbps vs the AK980 PRO's High-Speed 480Mbps" — a same-physical-device
   composite framing that is the OPPOSITE of ARCH-06's separate-device verdict, and
   whose 480Mbps figure contradicts ARCH-06's evidence table (ak980pro link speed is
   port-dependent, NOT asserted as High-Speed). This task RECONCILES the existing row
   to the ARCH-06 default verdict — it does NOT add a second duplicate row. The
   codename question (keep `ak980pro_dongle_24g` vs rename to `microdia_dongle_7016`
   per ARCH-06 §Binding) is resolved in Task 1 step 2.

NO build and NO hardware are required for this plan (ARCH-06 already holds the
topology evidence from live lsusb 2026-05-15). This plan is autonomous-executable
now. It WILL likely trigger the "Regenerate README + wiki AUTOGEN" pre-commit hook
(devices.yaml is doc-only / drives `scripts/generate-docs.py`).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-catalogue-v1-1-ui-verifies-back-fill/13-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-research-captures-hygiene/ARCH-06.md
@docs/_data/devices.yaml
@docs/protocols/keyboard/proprietary.md
@CLAUDE.md

<interfaces>
<!-- devices.yaml is doc-only and drives README.md + docs/wiki/Supported-Devices.md
     AUTOGEN blocks via scripts/generate-docs.py. NEVER hand-edit the AUTOGEN blocks
     in README/wiki — run `make docs` (or let the pre-commit hook do it). The runtime
     sidebar comes from src/devices/*/src/register.cpp, NOT this yaml — catalogue-only
     work does NOT make the device appear in the app. -->

devices.yaml schema (per-device row, observed fields):
codename: \<ascii_snake_case> # MUST be ASCII-only (Pitfall 32)
family: \<streamdeck|keyboard|mouse|dongle> # `dongle` is an established value
name: "<human readable>"
vid: "0xVVVV" # note 0x-prefixed, lower-case hex
pid: "0xPPPP"
maturity: \<scaffolded|probed|partial|functional|verified>
capabilities: \[<keys from the capabilities map>\] # [] for a pure transport
protocol_doc: <repo-relative path>
notes: "<free text>" # or `notes: |` block scalar for multi-line
feature_summary: # optional; works/partial/pending lists

PRE-EXISTING 0c45:7016 row to RECONCILE (codename ak980pro_dongle_24g, ~line 483):

- already at `maturity: probed`, `capabilities: []`, `family: dongle`
- protocol_doc currently points at docs/protocols/keyboard/proprietary.md (WRONG
  for DEVICES-09 — must point at the new microdia_dongle.md stub)
- notes contain the topology contradiction described in objective gate 2

ARCH-06 §"Binding to downstream plans" gives the canonical row shape (codename
microdia_dongle_7016, the four-fact topology table, the SONiX/"USB DEVICE" string
signature, the "paired downstream input device unknown" line, the ratification date).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the 0c45:7016 catalogue row to the ARCH-06 default verdict (DEVICES-08)</name>
  <files>docs/_data/devices.yaml</files>
  <action>
Per DEVICES-08 — there must be exactly ONE 0c45:7016 row, at `probed` tier,
`capabilities: []`, `family: dongle`, with topology notes consistent with ARCH-06's
separate-dongle default verdict.

1. Locate the existing `0c45:7016` row (`codename: ak980pro_dongle_24g`, ~line 483 in
   the `# ----- Dongles -----` block). Do NOT add a second row — DEVICES-08 is
   satisfied by reconciling this one.

1. Resolve the codename. ARCH-06 §Binding proposes `microdia_dongle_7016`; the existing
   row uses `ak980pro_dongle_24g`. Keep the existing codename `ak980pro_dongle_24g`
   (it is already ASCII-only per Pitfall 32, is referenced in the row's prose, and a
   rename would churn the AUTOGEN sort order for no functional gain) BUT add an alias
   note: the ARCH-06-canonical name `microdia_dongle_7016` is recorded in the `notes:`
   and in the stub doc filename so a future reader keying off the ADR finds it. Keep
   `family: dongle`, `maturity: probed`, `capabilities: []` unchanged (already correct).

1. Rewrite the `notes:` block scalar to remove the topology CONTRADICTION (objective
   gate 2). The reconciled notes MUST assert the ARCH-06 separate-device facts and MUST
   NOT assert the composite "same physical keyboard enumerates twice" framing:

   - Separate wireless 2.4GHz Microdia/SONiX receiver dongle — NOT a composite/secondary
     interface of ak980pro (this is the ARCH-06 verdict; the prior "enumerates twice"
     sentence is DELETED).
   - Different USB bus branch: usb1/1-13/1-13.1/1-13.1.2 vs ak980pro's usb1/1-10.
   - Full-Speed 12 Mbps; two boot-keyboard HID interfaces with 8-byte EPs (IF0 + IF1);
     iManufacturer "SONiX" / iProduct "USB DEVICE" / bcdDevice 1.03.
   - DELETE the "Full-Speed 12Mbps vs the AK980 PRO's High-Speed 480Mbps" clause — it
     asserts a 480Mbps figure ARCH-06 does not establish (ARCH-06 says ak980pro link
     speed "varies by port"). Cite ONLY the 0c45:7016 Full-Speed fact.
   - Paired downstream input device unknown — pending evtest /dev/hidrawN session per the
     DEVICES-09 identification methodology.
   - Cite ARCH-06 status as **DEFAULT VERDICT (PENDING CAPTURE CONFIRMATION)** and name
     the finalization gate (2-minute physical unplug test: unplug ak980pro 2.4G receiver,
     confirm 0c45:7016 does NOT disappear simultaneously). This is the D-05 honesty
     contract — do NOT write it as "Locked"/"ratified-final".

1. Update `protocol_doc:` to `docs/protocols/keyboard/microdia_dongle.md` (the NEW stub
   created in Task 2). DELETE the stale pointer to `proprietary.md`.

1. Reconcile the `feature_summary.pending:` list: keep the "Runtime entry in register.cpp
   ... catalog-only for now" item and the "2-minute physical unplug test ... Phase 9.x
   deferred" item (both are accurate). Remove any wording implying the dongle is the same
   physical device as ak980pro.

1. Run `make docs` (or `python scripts/generate-docs.py`) so README.md + the wiki
   AUTOGEN blocks regenerate. Do NOT hand-edit the AUTOGEN blocks (CLAUDE.md hard rule;
   devices.yaml is the source of truth).

Codename stays ASCII-only (Pitfall 32). No `nlohmann`/no C++ touched — this is doc-only.
</action>
<verify>
<automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c 'pid: "0x7016"' docs/\_data/devices.yaml)" -eq 1 && grep -q 'maturity: probed' docs/\_data/devices.yaml && ! grep -q 'enumerates twice' docs/\_data/devices.yaml && ! grep -q '480' docs/\_data/devices.yaml && grep -q 'DEFAULT VERDICT' docs/\_data/devices.yaml && grep -q 'microdia_dongle.md' docs/\_data/devices.yaml</automated>
</verify>
<done>
Exactly one 0c45:7016 row at `probed`/`capabilities: []`/`family: dongle`; notes assert
the ARCH-06 separate-dongle facts and cite the DEFAULT VERDICT conditional + unplug-test
gate; the "enumerates twice" composite framing and the "480Mbps" clause are gone;
protocol_doc points at microdia_dongle.md; README/wiki AUTOGEN regenerated from yaml.
</done>
</task>

<task type="auto">
  <name>Task 2: Create the microdia_dongle.md stub protocol doc (DEVICES-09)</name>
  <files>docs/protocols/keyboard/microdia_dongle.md</files>
  <action>
Per DEVICES-09 — NEW stub documenting the dongle's HID descriptor + topology +
identification methodology so a future SKU using the same dongle family is recognised
without redoing the topology forensics. Mirror the shape of existing protocol docs in
`docs/protocols/keyboard/` (title H1, a clean-room provenance line, an Identification
table, then content sections; mdformat-clean — the pre-commit mdformat hook runs).

Required sections:

1. **Title + provenance.** `# Microdia / SONiX 2.4GHz receiver dongle (0c45:7016) — topology & identification (stub)`.
   One line: this is a clean-room identification artefact derived from live `lsusb` +
   HID report-descriptor parsing on real hardware 2026-05-15 — no vendor firmware/driver
   disassembled.

1. **Status banner.** State explicitly that the separate-dongle verdict is **ARCH-06
   DEFAULT VERDICT (PENDING CAPTURE CONFIRMATION)** — gated on the Phase 9.x 2-minute
   physical unplug test. (D-05 honesty contract — the conditional must be visible.)

1. **Identification table** (Vendor ID 0x0c45, Product ID 0x7016, iManufacturer "SONiX",
   iProduct "USB DEVICE", bcdDevice 1.03, link speed Full-Speed 12 Mbps, ASCII codename
   ak980pro_dongle_24g / ARCH-06-canonical alias microdia_dongle_7016).

1. **USB topology — the four ARCH-06 facts** (as a table): sysfs parent path
   (usb1/1-13/1-13.1/1-13.1.2 vs ak980pro usb1/1-10), link speed, interface shape
   (two boot-keyboard HID interfaces, 8-byte EPs, IF0 + IF1), iManufacturer/iProduct
   strings. State that none of Pitfall 20's three composite-HID confirmations hold
   (different sysfs parent, different hub port, simultaneous-disappearance NOT yet
   tested — that is the unplug gate).

1. **HID descriptor.** A placeholder section: the raw report descriptor is captured via
   `sudo cat /sys/class/hidraw/hidrawN/device/report_descriptor` (human-readable via
   `hidrd-convert`). Mark the actual bytes as PENDING the Phase 9.x evtest/hidraw
   session (we have the topology, not the full descriptor dump yet). Reference the safe
   HID-descriptor-parsing methodology (no report capture needed — Pitfall 17 safe).

1. **Identification methodology** — the recipe for recognising a future SKU on the same
   dongle: match the `iManufacturer="SONiX"` / `iProduct="USB DEVICE"` + two-boot-keyboard

   - Full-Speed signature; resolve the sysfs parent path to confirm it is NOT a composite
     interface of an already-known device; then run an evtest session on each /dev/hidrawN
     to identify the paired downstream input device. Cross-reference
     `.planning/research/FEATURES.md` §4.4.

1. **Pointer to the ADR.** Link to
   `.planning/phases/09-research-captures-hygiene/ARCH-06.md` for the architectural
   decision (composite-HID dedup NOT firing in DeviceRegistry::enumerate).

Keep it a STUB — do not invent wire-format commands (the dongle is a transport with no
configurable capabilities; capability work belongs on the ak980pro codename). ASCII-only
content (Win32/mdformat safety). Ensure the file passes the mdformat pre-commit hook.
</action>
<verify>
<automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/protocols/keyboard/microdia_dongle.md && grep -q '0c45' docs/protocols/keyboard/microdia_dongle.md && grep -q 'ARCH-06' docs/protocols/keyboard/microdia_dongle.md && grep -q 'DEFAULT VERDICT' docs/protocols/keyboard/microdia_dongle.md && grep -q 'SONiX' docs/protocols/keyboard/microdia_dongle.md && LC_ALL=C grep -nP '[^\\x00-\\x7F]' docs/protocols/keyboard/microdia_dongle.md | grep -qv . </automated>
</verify>
<done>
microdia_dongle.md exists as a stub with: provenance line, ARCH-06 DEFAULT VERDICT status
banner, identification table, the four topology facts, an HID-descriptor placeholder
(bytes PENDING Phase 9.x), the identification methodology, and an ARCH-06 pointer.
ASCII-only; mdformat-clean.
</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                                                       |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ADR (ARCH-06) -> catalogue data                | The default-verdict ADR's conditional status must survive into the catalogue notes + stub doc without being silently promoted to "final" (honesty boundary, D-05) |
| devices.yaml (source) -> README/wiki (AUTOGEN) | Hand-editing the generated blocks would diverge the public docs from the source of truth                                                                          |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                                                                                                                   | Disposition | Mitigation Plan                                                                                                                                                                                        |
| ---------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-13a-01         | Repudiation            | ARCH-06 conditional status elided when copied into the catalogue row / stub doc                                                             | mitigate    | Both the row `notes:` and the stub status banner MUST carry "DEFAULT VERDICT (PENDING CAPTURE CONFIRMATION)" + the unplug-test gate (D-05). Verified by grep gate for `DEFAULT VERDICT` in both files. |
| T-13a-02         | Tampering              | Stale/contradictory topology (the pre-existing "enumerates twice" + "480Mbps" composite framing) survives, encoding the OPPOSITE of ARCH-06 | mitigate    | Task 1 deletes the composite framing; verify gate asserts `! grep enumerates twice` and `! grep 480`.                                                                                                  |
| T-13a-03         | Tampering              | Hand-edited README/wiki AUTOGEN blocks diverge from devices.yaml                                                                            | mitigate    | Regenerate via `make docs`/`generate-docs.py`; never hand-edit AUTOGEN (CLAUDE.md hard rule).                                                                                                          |
| T-13a-04         | Information disclosure | Non-ASCII codename mangles Win32 CMD codepage / Catch2 filters (Pitfall 32)                                                                 | mitigate    | Codename ak980pro_dongle_24g is ASCII; stub doc content ASCII-only; verify gate greps for non-ASCII bytes.                                                                                             |
| T-13a-SC         | Tampering              | npm/pip/cargo installs                                                                                                                      | accept      | No package-manager installs. `make docs` runs the existing in-tree `scripts/generate-docs.py`; no new dependency introduced. No [ASSUMED]/[SUS] packages.                                              |
| \</threat_model> |                        |                                                                                                                                             |             |                                                                                                                                                                                                        |

<verification>
1. `grep -c 'pid: "0x7016"' docs/_data/devices.yaml` == 1 (no duplicate row).
2. The 0c45:7016 row is `maturity: probed`, `capabilities: []`, `family: dongle`.
3. `grep 'enumerates twice' docs/_data/devices.yaml` and `grep '480' docs/_data/devices.yaml` both return nothing (composite framing removed).
4. `grep 'DEFAULT VERDICT' docs/_data/devices.yaml` matches (D-05 conditional cited).
5. `docs/protocols/keyboard/microdia_dongle.md` exists with the four topology facts, the ARCH-06 status banner, and the ARCH-06 pointer.
6. README.md + docs/wiki/Supported-Devices.md AUTOGEN blocks regenerate cleanly from the edited yaml (no hand-edit).
7. Both edited/new files are ASCII-only and mdformat-clean (pre-commit passes without `--no-verify`).
</verification>

\<success_criteria>

- DEVICES-08: exactly one 0c45:7016 row at `probed` / `capabilities: []` / `family: dongle`, ASCII codename, notes carrying the ARCH-06 separate-dongle topology evidence and the DEFAULT VERDICT conditional; the pre-existing composite contradiction reconciled away.
- DEVICES-09: `docs/protocols/keyboard/microdia_dongle.md` stub documents HID descriptor (placeholder) + topology + identification methodology + ARCH-06 pointer.
- ARCH-06 DEFAULT-VERDICT conditional surfaced in both the catalogue row and the stub doc (D-05 honesty contract); no composite-HID dedup logic referenced.
- README/wiki AUTOGEN regenerated from devices.yaml; one or more atomic Conventional Commits (`docs(devices): ...`).
  \</success_criteria>

<output>
After completion, create
`.planning/phases/13-catalogue-v1-1-ui-verifies-back-fill/13-01-SUMMARY.md` documenting:
the reconciliation of the pre-existing ak980pro_dongle_24g row (what contradictory wording
was removed and why), the codename decision (kept ak980pro_dongle_24g with the
microdia_dongle_7016 alias recorded), the new stub doc, whether `make docs` regenerated the
AUTOGEN blocks cleanly, and an explicit note that ARCH-06 remains DEFAULT VERDICT
(PENDING CAPTURE CONFIRMATION) — the 2-minute Phase 9.x unplug test still gates promotion
of the separate-dongle assumption to "Locked".
</output>
