---
phase: 17-plugin-protocol-completion
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/app/src/sd_plugin_server.cpp
  - src/app/src/sd_plugin_server.hpp
  - tests/unit/test_sd_plugin_server.cpp
autonomous: true
requirements: [PLUGIN-04]
must_haves:
  truths:
    - The host can SEND a JSON-envelope event to a registered plugin's socket addressed by plugin uuid
    - A host->plugin dialRotate event arrives at the loopback client carrying ticks/pressed/controller in payload
    - sendEvent returns false (no crash) when the target uuid has no live socket
    - Every §4.4 host->plugin event name can be sent through the same sendEvent seam
  artifacts:
    - path: src/app/src/sd_plugin_server.hpp
      provides: public sendEvent declaration — the seam Phase 19 calls
      contains: sendEvent
    - path: src/app/src/sd_plugin_server.cpp
      provides: sendEvent body + uuid->socket lookup that re-resolves the live slot each call
      contains: sendTextMessage
    - path: tests/unit/test_sd_plugin_server.cpp
      provides: dialRotate round-trip case + missing-target false case
      contains: dialRotate
  key_links:
    - from: SdPluginServer::sendEvent
      to: QWebSocket::sendTextMessage
      via: QJsonDocument(env).toJson(Compact) after live-slot lookup
      pattern: sendTextMessage
    - from: test_sd_plugin_server.cpp
      to: QWebSocket::textMessageReceived
      via: loopback client QSignalSpy on the received frame
      pattern: textMessageReceived
---

<objective>
Add the host->plugin event-sending seam to `SdPluginServer`: a `sendEvent(target, eventName, payload)`
method that serializes a JSON envelope and writes it to the registered plugin's socket, addressed by
plugin uuid. Cover the full §4.4 event surface incl. the encoder family `dialDown`/`dialUp`/`dialRotate`
(+ legacy `keyDownCord`/`keyUpCord`), `keyDown`/`keyUp`, `touchTap`, `willAppear`/`willDisappear`,
`deviceDidConnect`/`deviceDidDisconnect`, `applicationDidLaunch`/`applicationDidTerminate`,
`titleParametersDidChange`, `systemDidWakeUp`, `didReceiveSettings`/`didReceiveGlobalSettings`,
`sendToPlugin`/`sendToPropertyInspector`.

Purpose: PLUGIN-04. This is the seam Phase 19 calls with real device input (a physical encoder turn
becomes `sendEvent(uuid, "dialRotate", {...})`). It is also a prerequisite for 17-03, whose `passHello`
emission writes through this same seam. Built before auth deliberately.

Output: a public `sendEvent` method + uuid->socket lookup helper, and loopback round-trip tests.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-plugin-protocol-completion/17-CONTEXT.md
@.planning/phases/17-plugin-protocol-completion/17-RESEARCH.md
@.planning/phases/17-plugin-protocol-completion/17-VALIDATION.md
@CLAUDE.md

\<read_first>

- src/app/src/sd_plugin_server.cpp (uuidForClient :247-255 — the existing uuid->socket reverse needs a forward map; stop()/disconnect erase slots at :150-153, :72-82)
- src/app/src/sd_plugin_server.hpp (PluginConnection struct :143-146; public method block to extend :82-101)
- tests/unit/test_sd_plugin_server.cpp (the loopback harness; the actionReceived case :144-169 shows the register-then-send pattern; QWebSocket::textMessageReceived is the client receive signal)
- docs/protocols/streamdeck/akp_plugin_sdk.md §4.2 (envelope) + §4.4 (the host->plugin event table incl. dialRotate payload {ticks, pressed, controller, deviceCoordinates, settings})
- 17-RESEARCH.md Pattern 2 (sendEvent writer + the uuid-addressing recommendation; A1 assumption) + Pitfall 4 (never cache QWebSocket\*, re-resolve the live slot each call — use-after-free after auth rejection)
  \</read_first>

<interfaces>
From src/app/src/sd_plugin_server.hpp:
  struct PluginConnection { QString uuid; QWebSocket* socket{nullptr}; };  // 17-03 extends this
  QString uuidForClient(QWebSocket* client) const;  // existing socket->uuid; we need uuid->socket

§4.4 dialRotate payload shape (the test asserts this round-trips):
{"ticks": <int>, "pressed": <bool>, "controller": "Encoder",
"deviceCoordinates": {"point": ..., "width": ..., "height": ...}, "settings": {...}}

Addressing decision (LOCKED via 17-RESEARCH A1 + Claude's discretion): Phase 17 addresses sendEvent
by plugin `uuid` (the registered plugin). A `context` string MAY be echoed into the envelope but the
socket is resolved by uuid. The per-context action-instance registry arrives in Phase 19/20 (willAppear);
do NOT build it here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sendEvent + uuid->socket lookup to SdPluginServer</name>
  <files>src/app/src/sd_plugin_server.hpp, src/app/src/sd_plugin_server.cpp</files>
  <behavior>
    - sendEvent(targetUuid, eventName, payload) writes a compact JSON envelope {event, payload}
      to the matching registered plugin's socket and returns true.
    - sendEvent returns false (no write, no crash) when no live socket matches targetUuid.
    - The envelope always carries "event"; "payload" is included only when non-empty.
    - The lookup re-resolves the live slot on each call (never caches a raw QWebSocket*).
  </behavior>
  <action>
    Declare a public `bool sendEvent(QString const& targetUuid, QString const& eventName, QJsonObject const& payload = {});`
    in sd_plugin_server.hpp (in the public method block ~:82-101) with a Doxygen note that it is the
    host->plugin seam Phase 19 calls and addresses by registered plugin uuid (context-addressing deferred
    to Phase 19/20). Add a private helper `QWebSocket* socketForUuid(QString const& uuid) const;`. Implement
    socketForUuid in the .cpp by std::find_if over m_connections matching uuid && socket != nullptr (mirror
    uuidForClient :247). Implement sendEvent: look up the live slot via socketForUuid EACH call (Pitfall 4 —
    never cache the pointer; this guards use-after-free after a future auth rejection closes a socket); if
    null, return false; else build a QJsonObject env, insert "event"=eventName, insert "payload"=payload only
    when !payload.isEmpty(), serialize with QJsonDocument(env).toJson(QJsonDocument::Compact), call
    sock->sendTextMessage(QString::fromUtf8(...)), return true. Keep QJsonObject (COD-031 — no nlohmann).
    Add an AJAZZ_LOG_DEBUG line on send. No fenced code in the source beyond the literal implementation.
    Update the sd_plugin_server.hpp file-top Doxygen to note the host->plugin event sender now exists
    (drop it from any "NOT YET IMPLEMENTED" list it still appears in).
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -5; grep -c 'sendEvent' src/app/src/sd_plugin_server.cpp</automated>
  </verify>
  <done>sendEvent + socketForUuid compile; sendEvent re-resolves the slot per call and returns false for an unknown uuid; QJsonObject only (grep nlohmann == 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loopback tests — dialRotate round-trip + missing-target false + event-surface coverage</name>
  <files>tests/unit/test_sd_plugin_server.cpp</files>
  <behavior>
    - After registerPlugin, server.sendEvent(uuid, "dialRotate", {ticks:2, pressed:false, controller:"Encoder"})
      arrives at the loopback client; the received frame's event=="dialRotate" and payload.ticks==2,
      payload.controller=="Encoder", payload.pressed==false.
    - server.sendEvent("com.not.registered", "keyDown", {...}) returns false and the client receives nothing.
    - A representative subset of §4.4 event names (keyDown, keyUp, dialDown, dialUp, keyDownCord,
      touchTap, willAppear, deviceDidConnect, titleParametersDidChange, didReceiveSettings) each
      arrive at the client with the matching "event" name.
  </behavior>
  <action>
    Add a case "SdPluginProtocolTest roundTrip dialRotate carries ticks pressed controller" tagged
    "[plugin-server][events][encoder]": ensureQCoreApp(); start server on port 0; open a loopback
    QWebSocket; QSignalSpy on QWebSocket::textMessageReceived; send registerPlugin and drain (a passHello
    frame may NOT exist yet at this wave — do not assert on it; just register and continue); call
    server.sendEvent("com.test.x", "dialRotate", QJsonObject{{"ticks",2},{"pressed",false},{"controller","Encoder"}});
    waitForSpy(msgSpy); parse the last received frame with QJsonDocument::fromJson(...).object() and assert
    event=="dialRotate", payload.ticks==2, payload.controller=="Encoder", payload.pressed==false. Add a case
    "SdPluginProtocolTest sendEvent returns false for unknown uuid" tagged "[plugin-server][events]" asserting
    server.sendEvent("com.not.registered","keyDown",{}) == false and the client msgSpy stays empty after a
    short pump. Add a data-driven case "SdPluginProtocolTest host to plugin events arrive" tagged
    "[plugin-server][events]" that iterates the representative §4.4 names listed in <behavior>, calls
    sendEvent for each, and asserts each arrives with the correct event name. REUSE ensureQCoreApp()/waitForSpy()
    (never a new QCoreApplication — Pitfall 3). ASCII-only names ("-"/"->" only). Cases live inside the
    AJAZZ_HAVE_WEBSOCKETS-gated target. Because msgSpy may also catch a passHello frame once 17-03 lands,
    assert on the LAST frame's content (msgSpy.last()) rather than a fixed index, and filter for the expected
    event name when iterating.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R "plugin-server" 2>&1 | tail -15</automated>
  </verify>
  <done>dialRotate round-trips with ticks/pressed/controller intact; unknown-uuid sendEvent returns false with no client frame; the representative §4.4 event names each arrive.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                          |
| -------------------------------- | -------------------------------------------------------------------- |
| SdPluginServer -> plugin socket  | The host writes frames out to a (possibly soon-closed) plugin socket |
| network interface -> server bind | LAN must never reach the server (loopback-only, unchanged this plan) |

## STRIDE Threat Register

| Threat ID        | Category                           | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                          |
| ---------------- | ---------------------------------- | --------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-17-UAF         | Tampering / DoS                    | sendEvent writing to a closed/rejected socket | mitigate    | sendEvent re-resolves the live slot via socketForUuid on EVERY call and returns false if absent; never caches a raw QWebSocket\* (Pitfall 4 — guards the use-after-free that 17-03's reject-after-5 socket close would otherwise expose) |
| T-17-DEVTRUST    | Tampering                          | sendToDevice raw-byte forwarding (future)     | accept      | sendEvent is host->plugin only; it does NOT forward raw bytes to a device. The plugin->device trust concern lives in the Phase 19 bridge — note, do not implement here                                                                   |
| T-17-LAN         | Information Disclosure / Elevation | bind address                                  | mitigate    | Unchanged — LocalHost-only (re-pinned in 17-01); this plan adds no listener and no bind setter                                                                                                                                           |
| T-17-SC          | Tampering                          | npm/pip/cargo installs                        | n/a         | Zero external packages (Qt6 in-tree); no slopcheck applicable                                                                                                                                                                            |
| \</threat_model> |                                    |                                               |             |                                                                                                                                                                                                                                          |

<verification>
- `ctest --preset linux-release -R "plugin-server"` — all events cases green.
- `grep -c 'sendEvent' src/app/src/sd_plugin_server.hpp` — public seam declared.
- `grep -rn 'nlohmann' src/app/src/sd_plugin_server.cpp` returns 0 (COD-031).
- Full suite green: `ctest --preset linux-release`.
</verification>

\<success_criteria>

- A registered plugin receives host->plugin events through sendEvent, including dialRotate carrying ticks/pressed/controller (PLUGIN-04).
- sendEvent returns false for an unknown/closed target without crashing (Pitfall 4 closed).
- The full §4.4 event surface is reachable through the single sendEvent seam Phase 19 will call.
  \</success_criteria>

<output>
Create `.planning/phases/17-plugin-protocol-completion/17-02-SUMMARY.md` when done.
</output>
