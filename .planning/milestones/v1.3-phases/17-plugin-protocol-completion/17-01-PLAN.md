---
phase: 17-plugin-protocol-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/src/sd_plugin_server.cpp
  - src/app/src/sd_plugin_server.hpp
  - tests/unit/test_sd_plugin_server.cpp
autonomous: true
requirements: [PLUGIN-01, PLUGIN-02, PLUGIN-03]
must_haves:
  truths:
    - The WS server still binds QHostAddress::LocalHost only, never Any, on a random free port
    - Every one of the 41 supported plugin->host actions (15 standard + 26 AJAZZ per spec 4.3) routes via actionReceived and NEVER reaches unhandledEventReceived
    - A genuinely-unknown event name still surfaces via unhandledEventReceived (forward-compat preserved)
    - The verbatim JSON envelope (event/context/device/action/payload) round-trips through actionReceived for every routed action
  artifacts:
    - path: src/app/src/sd_plugin_server.cpp
      provides: sizeless routed-action membership set (15 standard + 26 AJAZZ = 41) replacing kStandardActions[13]
      contains: kRoutedActions
    - path: tests/unit/test_sd_plugin_server.cpp
      provides: inverted setBG case + all-routed-actions case (count derived from kRoutedActions.size()) + envelope round-trip case
      contains: actionReceived
  key_links:
    - from: dispatchClientMessage
      to: actionReceived
      via: sizeless routed-action membership set
      pattern: kRoutedActions
    - from: test_sd_plugin_server.cpp
      to: SdPluginServer::actionReceived
      via: loopback QWebSocket client + QSignalSpy
      pattern: actionReceived
---

<objective>
Route the full Stream Deck v6 plugin->host action surface on the existing `SdPluginServer`:
the 15 standard Elgato routed actions PLUS the 26 AJAZZ-only actions (41 routed total, derived
strictly from spec 4.3), so NOTHING falls through to `unhandledEventReceived`. Re-pin the
LocalHost-only bind invariant (PLUGIN-01) and the JSON envelope round-trip (PLUGIN-02), and invert
the now-obsolete `setBG -> unhandled` MVP test.

The "15 standard routed" count is the spec-4.3 standard set (17 Elgato actions) MINUS the two
`register*` actions (`registerPlugin`, `registerPropertyInspector`) which are handled in the EARLIER
dispatch branch and return early — they are therefore NOT in the routed set. `setFeedback` is a
STANDARD Elgato action per 4.3 (Stream Deck Plus encoder feedback), so it belongs in the standard
group, NOT the AJAZZ group. The AJAZZ-only group is exactly 26 and does NOT contain setFeedback.

Purpose: PLUGIN-03 closes the "26 AJAZZ extensions are unhandled" gap (sd_plugin_server.cpp:237).
This is the protocol-routing slice; auth (17-03) and the host->plugin sendEvent writer (17-02)
land in their own reviewable commits per CLAUDE.md.

Output: a sizeless routed-action set (15 standard + 26 AJAZZ = 41) in `dispatchClientMessage`,
updated/added Catch2 cases whose expected count is derived from the table's own size.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-plugin-protocol-completion/17-CONTEXT.md
@.planning/phases/17-plugin-protocol-completion/17-RESEARCH.md
@.planning/phases/17-plugin-protocol-completion/17-VALIDATION.md
@CLAUDE.md

\<read_first>

- src/app/src/sd_plugin_server.cpp (the dispatcher being modified — kStandardActions[13] at :212-224, the unhandled fallthrough at :237-244, actionReceived emit at :234)
- src/app/src/sd_plugin_server.hpp (the signals being preserved — actionReceived :121, unhandledEventReceived :125; the file-top "NOT YET IMPLEMENTED" Doxygen comment :34-40)
- tests/unit/test_sd_plugin_server.cpp (the loopback harness: ensureQCoreApp :44, waitForSpy :92, the actionReceived case :144-169, the setBG->unhandled case to INVERT :171-193)
- docs/protocols/streamdeck/akp_plugin_sdk.md §4.2 (envelope keys) + §4.3 (the AUTHORITATIVE action table, lines ~232-268 — count the routed set from THIS table, not from prose)
- tests/unit/CMakeLists.txt:333-342 (AJAZZ_HAVE_WEBSOCKETS gating — new cases go in this block, no new file needed)
  \</read_first>

<interfaces>
From src/app/src/sd_plugin_server.hpp (signals preserved verbatim — DO NOT rename):
  void actionReceived(QString const& pluginUuid, QJsonObject const& action);
  void unhandledEventReceived(QString const& pluginUuid, QString const& eventName);

Current dispatch (sd_plugin_server.cpp:212-244): kStandardActions is a
`static constexpr std::array<char const*, 13>` containing setTitle, setImage, showAlert,
showOk, getSettings, setSettings, getGlobalSettings, setGlobalSettings, switchToProfile,
openUrl, logMessage, registerPlugin, registerPropertyInspector. NOTE: registerPlugin /
registerPropertyInspector are handled in the EARLIER branch (:185-206, early-return) and
currently ALSO appear in the array (dead entries) — drop them from the routed set.

Routed set derived strictly from spec 4.3 (count from the table; do NOT hardcode the total):

STANDARD routed (15) — spec-4.3 "Standard Elgato? yes" MINUS the two register\* actions:
setTitle, setImage, setState, showAlert, showOk, getSettings, setSettings, getGlobalSettings,
setGlobalSettings, switchToProfile, sendToPropertyInspector, sendToPlugin, openUrl, logMessage,
setFeedback.
(setFeedback is STANDARD per 4.3 — Stream Deck Plus encoder feedback. registerPlugin /
registerPropertyInspector are standard but handled in the earlier branch, so NOT routed here.)

AJAZZ-only routed (26) — spec-4.3 "Standard Elgato? AJAZZ-only" (does NOT include setFeedback):
setBG, setBackground, clearIcon, sendToDevice, openTouchbarSecondaryMenu, exitTouchbarSecondaryMenu,
enterGatheringEvent, registrationScreenSaverEvent, unRegistrationScreenSaverEvent, setText,
lockScreen, unLockScreen, getScreenshot, getSystemAudioVolume, getUserInfo, setAcImgTop,
onSwitchToFolderProfile, onSwitchFromFolderProfile, deleteAction, stopBackground, exitFullScreen,
touchTap, getDetectedSensorsData, startAudioCapture, stopAudioCapture, sendUserInfo.

Total routed = 15 + 26 = 41. registerPlugin / registerPropertyInspector are NOT in this set
(handled before it). `authentication` is NOT in this set (handled by 17-03, before it).
The container is SIZELESS so the count cannot drift: any name in the table routes; everything
else falls through to unhandledEventReceived.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace kStandardActions[13] with the sizeless routed-action set (15 standard + 26 AJAZZ = 41)</name>
  <files>src/app/src/sd_plugin_server.cpp, src/app/src/sd_plugin_server.hpp</files>
  <behavior>
    - Each of the 15 standard routed action names (incl. setFeedback) routes via actionReceived (no unhandled).
    - Each of the 26 AJAZZ-only action names routes via actionReceived (no unhandled).
    - registerPlugin / registerPropertyInspector continue to bind UUID + emit pluginRegistered
      in the earlier branch (unchanged); they are NOT routed as actions.
    - A genuinely-unknown event ("someEventThatDoesNotExist") still emits unhandledEventReceived.
    - actionReceived carries the verbatim QJsonObject (envelope keys preserved unchanged).
  </behavior>
  <action>
    Replace the `static constexpr std::array<char const*, 13> kStandardActions` (sd_plugin_server.cpp:212-224)
    with a SIZELESS `static constexpr std::array kRoutedActions = {...};` (rely on CTAD — do NOT write an
    explicit array size, so the count is computed from the literal and can never drift out of sync). Populate
    it with the exact 41 names from the <interfaces> block: the 15 STANDARD routed names (setTitle, setImage,
    setState, showAlert, showOk, getSettings, setSettings, getGlobalSettings, setGlobalSettings, switchToProfile,
    sendToPropertyInspector, sendToPlugin, openUrl, logMessage, setFeedback) then the 26 AJAZZ-only names (setBG,
    setBackground, clearIcon, sendToDevice, openTouchbarSecondaryMenu, exitTouchbarSecondaryMenu,
    enterGatheringEvent, registrationScreenSaverEvent, unRegistrationScreenSaverEvent, setText, lockScreen,
    unLockScreen, getScreenshot, getSystemAudioVolume, getUserInfo, setAcImgTop, onSwitchToFolderProfile,
    onSwitchFromFolderProfile, deleteAction, stopBackground, exitFullScreen, touchTap, getDetectedSensorsData,
    startAudioCapture, stopAudioCapture, sendUserInfo). DROP the dead `registerPlugin` and
    `registerPropertyInspector` entries — those are handled in the :185-206 branch which executes BEFORE this
    loop and returns early. setFeedback goes in the STANDARD group, NOT the AJAZZ group (spec 4.3 marks it
    "Standard Elgato? yes"). Keep the existing linear membership scan (rename the loop's set reference to
    kRoutedActions); keep `emit actionReceived(senderUuid, msg)` for matches and keep the
    `unhandledEventReceived` fallthrough for non-members (forward-compat for genuinely-unknown events).
    Routing-vs-fulfillment per CONTEXT D-decision: Phase 17 only ROUTES — host-capability actions
    (getScreenshot/getSystemAudioVolume/getUserInfo/getDetectedSensorsData) and device-targeting actions
    (setImage/setTitle/setState/setBG/setFeedback/setText) all surface via actionReceived; the Phase-19
    bridge and later phases consume them. Do NOT add per-action fulfillment logic here. Add a one-line comment
    that the routed set is 15 standard + 26 AJAZZ = 41 (spec 4.3), that the array is intentionally sizeless so
    the count is derived not hardcoded, and that `authentication` is handled separately by the auth slice. NO
    fenced code in the source beyond the array literal itself; keep QJsonObject (COD-031 — never nlohmann here).
    Update the file-top Doxygen "NOT YET IMPLEMENTED" comment (sd_plugin_server.hpp:34-40) to drop "26 AJAZZ
    extensions" from the not-implemented list.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -5; grep -c 'kRoutedActions' src/app/src/sd_plugin_server.cpp</automated>
  </verify>
  <done>kRoutedActions is a sizeless std::array (CTAD, no explicit size) containing exactly the 41 names from <interfaces> (setFeedback in the standard group); build succeeds; registerPlugin/registerPropertyInspector are NOT in the array; actionReceived/unhandledEventReceived signals unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invert the setBG test + add all-routed-actions and envelope round-trip cases</name>
  <files>tests/unit/test_sd_plugin_server.cpp</files>
  <behavior>
    - setBG now reaches actionReceived (NOT unhandledEventReceived).
    - An unknown event "someEventThatDoesNotExist" still reaches unhandledEventReceived.
    - Sending each name in the routed table in turn produces zero unhandledEventReceived emissions
      and exactly kRoutedActions.size() actionReceived emissions (expected count derived from the
      table size, NOT a hardcoded literal — the table currently holds 41).
    - For a representative action (e.g. setImage), the actionReceived QJsonObject preserves the
      envelope: event/context/device/action/payload round-trip verbatim.
    - The server binds LocalHost only on a random free port (PLUGIN-01 invariant re-pinned).
  </behavior>
  <action>
    In tests/unit/test_sd_plugin_server.cpp, INVERT the existing case
    "SdPluginServer surfaces unknown events via unhandledEventReceived" (:171-193): keep an
    unhandledEventReceived assertion but switch its trigger to a truly-unknown event name
    ("someEventThatDoesNotExist"), and ADD a new assertion (or new case) that sends a setBG frame
    ({"event":"setBG","context":"c1","payload":{"color":"#FF0000"}}) after registerPlugin and asserts it
    reaches actionReceived (NOT unhandled). Add a data-driven case
    "SdPluginProtocolTest all routed actions route none unhandled" tagged "[plugin-server][actions]" that
    registers a plugin then sends, in turn, each name from the routed table (the same 15 standard + 26 AJAZZ
    names listed in the plan's <interfaces> block — keep this list in the test in lockstep with the source
    array). Drain the event loop with the existing waitForSpy/pump helpers and assert: unhandledEventReceived
    count == 0, and actionReceived count == the number of names the test sent. Derive the expected count from
    the test's own name list size (e.g. the size of the test-local names container), NOT a literal "41"; add a
    comment that this must equal kRoutedActions.size() in the source (currently 41). Add a case
    "SdPluginProtocolTest envelope round trips event context device action payload" tagged
    "[plugin-server][actions][envelope]" that sends a setImage frame with all five envelope keys and asserts
    the actionReceived QJsonObject preserves each verbatim. Add a case re-pinning PLUGIN-01:
    "SdPluginServer bind loopback only on random port" tagged "[plugin-server][security][loopback-only]"
    asserting bindAddress()==LocalHost, != Any/AnyIPv4/AnyIPv6, and serverPort()!=0 after start(0)
    (re-assert the existing invariant; do NOT add a bind-address setter). REUSE ensureQCoreApp() and
    waitForSpy() (never allocate a new QCoreApplication — Pitfall 3 static-destruction crash). ASCII-only
    test names (use "-" / "->" never em-dash/arrows — Win32 ctest codepage). New cases go inside the
    existing AJAZZ_HAVE_WEBSOCKETS-gated test target (no new file).
  </action>
  <verify>
    <automated>ctest --preset linux-release -R "plugin-server" 2>&1 | tail -15</automated>
  </verify>
  <done>All [plugin-server] cases pass; the all-routed case asserts actionReceived == the count it sent (currently 41, derived from the name-list size) and unhandled==0; the inverted case asserts setBG->actionReceived and someEventThatDoesNotExist->unhandledEventReceived; the loopback-only bind case passes.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                 |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| plugin (or loopback test client) -> SdPluginServer | Untrusted JSON text frames cross here; the server is the protocol boundary in the app layer |
| network interface -> server bind                   | The LAN must NEVER reach the server (loopback-only invariant)                               |

## STRIDE Threat Register

| Threat ID        | Category                           | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                            |
| ---------------- | ---------------------------------- | ---------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-17-LAN         | Information Disclosure / Elevation | SdPluginServer::start() bind       | mitigate    | Bind QHostAddress::LocalHost only (sd_plugin_server.cpp:55, already enforced); re-pin with the loopback-only test case in Task 2; the RE's #1 footgun (vendor binds Any) stays closed — no widening setter (sd_plugin_server.hpp has none; do not add one) |
| T-17-JSON        | DoS                                | onClientTextMessage parse          | mitigate    | Existing QJsonDocument::fromJson guard drops malformed/oversized frames (sd_plugin_server.cpp:170-175); keep it — the routed-set membership scan runs only on valid objects with a present "event" string                                                  |
| T-17-FWD         | Tampering                          | unhandledEventReceived fallthrough | accept      | Genuinely-unknown events still surface (not silently dropped) for forward-compat tracing; routing the 41 narrows the unhandled surface without removing the safety net                                                                                     |
| T-17-DEVFWD      | Tampering                          | sendToDevice / raw-byte forwarding | accept      | Phase 17 only ROUTES sendToDevice via actionReceived; the actual device write (a future device-trust concern) is Phase 19 — note it, do not forward bytes here                                                                                             |
| T-17-SC          | Tampering                          | npm/pip/cargo installs             | n/a         | Zero external packages installed this phase (all Qt6 in-tree); no slopcheck applicable                                                                                                                                                                     |
| \</threat_model> |                                    |                                    |             |                                                                                                                                                                                                                                                            |

<verification>
- `ctest --preset linux-release -R "plugin-server"` — all cases green, including the inverted setBG case and the all-routed-actions case.
- `grep -c 'kRoutedActions' src/app/src/sd_plugin_server.cpp` — the new set is referenced.
- `grep -n 'QHostAddress::LocalHost' src/app/src/sd_plugin_server.cpp` — the bind invariant is unchanged.
- `grep -rn 'nlohmann' src/app/src/sd_plugin_server.cpp` returns 0 (COD-031 — QJsonObject only).
</verification>

\<success_criteria>

- All 41 plugin->host actions (15 standard + 26 AJAZZ, per spec 4.3) route via actionReceived; none reach unhandledEventReceived (PLUGIN-03).
- The routed-action container is sizeless (CTAD) so the count is derived from the literal, not hardcoded.
- The LocalHost-only bind invariant is re-pinned by a test (PLUGIN-01).
- The JSON envelope round-trips verbatim through actionReceived (PLUGIN-02).
- The obsolete setBG->unhandled MVP test is inverted; a genuinely-unknown event keeps an unhandled assertion alive.
- Full suite green: `ctest --preset linux-release`.
  \</success_criteria>

<output>
Create `.planning/phases/17-plugin-protocol-completion/17-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
