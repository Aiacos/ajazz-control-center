---
phase: 16-device-controls-binding-persistence-pages
plan: 01
type: execute
wave: 1
depends_on: [14, 15]
files_modified:
  - src/app/src/stream_dock_control_service.hpp
  - src/app/src/stream_dock_control_service.cpp
  - src/app/src/application.hpp
  - src/app/src/application.cpp
  - src/app/qml/ProfileEditor.qml
  - tests/unit/test_stream_dock_controls.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [DISPLAY-09]
must_haves:
  truths:
    - A brightness Slider in the ProfileEditor Keys tab drives a LIG write with the dragged value via the control service (debounced).
    - A "Clear all keys" control issues a CLE write (clearKey(0xFF)) via the control service.
    - Dragging the slider does NOT flood LIG writes - N drag steps coalesce to a small bounded number of LIG writes.
    - The control service is QML-exposed as a QML_SINGLETON via create()/registerInstance() (never the bare macro).
  artifacts:
    - path: src/app/src/stream_dock_control_service.hpp
      provides: Q_INVOKABLE setBrightness(codename, percent) + clearAll(codename); QML_SINGLETON create/registerInstance + static_assert(!is_default_constructible)
      contains: Q_INVOKABLE
    - path: src/app/qml/ProfileEditor.qml
      provides: brightness Slider (debounced) + Clear-all button bound to StreamDockControlService, visible only on LCD-key devices
      contains: StreamDockControlService
    - path: tests/unit/test_stream_dock_controls.cpp
      provides: MockTransport wire assertions for setBrightness->LIG, clearAll->CLE, debounce-bound
      min_lines: 70
  key_links:
    - from: src/app/qml/ProfileEditor.qml
      to: StreamDockControlService
      via: Slider onMoved/onPressedChanged + Timer debounce -> setBrightness; Button onClicked -> clearAll
      pattern: StreamDockControlService\.(setBrightness|clearAll)
    - from: src/app/src/stream_dock_control_service.cpp
      to: core::IDisplayCapable
      via: resolve held/looked-up device, dynamic_cast + null-check within 3 lines, setBrightness/clearKey(0xFF)
      pattern: dynamic_cast<core::IDisplayCapable
    - from: src/app/src/application.cpp
      to: StreamDockControlService
      via: qmlRegisterSingletonInstance of the existing m_streamDockControl instance
      pattern: qmlRegisterSingletonInstance
---

<objective>
Add the DISPLAY-09 live device controls: a brightness Slider and a "Clear all keys" control
in the ProfileEditor Keys tab that drive the physical panel through the Phase-14
`StreamDockControlService` (`setBrightness` -> `LIG`; clear-all -> `clearKey(0xFF)` -> `CLE`).
This means (a) adding two `Q_INVOKABLE`s to the control service, (b) QML-exposing that service
as a `QML_SINGLETON` so QML can call it, and (c) adding the debounced Slider + Clear button to
the Keys tab.

Purpose: Phase 14 built the control service and its `LIG`/`CLE` wire path but exposed NO
QML-callable control surface and added NO brightness/clear UI (RESEARCH gap a). DISPLAY-09 is
the first user-facing control that drives the panel live. Brightness routes to the Stream Dock
`LIG` via the control service, NOT through `LightingService` (that is keyboard firmware-RGB, a
different capability/device class) - LOCKED decision.

Output: extended `stream_dock_control_service.{hpp,cpp}` (two `Q_INVOKABLE`s + QML_SINGLETON
exposure), `application.cpp` registration, the ProfileEditor slider/button + debounce, and a
MockTransport unit test that is the hardware-free gating proof (physical witness is Phase 25).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-device-controls-binding-persistence-pages/16-CONTEXT.md
@.planning/phases/16-device-controls-binding-persistence-pages/16-RESEARCH.md
@.planning/phases/16-device-controls-binding-persistence-pages/16-VALIDATION.md
@CLAUDE.md

\<blocking_prerequisite>
Phase 14 and Phase 15 MUST be executed before this plan runs. At Phase-16 plan time NO
`*-SUMMARY.md` exists for 14/15 and `src/app/src/stream_dock_control_service.{hpp,cpp}` does
NOT exist on disk (RESEARCH Pitfall 7; `grep StreamDockControlService src/app/` == 0 hits).
The executor MUST FIRST read `.planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md`
to learn the finalized seams it builds on:

- whether the control service was already QML-exposed (if so, only add the Q_INVOKABLEs);
- the profile-accessor seam (lambda vs ProfileController pointer);
- the keyIndex base mapping (profile -> device 1-based);
- the default-brightness constant value Phase 14 used at open.
  If `14-02-SUMMARY.md` is absent, STOP - Phase 16 cannot execute until Phase 14 lands.
  \</blocking_prerequisite>

<interfaces>
<!-- Reuse surfaces. Do NOT alter the wire layer; the LIG/CLE bytes are byte-tested in akp05. -->

StreamDockControlService (Phase 14 - read 14-02-PLAN.md interfaces block + 14-02-SUMMARY):
using DeviceLookup = std::function\<std::shared_ptr<core::IDevice>(QString const&)>;
explicit StreamDockControlService(DeviceLookup lookup, QObject\* parent);
void setActiveDevice(QString const& codename); // holds shared_ptr, opens, brightness-ON
void assignKeyImage(std::uint8_t keyIndex, QImage const&);
void repaintFromProfile();
QString firmwareVersionFor(QString const&) const;
// Held shared_ptr<core::IDevice> m_activeDevice; resolves via DeviceLookup.

core::IDisplayCapable (src/core/include/ajazz/core/capabilities.hpp:124 region):
void setBrightness(std::uint8_t percent); // clamps 0..100, emits LIG (byte[5]=='L')
void clearKey(std::uint8_t keyIndex); // 0xFF clears all -> CLE (byte[5..7]=='C','L','E')

QML_SINGLETON pattern to mirror (src/app/src/lighting_service.hpp:46-66):
QML_NAMED_ELEMENT(...); QML_SINGLETON;
static T\* create(QQmlEngine\*, QJSEngine\*); // returns the registered instance
static void registerInstance(T\*) noexcept; // Application sets the live instance
static_assert(!std::is_default_constructible_v<T>); // co-located build-break lock
Registration in Application: qmlRegisterSingletonInstance(...) of the constructed instance -
NEVER the bare QML_SINGLETON macro alone (CLAUDE.md: it silently spawns a second instance per
import; the slider would talk to a dead instance - Pitfall 2).

ProfileEditor.qml (src/app/qml/ProfileEditor.qml):
property string codename; property var capabilities;
readonly property int \_keyCount; readonly property bool \_showKeys (\_keyCount > 0);
TabBar with a "Keys" TabButton (visible: \_showKeys). The Keys-tab content + the sticky
Apply/Revert footer are the slider/clear-all insertion points.

MockTransport + factory (tests/unit/fixtures/mock_transport.hpp; src/devices/streamdeck/src/akp05.cpp:928):
core::DevicePtr makeAkp05WithTransport(DeviceDescriptor, DeviceId, TransportPtr);
MockTransport: open(); enqueueReadFeature(bytes); writes() -> recorded packets.
LIG packet: byte[5]=='L' (0x4C) + brightness byte. CLE packet: bytes[5..7]=='C','L','E'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Q_INVOKABLE setBrightness/clearAll to the control service + QML-expose it</name>
  <files>src/app/src/stream_dock_control_service.hpp, src/app/src/stream_dock_control_service.cpp, src/app/src/application.hpp, src/app/src/application.cpp, tests/unit/test_stream_dock_controls.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - .planning/phases/14-stream-dock-control-service/14-02-SUMMARY.md (MANDATORY first - finalized seams: whether QML-exposed already, profile accessor, default brightness, keyIndex base)
    - src/app/src/stream_dock_control_service.hpp + .cpp (full - the Phase-14 surface being extended; the DeviceLookup, m_activeDevice, the dynamic_cast<IDisplayCapable> + null-check pattern Phase 14 used)
    - src/app/src/lighting_service.hpp (lines 46-66 - QML_SINGLETON create()/registerInstance()/static_assert pattern + Q_INVOKABLE shape to mirror exactly)
    - src/app/src/lighting_service.cpp (create()/registerInstance() body; how the resolved device is dynamic_cast and null-checked)
    - src/app/src/application.cpp (where LightingService is qmlRegisterSingletonInstance'd; the existing m_streamDockControl construction from 14-02; the qmlRegister block)
    - src/app/src/application.hpp (member order; -Wreorder note from 14-02)
    - src/core/include/ajazz/core/capabilities.hpp (IDisplayCapable setBrightness/clearKey signatures)
    - tests/unit/test_stream_dock_control_service.cpp (Phase-14 test - the MockTransport + makeAkp05WithTransport + offscreen-service drive pattern + CMakeLists source-link block to mirror)
    - tests/unit/fixtures/mock_transport.hpp (writes() / enqueueReadFeature semantics)
  </read_first>
  <behavior>
    - Test (DISPLAY-09 brightness): build a MockTransport-backed Akp05Device via makeAkp05WithTransport, inject it via a fake DeviceLookup, call setBrightness(codename, 42); MockTransport.writes() contains a packet with byte[5]=='L' (0x4C) carrying the brightness byte 42. Calling setBrightness with 150 clamps to 100 (backend clamp - no out-of-range LIG byte).
    - Test (DISPLAY-09 clear): call clearAll(codename); MockTransport.writes() contains a packet with bytes[5..7]=='C','L','E' (the clearKey(0xFF) -> CLE path).
    - Test (DISPLAY-09 non-display device no-op): when the resolved device is NOT IDisplayCapable (dynamic_cast yields nullptr), setBrightness/clearAll no-op cleanly (no crash, no write).
  </behavior>
  <action>
    Read 14-02-SUMMARY.md FIRST and honor whatever it recorded. Then add two methods to
    StreamDockControlService:
      Q_INVOKABLE void setBrightness(QString const& codename, int percent);
      Q_INVOKABLE void clearAll(QString const& codename);
    setBrightness: resolve the device (prefer the held m_activeDevice when the codename matches,
    else m_lookup(codename)), dynamic_cast<core::IDisplayCapable*>(dev.get()) with a null-check
    within 3 lines (Pitfall 1 / mirror Phase 14), and call setBrightness(static_cast<std::uint8_t>(
    std::clamp(percent, 0, 100))) - the backend clamps too but clamp at the boundary as well.
    clearAll: same resolve + dynamic_cast + null-check, then clearKey(0xFF) (0xFF = clear all,
    per the interfaces block) -> CLE.
    Do NOT add a new LIG/CLE builder (the backend wire path is byte-tested - reuse it). Do NOT
    route through LightingService (LOCKED: that is keyboard firmware-RGB).
    QML exposure: if 14-02-SUMMARY says the service is ALREADY QML-exposed, only add the two
    Q_INVOKABLEs (the QML_SINGLETON machinery is already there). If it is NOT yet QML-exposed,
    add the QML_SINGLETON exposure now mirroring LightingService EXACTLY:
      QML_NAMED_ELEMENT(StreamDockControlService); QML_SINGLETON;
      static StreamDockControlService* create(QQmlEngine*, QJSEngine*);
      static void registerInstance(StreamDockControlService*) noexcept;
    and co-locate static_assert(!std::is_default_constructible_v<StreamDockControlService>);
    In application.cpp, register the EXISTING m_streamDockControl instance via
    qmlRegisterSingletonInstance (mirror how LightingService is registered) - NEVER the bare
    QML_SINGLETON macro (Pitfall 2). Do NOT construct a second instance for QML; QML must talk to
    the same instance Application already holds (so the slider drives the held handle).
    Then create tests/unit/test_stream_dock_controls.cpp: MockTransport + makeAkp05WithTransport
    + fake DeviceLookup; exercise the three behaviors; assert exact wire bytes via writes()
    (offsets per the interfaces block). ASCII-only TEST_CASE/SECTION titles, tag
    [stream-dock-controls] so `ctest -R StreamDockControls` selects it. Register the new test in
    tests/unit/CMakeLists.txt linking stream_dock_control_service.cpp + the streamdeck device
    source (mirror the Phase-14 test_stream_dock_control_service.cpp link block).
  </action>
  <verify>
    <automated>cmake --preset linux-release >/dev/null 2>&1; cmake --build --preset linux-release >/dev/null 2>&1 && ctest --preset linux-release -R StreamDockControls --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R StreamDockControls` runs and reports 100% passed.
    - setBrightness(codename, 42) produces a write with byte[5]==0x4C ('L') and the brightness byte 42 (DISPLAY-09).
    - setBrightness(codename, 150) clamps to 100 - no LIG byte exceeds 100.
    - clearAll(codename) produces a write with bytes[5..7]=='C','L','E' (DISPLAY-09).
    - A non-IDisplayCapable resolved device no-ops both calls with no write and no crash.
    - `grep -n 'dynamic_cast<core::IDisplayCapable' src/app/src/stream_dock_control_service.cpp` is followed within 3 lines by a nullptr check (Pitfall 1).
    - `grep -n 'qmlRegisterSingletonInstance' src/app/src/application.cpp` shows the control service registered as a singleton instance (NOT a bare QML_SINGLETON construction).
    - `grep -c nlohmann src/core/include` unchanged (COD-031 - service is app-layer).
    - `git diff --stat` shows NO changes to src/devices/streamdeck/src/akp05.cpp or akp05_protocol.hpp (LIG/CLE wire layer untouched).
    - TEST_CASE/SECTION titles ASCII-only.
    - Full `ctest --preset linux-release` suite green.
  </acceptance_criteria>
  <done>The control service exposes Q_INVOKABLE setBrightness/clearAll driving LIG/CLE, is QML-exposed as a single registered QML_SINGLETON instance, and the MockTransport test proves the wire bytes; wire layer + COD-031 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the debounced brightness Slider + Clear-all button to the Keys tab</name>
  <files>src/app/qml/ProfileEditor.qml</files>
  <read_first>
    - src/app/qml/ProfileEditor.qml (full - the Keys-tab content + the sticky Apply/Revert footer; `codename`, `_keyCount`, `_showKeys` properties; the import block and how AjazzControlCenter singletons are referenced)
    - src/app/qml/KeyDesigner.qml (how the Keys tab embeds KeyDesigner; where a header/footer control row can live without disturbing the grid)
    - src/app/src/lighting_service.hpp (the Q_INVOKABLE names + that the singleton is referenced in QML by its QML_NAMED_ELEMENT name)
    - 16-RESEARCH.md Pattern 2 (the Slider + Timer debounce idiom - onMoved restarts a Timer, onPressedChanged writes the final value on release)
  </read_first>
  <action>
    In the Keys-tab content of ProfileEditor.qml (NOT the keyboard/mouse tabs), add a controls row
    containing a brightness Slider (from: 0, to: 100, stepSize: 1) and a "Clear all keys" Button.
    Bind them to the QML-exposed StreamDockControlService (referenced by its QML element name from
    the AjazzControlCenter import - confirm the exact name added in Task 1). Pass root.codename as
    the device argument.
    Debounce the slider (Pitfall 3 - do NOT flood LIG): on Slider.onMoved restart a single-shot
    Timer (interval ~80 ms, repeat: false) whose onTriggered calls
    StreamDockControlService.setBrightness(root.codename, slider.value); and on
    Slider.onPressedChanged, when releasing (!pressed), issue one final
    setBrightness(root.codename, slider.value). This yields at most a small bounded number of LIG
    writes per drag (the timer-coalesced intermediate values plus one on release), never one per
    pixel.
    The Clear-all Button onClicked calls StreamDockControlService.clearAll(root.codename); guard it
    so it is only enabled/visible when root.codename !== "" and the device exposes LCD keys
    (_showKeys / _keyCount > 0). The brightness Slider is likewise gated to LCD-key devices.
    Keep the layout consistent with the existing Keys-tab styling vocabulary (reuse existing
    spacing/component patterns; do not introduce a new theme). Do NOT add any C++ here - this is
    pure QML wiring to the Task-1 Q_INVOKABLEs.
  </action>
  <verify>
    <automated>grep -nE 'StreamDockControlService\.(setBrightness|clearAll)' src/app/qml/ProfileEditor.qml && grep -n 'Timer' src/app/qml/ProfileEditor.qml && cmake --build --preset linux-release >/dev/null 2>&1 && ctest --preset linux-release -R "QmlSmoke|StreamDockControls" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'StreamDockControlService\.(setBrightness|clearAll)' src/app/qml/ProfileEditor.qml` shows both calls wired.
    - A `Timer` (single-shot, ~80 ms) coalesces slider drags before calling setBrightness (debounce - Pitfall 3).
    - The Slider issues one final setBrightness on release (onPressedChanged when !pressed).
    - The Clear-all Button onClicked calls clearAll(root.codename), gated to codename != "" and LCD-key devices.
    - The app target builds clean (`cmake --build --preset linux-release`); the offscreen QML smoke target (tests/qml) loads ProfileEditor without error if it covers it.
    - No C++ added in this task (QML-only diff); full `ctest --preset linux-release` suite green.
  </acceptance_criteria>
  <done>The Keys tab has a debounced brightness Slider and a Clear-all button bound to the QML-exposed control service; dragging coalesces LIG writes; both controls are gated to LCD-key devices.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                                                                          |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| QML slider/button -> control service Q_INVOKABLE | User-driven brightness percent / clear request crosses from QML into the app service. Brightness is clamped 0..100; clear is a fixed clearKey(0xFF). |
| app service -> USB device (held handle)          | The brightness/clear write reuses the Phase-14 held shared_ptr<IDevice>; a device yank mid-write degrades via the backend zombie-contract no-op.     |

## STRIDE Threat Register

| Threat ID | Category                    | Component                                                            | Disposition | Mitigation Plan                                                                                                                                                         |
| --------- | --------------------------- | -------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-16a-01  | DoS                         | slider-drag flooding LIG writes                                      | mitigate    | QML Timer debounce (~80 ms single-shot) + one final write on release; backend clamps 0..100. Bounded write count per drag (Pitfall 3).                                  |
| T-16a-02  | DoS                         | dynamic_cast nullptr deref when active device is not display-capable | mitigate    | Null-check within 3 lines of every dynamic_cast\<IDisplayCapable\*> (Pitfall 1); no-op cleanly for keyboards/mice.                                                      |
| T-16a-03  | Tampering (build invariant) | QML_SINGLETON dual-instance trap                                     | mitigate    | create()/registerInstance()/static_assert(!is_default_constructible) + qmlRegisterSingletonInstance of the live instance; never the bare macro (Pitfall 2 / CLAUDE.md). |
| T-16a-04  | Tampering                   | brightness percent out of range                                      | mitigate    | std::clamp(percent, 0, 100) at the service boundary in addition to the backend clamp (V5 input validation).                                                             |
| T-16a-SC  | Tampering                   | npm/pip/cargo installs                                               | accept      | No package installs - pure in-tree C++/Qt + QML. Package Legitimacy Gate N/A (16-RESEARCH.md audit: not applicable).                                                    |

V5 (input validation): brightness clamped 0..100 at both the service and backend; clear is a fixed-arg clearKey(0xFF); no new wire builder, no backend guard bypassed.
\</threat_model>

<verification>
- `ctest --preset linux-release -R StreamDockControls` green (DISPLAY-09 LIG + CLE wire assertions).
- ProfileEditor.qml calls StreamDockControlService.setBrightness/clearAll, debounced via a Timer.
- `grep -n 'qmlRegisterSingletonInstance' src/app/src/application.cpp` shows the control service exposed as a single registered instance.
- `git diff --stat` excludes akp05.cpp / akp05_protocol.hpp (LIG/CLE wire untouched).
- `grep -c nlohmann src/core/include` unchanged (COD-031 intact).
- App + tests compile -Wreorder-clean; full `ctest --preset linux-release` suite green.
</verification>

\<success_criteria>

- DISPLAY-09: a brightness slider drives the panel live (`LIG`) with the dragged value, debounced; a "clear all keys" control blanks the device (`CLE`).
- The control service is QML-exposed as one registered QML_SINGLETON instance (the slider drives the held handle, not a duplicate).
- Wire layer (LIG/CLE byte path) and the COD-031 boundary are untouched.
  \</success_criteria>

<output>
Create `.planning/phases/16-device-controls-binding-persistence-pages/16-01-SUMMARY.md` when done.
Record: whether the control service was already QML-exposed by Phase 14 (or exposed here), the
QML element name used for the singleton, the debounce interval chosen, the Keys-tab placement of
the controls, and any follow-up for Phase 25 (live brightness/clear hardware witness).
</output>
