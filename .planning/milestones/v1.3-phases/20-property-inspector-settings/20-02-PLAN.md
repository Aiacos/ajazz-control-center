---
phase: 20-property-inspector-settings
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/app/src/pi_cef_shim.hpp
  - src/app/src/pi_cef_shim.cpp
  - src/app/src/pi_bridge.hpp
  - src/app/src/pi_bridge.cpp
  - src/app/src/pi_url_policy.hpp
  - src/app/src/pi_url_policy.cpp
  - src/app/src/pi_url_request_interceptor.cpp
  - resources/streamdeck/sdpi.css
  - src/app/CMakeLists.txt
  - tests/unit/CMakeLists.txt
  - tests/unit/test_pi_bridge.cpp
autonomous: true
requirements: [PLUGIN-09]
must_haves:
  truths:
    - An injected JS script defines window.cefQuery and routes cefQuery to the bridge invoke dispatcher
    - PIBridge exposes a generic invoke(QString json) dispatcher that fans out only to the existing typed $SD slots
    - PI HTML referencing static/css/sdpi.css resolves to the bundled qrc resource; remote navigation stays blocked
  artifacts:
    - path: src/app/src/pi_cef_shim.hpp
      provides: kCefQueryShimSource constant JS + makeCefQueryShim() declaration
      contains: kCefQueryShimSource
    - path: src/app/src/pi_cef_shim.cpp
      provides: makeCefQueryShim() QWebEngineScript builder (DocumentCreation/MainWorld)
      contains: DocumentCreation
    - path: resources/streamdeck/sdpi.css
      provides: Bundled Elgato Property Inspector stylesheet (qrc resource)
      contains: sdpi
  key_links:
    - from: kCefQueryShimSource (JS)
      to: PIBridge::invoke
      via: channel.objects['$SD'].invoke(json)
      pattern: invoke
    - from: PIUrlRequestInterceptor::interceptRequest
      to: qrc sdpi.css
      via: info.redirect on a request whose path ends in sdpi.css
      pattern: redirect
---

<objective>
Close two of the four PLUGIN-09 gaps without rebuilding the PI stack:
(1) the cefQuery polyfill (no cefQuery exists in tree today) and its required bridge dispatcher;
(2) the bundled-and-served sdpi.css (no sdpi* file exists today). Both REUSE the established
QWebEngineScript injection mechanism the 18-03 PLAN specified for the Mirabox shim — mirror it, do
NOT duplicate it. The shim source string is unit-testable WITHOUT WebEngine (per the 18-03
string-test discipline); the injection-point assertion is WebEngine-gated.

Purpose: PI HTML calls window.cefQuery(...) and references static/css/sdpi.css; both must work so
an Elgato/Mirabox PI renders and reaches $SD. Per akp_plugin_sdk.md §8 the canonical mapping is
cefQuery(...) -> channel.objects.bridge.invoke(json).then(...), which requires a generic invoke
dispatcher (A1, the §8-verbatim path) that fans out ONLY to the existing typed slots.
Output: pi_cef_shim.{hpp,cpp}, PIBridge::invoke, the interceptor sdpi.css redirect, the bundled CSS
resource, and the cefquery + sdpi unit tests.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-property-inspector-settings/20-CONTEXT.md
@.planning/phases/20-property-inspector-settings/20-RESEARCH.md
@.planning/phases/20-property-inspector-settings/20-VALIDATION.md
@.planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Mechanism to MIRROR (18-03 Mirabox shim) — same profile, same injection point. -->

From 18-03-PLAN.md (the QWebEngineScript builder pattern to mirror, NOT rebuild):
inline constexpr std::string_view kMiraboxShimSource = R"JS( ... )JS"; // pure JS, unit-testable
#if defined(AJAZZ_HAVE_WEBENGINE)
QWebEngineScript makeMiraboxShim(); // name set; DocumentCreation; MainWorld; runsOnSubFrames(true)
#endif
// inserted via profile->scripts()->insert(...) BEFORE the page loads.

From src/app/src/pi_bridge.hpp (the existing typed $SD surface the dispatcher fans out to):
Q_INVOKABLE void setSettings(QString const& json);
Q_INVOKABLE void getSettings();
Q_INVOKABLE void setGlobalSettings(QString const& json);
Q_INVOKABLE void getGlobalSettings();
Q_INVOKABLE void sendToPlugin(QString const& json);
Q_INVOKABLE void setTitle(QString const& title, QString const& context, int target);
Q_INVOKABLE void setImage(QString const& imageData, QString const& context, int target);
Q_INVOKABLE void openUrl(QString const& url);
Q_INVOKABLE void logMessage(QString const& message);
signals: void didReceiveSettings(QString json); // + didReceiveGlobalSettings, sendToPropertyInspector

From src/app/src/pi_url_policy.hpp:
enum class UrlDecision { Allow, DenySchemeBlocked, DenyHttpRejected, DenyFileOutsidePiDir,
DenyHttpsNotAllowlist, DenyMalformed };
UrlDecision isLoadUrlAllowed(QUrl const& url, QString const& piDir); // qrc:/blob:/data: already Allow

From src/app/CMakeLists.txt conventions:

- WebEngine-gated TU registration block at lines 400-410 (target_sources(... src/pi_url_request_interceptor.cpp)).
- qrc alias: set_source_files_properties(<file> PROPERTIES QT_RESOURCE_ALIAS <alias>) then add the var to
  ACC_QML_MODULE_RESOURCES (lines 334-347); module RESOURCES land at qrc:/qt/qml/AjazzControlCenter/<alias>.
- pi_bridge.cpp + pi_url_policy.cpp are in the always-compiled app SOURCES (lines 63-68);
  pi_url_request_interceptor.cpp is WebEngine-gated.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PIBridge::invoke dispatcher + pi_cef_shim cefQuery polyfill</name>
  <files>src/app/src/pi_cef_shim.hpp, src/app/src/pi_cef_shim.cpp, src/app/src/pi_bridge.hpp, src/app/src/pi_bridge.cpp, src/app/CMakeLists.txt, tests/unit/CMakeLists.txt, tests/unit/test_pi_bridge.cpp</files>
  <read_first>
    - src/app/src/pi_bridge.hpp (full) + pi_bridge.cpp lines 234-416 (typed slots the dispatcher fans out to).
    - .planning/phases/18-plugin-manifest-discovery-lifecycle-spawn/18-03-PLAN.md (makeMiraboxShim builder + kMiraboxShimSource string-test discipline + Pitfall 4 "injected too late").
    - docs/protocols/streamdeck/akp_plugin_sdk.md §8 (cefQuery -> channel.objects.bridge.invoke(json).then(...) map, lines 392-427) and §7 (PI handshake, lines 367-390).
    - src/app/src/property_inspector_controller.cpp lines 196-223 (channel registers PIBridge as "$SD").
    - src/app/CMakeLists.txt lines 63-68 (app SOURCES) + 400-410 (WebEngine-gated target_sources).
    - CLAUDE.md (never QCefView; ASCII test names; COD-031 — Qt JSON only in pi_*).
  </read_first>
  <behavior>
    - Shim source (always-on, pure string): kCefQueryShimSource defines window.cefQuery taking opts with request/onSuccess/onFailure; it reaches the $SD object via the page QWebChannel transport and calls channel.objects["$SD"].invoke(opts.request), wiring .then(opts.onSuccess) / .catch(opts.onFailure) plus a try/catch routing synchronous errors to onFailure. The string contains the literals window.cefQuery, onSuccess, onFailure, $SD.
    - Injection point (WebEngine-gated): makeCefQueryShim() returns a QWebEngineScript named "ajazz-cefquery-shim" with injectionPoint()==QWebEngineScript::DocumentCreation, worldId()==QWebEngineScript::MainWorld, runsOnSubFrames()==true, sourceCode()==QString::fromUtf8(kCefQueryShimSource).
    - Dispatcher: PIBridge::invoke(QString json) parses json as a JSON object carrying an "event" string and dispatches to EXACTLY the existing typed slots using only fields present in json. Unknown event: AJAZZ_LOG_WARN + return "{}". Never reaches any C++ outside the fixed dispatch set.
  </behavior>
  <action>
    Create src/app/src/pi_cef_shim.hpp: declare inline constexpr std::string_view kCefQueryShimSource with
    the polyfill JS per behavior (reachable WITHOUT WebEngine — a pure string); inside
    #if defined(AJAZZ_HAVE_WEBENGINE) (mirroring pi_url_request_interceptor.hpp's gate + the QtWebEngineCore
    include discipline) declare QWebEngineScript makeCefQueryShim();. Create src/app/src/pi_cef_shim.cpp:
    implement makeCefQueryShim() per behavior with a comment citing akp_plugin_sdk.md §8 + 18-03 Pitfall 4
    (DocumentCreation, NOT runJavaScript-after-load). Add Q_INVOKABLE QString invoke(QString const& json) to
    PIBridge (declare in pi_bridge.hpp adjacent to the typed slots; implement in pi_bridge.cpp): parse with
    QJsonDocument/QJsonObject, dispatch to existing slots ONLY (A1, §8-verbatim); unknown events
    AJAZZ_LOG_WARN + return "{}". CMake (app): register src/pi_cef_shim.cpp in the WebEngine-gated
    target_sources block at lines 400-410 alongside pi_url_request_interceptor.cpp; the kCefQueryShimSource
    header string compiles unconditionally. CMake (tests): the unit binary builds WITHOUT
    AJAZZ_HAVE_WEBENGINE so the makeCefQueryShim body compiles out; the dispatcher lives in pi_bridge.cpp
    (linked by 20-01) and the header string is includable. Gate the injection-point TEST_CASE behind
    #if defined(AJAZZ_HAVE_WEBENGINE); the pure-string and dispatcher cases run unconditionally. Add
    [pi-bridge][cefquery] cases to test_pi_bridge.cpp: a string case asserting kCefQueryShimSource contains
    window.cefQuery / onSuccess / onFailure / $SD; a dispatcher case constructing
    PIBridge(nullptr,"com.example.foo","act1","ctx1"), connecting didReceiveSettings, calling invoke with a
    getSettings request body and asserting the signal fired; an unknown-event case asserting invoke returns
    "{}" and emits nothing. Qt JSON only (no nlohmann in pi_*). ASCII titles.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -5; ctest --preset linux-release -R "cefquery" --output-on-failure; grep -c 'window.cefQuery' src/app/src/pi_cef_shim.hpp; grep -rln nlohmann src/app/src/pi_cef_shim.cpp src/app/src/pi_bridge.cpp</automated>
  </verify>
  <acceptance_criteria>
    - The [cefquery] string + dispatcher cases pass under ctest --preset linux-release -R "cefquery".
    - grep -c 'window.cefQuery' src/app/src/pi_cef_shim.hpp >= 1; the source also contains $SD, onSuccess, onFailure.
    - invoke with the getSettings request body fires didReceiveSettings; an unknown event returns "{}" and emits nothing.
    - grep -rln nlohmann across pi_cef_shim.cpp + pi_bridge.cpp returns no files (COD-031 hygiene).
    - When AJAZZ_HAVE_WEBENGINE is defined, makeCefQueryShim sets DocumentCreation + MainWorld + runsOnSubFrames + matching sourceCode (gated case).
  </acceptance_criteria>
  <done>cefQuery polyfill source + the §8 invoke dispatcher exist, route only to the typed $SD slots, and are unit-tested; the WebEngine builder mirrors the 18-03 injection mechanism.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bundle and serve sdpi.css from a built-in qrc URL via the interceptor</name>
  <files>resources/streamdeck/sdpi.css, src/app/src/pi_url_policy.hpp, src/app/src/pi_url_policy.cpp, src/app/src/pi_url_request_interceptor.cpp, src/app/CMakeLists.txt, tests/unit/CMakeLists.txt, tests/unit/test_pi_bridge.cpp</files>
  <read_first>
    - src/app/src/pi_url_policy.hpp (full) + pi_url_policy.cpp (isLoadUrlAllowed decision tree; qrc/blob/data already Allow).
    - src/app/src/pi_url_request_interceptor.hpp (full) + pi_url_request_interceptor.cpp (interceptRequest body; deny logging shape).
    - 20-RESEARCH.md "Pattern 2: Serving a bundled sdpi.css from a canonical URL" (qrc + interceptor redirect; A2 — vendor the CSS, verify against upstream elgatosf/sdpi-css or the AJAZZ defaultPlugins copy).
    - src/app/CMakeLists.txt lines 144-193 (QT_RESOURCE_ALIAS convention) + 334-347 (ACC_QML_MODULE_RESOURCES) + tests/unit/CMakeLists.txt lines 294-299 (qt_add_resources test-qrc pattern).
    - akp_plugin_sdk.md §9 row "resources/sdpi.css" (test name PluginAssetTest::sdpi_css_servedFromBuiltinUrl).
  </read_first>
  <behavior>
    - isSdpiCssRequest(QUrl const& url) returns true iff the path ends in /static/css/sdpi.css or /sdpi.css (case-sensitive), false for .../other.css and .../sdpi.css.evil — testable without WebEngine.
    - resources/streamdeck/sdpi.css exists, is non-empty, and is reachable at qrc:/qt/qml/AjazzControlCenter/streamdock/sdpi.css once built.
    - In a WebEngine build, interceptRequest detects an sdpi.css request and info.redirect()s it to the fixed bundled qrc URL; the redirected qrc fetch passes the existing policy (qrc is Allow) and Chromium serves text/css by extension. Remote nav (http://, non-allowlist https://) stays blocked — unchanged.
  </behavior>
  <action>
    Vendor resources/streamdeck/sdpi.css: a copy of Elgato's standard PI stylesheet (the sdpi-wrapper /
    .sdpi-item class set). Per A2, source it from upstream elgatosf/sdpi-css or an AJAZZ defaultPlugins
    bundled copy and note the provenance in a top-of-file comment; the file must be non-empty and contain
    .sdpi-item and .sdpi-wrapper. Add bool isSdpiCssRequest(QUrl const& url) to pi_url_policy.hpp/.cpp (in
    the pure-policy TU so it is unit-testable without WebEngine), returning true for paths ending
    /static/css/sdpi.css or /sdpi.css. In pi_url_request_interceptor.cpp interceptRequest, BEFORE the
    allow/deny check, call isSdpiCssRequest(info.requestUrl()); if true,
    info.redirect(QUrl(QStringLiteral("qrc:/qt/qml/AjazzControlCenter/streamdock/sdpi.css"))) and return.
    Pin the redirect target as a fixed string constant (NOT attacker-controlled). CMake (app): declare
    set(ACC_SDPI_CSS resources/streamdeck/sdpi.css), set_source_files_properties(${ACC_SDPI_CSS} PROPERTIES
    QT_RESOURCE_ALIAS streamdock/sdpi.css), and append ${ACC_SDPI_CSS} to ACC_QML_MODULE_RESOURCES so it
    lands at the qrc path above. CMake (tests): isSdpiCssRequest is in pi_url_policy.cpp (already linked at
    line 197), so the helper test needs no new link; if you assert qrc-presence, embed the css as a test
    qrc by mirroring the streamdock-fallback.json qt_add_resources pattern (lines 294-299) with PREFIX
    /qt/qml/AjazzControlCenter and alias streamdock/sdpi.css. Add [pi-bridge][sdpi] cases to
    test_pi_bridge.cpp: isSdpiCssRequest true for .../static/css/sdpi.css and .../sdpi.css; false for
    .../other.css and .../sdpi.css.evil; plus (if the test qrc is wired) a bundled-resource-present case
    asserting the qrc QFile exists and size > 0. ASCII titles. Do NOT widen the https allowlist or relax the
    existing policy.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -5; ctest --preset linux-release -R "sdpi" --output-on-failure; test -s resources/streamdeck/sdpi.css && echo CSS_PRESENT; grep -c 'sdpi.css' src/app/src/pi_url_request_interceptor.cpp</automated>
  </verify>
  <acceptance_criteria>
    - isSdpiCssRequest true for paths ending /static/css/sdpi.css and /sdpi.css; false for other.css and sdpi.css.evil.
    - resources/streamdeck/sdpi.css exists, is non-empty (test -s), and contains .sdpi-item / .sdpi-wrapper.
    - grep -c 'sdpi.css' src/app/src/pi_url_request_interceptor.cpp >= 1; the redirect target is a fixed qrc constant, not derived from the request URL.
    - kPiHttpsCdnAllowlist is unchanged; http:// and non-allowlist https:// still deny.
  </acceptance_criteria>
  <done>sdpi.css is bundled as a qrc resource and the interceptor redirects the canonical PI reference to it; remote nav policy is unchanged; helper + presence cases green.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                          |
| ----------------------------------------- | -------------------------------------------------------------------- |
| Injected MainWorld JS -> $SD bridge       | The cefQuery shim runs in the same JS world as the untrusted PI page |
| PI page sub-resource fetch -> interceptor | Untrusted URL crosses into the host redirect/allow decision          |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                                   | Disposition                      | Mitigation Plan                                                                                                                                                                                                  |
| ---------------- | ---------------------- | ----------------------------------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-20-SHIM        | Tampering / EoP        | cefQuery shim leaking host capability into MainWorld        | mitigate                         | The shim is a pure forwarder to channel.objects["$SD"]; PIBridge::invoke fans out ONLY to the fixed typed-slot set (no default-to-arbitrary), adding no privileged path beyond the already-sandboxed $SD surface |
| T-20-SHIM-RACE   | Tampering / DoS        | shim injected after the plugin script -> cefQuery undefined | mitigate                         | Inject at QWebEngineScript::DocumentCreation (not runJavaScript-after-load) per 18-03 Pitfall 4 — asserted by the WebEngine-gated injection-point case                                                           |
| T-20-CSS-REDIR   | Tampering              | sdpi.css redirect opening a new fetch surface               | mitigate                         | Redirect target is a fixed qrc: constant; isSdpiCssRequest is a suffix-exact match (rejects sdpi.css.evil)                                                                                                       |
| T-20-SSRF        | Information Disclosure | arbitrary https/http fetch from PI                          | accept (pre-existing, unchanged) | Existing host-equality CDN allowlist + http block untouched; this plan adds no allowlist entry                                                                                                                   |
| \</threat_model> |                        |                                                             |                                  |                                                                                                                                                                                                                  |

<verification>
- ctest --preset linux-release -R "cefquery|sdpi" green.
- grep -c 'window.cefQuery' src/app/src/pi_cef_shim.hpp >= 1; grep -c 'DocumentCreation' src/app/src/pi_cef_shim.cpp >= 1.
- grep -rln nlohmann src/app/src/pi_cef_shim.cpp src/app/src/pi_bridge.cpp returns no files.
- grep -rn QCefView src/app/src/ returns 0.
</verification>

\<success_criteria>
PLUGIN-09 gaps (a) cefQuery polyfill and (b) sdpi.css served are closed; the polyfill routes only to
the typed $SD surface via a §8 invoke dispatcher; the CSS redirect target is fixed; the existing URL
policy is unchanged.
\</success_criteria>

<output>
Create `.planning/phases/20-property-inspector-settings/20-02-SUMMARY.md` when done.
</output>
