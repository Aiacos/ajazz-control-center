---
phase: 30-plugin-host-modular-foundation
plan: 03
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - src/app/src/i_plugin_host2.hpp
  - src/app/src/unified_plugin_host.hpp
  - src/app/src/unified_plugin_host.cpp
  - src/app/src/plugin_manager.hpp
  - src/app/src/plugin_manager.cpp
  - src/app/src/application.hpp
  - src/app/src/application.cpp
  - tests/unit/test_plugin_host2.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [HOST-01, HOST-03]

must_haves:
  truths:
    - A single IPluginHost2 contract unifies the Node, HTML, native, and Python runtimes behind one spawn/lifecycle/IPC surface
    - UnifiedPluginHost::dispatch() routes by UUID internally so a caller of pluginHost2()->dispatch() reaches BOTH the .sdPlugin WS path and a Python plugin (no routing in Application::dispatch())
    - PluginManager dispatch routes all four runtimes with zero SKU-specific branches
    - Each of the four runtimes has an explicit no-regression check (not just the disconnect test)
    - Device I/O from plugins reaches hardware only via PluginDeviceBridge -> StreamDockControlService -> sidecar
  artifacts:
    - path: src/app/src/i_plugin_host2.hpp
      provides: the unified spawn/lifecycle/IPC contract (Qt types, app-layer)
      min_lines: 30
      contains: class IPluginHost2
    - path: src/app/src/unified_plugin_host.hpp
      provides: UnifiedPluginHost aggregator implementing IPluginHost2, owning PluginManager + OutOfProcessPluginHost
      min_lines: 30
      contains: class UnifiedPluginHost
    - path: src/app/src/plugin_manager.hpp
      provides: PluginManager declared as IPluginHost2 implementer (.sdPlugin sub-host)
      contains: IPluginHost2
  key_links:
    - from: src/app/src/application.cpp
      to: UnifiedPluginHost
      via: pluginHost2() returns the UnifiedPluginHost& aggregator (NOT m_pluginManager), so every IPluginHost2 caller gets all-runtime routing
      pattern: UnifiedPluginHost
    - from: src/app/src/unified_plugin_host.cpp
      to: OutOfProcessPluginHost
      via: dispatch() routes a Python UUID to the OOP host internally (Qt->STL at the boundary)
      pattern: OutOfProcessPluginHost
    - from: src/app/src/plugin_manager.cpp
      to: PluginDeviceBridge
      via: device I/O only through the bridge, zero SKU strings in dispatch
      pattern: PluginDeviceBridge
---

<objective>
Introduce IPluginHost2 — the single app-layer spawn/lifecycle/IPC contract — and a UnifiedPluginHost
aggregator (RESEARCH Pattern 2 / assumption A4) that OWNS both concrete sub-hosts: PluginManager
(Node/HTML/native over WebSocket) and OutOfProcessPluginHost (Python). UnifiedPluginHost implements
IPluginHost2 and routes dispatch by UUID INTERNALLY, so a single contract honestly serves all four
runtimes for every caller. Application::pluginHost2() returns the UnifiedPluginHost (not m_pluginManager),
and no runtime-routing logic remains in Application::dispatch(). Add the four explicit per-runtime
no-regression checks plus a test proving pluginHost2()->dispatch() reaches a Python plugin, and verify
(code-review audit) the dispatch layer has zero SKU strings and reaches devices only through
PluginDeviceBridge.

Purpose: HOST-01 (FULL UNIFICATION NOW — the locked override of the keep-separate research verdict; a
facade that leaves Python dispatch outside the IPluginHost2 surface does NOT satisfy it) and HOST-03
(SKU/wire decoupling, code-review-only). The ADR from Plan 01 documents the decision; this plan
implements the unification at the contract level via the aggregator.

Output: new i_plugin_host2.hpp + unified_plugin_host.{hpp,cpp}; PluginManager implements IPluginHost2 as
the .sdPlugin sub-host; Application wires the UnifiedPluginHost; four per-runtime regression tests + a
Python-via-IPluginHost2 dispatch test; SKU audit clean.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plugin-host-modular-foundation/30-CONTEXT.md
@.planning/phases/30-plugin-host-modular-foundation/30-RESEARCH.md
@.planning/phases/30-plugin-host-modular-foundation/30-VALIDATION.md
@.planning/phases/30-plugin-host-modular-foundation/30-ADR-plugin-host-unification.md

<interfaces>
From src/app/src/plugin_manager.hpp (the .sdPlugin sub-host UnifiedPluginHost owns):
  std::vector<PluginManifest> discover();
  void spawn(PluginManifest const& manifest);     // .js -> QProcess(node), .html -> QWebEnginePage, else -> QProcess(native)
  void rediscover();
  void onProcessFailed(QString const& uuid);
  void shutdown();                                 // sendEvent(uuid,"exitApp") then terminate then kill
  void setPluginEnabled(QString const& pluginId, bool enabled);
  bool isDisabled(QString const& uuid) const;
  signals: void pluginDisabled(QString const& uuid, QString const& reason);

From src/plugins/include/ajazz/plugins/i_plugin_host.hpp (the EXISTING Python-only interface — do NOT confuse with IPluginHost2):
virtual void addSearchPath(std::filesystem::path const&) = 0;
virtual std::size_t loadAll() = 0;
virtual std::vector<PluginInfo> plugins() = 0;
virtual bool dispatch(std::string_view pluginId, std::string_view actionId, std::string_view settingsJson) = 0;
// Implemented ONLY by OutOfProcessPluginHost (src/plugins/, STL types). PluginManager does NOT implement this.

From src/plugins/include/ajazz/plugins/out_of_process_plugin_host.hpp (the Python sub-host UnifiedPluginHost owns):
std::vector<PluginInfo> plugins() override; // STL types
void addSearchPath(std::filesystem::path const&) override;
std::size_t loadAll() override;
bool dispatch(std::string_view, std::string_view, std::string_view) override;
// shutdown round-trip + SIGKILL fallback in destructor / shutdown().

From src/app/src/application.hpp (the host members today — to be reorganised under the aggregator):
std::unique_ptr<SdPluginServer> m_pluginServer;
std::unique_ptr<PluginManager> m_pluginManager; // .sdPlugin (becomes UnifiedPluginHost's WS sub-host)
std::unique_ptr<plugins::IPluginHost> m_pluginHost; // Python OOP (becomes UnifiedPluginHost's Python sub-host)
SdPluginServer\* pluginServer() const noexcept;
PluginManager\* pluginManager() const noexcept;
void initPluginHost();

From src/app/src/plugin_device_bridge.hpp (HOST-03 boundary owner — device I/O ONLY here):
// PluginDeviceBridge -> StreamDockControlService -> mirajazz sidecar. plugin_device_bridge.cpp lines
// 1242/1251 hold "akp05e" fallback strings — CORRECT in the bridge tier; do NOT move them into plugin_manager.cpp.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IPluginHost2 + UnifiedPluginHost aggregator; make PluginManager implement IPluginHost2</name>
  <files>src/app/src/i_plugin_host2.hpp, src/app/src/unified_plugin_host.hpp, src/app/src/unified_plugin_host.cpp, src/app/src/plugin_manager.hpp, src/app/src/plugin_manager.cpp</files>
  <read_first>
    - src/app/src/plugin_manager.hpp (full public surface: discover, spawn, rediscover, shutdown, onProcessFailed, setPluginEnabled, isDisabled, pluginDisabled)
    - src/plugins/include/ajazz/plugins/i_plugin_host.hpp (existing Python interface — IPluginHost2 must NOT be this; name-confusion Pitfall 6)
    - src/plugins/include/ajazz/plugins/out_of_process_plugin_host.hpp (the Python sub-host: plugins()/loadAll()/addSearchPath()/dispatch(), all STL types)
    - 30-RESEARCH.md "Pattern 2: IPluginHost2 Unified Contract" + assumption A4 + the RESOLVED Open Questions 1-2 (Qt-types recommendation; expose discover+spawn on the surface, route dispatch by UUID inside the aggregator)
    - 30-ADR-plugin-host-unification.md (the locked UNIFY decision + COD-031 note)
  </read_first>
  <action>
    Create src/app/src/i_plugin_host2.hpp declaring an abstract class IPluginHost2 (app-layer, Qt types only —
    QString/QJsonObject; NO nlohmann, COD-031). Minimal surface (names are Claude's discretion): virtual dtor;
    discover() returning the available manifests; spawn(PluginManifest const&); shutdown();
    connectedPluginCount() const noexcept; plugins() returning the MERGED live inventory (.sdPlugin + Python);
    dispatch(QString pluginUuid, QString actionId, QJsonObject payload); pluginServer() const noexcept returning
    SdPluginServer*. Doc-comment that this SUPERSEDES the per-runtime split and is NOT the Python-only
    plugins::IPluginHost.
    Make PluginManager implement IPluginHost2 as the .sdPlugin SUB-HOST: add IPluginHost2 as a base class in
    plugin_manager.hpp (alongside QObject) and provide overrides delegating to existing methods
    (discover/spawn/shutdown exist; connectedPluginCount() forwards to SdPluginServer; PluginManager::dispatch()
    handles ONLY the .sdPlugin WS action path; pluginServer() returns the injected server). PluginManager::plugins()
    returns only the .sdPlugin inventory — the Python fold happens in the aggregator, NOT here.
    Create src/app/src/unified_plugin_host.{hpp,cpp} declaring class UnifiedPluginHost that ALSO implements
    IPluginHost2 and OWNS both sub-hosts: a PluginManager* (the WS/.sdPlugin path) and an OutOfProcessPluginHost*
    (the Python path) injected at construction (ownership stays in Application; UnifiedPluginHost holds raw pointers
    or unique_ptrs per Claude's discretion, but it is the single IPluginHost2 the app exposes). Implement:
    UnifiedPluginHost::dispatch(QString pluginUuid, ...) ROUTES BY UUID internally — a .sdPlugin UUID goes to the
    PluginManager WS path; a Python UUID goes to OutOfProcessPluginHost::dispatch() converting the QJsonObject
    payload -> settingsJson std::string and QString -> std::string_view AT THIS BOUNDARY. UnifiedPluginHost::plugins()
    FOLDS both inventories into one std::vector<PluginInfo>, converting the Python STL PluginInfo to the unified
    entry shape at the boundary. discover()/spawn() are the unified surface; internally UnifiedPluginHost calls the
    Python host's addSearchPath()/loadAll() and PluginManager's discover()/spawn(). shutdown() tears down the
    .sdPlugin plugins (exitApp -> terminate -> kill) then the Python host. NO runtime/UUID routing logic may live in
    Application::dispatch() — it all lives in UnifiedPluginHost.
    Keep the four runtime branches inside PluginManager::spawn() (.js/.html/native) and the Python wrapper, but
    ensure NONE branch on a device SKU string. Do not regress the CR-02/WR-02/sentinel paths from Plan 02. Register
    unified_plugin_host.cpp in the app target's CMake source list.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -8 ; grep -c "class IPluginHost2" src/app/src/i_plugin_host2.hpp ; grep -c "class UnifiedPluginHost" src/app/src/unified_plugin_host.hpp ; grep -c "OutOfProcessPluginHost" src/app/src/unified_plugin_host.cpp</automated>
  </verify>
  <acceptance_criteria>
    - src/app/src/i_plugin_host2.hpp exists and declares `class IPluginHost2` with a virtual destructor and >= 4 pure-virtual methods (incl. dispatch + plugins).
    - src/app/src/unified_plugin_host.hpp declares `class UnifiedPluginHost` deriving from IPluginHost2: `grep -n "IPluginHost2" src/app/src/unified_plugin_host.hpp` returns a base-class hit.
    - unified_plugin_host.cpp references OutOfProcessPluginHost (Python routing present): `grep -c "OutOfProcessPluginHost" src/app/src/unified_plugin_host.cpp` returns >= 1.
    - plugin_manager.hpp shows PluginManager deriving from IPluginHost2: `grep -n "IPluginHost2" src/app/src/plugin_manager.hpp` returns a base-class hit.
    - The app target builds clean (no link error from the new base classes / aggregator).
    - `grep -rn "nlohmann" src/app/src/i_plugin_host2.hpp src/app/src/unified_plugin_host.hpp` returns nothing (COD-031); `grep -rn "nlohmann" src/core/include/` still returns 0.
    - `grep -rn "akp05e\|akp03\|akp153" src/app/src/plugin_manager.cpp src/app/src/unified_plugin_host.cpp` returns 0 (HOST-03 dispatch clean).
  </acceptance_criteria>
  <done>IPluginHost2 contract + UnifiedPluginHost aggregator exist; UnifiedPluginHost owns both sub-hosts and routes dispatch by UUID; PluginManager implements IPluginHost2; app builds; dispatch layer SKU-free; COD-031 intact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Application onto UnifiedPluginHost; remove runtime routing from Application::dispatch()</name>
  <files>src/app/src/application.hpp, src/app/src/application.cpp</files>
  <read_first>
    - src/app/src/application.hpp lines 140-170, 270-330 (m_pluginServer, m_pluginManager, m_pluginHost members; pluginServer()/pluginManager() accessors; initPluginHost())
    - src/app/src/application.cpp (initPluginHost() body — how OutOfProcessPluginHost is constructed, search paths, loadAll; and any current dispatch()/route-to-Python branch that must be DELETED)
    - src/app/src/plugin_manager.cpp shutdown() (the exitApp-then-terminate path UnifiedPluginHost::shutdown() must still drive for all live plugins)
    - src/app/src/unified_plugin_host.hpp (the aggregator surface from Task 1 to construct and expose)
    - 30-RESEARCH.md assumption A4 + RESOLVED Open Questions (aggregator owns both sub-hosts; dispatch routes by UUID INSIDE the aggregator, not in Application)
  </read_first>
  <action>
    Construct a UnifiedPluginHost in Application (e.g. m_pluginHost2) wired with the existing m_pluginManager and
    m_pluginHost (Python) as its two sub-hosts; keep the two concrete owners alive where they are, but the unified
    surface is the aggregator. Change Application::pluginHost2() to return IPluginHost2* (or UnifiedPluginHost&)
    backed by m_pluginHost2 — NOT m_pluginManager — so EVERY caller of the IPluginHost2 surface gets correct
    routing for all four runtimes. REMOVE any runtime/UUID routing branch that currently lives in
    Application::dispatch() (or wherever Application chooses .sdPlugin-vs-Python today): dispatch now calls
    m_pluginHost2->dispatch(uuid, ...) and the aggregator decides. plugins() seen by callers (LoadedPluginsModel)
    comes from m_pluginHost2->plugins(), which already folds .sdPlugin + Python. Preserve shutdown ordering by
    delegating to UnifiedPluginHost::shutdown() (exitApp to all live .sdPlugin plugins, then Python host teardown).
    Do NOT introduce any device-SKU coupling here; plugin device I/O continues to flow through PluginDeviceBridge only.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz-control-center 2>&1 | tail -6 ; echo "--- routing must be in the aggregator, not Application ---" ; grep -c "UnifiedPluginHost\|pluginHost2" src/app/src/application.hpp ; ctest --preset linux-release -R "OutOfProcess" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "UnifiedPluginHost\|pluginHost2" src/app/src/application.hpp` shows the aggregator member and the pluginHost2() accessor; pluginHost2() is backed by the UnifiedPluginHost, NOT m_pluginManager.
    - Application::dispatch() (or the prior route point) no longer contains a .sdPlugin-vs-Python branch: `grep -n "m_pluginHost->dispatch\|m_pluginHost2->dispatch" src/app/src/application.cpp` shows dispatch delegates to the aggregator (m_pluginHost2), not directly to the Python host.
    - The app target builds clean and links.
    - `ctest --preset linux-release -R OutOfProcess` stays green (Python dispatch through the aggregator unchanged in behaviour).
    - Conversion is at the app/aggregator/wrapper boundary; `grep -rn "nlohmann" src/core/include/` still returns 0 (COD-031).
  </acceptance_criteria>
  <done>Application exposes one IPluginHost2 surface backed by UnifiedPluginHost; runtime routing removed from Application::dispatch(); plugins() merges .sdPlugin + Python via the aggregator; shutdown ordering preserved; Python tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Four-runtime no-regression checks + Python-via-IPluginHost2 dispatch test + SKU audit</name>
  <files>tests/unit/test_plugin_host2.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - tests/unit/test_plugin_lifecycle.cpp (existing Node/HTML/native spawn-dispatch cases + FakeProbe to extend)
    - tests/unit/test_out_of_process_plugin_host.cpp (Python dispatch case pattern + a fake/stub OOP host to reuse)
    - tests/unit/test_plugin_host2.cpp (the Plan-01/02 file to extend; reuse its fixtures)
    - src/app/src/unified_plugin_host.hpp (the aggregator surface to drive through the IPluginHost2 pointer)
    - src/app/src/plugin_manager.cpp spawn() (.js/.html/native dispatch — confirm no SKU branch)
    - 30-VALIDATION.md Per-Task Verification Map (the four HOST-01 runtime rows + their ctest commands)
  </read_first>
  <action>
    Add four explicit per-runtime no-regression TEST_CASEs to test_plugin_host2.cpp (ASCII-only names), each
    asserting the runtime still spawns/dispatches through the IPluginHost2 surface without a SKU branch:
    (1) "Node runtime spawns and registers via unified host" — FakeProbe + a .js manifest; assert spawn routes to
    the node QProcess path (reuse lastNodeArgvForTesting) and connectedPluginCount reflects registration.
    (2) "HTML runtime loads via QWebEnginePage via unified host" — guard with AJAZZ_HAVE_WEBENGINE; assert the HTML
    branch is taken and the WR-02 no-respawn guard (process==nullptr) still holds.
    (3) "Native runtime spawns via unified host" — a non-.js/.html CodePath manifest; assert the native QProcess branch is taken.
    (4) "Python runtime dispatches via OOP host through unified surface" — drive dispatch() for a Python uuid and
    assert it reaches the OutOfProcessPluginHost path (use the existing OOP fixture / a fake host).
    ADD a fifth, contract-level TEST_CASE that proves the WARNING-2 fix: "unified host dispatch reaches Python
    through the IPluginHost2 pointer" — construct a UnifiedPluginHost wired with a fake/stub OutOfProcessPluginHost
    (records the last dispatch it received) and a PluginManager (or fake .sdPlugin sub-host); call dispatch() THROUGH
    an `IPluginHost2*` pointer (not the concrete type) for a Python UUID; assert the fake OOP host recorded the
    dispatch (uuid/action/payload converted Qt->STL), proving pluginHost2()->dispatch() reaches a Python plugin and
    does NOT silently miss it. Also assert a .sdPlugin UUID through the same IPluginHost2* pointer routes to the WS
    path (does not reach the Python fake).
    Where a case is better expressed against an existing suite, instead ADD assertions to test_plugin_lifecycle.cpp /
    test_out_of_process_plugin_host.cpp and reference them from the VALIDATION map — but each of the four runtime
    behaviours AND the Python-via-IPluginHost2 routing MUST have a named automated assertion. Then perform the
    HOST-03 code-review audit inline: run the SKU grep across plugin_manager.cpp + unified_plugin_host.cpp and record
    the result (0 hits) in the SUMMARY (no CI gate — code-review-only per the locked decision). Register any new
    source in tests/unit/CMakeLists.txt under the AJAZZ_HAVE_WEBSOCKETS block.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_unit_tests >/dev/null 2>&1 ; ctest --preset linux-release -R "PluginManagerTest|OutOfProcess|unified host" 2>&1 | tail -12 ; echo "SKU hits:" ; grep -rn "akp05e\|akp03\|akp153" src/app/src/plugin_manager.cpp src/app/src/unified_plugin_host.cpp | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - test_plugin_host2.cpp (or the referenced suites) contains a named automated assertion for each of the four runtimes (Node, HTML, native, Python).
    - A named TEST_CASE drives dispatch() through an `IPluginHost2*` pointer and asserts a Python UUID reaches the (fake) OutOfProcessPluginHost while a .sdPlugin UUID does not — proving pluginHost2()->dispatch() reaches Python: `grep -n "IPluginHost2" tests/unit/test_plugin_host2.cpp` shows the pointer-typed dispatch case.
    - `ctest --preset linux-release -R "PluginManagerTest|OutOfProcess|unified host"` is fully green (no runtime regressed; the Python-routing case passes).
    - `grep -rn "akp05e\|akp03\|akp153" src/app/src/plugin_manager.cpp src/app/src/unified_plugin_host.cpp | wc -l` returns 0 (HOST-03 audit clean).
    - Full suite `ctest --preset linux-release` is green vs the 694-test baseline.
  </acceptance_criteria>
  <done>Four per-runtime no-regression checks pass, the Python-via-IPluginHost2 dispatch case passes, SKU audit returns 0 in plugin_manager.cpp + unified_plugin_host.cpp, full suite green.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                                 | Description                                                                                                             |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| plugin runtime (Node/HTML/native/Python) -> IPluginHost2 | One contract (UnifiedPluginHost) mediates all four untrusted runtimes; dispatch routes by UUID, not by device internals |
| plugin -> PluginDeviceBridge -> sidecar                  | The only path plugin device I/O may take; SKU/wire internals stay below the bridge                                      |

## STRIDE Threat Register

| Threat ID        | Category                          | Component                                  | Disposition            | Mitigation Plan                                                                                                                                                                                              |
| ---------------- | --------------------------------- | ------------------------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-30-route       | Spoofing / Tampering              | UnifiedPluginHost::dispatch routing        | mitigate               | dispatch routes by UUID inside the aggregator; test drives dispatch via an IPluginHost2\* pointer and asserts Python UUIDs reach the OOP host (Task 3) — no silent-miss facade                               |
| T-30-sku         | Information Disclosure / coupling | PluginManager + UnifiedPluginHost dispatch | mitigate (code-review) | SKU grep audit returns 0 in plugin_manager.cpp + unified_plugin_host.cpp (Task 3); ADR documents the boundary; per locked decision NO CI gate added                                                          |
| T-30-cod031      | Tampering                         | layer boundary (nlohmann into core)        | mitigate               | IPluginHost2/UnifiedPluginHost use Qt types only; Qt->STL conversion lives at the aggregator boundary; grep asserts 0 nlohmann in src/core/include/, i_plugin_host2.hpp, unified_plugin_host.hpp (Tasks 1-2) |
| T-30-regress     | Denial of Service                 | four-runtime spawn/dispatch                | mitigate               | explicit per-runtime no-regression tests (Task 3); CR-02/WR-02/sentinel paths preserved                                                                                                                      |
| T-30-SC          | Tampering                         | npm/pip/cargo installs                     | accept                 | No package installs this phase (internal refactor); RESEARCH Package Legitimacy Audit = not applicable                                                                                                       |
| \</threat_model> |                                   |                                            |                        |                                                                                                                                                                                                              |

<verification>
- App target builds clean with IPluginHost2 implemented by both PluginManager and UnifiedPluginHost, and Application exposing pluginHost2() backed by the aggregator.
- ctest --preset linux-release -R "PluginManagerTest|OutOfProcess|unified host" green (all four runtimes no-regression + Python-via-IPluginHost2 routing).
- pluginHost2()->dispatch() reaches a Python plugin: the IPluginHost2*-pointer dispatch test passes.
- SKU audit: grep -rn "akp05e|akp03|akp153" src/app/src/plugin_manager.cpp src/app/src/unified_plugin_host.cpp returns 0.
- COD-031: grep -rn "nlohmann" src/core/include/ returns 0; i_plugin_host2.hpp / unified_plugin_host.hpp have no nlohmann.
- Full suite ctest --preset linux-release green vs the 694-test baseline.
- Live (debug channel, before declaring done): scripts/ajazz-debug plugin.list returns the merged plugin inventory without a crash.
</verification>

\<success_criteria>

- IPluginHost2 is the single contract; UnifiedPluginHost aggregates both sub-hosts and routes dispatch by UUID; Application exposes the aggregator (not m_pluginManager).
- pluginHost2()->dispatch() reaches all four runtimes — proven by the IPluginHost2\*-pointer Python dispatch test; no routing remains in Application::dispatch().
- All four runtimes (Node, HTML, native, Python) pass an explicit no-regression check.
- plugin dispatch is SKU-free (audit 0 across plugin_manager.cpp + unified_plugin_host.cpp); device I/O only via PluginDeviceBridge.
- COD-031 intact; full suite green.
  \</success_criteria>

<output>
Create .planning/phases/30-plugin-host-modular-foundation/30-03-SUMMARY.md when done.
</output>
