---
phase: 34-per-app-profiles-event-parity-audit
plan: 05
type: execute
wave: 4
depends_on: [34-04]
files_modified:
  - src/app/qml/SettingsPage.qml
  - src/app/src/profile_controller.hpp
  - src/app/src/profile_controller.cpp
  - tests/qml/CMakeLists.txt
autonomous: false
requirements: [APROF-03]
must_haves:
  truths:
    - A Per-app profiles section in SettingsPage lets the user map an application name to a profile, writing Profile::applicationHints
    - Every interactive control has an objectName and is drivable from scripts/ajazz-debug (qml.set/qml.click), using Button/SecondaryButton not Switch
    - A capability-warning chip is visible with non-empty warning text when the active watcher reports no foreground capability, hidden when present
    - The chip is amber/warning family (graceful degradation), not error red
  artifacts:
    - path: src/app/qml/SettingsPage.qml
      provides: Assign-profile-to-app surface + capability-warning chip
      contains: waylandCapabilityWarningChip
    - path: src/app/src/profile_controller.cpp
      provides: Q_INVOKABLE applicationHints writer
      contains: applicationHints
  key_links:
    - from: appProfileProfileSelector / appProfileAppNameField
      to: Profile::applicationHints
      via: ProfileController Q_INVOKABLE writer
      pattern: applicationHints
    - from: waylandCapabilityWarningChip.visible
      to: IActiveWindowWatcher::capabilityAvailable()
      via: ProfileController-exposed capability property
      pattern: capabilityAvailable|warningText
---

<objective>
Build the only UI of Phase 34 (APROF-03): a "Per-app profiles" assign surface inside `SettingsPage.qml` that maps application names to profiles (writing `Profile::applicationHints` via a new `ProfileController` Q_INVOKABLE), plus the Wayland/GNOME capability-warning chip driven by `IActiveWindowWatcher::capabilityAvailable()`. Then run the consolidated mandatory live debug-channel phase gate.

Purpose: closes the user-facing half of per-app profiles and surfaces graceful degradation. Per VERIF-01, every control is objectName-addressable and the phase is verified live, not just by ctest.
Output: the assign-profile section + warning chip in SettingsPage, the applicationHints writer + a capability property on ProfileController, and a green QML smoke + live debug-channel pass.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-RESEARCH.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-UI-SPEC.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-VALIDATION.md
@.planning/phases/34-per-app-profiles-event-parity-audit/34-04-SUMMARY.md

<interfaces>
<!-- The exact QML analogs + controller surface to reuse. Read SettingsPage.qml + LoadedPluginsPage.qml first. -->
From src/app/qml/SettingsPage.qml:46-64 (mirror EXACTLY):
  - Section Label (Theme.typeTitleMedium, Accessible.role: Accessible.Heading) + Frame card
    (background Rectangle: Theme.tile / Theme.borderSubtle / border.width 1 / radius Theme.radiusMd) wrapping a ColumnLayout.
From src/app/qml/LoadedPluginsPage.qml:168-209 (trust-chip -> capability chip):
  - Rectangle pill, Layout.preferredHeight 24, radius 12, border.width 1, centered Text;
    Theme.chipBgWarning / chipBorderWarning / chipFgWarning (AMBER, not error red); ToolTip on hover.
From src/app/src/profile_controller.hpp (writer shape to mirror): commitKeyBinding/commitEncoderBinding Q_INVOKABLE writers; profilesForDevice(codename) -> QVariantList.
From src/core/include/ajazz/core/profile.hpp:192: std::vector<std::string> applicationHints (no schema change/migration).
UI-SPEC objectNames (MUST set, may extend): addAppProfileMappingButton, appProfileAppNameField, appProfileProfileSelector, removeAppProfileMappingButton, waylandCapabilityWarningChip (+ addressable warningText).
HARNESS RULE (CLAUDE.md): use Button/SecondaryButton, NOT Switch (qml.invoke toggle does not fire onToggled).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileController applicationHints writer + capability property</name>
  <files>src/app/src/profile_controller.hpp, src/app/src/profile_controller.cpp</files>
  <read_first>
    - src/app/src/profile_controller.hpp + .cpp (commitKeyBinding/commitEncoderBinding writer shape; profilesForDevice; signals)
    - src/core/include/ajazz/core/profile.hpp (applicationHints @192)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-PATTERNS.md (profile_controller section)
  </read_first>
  <action>
    Add a Q_INVOKABLE that writes Profile::applicationHints for a given profile id + application name (mirror the commitKeyBinding/commitEncoderBinding writer shape and persistence path; add the app name to the target profile's applicationHints, persist, emit the relevant changed signal). Add a Q_INVOKABLE remover (remove an app name from a profile's hints). Add a Q_INVOKABLE/read property `foregroundCapabilityAvailable` reflecting the active IActiveWindowWatcher::capabilityAvailable() (Application injects the watcher's capability into ProfileController, or exposes it through the existing seam) and a `foregroundCapabilityWarning` string (the UI-SPEC detail copy) so the QML chip + live qml.get can read non-empty warning text without hover. No profile schema change/migration (RESEARCH Runtime State Inventory — applicationHints already persists). V5: bound/validate the app-name input length.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release >/tmp/b3405a.log 2>&1 && grep -q "applicationHints" src/app/src/profile_controller.cpp && grep -Eq "foregroundCapability|capabilityAvailable" src/app/src/profile_controller.hpp && echo CONTROLLER-OK || (tail -30 /tmp/b3405a.log; false)</automated>
  </verify>
  <acceptance_criteria>
    - profile_controller exposes a Q_INVOKABLE applicationHints writer + remover that persist to Profile::applicationHints (no schema migration).
    - profile_controller exposes a foreground-capability property + a non-empty warning-text string property.
    - The app-name input is length-bounded (V5).
    - The app builds.
  </acceptance_criteria>
  <done>ProfileController can write/remove applicationHints and exposes the watcher capability + warning text for the chip.</done>
</task>

<task type="auto">
  <name>Task 2: Per-app profiles section + capability-warning chip in SettingsPage.qml</name>
  <files>src/app/qml/SettingsPage.qml, tests/qml/CMakeLists.txt</files>
  <read_first>
    - src/app/qml/SettingsPage.qml (section Label + Frame card pattern @46-64; existing sections)
    - src/app/qml/LoadedPluginsPage.qml (trust-chip @168-209)
    - .planning/phases/34-per-app-profiles-event-parity-audit/34-UI-SPEC.md (full token/copy/objectName contract — Surface 1 + Surface 2)
  </read_first>
  <action>
    Add a "Per-app profiles" section to SettingsPage.qml mirroring the section-Label + Frame-card structure exactly (Accessible.Heading; Frame background Theme.tile/borderSubtle/radiusMd). Inside: a list of mapping rows (per row: appProfileAppNameField TextField placeholder "Application name" + appProfileProfileSelector listing profilesForDevice + removeAppProfileMappingButton SecondaryButton) and an addAppProfileMappingButton (primary "Add mapping") that writes via the Task-1 Q_INVOKABLE. Use the EmptyState component with the UI-SPEC empty-state copy when zero mappings; show the "No profiles available" error-state copy when zero profiles exist. Add waylandCapabilityWarningChip adjacent to the section heading: a Rectangle pill (height 24, radius 12, border.width 1) with Theme.chipBgWarning/chipBorderWarning/chipFgWarning (AMBER), label "Limited on this desktop" at Theme.fontXs/Font.DemiBold, a ToolTip carrying the full detail copy, and an addressable warningText property (bound to ProfileController.foregroundCapabilityWarning); visible only when foreground capability is absent. ALL strings via qsTr(), ASCII-only; ALL tokens from Theme.* (no literals except the sanctioned pill radius 12). Set objectName on every control per UI-SPEC. Use Button/SecondaryButton, never Switch. Register the new surface in the tests/qml smoke target if the QML harness needs the new objectNames covered (mirror existing tests/qml registration); keep it off Windows registration per the CLAUDE.md tests/qml Windows-hang carve-out.
  </action>
  <verify>
    <automated>cmake --build --preset linux-release >/tmp/b3405b.log 2>&1 && grep -q "waylandCapabilityWarningChip" src/app/qml/SettingsPage.qml && grep -q "addAppProfileMappingButton" src/app/qml/SettingsPage.qml && grep -q "appProfileProfileSelector" src/app/qml/SettingsPage.qml && ! grep -nE "\bSwitch\b" src/app/qml/SettingsPage.qml | grep -i appprofile && echo QML-OK</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage.qml has a Per-app profiles section mirroring the section-Label + Frame-card pattern.
    - objectNames present: addAppProfileMappingButton, appProfileAppNameField, appProfileProfileSelector, removeAppProfileMappingButton, waylandCapabilityWarningChip (+ warningText).
    - The chip uses Theme.chip*Warning (amber), label "Limited on this desktop", non-empty warningText.
    - No Switch is used for the app-profile actions; all strings qsTr() + ASCII; tokens from Theme.*.
    - The app builds and the tests/qml smoke target builds (Linux).
  </acceptance_criteria>
  <done>The assign-profile surface + amber capability-warning chip render with full objectName coverage and Theme tokens; no Switch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 34 behavior, verified live through the debug channel (VERIF-01 phase gate — ctest green is necessary but NOT sufficient). This consolidates the five mandatory live checks from VALIDATION.md.</what-built>
  <how-to-verify>
    Build, then launch headless-drivable (kill prior instance + stale socket first per CLAUDE.md recipe):
    `AJAZZ_DEBUG_CONTROL=1 nohup build/linux-release/src/app/ajazz-control-center >/tmp/acc34.log 2>&1 &`

```
1. APROF-02 latency (synthetic): `scripts/ajazz-debug screenshot` (before), `scripts/ajazz-debug window.setForeground --params '{"appId":"<app-mapped-in-hints>"}'`, then `screenshot` (after) within 500 ms; `scripts/ajazz-debug log.tail` shows willDisappear then willAppear in order.
2. Wayland live switch on niri: focus a mapped app then another; confirm the watcher emits the app_id change and the device repaints (< 500 ms).
3. APROF-03 chip: with the capability present, `scripts/ajazz-debug qml.get --params '{"objectName":"waylandCapabilityWarningChip"}'` shows hidden; force capability-absent (or run where the global is absent) and confirm visible + non-empty warningText; confirm NO crash and manual profile switching still works.
4. APROF-03 controls: `qml.set appProfileAppNameField`, `qml.set/get appProfileProfileSelector`, `qml.click addAppProfileMappingButton`, `qml.click removeAppProfileMappingButton` all drive the mapping; confirm the hint persists.
5. EVENT dispatch: trigger a synthetic switchToProfile plugin action -> profile switches; a synthetic systemDidWakeUp reaches a subscribed plugin (eventSent console / log.tail); applicationDidLaunch fires to a registered plugin.

Read each screenshot and the log.tail output; confirm real behavior.
```

</how-to-verify>
  <resume-signal>Type "approved" once all five live checks pass (or describe failures with the log/screenshot evidence).</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                               |
| --------------------------------- | ------------------------------------------------------------------------- |
| user UI input -> applicationHints | App-name strings typed in the assign-profile UI become profile match keys |
| watcher capability -> UI chip     | Degraded-capability signal surfaced to the user                           |

## STRIDE Threat Register

| Threat ID        | Category              | Component                                           | Disposition | Mitigation Plan                                                                                                               |
| ---------------- | --------------------- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-34-05-01       | Tampering             | UI-entered app-name used as a profile key           | mitigate    | V5 length-bound + validate the app-name input; stored strictly as a match token; never eval/exec                              |
| T-34-05-02       | (degraded capability) | Wayland global absent (GNOME/KDE-no-wlr)            | mitigate    | capabilityAvailable() drives the amber warning chip; manual switching still works; fail safe, never fail open                 |
| T-34-05-03       | Spoofing              | misleading "everything fine" when capability absent | mitigate    | Chip visible-on-absent (trusted-chip-invisible convention inverted); non-empty warning text addressable for live verification |
| \</threat_model> |                       |                                                     |             |                                                                                                                               |

<verification>
- `cmake --build --preset linux-release` builds the UI + controller.
- `grep` confirms all five objectNames + amber chip tokens in SettingsPage.qml; no Switch for app-profile actions.
- Full `ctest --preset linux-release` (incl. tests/qml on Linux) green.
- The consolidated live debug-channel checkpoint (5 checks) passes.
</verification>

\<success_criteria>

- APROF-03: assign-profile surface writes applicationHints; warning chip visible+non-empty on degraded capability, hidden when present, amber not red.
- Every new control objectName-addressable; no Switch.
- Phase 34 live-verified via the debug channel (VERIF-01 gate).
  \</success_criteria>

<output>
Create `.planning/phases/34-per-app-profiles-event-parity-audit/34-05-SUMMARY.md` when done.
</output>
