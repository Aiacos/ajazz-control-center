---
phase: 33-property-inspector-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/src/plugin_device_bridge.cpp
  - src/app/src/property_inspector_controller.hpp
  - src/app/src/property_inspector_controller.cpp
  - src/app/src/application.cpp
  - tests/unit/test_plugin_device_bridge.cpp
autonomous: true
requirements: [PI-04]

must_haves:
  truths:
    - titleParametersDidChange is emitted on the wire immediately after every willAppear send (keys, encoders, touch zones) for the same context
    - propertyInspectorDidAppear is emitted on the wire when a PI page opens (loadInspector completes)
    - propertyInspectorDidDisappear is emitted on the wire when a PI page closes (closeInspector) AND when loadInspector tears down a previous PI to switch to a new one
    - A Catch2 unit test asserts titleParametersDidChange payload completeness (all SDK-2 keys) and ordering after willAppear for the same context
  artifacts:
    - path: src/app/src/plugin_device_bridge.cpp
      provides: titlePayload(ctx) helper + 3 titleParametersDidChange sends after the 3 willAppear sites
      contains: titleParametersDidChange
    - path: src/app/src/property_inspector_controller.hpp
      provides: inspectorOpened / inspectorClosed signals
      contains: inspectorOpened
    - path: src/app/src/property_inspector_controller.cpp
      provides: emit inspectorOpened in loadInspector; emit inspectorClosed in closeInspector + in the PI-switch teardown block
      contains: emit inspectorOpened
    - path: src/app/src/application.cpp
      provides: connect inspectorOpened/inspectorClosed to SdPluginServer::sendEvent(propertyInspectorDidAppear/DidDisappear)
      contains: propertyInspectorDidAppear
    - path: tests/unit/test_plugin_device_bridge.cpp
      provides: titleParametersDidChange completeness + ordering Catch2 test
      contains: titleParametersDidChange
  key_links:
    - from: plugin_device_bridge.cpp willAppear sites
      to: SdPluginServer::sendEvent(titleParametersDidChange)
      via: inline emit immediately after each willAppear send
      pattern: titleParametersDidChange
    - from: PropertyInspectorController::loadInspector/closeInspector
      to: Application -> SdPluginServer::sendEvent(propertyInspectorDidAppear/DidDisappear)
      via: new inspectorOpened/inspectorClosed signals routed through the Application seam
      pattern: inspectorOpened|inspectorClosed
---

<objective>
Build the three MISSING PI-04 lifecycle events (PI-04). All three are emitted NOWHERE
in `src/` today — this is the phase's primary build work.

1. `titleParametersDidChange` — emit immediately after each of the 3 existing `willAppear`
   sends in `plugin_device_bridge.cpp` (keys / encoders / touch zones), same context, same
   `eventEnvelope`.
1. `propertyInspectorDidAppear` — emit when a PI page opens (loadInspector completes).
1. `propertyInspectorDidDisappear` — emit when a PI page closes (closeInspector) AND on the
   PI->PI switch edge (loadInspector tears down the previous channel without calling
   closeInspector).

Appear/disappear are routed through the EXISTING Application seam (new controller signals ->
Application lambda -> `SdPluginServer::sendEvent`), keeping the controller free of a raw
`SdPluginServer*` (matches the existing `toPluginRequested`/`contextSettingsChanged` indirection).

Purpose: closes the v1.3 PI stub's lifecycle gap so plugins receive the full SDK-2 event set.
Output: 3 new wire events + a Catch2 completeness/ordering test for the title event.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-property-inspector-end-to-end/33-RESEARCH.md
@.planning/phases/33-property-inspector-end-to-end/33-CONTEXT.md

@src/app/src/plugin_device_bridge.cpp
@src/app/src/property_inspector_controller.hpp
@src/app/src/property_inspector_controller.cpp
@src/app/src/application.cpp
@tests/unit/test_plugin_device_bridge.cpp

<interfaces>
<!-- Exact seams to build against. Extracted from the live code this session. Do NOT re-explore. -->

plugin_device_bridge.cpp (anon namespace, lines 311-343):
QJsonObject instancePayload(ActionContext const& ctx, bool isInMultiAction = false);
// -> { settings, coordinates:{row,column}, controller, state, isInMultiAction }
QJsonObject eventEnvelope(QString const& event, ActionContext const& ctx, QJsonObject const& payload);
// -> { event, action: ctx.actionUUID, context: deriveContextId(ctx), device: ctx.deviceId, payload }

The 3 existing willAppear send sites (each followed by the NEW title send):
line 1174 (keys): m_server->sendEvent(owner, eventEnvelope("willAppear", ctx, instancePayload(ctx)));
line 1217 (encoders): m_server->sendEvent(owner, eventEnvelope("willAppear", ctx, instancePayload(ctx)));
line 1266 (touch zones):m_server->sendEvent(owner, eventEnvelope("willAppear", ctx, instancePayload(ctx)));

property_inspector_controller.hpp:
void loadInspector(QString pluginUuid, QString htmlAbsPath, QString actionUuid, QString contextUuid); // Q_INVOKABLE
void closeInspector(); // Q_INVOKABLE
signals: hasHtmlInspectorChanged(); activeInspectorChanged(); activeBridgeChanged(PIBridge\* bridge);

property_inspector_controller.cpp:
loadInspector (169-283): under AJAZZ_HAVE_WEBENGINE it tears down the previous channel at
lines 239-243 (the PI->PI switch teardown — currently NO disappear fires here), creates the
new channel/bridge, emits activeBridgeChanged at :256, calls bridge->getSettings() at :270.
closeInspector (285-301): tears down activeChannel inside the `webEngine_->activeChannel != nullptr`
branch (:287-295) BEFORE clearing identity.

application.cpp (684-727): the activeBridgeChanged seam. Captures m_pluginServer.get()
(a raw pointer that outlives the connection) and connects bridge signals to
m_pluginServer->sendEvent(uuid, "...", payload). This is the pattern to mirror for the
appear/disappear wiring.

SdPluginServer::sendEvent has NO event-name allowlist (sd_plugin_server.cpp:534-566) — adding
a new event string "just works" on the wire. Do NOT add an allowlist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit titleParametersDidChange after each willAppear + lock it with a Catch2 test</name>
  <files>src/app/src/plugin_device_bridge.cpp, tests/unit/test_plugin_device_bridge.cpp</files>
  <behavior>
    - After each of the 3 willAppear sends (lines 1174 / 1217 / 1266), the same `owner`/`ctx`
      receives a `titleParametersDidChange` event, in that order (willAppear THEN title) for the
      same context.
    - The titleParametersDidChange payload contains EVERY SDK-2 key: `settings`,
      `coordinates:{row,column}`, `controller`, `state`, `title`, and
      `titleParameters:{fontFamily,fontSize,fontStyle,fontUnderline,showTitle,titleAlignment,titleColor}`.
    - The envelope carries top-level `action`/`context`/`device` siblings (via eventEnvelope), same
      as willAppear, with `context` matching the willAppear context for that ctx.
  </behavior>
  <action>
    Add a `titlePayload(ActionContext const& ctx)` helper in the anon namespace next to
    `instancePayload` (plugin_device_bridge.cpp:311). Model it on `instancePayload` (reuse the same
    settings parse, coordinates, controller, state) and ADD `title` (sourced from the binding label
    on `ctx`; empty string is acceptable when no label) and a `titleParameters` object with the
    SDK-2 defaults: fontFamily "", fontSize 12, fontStyle "", fontUnderline false, showTitle true,
    titleAlignment "middle", titleColor "#ffffff". These defaults are ASSUMED per research A1/A2 —
    the Catch2 test below LOCKS the chosen shape; flag in the SUMMARY that the exact values await
    the human-verify plugin.

```
Immediately after EACH of the 3 willAppear sends (keys 1174, encoders 1217, touch zones 1266),
add: `m_server->sendEvent(owner, eventEnvelope(QStringLiteral("titleParametersDidChange"), ctx, titlePayload(ctx)));`
Emit inline at the same site (not in a separate pass) so ordering with willAppear is guaranteed
with no extra plumbing (research Pitfall 1). Do NOT add an allowlist to sendEvent.

Add a Catch2 TEST_CASE in test_plugin_device_bridge.cpp (ASCII-only title, tag
`[plugin-device-bridge][e2e][lifecycle][PI-04]`). Model it on the existing
"willAppear sent on plugin registration with bound action" test (line 1233): build a Profile with
a key bound to a plugin action, connect a loopback client, call onPluginRegistered, pump, then:
(a) assert the received event-name sequence contains willAppear THEN titleParametersDidChange for
the SAME context (use the received-order helpers already in the file — receivedEventNames /
firstEventForEvent); (b) assert the title event's payload contains every SDK-2 key listed in the
behavior block. Use the existing test helpers (ensureQCoreApp, connectAndRegister, pump19,
receivedEventNames, firstEventForEvent) — do NOT invent new harness.
```

</action>
  <read_first>
    src/app/src/plugin_device_bridge.cpp:300-358 (helpers), :1160-1268 (the 3 willAppear sites)
    tests/unit/test_plugin_device_bridge.cpp:1230-1286 (the willAppear E2E test to model on)
  </read_first>
  <verify>
    <automated>ctest --preset linux-release -R "plugin_device|plugin-device-bridge" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c titleParametersDidChange src/app/src/plugin_device_bridge.cpp` returns >= 3 (one per willAppear site).
    - New Catch2 test passes; the assertion proves willAppear precedes titleParametersDidChange for the same context.
    - `grep -n "titlePayload" src/app/src/plugin_device_bridge.cpp` shows the helper defined and called 3x.
    - The title test asserts presence of all 7 titleParameters sub-keys + title + settings + coordinates + controller + state.
  </acceptance_criteria>
  <done>titleParametersDidChange is emitted inline after all 3 willAppear sends; a Catch2 test locks payload completeness + willAppear-then-title ordering; scoped ctest green.</done>
</task>

<task type="auto">
  <name>Task 2: Emit propertyInspectorDidAppear/DidDisappear via new controller signals + Application seam</name>
  <files>src/app/src/property_inspector_controller.hpp, src/app/src/property_inspector_controller.cpp, src/app/src/application.cpp</files>
  <action>
    In property_inspector_controller.hpp, add two signals next to activeBridgeChanged:
    `void inspectorOpened(QString pluginUuid, QString actionUuid, QString contextUuid);` and
    `void inspectorClosed(QString pluginUuid, QString actionUuid, QString contextUuid);`. Keep the
    controller free of any `SdPluginServer*` (research Open Question 1 / Don't-Hand-Roll: preserve
    the audited indirection, matching toPluginRequested).

```
In property_inspector_controller.cpp:
- loadInspector: at the end of the AJAZZ_HAVE_WEBENGINE branch (after `bridge->getSettings()`
  at :270), `emit inspectorOpened(pluginUuid, actionUuid, contextUuid);`.
- loadInspector PI->PI switch edge: in the previous-channel teardown block (:239-243), BEFORE
  nulling, capture the OUTGOING bridge's pluginUuid/actionUuid/contextUuid (from the prev
  PIBridge — it exposes pluginUuid(); use the bridge accessors / store the active identity as
  members if the prev bridge does not expose action/context) and
  `emit inspectorClosed(prevUuid, prevAction, prevCtx);` so switching directly between two PIs
  fires didDisappear for the first (research Pitfall 2 / Gap 1c edge case). If the previous
  bridge does not expose action/context, add small `activePluginUuid_/activeActionUuid_/activeContextUuid_`
  members set at load time and read them here — executor discretion, but the disappear MUST fire.
- closeInspector: inside the `webEngine_->activeChannel != nullptr` branch (:287), BEFORE
  teardown (while identity is still known), `emit inspectorClosed(...)`.

In application.cpp, near the activeBridgeChanged connect (684-710), add two connects:
`connect(m_propertyInspector.get(), &PropertyInspectorController::inspectorOpened, this,
  [this](QString uuid, QString action, QString ctx){ if (m_pluginServer)
    m_pluginServer->sendEvent(uuid, QStringLiteral("propertyInspectorDidAppear"),
      QJsonObject{{"action",action},{"context",ctx}}); });` and the symmetric one for
`inspectorClosed` -> `"propertyInspectorDidDisappear"`. Use the SDK-2 envelope `{action, context}`
(device can be added if the controller knows it; not required by PI-04). Do NOT add an allowlist.
```

</action>
  <read_first>
    src/app/src/property_inspector_controller.hpp:168-209 (invokables + signals + WebEngineImpl pimpl)
    src/app/src/property_inspector_controller.cpp:169-301 (loadInspector + closeInspector)
    src/app/src/application.cpp:678-727 (the activeBridgeChanged seam to mirror)
  </read_first>
  <verify>
    <automated>ctest --preset linux-release -R "pi-bridge|plugin-server|property-inspector" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "propertyInspectorDidAppear" src/app/src/application.cpp` returns >= 1.
    - `grep -c "propertyInspectorDidDisappear" src/app/src/application.cpp` returns >= 1.
    - `grep -c "inspectorOpened\|inspectorClosed" src/app/src/property_inspector_controller.cpp` shows the open emit + at least 2 close emits (closeInspector AND the PI-switch teardown).
    - The controller header declares both signals; no `SdPluginServer` type appears in property_inspector_controller.{hpp,cpp} (`grep -c SdPluginServer src/app/src/property_inspector_controller.*` returns 0).
    - Full build + scoped ctest green.
  </acceptance_criteria>
  <done>loadInspector emits inspectorOpened; closeInspector AND the PI-switch teardown emit inspectorClosed; Application routes both to SdPluginServer::sendEvent(propertyInspectorDidAppear/DidDisappear); controller stays free of a raw server pointer.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| host -> plugin (WebSocket) | The 3 new events are OUTBOUND host->plugin on the plugin's own already-authenticated socket. No new untrusted input crosses into the host. |

## STRIDE Threat Register

| Threat ID        | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                        |
| ---------------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-33-01          | Information disclosure | titleParametersDidChange payload | accept      | Payload is the plugin's own binding label/settings echoed back to the owning plugin only (sendEvent targets `owner`); no cross-plugin leak — owner resolution already enforced at the willAppear site. |
| T-33-02          | Tampering              | sendEvent event-name surface     | accept      | sendEvent intentionally forwards any event string (no allowlist); the new names are host-authored constants, not attacker-controlled. Do NOT add an allowlist (research anti-pattern).                 |
| T-33-SC          | Tampering              | npm/pip/cargo installs           | n/a         | Zero external installs this phase (research: no Package Legitimacy Audit needed).                                                                                                                      |
| \</threat_model> |                        |                                  |             |                                                                                                                                                                                                        |

<verification>
- `ctest --preset linux-release -R "plugin_device|plugin-device-bridge|pi-bridge|plugin-server"` green.
- Full `ctest --preset linux-release` green (no regression to the ~744 baseline).
- grep proves the 3 title sends + the appear/disappear emits + Application wiring exist.
</verification>

\<success_criteria>

- titleParametersDidChange emitted after all 3 willAppear sites; Catch2 test locks completeness + ordering (ROADMAP criterion 4).
- propertyInspectorDidAppear/DidDisappear wired through the Application seam (enables ROADMAP criterion 3 once the QML affordance lands in plan 02).
- No regression: full suite green.
  \</success_criteria>

<output>
Create `.planning/phases/33-property-inspector-end-to-end/33-01-SUMMARY.md` when done.
Flag in the SUMMARY: titleParameters default VALUES are [ASSUMED] (research A1/A2) — confirm at the
end-of-phase human-verify if a plugin actually reads them.
</output>
