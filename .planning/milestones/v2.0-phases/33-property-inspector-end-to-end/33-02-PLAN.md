---
phase: 33-property-inspector-end-to-end
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/app/qml/Inspector.qml
  - src/app/qml/PIWebView.qml
autonomous: true
requirements: [PI-01, PI-02, PI-03]

must_haves:
  truths:
    - The PI panel open affordance is reachable from the debug channel via qml.invoke (objectName openPiButton or a controller Q_INVOKABLE)
    - The PI panel close affordance is reachable via qml.invoke (objectName closePiButton)
    - The PI Loader and the PIWebView container carry objectNames (piPanelLoader, piWebView) so qml.get / screenshot can target them
    - Driving openPiButton renders a real plugin's PI HTML (screenshot not blank) and produces propertyInspectorDidAppear in plugin.protocolLog; closePiButton produces propertyInspectorDidDisappear
    - PI-01 (cefQuery@DocumentCreation, sdpi.css, per-plugin profile) and PI-03 (per-context+global round-trip, didReceiveSettings) are confirmed GREEN by their existing unit tests + the live debug-channel pass — NOT rebuilt
  artifacts:
    - path: src/app/qml/Inspector.qml
      provides: openPiButton + closePiButton affordances (Button or Q_INVOKABLE-backed) + piPanelLoader objectName
      contains: 'objectName: "piPanelLoader"'
    - path: src/app/qml/PIWebView.qml
      provides: piWebView objectName on the root Item / WebEngineView
      contains: 'objectName: "piWebView"'
  key_links:
    - from: openPiButton (Inspector.qml)
      to: PropertyInspectorController.loadInspector(...)
      via: onClicked / Q_INVOKABLE for the current selection
      pattern: loadInspector
    - from: closePiButton (Inspector.qml)
      to: PropertyInspectorController.closeInspector()
      via: onClicked
      pattern: closeInspector
---

<objective>
Close the PI-02 thin-UI debug contract and run the verify-only confirmation for PI-01 + PI-03.

PI-02 (PARTIAL): `loadInspector` is correctly wired (Inspector.qml:112, auto-triggered on
selection) but NONE of the PI QML files carry an `objectName`, so the debug channel (`findByName`)
cannot drive open/close or screenshot the panel. Add:

- `openPiButton` + `closePiButton` affordances (a `Button` whose `onClicked` calls
  loadInspector/closeInspector for the current selection — NOT a Switch, per the CLAUDE.md harness
  gap where qml.invoke cannot reproduce Switch.toggled; a controller `Q_INVOKABLE` is an acceptable
  fallback).
- `piPanelLoader` on the PI Loader (Inspector.qml:206) and `piWebView` on the PIWebView root.

PI-01 + PI-03 are GREEN (research): VERIFY-ONLY here. Do NOT touch pi_cef_shim, pi_bridge,
pi_url_policy, pi_url_request_interceptor, plugin_settings_store, plugin_mirabox_shim, or the relay
wiring — they are green + unit-tested and re-touching them risks the ~744 green baseline.

Purpose: makes criteria 1 & 3 headlessly verifiable and confirms the GREEN machinery end-to-end.
Output: 4 objectNames + 2 affordances + a documented live debug-channel pass for criteria 1/2/3.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-property-inspector-end-to-end/33-RESEARCH.md
@.planning/phases/33-property-inspector-end-to-end/33-CONTEXT.md

@src/app/qml/Inspector.qml
@src/app/qml/PIWebView.qml

<interfaces>
<!-- Exact QML seams. Extracted live this session. Do NOT re-explore the QML tree. -->

Inspector.qml:
root has: hasSelection, binding, keyIndex, encoderIndex, deviceCodename, selectionLabel.
function maybeLoadInspector() (:83-113) — resolves PI path + pluginUuid from
PluginCatalog.actionInfo(actionId), builds ctx via \_contextUuid(), then calls
PropertyInspectorController.loadInspector(pluginUuid, piAbs, actionId, ctx). REUSE this — the
openPiButton onClicked should call maybeLoadInspector() (it already encapsulates the resolve+load).
function \_contextUuid() (:121-139) — builds device#root#Keypad#row#col or device#root#Encoder#0#col.
Loader { id: htmlPiLoader; ... source: "PIWebView.qml" } at :206 — ADD objectName here.

PIWebView.qml:
root: Item { id: root } at :59 — ADD objectName "piWebView".
WebEngineView { id: webView } at :62 binds profile/webChannel/url from PropertyInspectorController.

PropertyInspectorController (singleton, available in QML as PropertyInspectorController):
Q_INVOKABLE loadInspector(pluginUuid, htmlAbsPath, actionUuid, contextUuid)
Q_INVOKABLE closeInspector()
Q_PROPERTY bool hasHtmlInspector, bool webEngineAvailable

Debug channel addressing: scripts/ajazz-debug qml.tree / qml.get / qml.invoke / qml.click /
screenshot address by objectName via findByName (CLAUDE.md). qml.invoke on a Button fires
clicked()/onClicked; it does NOT fire Switch.toggled — so use a Button or a Q_INVOKABLE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add objectNames + open/close affordances to the PI panel</name>
  <files>src/app/qml/Inspector.qml, src/app/qml/PIWebView.qml</files>
  <action>
    Inspector.qml:
    - Add `objectName: "piPanelLoader"` to the htmlPiLoader Loader (:206).
    - Add an `openPiButton` affordance: a `Button` with `objectName: "openPiButton"` whose
      `onClicked` calls `maybeLoadInspector()` (which already resolves the PI path + pluginUuid +
      context for the current selection and calls loadInspector). Place it in the Inspector layout
      where it is visible when a selection exists (e.g. near PageHeader, `visible: root.hasSelection`).
      Do NOT use a Switch/CheckBox (CLAUDE.md harness gap: qml.invoke cannot reproduce Switch.toggled).
    - Add a `closePiButton` affordance: a `Button` with `objectName: "closePiButton"`, `onClicked:
      PropertyInspectorController.closeInspector()`, visible when hasHtmlInspector is true.
    - Both buttons MUST carry objectName so scripts/ajazz-debug qml.invoke can drive them. Keep them
      small/unobtrusive — the panel still auto-opens on selection; these are the explicit
      debug-drivable affordances the criteria require.

```
PIWebView.qml:
- Add `objectName: "piWebView"` to the root Item (:59) so screenshot + qml.get can target the
  rendered container.

Do NOT modify any C++ PI file, the URL policy, the bridge, the store, or the shims (verify-only
territory — see objective). This task is QML-only.
```

</action>
  <read_first>
    src/app/qml/Inspector.qml:80-222 (maybeLoadInspector, _contextUuid, the layout + Loader)
    src/app/qml/PIWebView.qml:55-94 (root Item + WebEngineView)
  </read_first>
  <verify>
    <automated>cmake --build build/linux-release --target ajazz-control-center 2>&1 | tail -15 && grep -c 'objectName: "openPiButton"\|objectName: "closePiButton"\|objectName: "piPanelLoader"' src/app/qml/Inspector.qml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c objectName src/app/qml/Inspector.qml` returns >= 3 (openPiButton, closePiButton, piPanelLoader).
    - `grep -c 'objectName: "piWebView"' src/app/qml/PIWebView.qml` returns 1.
    - openPiButton onClicked calls maybeLoadInspector() (or loadInspector for the current selection); closePiButton onClicked calls PropertyInspectorController.closeInspector().
    - Neither affordance is a Switch/CheckBox.
    - App target builds clean.
  </acceptance_criteria>
  <done>The PI Loader, PIWebView container, and open/close affordances all carry objectNames; openPiButton drives loadInspector for the current selection; closePiButton drives closeInspector; QML-only, app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Live debug-channel pass — criteria 1/2/3 + PI-01/PI-03 verify (Claude drives headlessly, reads screenshot)</name>
  <files>(no source files — verification pass; record results in the SUMMARY)</files>
  <action>
    Per CLAUDE.md the live debug-channel pass is MANDATORY and part of DONE (ctest is necessary but
    NOT sufficient). Claude drives this autonomously through scripts/ajazz-debug and READS the
    screenshot output (this is the standard debug-channel verification Claude performs itself; the
    only human-gated step is criterion 5, deferred to plan 03).

```
1. Full suite: `ctest --preset linux-release` — must be green (confirms PI-01
   test_pi_bridge.cpp:419,682 and PI-03 :224/:252/:279 still pass; the title test from plan 01
   passes).
2. Build + launch: `cmake --build build/linux-release --target ajazz-control-center`, then launch
   with `AJAZZ_DEBUG_CONTROL=1` and an isolated XDG_RUNTIME_DIR. Kill any stale instance + rm the
   socket first — single-instance is keyed on app-ID (memory headless_live_drive_recipe). Launch
   via plain `nohup … &`; kill by exact PID (never `pkill -f` your own command line).
3. Install a real .sdPlugin with a settings PI (System Monitor recipe in memory
   project_plugin_install_demo_working, or `plugin.installFromFile`) and bind its action to a key.
4. Criterion 1: `scripts/ajazz-debug qml.invoke openPiButton` -> `qml.get piPanelLoader` shows
   active/visible -> `scripts/ajazz-debug screenshot` and READ it: the PI HTML is rendered, NOT a
   blank frame. (`qml.get piWebView` confirms the container exists.)
5. Criterion 3: with the PI open, `plugin.protocolLog` shows `propertyInspectorDidAppear`; then
   `scripts/ajazz-debug qml.invoke closePiButton` -> `plugin.protocolLog` shows
   `propertyInspectorDidDisappear`.
6. Criterion 2 (PI-03 headless half): `scripts/ajazz-debug plugin.simulatePiSettings {...}` ->
   `plugin.protocolLog` shows OUT `didReceiveSettings`.
7. Criterion 4: note the titleParametersDidChange Catch2 test (plan 01) passed in step 1.

Record the protocolLog excerpts + the screenshot-not-blank confirmation in the SUMMARY. If the
screenshot is blank or any protocolLog event is missing, capture the failure for a gap-closure
plan rather than marking DONE.
```

</action>
  <read_first>
    .planning/phases/33-property-inspector-end-to-end/33-RESEARCH.md (Validation Architecture + PI-04 gap)
    CLAUDE.md "Debug-channel verification — MANDATORY" + memory headless_live_drive_recipe
  </read_first>
  <verify>
    <automated>ctest --preset linux-release 2>&1 | tail -8</automated>
    <human-check>Claude inspects the screenshot (PI HTML rendered, not blank) and the protocolLog excerpts (didAppear/didDisappear/didReceiveSettings present) and records them in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - Full `ctest --preset linux-release` green (no regression; PI-01 + PI-03 unit tests pass).
    - `scripts/ajazz-debug screenshot` after `qml.invoke openPiButton` shows real PI HTML (not a blank frame) — recorded in SUMMARY.
    - `plugin.protocolLog` shows `propertyInspectorDidAppear` after open and `propertyInspectorDidDisappear` after `qml.invoke closePiButton`.
    - `plugin.simulatePiSettings` produces OUT `didReceiveSettings` in `plugin.protocolLog`.
  </acceptance_criteria>
  <done>Criteria 1/2/3 confirmed live via the debug channel (screenshot not blank, all three protocolLog events present); PI-01 + PI-03 confirmed GREEN by their unit tests; results recorded in the SUMMARY.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                             |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| QML affordance -> C++ invokable | The new buttons call existing, already-hardened invokables (loadInspector/closeInspector). No new untrusted input path. |

## STRIDE Threat Register

| Threat ID        | Category           | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                |
| ---------------- | ------------------ | ------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-33-03          | Spoofing/Tampering | new openPiButton/closePiButton affordances | accept      | They invoke existing hardened invokables with the current selection's resolved (pluginUuid, piAbs, ctx); no new parameter is attacker-controlled. The URL interceptor + nav guard (PI-01, GREEN) remain the security boundary — do NOT weaken. |
| T-33-04          | Info-disclosure    | screenshot of PI HTML                      | accept      | Local debug channel only (AJAZZ_DEBUG_CONTROL opt-in, Unix socket under XDG_RUNTIME_DIR); not exposed in normal runs.                                                                                                                          |
| \</threat_model> |                    |                                            |             |                                                                                                                                                                                                                                                |

<verification>
- App target builds clean with the new objectNames + affordances.
- Full `ctest --preset linux-release` green (PI-01 + PI-03 unit tests confirm GREEN; no regression).
- Live debug-channel pass: screenshot not blank; protocolLog shows didAppear/didDisappear/didReceiveSettings.
</verification>

\<success_criteria>

- ROADMAP criterion 1: PI HTML renders (screenshot not blank); openPiButton reachable via qml.invoke.
- ROADMAP criterion 2: simulatePiSettings -> didReceiveSettings in protocolLog.
- ROADMAP criterion 3: didAppear on open / didDisappear on close via the objectName-addressed controls.
- PI-01 + PI-03 confirmed GREEN (no rebuild).
  \</success_criteria>

<output>
Create `.planning/phases/33-property-inspector-end-to-end/33-02-SUMMARY.md` when done.
Record the protocolLog excerpts + the screenshot-not-blank confirmation. Note that ROADMAP
criterion 5 (real PI JS round-trip + restart) is still pending and handled by plan 03.
</output>
