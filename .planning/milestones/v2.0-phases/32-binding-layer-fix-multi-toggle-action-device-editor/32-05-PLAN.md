---
phase: 32-binding-layer-fix-multi-toggle-action-device-editor
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/qml/DeviceView.qml
  - tests/qml/test_device_view_geometry.qml
autonomous: false
requirements: [EDIT-01]
must_haves:
  truths:
    - The device grid (DeviceCanvas) is wrapped in a vertical ScrollView addressable as deviceCanvasScroll
    - An over-threshold SKU grid (>8 cols OR >4 rows incl. encoder+touch rows) becomes vertically scrollable
    - An at/under-threshold grid is not scrollable and stays centered
    - A horizontal scrollbar is never produced; scroll position is machine-readable via qml.get
    - trashBtn stays pinned top-right (not inside the scroll) and key/encoder/zone delegates stay distinct
  artifacts:
    - path: src/app/qml/DeviceView.qml
      provides: ScrollView#deviceCanvasScroll wrapping DeviceCanvas inside chassisArea
      contains: deviceCanvasScroll
    - path: tests/qml/test_device_view_geometry.qml
      provides: over/under-threshold + scrollbar.size + horizontal-off + trash-pinned + delegate-distinct assertions
  key_links:
    - from: src/app/qml/DeviceView.qml
      to: DeviceCanvas#deviceCanvas
      via: ScrollView content child, width-constrained to availableWidth
      pattern: deviceCanvasScroll
    - from: scripts/ajazz-debug qml.get
      to: deviceCanvasScroll / deviceCanvasFlick
      via: objectName exposes ScrollBar.vertical.size + contentY
      pattern: objectName
---

<objective>
EDIT-01: wrap the device grid in a vertical `ScrollView#deviceCanvasScroll` inside `chassisArea`
so oversized SKU grids scroll. This is the only frontend surface in the phase and is independent
of the dispatch work (no shared files) — it can plan/build in parallel with Wave 1 core/test plans.
Strictly per the design contract in 32-UI-SPEC.md: container-level change only, no delegate
redesign, no new theming/colors/copy.

Purpose: large SKU grids currently clip/overflow because nothing scrolls (32-UI-SPEC). Wrapping
the canvas in a width-constrained vertical ScrollView fixes overflow and makes scroll position
debug-addressable (success criterion 5; CLAUDE.md every-control-debug-addressable rule).
Output: the ScrollView wrapper + extended QML geometry smoke test.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-UI-SPEC.md

# Anchors (read these regions only):

# chassisArea structure — src/app/qml/DeviceView.qml:302-345 (chassisPhoto, deviceCanvas, trashBtn, EmptyState)

# geometry helpers — DeviceView.qml:52-78 (encoderCount, touchZoneCount, \_keyColumnsResolved, keyRowsResolved)

# QtQuick.Controls import— DeviceView.qml:40 (ScrollView/ScrollBar already available — no new import)

# delegate index trap — DeviceCanvas.qml:108-109 (do NOT redeclare index on KeyCell/dial delegates)

@src/app/qml/DeviceView.qml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap DeviceCanvas in ScrollView#deviceCanvasScroll (vertical-only, debug-addressable)</name>
  <read_first>
    src/app/qml/DeviceView.qml:302-345 (chassisArea + the DeviceCanvas#deviceCanvas anchors.fill block
    to relocate); DeviceView.qml:52-78 (geometry properties for the _gridOverflows helper);
    32-UI-SPEC.md "Frontend Change Contract" + "Content sizing" + "Debug-Addressability Contract".
  </read_first>
  <action>
    Insert a `ScrollView { id: deviceCanvasScroll; objectName: "deviceCanvasScroll" }` as a NEW sibling of
    chassisPhoto/trashBtn/EmptyState inside chassisArea: anchors.fill parent, anchors.margins
    Theme.spacingMd (taking over the canvas's former margins), `clip: true`,
    `ScrollBar.horizontal.policy: ScrollBar.AlwaysOff`, `ScrollBar.vertical.policy: ScrollBar.AsNeeded`.
    MOVE `DeviceCanvas#deviceCanvas` to be the ScrollView's content child; replace its `anchors.fill`/margins
    with `width: deviceCanvasScroll.availableWidth` (prevents the phantom horizontal scrollbar, Pitfall 5)
    and let height be natural content height; PRESERVE its existing `visible: !chassisPhoto.visible` binding
    and all its existing property bindings (keyRows/keyColumns/keyCount/etc.). Keep trashBtn and EmptyState
    as chassisArea siblings (NOT inside the ScrollView) so the trash zone stays pinned. Give the ScrollView's
    contentItem (the Flickable) `objectName: "deviceCanvasFlick"` so `qml.get deviceCanvasFlick` exposes
    contentY + contentHeight (the SPEC's machine-readable scroll surface). Add a readonly helper on
    DeviceView: `_gridOverflows = (_keyColumnsResolved > 8) || ((keyRowsResolved + (encoderCount>0?1:0) +
    (touchZoneCount>0?1:0)) > 4)` for tests/contract. Do NOT touch any delegate (no redeclaring `index`).
    No new import, no new Theme tokens/colors/copy.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R device_view --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'deviceCanvasScroll' src/app/qml/DeviceView.qml` shows the ScrollView id + objectName.
    - `grep -n 'deviceCanvasFlick' src/app/qml/DeviceView.qml` shows the Flickable objectName.
    - `grep -n 'ScrollBar.horizontal.policy: ScrollBar.AlwaysOff' src/app/qml/DeviceView.qml` matches.
    - `grep -n 'availableWidth' src/app/qml/DeviceView.qml` shows the width constraint on the canvas.
    - `ctest --preset linux-release -R device_view --output-on-failure` exits 0 (Linux; per CLAUDE.md the qml target is built-but-not-run on Windows).
  </acceptance_criteria>
  <done>DeviceCanvas is inside a vertical-only, debug-addressable ScrollView; trash pinned; delegates untouched; no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the QML geometry smoke test for over/under-threshold + scroll readability</name>
  <read_first>
    tests/qml/test_device_view_geometry.qml (full — existing geometry harness to extend);
    tests/qml/CMakeLists.txt (note the `if(NOT WIN32)` guard around catch_discover_tests — Windows hang,
    CLAUDE.md); 32-UI-SPEC.md "Acceptance Criteria" 1-7.
  </read_first>
  <action>
    Extend test_device_view_geometry.qml with cases: (1) drive an OVER-threshold geometry
    (e.g. keyColumns>8 or rows+encoder+touch>4) and assert `_gridOverflows === true` AND the
    ScrollView's vertical `ScrollBar.size < 1.0` (content exceeds viewport => scrollable);
    (2) drive an UNDER-threshold geometry (e.g. 3x3) and assert `_gridOverflows === false` AND
    vertical ScrollBar.size >= 1.0 (not scrollable, frame centered); (3) assert the ScrollView's
    horizontal policy is AlwaysOff (no horizontal scroll ever); (4) assert trashBtn remains a child of
    chassisArea (NOT of deviceCanvasScroll) so it stays pinned; (5) assert the three delegate types remain
    distinct (KeyCell present, dial Row present, TouchStripLane present) when the SKU has all three.
    ASCII-only test titles. Keep the Linux/macOS-only registration (do not remove the `if(NOT WIN32)` guard).
  </action>
  <verify>
    <automated>ctest --preset linux-release -R device_view --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R device_view --output-on-failure` exits 0 with the new geometry cases.
    - `grep -c '_gridOverflows' tests/qml/test_device_view_geometry.qml` >= 2 (over + under threshold asserted).
    - `grep -n 'AlwaysOff\|horizontal' tests/qml/test_device_view_geometry.qml` shows the no-horizontal assertion.
    - `grep -n 'trashBtn' tests/qml/test_device_view_geometry.qml` shows the trash-pinned (parent==chassisArea) assertion.
  </acceptance_criteria>
  <done>The QML smoke gate proves over/under-threshold scrollability, no-horizontal, trash-pinned, and delegate-distinctness.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A vertical ScrollView wrapping the device grid, addressable as deviceCanvasScroll/deviceCanvasFlick.
    MANDATORY live debug-channel verification of the 32-UI-SPEC acceptance criteria (ctest is necessary
    but NOT sufficient — CLAUDE.md).
  </what-built>
  <how-to-verify>
    1. Build, then launch headless: `AJAZZ_DEBUG_CONTROL=1 nohup build/linux-release/src/app/ajazz-control-center &`.
    2. Addressable: `scripts/ajazz-debug qml.get deviceCanvasScroll` returns an object including
       ScrollBar.vertical.size / .position and ScrollBar.horizontal.policy (or
       `qml.get deviceCanvasFlick` returns contentY + contentHeight).
    3. Over-threshold: drive `device.setActiveDevice` (or `device.renderTest`) to a SKU exceeding
       >8 cols OR >4 rows; `qml.get deviceCanvasScroll` reports vertical ScrollBar.size < 1.0; `screenshot`
       shows a vertical scrollbar and NO horizontal scrollbar.
    4. Under-threshold: switch to a 3x3-class device; ScrollBar.size >= 1.0, `screenshot` shows no vertical
       scrollbar and the frame centered.
    5. Trash pinned: after scrolling (set contentY via `qml.set deviceCanvasFlick`), `screenshot` shows
       trashBtn still anchored top-right.
    6. Delegates distinct: on an all-three-controller SKU, `screenshot` shows square key tiles, a horizontal
       touch strip, and circular dials as three lanes.
  </how-to-verify>
  <resume-signal>Type "approved" once qml.get returns scroll-position props, over/under threshold behaves, no horizontal scrollbar, trash stays pinned, and delegates look distinct — or describe the failure.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                          | Description                                              |
| --------------------------------- | -------------------------------------------------------- |
| descriptor geometry -> QML layout | device-supplied row/column counts drive container sizing |

## STRIDE Threat Register

| Threat ID        | Category          | Component                                        | Disposition | Mitigation Plan                                                                                                                               |
| ---------------- | ----------------- | ------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-32-04          | Denial of Service | extreme grid geometry overflowing the ScrollView | accept      | ScrollView clips (clip:true) and scrolls; vertical-only sizing bounds the content width to availableWidth; no unbounded allocation introduced |
| T-32-SC          | Tampering         | npm/pip/cargo installs                           | mitigate    | none — zero new dependencies (no new QML import; ScrollView already imported)                                                                 |
| \</threat_model> |                   |                                                  |             |                                                                                                                                               |

<verification>
- `ctest --preset linux-release -R device_view` green (Linux/macOS).
- Live: `qml.get deviceCanvasScroll`/`deviceCanvasFlick` return scroll-position props; over/under-threshold
  + no-horizontal + trash-pinned + delegate-distinct confirmed via screenshot.
</verification>

\<success_criteria>

- Over-threshold SKU scrolls vertically; under-threshold does not; no horizontal scroll ever.
- Scroll container is debug-addressable (qml.get returns scroll-position properties).
- Trash pinned; key/encoder/touch delegates remain visually + functionally distinct.
  \</success_criteria>

<output>
Create `.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-05-SUMMARY.md` when done.
</output>
