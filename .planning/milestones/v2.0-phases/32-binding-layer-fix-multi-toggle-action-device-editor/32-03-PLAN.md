---
phase: 32-binding-layer-fix-multi-toggle-action-device-editor
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - src/app/src/profile_controller.cpp
  - src/app/src/profile_controller.hpp
  - src/app/src/stream_dock_input_service.hpp
  - src/app/src/stream_dock_input_service.cpp
  - src/app/src/plugin_device_bridge.cpp
  - src/app/src/application.cpp
  - tests/unit/test_toggle_dispatch.cpp
  - tests/unit/CMakeLists.txt
autonomous: false
requirements: [BIND-05, BIND-07]
must_haves:
  truths:
    - Pressing a Toggle Action binding cycles currentState = (currentState + 1) mod N over ALL states (N>2 supported)
    - Each cycle renders instance.states[currentState] (imagePath/title) on the key via the existing repaint path
    - Each cycle emits a state-change willAppear with the new state index
    - The mutated currentState persists to the profile JSON (survives restart)
  artifacts:
    - path: src/app/src/profile_controller.cpp
      provides: a mutator that cycles a binding's instance.currentState, persists, and emits profileChanged
      contains: currentState
    - path: src/app/src/stream_dock_input_service.cpp
      provides: Toggle classification + cycle invocation at the dispatch seam
      contains: currentState
  key_links:
    - from: src/app/src/stream_dock_input_service.cpp
      to: ProfileController cycle mutator
      via: on press of a toggle binding, invoke the mutator + drive the per-state render
      pattern: currentState
    - from: src/app/src/plugin_device_bridge.cpp
      to: assignKeyImage (setState repaint path)
      via: render states[currentState].visual + emit state-change willAppear
      pattern: assignKeyImage
---

<objective>
BIND-07 (+ BIND-05 Toggle semantics): pressing a Toggle Action binding cycles `currentState`
through ALL `states[]` (mod N, N>2 supported), renders `states[currentState]` on the key, emits a
state-change `willAppear`, and PERSISTS the new currentState to the profile JSON (Q1 resolved:
persist, per CONTEXT user decision). Reuses the existing setState repaint path; does not add a new
render mechanism.

Purpose: the toggle mutation needs binding identity + a mutable Profile, which the opaque
`BuiltinHandler(string_view)` cannot provide. The seam is `StreamDockInputService::dispatch` (firing
binding + key index in scope) driving a `ProfileController` cycle mutator (owns the mutable Profile,
emits profileChanged + saves) and the bridge's per-state render (RESEARCH Finding 3, Q1/Q4).
Output: a ProfileController cycle mutator + the toggle seam + per-state render + state-change
willAppear + tests, with the Pitfall-3 connection-ordering invariant respected.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-RESEARCH.md
@.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-02-SUMMARY.md

<interfaces>
From src/core/include/ajazz/core/action_instance.hpp:
  struct ActionInstance { ...; std::uint32_t currentState{0}; std::vector<ActionState> states{}; ... };
  // currentState already round-trips through profileToJson/profileFromJson (test_action_instance.cpp).
From src/core/include/ajazz/core/builtin_action_registry.hpp:
  static constexpr std::string_view kBuiltinPrefix = "com.hotspot.streamdock.";
  // => toggle built-in id MUST be com.hotspot.streamdock.toggleaction (NOT opendeck.*, Q2).
</interfaces>

# Anchors:

# dispatch seam (KeyPressed) — src/app/src/stream_dock_input_service.cpp:204-208 (firing Binding + index)

# ProfileController mutator idiom + save — src/app/src/profile_controller.cpp:417-500 (commitKeyBinding pattern:

# validate -> mutate -> persist -> emit profileChanged)

# activeProfile() const accessor — src/app/src/profile_controller.cpp:364 (needs a non-const mutate path)

# setState repaint path to reuse — src/app/src/plugin_device_bridge.cpp:625-655 (assignKeyImage + reapplyTitle)

# willAppear emission + context state — src/app/src/plugin_device_bridge.cpp:310-342, :1073-1135 (m_registry.setState :162)

# connection ORDERING invariant — src/app/src/application.cpp:456-487 (repaint BEFORE populateContexts; Pitfall 3)

# cross-plugin ownership guard — src/app/src/plugin_device_bridge.cpp:573-575 (keep on any toggle-driven setState)

@src/app/src/stream_dock_input_service.cpp
@src/app/src/plugin_device_bridge.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileController cycle mutator (mutate currentState, persist, emit profileChanged)</name>
  <read_first>
    src/app/src/profile_controller.hpp (class decl — add the mutator + any needed Q_INVOKABLE);
    src/app/src/profile_controller.cpp:364 (activeProfile const accessor) + :417-500 (commitKeyBinding
    mutate->persist->emit idiom to mirror); src/core/include/ajazz/core/profile.hpp:95-120 (Binding/
    EncoderBinding .instance).
  </read_first>
  <behavior>
    - Test: cycleInstanceState on a key with a 3-state instance advances currentState 0->1->2->0 (mod N, N>2).
    - Test: a binding with no instance, or a single-state instance, is a safe no-op (currentState stays 0).
    - Test: after cycling, profileToJson reflects the new currentState (persistence path exercised).
  </behavior>
  <action>
    Add a ProfileController mutator, e.g. `cycleInstanceState(QString controller, int index)` (or
    key/encoder variants), that locates the binding's `instance`, sets
    `currentState = (currentState + 1) % states.size()` (guard states.size() <= 1 as a no-op), persists
    via the SAME save path commitKeyBinding uses (so it survives restart — Q1: persist), and emits
    profileChanged(). Provide a non-const access path to the active Profile internal to ProfileController
    (do NOT widen the public const activeProfile() contract — mutate the owned member directly, like the
    existing commit* mutators do). Add `tests/unit/test_toggle_dispatch.cpp` with a [toggle] suite covering
    the three behaviors (drive ProfileController directly or its core helper). Register the new test in
    tests/unit/CMakeLists.txt. ASCII-only TEST_CASE names.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R "toggle|action_instance" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R "toggle|action_instance" --output-on-failure` exits 0; [toggle] suite runs.
    - `grep -n 'currentState' src/app/src/profile_controller.cpp` shows the cycle mutation `% states.size()`.
    - A test asserts the 0->1->2->0 wrap (N=3) AND that profileToJson reflects the persisted currentState.
    - The single-state / no-instance no-op case passes.
  </acceptance_criteria>
  <done>cycleInstanceState advances currentState mod N, persists, emits profileChanged; safe no-op for <2 states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Toggle seam at dispatch + per-state render + state-change willAppear</name>
  <read_first>
    src/app/src/stream_dock_input_service.cpp:204-230 (KeyPressed/EncoderPressed cases — firing binding + index);
    src/app/src/plugin_device_bridge.cpp:625-655 (setState repaint: assignKeyImage + reapplyTitle) and
    :310-342 + :1073-1135 (eventEnvelope/instancePayload + m_registry.setState at :162);
    src/app/src/application.cpp:456-487 (Pitfall-3 connection ordering — repaint registered BEFORE
    populateContexts); src/core/include/ajazz/core/builtin_action_registry.hpp:53-64 (toggle id prefix).
  </read_first>
  <behavior>
    - Test: dispatch(KeyPressed) on a toggle binding invokes the cycle mutator once per press (assert via
      a recording mutator/seam) and the resulting currentState advances.
    - Test: the render hook is called with the new states[currentState] visual (imagePath/title) — assert
      via a recording render seam (no real device needed).
    - Test: a state-change willAppear is emitted carrying the new state index for that context.
  </behavior>
  <action>
    Register a classification entry `com.hotspot.streamdock.toggleaction` (builtin_actions_service, same
    prefix rule as Multi — Q2), used ONLY to classify "is this a toggle". At the dispatch seam
    (StreamDockInputService::dispatch, KeyPressed/EncoderPressed): if the firing binding's instance is a
    toggle (id == toggle id, OR states.size() > 1 — executor's discretion, prefer id-match), then
    (a) call the ProfileController cycle mutator (Task 1); (b) drive the per-state render of
    `states[currentState].visual` (imagePath/title) by REUSING the existing setState repaint path in
    PluginDeviceBridge (assignKeyImage + reapplyTitle, :633-648) sourced from the instance state rather
    than the plugin manifest — add a thin bridge method or render-hook injected into the input service
    (executor's discretion); (c) emit the state-change willAppear via the bridge
    (m_registry.setState(:162) then re-send willAppear for that one context, :1073-1135). Drive the render
    DIRECTLY from the toggle path (do NOT add a new profileChanged slot — Pitfall 3: a new profileChanged
    connection would have to be ordered after repaintFromProfile/repaintEncodersFromProfile). Keep the
    cross-plugin ownership guard (plugin_device_bridge.cpp:573-575) on any toggle-driven setState. Wire the
    render-hook injection in application.cpp without disturbing the existing :456-487 ordering. Do NOT branch
    on SKU/codename (BIND-06). Extend test_toggle_dispatch.cpp with the three behaviors using recording
    seams. ASCII-only TEST_CASE names.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R "toggle|plugin_device|action_instance" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R "toggle|plugin_device|action_instance" --output-on-failure` exits 0.
    - `grep -n 'com.hotspot.streamdock.toggleaction' src/app/src/builtin_actions_service.cpp` matches (correct prefix; NOT opendeck.*).
    - The dispatch toggle block contains no SKU literal: `! grep -qiE 'akp03|akp05|akp153' <the new toggle block>` (verify by reading the added code).
    - A test asserts state-change willAppear carries the incremented state index; another asserts the render hook receives states[currentState].
    - Pitfall 3 preserved: `git diff src/app/src/application.cpp` shows repaint connections still registered before populateContextsForActivePage (no reorder).
  </acceptance_criteria>
  <done>A toggle press cycles currentState, renders the new state via the existing repaint path, emits a state-change willAppear; ordering invariant intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Toggle Action dispatch: repeated presses cycle through all states, re-rendering the key face each
    time and persisting position. MANDATORY live debug-channel verification — ctest is necessary but NOT
    sufficient; the render path needs visual confirmation (CLAUDE.md, Phase-27 lesson).
  </what-built>
  <how-to-verify>
    1. Build, launch headless: `AJAZZ_DEBUG_CONTROL=1 AJAZZ_ALLOW_UNTRUSTED_PLUGINS=1 nohup build/linux-release/src/app/ajazz-control-center &`.
    2. Bind a 3-state Toggle Action to a key (3 distinct state images).
    3. Press three times: `scripts/ajazz-debug input.key {index,pressed:true}` (+release) x3.
    4. After EACH press: `scripts/ajazz-debug screenshot` and READ it — confirm the key face changes
       through state 0 -> 1 -> 2 and WRAPS back to 0 on the 4th press.
    5. If a plugin owns the action: `plugin.protocolLog` shows a willAppear with incrementing payload.state.
    6. Persistence: relaunch the app and confirm the key renders the last currentState (Q1 persist).
    Use synthetic input.key (demo unit delivers zero real input — Pitfall 2).
  </how-to-verify>
  <resume-signal>Type "approved" once the key face cycles through all 3 states + wraps, and the state persists across restart — or describe the failure.</resume-signal>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                        |
| ----------------------------------------- | ------------------------------------------------------------------ |
| toggle press -> profile mutation + render | input mutates persisted profile state and drives a device render   |
| built-in toggle -> plugin-owned context   | a built-in toggle must not drive a context owned by another plugin |

## STRIDE Threat Register

| Threat ID        | Category          | Component                                         | Disposition | Mitigation Plan                                                                                                                         |
| ---------------- | ----------------- | ------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-32-07          | Tampering         | cross-plugin context drive via toggle             | mitigate    | keep the ctx.pluginUuid == pluginUuid ownership check (plugin_device_bridge.cpp:573-575) on any willAppear/setState the toggle triggers |
| T-32-08          | Input Validation  | currentState out of range after mutation          | mitigate    | mutate via (currentState+1) % states.size() with a states.size()\<=1 no-op guard; model also clamps on read (action_instance.hpp:88)    |
| T-32-09          | Denial of Service | toggle render reorders profileChanged connections | mitigate    | drive render directly from the toggle path; do NOT add a new profileChanged slot (Pitfall 3); preserve application.cpp:456-487 order    |
| T-32-SC          | Tampering         | npm/pip/cargo installs                            | mitigate    | none — zero new dependencies this plan                                                                                                  |
| \</threat_model> |                   |                                                   |             |                                                                                                                                         |

<verification>
- `ctest --preset linux-release -R "toggle|plugin_device|action_instance"` green.
- Live: 3-state toggle cycles + wraps (screenshot per press); state persists across restart.
- After this plan: full suite `ctest --preset linux-release` green (phase-gate prerequisite).
</verification>

\<success_criteria>

- A toggle press cycles currentState mod N (N>2), renders states[currentState], emits state-change willAppear.
- currentState persists to the profile JSON (survives restart).
- Pitfall-3 connection ordering preserved; no SKU branching; cross-plugin guard intact.
  \</success_criteria>

<output>
Create `.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-03-SUMMARY.md` when done.
</output>
