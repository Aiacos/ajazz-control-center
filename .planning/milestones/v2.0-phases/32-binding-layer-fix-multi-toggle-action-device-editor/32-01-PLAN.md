---
phase: 32-binding-layer-fix-multi-toggle-action-device-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/include/ajazz/core/action_instance.hpp
  - src/core/src/profile.cpp
  - src/core/include/ajazz/core/action_chain_adapter.hpp
  - src/core/src/action_chain_adapter.cpp
  - src/core/CMakeLists.txt
  - tests/unit/test_action_instance.cpp
  - tests/unit/test_multiaction_dispatch.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [BIND-04, BIND-05]
must_haves:
  truths:
    - ActionInstance carries an optional per-step delayMs that round-trips through profile JSON
    - An ActionInstance with children flattens to an ordered ActionChain preserving child order
    - Each generated chain step carries its source child's delayMs so ActionEngine defers it
    - src/core/include/ has zero nlohmann includes (COD-031 boundary intact)
  artifacts:
    - path: src/core/include/ajazz/core/action_chain_adapter.hpp
      provides: instanceChildrenToChain declaration (pure core, Qt-free, nlohmann-free)
      contains: instanceChildrenToChain
    - path: src/core/src/action_chain_adapter.cpp
      provides: children -> ActionChain flattening with delayMs propagation
    - path: src/core/include/ajazz/core/action_instance.hpp
      provides: ActionInstance.delayMs field
      contains: delayMs
  key_links:
    - from: src/core/src/action_chain_adapter.cpp
      to: ActionEngine::run
      via: produces ActionChain (vector<Action>) the engine already walks
      pattern: ActionChain
    - from: src/core/src/profile.cpp
      to: ActionInstance.delayMs
      via: writeActionInstance / readActionInstance
      pattern: delayMs
---

<objective>
Add the Phase-31-model extension and the pure-core adapter that Multi Action (BIND-04/05)
dispatch depends on: (1) an additive optional `delayMs` on `ActionInstance` (serialized,
reader-tolerant, round-trip tested), and (2) `instanceChildrenToChain` — the
`ActionInstance.children -> core::ActionChain` adapter that does NOT yet exist anywhere
(Phase 31 was model-only). This is the foundation Wave 2's Multi Action handler reuses to
feed the existing `ActionEngine` sequential walk.

Purpose: ActionEngine already honors `Action::delayMs` (action_engine.cpp:114-131); the
missing piece is converting typed `ActionInstance.children` into the `ActionChain` the engine
consumes, carrying per-step delay. Keeping this in core (nlohmann-free, Qt-free) preserves the
COD-031 boundary.
Output: extended ActionInstance model + serializer, a new core adapter TU, and Catch2 tests.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-RESEARCH.md

<interfaces>
<!-- Phase 31 model + Action shape the adapter targets. Use directly; no exploration needed. -->

From src/core/include/ajazz/core/action_instance.hpp (ActionInstance — EXTEND with delayMs):
struct ActionInstance {
std::string id{}; // dotted plugin action id (wire "id"; reader also accepts "uuid")
std::vector<ActionState> states{}; // wire "states"
std::uint32_t currentState{0}; // wire "currentState"
std::string settings{}; // wire "settings" — ESCAPED JSON STRING (not nested object)
std::vector<ActionInstance> children{}; // wire "children" (recursive)
// ADD: std::uint32_t delayMs{0}; // wire "delayMs"; mirrors Action::delayMs
};

From src/core/include/ajazz/core/profile.hpp (the adapter's output element type):
struct Action {
ActionKind kind{ActionKind::Plugin}; // selects interpreter; Plugin routes through executors.plugin
std::string id{}; // plugin id when kind==Plugin
std::string settingsJson{}; // free-form JSON blob
std::string label{};
std::uint32_t delayMs{0}; // post-step delay; ActionEngine defers it
};
using ActionChain = std::vector<Action>; // (from action_engine.hpp:50)
</interfaces>

# Serializer anchors (read these regions, do not re-read the whole file):

# writeActionInstance — src/core/src/profile.cpp:278-325 (emit "delayMs" near "currentState")

# readActionInstance — src/core/src/profile.cpp:837-910 (parse "delayMs"; clamp not needed)

@src/core/include/ajazz/core/action_instance.hpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional delayMs to ActionInstance + serializer round-trip</name>
  <read_first>
    src/core/include/ajazz/core/action_instance.hpp (full — the struct to extend);
    src/core/src/profile.cpp:278-325 (writeActionInstance) and :837-910 (readActionInstance);
    tests/unit/test_action_instance.cpp (existing [action_instance] suite to extend).
  </read_first>
  <behavior>
    - Test: an ActionInstance with delayMs=250 round-trips (write -> read) preserving delayMs.
    - Test: a v1/legacy instance with NO "delayMs" key reads back delayMs=0 (reader-tolerant).
    - Test: delayMs survives nested children (a child with delayMs=100 round-trips inside parent.children).
  </behavior>
  <action>
    Add `std::uint32_t delayMs{0};` to `ajazz::core::ActionInstance` (action_instance.hpp), wire key
    "delayMs", doc-commented mirroring `Action::delayMs`. In writeActionInstance (profile.cpp), emit
    `"delayMs":<n>` (always emit, like currentState — it is a small int; or omit-when-zero for wire
    economy, executor's discretion, but the reader MUST tolerate absence). In readActionInstance, parse
    a "delayMs" key via the JSON reader's uint path (same idiom as the existing "currentState" branch,
    profile.cpp:879-880); default 0 when absent. Do NOT add nlohmann anywhere in src/core/include/.
    Extend the [action_instance] Catch2 suite with the three round-trip cases above. ASCII-only TEST_CASE
    names (use - and ->, never em-dash/right-arrow).
  </action>
  <verify>
    <automated>ctest --preset linux-release -R action_instance --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R action_instance --output-on-failure` exits 0 with the new cases counted.
    - `grep -n 'delayMs' src/core/include/ajazz/core/action_instance.hpp` shows the new field.
    - COD-031 gate holds: `! grep -rq '#include.*nlohmann' src/core/include/` (command exits 0 i.e. no match).
  </acceptance_criteria>
  <done>delayMs round-trips through profile JSON (present, absent->0, and nested); core headers nlohmann-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: instanceChildrenToChain adapter (children -> ActionChain) in core</name>
  <read_first>
    src/core/include/ajazz/core/action_engine.hpp:50 (ActionChain alias) and :153-180 (delay deferral
    contract of run/runFrom); src/core/include/ajazz/core/profile.hpp:72-77 (Action) + :47-55 (ActionKind);
    src/core/src/profile.cpp:678-690 + :883-895 (existing recursion DEPTH GUARD pattern, kMaxDepth=64,
    CR-01 commit 61bb6c6) — the adapter MUST honor an equivalent depth cap.
  </read_first>
  <behavior>
    - Test: a parent with children [A,B,C] (ids set) -> ActionChain of 3 Action steps in order A,B,C.
    - Test: each step copies child.id -> Action.id, child.settings -> Action.settingsJson,
      child.delayMs -> Action.delayMs, Action.kind == ActionKind::Plugin.
    - Test: an empty children vector -> empty chain.
    - Test: nested children flatten depth-first AND a malicious deep nest beyond the depth cap is
      truncated (no unbounded recursion / no crash).
  </behavior>
  <action>
    Create src/core/include/ajazz/core/action_chain_adapter.hpp declaring
    `ajazz::core::ActionChain instanceChildrenToChain(ActionInstance const& parent);` — Qt-free,
    nlohmann-free, standard-library only (COD-031 safe). Implement in
    src/core/src/action_chain_adapter.cpp: iterate `parent.children`, for each child emit an
    `Action{ kind=ActionKind::Plugin, id=child.id, settingsJson=child.settings, delayMs=child.delayMs }`;
    flatten nested children depth-first (a child's own children recurse inline) with a depth cap mirroring
    profile.cpp's DepthGuard (kMaxDepth=64) so a hostile nest cannot overflow the stack. The inter-step
    delay source is the child's own `delayMs` field (Q3 resolved by Task 1 — additive field, NOT a settings
    key and NOT interleaved Sleep steps). Register action_chain_adapter.cpp in src/core/CMakeLists.txt
    alongside the existing core TUs (find the ajazz_core target sources list and add it). Create
    tests/unit/test_multiaction_dispatch.cpp with a [multiaction] suite covering the four behaviors above;
    register it in tests/unit/CMakeLists.txt the same way neighboring test_*.cpp files are added.
    ASCII-only TEST_CASE names.
  </action>
  <verify>
    <automated>ctest --preset linux-release -R multiaction --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset linux-release -R multiaction --output-on-failure` exits 0; the [multiaction] suite runs >=4 cases.
    - `grep -n 'instanceChildrenToChain' src/core/include/ajazz/core/action_chain_adapter.hpp` matches.
    - `! grep -q 'nlohmann\|#include <Q' src/core/src/action_chain_adapter.cpp` (no nlohmann, no Qt in the core TU).
    - COD-031 gate: `! grep -rq '#include.*nlohmann' src/core/include/` exits 0.
    - Full core build clean: `ctest --preset linux-release -R "action_instance|multiaction|action_engine" --output-on-failure` exits 0.
  </acceptance_criteria>
  <done>instanceChildrenToChain converts children to an ordered, delay-carrying ActionChain with a depth cap; core stays nlohmann/Qt-free.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                               | Description                                               |
| -------------------------------------- | --------------------------------------------------------- |
| profile JSON -> ActionInstance reader  | untrusted/hand-edited profile data crosses into the model |
| ActionInstance.children -> ActionChain | recursive structure could be hostilely deep               |

## STRIDE Threat Register

| Threat ID        | Category          | Component                                       | Disposition | Mitigation Plan                                                                                                                    |
| ---------------- | ----------------- | ----------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-32-01          | Denial of Service | instanceChildrenToChain recursion over children | mitigate    | depth cap (kMaxDepth=64) mirroring the existing reader DepthGuard (CR-01, 61bb6c6); test a beyond-cap nest truncates without crash |
| T-32-02          | Tampering         | delayMs parsed from profile JSON                | accept      | a large delayMs only delays the user's own chain; ActionEngine already defers via injected executor; no privilege/resource leak    |
| T-32-SC          | Tampering         | npm/pip/cargo installs                          | mitigate    | none — zero new dependencies this plan (RESEARCH.md confirms no new packages)                                                      |
| \</threat_model> |                   |                                                 |             |                                                                                                                                    |

<verification>
- `ctest --preset linux-release -R "action_instance|multiaction"` green.
- COD-031: `! grep -rq '#include.*nlohmann' src/core/include/` exits 0.
- After this plan, the full suite still builds: `ctest --preset linux-release` green (Wave-merge gate).
</verification>

\<success_criteria>

- ActionInstance carries delayMs; it round-trips (present / absent->0 / nested).
- instanceChildrenToChain produces an ordered, delay-carrying ActionChain with a depth cap.
- src/core/include/ has zero nlohmann includes.
  \</success_criteria>

<output>
Create `.planning/phases/32-binding-layer-fix-multi-toggle-action-device-editor/32-01-SUMMARY.md` when done.
</output>
