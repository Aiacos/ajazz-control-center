---
phase: 31-actioninstance-core-model-profile-schema-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/include/ajazz/core/action_instance.hpp
  - src/core/include/ajazz/core/profile.hpp
  - src/core/src/profile.cpp
  - docs/protocols/PROFILE_SCHEMA.md
autonomous: true
requirements: [BIND-01]
user_setup: []

must_haves:
  truths:
    - A new nlohmann-free public header action_instance.hpp defines ActionInstance and ActionState
    - Binding and EncoderBinding each carry std::optional<ActionInstance> instance
    - profileToJson always emits an instance as states[]; never the singular state key inside an instance
    - 'profileFromJson reads an instance object: states[], currentState, settings, children, id'
    - "COD-031 holds: grep -rn '#include.*nlohmann' src/core/include/ returns 0 (no nlohmann include in any core public header)"
    - PROFILE_SCHEMA.md documents ActionInstance/ActionState $defs and the instance key
  artifacts:
    - path: src/core/include/ajazz/core/action_instance.hpp
      provides: ActionInstance + ActionState struct definitions (stdlib + KeyState only)
      contains: struct ActionInstance
    - path: src/core/include/ajazz/core/profile.hpp
      provides: optional<ActionInstance> instance on Binding and EncoderBinding
      contains: std::optional<ActionInstance> instance
    - path: src/core/src/profile.cpp
      provides: writeActionInstance/readActionInstance + writeActionState/readActionState helpers wired into binding I/O
      contains: writeActionInstance
    - path: docs/protocols/PROFILE_SCHEMA.md
      provides: ActionInstance + ActionState $defs and instance wire key
      contains: ActionInstance
  key_links:
    - from: src/core/include/ajazz/core/profile.hpp
      to: src/core/include/ajazz/core/action_instance.hpp
      via: '#include'
      pattern: include.*action_instance.hpp
    - from: src/core/src/profile.cpp writeBinding
      to: writeActionInstance
      via: optional emit when b.instance present
      pattern: writeActionInstance
    - from: src/core/src/profile.cpp readBinding
      to: readActionInstance
      via: key == "instance" dispatch
      pattern: readActionInstance
---

<objective>
Add the OpenDeck/StreamDeck-shaped ActionInstance / ActionState core data model and its
hand-rolled JSON serialization, wired additively onto Binding and EncoderBinding as an optional
field. This is the BIND-01 deliverable: the model exists in ajazz_core, serializes without
nlohmann, and the schema doc is the wire-key source of truth.

Purpose: Phases 32 (Multi/Toggle dispatch, device editor) and 33 (Property Inspector) build on
this model; it must be faithful, additive, and lossless before any dispatch or UI work begins.

Output: action_instance.hpp (new header), profile.hpp (additive field), profile.cpp (four new
serializer helpers + binding wiring + lazy v1->v2 read fold), PROFILE_SCHEMA.md ($defs + keys).
Tests are NOT in this plan — they land in 31-02 (Wave 2) which depends on this plan's serializer.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-actioninstance-core-model-profile-schema-v2/31-CONTEXT.md
@.planning/phases/31-actioninstance-core-model-profile-schema-v2/31-RESEARCH.md

<interfaces>
<!-- Key existing core contracts the executor builds on. Extracted from codebase. Use directly. -->

From src/core/include/ajazz/core/profile.hpp (KeyState is the per-state visual payload to REUSE):

```cpp
struct KeyState {
    std::optional<std::string> imagePath;
    std::optional<std::string> text;
    std::optional<Rgb> background;
    std::optional<Rgb> foreground;
    std::uint8_t fontSize{14};
};
struct Binding {
    std::vector<Action> onPress;
    std::vector<Action> onRelease;
    std::vector<Action> onLongPress;
    KeyState state;            // legacy singular Binding::state — UNTOUCHED by this phase
};
struct EncoderBinding {
    std::vector<Action> onCw;
    std::vector<Action> onCcw;
    std::vector<Action> onPress;
    KeyState state;            // UNTOUCHED
};
```

Existing serializer helpers in src/core/src/profile.cpp anonymous namespace (REUSE, do not reimplement):

- `void writeKeyState(std::ostringstream& out, KeyState const&)` (line ~156) — emits the $defs.KeyState object; always emits fontSize.
- `KeyState readKeyState(JsonReader& r)` (line ~667)
- `void escape(std::ostringstream& out, std::string const&)` — RFC-8259-minimal string escape.
- `JsonReader::readString()`, `readUInt()` (range-checks against UINT32_MAX, rejects signs, line ~489), `expect(c)`, `tryConsume(c)`, `skipValue()`.
- `void writeBinding(...)` (line ~190), `writeEncoderBinding(...)` (line ~205) — add the optional instance emit here.
- `Binding readBinding(JsonReader&)` (line ~697), `EncoderBinding readEncoderBinding(JsonReader&)` (line ~725) — add the `instance` key branch here.
- The touchZones lazy-migration / presence-discriminator pattern at profile.cpp:882-892 is the model to FOLLOW (gate on key presence, never on \_schemaVersion).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Define the ActionInstance / ActionState core header (contract first)</name>
  <files>src/core/include/ajazz/core/action_instance.hpp</files>
  <read_first>
    - src/core/include/ajazz/core/profile.hpp (KeyState definition + the file's include/style conventions; SPDX header line)
    - src/core/include/ajazz/core/capabilities.hpp (where Rgb / KeyState dependencies live — confirm KeyState is reachable via profile.hpp)
    - docs/protocols/PROFILE_SCHEMA.md (the settings = escaped-JSON-string convention; line ~15 and ~189)
    - .planning/phases/31-actioninstance-core-model-profile-schema-v2/31-RESEARCH.md (field-set table; A2 wrapper-struct recommendation)
  </read_first>
  <action>
    Create src/core/include/ajazz/core/action_instance.hpp as an installed PUBLIC core header,
    nlohmann-free and Qt-free (COD-031 + Qt-free-core invariant). Begin with the SPDX line
    `// SPDX-License-Identifier: GPL-3.0-or-later`, `#pragma once`, a Doxygen file-brief, and
    `#include "ajazz/core/profile.hpp"` (for KeyState) plus only `<cstdint>`, `<optional>`,
    `<string>`, `<vector>` from the stdlib — no other includes.

```
Define inside namespace ajazz::core two structs per the RESEARCH field-set table and CONTEXT
A2 (wrapper-struct seam):
- `struct ActionState { KeyState visual; };` — thin wrapper carrying full KeyState parity
  (imagePath/text/background/foreground/fontSize) via the reused KeyState; the wrapper leaves
  a forward-compatible seam for a future per-state non-visual field at zero wire cost.
- `struct ActionInstance` with members, in this exact C++ shape (wire keys in comments):
  `std::string id{};` (wire key "id"/"uuid"; dotted plugin action id, omitted from JSON when
  empty — per CONTEXT A1, cheap and forward-compatible),
  `std::vector<ActionState> states{};` (wire "states"),
  `std::uint32_t currentState{0};` (wire "currentState"; 0-based active index),
  `std::string settings{};` (wire "settings"; ESCAPED JSON STRING mirroring
  Action::settingsJson — NOT a nested object),
  `std::vector<ActionInstance> children{};` (wire "children"; recursive Multi Action nesting —
  std::vector of incomplete self-type is legal since C++17, do NOT add unique_ptr indirection).

Add Doxygen comments on each member naming its wire key. Do NOT add serialization functions
here — those live in profile.cpp's anon namespace (Task 3). Do NOT include any nlohmann or Qt
header.

NOTE on include direction: action_instance.hpp includes profile.hpp (for KeyState), and
profile.hpp will include action_instance.hpp (Task 2). Break the cycle by having
action_instance.hpp rely on profile.hpp's `#pragma once` + forward order: include profile.hpp
at the TOP of action_instance.hpp; in profile.hpp, place `#include "ajazz/core/action_instance.hpp"`
AFTER the KeyState struct definition is complete. If the executor finds the cycle awkward,
the acceptable alternative is to move ONLY KeyState's required dependency (it is already in
capabilities.hpp via Rgb) — but prefer the include-ordering approach; KeyState is defined in
profile.hpp and must be complete before ActionState uses it.
```

</action>
  <verify>
    <automated>test -f src/core/include/ajazz/core/action_instance.hpp && grep -q "struct ActionInstance" src/core/include/ajazz/core/action_instance.hpp && grep -q "struct ActionState" src/core/include/ajazz/core/action_instance.hpp && ! grep -rq '#include.*nlohmann' src/core/include/ ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/core/include/ajazz/core/action_instance.hpp` succeeds.
    - `grep -c 'struct ActionInstance' src/core/include/ajazz/core/action_instance.hpp` >= 1.
    - `grep -c 'struct ActionState' src/core/include/ajazz/core/action_instance.hpp` >= 1.
    - `! grep -rq '#include.*nlohmann' src/core/include/` exits 0 — i.e. `grep -rn '#include.*nlohmann' src/core/include/` returns 0 lines (no nlohmann *include* in any core public header). This satisfies ROADMAP success-criterion 1 / CLAUDE.md COD-031 intent (no nlohmann dependency in core public headers); do NOT gate on a bare `nlohmann::` regex — the 3 legitimate COD-031 boundary-documenting comments in builtin_action_registry.hpp:7,28 and input_synthesizer.hpp:9 contain the text `nlohmann::json` and would phantom-fail. The ROADMAP/CLAUDE.md prose wording is imprecise but is NOT to be edited by this phase.
    - `grep -rn -E '#include *<Q|#include *"Q' src/core/include/ajazz/core/action_instance.hpp` returns 0 (Qt-free header).
    - The header declares members `states`, `currentState`, `settings`, `children`, `id` on
      ActionInstance: `grep -E 'states|currentState|settings|children' src/core/include/ajazz/core/action_instance.hpp` matches all four.
  </acceptance_criteria>
  <done>action_instance.hpp exists, defines nlohmann-free/Qt-free ActionInstance (states/currentState/settings/children/id) + ActionState{KeyState visual}; `#include.*nlohmann` grep gate returns 0 in src/core/include/.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive optional<ActionInstance> instance field to Binding + EncoderBinding</name>
  <files>src/core/include/ajazz/core/profile.hpp</files>
  <read_first>
    - src/core/include/ajazz/core/profile.hpp (Binding line ~100-105, EncoderBinding line ~113-118, include block line ~21-30)
    - src/core/include/ajazz/core/action_instance.hpp (the header created in Task 1)
  </read_first>
  <action>
    Edit src/core/include/ajazz/core/profile.hpp to (a) `#include "ajazz/core/action_instance.hpp"`
    placed AFTER the KeyState struct definition completes but before the Binding struct (so KeyState
    is a complete type when action_instance.hpp's ActionState references it) — if include ordering
    forces it, place the include immediately before `struct Binding`; (b) add
    `std::optional<ActionInstance> instance;` as a new member on `struct Binding` (after the existing
    `KeyState state;` line) and the same member on `struct EncoderBinding` (after its `KeyState state;`).

```
Document each new member with a Doxygen comment: additive OpenDeck-shaped action instance
(per-state visuals + settings + Multi Action children); std::nullopt when the profile predates
v2 / carries no "instance" key. Leave the existing onPress/onRelease/onLongPress chains and the
legacy `KeyState state;` field UNTOUCHED (additive, not a replacement — CONTEXT lock + success
criterion 4). Do NOT touch TouchZoneBinding (out of scope this phase).
```

</action>
  <verify>
    <automated>grep -c 'std::optional<ActionInstance> instance' src/core/include/ajazz/core/profile.hpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'std::optional<ActionInstance> instance' src/core/include/ajazz/core/profile.hpp` == 2 (Binding + EncoderBinding).
    - `grep -c '#include "ajazz/core/action_instance.hpp"' src/core/include/ajazz/core/profile.hpp` == 1.
    - The legacy `KeyState state;` member is still present on Binding and EncoderBinding:
      `grep -c 'KeyState state;' src/core/include/ajazz/core/profile.hpp` >= 2 (additive, not replaced).
    - `grep -rn '#include.*nlohmann' src/core/include/` still returns 0 lines (COD-031 boundary held; gate on includes, not on the `nlohmann::` boundary-comment prose).
  </acceptance_criteria>
  <done>Binding and EncoderBinding each carry std::optional<ActionInstance> instance; legacy state field and action chains untouched; `#include.*nlohmann` grep in src/core/include/ still 0.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-roll the ActionInstance serializer + wire it into binding I/O (with lazy v1->v2 fold) and update the schema doc</name>
  <files>src/core/src/profile.cpp, docs/protocols/PROFILE_SCHEMA.md</files>
  <read_first>
    - src/core/src/profile.cpp lines 140-231 (writeKeyState, writeBinding, writeEncoderBinding, keyStateIsDefault, end of anon namespace)
    - src/core/src/profile.cpp lines 660-751 (readKeyState, readBinding, readEncoderBinding)
    - src/core/src/profile.cpp lines 855-905 (the touchZones presence-discriminator migration pattern — the model to follow; readUInt at ~489)
    - docs/protocols/PROFILE_SCHEMA.md lines 14-126 (the wire-key table, $defs.Binding/EncoderBinding/KeyState, compatibility policy at ~175)
  </read_first>
  <action>
    In src/core/src/profile.cpp's anonymous namespace, add four helpers mirroring the existing
    KeyState pair:
    - `void writeActionState(std::ostringstream& out, ActionState const& s)` — delegate to the
      existing `writeKeyState(out, s.visual)`; a states[] element is ALWAYS emitted as a non-empty
      KeyState object (writeKeyState always emits fontSize), which is correct for array elements.
    - `ActionState readActionState(JsonReader& r)` — return `ActionState{ readKeyState(r) }`.
    - `void writeActionInstance(std::ostringstream& out, ActionInstance const& inst)` — emit a JSON
      object: optional `"id"` ONLY when non-empty (escape it); ALWAYS `"states":[` array of
      writeActionState over inst.states `]`; ALWAYS `"currentState":<int>`; emit `"settings":`
      (escaped string via escape()) ONLY when non-empty; ALWAYS `"children":[` recursive
      writeActionInstance over inst.children `]`. The writer MUST NEVER emit the singular `"state"`
      key (Pitfall 2 — only the reader understands the legacy singular form).
    - `ActionInstance readActionInstance(JsonReader& r)` — parse the object, dispatching on key:
      `"states"` -> push readActionState per array element; `"state"` (legacy singular) -> clear
      states then push one readActionState (LAZY v1->v2 FOLD — this is BIND-02 migration);
      `"currentState"` -> readUInt(); `"settings"` -> readString(); `"children"` -> push
      readActionInstance per element (RECURSIVE); `"id"` OR `"uuid"` -> readString() into inst.id;
      anything else -> skipValue() (forward-compat). After the loop, defensively clamp:
      `if (inst.states.empty() || inst.currentState >= inst.states.size()) inst.currentState = 0;`
      (CONTEXT defensive-read rule — lossless load, never reject). Gate on KEY PRESENCE only; do
      NOT read or branch on _schemaVersion (Pitfall 1 / the CR-01/WR-06 anti-pattern).

```
Wire into the four binding functions:
- writeBinding: after the existing keyStateIsDefault(b.state) block, add
  `if (b.instance) { out << ",\"instance\":"; writeActionInstance(out, *b.instance); }`.
- writeEncoderBinding: same optional emit for its instance.
- readBinding: add `else if (key == "instance") { b.instance = readActionInstance(r); }` before
  the final `else { r.skipValue(); }`.
- readEncoderBinding: add the same `instance` branch.
Absent `"instance"` key leaves the optional as std::nullopt automatically (success criterion 4).

Then update docs/protocols/PROFILE_SCHEMA.md (the wire-key source of truth, CLAUDE.md hard rule):
add an `instance` property `{ "$ref": "#/$defs/ActionInstance" }` to BOTH $defs.Binding and
$defs.EncoderBinding; add `$defs.ActionInstance` (object with: `id` string; `states` array of
`#/$defs/ActionState`; `currentState` integer minimum 0; `settings` string described as an
escaped JSON document like Action.settings; `children` array of `#/$defs/ActionInstance`
recursive) and `$defs.ActionState` (described as carrying the same fields as `#/$defs/KeyState`;
either a `$ref` to KeyState or an inline copy of KeyState properties — prefer `$ref`). Add a row
to the wire-key table noting `ActionInstance::settings <-> "settings"` mirrors Action's escaped
string. Add a sentence to the compatibility policy: the reader also accepts a legacy singular
`"state"` inside an instance and folds it into `states[]` of one; the writer always emits
`states[]`. Keep all keys ASCII.

Pre-commit may run mdformat and reformat PROFILE_SCHEMA.md — if it re-stages the file, re-add
and re-commit (the normal re-add dance). NEVER use --no-verify.
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_core 2>&1 | tail -5; grep -c 'writeActionInstance\|readActionInstance' src/core/src/profile.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `cmake --build --preset linux-release --target ajazz_core` exits 0 (the recursive vector + new helpers compile).
    - `grep -c 'writeActionInstance' src/core/src/profile.cpp` >= 2 (definition + at least one call site).
    - `grep -c 'readActionInstance' src/core/src/profile.cpp` >= 3 (definition + readBinding + readEncoderBinding + recursive children call).
    - The writer never emits a singular instance state: `grep -n '"state":' src/core/src/profile.cpp` shows hits ONLY inside writeBinding/writeEncoderBinding/writeTouchZoneBinding legacy paths, NONE inside writeActionInstance (manual confirm the function body contains `"states":` not `"state":`).
    - No version-gating inside the new reader (function-scoped, excludes the pre-existing legitimate `_schemaVersion` handling at profile.cpp:858-869): `awk '/ActionInstance readActionInstance/,/^}/' src/core/src/profile.cpp | grep -c schemaVersion` == 0.
    - PROFILE_SCHEMA.md contains the new $defs: `grep -c 'ActionInstance' docs/protocols/PROFILE_SCHEMA.md` >= 1 AND `grep -c 'ActionState' docs/protocols/PROFILE_SCHEMA.md` >= 1.
    - `grep -rn '#include.*nlohmann' src/core/include/` still returns 0 lines (boundary unbroken by the wiring; gate on includes, not on `nlohmann::` boundary comments).
  </acceptance_criteria>
  <done>Four serializer helpers exist; writeBinding/writeEncoderBinding emit instance optionally; readBinding/readEncoderBinding parse it; legacy singular "state" folds to states[] of one on read; writer emits only states[]; currentState clamps defensively; no _schemaVersion gate inside readActionInstance; ajazz_core builds; PROFILE_SCHEMA.md documents ActionInstance/ActionState $defs + instance key; `#include.*nlohmann` grep 0 in src/core/include/.</done>
</task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                        |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| profile JSON file -> profileFromJson() | Untrusted/hand-editable/third-party profile files cross into the hand-rolled reader. This is the only attack surface in this plan. |

## STRIDE Threat Register

| Threat ID        | Category          | Component                                                    | Disposition | Mitigation Plan                                                                                                                                                                 |
| ---------------- | ----------------- | ------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-01          | Tampering         | currentState integer in readActionInstance                   | mitigate    | readUInt() already range-checks/UINT32_MAX + rejects signs; add `>= states.size()` clamp-to-0 (Task 3) so a stale/hostile index never indexes out of bounds.                    |
| T-31-02          | Denial of Service | recursive children parse (stack exhaustion via deep nesting) | accept      | LOW risk for local profile files; depth cap deferred per CONTEXT (fuzz/property tests out of scope). Documented as a known non-blocking hardening item; do not implement blind. |
| T-31-03          | Tampering         | malformed/truncated instance JSON                            | mitigate    | new branches inherit the existing reader's expect()/fail() which throw std::runtime_error with a byte offset; no silent acceptance.                                             |
| T-31-SC          | Tampering         | npm/pip/cargo installs                                       | n/a         | No package-manager installs in this phase (pure-core C++ + existing Catch2). No legitimacy gate required.                                                                       |
| \</threat_model> |                   |                                                              |             |                                                                                                                                                                                 |

<verification>
- `cmake --build --preset linux-release --target ajazz_core` exits 0.
- `grep -rn '#include.*nlohmann' src/core/include/` returns 0 lines (COD-031, phase success criterion 1 — gate on includes, not on the legitimate `nlohmann::json` boundary comments).
- Header + schema artifacts exist with the required structs/$defs.
- Round-trip and migration behavior is verified by the Catch2 tests in 31-02 (Wave 2) —
  this plan delivers only the model + serializer + schema doc.
</verification>

\<success_criteria>

- action_instance.hpp defines nlohmann-free/Qt-free ActionInstance + ActionState.
- Binding + EncoderBinding carry std::optional<ActionInstance> instance (additive; legacy chains + state untouched).
- profile.cpp serializes the instance (writer emits states[] only; reader folds legacy singular state, clamps currentState, gates on key presence not \_schemaVersion).
- PROFILE_SCHEMA.md documents ActionInstance/ActionState $defs + the instance key.
- ajazz_core compiles clean; `#include.*nlohmann` grep in src/core/include/ returns 0.
  \</success_criteria>

<output>
Create `.planning/phases/31-actioninstance-core-model-profile-schema-v2/31-01-SUMMARY.md` when done.
</output>
