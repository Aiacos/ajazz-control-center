---
phase: 08-scaffolded-device-wiring
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/app/src/device_model.hpp
  - src/app/src/device_model.cpp
  - src/app/qml/components/DeviceRow.qml
  - tests/unit/test_device_model.cpp
autonomous: true
requirements: [DEVICES-02]
must_haves:
  truths:
    - DeviceModel::Roles contains a MaturityRole enumerator (next free slot after HasTouchStripRole).
    - data(MaturityRole) returns a QString matching the device's `maturity` value from devices.yaml for the row's codename.
    - roleNames() maps MaturityRole → "maturity" (lowercase) so QML reads it as the `maturity` role.
    - 'The QML sidebar row (DeviceRow.qml) attaches a ToolTip that surfaces the maturity tier (e.g., `ToolTip.text: "Maturity: " + maturity`).'
    - Existing test surface (tests/unit/test_device_model.cpp) asserts MaturityRole returns the expected string for at least one functional and one scaffolded device.
  artifacts:
    - path: src/app/src/device_model.hpp
      provides: Roles::MaturityRole enumerator
      contains: MaturityRole
    - path: src/app/src/device_model.cpp
      provides: data() branch + roleNames() entry + maturity lookup map
      contains: MaturityRole
    - path: src/app/qml/components/DeviceRow.qml
      provides: ToolTip wired to the `maturity` role
      contains: ToolTip
    - path: tests/unit/test_device_model.cpp
      provides: MaturityRole coverage
      contains: MaturityRole
  key_links:
    - from: docs/_data/devices.yaml
      to: src/app/src/device_model.cpp
      via: build-time embedded resource OR generated static map (see action — pick based on existing pattern)
      pattern: (qrc:/.*devices\.yaml|kMaturityByCodename)
    - from: src/app/src/device_model.cpp
      to: src/app/qml/components/DeviceRow.qml
      via: QML role `maturity` consumed in delegate
      pattern: (maturity|MaturityRole)
---

<objective>
Add `MaturityRole` to `DeviceModel`'s role enum, wire `data()` + `roleNames()` to expose the per-row maturity string from devices.yaml, and surface the tier in the QML sidebar as a **tooltip** (NOT a visible badge — per D-03's "consistent with v1.0 styling vocabulary" rule).

Purpose: Implements **D-03** (locked). Per the v1.0 styling vocabulary, the tier surfaces on hover, not as a new visible element. A colored badge is optional and only viable if the existing `ConnectedRole` badge slot can be extended without crowding the row — default to tooltip-only.

Output: Updated DeviceModel header/impl, QML row tooltip, test coverage for MaturityRole.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-scaffolded-device-wiring/08-CONTEXT.md
@src/app/src/device_model.hpp
@src/app/src/device_model.cpp
@src/app/qml/components/DeviceRow.qml
@docs/_data/devices.yaml

**D-03 from CONTEXT.md (locked):** `MaturityRole` enumerator + sidebar tooltip. Optional colored badge only if existing `ConnectedRole` badge slot accommodates it. Default to tooltip-only.

**Existing Roles enum (device_model.hpp lines 62–75):**

```cpp
enum Roles {
    ModelRole = Qt::UserRole + 1,
    CodenameRole, FamilyRole, VidRole, PidRole,
    ConnectedRole, KeyCountRole, GridColumnsRole,
    EncoderCountRole, DpiStageCountRole,
    HasRgbRole, HasTouchStripRole,
};
```

Add `MaturityRole` as the next enumerator after `HasTouchStripRole`.

**roleNames() string:** QML side consumes the role as `maturity` (lowercase, matches devices.yaml field).

**Maturity source-of-truth pipeline:** devices.yaml is the canonical source. The C++ side does NOT parse YAML at runtime — instead, this plan generates a static C++ map at build time from devices.yaml (mirrors what existing code does for, e.g., the descriptor catalogue in `register.cpp`). If a generator script already exists for that pipeline, extend it; otherwise hand-write the map inside `device_model.cpp` keyed by codename (acceptable for 27 entries — small, churn-resistant, and the rename plan above is the trigger). Pick whichever pattern is simpler given the existing precedent — `read_first` includes register.cpp for the executor to inspect.

**Sidebar tooltip placement:** `DeviceRow.qml` is the per-row delegate. Attach a `ToolTip` to the root row item (or to the existing `ConnectedRole` badge) with text `qsTr("Maturity: %1").arg(maturity)` (where `maturity` is the role exposed by DeviceModel).

**Phase 5 cross-cutting (CONTEXT.md):** Phase 5 will add a `HasClockRole`. This plan adds `MaturityRole` as the next slot AFTER `HasTouchStripRole`. Phase 5's plan owns its own slot — no conflict if both phases pick the next-free slot and land in non-overlapping commits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MaturityRole enum + roleNames + data() branch in DeviceModel</name>
  <files>src/app/src/device_model.hpp, src/app/src/device_model.cpp</files>
  <read_first>
    - src/app/src/device_model.hpp (lines 62–75 for Roles enum; line 107 for roleNames signature)
    - src/app/src/device_model.cpp (full file — find the data() switch on `role` and the roleNames() return map)
    - docs/_data/devices.yaml (the post-Plan-01 `maturity:` values for each codename — 27 rows)
    - src/devices/streamdeck/src/register.cpp (precedent for codename → string lookup tables, if applicable)
  </read_first>
  <action>
    Per D-03:
    1. In `device_model.hpp`, add `MaturityRole` as the next enumerator after `HasTouchStripRole` in the `Roles` enum. Add a `///<` Doxygen comment: `///< Maturity tier string from devices.yaml ("scaffolded", "probed", "partial", "functional", "verified").`
    2. In `device_model.cpp`, populate a static const map at the top of the anonymous namespace (or as a `static const std::map<std::string, QString> kMaturityByCodename = {...};` at file scope) listing every codename → maturity value from the post-Plan-01 devices.yaml (27 entries). Format each entry on its own line for grep-friendliness, e.g. `{ "akp153", QStringLiteral("functional") },`.
    3. In `DeviceModel::data()`, add a `case MaturityRole:` branch that returns `kMaturityByCodename.lookup(m_rows[row].codename)` as a QVariant — when codename is missing from the map, return `QStringLiteral("scaffolded")` as a defensive fallback (newly added devices without explicit maturity default to scaffolded).
    4. In `DeviceModel::roleNames()`, add the entry `{ MaturityRole, QByteArrayLiteral("maturity") }` (lowercase to match devices.yaml field name).
    5. In `DeviceModel::capabilitiesFor()` (line 144), add a `"maturity"` key to the returned QVariantMap with the same value, so QML code that drives off `capabilitiesFor(codename)` can read maturity directly.
    Constraint: no `reinterpret_cast` in this code (Pitfall 10) — there is no packed-struct access here, this is a pure string lookup; flagged as a reminder for the broader phase.
  </action>
  <verify>
    <automated>grep -q "MaturityRole" src/app/src/device_model.hpp && grep -q "MaturityRole" src/app/src/device_model.cpp && grep -q 'QByteArrayLiteral("maturity")' src/app/src/device_model.cpp && grep -q "kMaturityByCodename\|maturity_for_codename" src/app/src/device_model.cpp && cmake --build build --target ajazz-control-center 2>&1 | tail -20 | grep -qE 'error:' && exit 1 || echo "build ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep MaturityRole src/app/src/device_model.hpp` returns at least 2 lines (declaration + Doxygen).
    - `grep -c MaturityRole src/app/src/device_model.cpp` returns ≥ 2 (data() branch + roleNames entry).
    - `grep -c '"maturity"' src/app/src/device_model.cpp` returns ≥ 2 (roleNames string + capabilitiesFor key).
    - The static map / lookup contains a row for every codename in devices.yaml (27 entries).
    - The build succeeds (`cmake --build build` exits 0; no compile errors).
    - `./run-clang-tidy.sh src/app/src/device_model.cpp` (or equivalent) reports zero `cppcoreguidelines-pro-type-reinterpret-cast` warnings (none expected; defensive check).
  </acceptance_criteria>
  <done>DeviceModel exposes MaturityRole; build green; QML can read `model.maturity` from delegates.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ToolTip in DeviceRow.qml to the maturity role</name>
  <files>src/app/qml/components/DeviceRow.qml</files>
  <read_first>
    - src/app/qml/components/DeviceRow.qml (current row delegate; identify the root item and where existing ConnectedRole/HasRgbRole roles are consumed)
    - src/app/qml/DeviceList.qml (parent ListView for context on how `model.<role>` properties are wired in)
  </read_first>
  <action>
    Per D-03 (tooltip, NOT visible badge):
    1. Add a `ToolTip` to the root `Item` (or whichever root QML type DeviceRow.qml uses) of the row delegate:
       ```
       ToolTip.visible: hoverArea.containsMouse
       ToolTip.delay: 500
       ToolTip.text: qsTr("Maturity: %1").arg(model.maturity ?? "unknown")
       ```
    2. If a `HoverHandler` or `MouseArea` (with `hoverEnabled: true`) already exists for the row (likely, since the row is selectable), reuse its `hovered` / `containsMouse` property instead of adding a second one.
    3. If no hover handler exists, add a `HoverHandler { id: hoverHandler }` and bind `ToolTip.visible: hoverHandler.hovered`.
    4. Do NOT add a visible maturity badge in this task. Optional colored badge is deferred — adds a visual element D-03 explicitly defers to "v1.0 styling vocabulary". Note in summary if the executor judges a small dot would fit without crowding, and surface as a follow-up rather than implementing here.
  </action>
  <verify>
    <automated>grep -q "ToolTip" src/app/qml/components/DeviceRow.qml && grep -q "model.maturity\|maturity" src/app/qml/components/DeviceRow.qml && cmake --build build --target ajazz-control-center 2>&1 | tail -10 | grep -qE 'error:' && exit 1 || echo "build ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ToolTip" src/app/qml/components/DeviceRow.qml` returns ≥ 1.
    - `grep -c "maturity" src/app/qml/components/DeviceRow.qml` returns ≥ 1.
    - QML loads without runtime warnings about unknown role `maturity` (manual check: run app, open sidebar, hover a row — tooltip appears with the tier name).
    - Build still succeeds (Qt's QML compiler validates the binding statically when `qt_add_qml_module` is in `IMPORTS` mode).
  </acceptance_criteria>
  <done>Hovering a device row in the sidebar shows a tooltip with the device's maturity tier; no visible badge added.</done>
</task>

<task type="auto">
  <name>Task 3: Extend test_device_model.cpp with MaturityRole assertions</name>
  <files>tests/unit/test_device_model.cpp</files>
  <read_first>
    - tests/unit/test_device_model.cpp (existing role-coverage tests — use the same pattern)
    - src/app/src/device_model.hpp (Roles enum)
  </read_first>
  <action>
    1. Add a test case (or extend the existing role-test if one exists) that:
       a. Constructs a DeviceModel with a real DeviceRegistry (matches the existing `ConnectedRole`/`HasRgbRole` test fixture pattern).
       b. Picks at least two codenames whose maturity is known from devices.yaml — one `functional` (e.g., `"akp153"`) and one `scaffolded` (e.g., `"akp815"` post-rename — Plan 01 keeps it scaffolded).
       c. Calls `model->data(model->index(row, 0), DeviceModel::MaturityRole)` and asserts the returned QVariant is a QString equal to the expected tier.
       d. Calls `model->roleNames()` and asserts the hash contains a `(MaturityRole, "maturity")` entry.
    2. The test must be deterministic — pick rows by codename via `m_rows` lookup, not by row index (row order depends on hot-plug refresh).
    3. If the existing test file uses a different fixture pattern (e.g., a fake registry), mirror that pattern instead of inventing a new one.
  </action>
  <verify>
    <automated>cmake --build build --target test_device_model 2>&1 | tail -10 | grep -qE 'error:' && exit 1; ctest --test-dir build -R test_device_model --output-on-failure 2>&1 | tail -20 | grep -q 'Passed\|0 tests failed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MaturityRole" tests/unit/test_device_model.cpp` returns ≥ 2 (one assertion per tier checked).
    - `grep -q '"maturity"' tests/unit/test_device_model.cpp` succeeds (roleNames assertion).
    - `ctest -R test_device_model` exits 0.
  </acceptance_criteria>
  <done>test_device_model passes including new MaturityRole assertions for at least one functional and one scaffolded codename.</done>
</task>

</tasks>

<verification>
- DeviceModel header + implementation expose MaturityRole; build green.
- DeviceRow.qml hover shows the maturity tooltip (manual sanity check at execute time — surface in SUMMARY).
- test_device_model passes including new role assertions.
- clang-tidy clean on the touched files (no `cppcoreguidelines-pro-type-reinterpret-cast` warnings; none expected — pure string lookup).
</verification>

\<success_criteria>

- DEVICES-02 satisfied: DeviceModel exposes MaturityRole; QML sidebar surfaces the tier as a tooltip (consistent with v1.0 styling vocabulary).
- Atomic commit boundary: all three tasks land in ONE atomic commit.
  \</success_criteria>

<output>
After completion, create `.planning/phases/08-scaffolded-device-wiring/08-02-SUMMARY.md` describing:
- Whether the static map approach or a generated lookup was used (and why).
- Whether a colored badge was added (default: no — note as deferred follow-up if executor surfaces).
- Any QML import or hover-handler refactoring required.
</output>
