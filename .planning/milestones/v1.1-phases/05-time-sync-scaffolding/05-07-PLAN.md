---
phase: 05-time-sync-scaffolding
plan: 07
type: execute
wave: 5
depends_on: [05-04, 05-06]
files_modified:
  - src/app/src/application.hpp
  - src/app/src/application.cpp
autonomous: true
requirements: [TIMESYNC-03, TIMESYNC-05]
must_haves:
  truths:
    - Application owns a std::unique_ptr<TimeSyncService> m_timeSync member, constructed with a reference to the existing DeviceRegistry.
    - TimeSyncService is registered as a QML singleton via qmlRegisterSingletonInstance<TimeSyncService>("Ajazz.App", 1, 0, "TimeSyncService", m_timeSync.get()) at app startup — singleton-instance pattern (NOT QML_SINGLETON which would default-construct, breaking the static_assert per Pitfall 4).
    - HotplugMonitor::deviceArrived signal is connected to TimeSyncService::onDeviceArrived in Application::Application() (or the equivalent setup phase) — closes the auto-sync loop end-to-end.
    - 'D-01 amendment 3 (A-04) honored: TimeSyncService consumes the shared_ptr<IDevice> from DeviceRegistry::open() per Phase 4 D-06 — verified by Plan 05-04 grep on m_registry.open() and shared_ptr capture.'
    - 'After Plan 05-07 lands, the app smoke-test passes: launching the binary, the QML loads without error, TimeSyncService is reachable from QML (SettingsPage SwitchDelegate is interactive), the Sync button is visible on hasClock=true rows, and clicking it emits a "not yet supported" toast (NotImplemented path).'
  artifacts:
    - path: src/app/src/application.hpp
      provides: 'std::unique_ptr<TimeSyncService> m_timeSync member; #include of time_sync_service.hpp'
      contains: TimeSyncService
    - path: src/app/src/application.cpp
      provides: TimeSyncService construction, qmlRegisterSingletonInstance call, HotplugMonitor::deviceArrived → TimeSyncService::onDeviceArrived connect()
      contains: qmlRegisterSingletonInstance
  key_links:
    - from: Application::Application ctor
      to: TimeSyncService construction (passing DeviceRegistry&)
      via: std::make_unique
      pattern: make_unique<TimeSyncService
    - from: qmlRegisterSingletonInstance call
      to: m_timeSync.get() (raw pointer to the owned instance)
      via: QML registration at app startup
      pattern: qmlRegisterSingletonInstance.*TimeSyncService
    - from: HotplugMonitor::deviceArrived signal
      to: TimeSyncService::onDeviceArrived slot
      via: QObject::connect (Qt::QueuedConnection — cross-thread safe)
      pattern: connect.*deviceArrived.*onDeviceArrived
---

<objective>
Wire `TimeSyncService` into the Application lifecycle: own it as a `std::unique_ptr` member, register it as a QML singleton via `qmlRegisterSingletonInstance` (the v1.0 canonical pattern that doesn't default-construct, preserving the Pitfall 4 lock from Plan 05-04), and connect `HotplugMonitor::deviceArrived` to `TimeSyncService::onDeviceArrived` so the auto-sync loop closes end-to-end.

After this plan, the entire Phase 5 surface is runtime-active: launching the app shows the Sync button on capable device rows, clicking it produces a "not yet supported" toast (NotImplemented path), the SettingsPage toggle persists in QSettings, and connecting/disconnecting a Stream Dock with autoSync=true triggers the 300ms-debounced auto-sync (which also fails NotImplemented, surfacing only as a glyph per D-02).

This plan is a thin pointer-plan: it adopts **Task 7 of the upstream plan at `docs/superpowers/plans/2026-05-13-time-sync.md` (lines 1281-1439)** — with one amendment captured below for D-01 amendment 3 (the open() shared_ptr lookup integration).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-time-sync-scaffolding/05-CONTEXT.md
@.planning/phases/04-hot-plug-hardening/04-CONTEXT.md
@.planning/research/PITFALLS.md
@docs/superpowers/plans/2026-05-13-time-sync.md
@docs/superpowers/specs/2026-05-13-time-sync-design.md
@src/app/src/application.hpp
@src/app/src/application.cpp
@src/app/src/time_sync_service.hpp
@src/app/src/branding_service.hpp
@src/core/include/ajazz/core/device_registry.hpp
@src/core/include/ajazz/core/hotplug_monitor.hpp

\<upstream_pointer>
**Source plan:** `docs/superpowers/plans/2026-05-13-time-sync.md` — Task 7 (lines 1281-1439).

**Steps adopted verbatim:**

- Step 7.1 (lines 1289-1304) — Add `#include "time_sync_service.hpp"` and `std::unique_ptr<TimeSyncService> m_timeSync;` member to `application.hpp`. Add a public `TimeSyncService* timeSync() const noexcept` accessor (mirrors existing `branding()` / `deviceModel()` accessors).
- Step 7.2 (lines 1305-1339) — In Application's setup phase (Application::Application body or Application::initialize() — match the existing service init order; TimeSyncService comes AFTER DeviceRegistry but BEFORE HotplugMonitor::start() to satisfy the connect ordering): construct `m_timeSync = std::make_unique<TimeSyncService>(m_deviceRegistry, this);` and register it: `qmlRegisterSingletonInstance<TimeSyncService>("Ajazz.App", 1, 0, "TimeSyncService", m_timeSync.get());`. The URI / version / name MUST match the BrandingService precedent in this codebase — read branding_service.hpp + the application.cpp registration call for it as the exemplar.
- Step 7.3 (lines 1340-1362) — Wire auto-sync: in the same setup phase, `connect(&m_hotplug, &HotplugMonitor::deviceArrived, m_timeSync.get(), &TimeSyncService::onDeviceArrived, Qt::QueuedConnection);`. Queued connection because HotplugMonitor may live on a different thread (verify by grep — if same thread, AutoConnection is fine).
- Step 7.4 (lines 1363-1372) — Build to verify.
- Step 7.5 (lines 1373-1382) — Run all tests.
- Step 7.6 (lines 1383-1414) — Smoke-test the running app: launch, observe Sync button on hasClock=true rows, click it → toast "Time sync not yet supported", toggle SettingsPage switch → key persists in QSettings, plug a Stream Dock with autoSync=on → glyph updates (no toast — D-02). Document smoke-test result in SUMMARY.md.
- Step 7.7 (lines 1415-1439) — Commit message: `feat(app): wire TimeSyncService + auto-sync hook into Application`.

**Amendment to Task 7 (this plan only):**

- **A-06 (D-01 amendment 3 reinforcement) — shared_ptr cache integration verification.** The upstream plan was written before Phase 4 D-06 landed, so the upstream's specification of how `TimeSyncService` looks up devices via the registry is now stale (raw pointer → shared_ptr). Plan 05-04 already implemented the corrected lookup inside `TimeSyncService::setSystemTimeOn` and `onDeviceArrived` (per A-04). This plan's amendment is purely a verification step:
  - In Step 7.6's smoke test, ALSO observe: with the dev console / debug build, confirm `DeviceRegistry::open()` is called from TimeSyncService AND that the returned shared_ptr is captured for the call duration (ASAN / Valgrind clean — no UAF). If a debug build with sanitizers is available, run `ASAN_OPTIONS=detect_leaks=1 ./ajazz_app` and exercise the Sync button manually; it should not report use-after-free or leak.
  - This is a runtime verification of A-04, not a code change.

**No change to the QML registration URI/version, the connect direction, or the smoke-test surface.**

\</upstream_pointer>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply upstream Steps 7.1, 7.2, 7.3 — Application owns + registers TimeSyncService + wires auto-sync</name>
  <files>src/app/src/application.hpp, src/app/src/application.cpp</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 1281-1362 (Steps 7.1, 7.2, 7.3 — full setup-phase skeleton)
    - src/app/src/application.hpp (current owned-services pattern: BrandingService, DeviceModel, HotplugMonitor — TimeSyncService follows the same convention)
    - src/app/src/application.cpp (current setup phase: constructor body, qmlRegisterSingletonInstance call for BrandingService — exemplar)
    - src/app/src/branding_service.hpp (the canonical QML_NAMED_ELEMENT + qmlRegisterSingletonInstance pattern this app uses)
    - src/app/src/time_sync_service.hpp (signature of TimeSyncService::TimeSyncService(DeviceRegistry&, QObject*=nullptr) — landed in Plan 05-04)
    - src/core/include/ajazz/core/hotplug_monitor.hpp (HotplugMonitor::deviceArrived signal signature)
    - src/core/include/ajazz/core/device_registry.hpp (DeviceRegistry — confirmed shared_ptr<IDevice> open() landed in Phase 4)
    - .planning/phases/05-time-sync-scaffolding/05-CONTEXT.md decision D-01 amendment 3 + Application::onHotplug rationale
  </read_first>
  <action>
    Apply upstream Step 7.1 — in src/app/src/application.hpp:

```
1. Add `#include "time_sync_service.hpp"` near the existing #include of branding_service.hpp.
2. Add a private member: `std::unique_ptr<TimeSyncService> m_timeSync;` placed in the same group as m_brandingService / m_deviceModel.
3. Add a public accessor: `TimeSyncService* timeSync() const noexcept { return m_timeSync.get(); }` — mirrors branding() accessor.

Apply upstream Step 7.2 — in src/app/src/application.cpp's setup phase (Application::Application body OR Application::initialize() — match wherever BrandingService is registered):
1. After m_deviceRegistry is constructed and before HotplugMonitor::start() is called: `m_timeSync = std::make_unique<TimeSyncService>(m_deviceRegistry, this);`
2. Register the QML singleton: `qmlRegisterSingletonInstance<TimeSyncService>("Ajazz.App", 1, 0, "TimeSyncService", m_timeSync.get());`
   - Match the URI/version BrandingService uses (replace "Ajazz.App" / 1 / 0 with the actual project URI/version if different — read branding_service registration first).
   - This is the canonical "singleton-instance" pattern that does NOT trigger the default-constructor (Pitfall 4 — converted to a build break by the static_assert in Plan 05-04).

Apply upstream Step 7.3 — wire the auto-sync hook:
`connect(&m_hotplugMonitor, &HotplugMonitor::deviceArrived, m_timeSync.get(), &TimeSyncService::onDeviceArrived, Qt::QueuedConnection);`
- Choose Qt::QueuedConnection if HotplugMonitor lives on a worker thread (verify by reading hotplug_monitor.hpp / the existing connect calls to its signals); else Qt::AutoConnection.

CRITICAL ordering: TimeSyncService MUST be constructed AFTER m_deviceRegistry (it captures a reference) AND BEFORE HotplugMonitor::start() (so the connect is in place when the first deviceArrived signal fires). If the existing init order makes this impossible without restructuring, document in SUMMARY.md and choose the safe path (delay HotplugMonitor::start() if needed).
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release 2>&1 | tee /tmp/build_05_07_t1.log | tail -20 ; grep -E "error:|undefined reference" /tmp/build_05_07_t1.log && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '#include "time_sync_service.hpp"' src/app/src/application.hpp` exits 0
    - `grep -q 'std::unique_ptr<TimeSyncService> m_timeSync' src/app/src/application.hpp` exits 0
    - `grep -q 'TimeSyncService\* timeSync()' src/app/src/application.hpp` exits 0
    - `grep -q 'std::make_unique<TimeSyncService>' src/app/src/application.cpp` exits 0
    - `grep -q 'qmlRegisterSingletonInstance<TimeSyncService>' src/app/src/application.cpp` exits 0 (NOT qmlRegisterSingletonType — that path would default-construct and trip the static_assert)
    - `grep -q 'HotplugMonitor::deviceArrived' src/app/src/application.cpp` exits 0 + `grep -q 'TimeSyncService::onDeviceArrived' src/app/src/application.cpp` exits 0 (the connect is in place)
    - The connect line includes Qt::QueuedConnection or Qt::AutoConnection (whichever matches the threading model).
    - `cmake --build --preset linux-release` exits 0 — full project links.
  </acceptance_criteria>
  <done>
    Application owns TimeSyncService, registers it as a QML singleton, and routes hotplug arrivals to it. The QML SettingsPage SwitchDelegate (Plan 05-06) is now interactive at runtime; the Sync button (Plan 05-06) is now wired end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply upstream Steps 7.4, 7.5, 7.6 — build, tests, and smoke-test (with A-06 ASAN observation)</name>
  <files>(no file modifications — verification + smoke test)</files>
  <read_first>
    - docs/superpowers/plans/2026-05-13-time-sync.md lines 1363-1414 (Steps 7.4, 7.5, 7.6)
    - .planning/phases/05-time-sync-scaffolding/05-CONTEXT.md D-01 amendment 3 (A-06 verification expectations)
  </read_first>
  <action>
    Run upstream Step 7.4 (build) and Step 7.5 (full ctest) verbatim.

```
Apply upstream Step 7.6 (smoke test) — launch the app binary and verify the following manually OR via an automated smoke harness if one exists:
1. App launches, QML loads without error.
2. SettingsPage opens; the "Auto-sync time on device connect" SwitchDelegate is visible AND toggleable.
3. Toggle the switch on, restart the app — the switch comes back on (QSettings persistence works).
4. Toggle the switch off + on with no Stream Dock connected — observe an INFO log "auto-sync persisted ON but no capable device" (Pitfall 13 load-time validation; this only fires if the switch is ON at next launch with no capable device connected, so the verification is: toggle on, restart, see INFO).
5. With a Stream Dock connected, the device row shows the "Sync time" ToolButton (deviceHasClock=true gating).
6. Click "Sync time" — observe a Toast "Time sync not yet supported on this device" + amber exclamation glyph on the row.
7. With autoSync=on, plug/unplug a Stream Dock — the row glyph updates after ~300ms; NO toast appears (D-02 enforcement).

A-06 amendment: if a debug/ASAN build is available, repeat the click on Sync button under `ASAN_OPTIONS=detect_leaks=1` — verify no UAF / leak. This validates Plan 05-04's A-04 shared_ptr capture in a runtime context.

Document the smoke-test outcome in SUMMARY.md (per upstream Step 7.6 + A-06).

If a Stream Dock is unavailable (CI / dev box without device), document that the smoke test was DEFERRED to manual integration test — Plan 05-08 has the synthetic-event integration test that covers the auto-sync path without real hardware.

Then apply Step 7.7 — commit: `feat(app): wire TimeSyncService + auto-sync hook into Application`, body line `Refs: TIMESYNC-03, TIMESYNC-05, D-01 amendment 3 (A-04 / A-06).`
```

</action>
  <verify>
    <automated>cmake --build --preset linux-release 2>&1 | tail -10 ; ctest --preset linux-release 2>&1 | tail -20 ; ls build/dev/src/app/ajazz_app 2>/dev/null && echo "binary exists" || echo "binary missing — preset may differ"</automated>
  </verify>
  <acceptance_criteria>
    - `cmake --build --preset linux-release` exits 0.
    - `ctest --preset linux-release` exits 0 — full suite green.
    - The app binary exists in build/<preset>/ — the link succeeded.
    - Smoke-test outcome documented in SUMMARY.md (whether observed live or deferred to Plan 05-08 integration test).
    - If ASAN was run: no UAF / leak reported on the Sync-button manual exercise.
  </acceptance_criteria>
  <done>
    The full runtime path for time sync is active. Plan 05-08 closes Phase 5 with documentation, devices.yaml updates, and the integration test that exercises the auto-sync hotplug path with synthetic events.
  </done>
</task>

</tasks>

<verification>
- `git show --stat HEAD` lists exactly 2 files: application.hpp, application.cpp.
- `grep -c "qmlRegisterSingletonInstance" src/app/src/application.cpp` reports ≥ 2 (the existing BrandingService registration + the new TimeSyncService registration). If only 1, the existing registration is missing (regression) — escalate.
- The connect to HotplugMonitor::deviceArrived is the FIRST connect to TimeSyncService::onDeviceArrived in the codebase (no duplicates).
- D-01 amendment 3 (A-06) runtime verified: Sync button click succeeds with no ASAN findings; the shared_ptr capture path from Plan 05-04 holds under runtime exercise.
</verification>

\<success_criteria>

- ROADMAP SC3 closed: Settings page auto-sync switch is QSettings-persisted and validated at load (Pitfall 13). 300ms-debounced auto-sync fires after stable arrival; nullptr cast is a quiet no-op (Pitfall 2).
- ROADMAP SC5 closed runtime: TimeSyncService is registered via qmlRegisterSingletonInstance with the static_assert lock from Plan 05-04 still intact (no QML_SINGLETON / no default-construct path).
- TIMESYNC-03 and TIMESYNC-05 fully closed (manual sync surface + auto-sync surface end-to-end).
- D-01 amendment 3 (A-04 + A-06) — shared_ptr capture verified at runtime.

\</success_criteria>

<output>
After completion, create `.planning/phases/05-time-sync-scaffolding/05-07-SUMMARY.md` noting: (1) the smoke-test observations (each of the 7 smoke-test steps with PASS/FAIL/DEFERRED), (2) ASAN result if run (clean / findings / not run), (3) any init-order restructuring needed in Application::Application body, (4) the QML URI/version actually used (should match BrandingService).
</output>
