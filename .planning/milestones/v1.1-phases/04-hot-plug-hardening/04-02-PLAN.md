---
phase: 04-hot-plug-hardening
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/core/include/ajazz/core/hotplug_monitor.hpp
  - src/core/src/hotplug_monitor.cpp
  - src/core/include/ajazz/core/device_registry.hpp
  - src/core/src/device_registry.cpp
  - tests/unit/mock_hid_enumerator.hpp
autonomous: true
requirements: [HOTPLUG-06]
must_haves:
  truths:
    - Tests can synthesise a HotplugEvent into the same pipeline real udev/WM_DEVICECHANGE events use, without spinning up a real OS event source (ARCH-02 injectEvent shim).
    - Tests can construct a DeviceRegistry whose live-presence enumeration returns a synthetic (vid, pid) set rather than walking real hidapi (constructor-injectable HidEnumerator).
    - Production builds (without AJAZZ_TESTING defined) compile zero injectEvent code — the shim is invisible at the ABI.
  artifacts:
    - path: src/core/include/ajazz/core/hotplug_monitor.hpp
      provides: 'void HotplugMonitor::injectEvent(HotplugEvent const&) under #ifdef AJAZZ_TESTING'
      contains: '#ifdef AJAZZ_TESTING'
    - path: src/core/src/hotplug_monitor.cpp
      provides: injectEvent body — synthesises into the same internal Callback pipeline real events use
      contains: snapshotCallback
    - path: src/core/include/ajazz/core/device_registry.hpp
      provides: DeviceRegistry ctor takes optional HidEnumerator; default = real hid_enumerate walker
      contains: HidEnumerator
    - path: tests/unit/mock_hid_enumerator.hpp
      provides: MockHidEnumerator helper — settable synthetic (vid, pid) set + setKeys()
      contains: MockHidEnumerator
  key_links:
    - from: Test code
      to: HotplugMonitor::injectEvent
      via: synthesises through internal Callback emitter
      pattern: injectEvent\(
    - from: DeviceRegistry ctor
      to: HidEnumerator parameter
      via: constructor injection (defaulted to real hidapi walker)
      pattern: HidEnumerator\s*=
---

<objective>
Land the two test seams Phase 4's integration harness needs: (a) `HotplugMonitor::injectEvent(HotplugEvent const&)` shim guarded by `#ifdef AJAZZ_TESTING` per ARCH-02; (b) constructor-injectable `HidEnumerator` parameter on `DeviceRegistry` so tests can drive `MockHidEnumerator` without touching real hidapi. Plus a tiny `MockHidEnumerator` helper header for tests.

Purpose: ARCH-02 calls these the cheapest seams that meet the needs of HOTPLUG-06 (multi-device integration test) and the Windows CI smoke for the `WM_DEVICECHANGE` translation path. The shim is invisible to production builds; the constructor parameter has a default that preserves all existing call sites.

Output: Tests can synthesise hot-plug events end-to-end and assert against a controlled "currently-connected" device set. No production-side behavioural change.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/phases/04-hot-plug-hardening/04-CONTEXT.md
@.planning/phases/03-architectural-decisions/ARCH-02-mock-seam.md
@src/core/include/ajazz/core/hotplug_monitor.hpp
@src/core/src/hotplug_monitor.cpp
@src/core/include/ajazz/core/device_registry.hpp
@src/core/src/device_registry.cpp
@tests/unit/test_action_engine.cpp

<interfaces>
From src/core/include/ajazz/core/hotplug_monitor.hpp:
- struct HotplugEvent { HotplugAction action; uint16_t vid; uint16_t pid; std::string serial; };
- using Callback = std::function<void(HotplugEvent const&)>;
- HotplugMonitor::Callback is replaceable via setCallback(Callback);
- struct Impl is forward-declared public so .cpp helpers can take Impl& and call snapshotCallback().

From src/core/src/hotplug_monitor.cpp:

- struct Impl { Callback cb; ... Callback snapshotCallback() const { lock_guard...; return cb; } };
- Real events flow: per-OS pump -> per-OS helper -> calls `impl.snapshotCallback()(event)` to invoke user Callback.

From tests/unit/test_action_engine.cpp:119 (FakeAsyncExecutor — the precedent ARCH-02 cites):

- A test-local subclass of an Executor interface that queues continuations into a Pending list and exposes drain() — same shape we want for injectEvent (synthesise into the same pipeline real events use).

From src/core/include/ajazz/core/device_registry.hpp current ctor:

- DeviceRegistry() = default; — no parameters.

From src/core/src/device_registry.cpp:71-85:

- enumerateConnectedHidKeys() walks ::hid_enumerate(0, 0) and returns std::set\<std::pair\<uint16_t,uint16_t>>. This is the function tests want to mock.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add HotplugMonitor::injectEvent shim under #ifdef AJAZZ_TESTING</name>
  <files>src/core/include/ajazz/core/hotplug_monitor.hpp, src/core/src/hotplug_monitor.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - .planning/phases/03-architectural-decisions/ARCH-02-mock-seam.md (the LOCKED contract)
    - src/core/include/ajazz/core/hotplug_monitor.hpp (full file — note Impl is forward-declared public at line 104)
    - src/core/src/hotplug_monitor.cpp (line 45-77 Impl struct + snapshotCallback pattern; observe the per-OS dispatch pattern for how real events reach the user Callback)
    - tests/unit/test_action_engine.cpp:119-145 (FakeAsyncExecutor pattern ARCH-02 cites)
  </read_first>
  <action>
    Add to src/core/include/ajazz/core/hotplug_monitor.hpp inside the `class HotplugMonitor { public: ... };` block, AFTER the existing public methods and BEFORE `struct Impl;`, the test-only declaration:
    ```
    #ifdef AJAZZ_TESTING
        /// Test-only hook: synthesise a HotplugEvent into the same internal
        /// dispatch pipeline real udev/WM_DEVICECHANGE/IOKit events use. Per
        /// ARCH-02, this is the cheapest seam that meets HOTPLUG-06 (mock
        /// multi-device integration) and the Windows WM_DEVICECHANGE smoke
        /// test. Production builds (without AJAZZ_TESTING) do not see this
        /// declaration; the shim adds zero ABI surface.
        void injectEvent(HotplugEvent const& ev);
    #endif
    ```

````
Add the corresponding implementation to src/core/src/hotplug_monitor.cpp (anywhere in the `namespace ajazz::core { ... }` block — recommended near the existing `void HotplugMonitor::setCallback(...)` definition), guarded by the same `#ifdef AJAZZ_TESTING`:
```
#ifdef AJAZZ_TESTING
void HotplugMonitor::injectEvent(HotplugEvent const& ev) {
    if (auto cb = p_->snapshotCallback()) { cb(ev); }
}
#endif
```
The synthesised event flows through `p_->snapshotCallback()` — the exact same path real per-OS events take — so all subscribers (Application::onHotplug, future debouncer) see it indistinguishably. No state mutation, no thread hop; the test caller's thread invokes the Callback directly (matches the production contract that the Callback runs on a non-GUI thread).

Wire `AJAZZ_TESTING` into the test-target build only. Edit tests/unit/CMakeLists.txt (or wherever `ajazz_unit_tests` is defined) and add `target_compile_definitions(ajazz_unit_tests PRIVATE AJAZZ_TESTING)`. Do NOT define it in the main `ajazz_core` target — production builds must not see the shim.
````

</action>
  <verify>
    <automated>cmake --build --preset linux-release --target ajazz_core && cmake --build --preset linux-release --target ajazz_unit_tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 "AJAZZ_TESTING" src/core/include/ajazz/core/hotplug_monitor.hpp | grep -q "injectEvent"` exits 0.
    - `grep -A2 "void HotplugMonitor::injectEvent" src/core/src/hotplug_monitor.cpp | grep -q "snapshotCallback"` exits 0.
    - `grep -q "AJAZZ_TESTING" tests/unit/CMakeLists.txt` exits 0.
    - `cmake --build --preset linux-release --target ajazz_core` succeeds — production target builds without seeing `injectEvent` (proves `#ifdef` guard works).
    - `cmake --build --preset linux-release --target ajazz_unit_tests` succeeds — test target sees and links the symbol.
    - `nm $(find build -name 'libajazz_core*' -type f | head -1) 2>/dev/null | grep -c injectEvent` returns 0 — production library does NOT export the shim symbol.
  </acceptance_criteria>
  <done>
    `HotplugMonitor::injectEvent` is declared + defined under `AJAZZ_TESTING`, only the test target sees it, and the synthesised events flow through the same `snapshotCallback()` path as real OS events. Pre-condition for the multi-device integration test (Plan 05) and the Windows WM_DEVICECHANGE smoke (Plan 06).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add constructor-injectable HidEnumerator to DeviceRegistry + MockHidEnumerator helper</name>
  <files>src/core/include/ajazz/core/device_registry.hpp, src/core/src/device_registry.cpp, tests/unit/mock_hid_enumerator.hpp</files>
  <read_first>
    - src/core/include/ajazz/core/device_registry.hpp (ctor at line 47, enumerateConnectedHidKeys at line 98, private members)
    - src/core/src/device_registry.cpp (line 71-85 — enumerateConnectedHidKeys body)
    - .planning/phases/04-hot-plug-hardening/04-CONTEXT.md (Code Insights — "MockHidEnumerator constructor injection on DeviceRegistry")
  </read_first>
  <action>
    Edit src/core/include/ajazz/core/device_registry.hpp:
    - Add the type alias near the top of the class (or in the namespace): `using HidEnumerator = std::function<std::set<std::pair<std::uint16_t, std::uint16_t>>()>;`
    - Replace the defaulted ctor `DeviceRegistry() = default;` with `explicit DeviceRegistry(HidEnumerator enumerator = {});`. The `explicit` keyword preserves the no-arg ergonomics for production callers (`Application` constructs `core::DeviceRegistry m_registry;` as a data member with no args) — the default-constructed `std::function` flags "use the real hidapi walker."
    - Add a private member: `HidEnumerator m_enumerator;` (after m_open_devices from Plan 01).
    - The `static_assert(!std::is_default_constructible_v<DeviceRegistry>, ...)` from Plan 01's QML_SINGLETON sweep does NOT apply here — `DeviceRegistry` is not a QML singleton. Do not add it.

````
Edit src/core/src/device_registry.cpp:
- Add the ctor body: `DeviceRegistry::DeviceRegistry(HidEnumerator enumerator) : m_enumerator(std::move(enumerator)) {}`.
- In `enumerateConnectedHidKeys()` body (currently line 71-85), branch on `m_enumerator`: if it is set, call `return m_enumerator();` directly; otherwise execute the existing real-hidapi walker (the current `hid_enumerate(0, 0)` body becomes the else-branch).

Create a new helper header tests/unit/mock_hid_enumerator.hpp:
```cpp
// SPDX-License-Identifier: GPL-3.0-or-later
/// Test helper: MockHidEnumerator — settable synthetic (vid, pid) set
/// passed into DeviceRegistry's constructor injection point so tests can
/// drive "currently connected" without touching real hidapi.
#pragma once
#include "ajazz/core/device_registry.hpp"
#include <set>
#include <utility>
#include <cstdint>

class MockHidEnumerator {
public:
    void setKeys(std::set<std::pair<std::uint16_t, std::uint16_t>> keys) { m_keys = std::move(keys); }
    ajazz::core::DeviceRegistry::HidEnumerator asEnumerator() {
        return [this]() { return m_keys; };
    }
private:
    std::set<std::pair<std::uint16_t, std::uint16_t>> m_keys;
};
```

Verify all existing call sites of `DeviceRegistry()` (notably `Application` member init and any test that does `core::DeviceRegistry registry;`) still compile with no source change — the ctor change is source-compatible because the new parameter is defaulted.
````

</action>
  <verify>
    <automated>cmake --build --preset linux-release && ctest --preset linux-release -R device_registry 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "using HidEnumerator" src/core/include/ajazz/core/device_registry.hpp` exits 0.
    - `grep -q "explicit DeviceRegistry(HidEnumerator" src/core/include/ajazz/core/device_registry.hpp` exits 0.
    - `grep -q "m_enumerator" src/core/src/device_registry.cpp` exits 0.
    - `test -f tests/unit/mock_hid_enumerator.hpp && grep -q "asEnumerator" tests/unit/mock_hid_enumerator.hpp` exits 0.
    - Existing `test_device_registry.cpp` passes unchanged (zero modifications to the test).
    - `cmake --build --preset linux-release` exits 0 (full project links — no source-incompatibility).
  </acceptance_criteria>
  <done>
    DeviceRegistry has a defaulted HidEnumerator parameter; tests construct `core::DeviceRegistry registry{mock.asEnumerator()};`. MockHidEnumerator helper exists in tests/unit/. All existing tests + production callers compile unchanged.
  </done>
</task>

</tasks>

<verification>
- `cmake --build --preset linux-release && ctest --preset linux-release` — all existing tests still pass.
- `nm` confirms injectEvent absent from production library.
- `git diff --stat HEAD~1` (after commit) shows exactly the 5 files in `files_modified` (+ tests/unit/CMakeLists.txt for the AJAZZ_TESTING define).
</verification>

\<success_criteria>

- ARCH-02 contract satisfied: `injectEvent` is the only mock seam on HotplugMonitor; no virtual call added per real event; production ABI unchanged.
- HidEnumerator constructor injection lets Plan 05's multi-device harness drive synthetic device sets without touching real hidapi.
- Pre-condition for HOTPLUG-06 (multi-device integration test).
  \</success_criteria>

<output>
After completion, create `.planning/phases/04-hot-plug-hardening/04-02-SUMMARY.md` summarising: how the AJAZZ_TESTING wiring landed (target_compile_definitions vs. config header), whether the production library symbol-table check confirmed shim invisibility, and the size of the MockHidEnumerator helper.
</output>
